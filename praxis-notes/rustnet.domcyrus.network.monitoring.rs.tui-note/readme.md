[src/gh]: https://github.com/domcyrus/rustnet.git "(Apache-2.0) (Languages: Rust 96.1%, C 3.0%, Other 0.9%) A cross-platform network monitoring terminal UI tool built with Rust. // 一个用 Rust 构建的跨平台网络监控终端 UI 工具。 /// A cross-platform network monitoring tool built with Rust. RustNet provides real-time visibility into network connections with detailed state information, connection lifecycle management, deep packet inspection, and a terminal user interface. // 一款使用 Rust 构建、跨平台的网络监控工具。RustNet 提供实时网络连接的可视化，包含详细的状态信息、连接生命周期管理、深度包检测以及终端用户界面。"
[app.oci/ghcr]: https://github.com/domcyrus/rustnet/pkgs/container/rustnet "(: docker run --rm -it --cap-add=NET_RAW --cap-add=NET_ADMIN --net=host -- ghcr.io/domcyrus/rustnet -i eth0 # Run with specific interface) (: docker run --rm -it --privileged --net=host -- ghcr.io/domcyrus/rustnet # Alternative: Run with privileged mode (less secure but simpler))"
[app.cargo/crates]: https://crates.io/crates/rustnet-monitor "(: cargo install -- rustnet-monitor) (Apache-2.0) (687 KiB)"
