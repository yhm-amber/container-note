[main.src/gh]: https://github.com/tikv/tikv.git "(Apache-2.0) (Languages: Rust 97.3%, Python 1.8%, Perl 0.7%, Shell 0.1%, Makefile 0.1%, C 0.0%) Distributed transactional key-value database, originally created to complement TiDB // 分布式事务型键值数据库，最初是为了补充 TiDB 而创建的。 /// TiKV is an open-source, distributed, and transactional key-value database. Unlike other traditional NoSQL systems, TiKV not only provides classical key-value APIs, but also transactional APIs with ACID compliance. Built in Rust and powered by Raft, TiKV was originally created by PingCAP to complement TiDB, a distributed HTAP database compatible with the MySQL protocol. // TiKV 是一个开源的分布式事务型键值数据库。与其他传统的 NoSQL 系统不同，TiKV 不仅提供经典的键值 API，还提供符合 ACID 标准的事务型 API。TiKV 使用 Rust 语言编写，并基于 Raft 框架，最初由 PingCAP 创建，旨在与 TiDB （一个兼容 MySQL 协议的分布式 HTAP 数据库）形成互补。 /// The design of TiKV ('Ti' stands for titanium) is inspired by some great distributed systems from Google, such as BigTable, Spanner, and Percolator, and some of the latest achievements in academia in recent years, such as the Raft consensus algorithm. // TiKV（“Ti”代表钛）的设计灵感来源于谷歌的一些优秀的分布式系统，如 BigTable、Spanner 和 Percolator，以及近年来学术界的一些最新成果，如 Raft 共识算法。 /// With the implementation of the Raft consensus algorithm in Rust and consensus state stored in RocksDB, TiKV guarantees data consistency. Placement Driver (PD) (. gh: pingcap/pd.git), which is introduced to implement auto-sharding, enables automatic data migration. The transaction model is similar to Google's Percolator with some performance improvements. TiKV also provides snapshot isolation (SI), snapshot isolation with lock (SQL: SELECT ... FOR UPDATE), and externally consistent reads and writes in distributed transactions. // TiKV 使用 Rust 实现 Raft 共识算法，并将共识状态存储在 RocksDB 中，从而保证了数据的一致性。引入的 Placement Driver (PD) 实现了自动分片，并支持自动数据迁移。其事务模型类似于 Google 的 Percolator，但在性能方面有所改进。TiKV 还提供了快照隔离 (SI)、带锁快照隔离（SQL： SELECT ... FOR UPDATE ）以及分布式事务中的外部一致性读写。 /// TiKV has the following key features: // TiKV 具有以下主要特点： /// - Geo-Replication // 地理复制 ///: TiKV uses Raft and the Placement Driver to support Geo-Replication. // TiKV 使用 Raft 和 Placement Driver 来支持地理复制。 /// - Horizontal scalability // 水平扩展性 ///: With PD and carefully designed Raft groups, TiKV excels in horizontal scalability and can easily scale to 100+ TBs of data. // 凭借 PD 和精心设计的 Raft 组，TiKV 在水平扩展方面表现出色，可以轻松扩展到 100+ TB 的数据。 /// - Consistent distributed transactions // 一致性分布式交易 ///: Similar to Google's Spanner, TiKV supports externally-consistent distributed transactions. // 与谷歌的 Spanner 类似，TiKV 支持外部一致的分布式事务。 /// - Coprocessor support // 协处理器支持 ///: Similar to HBase, TiKV implements a coprocessor framework to support distributed computing. // 与 HBase 类似，TiKV 实现了一个协处理器框架来支持分布式计算。 /// - Cooperates with TiDB // 与 TiDB 合作 ///: Thanks to the internal optimization, TiKV and TiDB can work together to be a compelling database solution with high horizontal scalability, externally-consistent transactions, support for RDBMS, and NoSQL design patterns. // 由于内部优化，TiKV 和 TiDB 可以协同工作，成为一个具有高水平可扩展性、外部一致性事务、支持关系数据库管理系统和 NoSQL 设计模式的强大数据库解决方案。 /// TiKV software stack // TiKV 软件栈 /// - Placement Driver: PD is the cluster manager of TiKV, which periodically checks replication constraints to balance load and data automatically. // Placement Driver： PD 是 TiKV 的集群管理器，它会定期检查复制约束，以自动平衡负载和数据。 /// - Store: There is a RocksDB within each Store and it stores data into the local disk. // 存储： 每个存储中都有一个 RocksDB，它将数据存储到本地磁盘中。 /// - Region: Region is the basic unit of Key-Value data movement. Each Region is replicated to multiple Nodes. These multiple replicas form a Raft group. // 区域： 区域是键值数据传输的基本单元。每个区域都会复制到多个节点。这些多个副本构成一个 Raft 组。 /// - Node: A physical node in the cluster. Within each node, there are one or more Stores. Within each Store, there are many Regions. // 节点： 集群中的一个物理节点。每个节点内包含一个或多个 Store。每个 Store 内包含多个 Region。 /// When a node starts, the metadata of the Node, Store and Region are recorded into PD. The status of each Region and Store is reported to PD regularly. // 当节点启动时，节点、商店和区域的元数据会被记录到 PD 中。每个区域和商店的状态都会定期上报给 PD。"
[driver.src/gh]: https://github.com/tikv/pd.git "(Apache-2.0) (Languages: Go 99.6%, Other 0.4%) Placement driver for TiKV // TiKV 的放置驱动程序 /// PD is the abbreviation for Placement Driver. It manages and schedules TiKV clusters. // PD 是 Placement Driver 的缩写，它负责管理和调度 TiKV 集群。 /// PD supports fault-tolerance by embedding etcd. // PD 通过嵌入 etcd 来支持容错。 /// As a component of the TiKV project, PD needs to run with TiKV to work. The cluster can also include TiDB to provide SQL services. For detailed instructions to deploy a cluster, refer to [Deploy a TiDB Cluster Using TiUP] (. web: docs.pingcap.com/tidb/stable/production-deployment-using-tiup) or [TiDB on Kubernetes Documentation] (. web: docs.pingcap.com/tidb-in-kubernetes/stable). // 作为 TiKV 项目的组件，PD 需要与 TiKV 一起运行才能正常工作。集群还可以包含 TiDB 以提供 SQL 服务。有关部署集群的详细说明，请参阅 “使用 TiUP 部署 TiDB 集群” 或 “TiDB on Kubernetes” 文档。"
[site/org]: https://tikv.org/ "TiKV is a highly scalable, low latency, and easy to use key-value database. // TiKV 是具有高度可扩展性、低延迟和易于使用等优点的键值数据库。 /// TiKV provides both raw and ACID-compliant transactional key-value API, which is widely used in online serving services, such as the metadata storage system for object storage service, the storage system for recommendation systems, the online feature store, etc. // TiKV 提供原始和符合 ACID 规范的事务性键值 API，广泛应用于在线服务，例如对象存储服务的元数据存储系统、推荐系统的存储系统、 在线特征存储等。 /// TiKV is also widely used as the storage layer for database management systems, for example: // TiKV 也被广泛用作数据库管理系统的存储层，例如： /// - TiDB (. gh: pingcap/tidb.git): An open-source MySQL compatible NewSQL database that supports Hybrid Transactional and Analytical Processing (HTAP) workloads. // TiDB ：一个开源的、与 MySQL 兼容的 NewSQL 数据库，支持混合事务和分析处理 (HTAP) 工作负载。 /// - Zetta (. gh: zhihu/zetta.git): An open-source NoSQL database that supports Transaction and Cloud Spanner like API. // Zetta ：一个开源的 NoSQL 数据库，支持事务和类似 Cloud Spanner 的 API。 /// - Tidis (. gh: yongman/tidis.git): a Distributed NoSQL database, providing a Redis protocol API (string, list, hash, set, sorted set), written in Go. // Tidis ：一个分布式 NoSQL 数据库，提供 Redis 协议 API（字符串、列表、哈希、集合、有序集合），用 Go 编写。 /// - Titan (. gh: distributedio/titan.git): A distributed implementation of Redis compatible layer based on TiKV. // Titan ：基于 TiKV 的 Redis 兼容层的分布式实现。 /// - JuiceFS (. gh: juicedata/juicefs.git): An open-source POSIX file system that is based on TiKV and S3. // JuiceFS ：一个基于 TiKV 和 S3 的开源 POSIX 文件系统。 /// Why TiKV // 为什么是 TiKV /// - Low and stable latency // 低且稳定的延迟 ///: RawKV’s average response time less than 1 ms (P99=10 ms). // RawKV 的平均响应时间小于 1 毫秒 (P99=10 毫秒)。 /// - High scalability // 高可扩展性 ///: With the Placement Driver and carefully designed Raft groups, TiKV excels in horizontal scalability and can easily scale to 100+ terabytes of data. Scale-out your TiKV cluster to fit the data size growth without any impact on the application. // 凭借 Placement Driver 和精心设计的 Raft 集群，TiKV 在横向扩展方面表现出色，可轻松扩展到 100 TB 以上的数据量。您可以横向扩展 TiKV 集群以适应数据规模的增长，而不会对应用程序造成任何影响。 /// - Easy to use // 便于使用 ///: Run a single command to deploy a TiKV cluster with everything you need for production environments. Easily scale out or scale in the cluster with TiUP or TiKV operator. // 只需运行一条命令即可部署包含生产环境所需一切的 TiKV 集群。使用 TiUP 或 TiKV Operator 即可轻松横向扩展或缩减集群。 /// - Easy to maintain // 易于维护 ///: TiKV is based on the design of Google Spanner and HBase, but simpler to manage without dependencies on any distributed file system. // TiKV 基于 Google Spanner 和 HBase 的设计，但由于不依赖任何分布式文件系统，因此管理起来更简单。 /// - Consistent distributed transactions // 一致性分布式交易 ///: Similar to Google’s Spanner, TiKV (TxnKV mode) supports externally consistent distributed transactions. // 与 Google 的 Spanner 类似，TiKV（TxnKV 模式）支持外部一致的分布式事务。 /// - Adjustable consistency // 可调节稠度 ///: In RawKV and TxnKV modes, you can customize the balance between consistency and performance. // 在 RawKV 和 TxnKV 模式下，您可以自定义一致性和性能之间的平衡。"

