[src/gh]: https://github.com/simonpcouch/side.git "(MIT) (Languages: R 97.6%, JavaScript 1.7%, CSS 0.7%) [Experimental] A coding agent for RStudio // [实验性] 用于 RStudio 的编码代理 /// side::kick() is a coding agent for data science in RStudio, implemented entirely in R. It can interact with your files, talk to your active R session, and run code. // side::kick() 是一个用于 RStudio 的数据科学编程代理，完全使用 R 实现。它可以与您的文件交互，与您的活动 R 会话对话，并运行代码。 /// Caution // 注意 /// This project is highly experimental. // 这个项目高度实验性。 /// I put this app together to give myself a first pass at implementing several bits of functionality I’d like to see in ellmer/shinychat/btw. This package will not go to CRAN. Ideally, from here, more and more of the functionality prototyped in this app can make its way into ellmer/shinychat/btw, done “the right way,” and ported back into this app. // 我将这个应用程序组合在一起，以便自己初步实现 ellmer/shinychat/btw 中我希望看到的一些功能。这个软件包不会上传到 CRAN。理想情况下，从这里开始，这个应用程序中原型设计的更多功能可以逐步进入 ellmer/shinychat/btw，以“正确的方式”完成，并移植回这个应用程序。 /// side::kick() is intended for use with RStudio’s Sidebar, a full-height pane that’s currently only available in RStudio Dailies. Install RStudio, then search for “Sidebar” in the command palette. // side::kick() 旨在与 RStudio 的侧边栏一起使用，侧边栏是一个全高度的窗格，目前仅在 RStudio Dailies 中可用。安装 RStudio，然后在命令面板中搜索“侧边栏”。 /// side::kick() is implemented with btw, ellmer, and shinychat. // side::kick() 是使用 btw、ellmer 和 shinychat 实现的。"
[knows_by]: https://github.com/simonpcouch/ "- simonpcouch/side: side::kick(), a coding agent for RStudio // simonpcouch/side: side::kick() ，一个用于 RStudio 的编码代理"
