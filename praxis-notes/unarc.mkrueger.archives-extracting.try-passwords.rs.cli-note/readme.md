[src/gh]: https://github.com/mkrueger/unarc-rs.git "(Apache-2.0) (Languages: Assembly 67.1%, Rust 32.9%) Library for handling & extracting arc archives // 用于处理和提取 arc 归档文件的库 /// A Rust library and CLI tool for reading and extracting various archive formats, with a focus on legacy/retro formats from the BBS era, plus modern formats like 7z. // 一个用于读取和提取各种归档格式的 Rust 库和命令行工具，特别关注 BBS 时代的传统/复古格式，以及 7z 等现代格式。 /// A command-line tool for listing and extracting files from various archive formats, with a focus on legacy/retro formats from the BBS era. No other command line clients required. Everything in an all in one package. List/Extract/Retrieve lost passwords - everything possible with unarc. // 一个用于列出和提取各种归档格式文件的命令行工具，特别关注 BBS 时代的传统/复古格式。无需其他命令行客户端。所有功能都包含在一个包中。列出/提取/恢复丢失的密码 - unarc 可以实现所有可能的功能。 /// - List Archive Contents: (: unarc list archive.arj ;: unarc l archive.zip # short alias) /// - Extract Files: (: unarc extract archive.arj ;: unarc x archive.zip -o ./output # extract to specific directory) /// - Show Supported Formats: (: unarc formats) /// - Try Passwords (Password Cracking): (: unarc try-passwords archive.arj -f passwords.txt # Try passwords from a file) (: unarc tp encrypted.7z -d ~/wordlists/ # Try passwords from all .txt files in a directory (recursive)) (: crunch 4 6 abc123 | unarc tp archive.zip --stdin # Try passwords from stdin (pipe from another tool)) (: unarc tp large_archive.rar -f passwords.txt -e small_file.txt # Select a specific small file for faster testing) /// Features: // 特性： /// - Parallel processing using all CPU cores (via rayon) // 使用所有 CPU 核心（通过 rayon）进行并行处理 /// - Multiple input sources: single file, directory of wordlists, or stdin // 多个输入源：单个文件、单词列表目录或标准输入 /// - Entry selection: Use -e to pick a small file for faster password testing // 条目选择：使用 -e 选择一个小文件以加快密码测试 /// - Progress reporting: See passwords/sec and current progress // 进度报告：查看密码/秒和当前进度 /// - Smart verification: Uses CRC32 + size validation to avoid false positives // 智能验证：使用 CRC32 + 大小验证来避免误报 /// Supported formats for password testing: ARC, ARJ, ACE, ZIP, RAR, 7z // 用于密码测试的支持格式：ARC、ARJ、ACE、ZIP、RAR、7z /// Performance varies by format due to encryption complexity: // 由于加密复杂性的不同，性能因格式而异： /// - ARC: ~3.5 million passwords/sec (simple XOR) // ARC：~3.5 百万个密码/秒（简单异或） /// - ARJ: ~2,000 passwords/sec (Garble) // ARJ: ~2,000 个密码/秒（Garble） /// - ZIP: ~50,000 passwords/sec (ZipCrypto) // ZIP: ~50,000 个密码/秒（ZipCrypto） /// - 7z: ~16 passwords/sec (AES-256, by design slow) // 7z: ~16 个密码/秒（AES-256，设计上较慢）"
[lib.cargo/crates]: https://crates.io/crates/unarc-rs "(: cargo add -- unarc-rs) (MIT OR Apache-2.0) (65K SLoC) (7.45 MiB) Universal archive extraction & decompression library for retro + modern formats (7z, ZIP, RAR, LHA/LZH, TAR, ARJ, ARC/PAK, ZOO, HA, UC2, SQ/SQ2, SQZ, HYP, gz, bz2, Z). // 通用归档提取与解压缩库，支持复古及现代格式（7z、ZIP、RAR、LHA/LZH、TAR、ARJ、ARC/PAK、ZOO、HA、UC2、SQ/SQ2、SQZ、HYP、gz、bz2、Z）。 (src: gh:mkrueger/unarc-rs.git)"
[cli.cargo/crates]: https://crates.io/crates/unarc-cli "(: cargo install -- unarc-cli) (MIT OR Apache-2.0) (1,355 SLoC) (26.2 KiB) Universal Archive Extractor - CLI tool for extracting various archive formats // 通用归档提取器 - 用于提取多种归档格式的命令行工具 (src: gh:mkrueger/unarc-rs.git)"

