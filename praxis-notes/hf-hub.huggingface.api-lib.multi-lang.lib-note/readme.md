[lib-rs.src/gh]: https://github.com/huggingface/hf-hub.git '(Apache-2.0) (Languages: Rust 99.5%, Nix 0.5%) Rust client for the huggingface hub aiming for minimal subset of features over `huggingface-hub` python package // 用于 Huggingface Hub 的 Rust 客户端，旨在提供比 `huggingface-hub` Python 包更少的功能。 /// This crates aims to emulate and be compatible with the huggingface_hub python package. // 这个箱子旨在模仿并与……兼容 huggingface_hub python 包。 /// compatible means the Api should reuse the same files skipping downloads if they are already present and whenever this crate downloads or modifies this cache it should be consistent with huggingface_hub // 兼容性意味着 API 应该重用相同的文件，如果文件已存在则跳过下载；并且无论何时此 crate 下载或修改此缓存，都应与 huggingface_hub 保持一致。 /// At this time only a limited subset of the functionality is present, the goal is to add new features over time. We are currently treating this as an internal/external tool, meaning we are currently modifying everything at will for our internal needs. This will eventually stabilize as it matures to accomodate most of our needs. // 目前该工具仅具备部分功能，我们的目标是逐步添加新功能。目前，我们将其视为内部/外部工具，这意味着我们会根据内部需求随时对其进行修改。随着工具的不断完善，最终会稳定下来，满足我们的大部分需求。 /// If you\'re interested in using this, you\'re welcome to do it but be warned about potential changing grounds. // 如果你有兴趣使用此功能，我们欢迎你使用，但请注意，情况可能会发生变化。 /// (: use hf_hub::api::sync::Api ;: let api = Api::new().let host = env::var("HOST").expect("The `HOST` must set in envvar !!") ;: let repo = api.model("zai-org/codegeex4-all-9b-GGUF".to_string()) ;: let _filename = repo.get("config.json").ok_or("Get config.json from repo failed.") ;: // filename: is now the local location within hf cache of the config.json file)'
[lib-rs.cargo/crates]: https://crates.io/crates/hf-hub "(: cargo add -- hf-hub) (Apache-2.0) (2.6K SLoC) (45.4 KiB) This crates aims ease the interaction with web:huggingface.co // 这个 crate 旨在简化与 web:huggingface.co 的交互。 (src: gh:huggingface/hf-hub.git)"
[lib-py.src/gh]: https://github.com/huggingface/huggingface_hub.git "(Apache-2.0) (Languages: Python 99.0%, Other 1.0%) The official Python client for the Hugging Face Hub. // Hugging Face Hub 的官方 Python 客户端。 /// Key features // 主要特点 /// - Download files from the Hub. // 从 Hub 下载文件 。 /// - Upload files to the Hub. // 将文件上传到中心。 /// - Manage your repositories. // 管理您的存储库 。 /// - Run Inference on deployed models. // 对已部署的模型运行推理 。 /// - Search for models, datasets and Spaces. // 搜索模型、数据集和空间。 /// - Share Model Cards to document your models. // 分享模型卡片 ，记录您的模型。 /// - Engage with the community through PRs and comments. // 通过公关稿和评论与社区互动 。"
[lib-py.docs/hf]: https://huggingface.co/docs/huggingface_hub/index "(~ web:hf.co/docs/huggingface_hub) 🤗 Hub client library // 🤗 Hub 客户端库 /// The huggingface_hub library allows you to interact with the Hugging Face Hub, a machine learning platform for creators and collaborators. Discover pre-trained models and datasets for your projects or play with the hundreds of machine learning apps hosted on the Hub. You can also create and share your own models and datasets with the community. The huggingface_hub library provides a simple way to do all these things with Python. // huggingface_hub 库允许您与 Hugging Face Hub 进行交互，Hugging Face Hub 是一个面向创作者和合作者的机器学习平台。您可以找到适用于您项目的预训练模型和数据集，或者体验 Hub 上托管的数百个机器学习应用程序。您还可以创建自己的模型和数据集，并与社区分享。huggingface_hub 库提供了一种使用 Python 轻松完成所有这些操作 huggingface_hub 方法。"
[lib.py.pip/pypi]: https://pypi.org/project/huggingface-hub/ "(: pip install -- huggingface-hub) (License: Apache Software License (Apache)) (Requires: Python >=3.9) (Provides-Extra: [oauth], [torch], [fastai], [hf-xet], [mcp], [testing], [typing], [quality], [all], [dev]) Client library to download and publish models, datasets and other repos on the huggingface.co hub // 客户端库，用于在 huggingface.co 中心下载和发布模型、数据集和其他代码库。 (src: gh:huggingface/huggingface_hub.git)"
[knowsby]: https://huggingface.github.io/candle/inference/hub.html "Using the hub /// Install the hf-hub crate // 安装 hf-hub 软件包 /// ..."

