[src/gh]: https://github.com/flamestro/deff.git "(MIT) (Languages: Rust 99.2%, Shell 0.8%) interactive, side-by-side file review for git diffs with per-file navigation, vertical and horizontal scrolling, syntax highlighting, and added/deleted line tinting // 交互式、并排文件审查，用于 git 差异，支持逐文件导航、垂直和水平滚动、语法高亮以及添加/删除行的着色 /// My name is deff // 我的名字是 deff /// deff is a Rust TUI: interactive, side-by-side file review for git diffs with per-file navigation, vertical and horizontal scrolling, syntax highlighting, and added/deleted line tinting. // deff 是一个 Rust TUI：用于 git 差异的交互式、并排文件审查，支持逐文件导航、垂直和水平滚动、语法高亮以及添加/删除行的着色。 /// Features // 功能 /// - upstream-ahead strategy (default) to compare local branch changes against its upstream // upstream-ahead 策略（默认）用于比较本地分支更改与其上游 /// - range strategy for explicit --base / --head comparison // range 用于显式 --base / --head 比较的策略 /// - Optional --include-uncommitted mode to include working tree and untracked files // 可选的 --include-uncommitted 模式，用于包含工作树和未跟踪文件 /// - Side-by-side panes with independent horizontal scroll offsets // 并排窗格具有独立的水平滚动偏移量 /// - Keyboard and mouse navigation (including wheel + shift-wheel) // 键盘和鼠标导航（包括滚轮+Shift 滚轮） /// - Vim-like motion navigation (h/j/k/l, g/G, Ctrl+u/Ctrl+d) // 类似 Vim 的移动导航（ h / j / k / l ， g / G ， Ctrl+u / Ctrl+d ） /// - In-diff search (/ + Enter, then n / N to navigate matches) // 差分内搜索（ / + Enter，然后 n / N 导航匹配项） /// - Per-file reviewed toggles (r) with local persistence under .git // 逐文件审查切换（ r ）在 .git 下具有本地持久性 /// - Language-aware syntax highlighting and line-level add/delete tinting // 语言感知语法高亮和行级添加/删除着色 /// Prerequisites: // 前提条件： /// - Rust toolchain (cargo) // Rust 工具链 (cargo) /// - git /// - Interactive terminal (TTY) // 交互式终端 (TTY) /// Theme selection: // 主题选择： /// - By default, deff prefers a dark syntax theme (better for black/dark terminals). // 默认情况下， deff 更倾向于使用暗色语法主题（更适合黑色/深色终端）。 /// - Use --theme auto|dark|light to control rendering for your terminal. // 使用 --theme auto|dark|light 来控制终端的渲染。 /// - --theme takes precedence over DEFF_THEME=dark|light. // --theme 优先于 DEFF_THEME=dark|light 。 /// Custom syntax grammars: // 自定义语法语法： /// - deff loads syntect defaults, bundled deff grammars, plus any extra .sublime-syntax files found in: // deff 加载 syntect 默认设置，包含捆绑的 deff 语法规则，以及任何在以下位置找到的额外 .sublime-syntax 文件： /// - - assets/syntaxes (current working directory) // assets/syntaxes (当前工作目录) /// - - .deff/syntaxes (current working directory) // .deff/syntaxes (当前工作目录) /// - Any *.sublime-syntax file added under this repo's assets/syntaxes is auto-bundled at build time. // 任何在当前仓库的 assets/syntaxes 下添加的 *.sublime-syntax 文件都会在构建时自动捆绑。 /// Search and reviewed workflow: // 搜索和审查工作流程： /// - Press / to enter a search query for the current file (searches both panes). // 按 / 进入当前文件的搜索查询（搜索两个面板）。 /// - Press Enter to apply the query, then use n / N to jump matches. // 按 Enter 应用查询，然后使用 n / N 跳转到匹配项。 /// - Press r to mark the current file reviewed/unreviewed. // 按 r 将当前文件标记为已审查/未审查。 /// - Reviewed state is persisted locally in .git/deff/reviewed/ and keyed by comparison scope + file content hash. // 已审阅状态在 .git/deff/reviewed/ 中本地持久化，并通过比较范围+文件内容哈希进行键控。"
[cli.cargo/crates]: https://crates.io/crates/deff "(: cargo install -- deff) (MIT) (2.7K SLoC) (954 KiB) Interactive side-by-side git diff viewer for PR review // 用于 PR 审查的交互式并排 git diff 查看器 (src: gh:flamestro/deff.git)"
