[src/gh]: https://github.com/afshinm/zerobox.git "(Apache-2.0) (Languages: Rust 75.9%, Python 10.6%, TypeScript 8.4%, Shell 4.5%, JavaScript 0.6%) Lightweight, cross-platform process sandboxing powered by OpenAI Codex's runtime. Sandbox any command with file, network, and credential controls. // 基于 OpenAI Codex 运行时的轻量级、跨平台进程沙箱。可对任何命令进行沙箱隔离，并提供文件、网络和凭证控制。 /// 🫙 Zerobox /// Sandbox any command with file, network, and credential controls. // 使用文件、网络和凭据控制对任何命令进行沙盒隔离。 /// Lightweight, cross-platform process sandboxing powered by OpenAI Codex's sandbox runtime (. gh: openai/codex.git). // 由 OpenAI Codex (. gh: openai/codex.git) 的沙箱运行时支持的轻量级、跨平台进程沙箱。 /// - Deny by default: Writes, network, and environment variables are blocked unless you allow them // 默认拒绝： 除非您允许，否则写入、网络和环境变量操作将被阻止。 /// - Credential injection: Pass API keys that the process never sees. Zerobox injects real values only for approved hosts // 凭证注入： 传递进程永远无法看到的 API 密钥。Zerobox 仅向已批准的主机注入真实值。 /// - File access control: Allow or deny reads and writes to specific paths // 文件访问控制： 允许或拒绝对特定路径的读取和写入操作 /// - Network filtering: Allow or deny outbound traffic by domain // 网络过滤： 按域名允许或拒绝出站流量 /// - Clean environment: Only essential env vars (PATH, HOME, etc.) are inherited by default // 干净的环境： 默认情况下只继承必要的环境变量（PATH、HOME 等）。 /// - SDKs for Rust, TypeScript, and Python with a consistent API across languages // 适用于 Rust、TypeScript 和 Python 的 SDK， 跨语言提供一致的 API /// - Cross-platform: macOS and Linux. Windows support planned // 跨平台： 支持 macOS 和 Linux。计划支持 Windows 系统。 /// - Single binary: No Docker, no VMs, ~10ms overhead // 单个二进制文件： 无需 Docker，无需虚拟机，开销约 10 毫秒 /// Quick start // 快速入门 /// - Run a command with no writes and no network access // 运行一个不带写入和网络访问权限的命令 ///: (: zerobox -- node -e \"console.log('hello')\") /// - Allow writes to a specific directory // 允许写入指定目录 ///: (: zerobox --allow-write=. -- node script.js) /// - Allow network to a specific domain // 允许网络访问特定域 ///: (: zerobox --allow-net=api.openai.com -- node agent.js) /// - Pass a secret to a specific host and the inner process never sees the real value // 将密钥传递给特定主机，内部进程将永远无法看到其真正的值 ///: (: zerobox --secret OPENAI_API_KEY=sk-proj-123 --secret-host OPENAI_API_KEY=api.openai.com -- node agent.js) /// - Record filesystem changes and undo them after execution // 记录文件系统更改并在执行后撤销这些更改 ///: (: zerobox --restore --allow-write=. -- npm install ...) /// - Or record without restoring, then inspect and undo later // 或者先录制但不恢复，然后再检查并撤销 ///: (: zerobox --snapshot --allow-write=. -- npm install ...) (: zerobox snapshot list ;: zerobox snapshot diff <session-id> ;: zerobox snapshot restore <session-id>) /// For programmatic usage jump to the SDK that matches your stack: // 如需以编程方式使用，请跳转到与您的技术栈匹配的 SDK： /// - Rust SDK (. gh: afshinm/zerobox.git @main/crates/zerobox) /// - TypeScript SDK (. gh: afshinm/zerobox.git @main/packages/zerobox) /// - Python SDK (. gh: afshinm/zerobox.git @main/sdks/python)"
[cli.cargo/crates]: https://crates.io/crates/zerobox "(: cargo install -- zerobox) (Apache-2.0) (3.7K SLoC) (65.1 KiB) Sandbox any command with file, network, and credential controls. // 使用文件、网络和凭据控制对任何命令进行沙盒隔离。 (src: gh:afshinm/zerobox.git)"
[cli.pip/pypi]: https://pypi.org/project/zerobox/ "(: pip install -- zerobox) (License Expression: Apache-2.0) (Requires: Python >=3.9) Sandbox any command with file, network, and credential controls. // 使用文件、网络和凭据控制对任何命令进行沙盒隔离。 (src: gh:afshinm/zerobox.git)"
[cli.npm/npmjs]: https://npmjs.com/package/zerobox "(: npm i -- zerobox) (Apache-2.0)  (src: gh:afshinm/zerobox.git)"
[flow.svg/.src]: https://raw.githubusercontent.com/afshinm/zerobox/refs/heads/main/packages/zerobox/assets/flow.svg
