[src/gh]: https://github.com/luinbytes/bongocat.git "(MIT) (Languages: Python 99.3%, Other 0.7%) Bongo Cat Desktop Buddy // Bongo Cat 桌面伙伴 /// A desktop pet that slaps its paws whenever you press a key, click a mouse button, or push a controller input. Frameless, always-on-top, draggable. Tracks slap count, builds combos, and unlocks achievements as you use it. // 一款桌面宠物，每次你按下键盘、点击鼠标或按下手柄按键时，它都会拍打爪子。无边框、始终置顶、可拖动。它会记录拍打次数、累积连击数，并在你使用过程中解锁成就。 /// Troubleshooting // 故障排除 /// - Cat ignores keys or mouse. Global hooks need permission: // 猫会忽略键盘或鼠标。 全局钩子需要权限： /// - - Windows: run as Administrator. // Windows 系统：以管理员身份运行。 /// - - macOS: System Settings → Privacy & Security → Accessibility, allow the app. // macOS：系统设置 → 隐私与安全 → 辅助功能，允许该应用。 /// - - Linux: add yourself to the input group. // Linux：将自己添加到 input 组。 /// - Controller not detected. Confirm pygame is installed (pip install pygame), reconnect the pad, relaunch. // 未检测到控制器。 请确认 pygame 已安装（ pip install pygame ），重新连接手柄，重新启动游戏。 /// - No sound. Confirm pygame is installed, sound is enabled in Settings, and sounds/default/ exists with audio files. // 没有声音。 请确认 pygame 已安装，声音已在设置中启用，并且 sounds/default/ 目录下存在音频文件。 /// - pyinstaller not found. Install it (pip install 'pyinstaller>=5.0') or invoke via python -m PyInstaller. // 未找到 pyinstaller 。 请安装它（ pip install 'pyinstaller>=5.0' ）或通过 python -m PyInstaller 调用它。"
