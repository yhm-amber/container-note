[src/gh]: https://github.com/nickolaj-jepsen/fnug.git "(GPL-3.0) (Languages: Rust 56.8%, Python 42.6%, Shell 0.6%) Fnug runs all your lints, tests and commands at once, in the terminal. With git integration and file watching // Fnug 可以一次性在终端中运行您的所有代码检查、测试和命令。支持 git 集成和文件监控。 /// Fnug is a command runner, well actually it's a terminal multiplexer (like tmux), but with a focus on running all your lint and test commands, at once, and displaying the result of those command. // Fnug 是一个命令运行器，实际上它是一个终端复用器（类似于 tmux），但专注于一次性运行所有您的 lint 和测试命令，并显示这些命令的结果。 /// Features // 特性 /// - User-friendly terminal interface, with 100% support for both keyboard and mouse navigation // 用户友好的终端界面，完全支持键盘和鼠标导航 /// - Git integration, automatically select lints and tests that's should be run, based on what files have uncommitted changes // Git 集成，根据哪些文件有未提交的更改，自动选择应运行的 lint 和测试 /// - Track file changes, and selects commands based on the changed files // 跟踪文件变化，并根据变化的文件选择命令 /// - Terminal emulation with scroll back, for those really long error messages // 终端模拟，支持滚动回溯，用于处理那些非常长的错误消息"
[pkg.x/x-cmd]: https://x-cmd.com/install/fnug "(: xd alp -- fnug) (: x install -- fnug)"
[pkg.pip/pypi]: https://pypi.org/project/fnug/ "(: pip install -- fnug) (License: GNU General Public License v3 (GPLv3) (GPL-3.0)) (Requires: Python >=3.10) A nice lint runner (src: gh:nickolaj-jepsen/fnug.git)"
