[src/gh]: https://github.com/PrivateBin/PrivateBin.git "(Zlib/libpng, GPL-2.0, BSD-3-clause, MIT, Apache-2.0, CC-BY) (Languages: PHP 48.3%, JavaScript 34.9%, CSS 16.5%, Other 0.3%) A minimalist, open source online pastebin where the server has zero knowledge of pasted data. Data is encrypted/decrypted in the browser using 256 bits AES. // 一个极简的开源在线粘贴网站，服务器对粘贴的数据一无所知。数据在浏览器中使用 256 位 AES 加密/解密。 /// FAQ /// - Why is it named “PrivateBin”? // 为什么叫“PrivateBin”？ ///: We asked the ZeroBin community when we decided to switch to a new name and chose the most popular name. // 我们决定改名时询问了 ZeroBin 社区的意见 ，并选择了最受欢迎的名称 。 /// - Why have you changed your name? // 你为什么改名了？ ///: PrivateBin started as a fork of ZeroBin. The fork continued development after ZeroBin stopped getting updated. Many new features got added, the general code structure improved and security vulnerabilities addressed. It therefore seemed appropriate to change the forks' name, reflecting these development efforts. // PrivateBin 最初是 ZeroBin 的一个分支。在 ZeroBin 停止更新后，该分支继续进行开发。它添加了许多新功能，改进了整体代码结构，并修复了安全漏洞。因此，更改分支名称以反映这些开发成果似乎很合适。 /// - If I use PrivateBin and post the link in a forum, in social media, etc. is my content secure? // 如果我使用 PrivateBin 并将链接发布到论坛、社交媒体等，我的内容安全吗？ ///: As long as you do not set a password, everyone who gets the link can decrypt the contents of a PrivateBin document! The only way to prevent this is to set a long, secure password when creating a new document. You can find many hints on how to create secure password online, such as the method outlined in this xkcd comic (explanation). // 只要你不设置密码， 任何获得链接的人都可以解密 PrivateBin 文档的内容 ！防止这种情况发生的唯一方法是在创建新文档时设置一个足够长且安全的密码。你可以在网上找到很多关于如何创建安全密码的提示，例如这篇 xkcd 漫画中描述的方法（ 解释 ）。 /// - Which PrivateBin server should I use? // 我应该使用哪个 PrivateBin 服务器？ ///: It is recommended to host your own instance of PrivateBin. You can do this on cheap equipment directly at home, if you are not afraid of setting up a server or can use containers, on a NAS device or similar. Alternatively, find a server that you can trust. Ideally this means you can trust in the server operator. For this a relative or friend may help you or might even set up an instance for you. // 建议您自行托管 PrivateBin 实例。如果您不害怕搭建服务器，或者可以使用容器，也可以在家中使用价格低廉的设备，例如 NAS 设备或类似设备，直接搭建 PrivateBin 实例。或者，找一个你信任的服务器。理想情况下，这意味着你信任服务器运营商。鉴于此，你的亲戚或朋友或许可以帮到你、甚至可以帮你搭建一个服务器实例。"
[site/info]: https://privatebin.info/ "PrivateBin is a minimalist, open source online pastebin where the server has zero knowledge of stored data. // PrivateBin 是一个极简主义的开源在线 pastebin 服务、不过服务器对存储的数据一无所知。 /// Data is encrypted and decrypted in the browser using 256bit AES in Galois Counter mode (. web: en.wikipedia.org/wiki/Galois/Counter_Mode). // 数据在浏览器中通过 256 位 AES 伽罗瓦计数器模式 (. web: en.wikipedia.org/wiki/Galois/Counter_Mode) 进行加密和解密。 /// This is a fork of ZeroBin, originally developed by Sébastien Sauvage. PrivateBin was refactored to allow easier and cleaner extensions and has many additional features. // 这是一个源自 Sébastien Sauvage 开发的 ZeroBin (. gh: sebsauvage/ZeroBin.git) 的分支版本。 PrivateBin 经过重构，以实现更简单、更清晰的扩展，并拥有许多附加功能。 /// What PrivateBin provides // PrivateBin 提供什么 /// - As a server administrator you don't have to worry if your users post content that is considered illegal in your country. You have plausible deniability of any of the pastes content. If requested or enforced, you can delete any paste from your system. // 作为服务器管理员，您无需担心用户发布的内容在您所在的国家/地区被视为非法。您可以对任何粘贴的内容进行否认。如有要求或强制执行，您可以从系统中删除任何粘贴的内容。 /// - Pastebin-like system to store text documents, code samples, etc. // 类似 Pastebin 的系统，用于存储文本文件、代码示例等。 /// - Encryption of data sent to server. // 对发送到服务器的数据进行加密。 /// - Possibility to set a password which is required to read the paste. It further protects a paste and prevents people stumbling upon your paste's link from being able to read it without the password. // 可以设置密码，只有输入密码才能阅读粘贴内容。这样可以进一步保护粘贴内容，防止他人偶然发现你的粘贴内容链接后，在没有密码的情况下阅读其内容。 /// What it doesn't provide // 它不提供的 /// - As a user you have to trust the server administrator not to inject any malicious code. For security, a PrivateBin installation has to be used over HTTPS! Otherwise you would also have to trust your internet provider, and any jurisdiction the traffic passes through. Additionally the instance should be secured by HSTS. It can use traditional certificate authorities and/or use a DNSSEC protected DANE record. // 作为用户，您必须信任服务器管理员不会注入任何恶意代码。为了安全起见，PrivateBin 安装必须通过 HTTPS 进行！否则，你还需要信任你的互联网服务提供商以及任何交通经过的司法管辖区。此外，该实例应受 HSTS (. wiki-en: HTTP_Strict_Transport_Security) 保护。它可以 使用传统证书颁发机构和/或使用受 DNSSEC (. wiki-en: Domain_Name_System_Security_Extensions) 保护的 DANE (. wiki-en: DNS-based_Authentication_of_Named_Entities) 记录。 /// - The “key” used to encrypt the paste is part of the URL (in the fragment part separated by the # (. web: en.wikipedia.org/wiki/URL#fragment)). If you publicly post the URL of a paste that is not password-protected, anyone can read it. Use a password if you want your paste to remain private. In that case, make sure to use a strong password and share it privately and end-to-end-encrypted. // 用于加密粘贴内容的“密钥”是 URL 的一部分（在 以 # 分隔的片段部分 (. web: en.wikipedia.org/wiki/URL#fragment)）。如果您公开分享未设密码保护的粘贴内容的 URL，任何人都可以阅读。如果您希望粘贴内容保持私密，请使用密码。在这种情况下，请确保使用强密码，并以私密且端到端加密的方式分享。 /// - A server admin can be forced to hand over access logs to the authorities. PrivateBin encrypts your text and the discussion contents, but who accessed a paste (first) might still be disclosed via access logs. // 服务器管理员可能被强制要求向有关部门提供访问日志。PrivateBin 会对您的文本和讨论内容进行加密，但谁（首先）访问了粘贴的内容仍有可能通过访问日志泄露出来。 /// - In case of a server breach your data is secure as it is only stored encrypted on the server. However, the server could be abused or the server admin could be legally forced into sending malicious code to their users, which logs the decryption key and sends it to a server when a user accesses a paste. Therefore, do not access any PrivateBin instance if you think it has been compromised. As long as no user accesses this instance with a previously generated URL, the content can't be decrypted. // 即使服务器遭到入侵，您的数据仍然安全，因为它仅以加密形式存储在服务器上。但是，服务器可能被滥用，或者服务器管理员可能被法律强制要求向用户发送恶意代码，该代码会记录解密密钥，并在用户访问粘贴内容时将其发送到服务器。因此，如果您认为某个 PrivateBin 实例已被入侵，请勿访问。只要没有用户使用之前生成的 URL 访问该实例，其内容就无法被解密。 /// Options // 选项 /// Some features are optional and can be enabled or disabled in the configuration file: // 某些功能是可选的，可以在配置文件中启用或禁用： /// - Password protection // 密码保护 /// - Discussions, anonymous or with nicknames and IP based identicons or vizhashes // 讨论，无论是匿名讨论还是使用昵称和基于 IP 的标识符或 vizhashes 进行的讨论 /// - Expiration times, including a \"forever\" and \"burn after reading\" option // 有效期选项，包括“永久有效”和“阅读后销毁”。 /// - Markdown format support for HTML formatted pastes, including preview function // Markdown 格式支持 HTML 格式粘贴，包括预览功能 /// - Syntax highlighting for source code using prettify.js, including 4 prettify themes // 使用 prettify.js 对源代码进行语法高亮显示，包含 4 个 prettify 主题 /// - File upload support, image, media and PDF preview (disabled by default, size limit adjustable) // 支持文件上传、图像、媒体和 PDF 预览（默认禁用，大小限制可调） /// - Templates: By default there are bootstrap5, bootstrap CSS and darkstrap to choose from and it is easy to adapt these to your own websites layout or create your own. // 模板：默认情况下有 bootstrap5、bootstrap CSS 和 darkstrap 可供选择，您可以轻松地将这些模板调整到您自己的网站布局中，或者创建您自己的模板。 /// - Translation system and automatic browser language detection (if enabled in browser) // 翻译系统和浏览器自动语言检测（如果浏览器已启用） /// - Language selection (disabled by default, as it uses a session cookie) // 语言选择（默认禁用，因为它使用会话 cookie） /// - QR code for paste URLs, to easily transfer them over to mobile devices // 用于粘贴网址的二维码，方便将其传输到移动设备。"
[instances.src/gh]: https://github.com/PrivateBin/Directory.git "(Apache-2.0, MIT, Unicode-3.0, ISC, BSD-3-Clause, AGPL-3.0, CC0-1.0, CDLA-Permissive-2.0, Zlib) (Languages: Rust 90.1%, CSS 3.4%, Makefile 3.1%, JavaScript 1.7%, Dockerfile 1.2%, Handlebars 0.5%) Rust based directory application to collect list of federated instances // 基于 Rust 的目录应用程序，用于收集联合实例列表 /// The application is built primarily using the following libraries: // 该应用程序主要使用以下库构建： /// - https://rocket.rs - web framework, including a web server and multi-threaded request handler // https://rocket.rs - Web 框架，包含 Web 服务器和多线程请求处理程序 /// - https://hyper.rs - http client and server // https://hyper.rs - HTTP 客户端和服务器 /// - https://tera.netlify.com - template engine // https://tera.netlify.com - 模板引擎 /// - https://diesel.rs - database ORM and query builder // https://diesel.rs - 数据库 ORM 和查询构建器 /// Configuration // 配置 /// The image supports the use of the following environment variables: // 该镜像支持使用以下环境变量： /// - CRON: (Optional) If set when running the app a cron cycle is performed. Set it to CRON=FULL to run a full cron (once a day). // CRON ：（可选）如果设置此项，则在运行应用程序时执行 cron 周期。将其设置为 CRON=FULL 可运行完整的 cron 任务（每天一次）。 /// - GEOIP_MMDB: path to the GeoIP database, in MaxMind format // GEOIP_MMDB ：GeoIP 数据库的路径，采用 MaxMind 格式 /// - ROCKET_DATABASES: database dict for Diesel SQLite library integration into Rocket // ROCKET_DATABASES ： 数据库字典 将 Diesel SQLite 库集成到 Rocket 中 /// - ROCKET_SECRET_KEY: Needed in production environments, used to protect private cookies, generate this using openssl rand -base64 32 // ROCKET_SECRET_KEY ：生产环境中需要，用于保护私有 cookie，使用 openssl rand -base64 32 生成。 /// olumes // 卷 /// - /var/directory.sqlite: Database file, needs to be writeable // /var/directory.sqlite ：数据库文件，需要可写。 /// - /var/geoip-country.mmdb: GeoIP database, country level is sufficient // /var/geoip-country.mmdb 数据库，国家/地区级别即可。 /// Network ports  网络端口 /// - 8000/tcp: HTTP of web service // 8000/tcp ：Web 服务的 HTTP 端口 /// Usage // 用法 ///: (: make help)"
[instances.wui/.site]: https://privatebin.info/directory/ "Using the instance list programmatically // 以编程方式使用实例列表 ///: The instance list can be retrieved as a JSON encoded data structure by calling the /api endpoint with the HTTP header Accept: application/json set. The list order is randomized every time to spread the load to the instances. // 可以通过调用带有 Accept: application/json 标头的 /api 端点来获取 JSON 编码的数据结构形式的实例列表。列表顺序每次都会随机化，以分散实例的负载。 /// (: curl --header 'Accept: application/json' -- https://privatebin.info/directory/api) /// Additionally, the following optional GET parameters can be sent, to adjust the output: // 此外，还可以发送以下可选的 GET 参数来调整输出： /// - top // 顶部 ///: Number between 1 - 100, defaults to 10. How many instances to return from the top of the list. // 1 到 100 之间的数字，默认为 10。从列表顶部返回的实例数。 /// - attachments // 附件 ///: Boolean (true or false), unset by default. Only return instances that offer attachment upload in their web UI - third party clients can always upload attachments. // 布尔值（真或假），默认未设置。仅返回在其 Web 用户界面中提供附件上传功能的实例——第三方客户端始终可以上传附件。 /// - country // 国家 ///: ISO 3166-1 alpha-2 country code, unset by default. Only return instances of that country. Note the limitations of this type of lookup, as explained above. // ISO 3166-1 alpha-2 国家/地区代码，默认未设置。仅返回该国家/地区的实例。请注意上述此类查找的局限性。 /// - csp_header ///: Boolean (true or false), unset by default. Only return instances that set the currently recommend HTTP Content-Security-Policy (CSP) header (see above). // 布尔值（true 或 false），默认未设置。仅返回设置了当前推荐的 HTTP Content-Security-Policy ( CSP ) 标头的实例（见上文）。 /// - https ///: Boolean (true or false), unset by default. Only return instances that offer HTTPS. // 布尔值（true 或 false），默认未设置。仅返回提供 HTTPS 服务的实例。 /// - https_redirect ///: Boolean (true or false), unset by default. Only return instances that enforce HTTPS, either by having HTTP disabled or redirected to HTTPS. // 布尔值（true 或 false），默认未设置。仅返回强制使用 HTTPS 的实例，这些实例要么禁用了 HTTP，要么重定向到了 HTTPS。 /// - version // 版本 ///: Version prefix, for example “1” or “1.5” or “1.5.1”. Only return instances that start with that version string. // 版本前缀，例如“1”、“1.5”或“1.5.1”。仅返回以该版本字符串开头的实例。 /// - min_uptime // 最小运行时间 ///: Number between 1 - 100, unset by default. Only return instances that have an uptime of that percentage or higher. // 取值范围为 1 到 100 之间的数字，默认未设置。仅返回正常运行时间达到或超过该百分比的实例。 /// - min_rating // 最低评分 ///: Ratings as per Academic grading in the USA (A+, A, A−, B+, B, B−, C+, C, C−, D+, D, D−, F & - (unrated)), unset by default. Only return instances that have that rating or higher. // 评分标准采用美国学术评分体系 （A+、A、A−、B+、B、B−、C+、C、C−、D+、D、D−、F 和 -（未评分）），默认未设置。仅返回评分等于或高于该标准的实例。 /// For example, to retrieve the top 3 instances, randomized: // 例如，要检索前 3 个实例，请随机排序： /// (: curl --header 'Accept: application/json' -- https://privatebin.info/directory/api?top=3)"
[instance:privatebin.net]: https://privatebin.net/ "PrivateBin - 以不知为幸 /// PrivateBin 是一个极简、开源、对粘贴内容毫不知情的在线粘贴板，数据在浏览器内进行 AES-256 加密和解密。"
[instance:anonpaste.org]: https://anonpaste.org/ "AnonPaste — Built on freedom and privacy /// FREE, ENCRYPTED, AND SECURE STORAGE FOR NOTES: WITH OUR END-TO-END ENCRYPTION, YOU CAN SECURELY STORE AND SHARE YOUR MOST SENSITIVE NOTES AND EVEN ATTACH FILES UP TO 10 MB, ALL WITHOUT THIRD-PARTY ACCESS. UNLIKE BIG TECH PLATFORMS, ANONPASTE CANNOT VIEW OR ACCESS YOUR PRIVATE MESSAGES OR FILES. YOUR DATA REMAINS FULLY UNDER YOUR OWNERSHIP AND CONTROL. YOUR PRIVACY IS OUR TOP AND ONLY PRIORITY. // 免费、加密且安全的笔记存储：借助我们的端到端加密技术，您可以安全地存储和分享您最敏感的笔记，甚至可以附加最大 10 MB 的文件，所有这一切都无需第三方访问。与大型科技平台不同，ANONPASTE 无法查看或访问您的私人消息或文件。您的数据完全由您拥有和控制。您的隐私是我们最重要的优先事项。 /// AnonPaste — freedom requires privacy. PRIVACY IS THE BEDROCK OF DEMOCRATIC FREEDOM. It gives you the peace of mind to lead the life you wish and enables society to function free from manipulation. In today’s world, protecting your data is a core part of privacy. YOU CAN REPORT SERVICE ABUSE — anonpaste@protonmail.com. // AnonPaste — 自由需要隐私。隐私是民主自由的基石。它让你能够过上你想要的生活，并使社会能够免受操纵。在当今世界，保护你的数据是隐私的核心部分。你可以举报服务滥用 —— anonpaste@protonmail.com 。"
[instance:bin.oss.zone]: https://bin.oss.zone/ "PrivateBin - 以不知为幸 /// PrivateBin 是一个极简、开源、对粘贴内容毫不知情的在线粘贴板，数据在浏览器内进行 AES-256 加密和解密。"
[instance:enjoys.rocks]: https://enjoys.rocks/ "RockBin - 以不知为幸 /// RockBin 是一个极简、开源、对粘贴内容毫不知情的在线粘贴板，数据在浏览器内进行 AES-256 加密和解密。"
[instance:paste.plus]: https://paste.plus/ "paste.plus - 以不知为幸 /// paste.plus 是一个极简、开源、对粘贴内容毫不知情的在线粘贴板，数据在浏览器内进行 AES-256 加密和解密。 闽ICP备2024064389号-4. (. web: beian.miit.gov.cn)"
[instance:pb.envs.net]: https://pb.envs.net/ "envs.net | PrivateBin - 以不知为幸 /// envs.net | PrivateBin 是一个极简、开源、对粘贴内容毫不知情的在线粘贴板，数据在浏览器内进行 AES-256 加密和解密。"
[instance:bin.disroot.org]: https://bin.disroot.org/ "Disroot Bin - Encrypted pastebin by PrivateBin - 以不知为幸 /// Disroot Bin - Encrypted pastebin by PrivateBin 是一个极简、开源、对粘贴内容毫不知情的在线粘贴板，数据在浏览器内进行 AES-256 加密和解密。 {{ ~ n63ite5off46lfh7qei4uhkvttrgvpve7ag3kwftlqkxo4o5mu7l4cqd.onion ~ }}"
[instance:paste.ononoki.org]: https://paste.ononoki.org/ "ononoki's Pastebin - 以不知为幸 /// ononoki's Pastebin 是一个极简、开源、对粘贴内容毫不知情的在线粘贴板，数据在浏览器内进行 AES-256 加密和解密。"
