[src/gh]: https://github.com/Dawaman43/xsql.git "(MIT) (Languages: Rust 46.9%, Shell 43.7%, Perl 9.4%) Convert SQL schema DDL between MySQL, PostgreSQL, and SQLite using a Rust CLI + interactive TUI, powered by a minimal intermediate representation. // 使用 Rust CLI + 交互式 TUI，通过最小的中间表示，在 MySQL、PostgreSQL 和 SQLite 之间转换 SQL 模式 DDL。 /// xsql /// Convert SQL schema DDL between dialects using a compact intermediate representation. // 使用紧凑的中间表示形式在不同方言之间转换 SQL 模式 DDL。 /// Convert SQL schema DDL between dialects (focused on CREATE TABLE) via a small intermediate representation (IR). // 通过小型中间表示 (IR) 在方言之间转换 SQL 模式 DDL （重点是 CREATE TABLE ）。 /// xsql is a lightweight CLI and TUI that parses, diffs, and emits SQL schema DDL for MySQL, PostgreSQL, and SQLite. It relies on an experimental IR v2 to improve portability, enable round-tripping and linting, and to pave the way for future ALTER-statement generation. // xsql 是一个轻量级的命令行界面 (CLI) 和文本用户界面 (TUI)，用于解析、比较和生成 MySQL、PostgreSQL 和 SQLite 的 SQL 模式 DDL。它依赖于实验性的 IR v2 版本，以提高可移植性，实现往返传输和代码检查，并为未来的 ALTER 语句生成铺平道路。 /// ## What it does (today) // 它（今天）的功能 /// - Parses schema DDL from: // 解析模式 DDL 来自： /// - - MySQL /// - - PostgreSQL /// - - SQLite /// - Emits schema DDL to: // 生成模式 DDL 到： /// - - MySQL /// - - PostgreSQL /// - - SQLite /// - Converts: // 转换： /// - - a single .sql file → another .sql file // 一个 .sql 文件 → 另一个 .sql 文件 /// - - a whole folder tree of .sql files → a destination folder (preserves relative paths) // 包含 .sql 文件的完整文件夹树 → 目标文件夹（保留相对路径） /// - Includes an interactive TUI (xsql tui). // 包含交互式 TUI（ xsql tui ）。 /// Architecture // 建筑学 /// xsql uses a small intermediate representation (IR) to separate parsing from emitting: // xsql 使用小型中间表示 (IR) 将解析与输出分离： /// - Parser crates (in crates/xsql-parser) read dialect-specific SQL into the IR. // 解析器 crates（位于 crates/xsql-parser 中）将特定于方言的 SQL 读取到 IR 中。 /// - The IR crate (crates/xsql-ir) contains a minimal typed model for tables, columns and basic constraints. // IR crate（ crates/xsql-ir ）包含一个最小的类型化模型，用于表、列和基本约束。 /// - Emitter crates (in crates/xsql-emitter) produce dialect-specific SQL from the IR. // 发射器 crates（位于 crates/xsql-emitter 中）从 IR 生成特定于方言的 SQL。 /// This design makes it straightforward to add new dialects and to centralize mapping logic between types and constraints. // 这种设计使得添加新的方言变得简单，并集中管理类型和约束之间的映射逻辑。"

