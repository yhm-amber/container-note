[src/gh]: https://github.com/ccheshirecat/chaser-oxide.git "(~ forked from: gh:mattsse/chromiumoxide.git) (Apache-2.0) (Languages: Rust 100.0%) Undetectable, high-performance browser automation in Rust. Protocol-level stealth for Chromium. // 用 Rust 编写的不可检测、高性能浏览器自动化程序。针对 Chromium 的协议级隐身功能。 /// chaser-oxide // 追赶氧化物 /// A Rust-based fork of chromiumoxide modified for hardened browser automation. // 基于 Rust 的 chromiumoxide 分支，经过修改以增强浏览器自动化。 /// chaser-oxide is an experimental fork of the chromiumoxide library. It incorporates modifications to the core Chrome DevTools Protocol (CDP) client and high-level interaction utilities to reduce the footprint of automated browser sessions. // chaser-oxide 是 chromiumoxide 库的一个实验性分支。它对核心 Chrome DevTools Protocol (CDP) 客户端和高级交互实用程序进行了修改，以减少自动化浏览器会话的资源占用。 /// ## Features // 特征 /// - Protocol-Level Stealth: Patches CDP at the transport layer, not via JavaScript wrappers // 协议层隐蔽式修复 ：在传输层而非通过 JavaScript 封装层修补 CDP。 /// - Fingerprint Profiles: Pre-configured Windows, Linux, macOS profiles with consistent hardware fingerprints // 指纹配置文件 ：预配置的 Windows、Linux 和 macOS 配置文件，具有一致的硬件指纹 /// - Human Interaction Engine: Physics-based mouse movements and realistic typing patterns // 人机交互引擎 ：基于物理的鼠标移动和逼真的打字模式 /// - Request Interception: Built-in request modification and blocking capabilities // 请求拦截 ：内置请求修改和阻止功能 /// - Low Memory Footprint: ~50-100MB vs ~500MB+ for Node.js alternatives // 内存占用低 ：约 50-100MB，而 Node.js 的替代方案则约 500MB 以上 /// ## Core Modifications // 核心修改 /// ### 1. Protocol-Level Stealth // 1. 协议级隐身 /// Standard CDP clients trigger internal browser signals during initialization. chaser-oxide modifies these behaviors: // 标准 CDP 客户端在初始化期间会触发内部浏览器信号。chaser-oxide 会修改这些行为： /// - Runtime.enable Mitigation: Uses Page.createIsolatedWorld to execute scripts in a secondary environment that bypasses detection vectors. // Runtime.enable 缓解 ：使用 Page.createIsolatedWorld 在绕过检测向量的辅助环境中执行脚本。 /// - Utility World Renaming: The default \"Puppeteer\" or \"Chromiumoxide\" utility world names have been neutralized. // 实用世界重命名 ：默认的“Puppeteer”或“Chromiumoxide”实用世界名称已被取消。 /// ### 2. Fingerprint Synchronization // 2. 指纹同步 /// Anti-bot systems look for discrepancies between the reported User-Agent and the browser's execution environment. // 反机器人系统会查找所报告的用户代理与浏览器执行环境之间的差异。 /// - State Management: Injects scripts during document creation to synchronize navigator.platform, WebGL vendor/renderer strings, and hardware concurrency values. // 状态管理 ：在文档创建期间注入脚本，以同步 navigator.platform 、 WebGL 供应商/渲染器字符串和硬件并发值。 /// - Consistency Enforcement: Values are enforced via the IsolatedWorld mechanism to ensure they are available before the target site's scripts execute. // 一致性强制执行 ：通过 IsolatedWorld 机制强制执行值，以确保在目标站点的脚本执行之前这些值可用。 /// ### 3. Human Interaction Simulation // 3. 人机交互模拟 /// - Bezier Mouse Curves: Mouse movements follow randomized Bezier paths with acceleration and deceleration profiles. // 贝塞尔鼠标曲线 ：鼠标移动遵循随机的贝塞尔路径，具有加速和减速曲线。 /// - Typing Physics: Keypresses include variable inter-character delays and optional typo-correction simulation. // 打字物理 ：按键操作包括可变字符间延迟和可选的拼写错误纠正模拟。 /// Dependencies // 依赖关系 /// - chromiumoxide (. gh: mattsse/chromiumoxide.git) - Base CDP client (forked) // 氧化铬 - 基础 CDP 客户端（分支版） /// - tokio (. web: tokio.rs) - Async runtime // tokio - 异步运行时 /// - futures (. crates: futures) - Async utilities // 期货 - 异步工具 /// ## Acknowledgements // 致谢 /// This project is a specialized fork of chromiumoxide. The core CDP client and session management are derived from their excellent work. // 本项目是 chromiumoxide 的一个专门分支。其核心 CDP 客户端和会话管理功能均源自 chromiumoxide 的优秀工作。"

