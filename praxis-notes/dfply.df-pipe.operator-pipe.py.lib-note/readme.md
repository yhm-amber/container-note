[src/gh]: https://github.com/kieferk/dfply.git "(GPL-3.0) (Languages: Python 100.0%) dplyr-style piping operations for pandas dataframes // pandas 数据框的 dplyr 风格管道操作 /// Note: Version 0.3.0 is the first big update in awhile, and changes a lot of the \"base\" code. The pandas-ply package is no longer being imported. I have coded my own version of the \"symbolic\" objects that I was borrowing from pandas-ply. Also, I am no longer supporting Python 2, sorry! // 注意：0.3.0 版本是近期的一次重大更新，对很多“基础”代码进行了修改。 pandas-ply 包不再被导入。我已经编写了自己的“符号”对象版本，之前是从 pandas-ply 借用的。此外，我不再支持 Python 2，抱歉！ /// dfply uses a decorator-based architecture for the piping functionality and to \"categorize\" the types of data manipulation functions. The goal of this // dfply 使用基于装饰器的架构来实现管道功能，并用于“分类”数据处理函数的类型。这个目标 /// architecture is to make dfply concise and easily extensible, simply by chaining together different decorators that each have a distinct effect on the wrapped function. There is a more in-depth overview of the decorators and how dfply can be customized below. // 其架构旨在通过串联具有不同效果的装饰器，使 dfply 简洁且易于扩展。关于装饰器以及如何自定义 dfply 的更深入概述如下。 /// (这个包通过额外提供函数与增加魔术方法为 pandas DF 提供了类似 dplyr 风格的管道运算符与操作函数)"
[pkg/pypi]: https://pypi.org/project/dfply/ "(: pip install -- dfply)"
