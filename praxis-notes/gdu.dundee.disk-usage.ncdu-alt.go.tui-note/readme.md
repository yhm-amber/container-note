[src/gh]: https://github.com/dundee/gdu.git "(MIT) (Languages: Go 97.3%, Makefile 1.5%, Roff 1.1%, Dockerfile 0.1%) Fast disk usage analyzer with console interface written in Go // 快速磁盘使用分析器，具有控制台界面，用 Go 语言编写 /// Gdu is intended primarily for SSD disks where it can fully utilize parallel processing. However HDDs work as well, but the performance gain is not so huge. // Gdu 主要针对 SSD 磁盘设计，可以充分利用并行处理。然而 HDDs 也可以使用，但性能提升并不那么巨大。 /// Alternatives -- dua (: Byron/dua-cli.git) - Tool written in Rust with interface similar to gdu (and ncdu) // (就是说 gdu, dua, ncdu 是不同语言实现的类似的东西)"

[pkg/brew]: https://formulae.brew.sh/formula/gdu "(: brew install -- gdu)"

[knowsby]: https://dev.yorhel.nl/ncdu "Similar projects -- There’s no shortage of alternatives to ncdu nowadays. In no particular order: gdu - TUI/CLI, Go, supports ncdu JSON export and import."
