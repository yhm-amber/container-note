[src/gh]: https://github.com/wahani/modules.git "(YEAR: 2016-2023 - COPYRIGHT HOLDER: Sebastian Warnholz) (Languages: R 100.0%) Modules in R // R 中的模块 /// (: mdu <- modules::module({ foo <- \ () ... }) ;: mdu$foo() # create module by code as list) (: mdu <- modules::use('mdu.R') ;: mdu$foo() # create module by R file as a list) /// modules are isolated from the `.GlobalEnv` so the following objects in `.GlobalEnv` can not be found in inner module scope: // 模块与 `.GlobalEnv` 隔离，因此 `.GlobalEnv` 中的以下对象在内部模块作用域中无法找到"
[package/cran]: https://cran.r-project.org/web/packages/modules/ "(Version: 	0.13.0) (Depends: 	R (≥ 3.2.0)) (Imports: 	utils) (Suggests: 	testthat, devtools, knitr, lintr, rmarkdown, parallel) (Published: 	2024-01-21) (DOI: 	10.32614/CRAN.package.modules) (Author: 	Sebastian Warnholz [aut, cre]) (Maintainer: 	Sebastian Warnholz <wahani at gmail.com>) (License: 	MIT + file LICENSE) (NeedsCompilation: 	no) modules: Self Contained Units of Source Code // 模块：自包含的源代码单元 /// Provides modules as an organizational unit for source code. Modules enforce to be more rigorous when defining dependencies and have a local search path. They can be used as a sub unit within packages or in scripts. // 将模块作为源代码的组织单元。模块在定义依赖关系时要求更严格，并具有本地搜索路径。它们可以用作包内的子单元或在脚本中使用。 (src: gh:wahani/modules.git)"
