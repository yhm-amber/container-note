[src/gh]: https://github.com/cross-rs/cross.git "(Apache-2.0, MIT) (Languages: Rust 59.0%, Shell 19.9%, Dockerfile 12.7%, Python 7.3%, Other 1.1%) “Zero setup” cross compilation and “cross testing” of Rust crates // “零配置”跨编译和 Rust 包的“跨测试” /// This project is developed and maintained by the cross-rs team. It was previously maintained by the Rust Embedded Working Group Tools team. New contributors are welcome! Please join our Matrix room and say hi. // 该项目由 cross-rs 团队开发和维护。它之前由 Rust 嵌入式工作组工具团队维护。欢迎新贡献者！请加入我们的 Matrix 房间并打招呼。 /// Features // 特性 /// - cross will provide all the ingredients needed for cross compilation without touching your system installation. // cross 将提供所有用于交叉编译的组件，而无需触及您的系统安装。 /// - cross provides an environment, cross toolchain and cross compiled libraries, that produces the most portable binaries. // cross 提供了一个环境、交叉工具链和交叉编译的库，以生成最可移植的二进制文件。 /// - “cross testing”, cross can test crates for architectures other than i686 and x86_64. // “交叉测试”， cross 可以测试除 i686 和 x86_64 之外架构的 crates。 /// - The stable, beta and nightly channels are supported. // 支持稳定版、测试版和夜间版频道。 /// Dependencies // 依赖项 /// - rustup /// - A Linux kernel with binfmt_misc support is required for cross testing. // 进行交叉测试需要一个支持 binfmt_misc 的 Linux 内核。 /// One of these container engines is required. If both are installed, cross will default to docker. // 需要其中一个容器引擎。如果两者都安装了， cross 将默认为 docker 。 /// - Docker. Note that on Linux non-sudo users need to be in the docker group or use rootless docker. Read the container engine install guide for the required installation and post-installation steps. Requires version 20.10 (API 1.40) or later. // Docker。请注意，在 Linux 上，非 sudo 用户需要属于 docker 组或使用 rootless docker。请阅读容器引擎安装指南以了解所需的安装和安装后步骤。需要版本 20.10 (API 1.40) 或更高版本。 /// - Podman. Requires version 3.4.0 or later. // Podman。需要版本 3.4.0 或更高版本。"
[crates]: https://crates.io/crates/cross "(: cargo install -- cross) (MIT OR Apache-2.0) (224 KiB)"
