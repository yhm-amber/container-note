[src/gh]: https://github.com/h2oai/db-benchmark.git "(MPL-2.0) (Languages: R 49.1%, Python 39.2%, Shell 7.7%, Julia 3.9%, HTML 0.1%) reproducible benchmark of database-like ops // 可重复的类数据库操作基准测试"
[show.web/ghio]: https://h2oai.github.io/db-benchmark/

[knowsby]: https://github.com/elixir-explorer/explorer "The aim here isn't to have the fastest dataframe library around (though it certainly helps that [we're building on Polars, one of the fastest](https://h2oai.github.io/db-benchmark/)). Instead, we're aiming to bridge the best of many worlds: the elegance of dplyr; the speed of polars; the joy of Elixir. That means you can expect the guiding principles to be 'Elixir-ish'. For example, you won't see the underlying data mutated, even if that's the most efficient implementation. Explorer functions will always return a new dataframe or series. // 我们的目标并不是拥有最快的数据帧库（尽管它肯定是，这得益于我们在 Polars 这个最快的库之一上构建数据帧库）。相反，我们的目标是在多个世界中架起一座最好的桥梁: 来自 dplyr 的优雅; 来自 Polars 的速度; 来自 Elixir 的喜悦。这意味着你可以期待 \"Elixir 风格\" 的指导原则。例如，在底层你不会看到数据变异，即使这是最高效实现的方案。 Explorer 函数将始终返回一个新的数据帧或序列。"
