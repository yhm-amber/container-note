[src/gh]: https://github.com/openscad/openscad.git "(GPL-2.0) (Languages: C++ 64.0%, C 22.4%, OpenSCAD 4.3%, CMake 3.0%, Shell 2.7%, Python 1.2%, Other 2.4%) OpenSCAD - The Programmers Solid 3D CAD Modeller // OpenSCAD - 程序员的实体 3D CAD 建模器 /// OpenSCAD is a software for creating solid 3D CAD objects. It is free software and available for Linux/UNIX, MS Windows and macOS. // OpenSCAD 是一款用于创建实体 3D CAD 对象的软件。它是一款免费软件，可在 Linux/UNIX、MS Windows 和 macOS 上使用。 /// Unlike most free software for creating 3D models (such as the famous application Blender), OpenSCAD focuses on the CAD aspects rather than the artistic aspects of 3D modeling. Thus this might be the application you are looking for when you are planning to create 3D models of machine parts but probably not the tool for creating computer-animated movies. // 与大多数用于创建 3D 模型的免费软件（例如著名的应用程序 Blender）不同，OpenSCAD 更侧重于 CAD 方面，而不是 3D 建模的艺术方面。因此，当您计划创建机器零件的 3D 模型时，这可能就是您要找的应用程序，但可能不是用于创建计算机动画电影的工具。 /// OpenSCAD is not an interactive modeler. Instead it is more like a 3D-compiler that reads a script file that describes the object and renders the 3D model from this script file (see examples below). This gives you, the designer, complete control over the modeling process and enables you to easily change any step in the modeling process or make designs that are defined by configurable parameters. // OpenSCAD 不是一个交互式建模器。相反，它更像是一个 3D 编译器，读取一个描述对象的脚本文件，并从这个脚本文件中渲染 3D 模型（见下文示例）。这让你，作为设计师，对建模过程有完全的控制权，并使你能够轻松地更改建模过程中的任何步骤，或者制作由可配置参数定义的设计。 /// OpenSCAD provides two main modeling techniques: First there is constructive solid geometry (aka CSG) and second there is extrusion of 2D outlines. As the data exchange format for these 2D outlines Autocad DXF files are used. In addition to 2D paths for extrusion it is also possible to read design parameters from DXF files. Besides DXF files OpenSCAD can read and create 3D models in the STL and OFF file formats. // OpenSCAD 提供了两种主要的建模技术：首先是构造实体几何（简称 CSG），其次是二维轮廓的拉伸。作为这些二维轮廓的数据交换格式，使用 AutoCAD DXF 文件。除了用于拉伸的二维路径外，还可以从 DXF 文件中读取设计参数。除了 DXF 文件，OpenSCAD 还可以读取和创建 STL 和 OFF 文件格式的三维模型。"
[site/org]: https://openscad.org/ "OpenSCAD - The Programmers Solid 3D CAD Modeller // 程序员的实体 3D CAD 建模器"
[tur/wikibooks.org]: https://en.wikibooks.org/wiki/OpenSCAD_Tutorial "OpenSCAD Tutorial"
[playground.wui/ochafik.com]: https://ochafik.com/openscad2/ "OpenSCAD Playground - Open the Demo"
[playground.src/gh]: https://github.com/openscad/openscad-playground.git "(GPL-2.0 OR LATER, GPL-3.0, MIT) (Languages: TypeScript 85.3%, JavaScript 11.5%, HTML 2.4%, Other 0.8%) OpenSCAD Web Playground // OpenSCAD 网页游乐场 /// This is a limited port of OpenSCAD to WebAssembly, using at its core a headless WASM build of OpenSCAD (done by @DSchroer), wrapped in a UI made of pretty PrimeReact components, a React Monaco editor (VS Codesque power!), and an interactive model-viewer renderer. // 这是 OpenSCAD 到 WebAssembly 的一个有限移植版本，其核心是 OpenSCAD 的无头 WASM 构建版本（ 由 @DSchroer 完成 (. gh: DSchroer/openscad-wasm.git) ），并封装在一个由漂亮的 PrimeReact (. gh: primefaces/primereact.git) 组件组成的 UI、一个 React Monaco 编辑器 (. gh: react-monaco-editor/react-monaco-editor.git) （具有类似 VS Code 的功能！）和一个交互式模型查看器渲染器 (. web: modelviewer.dev) 中。 /// It defaults to the Manifold backend so it's super fast. // 默认情况下，它的速度非常快。 /// Features // 特征 /// - Automatic preview on edit (F5), and full rendering on Ctrl+Enter (or F6). Using a trick to force $preview=true. // 编辑时自动预览（F5），按 Ctrl+Enter（或 F6）进行完整渲染。使用技巧强制 $preview=true。 /// - Customizer support // 定制器支持 /// - Syntax highlighting // 语法高亮 /// - Ships with many standard SCAD libraries (can browse through them in the UI) // 内置多种标准 SCAD 库（可在用户界面中浏览）。 /// - Autocomplete of imports // 导入自动完成 /// - Autocomplete of symbols / function calls (pseudo-parses file and its transitive imports) // 符号/函数调用自动补全（伪解析文件及其传递导入） /// - Responsive layout. On small screens editor and viewer are stacked onto each other, while on larger screens they can be side-by-side // 响应式布局。在小屏幕上，编辑器和查看器上下堆叠显示；而在大屏幕上，它们可以并排显示。 /// - Installable as a PWA (then persists edits in localStorage instead of the hash fragment). On iOS just open the sharing panel and tap \"Add to Home Screen\". Should not require any internet connectivity once cached. // 可作为 PWA 安装（这样会将编辑内容保存到 localStorage 而非哈希片段中）。在 iOS 上，只需打开分享面板并点击“添加到主屏幕”即可。缓存后无需网络连接。"
[wasm-port.src/gh]: https://github.com/openscad/openscad-wasm.git "(GPL-2.0) (Languages: TypeScript 41.5%, Makefile 36.2%, JavaScript 9.4%, OpenSCAD 5.5%, HTML 3.7%, Dockerfile 3.7%) Webassembly port of OpenSCAD // OpenSCAD 的 WebAssembly 移植版 /// A full port of OpenSCAD to WASM. // 将 OpenSCAD 完全移植到 WASM。 /// This project cross compiles all of the project dependencies and created a headless OpenSCAD WASM module. // 该项目对所有项目依赖项进行交叉编译，并创建了一个无头 OpenSCAD WASM 模块。"
[liberachat]: https://web.libera.chat/?channel=#openscad
[bluesky]: https://bsky.app/profile/openscad.bsky.social
[mastdon]: https://floss.social/@OpenSCAD
