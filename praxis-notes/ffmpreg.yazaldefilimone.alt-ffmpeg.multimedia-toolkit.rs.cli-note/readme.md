[src/gh]: https://github.com/yazaldefilimone/ffmpreg.git "(Apache-2.0) (Languages: Rust 100.0%) In-development universal safe multimedia toolkit. // 正在开发中的通用安全多媒体工具包。 /// ffmpreg is a rust-native multimedia toolkit that decodes, transforms, and encodes audio and video without requiring FFmpeg. It provides both a command-line interface and a library API, allowing you to process media files directly from the terminal or integrate media processing into Rust applications. The project aims to deliver a safe, deterministic, and modular media processing engine with a focus on explicit pipelines and developer control. // ffmpreg 是一个 Rust 原生多媒体工具包，无需 FFmpeg 即可解码、转换和编码音频和视频。它提供命令行界面和库 API，允许您直接从终端处理媒体文件，或将媒体处理集成到 Rust 应用程序中。该项目旨在提供一个安全、确定性且模块化的媒体处理引擎，重点在于显式管道和开发者控制。 /// ### Core Concepts // 核心概念 /// Media processing revolves around two main data types: Packet and Frame. A Packet represents encoded data in a container, while a Frame represents decoded data ready for processing or playback. Data flows predictably: readers demux containers into packets, decoders convert packets into frames, transforms modify frames, encoders convert frames back into packets, and writers mux packets into output files. // 媒体处理主要围绕两种数据类型展开： Packet 和 Frame 。 Packet 代表容器中编码的数据，而 Frame 代表解码后的数据，可供处理或播放。数据流按预期进行：读取器将容器解复用成数据包，解码器将数据包转换为帧，变换器修改帧，编码器将帧转换回数据包，写入器将数据包复用成输出文件。 /// ## Transforms // 转换 /// Transforms modify frames in the pipeline. Built-in transforms include Gain for multiplying sample amplitudes and Normalize for scaling samples to a target peak. // 变换用于修改流水线中的帧。内置变换包括 Gain （用于乘以样本振幅）和 Normalize 用于将样本缩放到目标峰值）。 /// ## Formats // 格式 /// - WAV: uncompressed audio, suitable for lossless pipelines. // WAV ：未压缩音频，适用于无损传输管道。 /// - Y4M: raw video frames with a text-based header. // Y4M ：带有文本头的原始视频帧。 /// Codecs: // 编解码器： /// - PCM: uncompressed 16-bit audio, multi-channel support. // PCM ：无压缩 16 位音频，支持多声道。 /// - ADPCM: adaptive differential audio compression. // ADPCM ：自适应差分音频压缩。 /// - Raw video: passes through YUV frame data unchanged. // 原始视频 ：直接以 YUV 帧数据形式传输。 /// ## CLI Reference // CLI 参考 /// - -i: input file or glob pattern. // -i ：输入文件或 glob 模式。 /// - -o: output file or directory. // -o ：输出文件或目录。 /// - --show: inspection mode, prints frame metadata. // --show ：检查模式，打印帧元数据。 /// - --apply: add transform to pipeline (multiple allowed). // --apply ：将转换添加到管道（允许多个）。 /// - --codec: select output codec (default matches input). // --codec ：选择输出编解码器（默认与输入匹配）。 /// ## Goals // 目标 /// ffmpreg aims to be a safe, reliable, and maintainable Rust-native alternative to FFmpeg. The project emphasizes deterministic and explicit pipelines, clear developer control, and minimal runtime surprises. All development prioritizes safety by avoiding unsafe code wherever possible, minimizing unwrap usage, reducing panics, and keeping external dependencies to a minimum. The goal is to provide a high-performance media processing engine that is easy to understand, integrate, and extend while maintaining strong Rust safety guarantees. // ffmpreg 旨在成为一个安全、可靠且易于维护的 Rust 原生 FFmpeg 替代方案。该项目强调确定性和明确的流水线、清晰的开发者控制以及尽可能减少运行时意外情况。所有开发工作都以安全性为首要考量，尽可能避免编写不安全代码，最大限度地减少 unwrap 的使用，减少 panic，并将外部依赖项降至最低。其目标是提供一个高性能的媒体处理引擎，该引擎易于理解、集成和扩展，同时保持强大的 Rust 安全性保证。"
[cli.cargo/crates]: https://crates.io/crates/ffmpreg "(: cargo install -- ffmpreg) (Apache-2.0) (407 SLoC) (8.99 KiB) An experimental general-purpose multimedia toolkit. // 一个实验性的通用多媒体工具包。 (src: gh:yazaldefilimone/ffmpreg.git)"

