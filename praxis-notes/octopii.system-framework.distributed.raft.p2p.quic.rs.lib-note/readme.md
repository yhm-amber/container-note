[src/gh]: https://github.com/octopii-rs/octopii.git "(Apache-2.0) (Languages: Rust 98.5%, Other 1.5%) 🦑 framework for building distributed systems // 🦑 用于构建分布式系统的框架 /// Octopii: A batteries-included framework for building distributed systems // Octopii：一个用于构建分布式系统的全功能框架 /// What's Included // 包含内容 /// Octopii provides everything you need to build replicated, fault-tolerant systems: // Octopii 提供了构建复制、容错系统所需的一切： /// - Raft Consensus - Leader election, log replication, and membership changes via OpenRaft // Raft 共识 - 通过 OpenRaft 实现领导者选举、日志复制和成员变更 /// - QUIC Transport - Fast, encrypted, multiplexed networking with Quinn // QUIC 传输 - 使用 Quinn 实现快速、加密、多路复用的网络 /// - Durable Persistence - Crash-resistant Write-Ahead Log (Walrus) with checksums // 持久化存储 - 具备抗崩溃能力的预写式日志 (Walrus) 及校验和 /// - Pluggable State Machines - Bring your own replication logic (KV stores, counters, registries, etc.) // 可插拔状态机 - 支持自定义复制逻辑（键值存储、计数器、注册表等） /// - Large File Transfers - P2P streaming with automatic checksum verification (Shipping Lane) // 大文件传输 - 基于 P2P 流式传输并自动进行校验和验证 (Shipping Lane) /// - RPC Framework - Request/response messaging with correlation and timeouts // RPC 框架 - 带有相关性和超时的请求/响应消息 /// - Runtime Management - Flexible async execution with Tokio // 运行时管理 - 使用 Tokio 的灵活异步执行 /// Building Your Distributed System // 构建您的分布式系统 /// Octopii makes it easy to replicate custom application logic: // Octopii 可以轻松复制自定义应用逻辑： /// - Implement your state machine - Define how commands are applied to your application state // 实现你的状态机 - 定义命令如何应用于你的应用状态 /// - Configure your cluster - Specify node IDs, addresses, and persistence settings // 配置你的集群 - 指定节点 ID、地址和持久化设置 /// - Start your nodes - Octopii handles leader election, replication, and failover // 启动你的节点 - Octopii 处理领导者选举、复制和故障转移 /// - Use the API - Propose writes, query state, manage membership // 使用 API - 提出写入建议，查询状态，管理成员资格 /// Examples // 示例 /// Working examples are available in the examples/ directory: // 可工作的示例位于 examples/ 目录中： /// - examples/node/ - Three-node cluster setup // examples/node/ - 三节点集群设置 /// - examples/custom_state_machine/ - Custom state machine implementation // examples/custom_state_machine/ - 自定义状态机实现 /// - examples/shipping_lane/ - Large file transfers // examples/shipping_lane/ - 大文件传输 /// - examples/rpc/ - Custom RPC messaging // examples/rpc/ - 自定义 RPC 消息传递 /// - examples/wal/ - Direct WAL usage // examples/wal/ - 直接使用 WAL /// Run an example: // 运行示例： /// (: cargo run --example node --features openraft) /// Why Octopii? // 为什么选择 Octopii？ ///: Most distributed systems require assembling multiple components: a consensus library, network transport, persistence layer, RPC framework, and data transfer mechanisms. Octopii provides all of these integrated and tested together, so you can focus on your application logic instead of infrastructure plumbing. // 大多数分布式系统需要组合多个组件：一个共识库、网络传输、持久化层、RPC 框架和数据传输机制。Octopii 将这些组件集成并一起测试，让你可以专注于应用逻辑，而不是基础设施的管道。"
