[src/gh]: https://github.com/rossellhayes/incase.git "(MIT) (Languages: R 100.0%) Pipe-Friendly Vector Replacement with Case Statements // 使用 case 语句进行管道友好的向量替换 /// - Pipe-friendly conditionals // 适合管道操作的条件语句 ///: incase’s `in_case()` and `if_case()` accept a vector as their first input, allowing you to take full advantage of magrittr’s `.` // incase 的 `in_case()` 和 `if_case()` 接受一个向量作为它们的第一个输入，使您能够充分利用 magrittr 的 `.` /// - Automatic type conversion // 自动类型转换 ///: incase functions automatically coerce types. This is especially useful when dealing with integers or `NA`s. With incase, you no longer have to worry about specifying the type of your `NA`s or adding `L` to your integers. // incase 函数会自动转换类型。这在处理整数或 `NA` 时特别有用。使用 incase，您不再需要担心指定 `NA` 的类型或将 `L` 添加到您的整数中。 /// - Easy default values // 易用的默认值 ///: `in_case()` adds `.preserve` and `.default` arguments as a more intuitive alternative to `TRUE ~ ...`. // `in_case()` 添加 `.preserve` 和 `.default` 参数作为 `TRUE ~ ...` 的更直观替代方案。 /// - Simplified interface for recoding // 简洁的重新编码界面 ///: `switch_case()` works as a convenient shorthand for `in_case()` when recoding discrete values. `grep_case()` allows you to recode values with pattern matching. // `switch_case()` 在重编码离散值时，可作为 `in_case()` 的便捷简写。 `grep_case()` 允许您使用模式匹配重新编码值。 /// - Easily recode to (ordered) factor // 轻松重编码为（有序）因子 ///: When you need an ordered factor, the `*_fct()` family of functions lets you save a step by using the order of your cases as the order of your factor levels. Use `.ordered = TRUE` to create an ordered factor and `.ordered = FALSE` to make a regular-old factor. // 当你需要一个有序因子时， `*_fct()` 系列的函数可以通过使用你的 case 顺序作为因子水平的顺序来节省一步。使用 `.ordered = TRUE` 来创建一个有序因子，使用 `.ordered = FALSE` 来创建一个普通的因子。"
[site/com]: https://pkg.rossellhayes.com/incase/ "incase provides a more pipe-friendly alternative to dplyr’s case_when() and if_else(). // incase 提供了一种比 dplyr 的 case_when() 和 if_else() 更适合管道操作的替代方案。"
[package/cran]: https://cran.r-project.org/web/packages/incase/ "(Version:	0.4.0) (Imports:	backports, cli, glue, lifecycle, magrittr, plu, rlang) (Suggests:	dplyr, testthat (≥ 3.0.0), tibble) (Published:	2025-07-24) (DOI:	10.32614/CRAN.package.incase) (Author:	Alexander Rossell Hayes ORCID iD [aut, cre, cph], Patrice Kiener ORCID iD [ctb] (Contributed example for fn_case())) (Maintainer:	Alexander Rossell Hayes <alexander at rossellhayes.com>) (License:	MIT + file LICENSE) (NeedsCompilation:	no) incase: Pipe-Friendly Vector Replacement with Case Statements // incase：支持管道操作的向量替换，使用情况语句 /// Offers a pipe-friendly alternative to the 'dplyr' functions case_when() and if_else(), as well as a number of user-friendly simplifications for common use cases. These functions accept a vector as an optional first argument, allowing conditional statements to be built using the 'magrittr' dot operator. The functions also coerce all outputs to the same type, meaning you no longer have to worry about using specific typed variants of NA or explicitly declaring integer outputs, and evaluate outputs somewhat lazily, so you don't waste time on long operations that won't be used. // 提供了一种支持管道操作的'dplyr'函数`case_when()`和`if_else()`的替代方案，并为常见的使用场景提供了一些用户友好的简化。这些函数接受一个向量作为可选的第一个参数，允许使用'magrittr'点操作符构建条件语句。这些函数还将所有输出强制转换为相同类型，这意味着你不再需要担心使用特定类型的 NA 变体或显式声明整数输出，并且输出在一定程度上是惰性求值的，因此你不会浪费时间在那些不会被使用的长操作上。 (src: gh:rossellhayes/incase.git)"
