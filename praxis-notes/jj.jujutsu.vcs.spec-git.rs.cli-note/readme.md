[jj-cli.src/gh]: https://github.com/jj-vcs/jj.git "(Apache-2.0) (Languages: Rust 99.9%, Nix 0.1%) Jujutsu—a version control system // 柔术 —— 版本控制系统 /// Introduction // 介绍 /// Jujutsu is a powerful version control system (. web: en.wikipedia.org/wiki/Version_control) for software projects. You use it to get a copy of your code, track changes to the code, and finally publish those changes for others to see and use. It is designed from the ground up to be easy to use—whether you're new or experienced, working on brand new projects alone, or large scale software projects with large histories and teams. // 柔术是一种强大的版本控制系统 用于软件项目。你可以用它来获取代码副本，跟踪变化 最后将这些更改发布给他人查看和使用。 它从零开始设计，便于使用——无论你是新手还是 有经验、独自进行全新项目，或是大规模软件 拥有丰富历史和团队的项目。 /// Jujutsu is unlike most other systems, because internally it abstracts the user interface and version control algorithms from the storage systems used to serve your content. This allows it to serve as a VCS with many possible physical backends, that may have their own data or networking models—like Mercurial (. web: mercurial-scm.org) or Breezy (. web: breezy-vcs.org, gh: breezy-team/breezy.git), or hybrid systems like Google's cloud-based design, Piper/CitC (. web: youtu.be/W71BTkUbdqE?t=645). // 柔术不同于大多数其他系统，因为它内部将用户界面和版本控制算法从用于提供内容的存储系统中抽象出来。这使得它能够作为一个 VCS，拥有多种可能的物理后端，这些后端可能拥有自己的数据或网络模型——如 Mercurial 或 Breezy，或者像谷歌云端设计的 Piper/CitC 那样的混合系统。 /// Today, we use Git repositories as a storage layer to serve and track content, making it compatible with many of your favorite Git-based tools, right now! All core developers use Jujutsu to develop Jujutsu, right here on GitHub. But it should hopefully work with your favorite Git forges, too. // 如今，我们使用 Git 仓库作为存储层来提供和跟踪内容，使其与您喜爱的许多基于 Git 的工具兼容，立即生效！ 所有核心开发者都用柔术来开发柔术，就在 GitHub 上。但它 希望它也能和你喜欢的 Git 锻造台兼容。 /// We combine many distinct design choices and concepts from other version control systems into a single tool. Some of those sources of inspiration include: // 我们将许多来自其他版本控制系统的不同设计选择和概念整合到一个工具中。其中一些灵感来源包括： /// - Git: We make an effort to [be fast] (. gh: jj-vcs/jj.git :: !discussions/49) — with a snappy UX, efficient algorithms, correct data structures, and good-old-fashioned attention to detail. The default storage backend uses Git repositories for “physical storage”, for wide interoperability and ease of onboarding. // Git：我们努力做到——通过流畅的用户体验、高效的算法、正确的数据结构和传统的细节关注。默认的存储后端使用 Git 仓库作为“物理存储”，以实现广泛的互作性和易于上线。 /// - Mercurial & Sapling: There are many Mercurial-inspired features, such as the revset (. web: docs.jj-vcs.dev/latest/revsets/) language to select commits. There is [no explicit index] (. web: docs.jj-vcs.dev/latest/git-comparison/#the-index) or staging area. Branches are “anonymous” like Mercurial, so you don't need to make up a name for each small change. Primitives for rewriting history are powerful and simple. Formatting output is done with a robust template language that can be configured by the user. // Mercurial 和 Supling：有许多受 Mercurial 启发的功能，比如用于选择提交的 revset 语言。 没有明确的索引 或者集结区。分支像水星一样是“匿名”的，所以你不需要 为每一个小变化起个名字。用于重写历史的原语有 强大而简单。格式化输出采用稳健的模板语言 用户可以自行配置。 /// - Darcs: Jujutsu keeps track of conflicts as [first-class objects] (. web: docs.jj-vcs.dev/latest/conflicts/) in its model; they are first-class in the same way commits are, while alternatives like Git simply think of conflicts as textual diffs. While not as rigorous as systems like Darcs (which is based on a formalized theory of patches, as opposed to snapshots), the effect is that many forms of conflict resolution can be performed and propagated automatically. // 《Darcs： Jujutsu》将冲突视为一流对象 ;它们和提交一样是一流的，而像 Git 这样的替代方案则只是把冲突看作文本差异。虽然不如 Darcs（基于形式化补丁理论而非快照）等系统严谨，但其效果是许多冲突解决形式可以自动执行和传播。 /// And it adds several innovative, useful features of its own: // 它还增加了几个创新且实用的功能： /// - Working-copy-as-a-commit: Changes to files are [recorded automatically] (. web: docs.jj-vcs.dev/latest/working-copy/) as normal commits, and amended on every subsequent change. This “snapshot” design simplifies the user-facing data model (commits are the only visible object), simplifies internal algorithms, and completely subsumes features like Git's stashes or the index/staging-area. // 作为提交复制工作：文件的更改会自动记录 如同正常提交，并在每次后续更改时进行修改。这个“快照” 设计简化了面向用户的数据模型（提交是唯一可见的部分） 对象），简化了内部算法，并完全涵盖了以下特征。 Git 的藏匿点或者索引/集结区。 /// - Operation log & undo: Jujutsu records every operation that is performed on the repository, from commits, to pulls, to pushes. This makes debugging problems like “what just happened?” or “how did I end up here?” easier, especially when you're helping your coworker answer those questions about their repository! And because everything is recorded, you can undo that mistake you just made with ease. Version control has finally entered [the 1960s] (. web: en.wikipedia.org/wiki/Undo#History)! // 作日志与撤销 ：Jujutsu 记录仓库上执行的所有作，从提交、拉取到推送。这让调试“刚刚发生了什么？”或“我怎么会走到这一步？”等问题变得更容易， 尤其是当你在帮同事回答关于他们仓库的问题时！而且因为一切都被记录下来，你可以轻松纠正刚才犯的错误。版本控制终于进入了 1960 年代 ！ /// - Automatic rebase and conflict resolution: When you modify a commit, every descendent is automatically rebased on top of the freshly-modified one. This makes “patch-based” workflows a breeze. If you resolve a conflict in a commit, the resolution of that conflict is also propagated through descendants as well. In effect, this is a completely transparent version of git rebase --update-refs combined with git rerere, supported by design. // 自动重基和冲突解决 ：当你修改一个提交时，每个后代都会自动基于新修改的后代。这使得“基于补丁”的工作流程变得轻而易举。如果你在提交中解决了冲突，该冲突的解决也会通过后代传递。实际上，这是 git rebase --update-refs 与 git rerere 的完全透明版本，设计上支持。 /// Warning // 警告 ///: The following features are available for use, but experimental; they may have bugs, backwards incompatible storage changes, and user-interface changes! // 以下功能可供使用，但仍处于实验阶段;它们可能存在漏洞、向后不兼容的存储更改以及用户界面的变化！ /// - Safe, concurrent replication: Have you ever wanted to store your version controlled repositories inside a Dropbox folder? Or continuously backup repositories to S3? No? Well, now you can! // 安全并发复制 ：你是否曾想过将版本控制的仓库存储在 Dropbox 文件夹中？还是持续备份仓库到 S3？不？现在你可以了！ /// - The fundamental problem with using filesystems like Dropbox and backup tools like rsync on your typical Git/Mercurial repositories is that they rely on local filesystem operations being atomic, serialized, and non-concurrent with respect to other reads and writes—which is not true when operating on distributed file systems, or when operations like concurrent file copies (for backup) happen while lock files are being held. // 在典型的 Git/Mercial 仓库中使用像 Dropbox 这样的文件系统和 rsync 等备份工具的根本问题在于，它们依赖于本地文件系统作是原子级的、序列化的，并且与其他读写作不一致——而在分布式文件系统上运行时，或者像备份文件并发复制等作在锁文件保存时，这并不成立。 /// - Jujutsu is instead designed to be [safe under concurrent scenarios] (. web: docs.jj-vcs.dev/latest/technical/concurrency/); simply using rsync or Dropbox and then using that resulting repository should never result in a repository in a corrupt state. The worst that should happen is that it will expose conflicts between the local and remote state, leaving you to resolve them. // 柔术设计上是在同时发生的情境下保持安全；仅仅使用 rsync 或 Dropbox 然后使用该存储库，不应该让存储库处于损坏状态 。最糟糕的是 应该发生的是，这会暴露出本地和偏远州之间的冲突，让你自己去解决。 /// The command-line tool is called jj for now because it's easy to type and easy to replace (rare in English). The project is called “Jujutsu” because it matches “jj”. // 命令行工具暂时叫 jj，因为它容易打字，也容易替换（英文中很少见）。这个项目叫“柔术”，因为它和“柔术”相符。"
[lazyjj-tui.src/gh]: https://github.com/Cretezy/lazyjj.git "(Apache-2.0) (Languages: Rust 100.0%) TUI for Jujutsu/jj /// TUI for Jujutsu/jj. Built in Rust with Ratatui. Interacts with jj CLI. // Jujutsu/jj 的 TUI。使用 Rust 和 Ratatui 构建。与 jj 命令行界面交互。 /// Features // 特征 /// Log // 日志 /// - Scroll through the jj log and view change details in side panel // 滚动浏览 jj 日志，并在侧面板中查看更改详情 /// - Create new changes from selected change with n // 使用 n 从选定的更改创建新的更改 /// - Edit changes with e/E // 使用 e / E 编辑更改 /// - Describe changes with d // 描述与 d 变化 /// - Abandon changes with a // a 更改 /// - Toggle between color words and git diff with p // 使用 p 在颜色词和 git diff 之间切换。 /// - See different revset with r // 使用 r 查看不同的 revset。 /// - Set a bookmark to selected change with b // 使用 b 将书签设置为选定更改。 /// - Fetch/push with f/p // 使用 f / p 获取/推送 /// - Squash current changes to selected change with s/S // 使用 s / S 压缩当前对选定更改的更改 /// Files // 文件 /// - View files in current change and diff in side panel // 在侧面板中查看当前更改和差异的文件 /// - See a change's files from the log tab with Enter // 按 Enter 可在日志选项卡中查看更改的文件。 /// - View conflicts list in current change // 查看当前变更中的冲突列表 /// - Toggle between color words and git diff with w // 使用 w 在颜色词和 git diff 之间切换。 /// - Untrack file with x // 使用 x 取消跟踪文件 /// Bookmarks // 书签 /// - View list of bookmarks, including from all remotes with a // 查看书签列表，包括来自所有遥控器的 a /// - Create with c, rename with r, delete with d, forget with f // 使用 c 创建，使用 r 重命名，使用 d 删除，使用 f 忘记。 /// - Track bookmarks with t, untrack bookmarks with T // 使用 t 跟踪书签，使用 T 取消跟踪书签 /// - Create new change with n, edit change with e/E // 使用 n 创建新更改，使用 e / E 编辑更改 /// - Command log: View every command lazyjj executes // 命令日志：查看 lazyjj 执行的每条命令。 /// - Config: Configure lazyjj with your jj config // 配置：使用您的 jj 配置来配置 lazyjj /// - Command box: Run jj commands directly in lazyjj with : // 命令框：在 lazyjj 中直接运行 jj 命令 : /// - Help: See all key mappings with ? // 帮助：使用 ? 查看所有键映射 /// Usage // 用法 /// To start lazyjj for the repository in the current directory: lazyjj // 要为当前目录中的仓库启动 lazyjj： lazyjj /// To use a different repository: lazyjj --path ~/path/to/repo // 要使用不同的仓库： lazyjj --path ~/path/to/repo /// To start with a different default revset: lazyjj -r '::@' // 首先使用不同的默认版本集： lazyjj -r '::@'"
[jjui-tui.src/gh]: https://github.com/idursun/jjui.git "(MIT) (Languages: Go 99.8%, Nix 0.2%) Jujutsu UI (jjui) is a Text User Interface (TUI) designed for interacting with the Jujutsu version control system. // Jujutsu UI (jjui) 是一个文本用户界面 (TUI)，旨在与 Jujutsu 版本控制系统进行交互。 /// jjui is a terminal user interface for working with Jujutsu version control system. I have built it according to my own needs and will keep adding new features as I need them. I am open to feature requests and contributions. // jjui 是一个用于操作 Jujutsu 版本控制系统的终端用户界面。它是我根据自身需求开发的，并将根据需要不断添加新功能。欢迎提出功能建议和贡献代码。 /// Currently, you can: // 目前，您可以： /// Change revset with auto-complete // 使用自动完成功能更改版本设置 ///: You can change revset while enjoying auto-complete and signature help while typing. // 您可以在输入时享受自动完成功能和签名帮助来更改版本设置。 /// - Rebase // 重基 ///: You can rebase a revision or a branch onto another revision in the revision tree. // 您可以将修订版或分支重基到修订版树中的另一个修订版上。 /// - Squash // 压缩 ///: You can squash revisions into one revision, by pressing S. The following revision will be automatically selected. However, you can change the selection using j and k. // 您可以通过按 S 将多个修订版压缩为一个修订版。下一个修订版将被自动选中。但是，您可以使用 j 和 k 更改选择。 /// - Show revision details // 显示修订版详情 ///: Pressing l (as in going right into the details of a revision) will open the details view of the revision you selected. In this mode, you can: Restore selected files using r (press i in the dialog for interactive chunk restore); Split selected files using s; View diffs of the highlighted by pressing d. // 按下 l 键（就像直接进入修订详情一样）将打开所选修订的详细信息视图。在此模式下，您可以：使用 r 恢复选定文件（在对话框中按 i 可进行交互式块恢复）；使用 s 拆分选定的文件；按 d 键查看高亮部分的差异。 /// - Bookmarks // 书签 ///: You can move bookmarks to the revision you selected. // 您可以将书签移动到所选修订版。 /// - Op Log // 操作日志 ///: You can switch to op log view by pressing o. Pressing r restores the selected operation. For more information, see Op log wiki page (. gh: idursun/jjui.git :: .wiki/Oplog). // 您可以按 o 切换到操作日志视图。按 r 恢复所选操作。有关更多信息，请参阅操作日志维基页面。 /// - Preview // 预览 ///: You can open the preview window by pressing p. If the selected item is a revision, then the output of jj show command is displayed. Similarly, jj diff output is displayed for selected files, and jj op show output is displayed for selected operations. // 您可以按 p 键打开预览窗口。如果选中的项目是修订版本，则会显示 jj show 命令的输出。同样，对于选中的文件，会显示 jj diff 命令的输出；对于选中的操作，会显示 jj op show 命令的输出。 ///; While the preview window is showing, you can press; ctrl+n to scroll one line down, ctrl+p to scroll one line up, ctrl+d to scroll half a page down, ctrl+u to scroll half a page up. // 在预览窗口显示时，您可以按： ctrl+n 向下滚动一行， ctrl+p 向上滚动一行， ctrl+d 向下滚动半页， ctrl+u 向上滚动半页。 ///; Additionally, you can press d to show the contents of preview in diff view. // 此外，您可以按 d 以不同视图显示预览内容。"
[jj-cli.site/dev]: https://jj-vcs.dev "Colocating a Jujutsu repository allows you to use both Jujutsu and Git in the same working copy. The benefits of doing so are: // 将 Jujutsu 代码库放在同一位置，即可在同一个工作副本中使用 Jujutsu 和 Git。这样做的好处有： /// - You can use Git commands when you're not sure how to do something with Jujutsu, Jujutsu hasn't yet implemented a feature (e.g., tagging), or you simply prefer Git in some situations. // 当您不确定如何使用 Jujutsu 执行某些操作、Jujutsu 尚未实现某个功能（例如，标签）或您在某些情况下更喜欢使用 Git 时，可以使用 Git 命令。 /// - Tooling that expects a Git repository still works (IDEs, build tooling, etc.) // 需要 Git 仓库的工具仍然可用（IDE、构建工具等）。"
[jjui-tui.site/ghio]: https://idursun.github.io/jjui/ "Core Features // 核心功能 /// - Revisions (. web: ./Revisions.html): Main view for exploring and managing revisions // 修订 ：用于浏览和管理修订的主要视图 /// - Preview (. web: ./Preview.html): View diffs and details of commits and files (with mouse-aware scrolling and auto placement) // 预览 ：查看提交和文件的差异和详细信息（支持鼠标感应滚动和自动定位） /// - Details (. web: ./Details.html): Explore files in a commit, with diff, split, and restore capabilities // 详细信息 ：浏览提交中的文件，包括差异比较、拆分和恢复功能。 /// - Oplog (. web: ./Oplog.html): View the operation log of your repository // 操作日志 ：查看存储库的操作日志 /// - Command Execution (. web: ./Command-Execution.html): Execute shell and jj commands directly from jjui // 命令执行 ：直接从 jjui 执行 shell 和 jj 命令 /// - Custom Commands (. web: ./Custom-Commands.html): Define your own commands with custom keybindings, key sequences, and Lua scripting // 自定义命令 ：使用自定义按键绑定、按键序列和 Lua 脚本定义您自己的命令 /// - Fuzzy File Finder (. web: ./Fuzzy-File-Finder.html): Fuzzy find on all files and explore changes on them. // 模糊文件查找器 ：对所有文件进行模糊查找并探索其更改。 /// - Ace Jump (. web: ./Ace-Jump.html): Quickly move between revisions using fast key strokes. // Ace Jump ：使用快速按键在不同版本之间快速切换。 /// - Exec (. web: ./Exec.html): Run interactive jj and shell commands // 执行 ：运行交互式 jj 和 shell 命令 /// - Flash Messages (. web: ./Flash-Messages.html): See command output and errors in the UI // 刷新消息：在用户界面中查看命令输出和错误 /// - Bookmarks (. web: ./Bookmarks.html): Manage bookmarks with a pop-up menu // 书签 ：使用弹出菜单管理书签 /// - Git (. web: ./Git.html): Run git commands from a pop-up menu // Git ：从弹出菜单运行 Git 命令 /// Revision Operations // 修订操作 /// - Rebase (. web: ./Rebase.html): Interactive rebase of commits // 变基 ：交互式变基提交 /// - Absorb (. web: ./Absorb.html): Automatically absorb changes into commits // 吸收 ：自动将更改吸收到提交中 /// - Abandon (. web: ./Abandon.html): Remove commits from your history // 放弃 ：从历史记录中删除提交 /// - Duplicate (. web: ./Duplicate.html): Copy revisions with interactive UI // 复制 ：通过交互式用户界面复制修订版本 /// - Squash (. web: ./Squash.html): Squash multiple revisions into one // 合并 ：将多个版本合并为一个版本。 /// - Evolog (. web: ./Evolog.html): Explore and restore changes from the evolution log // 进化日志 ：探索并恢复进化日志中的更改 /// - Inline Describe (. web: ./Inline-Describe.html): Edit revision descriptions inline // 内联描述 ：直接编辑修订描述 /// - Bookmark (. web: ./Bookmark.html): Set or move bookmarks on revisions // 书签 ：在修订时设置或移动书签 /// Customization // 定制 /// - Configuration (. web: ./Configuration.html): How to configure JJUI to your needs // 配置 ：如何根据您的需求配置 JJUI /// - Command Line Options (. web: ./Command-Line-Options.html): Customize behavior with command line flags // 命令行选项 ：使用命令行标志自定义行为 /// - Leader Key (. web: ./Leader-Key.html): Create powerful mnemonic shortcuts with nested keymaps // Leader Key ：使用嵌套键映射创建强大的助记快捷键 /// - Themes (. web: ./Themes.html): Customize the appearance of the UI with detailed styling options // 主题 ：通过详细的样式选项自定义用户界面外观 /// - Tracing (. web: ./Tracing.html): Highlight lanes and dim out-of-lane revisions // 追踪 ：高亮显示车道，暗化车道外的修改"
[jj-cli.guix/gnu]: https://packages.guix.gnu.org/packages/jujutsu/ "(: guix install -- jujutsu) (Licenses: ASL 2.0) (Package source: cb:guix/guix.git@master/gnu/packages/rust-apps.scm#L1586) jujutsu // 柔术 /// Git-compatible distributed version control system // 兼容 Git 的分布式版本控制系统 /// Jujutsu is a version control system designed to be easy to use. It uses a real commit to represent the working copy, records all operations performed on the repository and supports automatic conflict resolution. // Jujutsu 是一款易于使用的版本控制系统。它使用真实的提交来表示工作副本，记录对代码库执行的所有操作，并支持自动冲突解决。 /// Although Jujutsu uses a Git repository as its storage backend, it internally abstracts the user interface and version control algorithms from the storage systems. This opens possibility for it to serve as a version control system with other physical backends. // 尽管 Jujutsu 使用 Git 仓库作为存储后端，但它在内部对用户界面和版本控制算法进行了抽象，使其与存储系统脱钩。这使得它能够与其他物理后端配合使用，作为版本控制系统。 (src: gh:jj-vcs/jj.git)"
[jj-cli.nix/nixos]: https://search.nixos.org/packages?show=jujutsu "(: nix profile add -- 'nixpkgs#jujutsu')"
[lazyjj-tui.nix/nixos]: https://search.nixos.org/packages?show=lazyjj "(: nix profile add -- 'nixpkgs#lazyjj')"
[jjui-tui.nix/nixos]: https://search.nixos.org/packages?show=jjui "(: nix profile add -- 'nixpkgs#jjui')"
[jj-cli.cargo/crates]: https://crates.io/crates/jj-cli "(: cargo install --locked --bin jj -- jj-cli) (Apache-2.0) (41K SLoC) (932 KiB) Jujutsu - an experimental version control system // Jujutsu —— 一个实验性的版本控制系统 (src: gh:jj-vcs/jj.git)"
[lazyjj-tui.cargo/crates]: https://crates.io/crates/lazyjj "(: cargo install -- lazyjj) (Apache-2.0) (5.0K SLoC) (52.3 KiB) TUI for Jujutsu/jj // Jujutsu/jj 的 TUI (src: gh:Cretezy/lazyjj.git)"
[jj-cli.mise/mise]: https://mise.jdx.dev/registry.html "(: mise use -g -- jj jjui)"
[jj-cli.x/x-cmd]: https://x-cmd.com/install/jj "(: xd alp -- jj) (: x install -- jj)"
[jj-cli.brew/homebrew]: https://formulae.brew.sh/formula/jj "(: brew install -- jj)"
[lazyjj-tui.brew/homebrew]: https://formulae.brew.sh/formula/lazyjj "(: brew install -- lazyjj)"
[jjui-tui.brew/homebrew]: https://formulae.brew.sh/formula/jjui "(: brew install -- jjui)"
[jj-cli.winget/wintall]: https://winstall.app/apps/jj-vcs.jj "(: winget install -e --id jj-vcs.jj)"
[lazyjj-tui.winget/wintall]: https://winstall.app/apps/Cretezy.lazyjj "(: winget install -e --id Cretezy.lazyjj)"
[jjui-tui.winget/wintall]: https://winstall.app/apps/IbrahimDursun.jjui "(: winget install -e --id IbrahimDursun.jjui)"

