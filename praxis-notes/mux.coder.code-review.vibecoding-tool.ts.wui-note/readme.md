[src/gh]: https://github.com/coder/mux.git "(AGPL-3.0) (Languages: TypeScript 96.4%, CSS 1.0%, Shell 0.9%, JavaScript 0.7%, Makefile 0.4%, Python 0.3%, Other 0.3%) A desktop app for isolated, parallel agentic development// 一款用于独立、并行智能体开发的桌面应用程序 /// mux - coding agent multiplexer// mux - 编码智能体多路复用器 /// A desktop application for parallel agentic development.// 一款用于并行智能体开发的桌面应用程序。 /// Why parallelize? // 为什么要并行化？ /// Here are some specific use cases we enable: // 以下是我们支持的一些具体应用场景： /// - Contextual continuity between relevant changes: // 相关变化之间的上下文连续性： /// - - e.g. create a workspace for code-review, refactor, and new-feature // 例如，创建一个用于 code-review 、 refactor 和 new-feature 的工作区。 /// - GPT-5-Pro: use the slow but powerful GPT-5-Pro for complex issues // GPT-5-Pro：使用速度较慢但功能强大的 GPT-5-Pro 来解决复杂问题。 /// - - Run in the background for hours on end // 在后台连续运行数小时 /// - - The stream will automatically resume after restarts or intermittent connection issues. If the model completes early we will show an indicator. // 重启或出现间歇性连接问题后，视频流将自动恢复。如果模型提前完成，我们会显示提示信息。 /// - A/B testing: run multiple workspaces in parallel on the same problem but different approaches, abandon the bad ones. // A/B 测试：针对同一问题并行运行多个工作区，但采用不同的方法，放弃效果不佳的工作区。 /// - Tangent exploration: launch tangents in mux away from main work // 切线探索：在远离主工作区的 mux 中启动切线 /// Features // 特征 /// - Isolated workspaces with central view on git divergence // 具有集中视图的独立工作区 ，用于查看 Git 分歧 /// - - Local: run directly in your project directory // 本地运行：直接在项目目录中运行 /// - - Worktree: git worktrees on your local machine // 工作树 ：本地计算机上的 git 工作树 /// - - SSH: remote execution on a server over SSH // SSH ：通过 SSH 在服务器上进行远程执行 /// - Multi-model (sonnet-4-*, grok-*, gpt-5-*, opus-4-*) // 多模型 （ sonnet-4-* 、 grok-* 、 gpt-5-* 、 opus-4-* ） /// - - Ollama supported for local LLMs (docs) // Ollama 支持本地 LLM（ 文档 ） /// - - OpenRouter supported for long-tail of LLMs (docs) // OpenRouter 支持长尾 LLM（ 文档 ） /// - VS Code Extension: Jump into mux workspaces directly from VS Code (docs) // VS Code 扩展 ：直接从 VS Code 进入 mux 工作区（ 文档 ） /// - Supporting UI and keybinds for efficiently managing a suite of agents // 支持用于高效管理一系列智能体的 UI 和快捷键。 /// - Rich markdown outputs (mermaid diagrams, LaTeX, etc.) // 丰富的 Markdown 输出（美人鱼图、LaTeX 等） /// mux has a custom agent loop but much of the core UX is inspired by Claude Code. You'll find familiar features like Plan/Exec mode, vim inputs, /compact and new ones like opportunistic compaction and mode prompts. // mux 拥有自定义的智能体循环，但其核心用户体验很大程度上借鉴了 Claude Code 的设计理念。你会发现一些熟悉的功能，例如 Plan/Exec 模式、vim 输入、 /compact 命令，以及一些新功能，例如机会性压缩和模式提示 。"
[site/coder]: https://mux.coder.com/ "mux - Coding Agent Multiplexer for AI-assisted development // mux - 用于人工智能辅助开发的编码智能体多路复用器"

