[src/gh]: https://github.com/syrupy-project/syrupy.git "(MIT) (Languages: Python 97.3%, AppleScript 1.6%, Other 1.1%) 🥞 The sweeter pytest snapshot plugin // 🥞 更甜的 pytest 快照插件 /// Syrupy is a zero-dependency pytest (. docs.pytest.org) snapshot plugin. It enables developers to write tests which assert immutability of computed results. // Syrupy 是一个零依赖的 pytest 快照插件。它使开发者能够编写断言计算结果不可变的测试。 /// Syrupy upholds three principles: // Syrupy 遵循三个原则： /// - Extensible: If a particular data type is not supported, users should be able to easily and quickly add support. // 可扩展：如果某个特定数据类型不受支持，用户应该能够轻松快速地添加支持。 /// - Idiomatic: Snapshot testing should fit naturally among other test cases in pytest, e.g. assert x == snapshot vs. snapshot.assert_match(x). // 习惯用法：快照测试应该在 pytest 中的其他测试用例中自然地融入，例如 assert x == snapshot 与 snapshot.assert_match(x) 。 /// - Soundness: Snapshot tests should uncover even the most minute issues. Unlike other snapshot libraries, Syrupy will fail a test suite if a snapshot does not exist, not just on snapshot differences. // 可靠性：快照测试应该揭示哪怕是极其微小的问题。与其他快照库不同，Syrupy 如果快照不存在，将会使整个测试套件失败，而不仅仅是快照差异。"
[site/ghio]: https://syrupy-project.github.io/syrupy/ "(: pytest --snapshot-update) /// Migration from snapshottest // 从 snapshottest 迁移 /// You cannot use syrupy alongside snapshottest due to argument conflicts. To ease migration, we’ve made syrupy aware of snapshottest call syntax. Simply uninstall snapshottest and remove old snapshots // 由于参数冲突，您不能同时使用 syrupy 和 snapshottest。为了简化迁移过程，我们让 syrupy 能够识别 snapshottest 的调用语法。只需卸载 snapshottest 并删除旧快照。"
[pkg.pip/pypi]: https://pypi.org/project/syrupy/ "(: pip install -- syrupy)"
