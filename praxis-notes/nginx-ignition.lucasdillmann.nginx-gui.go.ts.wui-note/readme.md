[src/gh]: https://github.com/lucasdillmann/nginx-ignition.git "(MIT) (Languages: Go 59.2%, TypeScript 38.5%, CSS 1.5%, Makefile 0.3%, HTML 0.2%, JavaScript 0.1%, Other 0.2%) An user interface for the nginx web server, aimed at developers and enthusiasts that don't want to manage configuration files manually // 一个用于 nginx Web 服务器的用户界面，面向那些不想手动管理配置文件的开发者和爱好者 /// Although it isn't the goal to be feature-complete (if your use-case is quite advanced or complex, you probably will not use a UI anyway), the project does aim to provide an intuitive and powerful way to configure and run nginx. // 尽管并非追求功能完备（如果你的使用场景非常高级或复杂，你可能根本不会使用界面），但该项目旨在提供一种直观且强大的方式来配置和运行 nginx。 /// Some of the available features include: // 一些可用的功能包括： /// - Multiple nginx virtual hosts, each one with its customized set of domains, routes and bindings (port listeners) // 多个 nginx 虚拟主机，每个主机都有其自定义的域名、路由和绑定（端口监听器） /// - Multiple nginx streams (for proxying raw TCP, UPD and unix sockets traffic), each one with its customized binding and backing service, with support for domain-based routing (using the SNI from the TLS protocol), circuit breakers, weights and more. // 多个 nginx 流（用于代理原始 TCP、UDP 和 Unix 套接字流量），每个流都有其自定义的绑定和后端服务，支持基于域的路由（使用 TLS 协议的 SNI），断路器、权重等。 /// - Each host route can act as a proxy, redirection, execute custom code (JavaScript or Lua), reply with a static response or serve static files (directory listing can be optionally enabled) // 每个主机路由可以充当代理、重定向、执行自定义代码（JavaScript 或 Lua）、回复静态响应或提供静态文件（目录列表可以可选启用） /// - Easy configuration of the nginx server (maximum body/upload size, server tokens, timeouts, log level, etc) // nginx 服务器的轻松配置（最大请求体/上传大小、服务器令牌、超时、日志级别等） /// - SSL certificates (Let's Encrypt, self-signed or bring your custom one) with automatic renew (when applicable) // SSL 证书（Let's Encrypt、自签名或带来您自定义的证书），支持自动续期（适用时） /// - Server and virtual hosts access and error logs with automatic log rotation // 服务器和虚拟主机访问及错误日志，支持自动日志轮转 /// - Multiple users with attribute-based access control (ABAC) // 支持基于属性的访问控制（ABAC）的多个用户 /// - Native integration with TrueNAS, allowing to easily configure to proxy to an app hosted in your NAS // 与 TrueNAS 原生集成，可轻松配置以将流量代理到您 NAS 中托管的 应用程序 /// - Native integration with Docker for easy pick of a container as the proxy target // 与 Docker 原生集成，可轻松选择容器作为代理目标 /// - Access lists for easy control of who can access what using basic authentication and/or source IP address checks // 访问列表，使用基本认证和/或源 IP 地址检查，轻松控制谁可以访问什么"
[pkg.oci/dockerhub]: https://hub.docker.com/r/dillmann/nginx-ignition "(: docker run -p 8090:8090 -p 80:80 -- dillmann/nginx-ignition # After a few seconds, you can open your favorite browser at http://localhost:8090 and start using it. There's no default username or password, the nginx ignition will guide you to create your user. // 几秒钟后，您可以在 http://localhost:8090 上打开您喜欢的浏览器并开始使用它。没有默认的用户名或密码，nginx ignition 将引导您创建用户。)"

