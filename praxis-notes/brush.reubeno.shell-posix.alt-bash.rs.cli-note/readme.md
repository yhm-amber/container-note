[src/gh]: https://github.com/reubeno/brush.git "(MIT) (Languages: Rust 97.3%, Python 2.1%, Other 0.6%) 🐚bash/POSIX-compatible shell implemented in Rust 🦀 // 🐚用 Rust 实现的兼容 bash/POSIX 的 shell 🦀 /// brush (Bo(u)rn(e) RUsty SHell) is a modern bash- and POSIX- compatible shell written in Rust. Run your existing scripts and .bashrc unchanged -- with syntax highlighting and auto-suggestions built in. // brush （ Bourse Rust shell ）是一个用 Rust 编写的现代化的 bash 和 POSIX 兼容 shell。您可以直接运行现有的脚本和 .bashrc 文件，无需任何修改——它内置了语法高亮和自动建议功能。 /// ## At a glance // 一览 /// - ✅ Your existing .bashrc just works—aliases, functions, completions, all of it. // ✅ 您现有的 .bashrc 可以直接使用——别名、函数、自动补全等等。 /// - ✨ Syntax highlighting and auto-suggestions built in. // ✨ 内置语法高亮和自动建议功能。 /// - 🧪 Validated against bash with ~1700 compatibility tests. // 🧪 通过对 bash 进行约 1700 项兼容性测试验证。 /// - 🧩 Easily embeddable in your Rust apps using brush_core::Shell. // 🧩 使用 brush_core::Shell 可以轻松嵌入到您的 Rust 应用程序中。 /// ## ✨ Features // ✨ 特点 /// ### 🐚 bash Compatibility // 🐚 bash 兼容性 /// - ✅	50+ builtins // 50多个内置 ///	echo, declare, read, complete, trap, ulimit, ... // echo 、 declare 、 read 、 complete 、 trap 、 ulimit 、…… /// - ✅	Full expansions // 完全扩展 ///	brace, parameter, arithmetic, command/process substitution, globs, extglob, globstar // 花括号、参数、算术运算、命令/进程替换、通配符、 extglob 、 globstar /// - ✅	Control flow // 控制流 ///	if/for/while/until/case, &&/||, subshells, pipelines, etc. // if / for / while / until / case , && / || , 子 shell、管道等。 /// - ✅	Redirection // 重定向 ///	here docs, here strings, fd duplication, process substitution redirects // 这里是文档，这里是字符串，文件描述符重复，进程替换重定向 /// - ✅	Arrays & variables // 数组和变量 ///	indexed/associative arrays, dynamic variables, standard well-known variables, etc. // 索引/关联数组、动态变量、标准已知变量等。 /// - ✅	Programmable completion // 可编程完成 ///	Works with bash-completion out of the box // 开箱即用 ，支持 bash 补全功能 /// - ✅	Job control // 工作控制 ///	background jobs, suspend/resume, fg/bg/jobs // 背景工作，暂停/恢复， fg / bg / jobs /// - 🔷	Traps & options // 陷阱和选项 ///	DEBUG/ERR/EXIT traps work; signal traps and options in progress // DEBUG / ERR / EXIT 陷阱已启用；信号陷阱和选项正在开发中 /// ### ⌨️ User Experience // ⌨️ 用户体验 /// - ✅	Syntax highlighting // 语法高亮 ///	Real-time as you type (reedline (. gh:nushell/reedline.git)) // 实时输入（ reedline (. gh:nushell/reedline.git)） /// - ✅	Auto-suggestions // 自动建议 ///	History-based hints as you type (reedline (. gh:nushell/reedline.git)) // 输入时提供基于历史记录的提示（ reedline (. gh:nushell/reedline.git)） /// - ✅	Rich prompts // 丰富的提示 ///	PS1/PROMPT_COMMAND, right prompts, starship (. web: starship.rs) compatible // PS1 / PROMPT_COMMAND ，正确的提示，兼容星舰 (. web: starship.rs) /// - ✅	TOML config // TOML 配置 ///	~/.config/brush/config.toml for persistent settings // ~/.config/brush/config.toml 用于持久化设置 /// - 🧪	Extras // 额外内容 ///	fzf/atuin support, zsh-style precmd/preexec hooks (experimental), VS Code terminal integration // 支持 fzf / atuin 、zsh 风格的 precmd / preexec 钩子（实验性）、VS Code 终端集成 /// ## Related Projects // 相关项目 ///: Other POSIX-ish shells implemented in non-C/C++ languages: // 其他用非 C/C++ 语言实现的类 POSIX shell： /// - nushell (. web: nushell.sh) — modern Rust shell (provides reedline) // nushell (. web: nushell.sh) — 现代 Rust shell（提供 reedline ） /// - fish (. web: fishshell.com) — user-friendly shell (Rust port in 4.0 (. web: fishshell.com/blog/rustport)) // fish (. web: fishshell.com) — 用户友好的 shell（ Rust 4.0 版本移植 (. web: fishshell.com/blog/rustport)） /// - Oils (. gh: oils-for-unix/oils.git) — bash-compatible with new Oil language // Oils (. gh: oils-for-unix/oils.git) 与 bash 兼容，并支持新的 Oil 语言 /// - mvdan/sh (. gh: mvdan/sh.git) — Go implementation // mvdan/sh (. gh: mvdan/sh.git) — Go 实现 /// - rusty_bash (. gh: shellgei/rusty_bash.git) — another Rust bash-like shell // rusty_bash (. gh: shellgei/rusty_bash.git) — 另一个类似 Rust bash 的 shell"
[site/sh]: https://brush.sh/ "brush // 刷子 /// A bash-compatible shell, implemented in Rust. // 一个用 Rust 实现的、与 bash 兼容的 shell。"
[docs/.site]: https://brush.sh/docs/ "Welcome! // 欢迎！"
[cli.cargo/crates]: https://crates.io/crates/brush-shell "(: cargo install --locked -- brush-shell # Build from sources) (: cargo binstall -- brush-shell # using cargo-binstall) (MIT) (1.6K SLoC) (163 KiB) Rust-implemented shell focused on POSIX and bash compatibility // 一个用 Rust 实现的 shell，专注于与 POSIX 和 bash 的兼容性。 (src: gh:reubeno/brush.git)"
[discord]: https://discord.gg/kPRgC9j3Tj
