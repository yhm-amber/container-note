[src/gh]: https://github.com/abokhalill/chr2.git "(Apache-2.0, MIT) (Languages: Rust 100.0%) A deterministic, crash-safe distributed state machine with exactly-once side effects // 具有确定性、崩溃安全的分布式状态机，且副作用恰好为一次 /// chronon // 计时器 /// Deterministic execution kernel with crash-safe replication and exactly-once side effects. // 具有崩溃安全复制和精确一次副作用的确定性执行内核。 /// Guarantees // 保证 /// - Durability // 耐久性 ///: 	Quorum writes survive f failures in 2f+1 cluster // 在 2f+1 个集群中，仲裁写入能够承受 f 次故障。 /// - Consistency // 一致性 ///: 	Linearizable via VSR consensus // 通过 VSR 共识实现线性化 /// - Exactly-once // 恰好一次 ///: 	Durable outbox with epoch-fenced execution // 坚固耐用的出货箱，具有划时代的执行力 /// - Determinism // 决定论 ///: 	Hash-chained log, BTreeMap iteration, consensus timestamps // 哈希链式日志、BTreeMap 迭代、共识时间戳 /// Principles // 原则 /// - Crash-only: No graceful shutdown. Crash anywhere, recover everywhere. // 仅崩溃模式 ：不进行优雅关机。无论在何处崩溃，都能在所有地方恢复。 /// - Hash chains: Every entry links to predecessor. Corruption detectable. // 哈希链 ：每个条目都链接到其前一个条目。可检测数据损坏。 /// - Explicit durability: barrier() or it didn't happen. // 显式持久性 ： barrier() 否则就等于没发生过。 /// - Fencing: View numbers gate all leader operations. Zombies die fast. // 围栏 ：查看所有领导者操作的数字门。僵尸死得很快。"
[lib.cargo/crates]: https://crates.io/crates/chronon "(: cargo add -- chronon) (MIT OR Apache-2.0) (14K SLoC) (131 KiB) chronon // 计时器 /// Deterministic execution kernel with crash-safe replication and exactly-once side effects // 具有崩溃安全复制和精确一次副作用的确定性执行内核 (src: gh:abokhalill/chr2.git)"
