[src/gh]: https://github.com/tonbo-io/tonbo.git "(Apache-2.0) (Languages: Rust 94.0%, Python 6.0%) A portable embedded database using Arrow. // 基于 Arrow 格式的便携式嵌入式数据库。 /// Tonbo is an embedded, persistent database offering fast KV-like methods for conveniently writing and scanning type-safe structured data. Tonbo can be used to build data-intensive applications, including other types of databases. // Tonbo 是一个嵌入式持久数据库，提供快速的类似 KV 的方法，可以方便地写入和扫描类型安全的结构化数据。 Tonbo 可用于构建数据密集型应用程序，包括其他类型的数据库。 /// Tonbo is implemented with a Log-Structured Merge Tree, constructed using Apache Arrow & Apache Parquet data blocks. Leveraging Arrow and Parquet, Tonbo supports: // Tonbo 通过使用 Apache Arrow 和 Apache Parquet 数据块构建的日志结构合并树来实现。利用 Arrow 和 Parquet，Tonbo 支持： /// - Pushdown limits, predicates, and projection operators // 下推限制、谓词和投影运算符 /// - Zero-copy deserialization // 零拷贝反序列化 /// - Various storage backends: OPFS, S3, etc. (to be supported in v0.2.0) // 各种存储后端：OPFS、S3等（v0.2.0支持） /// These features enhance the efficiency of queries on structured data. // 这些功能提高了结构化数据的查询效率。 /// Tonbo is designed to integrate seamlessly with other Arrow analytical tools, such as DataFusion. For an example, refer to this [preview](./examples/datafusion.rs) (official support for DataFusion will be included in v0.2.0). // Tonbo 旨在与其他 Arrow 分析工具（例如 DataFusion）无缝集成。有关示例，请参阅此预览版（v0.2.0 中将包含对 DataFusion 的官方支持）。"
[sqlite-plg.src/gh]: https://github.com/tonbo-io/tonbolite.git "(Apache-2.0) (Rust 100.0%) TonboLite: Analysis-ready SQLite using Tonbo // 古墓轻巧：以 Tombo 使 SQLite 用于分析 /// TonboLite is a WASM compatible SQLite extension that allows users to create tables which supports analytical processing directly in SQLite. Its storage engine is powered by our open-source embedded key-value database, Tonbo. // TonboLite 是一个与 WASM 兼容的 SQLite 扩展，允许用户直接在 SQLite 中创建支持分析处理的表。其存储引擎由我们的开源嵌入式键值数据库 Tonbo 提供支持。"

[site]: https://tonbo.io/ "Tonbo incurs minimal cost to insert and scan structured data via Apache Arrow. // Tonbo (via Apache Arrow) 使结构化数据以最低成本插入和扫描。 /// The simplest path to shared data storage. // 共享数据存储的最简单路径。"

[knowsby]: https://rustcc.cn/article?id=2d10b6f8-fe51-4a4b-8733-b94131b40c08 "它使 SQLite 能够在目标平台（如 WebAssembly 浏览器）上创建适合分析处理的表格，并高效写入数据。表格中的数据组织为分层的 Apache Parquet 格式文件，按需存储在本地磁盘（使用 OPFS 作为本地 I/O）或对象存储服务（如 S3）上。你可以通过在常规 SQLite 中创建虚拟表来使用它。"
