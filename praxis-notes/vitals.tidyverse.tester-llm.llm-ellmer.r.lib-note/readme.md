[src/gh]: https://github.com/tidyverse/vitals.git "(MIT) (Languages: JavaScript 98.4%, R 1.6%) Large language model evaluation for R // R 语言大语言模型评估 /// (: tsk = base::c('What is 2+2?', 'What is 2+3?', 'What is 2+4?') |> tibble::tibble(input = _, target = 2 + 2:4) |> vitals::Task$new(dataset = _, solver = vitals::generate(ellmer::chat_vllm(...)), scorer = vitals::model_graded_qa()) # create a task by df # Uses ellmer::chat_vllm(...) as the model backend ;: tsk$eval() # Evaluate an LLM on a simple math task ;: tsk$view() # View results)"
[site/tidyverse.org]: https://vitals.tidyverse.org/ "vitals is a framework for large language model evaluation in R. It’s specifically aimed at ellmer users who want to measure the effectiveness of their LLM products like [custom chat apps](https://posit.co/blog/custom-chat-app/ 'The fact that these models are able to read a couple hundred thousand words in an instant and keep all of it in their “short-term memory” gives us the solution to our problem. We can describe how our package (or set of packages) works super thoroughly and allow the model to sift through all that information to find what it needs. // 这些模型能够瞬间阅读几十万字并将所有信息都保持在它们的“短期记忆”中的事实，为我们提供了解决问题的方案。我们可以非常详细地描述我们的包（或一组包）的工作原理，并允许模型筛选所有这些信息以找到它需要的内容。') and querychat (. gh:posit-dev/querychat.git) apps. You can use it to: // vitals 是一个用于 R 语言中大型语言模型评估的框架。它专门面向 ellmer 用户，旨在测量他们 LLM 产品（如自定义聊天应用 (btw) 和 querychat 应用）的有效性。您可以使用它来： /// - Measure whether changes in your prompts or additions of new tools improve performance in your LLM product // 测量您的提示更改或添加新工具是否提高了 LLM 产品的性能 /// - Compare how different models affect performance, cost, and/or latency of your LLM product // 比较不同模型如何影响 LLM 产品的性能、成本和/或延迟 /// - Surface problematic behaviors in your LLM product // 在您的 LLM 产品中识别问题行为 /// The package is an R port of the widely adopted Python framework Inspect. While the package doesn’t integrate with Inspect directly, it allows users to interface with the Inspect log viewer and provides an on-ramp to transition to Inspect if need be by writing evaluation logs to the same file format. // 该软件包是广泛采用的 Python 框架 Inspect 的 R 语言版本。虽然该软件包不直接与 Inspect 集成，但它允许用户与 Inspect 日志查看器进行交互，并提供了一个过渡到 Inspect 的入口，如果需要的话，可以通过将评估日志写入相同的文件格式来实现。"
[package/cran]: https://cran.r-project.org/web/packages/vitals/ "(Version: 	0.1.0) (Depends: 	R (≥ 4.1)) (Imports: 	cli, dplyr, ellmer (≥ 0.2.1), glue, httpuv, jsonlite, purrr, R6, rlang, rstudioapi, S7, tibble, tidyr, withr) (Suggests: 	ggplot2, here, htmltools, knitr, ordinal, rmarkdown, testthat (≥ 3.0.0)) (Published: 	2025-06-24) (DOI: 	10.32614/CRAN.package.vitals) (Author: 	Simon Couch ORCID iD [aut, cre], Max Kuhn [ctb], Hadley Wickham ORCID iD [ctb], Mine Cetinkaya-Rundel ORCID iD [ctb], Posit Software, PBC ROR ID [cph, fnd]) (Maintainer: 	Simon Couch <simon.couch at posit.co>) (License: 	MIT + file LICENSE) (NeedsCompilation: 	no) vitals: Large Language Model Evaluation // vitals: 大型语言模型评估 /// A port of 'Inspect', a widely adopted 'Python' framework for large language model evaluation. Specifically aimed at 'ellmer' users who want to measure the effectiveness of their large language model-based products, the package supports prompt engineering, tool usage, multi-turn dialog, and model graded evaluations. // 'Inspect' 的移植版本，是一个广泛采用用于大型语言模型评估的 'Python' 框架。特别针对希望测量其基于大型语言模型产品的效果的 'ellmer' 用户，该软件包支持提示工程、工具使用、多轮对话和模型分级评估。 (src: gh:tidyverse/vitals.git)"
[knows_by]: https://github.com/simonpcouch/ "I'm Simon (he/him), an R developer. I build tools for data scientists at Posit PBC (formerly RStudio).🐛 // 我是西蒙（他/他），一名 R 开发者。我在 Posit PBC（前身为 RStudio）为数据科学家构建工具。🐛 /// Most of the time, I'm focused on helping R users get the most out of LLMs: // 大多数时候，我专注于帮助 R 用户充分利用 LLMs： /// - tidyverse/vitals: LLM evaluation // tidyverse/vitals: LLM 评估"

