[tursodb-core.src/gh]: https://github.com/tursodatabase/turso.git "(MIT) (Languages: Rust 77.5%, C 6.8%, Tcl 5.9%, TypeScript 2.3%, Java 2.2%, Dart 1.9%, Other 3.4%) Turso is an in-process SQL database, compatible with SQLite. // Turso 是一个进程内 SQL 数据库，兼容 SQLite。 /// Turso Database // Turso 数据库 /// An in-process SQL database, compatible with SQLite. // 一个进程内 SQL 数据库，兼容 SQLite。 /// Turso Database is an in-process SQL database written in Rust, compatible with SQLite. // Turso Database 是一个用 Rust 编写的进程内 SQL 数据库，兼容 SQLite。 /// Turso Database is a project to build the next evolution of SQLite in Rust, with a strong open contribution focus and features like native async support, vector search, and more. The libSQL project is also an attempt to evolve SQLite in a similar direction, but through a fork rather than a rewrite. // Turso 数据库是一个用 Rust 构建 SQLite 下一代演进的项目，具有强烈的开放贡献焦点，并支持原生异步、向量搜索等功能。libSQL 项目也是一个试图以类似方向演进 SQLite 的尝试，但它是通过分支而非重写实现的。 /// Rewriting SQLite in Rust started as an unassuming experiment, and due to its incredible success, replaces libSQL as our intended direction. At this point, libSQL is production ready, Turso Database is not - although it is evolving rapidly. More details here. // 用 Rust 重写 SQLite 最初只是一个不起眼的实验，但由于其惊人的成功，它成为了我们预期的方向。目前，libSQL 已经可以投入生产，而 Turso Database 尚未准备好——尽管它正在迅速发展。更多详情请查看此处。"
[tursodb-lib:go-api<core-impl>.src/gh]: https://github.com/tursodatabase/turso-go.git "(MIT) (Languages: Go 69.0%, Rust 20.2%, Python 6.4%, Shell 3.3%, Makefile 1.1%) Go language bindings for Turso database. // Turso 数据库的 Go 语言绑定。"
[tursodb-app.wui/.site]: https://shell.turso.tech/ "Turso Shell /// (左边可以写 sql 右边显示查询结果 …… 就在浏览器上。)"
[turso-site/tech]: https://turso.tech/ "The Database for the Age of AI Agents // AI 代理时代的数据库 /// Databases Everywhere // 无处不在的数据库 /// Turso is the lightweight database that scales to millions of instances. Build agents, AI assistants, and intelligent apps by deploying databases everywhere: on servers, browsers, and devices, just like files. Turso is a complete SQLite drop-in replacement, built for the agentic future. // Turso 是可扩展至数百万实例的轻量级数据库。通过在服务器、浏览器和设备上部署数据库来构建代理、AI 助手和智能应用，就像文件一样。Turso 是完整的 SQLite 即插即用替代品，专为代理未来而构建。 /// Why Agents Need a New Database // 为什么代理需要新的数据库 /// Agents will exist by the trillions and run everywhere. LLMs are wild, so compute gets sandboxed. The data pairing of this is giving each agent its own database to track files and assets, store memories, and coordinate tasks. // 代理将存在数十亿个，并运行在各个地方。LLMs 非常狂野，因此计算被沙盒化。这种数据配对方式是为每个代理分配自己的数据库，以跟踪文件和资产，存储记忆，并协调任务。 /// Traditional databases were designed for the client-server era, not the agentic age. Agents need lightweight data that powers their sandboxes. Something that behaves like a filesystem but has the power of a database. SQLite has the right shape for the agentic era. But it needs to evolve. Turso is this evolution. // 传统数据库是为客户端-服务器时代设计的，而不是为代理时代设计的。代理需要轻量级的数据来驱动它们的沙盒。需要像文件系统一样工作，但拥有数据库的强大功能。SQLite 适合代理时代，但它需要进化。Turso 就是这种进化。 /// - Vector Search // 向量搜索 ///: Native similarity search for AI apps and RAG workflows, no extensions required. // AI 应用和 RAG 工作流的原生相似度搜索，无需扩展。 /// - Async Design // 异步设计 ///: Modern async primitives such as Linux io_uring to keep your apps responsive. // 现代异步原语，如 Linux io_uring ，以保持您的应用程序响应。 /// - Concurrent Writes // 并发写入 ///: Multiple writers, zero conflicts, without locking. // 多写者，零冲突，无需锁定。 /// - Browser + Persistence // 浏览器 + 持久化 ///: Turso runs in the browser with WebAssembly & OPFS. // Turso 在浏览器中运行，使用 WebAssembly & OPFS。 /// - SQLite Compatible // SQLite 兼容 ///: Fully backwards compatible SQLite, but built for the next generation of agents. // 完全向后兼容的 SQLite，但专为下一代代理构建。 /// - Open Contribution // 开放贡献 ///: Take your seat at the table and contribute to the next evolution of SQLite. // 加入我们，为 SQLite 的下一次进化贡献力量。"
[tursodb-lib:rust-api<core-impl>.cargo/crates]: https://crates.io/crates/turso "(: cargo add -- turso) (MIT) (1.6K SLoC) (32.3 KiB) Turso Rust API /// The next evolution of SQLite: A high-performance, SQLite-compatible database library for Rust // SQLite 的下一代演进：一个高性能、兼容 SQLite 的 Rust 数据库库 /// Features // 特性 /// - SQLite Compatible: Similar interface to rusqlite with familiar API apart from using async Rust // 兼容 SQLite：与 rusqlite 类似，具有熟悉的 API，除了使用异步 Rust /// - High Performance: Built with Rust for maximum speed and efficiency // 高性能：使用 Rust 构建，以实现最大速度和效率 /// - Async/Await Support: Native async operations with tokio support // 异步/等待支持：具有 tokio 支持的本地异步操作 /// - In-Process: No network overhead, runs directly in your application // 进程内：无网络开销，直接运行在您的应用程序中 /// - Cross-Platform: Supports Linux, macOS, and Windows // 跨平台：支持 Linux、macOS 和 Windows /// - Transaction Support: Full ACID transactions with rollback support // 事务支持：支持完整 ACID 事务及回滚功能 /// - Prepared Statements: Optimized query execution with parameter binding // 预处理语句：通过参数绑定优化查询执行 (src: gh:tursodatabase/turso.git@main/bindings/rust)"
[tursodb-lib:python-api<core-impl>.pip/pypi]: https://pypi.org/project/pyturso/ "(: pip install -- pyturso) (License: MIT License) (Requires: Python >=3.9) Turso is a work-in-progress, in-process OLTP database management system, compatible with SQLite. // Turso 是一个正在开发中的、进程内 OLTP 数据库管理系统，与 SQLite 兼容。 (src: gh:tursodatabase/turso.git@main/bindings/python)"
[tursodb-lib:javascript-api<core-impl>.npm/npmjs.com]: https://npmjs.com/package/@tursodatabase/database "(: npm i -- @tursodatabase/database) (License: MIT) (Unpacked Size: 44.1 kB) (Total Files: 15) This package is the Turso embedded database library for JavaScript in Node. // 这个包是 Node.js 中的 Turso 嵌入式数据库库。 (src: gh:tursodatabase/turso.git@main/bindings/javascript)"
[tursodb-lib:java-api<core-impl>.mvn/sonatype]: https://central.sonatype.com/artifact/tech.turso/turso "(: mvn dependency:get -Dartifact=tech.turso:turso) (License: MIT) The Turso JDBC driver is a library for accessing and creating Turso database files using Java. // Turso JDBC 驱动是一个用于使用 Java 访问和创建 Turso 数据库文件的库。 /// Code style // 代码风格 /// Favor composition over inheritance. For example, JDBC4Connection doesn't implement TursoConnection. Instead, it includes TursoConnection as a field. This approach allows us to preserve the characteristics of Turso using TursoConnection easily while maintaining interoperability with the Java world using JDBC4Connection. // 推荐组合优于继承。例如， JDBC4Connection 没有实现 TursoConnection 。相反，它将 TursoConnection 作为字段包含。这种方法使我们能够轻松地使用 TursoConnection 保留 Turso 的特性，同时使用 JDBC4Connection 保持与 Java 世界的互操作性。 (src: gh:tursodatabase/turso.git@main/bindings/java)"
[tursodb-lib:dart-api<core-impl>.flutter/pub.dev]: https://pub.dev/packages/turso_dart "(: flutter pub add -- turso_dart) (License: MIT) Dart/Flutter binding for turso database. // Turso 数据库的 Dart/Flutter 绑定。 (src: gh:tursodatabase/turso.git@main/bindings/dart)"
[tursodb-lib:go-api<core-impl>.go/pkg.go.dev]: https://pkg.go.dev/github.com/tursodatabase/turso-go "(: go get github.com/tursodatabase/turso-go) (License: MIT) (Imports: 19) (Imported by: 2) (src: gh:tursodatabase/turso-go.git)"
[tursocloud-cli.src/gh]: https://github.com/tursodatabase/turso-cli "(MIT) (Languages: Go 99.0%, Other 1.0%) Command line interface to the Turso Cloud. // Turso Cloud 的命令行界面。"
[tursocloud-cli.go/pkg.go.dev]: https://pkg.go.dev/github.com/tursodatabase/turso-cli/cmd/turso "(: go install github.com/tursodatabase/turso-cli/cmd/turso@latest) (License: MIT) (Imports: 1) (Imported by: 0) (src: gh:tursodatabase/turso-cli.git)"
[libsql-core.src/gh]: https://github.com/tursodatabase/libsql.git "(MIT) (Languages: C 85.8%, Rust 6.9%, Tcl 1.8%, JavaScript 1.5%, Java 1.2%, Shell 1.2%, Other 1.6%) libSQL is a fork of SQLite that is both Open Source, and Open Contributions. // libSQL 是 SQLite 的一个分支，它既开源，也开放贡献。 ///  libSQL is an open source, open contribution fork of SQLite, created and maintained by Turso. // libSQL 是一个开源、开放贡献的 SQLite 分叉版本，由 Turso 创建和维护。 /// We aim to evolve it to suit many more use cases than SQLite was originally designed for, and plan to use third-party OSS code wherever it makes sense. // 我们的目标是将其发展以适应比 SQLite 原始设计更多的使用场景，并计划在合适的地方使用第三方开源代码。 /// libSQL has many great features, including: // libSQL 具有许多出色的特性，包括： /// - Embedded replicas that allow you to have replicated database inside your app. // 嵌入式副本，允许您在应用程序内部拥有复制的数据库。 /// - libSQL server (. gh:tursodatabase/libsql.git@main/libsql-server) for remote SQLite access, similar to PostgreSQL or MySQL // libSQL 服务器，用于远程 SQLite 访问，类似于 PostgreSQL 或 MySQL /// - Supports Rust, JavaScript, Python, Go, and more. // 支持 Rust、JavaScript、Python、Go 等。 /// There are also various improvements and extensions to the core SQLite: // 还有对核心 SQLite 的各种改进和扩展： /// - ALTER TABLE extension for modifying column types and constraints (. gh:tursodatabase/libsql.git@main/libsql-sqlite3/doc/libsql_extensions.md#altering-columns) /// - Randomized ROWID (. gh:tursodatabase/libsql.git#issues/12) /// - WebAssembly User Defined Functions (. https://turso.tech/blog/webassembly-functions-for-your-sqlite-compatible-database-7e1ad95a2aa7) /// - Pass down SQL string to virtual table implementation (. gh:tursodatabase/libsql.git#pull/87) /// - Virtual write-ahead log interface (. gh:tursodatabase/libsql.git#pull/53) /// Compatibility with SQLite is of great importance for us. But it can mean many things. So here's our stance: // 与 SQLite 的兼容性对我们来说非常重要。但这可以指很多方面。因此，以下是我们的立场： /// - The file format: libSQL will always be able to ingest and write the SQLite file format. We would love to add extensions like encryption, and CRC that require the file to be changed. But we commit to always doing so in a way that generates standard SQLite files if those features are not used. // 文件格式：libSQL 将始终能够读取和写入 SQLite 文件格式。我们希望添加诸如加密和 CRC 等需要修改文件的扩展功能。但我们承诺，如果这些功能未被使用，始终以生成标准 SQLite 文件的方式来进行。 /// - The API: libSQL will keep 100% compatibility with the SQLite API, but we may add additional APIs. // API：libSQL 将保持与 SQLite API 的 100% 兼容性，但我们也可能添加额外的 API。 /// - Embedded: SQLite is an embedded database that can be consumed as a single .c file with its accompanying header. libSQL will always be embeddable, meaning it runs inside your process without needing a network connection. But we may change the distribution, so that object files are generated, instead of a single .c file. // 嵌入式：SQLite 是一个嵌入式数据库，可以作为一个单独的 .c 文件及其伴随的头文件来使用。libSQL 将始终是可嵌入的，这意味着它可以在你的进程中运行，而无需网络连接。但我们可能会改变分发方式，以便生成对象文件，而不是一个单独的 .c 文件。"
[libsql-lib:dart-api<core-impl>.src/gh]: https://github.com/dikatok/libsql_dart.git "(MIT) (Languages: Dart 61.5%, Rust 26.3%, CMake 4.6%, C++ 4.4%, Ruby 1.2%, Shell 0.8%, Other 1.2%) LibSQL Dart client library to interact with LibSQL/Turso database instance. // 用于与 LibSQL/Turso 数据库实例交互的 LibSQL Dart 客户端库。"
[libsql-lib:dart-api<core-impl>.flutter/pub.dev]: https://pub.dev/packages/libsql_dart "(: flutter pub add -- libsql_dart) (License: MIT) LibSQL Dart client library to interact with LibSQL/Turso database instance. // LibSQL Dart 客户端库，用于与 LibSQL/Turso 数据库实例交互。 (src: gh:dikatok/libsql_dart.git)"
[knows_by]: https://github.com/penberg/agentfs.git "At the heart of AgentFS is the agent filesystem - a complete SQLite-based storage system for agents implemented using Turso. It combines three essential components: a POSIX-like virtual filesystem for files and directories, a key-value store for agent state and context, and an audit trail tool for debugging and analysis. Everything an agent does - every file it creates, every piece of state it stores, every tool it invokes - lives in a single SQLite database file. // AgentFS 的核心是代理文件系统——一个基于 SQLite 的完整存储系统，使用 Turso 实现。它结合了三个关键组件：一个 POSIX 类似的虚拟文件系统，用于文件和目录；一个键值存储，用于代理状态和上下文；以及一个用于调试和分析的审计工具。代理所做的每一件事——创建的每个文件、存储的每段状态、调用的每个工具——都存在于一个单一的 SQLite 数据库文件中。"
