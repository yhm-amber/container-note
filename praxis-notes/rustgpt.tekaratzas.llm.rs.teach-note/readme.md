[src/gh]: https://github.com/tekaratzas/RustGPT.git "(MIT) (Languages: Rust 100.0%) An transformer based LLM. Written completely in Rust // 一个基于 transformer 的 LLM。完全使用 Rust 编写。 /// 🦀 Rust LLM from Scratch // 🦀 从头开始构建 Rust LLM /// A complete Large Language Model implementation in pure Rust with no external ML frameworks. Built from the ground up using only ndarray for matrix operations. // 一个完全使用纯 Rust 实现的大型语言模型，不依赖任何外部机器学习框架。该模型完全从底层构建，仅使用 ndarray 进行矩阵运算。 /// 🚀 What This Is // 🚀 项目简介 /// This project demonstrates how to build a transformer-based language model from scratch in Rust, including: // 本项目展示了如何在 Rust 中从头开始构建基于 Transformer 的大型语言模型，包括： /// - Pre-training on factual text completion // 在事实性文本上进行预训练 /// - Instruction tuning for conversational AI // 指令调优用于对话式 AI /// - Interactive chat mode for testing // 交互式聊天模式用于测试 /// - Full backpropagation with gradient clipping // 带梯度裁剪的全反向传播 /// - Modular architecture with clean separation of concerns // 模块化架构，职责清晰分离 /// ❌ What This Isn't // ❌ 这不是生产级的 LLM /// This is not a production grade LLM. It is so far away from the larger models. // 它与大型的模型相差甚远。 /// This is just a toy project that demonstrates how these models work under the hood. // 这只是一个玩具项目，展示了这些模型在底层是如何工作的。"
