[src/gh]: https://github.com/jacek-kurlit/pik.git "(MIT) (Languages: Rust 99.5%, Shell 0.5%) Process Interactive Kill // 交互式终止进程 /// Process Interactive Kill is a command line tool that helps to find and kill process. It works like pkill command but search is interactive. // Process Interactive Kill 是一个命令行工具，用于查找和终止进程。它类似于 pkill 命令，但搜索是交互式的。 /// Pik allows to fuzzy search processes by: // Pik 允许通过以下方式模糊搜索进程： /// - Name - No prefix is required, just type process name, for example 'firefox' // 名称 - 无需前缀，只需输入进程名称，例如 'firefox' /// - Cmd Path - Prefix search with '/', for example '/firefox' // Cmd Path - 以 '/' 开头进行前缀搜索，例如 '/firefox' /// - Arguments - Prefix search with '-' for example '-foo'. Please note that if you want to use this feature in cli you must add --, for example pik -- -foo // 参数 - 使用'-'作为前缀搜索，例如'-foo'。请注意，如果您想在 CLI 中使用此功能，必须添加 -- ，例如 pik -- -foo /// - Ports - Prefix search with ':' for example ':8080' // 端口 - 使用 ':' 进行前缀搜索，例如 ':8080' /// - Everywhere - Prefix search with '~' for example '~firefox' // 每个地方 - 使用 '~' 作为前缀进行搜索，例如 '~firefox' /// - Select exact process by id - Prefix with '!' for example '!1234' // 通过 ID 选择精确进程 - 以 '!' 开头，例如 '!1234' /// - Select process family (process + it's children) - Prefix with '@' for example '@1234' // 选择进程家族（进程及其子进程）- 以'@'为前缀，例如'@1234' /// After selecting process you can kill it with Ctrl + X // 在选中进程后，可以使用 Ctrl + X 杀死它"
[pkg.x/x-cmd]: https://x-cmd.com/install/pik "(: xd alp -- pik) (: x install -- pik)"
[pkg.cargo/crates]: https://crates.io/crates/pik "(: cargo install -- pik) (License: non-standard) (3.23 MiB)"

