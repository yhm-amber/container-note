[src/gh]: https://github.com/containers/toolbox.git "(Apache-2.0) (Languages: Go 49.0%, Shell 45.2%, Dockerfile 3.9%, Meson 1.2%, Other 0.7%) Tool for interactive command line environments on Linux // Linux 交互式命令行环境工具 /// Toolbx is a tool for Linux, which allows the use of interactive command line environments for software development and troubleshooting the host operating system, without having to install software on the host. It is built on top of Podman and other standard container technologies from OCI. // Toolbx 是一款 Linux 工具，它允许用户使用交互式命令行环境进行软件开发和主机操作系统故障排除，而无需在主机上安装任何软件。它基于 Podman 和 OCI 的其他标准容器技术构建。 /// Toolbx was previously known as Toolbox, and even before that as Fedora Toolbox. Work is in progress to update the name to Toolbx in various places. Thus this Git repository and the binary are still toolbox and the package may still be toolbox on various systems. // Toolbx 是 之前 被称为工具箱，甚至 在此之前 原名为 Fedora Toolbox。目前正在进行名称更新工作，拟将其更改为 Toolbx。 各个地方。因此，这个 Git 仓库和二进制文件仍然是 toolbox 。 而且该软件包在各种系统上可能仍然是 toolbox 。 /// If you'd like to help, here's a to-do list. // 如果你想帮忙， 以下是待办事项清单。 /// “Toolbx” with a capital “T” and without an “o” is now the canonical name. // “Toolbx”（首字母大写，不带字母“o”）现在是官方名称。 /// Toolbx environments have seamless access to the user's home directory, the Wayland and X11 sockets, networking (including Avahi and CA certificates), removable devices (like USB sticks), systemd journal, SSH agent, D-Bus, ulimits, /dev and the udev database, etc. The host file system can be accessed at /run/host. // Toolbx 环境可以无缝访问用户的主目录、Wayland 和 X11 套接字、网络（包括 Avahi 和 CA 证书）、可移动设备（如 USB 闪存盘）、systemd 日志、SSH 代理、D-Bus、ulimits、 /dev 和 udev 数据库等。主机文件系统可以通过 /run/host 访问。 /// This is particularly useful on OSTree based operating systems like Fedora CoreOS and Silverblue. The intention of these systems is to discourage installation of software on the host, and instead install software as (or in) containers — they mostly don't even have package managers like DNF or YUM. This makes it difficult to set up a development environment or troubleshoot the operating system in the usual way. // 这在 OSTree 上尤其有用。 基于操作系统的 Fedora CoreOS 和 Silverblue 等系统旨在避免在主机上安装软件，而是将软件以容器形式（或容器内）安装——它们大多甚至没有像 DNF 或 YUM 这样的包管理器。这使得搭建开发环境或以常规方式排查操作系统故障变得困难。 /// Toolbx solves this problem by providing a fully mutable container within which one can install their favorite development and troubleshooting tools, editors and SDKs. For example, it's possible to do yum install ansible without affecting the base operating system. // Toolbx 通过提供一个完全可变的容器来解决这个问题，用户可以在其中安装自己喜欢的开发和故障排除工具、编辑器和 SDK。例如，可以在不影响基础操作系统的情况下执行 yum install ansible 。 /// However, this tool doesn't require using an OSTree based system. It works equally well on Fedora Workstation and Server, and that's a useful way to incrementally adopt containerization. // 然而，这款工具并不要求使用基于 OSTree 的系统。它在 Fedora 工作站和服务器上都能完美运行，这是一种逐步采用容器化技术的有效方法。 /// The Toolbx environment is based on an OCI image. On Fedora this is the fedora-toolbox image. This image is used to create a Toolbx container that offers the interactive command line environment. // Toolbx 环境基于 OCI 镜像。在 Fedora 系统中，这是 fedora-toolbox 镜像。该镜像用于创建 Toolbx 容器，该容器提供交互式命令行环境。 /// Note that Toolbx makes no promise about security beyond what's already available in the usual command line environment on the host that everybody is familiar with. // 请注意，Toolbx 不保证在主机上常见的命令行环境中提供的安全措施之外，还能提供其他人都熟悉的安全措施。"
[site/org]: https://containertoolbx.org/ "Toolbx is a tool for Linux, which allows the use of interactive command line environments for software development and troubleshooting the host operating system, without having to install software on the host. It is built on top of Podman and other standard container technologies from OCI. // Toolbx 是一款 Linux 工具，它允许用户使用交互式命令行环境进行软件开发和主机操作系统故障排除，而无需在主机上安装任何软件。它基于 Podman 和 OCI 的其他标准容器技术构建。 /// Toolbx environments have seamless access to the user’s home directory, the Wayland and X11 sockets, networking (including Avahi and CA certificates), removable devices (like USB sticks), systemd journal, SSH agent, D-Bus, ulimits, /dev and the udev database, etc. The host file system can be accessed at /run/host. // Toolbx 环境可以无缝访问用户的主目录、Wayland 和 X11 套接字、网络（包括 Avahi 和 CA 证书）、可移动设备（如 USB 闪存盘）、systemd 日志、SSH 代理、D-Bus、ulimits、 /dev 和 udev 数据库等。主机文件系统可以通过 /run/host 访问。"
[knowsby]: https://youtu.be/Q2PrISAOtbY "How I use distrobox on Fedora Silverblue"
