[group-ietf.site/org]: https://ietf.org/ "The Internet Engineering Task Force (IETF), founded in 1986, is the premier standards development organization (SDO) for the Internet. The IETF makes voluntary standards that are often adopted by Internet users, network operators, and equipment vendors, and it thus helps shape the trajectory of the development of the Internet. But in no way does the IETF control, or even patrol, the Internet. // 互联网工程任务组（IETF）成立于 1986 年，是互联网领域首屈一指的标准制定组织（SDO）。IETF 制定自愿性标准，这些标准通常被互联网用户、网络运营商和设备供应商采用，从而有助于塑造互联网的发展方向。但 IETF 绝不控制或监管互联网。 /// “the overall goal of the IETF is to make the Internet work better.” // “IETF 的总体目标是让互联网更好地运行。” /// “Its mission is to produce high quality, relevant technical and engineering documents that influence the way people design, use, and manage the Internet in such a way as to make the Internet work better. These documents include protocol standards, best current practices, and informational documents of various kinds.” // “其使命是制作高质量、相关的技术和工程文档，从而影响人们设计、使用和管理互联网的方式，使互联网更好地运行。这些文档包括协议标准、最佳实践和各种类型的信息文档。” /// The Mission Statement further states that the Internet isn't value-neutral, and neither is the IETF. The IETF wants the Internet to be useful for communities that share our commitment to openness and fairness. The IETF embraces technical concepts such as decentralized control, edge-user empowerment and sharing of resources, because those concepts resonate with the core values of the IETF community. These concepts have little to do with the technology that's possible, and much to do with the technology that the IETF chooses to create. // 使命宣言进一步指出，互联网并非价值中立，IETF 亦是如此。IETF 希望互联网能够服务于那些与我们一样秉持开放和公平理念的社群。IETF 拥抱去中心化控制、边缘用户赋能和资源共享等技术理念，因为这些理念与 IETF 社群的核心价值观相契合。这些理念与现有技术本身关系不大，而与 IETF 选择开发的技术密切相关。 /// Participants // 参与者 /// There is no membership in the IETF. Anyone can participate by signing up to a working group mailing list (more on that below), or registering for an IETF meeting. All IETF participants are considered volunteers and expected to participate as individuals, including those paid to participate. // IETF 没有会员制度。任何人都可以通过注册加入工作组邮件列表（详见下文）或报名参加 IETF 会议来参与。所有 IETF 参与者都被视为志愿者，并应以个人身份参与，包括那些获得报酬的参与者。 /// The IETF welcomes all interested individuals and participants come from all over the world and from many different parts of the Internet industry. In any one year, over 7000 people actively participate in the IETF either by authoring a document, engaging in a mailing list discussion, or attending a meeting. // IETF 欢迎所有感兴趣的个人和组织，参与者来自世界各地以及互联网行业的各个领域。每年有超过 7000 人积极参与 IETF 的工作，他们或撰写文档，或参与邮件列表讨论，或出席会议。 /// The only fee the IETF charges is for registering for an IETF meeting, with options in place to prevent that fee from becoming a barrier to participation. // IETF 只收取参加 IETF 会议的注册费，并设有相关措施防止该费用成为参与的障碍。"
[group-ietf.home/gh]: https://github.com/ietf/ "Internet Engineering Task Force (IETF) // 互联网工程任务组（IETF） /// Various organizational repos of the IETF."
[group-ietf.mastodon]: https://mastodon.online/@ietf
[group-ietf.twitter]: https://twitter.com/ietf
[spec-rfc9000.site/org]: https://rfc-editor.org/rfc/rfc9000.html "RFC 9000 /// QUIC: A UDP-Based Multiplexed and Secure Transport // QUIC：一种基于 UDP 的多路复用安全传输协议 /// This document defines the core of the QUIC transport protocol. QUIC provides applications with flow-controlled streams for structured communication, low-latency connection establishment, and network path migration. QUIC includes security measures that ensure confidentiality, integrity, and availability in a range of deployment circumstances. Accompanying documents describe the integration of TLS for key negotiation, loss detection, and an exemplary congestion control algorithm. // 本文档定义了 QUIC 传输协议的核心。QUIC 为应用程序提供流控制数据流，用于结构化通信、低延迟连接建立和网络路径迁移。QUIC 包含安全措施，确保在各种部署环境下数据的机密性、完整性和可用性。相关文档描述了 TLS 的集成，用于密钥协商、丢包检测以及一个示例性的拥塞控制算法。 /// This is an Internet Standards Track document. // 这是一份互联网标准跟踪文件。 /// This document is a product of the Internet Engineering Task Force (IETF). It represents the consensus of the IETF community. It has received public review and has been approved for publication by the Internet Engineering Steering Group (IESG). Further information on Internet Standards is available in Section 2 of RFC 7841. // 本文档由互联网工程任务组 (IETF) 编写，代表了 IETF 社区的共识。它已通过公开审查，并经互联网工程指导组 (IESG) 批准发布。有关互联网标准的更多信息，请参阅 RFC 7841 第 2 节。 /// QUIC is a secure general-purpose transport protocol. This document defines version 1 of QUIC, which conforms to the version-independent properties of QUIC defined in [QUIC-INVARIANTS]. // QUIC 是一种安全的通用传输协议。本文档定义了 QUIC 的版本 1，它符合 [ QUIC-INVARIANTS ] 中定义的 QUIC 的版本无关属性。¶ /// QUIC is a connection-oriented protocol that creates a stateful interaction between a client and server. // QUIC 是一种面向连接的协议，它在客户端和服务器之间创建有状态的交互。 /// The QUIC handshake combines negotiation of cryptographic and transport parameters. QUIC integrates the TLS handshake [TLS13], although using a customized framing for protecting packets. The integration of TLS and QUIC is described in more detail in [QUIC-TLS]. The handshake is structured to permit the exchange of application data as soon as possible. This includes an option for clients to send data immediately (0-RTT), which requires some form of prior communication or configuration to enable. // QUIC 握手结合了加密参数和传输参数的协商。QUIC 集成了 TLS 握手 [ TLS13 ] ，但使用定制的帧结构来保护数据包。TLS 和 QUIC 的集成在 [ QUIC-TLS ] 中有更详细的描述。握手过程旨在尽快交换应用程序数据。这包括客户端立即发送数据（0-RTT）的选项，但启用此选项需要事先进行某种形式的通信或配置。 /// Endpoints communicate in QUIC by exchanging QUIC packets. Most packets contain frames, which carry control information and application data between endpoints. QUIC authenticates the entirety of each packet and encrypts as much of each packet as is practical. QUIC packets are carried in UDP datagrams [UDP] to better facilitate deployment in existing systems and networks. // 端点之间通过交换 QUIC 数据包进行 QUIC 通信。大多数数据包包含 帧，用于在端点之间传输控制信息和应用程序数据。 QUIC 对每个数据包进行全面身份验证，并尽可能多地加密每个数据包的内容。 数据包尽可能地以可行方式传输。QUIC 数据包通过 UDP 数据报承载。 [ UDP ] 以便更好地在现有系统和网络中部署。 /// Application protocols exchange information over a QUIC connection via streams, which are ordered sequences of bytes. Two types of streams can be created: bidirectional streams, which allow both endpoints to send data; and unidirectional streams, which allow a single endpoint to send data. A credit-based scheme is used to limit stream creation and to bound the amount of data that can be sent. // 应用层协议通过 QUIC 连接以数据流（有序的字节序列）的形式交换信息。可以创建两种类型的数据流：双向数据流，允许两个端点都发送数据；以及单向数据流，只允许单个端点发送数据。采用基于信用的机制来限制数据流的创建，并限制可发送的数据量。 /// QUIC provides the necessary feedback to implement reliable delivery and congestion control. An algorithm for detecting and recovering from loss of data is described in Section 6 of [QUIC-RECOVERY]. QUIC depends on congestion control to avoid network congestion. An exemplary congestion control algorithm is described in Section 7 of [QUIC-RECOVERY]. // QUIC 提供必要的反馈以实现可靠的数据传输和拥塞控制。 [ QUIC-RECOVERY ] 第 6 节描述了一种用于检测和恢复数据丢失的算法。QUIC 依赖拥塞控制来避免网络拥塞。 [ QUIC-RECOVERY ] 第 7 节描述了一个示例性的拥塞控制算法。 /// QUIC connections are not strictly bound to a single network path. Connection migration uses connection identifiers to allow connections to transfer to a new network path. Only clients are able to migrate in this version of QUIC. This design also allows connections to continue after changes in network topology or address mappings, such as might be caused by NAT rebinding. // QUIC 连接并非严格绑定到单一网络路径。连接迁移利用连接标识符允许连接转移到新的网络路径。在此版本的 QUIC 中，只有客户端能够进行迁移。这种设计还允许连接在网络拓扑或地址映射发生变化后继续保持，例如由 NAT 重绑定引起的变化。 /// Once established, multiple options are provided for connection termination. Applications can manage a graceful shutdown, endpoints can negotiate a timeout period, errors can cause immediate connection teardown, and a stateless mechanism provides for termination of connections after one endpoint has lost state. // 连接建立后，提供了多种连接终止选项。应用程序可以管理优雅关闭，端点可以协商超时时间，错误可以导致连接立即断开，而无状态机制则允许在某个端点丢失状态后终止连接。"
[intro.blog/cf]: https://blog.cloudflare.com/the-road-to-quic/ "The Road to QUIC // 通往 QUIC 之路 /// QUIC (Quick UDP Internet Connections) is a new encrypted-by-default Internet transport protocol, that provides a number of improvements designed to accelerate HTTP traffic as well as make it more secure, with the intended goal of eventually replacing TCP and TLS on the web. In this blog post we are going to outline some of the key features of QUIC and how they benefit the web, and also some of the challenges of supporting this radical new protocol. // QUIC（快速 UDP 互联网连接）是一种默认加密的新型互联网传输协议，它提供了一系列旨在加速 HTTP 流量并提高其安全性的改进，最终目标是取代网络上的 TCP 和 TLS。在本篇博文中，我们将概述 QUIC 的一些关键特性及其对网络的益处，以及支持这一全新协议所面临的一些挑战。 /// There are in fact two protocols that share the same name: “Google QUIC” (“gQUIC” for short), is the original protocol that was designed by Google engineers several years ago, which, after years of experimentation, has now been adopted by the IETF (Internet Engineering Task Force) for standardization. // 事实上，有两个协议同名：“Google QUIC”（简称“gQUIC”）是几年前由谷歌工程师设计的原始协议，经过多年的实验，现在已被 IETF （互联网工程任务组）采纳为标准。 /// “IETF QUIC” (just “QUIC” from now on) has already diverged from gQUIC quite significantly such that it can be considered a separate protocol. From the wire format of the packets, to the handshake and the mapping of HTTP, QUIC has improved the original gQUIC design thanks to open collaboration from many organizations and individuals, with the shared goal of making the Internet faster and more secure. // “IETF QUIC”（以下简称“QUIC”）与 gQUIC 的差异已相当显著，可以被视为一个独立的协议。从数据包的传输格式到握手过程以及 HTTP 的映射，QUIC 在众多组织和个人的开放协作下，对原 gQUIC 的设计进行了改进，其共同目标是让互联网更快、更安全。 /// So, what are the improvements QUIC provides? // 那么，QUIC 带来了哪些改进？ /// - Built-in security (and performance) // 内置安全性（和性能） ///: (like TCP TLS-1.3) But QUIC goes even further, and also encrypts additional connection metadata that could be abused by middle-boxes to interfere with connections. For example packet numbers could be used by passive on-path attackers to correlate users activity over multiple network paths when connection migration is employed (see below). By encrypting packet numbers QUIC ensures that they can't be used to correlate activity by any entity other than the end-points in the connection. // (就像 TCP 的 TLS-1.3) 但 QUIC 更进一步，它还会加密额外的连接元数据，防止中间设备滥用这些元数据来干扰连接。例如，当启用连接迁移时（见下文），被动路径攻击者可以利用数据包编号来关联用户在多个网络路径上的活动。通过加密数据包编号，QUIC 确保除了连接中的端点之外，任何其他实体都无法利用这些编号来关联活动。 /// - Head-of-line blocking // 队伍首位阻挡 ///: (like HTTP/1 to HTTP/2 but) QUIC goes a bit deeper and provides first class support for multiplexing such that different HTTP streams can in turn be mapped to different QUIC transport streams, but, while they still share the same QUIC connection so no additional handshakes are required and congestion state is shared, QUIC streams are delivered independently, such that in most cases packet loss affecting one stream doesn't affect others. // (正如 HTTP/1 到 HTTP/2 但) QUIC 更进一步，为多路复用提供了一流的支持，使得不同的 HTTP 流可以依次映射到不同的 QUIC 传输流，虽然它们仍然共享同一个 QUIC 连接，因此不需要额外的握手，并且拥塞状态是共享的，但 QUIC 流是独立传输的，因此在大多数情况下，影响一个流的丢包不会影响其他流。"
[wg.site/org]: https://quicwg.org/ "The IETF QUIC Working Group produced QUIC version 1 — a UDP-based, stream-multiplexing, encrypted transport protocol. The protocol itself is published as RFC 9000, and there are other related RFCs of note, see below. // IETF QUIC 工作组制定了 QUIC 版本 1——一种基于 UDP 的流复用加密传输协议。该协议本身以 RFC 9000 的形式发布。 还有其他值得注意的相关 RFC，请参见下文。 /// We are now chartered to be the focal point for any QUIC-related work in the IETF. Our work covers maintenance and evolution of published specifications, the deployability of QUIC, and new extensions to QUIC. // 我们现在被指定为 IETF 中所有与 QUIC 相关工作的中心。我们的工作涵盖已发布规范的维护和演进、QUIC 的可部署性以及 QUIC 的新扩展。 /// The QUIC Working Group originated HTTP/3, the mapping of HTTP to QUIC, and the QPACK header compression scheme. These are now maintained by the HTTP Working Group. // QUIC 工作组制定了 HTTP/3、HTTP 到 QUIC 的映射以及 QPACK 头部压缩方案。这些现在由 HTTP 工作组维护。"
[tester.web/cloudflare]: https://cloudflare-quic.com/ "Does my browser support HTTP/3 & QUIC? // 我的浏览器支持 HTTP/3 和 QUIC 吗？ /// When loading this page from Cloudflare's edge network, your browser used HTTP/1.1. // 当从 Cloudflare 的边缘网络加载此页面时，您的浏览器使用了 HTTP/1.1 。 /// This page is HTTP/3 & QUIC enabled. Try reloading a few times to spring it into action. // 此页面已启用 HTTP/3 和 QUIC 协议。请尝试刷新几次页面以使其生效。 /// Not all HTTP clients have HTTP/3 & QUIC support configured. See our documentation for more information about how to check and configure your favorite client such as Chrome, Firefox or curl. // 并非所有 HTTP 客户端都配置了 HTTP/3 和 QUIC 支持。请参阅我们的文档 ，了解如何检查和配置您常用的客户端，例如 Chrome、 Firefox 或 curl。"

