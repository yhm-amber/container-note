[meta.src/gh]: https://github.com/tidymodels/tidymodels.git "(MIT) (Languages: R 100.0%) Easily install and load the tidymodels packages // 轻松安装和加载 tidymodels 包 /// tidymodels is a “meta-package” for modeling and statistical analysis that shares the underlying design philosophy, grammar, and data structures of the tidyverse. // tidymodels 是一个用于建模和统计分析的“元包”，它共享 tidyverse 的底层设计理念、语法和数据结构。 /// It includes a core set of packages that are loaded on startup: // 它包括一组在启动时加载的核心包： /// - broom takes the messy output of built-in functions in R, such as lm, nls, or t.test, and turns them into tidy data frames. // broom 将 R 内置函数的杂乱输出，如 lm 、 nls 或 t.test ，转换为整洁的数据框。 /// - dials has tools to create and manage values of tuning parameters. // dials 提供创建和管理调参值工具。 /// - dplyr contains a grammar for data manipulation. // dplyr 包含数据操作语法。 /// - ggplot2 implements a grammar of graphics. // ggplot2 实现了一种图形语法。 /// - infer is a modern approach to statistical inference. // infer 是一种现代的统计推断方法。 /// - parsnip is a tidy, unified interface to creating models. // parsnip 是创建模型的整洁、统一的接口。 /// - purrr is a functional programming toolkit. // purrr 是一个函数式编程工具包。 /// - recipes is a general data preprocessor with a modern interface. It can create model matrices that incorporate feature engineering, imputation, and other help tools. // recipes 是一个具有现代界面的通用数据预处理程序。它可以创建包含特征工程、插补和其他辅助工具的模型矩阵。 /// - rsample has infrastructure for resampling data so that models can be assessed and empirically validated. // rsample 具有重采样数据的基础设施，以便评估和经验验证模型。 /// - tibble has a modern re-imagining of the data frame. // tibble 对数据框进行了现代再想象。 /// - tune contains the functions to optimize model hyper-parameters. // tune 包含用于优化模型超参数的函数。 /// - workflows has methods to combine pre-processing steps and models into a single object. // workflows 具有将预处理步骤和模型合并为单个对象的方法。 /// - yardstick contains tools for evaluating models (e.g. accuracy, RMSE, etc.). // yardstick 包含用于评估模型（例如准确率、均方根误差等）的工具。"
[meta.site]: https://tidymodels.tidymodels.org/
[meta.package/cran]: https://cran.r-project.org/web/packages/tidymodels/ "(Version: 	1.3.0) (Depends: 	R (≥ 3.6)) (Imports: 	broom (≥ 1.0.7), cli (≥ 3.6.4), conflicted (≥ 1.2.0), dials (≥ 1.4.0), dplyr (≥ 1.1.4), ggplot2 (≥ 3.5.1), hardhat (≥ 1.4.1), infer (≥ 1.0.7), modeldata (≥ 1.4.0), parsnip (≥ 1.3.0), purrr (≥ 1.0.4), recipes (≥ 1.1.1), rlang (≥ 1.1.5), rsample (≥ 1.2.1), rstudioapi (≥ 0.17.1), tibble (≥ 3.2.1), tidyr (≥ 1.3.1), tune (≥ 1.3.0), workflows (≥ 1.2.0), workflowsets (≥ 1.1.0), yardstick (≥ 1.3.2)) (Suggests: 	covr, glue, knitr, rmarkdown, testthat (≥ 3.0.0), xml2) (Published: 	2025-02-21) (DOI: 	10.32614/CRAN.package.tidymodels) (Author: 	Max Kuhn ORCID iD [aut, cre], Hadley Wickham [aut], Posit Software, PBC [cph, fnd]) (Maintainer: 	Max Kuhn <max at posit.co>) (License: 	MIT + file LICENSE) (NeedsCompilation: 	no) tidymodels: Easily Install and Load the 'Tidymodels' Packages // tidymodels: 轻松安装和加载 'Tidymodels' 包 /// The tidy modeling \"verse\" is a collection of packages for modeling and statistical analysis that share the underlying design philosophy, grammar, and data structures of the tidyverse. // 整洁建模“领域”是一组用于建模和统计分析的软件包，它们共享 tidyverse 的基础设计理念、语法和数据结构。"

[site]: https://tidymodels.org/ "The tidymodels framework is a collection of packages for modeling and machine learning using tidyverse principles. // Tidymodels 框架是一组用于使用 tidyverse 原则进行建模和机器学习的软件包。"

[parsnip.src/gh]: https://github.com/tidymodels/parsnip.git "(MIT) (Languages: R 100.0%) A tidy unified interface to models // 一个整洁统一的模型界面"
[parsnip.site]: https://parsnip.tidymodels.org/ "parsnip // 洋荒菜 /// The goal of parsnip is to provide a tidy, unified interface to models that can be used to try a range of models without getting bogged down in the syntactical minutiae of the underlying packages. // Parsnip 的目标是提供一个整洁、统一的接口，用于使用各种模型，而无需陷入底层包的语法细节。 /// The engine (e.g. Spark, ranger, ...) can be easily changed. // 引擎 (如 Spark, ranger, ...) 可以轻松更改。"
[parsnip.package/cran]: https://cran.r-project.org/web/packages/parsnip/ "(Version: 	1.3.1) (Depends: 	R (≥ 3.6)) (Imports: 	cli, dplyr (≥ 1.1.0), generics (≥ 0.1.2), ggplot2, globals, glue, hardhat (≥ 1.4.1), lifecycle, magrittr, pillar, prettyunits, purrr (≥ 1.0.0), rlang (≥ 1.1.0), sparsevctrs (≥ 0.2.0), stats, tibble (≥ 2.1.1), tidyr (≥ 1.3.0), utils, vctrs (≥ 0.6.0), withr) (Suggests: 	bench, C50, covr, dials (≥ 1.1.0), earth, ggrepel, keras, kernlab, kknn, knitr, LiblineaR, MASS, Matrix, methods, mgcv, modeldata, nlme, prodlim, ranger (≥ 0.12.0), remotes, rmarkdown, rpart, sparklyr (≥ 1.0.0), survival, tensorflow, testthat (≥ 3.0.0), xgboost (≥ 1.5.0.1)) (Published: 	2025-03-12) (DOI: 	10.32614/CRAN.package.parsnip) (Author: 	Max Kuhn [aut, cre], Davis Vaughan [aut], Emil Hvitfeldt [ctb], Posit Software, PBC [cph, fnd]) (Maintainer: 	Max Kuhn <max at posit.co>) (License: 	MIT + file LICENSE) (NeedsCompilation: 	no) parsnip: A Common API to Modeling and Analysis Functions // 洋姜：建模和分析函数的通用 API /// A common interface is provided to allow users to specify a model without having to remember the different argument names across different functions or computational engines (e.g. 'R', 'Spark', 'Stan', 'H2O', etc). // 一个通用的接口允许用户在不记住不同函数或计算引擎（例如 'R', 'Spark', 'Stan', 'H2O' 等）中不同参数名称的情况下指定模型。"

[parsnip:models/site]: https://tidymodels.org/find/parsnip/ "Search parsnip models // 搜索胡萝卜模型 /// Find model types, engines, and arguments to fit and predict in the tidymodels framework. // 查找适合在 tidymodels 框架中进行拟合和预测的模型类型、引擎和参数。"


[corrr.src/gh]: https://github.com/tidymodels/corrr.git "(YEAR: 2016, COPYRIGHT HOLDER: Simon Jackson) (Languages: R 54.2%, HTML 45.8%) Explore correlations in R // 探索 R 中的相关性"
[corrr.site]: https://corrr.tidymodels.org/ "corrr is a package for exploring correlations in R. It focuses on creating and working with data frames of correlations (instead of matrices) that can be easily explored via corrr functions or by leveraging tools like those in the tidyverse. // corrr 是一个用于在 R 中探索相关性的包。它专注于创建和使用相关性数据框（而不是矩阵），这些数据框可以通过 corrr 函数或利用 tidyverse 等工具轻松探索。"

[corrr.package/cran]: https://cran.r-project.org/web/packages/corrr/ "(Version: 	0.4.4) (Depends: 	R (≥ 3.4)) (Imports: 	dplyr (≥ 1.0.0), ggplot2 (≥ 2.2.0), ggrepel (≥ 0.6.5), glue (≥ 1.4.2), purrr (≥ 0.2.2), rlang (≥ 0.4.0), seriation (≥ 1.2-0), tibble (≥ 2.0)) (Suggests: 	covr, DBI, dbplyr (≥ 1.2.1), knitr (≥ 1.13), rmarkdown (≥ 0.9.6), RSQLite, sparklyr (≥ 0.9), testthat (≥ 3.0.0)) (Published: 	2022-08-16) (DOI: 	10.32614/CRAN.package.corrr) (Author: 	Max Kuhn [aut, cre], Simon Jackson [aut], Jorge Cimentada [aut]) (Maintainer: 	Max Kuhn <max at rstudio.com>) (License: 	MIT + file LICENSE) (NeedsCompilation: 	no) corrr: Correlations in R // corrr: R 中的相关性 /// A tool for exploring correlations. It makes it possible to easily perform routine tasks when exploring correlation matrices such as ignoring the diagonal, focusing on the correlations of certain variables against others, or rearranging and visualizing the matrix in terms of the strength of the correlations. // 一个用于探索相关性的工具。它使得在探索相关性矩阵时轻松执行常规任务成为可能，例如忽略对角线、关注某些变量与其他变量的相关性，或根据相关性的强度重新排列和可视化矩阵。"









[knows.by]: https://spark.posit.co/guides/tidymodels.html "tidymodels is a collection of packages for modeling and machine learning. Just like sparklyr, tidymodels uses tidyverse principles. // tidymodels 是用于建模和机器学习的包集合。就像 sparklyr 一样， tidymodels 使用 tidyverse 原则。"



