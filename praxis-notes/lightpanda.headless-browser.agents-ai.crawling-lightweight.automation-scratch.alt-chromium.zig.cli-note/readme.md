[src/gh]: https://github.com/lightpanda-io/browser.git "(AGPL-3.0) (Languages: Zig 75.0%, HTML 23.7%, Rust 0.8%, JavaScript 0.3%, Makefile 0.1%, Dockerfile 0.1%) Lightpanda: the headless browser designed for AI and automation // Lightpanda：专为人工智能和自动化设计的无头浏览器 /// Lightpanda Browser // Lightpanda 浏览器 /// The headless browser built from scratch for AI agents and automation. // 专为人工智能代理和自动化而从零开始构建的无头浏览器。 /// Not a Chromium fork. Not a WebKit patch. A new browser, written in Zig. // 不是 Chromium 的分支，也不是 WebKit 的补丁，而是一款用 Zig 编写的全新浏览器。 /// - Dump a URL // 输出一个 URL 的页面内容 ///: (: lightpanda fetch --obey-robots --dump html --log-format pretty --log-level info https://demo-browser.lightpanda.io/campfire-commerce/ # You can use --dump markdown to convert directly into markdown. --wait-until, --wait-ms, --wait-selector and --wait-script are available to adjust waiting time before dump. // 您可以使用 --dump markdown 直接转换为 markdown 格式。 --wait-until 、 --wait-ms 、 --wait-selector 和 --wait-script 可用于调整转储前的等待时间。) /// - Start a CDP server // 启动 CDP 服务器 ///: (: lightpanda serve --obey-robots --log-format pretty --log-level info --host 127.0.0.1 --port 9222 # Once the CDP server started, you can run a Puppeteer script by configuring the browserWSEndpoint. // CDP 服务器启动后，您可以通过配置 browserWSEndpoint 来运行 Puppeteer 脚本。) Example Puppeteer script (: import puppeteer from 'puppeteer-core' ;: const browser = await puppeteer.connect({ browserWSEndpoint: 'ws://127.0.0.1:9222' }) // use browserWSEndpoint to pass the Lightpanda's CDP server address. // The rest of your script remains the same.) (: const context = await browser.createBrowserContext() ;: const frame = await context.newPage() ;: await frame.goto('https://demo-browser.lightpanda.io/amiibo/', {waitUntil: 'networkidle0'}) // Dump all the links from the frame ;: const links = await frame.evaluate(() => { return Array.from(document.querySelectorAll('a')).map(row => { return row.getAttribute('href') }) }) ;: console.log(links) ;: await frame.close() ;: await context.close() ;: await browser.disconnect() // fin) ///; Docker: (: docker run -d --name lightpanda -p 127.0.0.1:9222:9222 -- docker.io/lightpanda/browser:nightly) /// ## Why Lightpanda?  为什么选择 Lightpanda？ /// ### Javascript execution is mandatory for the modern web // 对于现代网络来说，JavaScript 执行是必不可少的。 /// Simple HTTP requests used to be enough for web automation. That's no longer the case. Javascript now drives most of the web: // 过去，简单的 HTTP 请求就足以实现 Web 自动化。但现在情况已经不同了。如今，JavaScript 驱动着 Web 的大部分功能： /// - Ajax, Single Page Apps, infinite loading, instant search // Ajax、单页应用、无限加载、即时搜索 /// - JS frameworks: React, Vue, Angular, and others // JS 框架：React、Vue、Angular 等 /// ### Chrome is not the right tool // Chrome 不是合适的工具 /// Running a full desktop browser on a server works, but it does not scale well. Chrome at hundreds or thousands of instances is expensive: // 在服务器上运行完整的桌面浏览器是可行的，但扩展性不佳。运行数百甚至数千个 Chrome 实例的成本很高： /// - Heavy on RAM and CPU // 占用大量内存和 CPU 资源 /// - Hard to package, deploy, and maintain at scale // 难以大规模打包、部署和维护。 /// - Many features are not necessary in headless made // 无头模式下，许多功能并非必要。 /// ### Lightpanda is built for performance // Lightpanda 专为高性能而设计 /// Supporting Javascript with real performance meant building from scratch rather than forking Chromium: // 要真正实现高性能的 Javascript 支持，就意味着要从零开始构建，而不是对 Chromium 进行分支： /// - Not based on Chromium, Blink, or WebKit // 并非基于 Chromium、Blink 或 WebKit。 /// - Written in Zig, a low-level language with explicit memory control // 用 Zig 编写，Zig 是一种具有显式内存控制的底层语言。 /// - No graphical rendering engine // 没有图形渲染引擎"
[site/io]: https://lightpanda.io/ "The first browser // 第一个浏览器 /// for machines, not humans // 为机器，而非为人类 /// A fast, lightweight browser engine for automation, crawling and AI agents. JavaScript execution, no graphical rendering. // 一款快速轻量的浏览器引擎，适用于自动化、爬虫和人工智能代理。它执行 JavaScript 代码，无需图形渲染。 /// - Execution time - 9x // 执行时间 - 9倍 /// - Memory peak - 16x // 记忆高峰 - 16倍 ///  [LLMs] [AI Agents] [Machines] [Crawlers] ///: deserve a 100x better web browser // 值得拥有比现在好100倍的浏览器 /// - Built from scratch // 从零开始构建 ///: Not reliant on existing browsers. // 不依赖于现有浏览器。 /// - Created with Zig // 使用 Zig 创建 ///: Developed in a low-level system programming language designed for performance and efficiency. // 采用专为高性能和高效率而设计的底层系统编程语言开发。 /// - Focused and opinionated // 专注且有主见 ///: Purpose-built for headless operation, without rendering overhead. // 专为无头操作而设计，无渲染开销。 /// Integrated into leading agent frameworks // 集成到领先的代理框架中 ///: [Vercel: gh:vercel-labs/agent-browser.git] [DUST: gh:dust-tt/browse.git] [OpenClaw: clawhub.ai:krichprollsch/lightpanda-browser] [Trigger.dev: gh:triggerdotdev/trigger.dev.git] /// ## Web automation is being held back // 网络自动化受到阻碍 ///: Web browsers lack the speed, durability, and security for AI workloads. // 网络浏览器在速度、稳定性和安全性方面都无法满足人工智能工作负载的需求。 /// ### Chrome wasn’t built for the cloud // Chrome 浏览器并非为云端而设计 /// Running Chrome in the cloud is expensive and slow. // 在云端运行 Chrome 浏览器成本高昂且速度缓慢。 /// You’re paying for decades of code built for people, not systems. // 你付钱买的是几十年前为人编写的代码，而不是为系统编写的代码。 /// ### Browsers are slow and brittle // 浏览器运行缓慢且不稳定 /// Real-time automation shouldn't wait for browsers. // 实时自动化不应该等待浏览器响应。 /// Chrome takes seconds to start, which multiply across every process. // Chrome 启动需要几秒钟，这会放大到每个进程的启动时间。 /// ### Shared state is a security risk // 共享状态存在安全风险 /// Automation needs isolated environments. // 自动化需要隔离的环境。 /// Traditional browsers carry persistent state, cookies & sessions across tasks. // 传统浏览器会在任务之间保存持久状态、cookie 和会话。"
[cli.oci/dockerhub]: https://hub.docker.com/r/lightpanda/browser "(Size: 64.7 MB) (: docker run -d --name lightpanda -p 127.0.0.1:9222:9222 -- docker.io/lightpanda/browser:nightly # as cdp server) Lightpanda open-source browser made for headless usage // Lightpanda 是一款专为无头模式设计的开源浏览器。 (src: gh:lightpanda-io/browser.git)"
