[src/gh]: https://github.com/dora-rs/dora.git "(Apache-2.0) (Languages: Rust 93.2%, Python 4.6%, C 1.0%, Shell 0.4%, C++ 0.3%, PowerShell 0.3%, Other 0.2%) DORA (Dataflow-Oriented Robotic Architecture) is middleware designed to streamline and simplify the creation of AI-based robotic applications. It offers low latency, composable, and distributed dataflow capabilities. Applications are modeled as directed graphs, also referred to as pipelines. // DORA（面向数据流的机器人架构）是一种中间件，旨在简化 AI 驱动的机器人应用的开发。它提供低延迟、可组合和分布式数据流功能。应用被建模为有向图，也称为管道。 /// Highlights // 亮点 /// - 🚀 dora-rs is a framework to run realtime multi-AI and multi-hardware applications. // 🚀 dora-rs 是一个用于运行实时多 AI 和多硬件应用的框架。 /// - 🦀 dora-rs internals are 100% Rust making it extremely fast compared to alternative such as being ⚡️ 10-17x faster than ros2. // 🦀 dora-rs 内部完全使用 Rust 编写，使其与其他替代方案相比速度极快，例如比 ros2 快 10-17 倍。 /// - ❇️ Includes a large set of pre-packaged nodes for fast prototyping which simplifies integration of hardware, algorithms, and AI models. // ❇️ 包含一套大型预打包节点，用于快速原型设计，简化了硬件、算法和 AI 模型的集成。 /// ## What is Dora? And what features does Dora offer? // 什么是 Dora？Dora 提供了哪些功能？ /// Dataflow-Oriented Robotic Architecture (dora-rs) is a framework that makes creation of robotic applications fast and simple. // 数据流导向机器人架构（ dora-rs ）是一个框架，它使机器人应用程序的创建变得快速和简单。 /// dora-rs implements a declarative dataflow paradigm where tasks are split between nodes isolated as individual processes. // dora-rs 实现了一种声明式数据流范式，其中任务在节点之间分割，每个节点作为独立的进程隔离。 /// The dataflow paradigm has the advantage of creating an abstraction layer that makes robotic applications modular and easily configurable. // 数据流范式具有创建抽象层的优势，使机器人应用程序模块化且易于配置。 /// ### TCP Communication and Shared Memory // TCP 通信和共享内存 /// Communication between nodes is handled with shared memory on a same machine and TCP on distributed machines. Our shared memory implementation tracks messages across processes and discards them when obsolete. Shared memory slots are cached to avoid new memory allocation. // 节点之间的通信在同一台机器上使用共享内存，在分布式机器上使用 TCP。我们的共享内存实现跟踪跨进程的消息，并在它们过时时将其丢弃。共享内存槽被缓存以避免新的内存分配。 /// ### Arrow Message Format // 箭头消息格式 /// Nodes communicate with Apache Arrow Data Format. // 节点使用 Apache Arrow 数据格式进行通信。 /// Apache Arrow is a universal memory format for flat and hierarchical data. The Arrow memory format supports zero-copy reads for lightning-fast data access without serialization overhead. It defines a C data interface without any build-time or link-time dependency requirement, that means that dora-rs has no compilation step beyond the native compiler of your favourite language. // Apache Arrow 是一种通用的内存格式，适用于扁平化和层次化数据。Arrow 内存格式支持零拷贝读取，实现闪电般快速的数据访问，无需序列化开销。它定义了一个 C 数据接口，没有任何编译时或链接时依赖要求，这意味着 dora-rs 除了你最喜欢的语言的本地编译器之外，没有额外的编译步骤。 /// ### Opentelemetry /// dora-rs uses Opentelemetry to record all your logs, metrics and traces. This means that the data and telemetry can be linked using a shared abstraction. // dora-rs 使用 OpenTelemetry 来记录所有您的日志、指标和跟踪。这意味着数据和遥测数据可以通过一个共享的抽象进行关联。 /// Opentelemetry is an open source observability standard that makes dora-rs telemetry collectable by most backends such as elasticsearch, prometheus, Datadog... // OpenTelemetry 是一个开源的可观测性标准，使得 dora-rs 遥测数据可以被大多数后端（如 Elasticsearch、Prometheus、Datadog 等）收集。 /// Opentelemetry is language independent, backend agnostic, and easily collect distributed data, making it perfect for dora-rs applications. // OpenTelemetry 是语言无关的、后端无关的，并且可以轻松收集分布式数据，这使得它非常适合 dora-rs 应用程序。 /// ### ROS2 Bridge /// Note: this feature is marked as unstable. // 注意：此功能被标记为不稳定。 /// - Compilation Free Message passing to ROS 2 // 编译自由的 ROS 2 消息传递 /// - Automatic conversion ROS 2 Message <-> Arrow Array // 自动转换 ROS 2 消息 <-> Arrow 数组"
[cli-rs.cargo/crates]: https://crates.io/crates/dora-cli "(: cargo install -- dora-cli) (Apache-2.0) (3.0K SLoC) (80.6 KB) `dora` goal is to be a low latency, composable, and distributed data flow. // `dora` 的目标是成为一个低延迟、可组合、分布式的数据流。 (src: gh:dora-rs/dora.git)"
[lib-rs.cargo/crates]: https://crates.io/crates/dora-rs "(: cargo install -- dora-rs) (Apache-2.0) (3 SLoC) (778 B) Dataflow Oriented Robotic Architecture // 面向数据流的机器人架构 (src: gh:dora-rs/dora.git)"
[cli-py.pip/pypi]: https://pypi.org/project/dora-rs-cli/ "(: pip install -- dora-rs-cli) (License: MIT) (Requires: Python >=3.8)  (src: gh:dora-rs/dora.git)"
[lib-py.pip/pypi]: https://pypi.org/project/dora-rs/ "(: pip install -- dora-rs) (License: MIT) (Requires: Python >=3.7)  (src: gh:dora-rs/dora.git)"
[site/ai]: https://dora-rs.ai/ "🚙🤖💻 Making robotic applications fast and simple! /// Features // 特性 /// - ⭐ Zero Overhead // ⭐ 零开销 ///: Transfer message with zero copy on shared memory! We use Arrow and our own shared memory daemon to make lightspeed communication on a single machine. // 在共享内存上进行零拷贝传输消息！我们使用 Arrow 和自有的共享内存守护进程，实现单机上的极速通信。 /// - 🤯 Scalable // 🤯 可扩展 ///: Built to scale across machines and robots! We use YAML description to make our software declarative in order to be distributable on multiple machines. // 专为跨机器和机器人扩展而设计！我们使用 YAML 描述使我们的软件声明式化，以便在多台机器上分发。 /// - 👨‍🏭 Fast Prototyping // 👨‍🏭 快速原型开发 ///: Use readily available nodes from your YAML dataflow, so that you don't need to bother copy-pasting boilerplate code. You can also tinker live using Python! // 使用来自您 YAML 数据流的现成节点，这样您就不需要费心复制粘贴样板代码。您还可以使用 Python 实时进行调试！ /// - 🔭 Observable // 🔭 可观察 /// Get logs, traces and metrics through our cli and opentelemetry! // 通过我们的 cli 和 opentelemetry 获取日志、跟踪和指标！ /// - 💻 Large Support Matrix // 💻 支持矩阵庞大 ///: dora is available in Python, Rust, C and C++ on most platforms and architecture! // dora 支持在大多数平台和架构上使用 Python、Rust、C 和 C++！ /// - 🧑‍🤝‍🧑 Community // 🧑‍🤝‍🧑 社区 ///: We hope to make dora a community-driven project and help other learn about robotics. // 我们希望将 dora 打造成一个社区驱动的项目，并帮助其他人了解机器人技术。"
[LeRobot-pipeline.src/gh]: https://github.com/dora-rs/dora-lerobot.git "(Apache-2.0) (Languages: Python 94.1%, Rust 5.7%, Shell 0.2%) Lerobot boosted with dora // 使用 dora 提升 Lerobot /// Dora-LeRobot /// Dora-LeRobot is a 100% Dora pipeline for manipulating robots, cameras and all possible hardware compatible with LeRobot. // Dora-LeRobot 是一个用于操控机器人、相机以及所有与 LeRobot 兼容的可能硬件的 100% Dora 管道。 /// About Dora // 关于 Dora /// Dora is a framework that lets you build applications by connecting components (nodes) together. It is based on the concept of a graph where nodes are connected by edges. Each node can have inputs and outputs that are connected to other nodes. The nodes can be written in any language and can be run on any platform. The communication between nodes is done via a shared memory space. // Dora 是一个框架，允许你通过连接组件（节点）来构建应用程序。它基于图的概念，其中节点通过边连接。每个节点可以有输入和输出，这些输入和输出可以连接到其他节点。节点可以用任何语言编写，并可以在任何平台上运行。节点之间的通信是通过共享内存空间完成的。 /// Building a robotic application can be summed up as bringing together hardware, algorithms, and AI models, and make them communicate with each others. At dora-rs, we try to: // 构建一个机器人应用可以概括为将硬件、算法和人工智能模型结合起来，并使它们相互通信。在 dora-rs 中，我们试图做到： /// - make integration of hardware and software easy by supporting Python, C, C++, and also ROS2. // 通过支持 Python、C、C++ 和 ROS2，使软硬件集成变得简单。 /// - make communication low latency by using zero-copy Arrow messages. // 通过使用零拷贝 Arrow 消息，使通信低延迟。"
[autodrive-teach.src/gh]: https://github.com/dora-rs/dora-drives.git "(Apache-2.0) (Languages: Python 99.0%, Shell 1.0%) A step-by-step tutorial that allows beginners to write their own autonomous vehicle program from scratch using a simple starter kit. Dora-drives makes learning autonomous vehicle systems faster and easier. // 一个逐步教程，让初学者使用简单的入门套件从头开始编写自己的自动驾驶车辆程序。Dora-drives 使学习自动驾驶系统更快、更容易。 /// dora-drives is a step-by-step tutorial that allows beginners to write their own autonomous vehicle program from scratch using a simple starter kit. // dora-drives 是一个逐步教程，允许初学者使用简单的入门套件从零开始编写自己的自动驾驶车辆程序。 /// Why dora-drives? // 为什么选择 dora-drives？ /// We believe that programming autonomous driving vehicles is the perfect starting point to learning robotic applications as: // 我们相信，编程自动驾驶车辆是学习机器人应用的完美起点，因为： /// - Autonomous driving is foundational for many robotic applications. // 自动驾驶是许多机器人应用的基础。 /// - Autonomous driving is simple to explain. // 自动驾驶很简单。 /// - There are a lot of datasets, models and documentation available online. // 网上有很多数据集、模型和文档可供使用。"


