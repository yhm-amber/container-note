[src/gh]: https://github.com/krazyjakee/AGENTS.db.git "(MIT) (Languages: Rust 77.7%, TypeScript 14.8%, JavaScript 3.3%, Shell 1.8%, HTML 1.8%, CSS 0.6%) AGENTS.db is AGENTS.md on steroids. A file format and toolkit for deterministic context. // AGENTS.db 是功能更强大的 AGENTS.md。它是一种用于确定性上下文的文件格式和工具包。 /// AGENTS.db is a vectorized, flatfile database for your LLM to query and store context. // AGENTS.db 是一个矢量化的平面文件数据库，供您的 LLM 查询和存储上下文。 /// It’s designed for agent systems that need: // 它专为需要以下功能的智能体代理系统而设计： /// - A read-only, canonical knowledge base (the Base layer). // 只读的、规范的知识库（基础层）。 /// - Append-only layers for new notes, derived summaries, and proposals. // 仅追加图层， 用于添加新注释、派生摘要和提案。 /// - Clear provenance (who/what wrote a chunk, and what sources it came from). // 明确出处 （谁/什么写了一段文字，以及它来自哪些来源）。 /// - Fast local search. // 快速本地搜索。 /// This repo is currently targeting the spec in docs/RFC.md. // 该仓库目前以 docs/RFC.md 中的规范为目标。 /// The Big Idea // 大创意 /// AGENTS.md is a great standard - good job! However, beyond a small code base it quickly breaks down and the llm will need to start branching off to look elsewhere for the context it needs. // AGENTS.md 是一个很棒的标准——做得好！然而，当代码库规模较大时，它很快就会失效，LLM 需要另寻其他方法来获取所需的上下文信息。 /// AGENTS.db is a one stop shop for context. All the benefits of AGENTS.md but in a single, structured, vectorized, flatfile database. // AGENTS.db 是一个提供上下文信息的一站式平台。它拥有 AGENTS.md 的所有优势，但全部集成在一个结构化的、向量化的平面文件数据库中。 /// So why is it spread across multiple files? Think of your project knowledge as “chunks” stored in layer files that are source control safe: // 那么，为什么它会分散在多个文件中呢？你可以把你的项目知识想象成存储在图层文件中的“数据块”，这些图层文件是受源代码控制的： /// - Base: AGENTS.db (immutable; source of the truest truth). /// 数据库 ： AGENTS.db （不可变；最真实真理的来源）。 /// - User: AGENTS.user.db (append-only; durable human additions). /// 用户 ： AGENTS.user.db （仅追加；持久的人工添加）。 /// - Delta: AGENTS.delta.db (append-only; reviewable proposed additions). /// Delta ： AGENTS.delta.db （仅追加；可审核的提议添加）。 /// - Local: AGENTS.local.db (append-only; ephemeral/session notes/Don't commit to source control). /// 本地 ： AGENTS.local.db （仅追加；临时/会话笔记/不要提交到源代码控制）。 /// When searching across layers, higher-precedence layers win: // 跨层搜索时，优先级较高的层优先： /// (: local > user > delta > base :) /// AGENTS.db is immutable? Why? // AGENTS.db 是不可变的吗？为什么？ /// AGENTS.db is your absolute source of truth. It should contain real documentation from the code base, ideally written and verified by humans. Therefore, it should take top priority when the LLM is comparing 2 conflicting contexts. Use the user layer for good context, delta for proposed good context (like a pull request) and finally local for your own, personal context. // AGENTS.db 是绝对权威的信息来源。它应该包含来自代码库的真实文档，理想情况下应由人工编写和验证。因此，当 LLM 比较两个冲突的上下文时，它应该具有最高优先级。使用 user 层获取可靠的上下文，使用 delta 获取建议的可靠上下文（例如拉取请求），最后 local 获取您自己的个人上下文。 /// For more information about how best to use layers, see docs/WORKFLOW.md // 有关如何更好地使用图层的更多信息，请参阅 docs/WORKFLOW.md"

