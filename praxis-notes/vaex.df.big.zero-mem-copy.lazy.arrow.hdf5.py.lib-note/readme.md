[src/gh]: https://github.com/vaexio/vaex.git "(MIT) (Languages: Python 60.8%, JavaScript 24.5%, C++ 10.5%, HTML 3.1%, PHP 0.6%, Shell 0.2%, Other 0.3%) Vaex is a high performance Python library for lazy Out-of-Core DataFrames (similar to Pandas), to visualize and explore big tabular datasets. It calculates statistics such as mean, sum, count, standard deviation etc, on an N-dimensional grid for more than a billion (10^9) samples/rows per second. Visualization is done using histograms, density plots and 3d volume rendering, allowing interactive exploration of big data. Vaex uses memory mapping, zero memory copy policy and lazy computations for best performance (no memory wasted). // Vaex 是一个高性能的 Python 库，用于懒加载的 Out-of-Core DataFrame（类似于 Pandas），用于可视化和探索大型表格数据集。它可以在每秒超过十亿（ 10^9 ）个样本/行上，在一个 N 维网格上计算均值、总和、计数、标准差等统计数据。可视化使用直方图、密度图和 3D 体积渲染完成，允许交互式探索大数据。Vaex 使用内存映射、零内存拷贝策略和懒加载计算，以获得最佳性能（不浪费内存）。 /// HDF5 and Apache Arrow supported. // HDF5 和 Apache Arrow 格式受支持。 (: df = vaes.open('path/to/big-dataset.hdf5') # local read ;: df = vaes.open('s3://path/to/big-dataset.hdf5') # read data from s3 storage)"
[site]: https://vaex.io/ "Big data made simple // 大数据变得简单 /// Data science solutions, insights, dashboards, machine learning, deployment. We start at 100GB. // 数据科学解决方案、洞察、仪表盘、机器学习、部署。我们从 100GB 开始。 /// At Vaex.io we aim to democratize big data and make it available to anyone, on any machine, at any scale. // 在 Vaex.io，我们的目标是使大数据民主化，使其在任何机器上、任何规模下都可供任何人使用。 /// - Be Agile // 敏捷行动 ///: Cut developement time by 80%. Your prototype is your solution. Create automatic pipelines for any model. // 将开发时间缩短 80%。您的原型就是您的解决方案。为任何模型创建自动管道。 /// - Make Impact // 产生影响 ///: Improve your business right away. Deploy your models to Amazon Web Services or Google Cloud Platform with a single command. // 立即提升您的业务。通过一条命令将您的模型部署到亚马逊云科技或谷歌云平台。 /// - Be Resourceful // 资源丰富 ///: Empower your data scientists. Turn any laptop into a big data powerhouse. No clusters, no engineers. // 赋能您的数据科学家。将任何笔记本电脑变成大数据超级计算机。无需集群，无需工程师。"
[docs/.site]: https://vaex.io/docs "Why vaex // 为什么选择 vaex /// - Performance: works with huge tabular data, processes >10^9 rows/second // 性能：可处理大型表格数据，每秒处理 >10^9 行 /// - Lazy / Virtual columns: compute on the fly, without wasting ram // 懒加载/虚拟列：即时计算，不浪费内存 /// - Memory efficient no memory copies when doing filtering/selections/subsets. // 内存高效：进行过滤/选择/子集操作时无需内存复制。 /// - Visualization: directly supported, a one-liner is often enough. // 可视化：直接支持，通常一行代码就足够。 /// - User friendly API: you will only need to deal with the DataFrame object, and tab completion + docstring will help you out: ds.mean<tab>, feels very similar to Pandas. // 用户友好的 API：你只需要处理 DataFrame 对象，标签补全+文档字符串将助你一臂之力： ds.mean<tab> ，感觉非常类似于 Pandas。 /// - Lean: separated into multiple packages // 精简：分为多个包 /// - - vaex-core: DataFrame and core algorithms, takes numpy arrays as input columns. // vaex-core ：DataFrame 和核心算法，接受 numpy 数组作为输入列。 /// - - vaex-hdf5: Provides memory mapped numpy arrays to a DataFrame. // vaex-hdf5 ：为 DataFrame 提供内存映射的 numpy 数组。 /// - - vaex-arrow: Arrow support for cross language data sharing. // vaex-arrow : 支持跨语言数据共享的 Arrow 功能。 /// - - vaex-viz: Visualization based on matplotlib. // vaex-viz : 基于 matplotlib 的可视化。 /// - - vaex-jupyter: Interactive visualization based on Jupyter widgets / ipywidgets, bqplot, ipyvolume and ipyleaflet. // vaex-jupyter : 基于 Jupyter widgets / ipywidgets、bqplot、ipyvolume 和 ipyleaflet 的交互式可视化。 /// - - vaex-astro: Astronomy related transformations and FITS file support. // vaex-astro : 天文相关的转换和 FITS 文件支持。 /// - - vaex-server: Provides a server to access a DataFrame remotely. // vaex-server : 提供一个服务器以远程访问 DataFrame。 /// - - vaex-distributed: (Deprecated) Now part of vaex-enterprise. // vaex-distributed : (已弃用) 现为 vaex-enterprise 的一部分。 /// - - vaex-qt: Program written using Qt GUI. // vaex-qt : 使用 Qt GUI 编写的程序。 /// - - vaex: Meta package that installs all of the above. // vaex : 安装上述所有内容的元包。 /// - - vaex-ml: Machine learning // vaex-ml : 机器学习 /// - Jupyter integration: vaex-jupyter will give you interactive visualization and selection in the Jupyter notebook and Jupyter lab. // Jupyter 集成：vaex-jupyter 将为您提供在 Jupyter 笔记本和 Jupyter lab 中的交互式可视化和选择。"
[pkg-lib.pip/pypi]: https://pypi.org/project/vaex/ "(: pip install -- vaex # install all packages cause that one is a meta package) (: pip install -- vaex-core vaex-viz vaex-jupyter vaex-server vaex-hdf5 vaex-astro vaex-ml # You can copy paste the this line and remove what you do not need /// e.g., if you don’t need astronomy related parts (vaex-astro), or don’t care about graphql (vaex-graphql), you can leave out those packages. // 如果你不需要天文学相关的部分（ vaex-astro ），或者不关心 graphql（ vaex-graphql ），你可以省略这些包。) It is recommended not to install directly into your operating system’s Python using sudo since it may break your system. Instead, you should install Anaconda, which is a Python distribution that makes installing Python packages much easier or use virtualenv or venv. // 不建议使用 sudo 直接在操作系统 Python 中安装，因为可能会破坏您的系统。相反，您应该安装 Anaconda，这是一个使安装 Python 包更简单的 Python 发行版，或者使用 virtualenv 或 venv。 (__. {[.docs]}: .../installing.html)"
[read-data.docs/.docs]: https://vaex.io/docs/guides/io.html "I/O Kung-Fu: get your data in and out of Vaex"
[install.docs/.docs]: https://vaex.io/docs/guides/io.html "Installing"
[slack]: https://join.slack.com/t/vaexio/shared_invite/zt-shhxzf5i-Cf5n2LtkoYgUjOjbB3bGQQ
