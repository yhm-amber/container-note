[src/gh]: https://github.com/dmotz/trystero.git "(MIT) (Languages: JavaScript 99.8%, HTML 0.2%) ✨🤝✨ Build instant multiplayer webapps, no server required — Magic WebRTC matchmaking over BitTorrent, Nostr, MQTT, IPFS, Supabase, and Firebase // ✨🤝✨ 快速构建多人网络应用，无需服务器 — 基于 BitTorrent、Nostr、MQTT、IPFS、Supabase 和 Firebase 的魔法 WebRTC 匹配 /// Trystero manages a clandestine courier network that lets your application's users talk directly with one another, encrypted and without a server middleman. // Trystero 管理一个秘密的快递网络，让您的应用程序用户可以直接相互交流，加密且无需服务器中转。 /// The net is full of open, decentralized communication channels: torrent trackers, IoT device brokers, boutique file protocols, and niche social networks. // 网络中充满了开放、去中心化的通信渠道：种子追踪器、物联网设备中介、定制文件协议和利基社交网络。 /// Trystero piggybacks on these networks to automatically establish secure, private, P2P connections between your app's users with no effort on your part. // Trystero 借助这些网络，自动在您的应用程序用户之间建立安全、私密的 P2P 连接，而无需您付出任何努力。 /// Peers can connect via 🌊 BitTorrent, 🐦 Nostr, 📡 MQTT, ⚡️ Supabase, 🔥 Firebase, or 🪐 IPFS – all using the same API. // 对等节点可以通过 🌊 BitTorrent、🐦 Nostr、📡 MQTT、⚡️ Supabase、🔥 Firebase 或 🪐 IPFS 连接——所有使用相同的 API。 /// Besides making peer matching automatic, Trystero offers some nice abstractions on top of WebRTC: // 除了实现节点匹配自动化，Trystero 还在 WebRTC 之上提供了一些不错的抽象： /// - 👂📣 Rooms / broadcasting // 👂📣 房间 / 广播 /// - 🔢📩 Automatic serialization / deserialization of data // 🔢📩 数据的自动序列化 / 反序列化 /// - 🎥🏷 Attach metadata to binary data and media streams // 🎥🏷 将元数据附加到二进制数据和媒体流上 /// - ✂️⏳ Automatic chunking and throttling of large data // ✂️⏳ 大数据自动分块和限速 /// - ⏱🤞 Progress events and promises for data transfers // ⏱🤞 数据传输的进度事件和承诺 /// - 🔐📝 Session data encryption // 🔐📝 会话数据加密 /// - 🏭⚡ Runs server-side // 🏭⚡ 运行服务器端 /// - ⚛️🪝 React hooks /// You can see what people are building with Trystero here. // 你可以在 Trystero 这里看到人们正在构建什么。 /// To establish a direct peer-to-peer connection with WebRTC, a signalling channel is needed to exchange peer information (SDP) (. https://en.wikipedia.org/wiki/Session_Description_Protocol). Typically this involves running your own matchmaking server but Trystero abstracts this away for you and offers multiple “serverless” strategies for connecting peers (currently BitTorrent, Nostr, MQTT, Supabase, Firebase, and IPFS). // 要与 WebRTC 建立直接的点对点连接，需要一个信令通道来交换对端信息（SDP）。通常这需要运行自己的匹配服务器，但 Trystero 将此抽象化，并为对端连接提供了多种“无服务器”策略（目前支持 BitTorrent、Nostr、MQTT、Supabase、Firebase 和 IPFS）。 /// Beyond peer discovery, your app's data never touches the strategy medium and is sent directly peer-to-peer and end-to-end encrypted between users. // 除了对等发现之外，您的应用程序数据永远不会接触到策略媒介，而是直接在用户之间进行端到端加密的点对点传输。"
[pkg.mpm/npmjs]: https://npmjs.com/package/trystero "(: npm i -- trystero) (License: MIT) (Unpacked Size: 84.3 kB) (Total Files: 22) Build instant multiplayer web apps, no server required (src: gh:dmotz/trystero.git)"
[play/.site]: https://oxism.com/trystero/ "Make any site multiplayer in a few lines. // 只需几行代码即可将任何网站变为多人游戏。 /// Serverless WebRTC matchmaking for painless P2P. // 为 P2P 提供无痛苦的 Serverless WebRTC 匹配服务。 ///  Below is some code describing whatʼs happening on this site right now. // 以下是描述当前网站正在发生的事情的代码。 /// Anyone on this page will connect directly to others in real-time, syncing mouse movement and clicks. (Try it, I dare you). // 此页面上任何人都会实时直接连接到其他人，同步鼠标移动和点击。（试试看，我敢你）。 /// Trystero can connect peers via 🌊 BitTorrent, 🐦 Nostr, 📡 MQTT, 🪐 IPFS, ⚡️ Supabase, and 🔥 Firebase. // Trystero 可以通过 🌊 BitTorrent、🐦 Nostr、📡 MQTT、🪐 IPFS、⚡️ Supabase 和 🔥 Firebase 连接对等节点。 /// Right now youʼre the only person with the page open, but you can cheat and just open this URL in another tab to see what itʼs like with others. // 目前只有你一个人打开了此页面，但你可以在另一个标签页中打开此 URL 来作弊，看看和其他人一起是什么样子。 /// Hereʼs how you use it. // 这是如何使用它的方法。 ///: (: import {joinRoom} from 'trystero' ;: const room = joinRoom({appId: 'trystero-lounge'}, '101') // Join a room with an app ID and a room ID // 使用应用 ID 和房间 ID 加入一个房间) (: room.onPeerJoin(addCursor) ;: room.onPeerLeave(removeCursor) // Listen for peers joining and leaving (leaving out some boring UI code) // 监听加入和离开的同伴（省略一些无聊的 UI 代码）) (: const [sendMove, getMove] = room.makeAction('mouseMove') ;: const [sendClick, getClick] = room.makeAction('click') // Make some actions to send and respond to // 执行一些操作来发送和响应) (: window.addEventListener('mousemove', e => sendMove([e.clientX, e.clientY])) ;: window.addEventListener('click', () => sendClick(randomFruit())) // Broadcast your actions to other peers // 向其他对等方广播你的操作) (: getMove(([x, y], peerId) => setCursorPosition(peerId, x, y)) ;: getClick((fruit, peerId) => dropFruitFrom(peerId, fruit)) // And listen for when peers send theirs // 并监听对等方发送的操作) /// Thatʼs all this page is doing, but you can do much more like audio/video streams and binary data (like files). Peruse the readme to see how. // 这个页面就做了这些，但你还可以做更多，比如音频/视频流和二进制数据（如文件）。查看 readme 了解如何操作。"
[knowsby]: https://github.com/kermanx/p2p-live-share.git "- No Account Required // 无需账户 ///: You won't need to sign in any account to use it. You can also improve it's security and connectivity by self-hosting a relay server. By default, it uses public signaling servers listed by trystero. // 您无需登录任何账户即可使用它。您还可以通过自托管中继服务器来提高其安全性和连接性。默认情况下，它使用由 trystero 列出的公共信令服务器。"
