[src/gh]: https://github.com/segmentio/encoding.git "(MIT) (Languages: Go 96.8%, Makefile 3.2%) Go package containing implementations of efficient encoding, decoding, and validation APIs. // 包含高效编码、解码和验证 API 实现的 Go 包。 /// At Segment, we do a lot of marshaling and unmarshaling of data when sending, queuing, or storing messages. The resources we need to provision on the infrastructure are directly related to the type and amount of data that we are processing. At the scale we operate at, the tools we choose to build programs can have a large impact on the efficiency of our systems. It is important to explore alternative approaches when we reach the limits of the code we use. // 在 Segment，我们在发送、排队或存储消息时会进行大量的数据编组和解组操作。我们需要在基础设施上配置的资源与我们处理的数据类型和数量直接相关。在我们目前的规模下，我们选择的程序构建工具会对系统效率产生重大影响。因此，当我们的代码达到极限时，探索替代方案至关重要。 /// This repository includes experiments for Go packages for marshaling and unmarshaling data in various formats. While the focus is on providing a high performance library, we also aim for very low development and maintenance overhead by implementing APIs that can be used as drop-in replacements for the default solutions. // 本仓库包含用于对各种格式的数据进行序列化和反序列化的 Go 包实验。虽然我们专注于提供高性能库，但也力求通过实现可直接替代默认解决方案的 API 来最大限度地降低开发和维护成本。 /// The json sub-package provides a re-implementation of the functionalities offered by the standard library's encoding/json package, with a focus on lowering the CPU and memory footprint of the code. // json 子包重新实现了标准库 encoding/json 提供的功能。 该软件包旨在降低代码的 CPU 和内存占用。 /// The iso8601 sub-package exposes APIs to efficiently deal with with string representations of iso8601 dates. // iso8601 子包公开了 API，可以高效地处理 iso8601 日期的字符串表示形式。"
[lib.go/pgd]: https://pkg.go.dev/github.com/segmentio/encoding "(: go get -- github.com/segmentio/encoding) (License: MIT)  (src: gh:segmentio/encoding.git)"
[libsub-json.go/pgd]: https://pkg.go.dev/github.com/segmentio/encoding/json "(: go get -- github.com/segmentio/encoding/json) (License: MIT) (Imports: 26) (Imported by: 339)  (src: gh:segmentio/encoding.git)"
[libsub-iso8601.go/pgd]: https://pkg.go.dev/github.com/segmentio/encoding/iso8601 "(: go get -- github.com/segmentio/encoding/iso8601) (License: MIT) (Imports: 4) (Imported by: 1)  (src: gh:segmentio/encoding.git)"

