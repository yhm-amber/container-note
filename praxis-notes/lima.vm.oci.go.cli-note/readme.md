[src/gh]: https://github.com/lima-vm/lima.git "(Apache-2.0) (Languages: Go 74.2%, Shell 22.1%, Makefile 2.0%, Perl 1.0%, HTML 0.6%, SCSS 0.1%) Linux virtual machines, with a focus on running containers // Linux 虚拟机，专注于运行容器 /// Lima launches Linux virtual machines with automatic file sharing and port forwarding (similar to WSL2). // Lima 启动 Linux 虚拟机，支持自动文件共享和端口转发（类似于 WSL2）。 /// The original goal of Lima was to promote containerd including nerdctl (contaiNERD ctl) to Mac users, but Lima can be used for non-container applications as well. // Lima 的最初目标是推广 containerd 及其工具 nerdctl（contaiNERD ctl）给 Mac 用户，但 Lima 也可用于非容器应用程序。 /// Lima also supports other container engines (Docker, Podman, Kubernetes, etc.) and non-macOS hosts (Linux, NetBSD, etc.). // Lima 还支持其他容器引擎（Docker、Podman、Kubernetes 等）和非 macOS 主机（Linux、NetBSD 等）。 /// (: brew install lima) (: limactl start)"
[limactl-cli.go/pgd]: https://pkg.go.dev/github.com/lima-vm/lima/cmd/limactl "(: go install -- github.com/lima-vm/lima/cmd/limactl) (License: Apache-2.0) (Imports: 68) (Imported by: 0)  (src: gh:lima-vm/lima.git)"
[limactl-cli.brew/homebrew]: https://formulae.brew.sh/formula/lima "(: brew install -- lima) (License: Apache-2.0)  (src: gh:lima-vm/lima.git)"
[site/io]: https://lima-vm.io/ "What is Lima? // 什么是利马？ /// Lima launches Linux virtual machines with automatic file sharing and port forwarding (similar to WSL2). // Lima 启动 Linux 虚拟机，支持自动文件共享和端口转发（类似于 WSL2）。"

