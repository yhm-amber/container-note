[src/gh]: https://github.com/microsoft/edit.git "(MIT) (Languages: Rust 99.9%, Roff 0.1%) We all edit. // 咱啥都编辑。 /// A simple editor for simple needs. // 一款满足简单需求的简单编辑器。 /// This editor pays homage to the classic MS-DOS Editor (. web: en.wikipedia.org/wiki/MS-DOS_Editor), but with a modern interface and input controls similar to VS Code. The goal is to provide an accessible editor that even users largely unfamiliar with terminals can easily use. // 这款编辑器向经典的 MS-DOS 编辑器致敬，但采用了类似于 VS Code 的现代化界面和输入控件。其目标是提供一款易于使用的编辑器，即使是对终端不太熟悉的用户也能轻松上手。 /// Package Naming // 包命名 /// The canonical executable name is “edit” and the alternative name is “msedit”. We're aware of the potential conflict of “edit” with existing commands and recommend alternatively naming packages and executables “msedit”. Names such as “ms-edit” should be avoided. Assigning an “edit” alias is recommended, if possible. // 规范的可执行文件名是“edit”，备用文件名是“msedit”。我们意识到“edit”可能与现有命令冲突，因此建议将软件包和可执行文件命名为“msedit”。应避免使用“ms-edit”之类的名称。如果可能，建议为“edit”指定一个别名。 /// ### ICU library name (SONAME) // ICU 库名称（SONAME） /// This project optionally depends on the ICU library for its Search and Replace functionality. By default, the project will look for a SONAME without version suffix: // 项目可选地依赖 ICU 库来实现搜索和替换功能。默认情况下，项目会查找不带版本后缀的 SONAME： /// - Windows: icuuc.dll // Windows： icuuc.dll /// - macOS: libicuuc.dylib // macOS： libicuuc.dylib /// - UNIX, and other OS: libicuuc.so // UNIX 和其他操作系统： libicuuc.so /// If your installation uses a different SONAME, please set the following environment variable at build time: // 如果您的安装使用了不同的 SONAME，请在构建时设置以下环境变量： /// - EDIT_CFG_ICUUC_SONAME: For instance, libicuuc.so.76. // EDIT_CFG_ICUUC_SONAME ：例如， libicuuc.so.76 。 /// - EDIT_CFG_ICUI18N_SONAME: For instance, libicui18n.so.76. // EDIT_CFG_ICUI18N_SONAME ：例如 libicui18n.so.76 。 /// Additionally, this project assumes that the ICU exports are exported without _ prefix and without version suffix, such as u_errorName. If your installation uses versioned exports, please set: // 此外，本项目假定 ICU 导出时不带 _ 前缀和版本后缀，例如 u_errorName 。如果您的安装使用了版本化导出，请进行如下设置： /// - EDIT_CFG_ICU_CPP_EXPORTS: If set to true, it'll look for C++ symbols such as _u_errorName. Enabled by default on macOS. // EDIT_CFG_ICU_CPP_EXPORTS ：如果设置为 true ，则会查找 C++ 符号，例如 _u_errorName 。在 macOS 上默认启用。 /// - EDIT_CFG_ICU_RENAMING_VERSION: If set to a version number, such as 76, it'll look for symbols such as u_errorName_76. // EDIT_CFG_ICU_RENAMING_VERSION ：如果设置为版本号，例如 76 ，它将查找诸如 u_errorName_76 之类的符号。 /// Finally, you can set the following environment variables: // 最后，您可以设置以下环境变量： /// - EDIT_CFG_ICU_RENAMING_AUTO_DETECT: If set to true, the executable will try to detect the EDIT_CFG_ICU_RENAMING_VERSION value at runtime. The way it does this is not officially supported by ICU and as such is not recommended to be relied upon. Enabled by default on UNIX (excluding macOS) if no other options are set. // EDIT_CFG_ICU_RENAMING_AUTO_DETECT ：如果设置为 true ，可执行文件将在运行时尝试检测 EDIT_CFG_ICU_RENAMING_VERSION 值。此方法并非 ICU 官方支持，因此不建议依赖此方法。如果未设置其他选项，则在 UNIX（macOS 除外）上默认启用。"
[cli.cargo/crates]: https://crates.io/crates/msedit "(: cargo install -- msedit) (MIT) (16K SLoC) (439 KiB)  (src: gh:microsoft/edit.git)"
[cli.brew/homebrew]: https://formulae.brew.sh/formula/msedit "(: brew install -- msedit)"
[cli.apk/termux]: https://termux-packages.ajam.dev/#:~:text=msedit "(: pkg install -- msedit)"
[cli.dnf/fedora]: https://packages.fedoraproject.org/pkgs/msedit/msedit/ "(: sudo dnf install -- msedit)"
[repology]: https://repology.org/project/microsoft-edit

