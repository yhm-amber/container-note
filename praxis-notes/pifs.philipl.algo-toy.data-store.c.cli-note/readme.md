[src/gh]: https://github.com/philipl/pifs.git "(GPL-3.0) (Languages: C 94.4%, M4 3.3%, Shell 1.3%, Makefile 1.0%) πfs - the data-free filesystem! // πfs —— 无数据文件系统！ /// πfs: Never worry about data again! // πfs：再也不用担心数据问题了！ /// πfs is a revolutionary new file system that, instead of wasting space storing your data on your hard drive, stores your data in π! You'll never run out of space again - π holds every file that could possibly exist! They said 100% compression was impossible? You're looking at it! // πfs 是一种革命性的新型文件系统，它不会像传统文件系统那样将数据存储在硬盘上，而是存储在 π 中！从此告别空间不足的烦恼——π 可以容纳所有可能存在的文件！他们说 100% 压缩是不可能的？现在，它就在这里！ /// πfs is dead simple to use: // πfs 的使用非常简单： /// (: πfs -o mdd=<metadata directory> <mountpoint>) /// where the metadata directory is where πfs should store its metadata (such as filenames or the locations of your files in π) and mountpoint is your usual filesystem mountpoint. // 其中 metadata 目录是 πfs 应该存储其元数据（例如文件名或文件在 π 中的位置）的地方， mountpoint 是你的常用文件系统挂载点。 /// ## What does π have to do with my data? /// π 与我的数据有什么关系？ // π (or pi) is one of the most important constants in mathematics and has a variety of interesting properties (which you can read about at wikipedia (. web: en.wikipedia.org/wiki/Pi)) // π（或圆周率）是数学中最重要的常数之一，具有各种有趣的性质（您可以在维基百科上阅读相关内容）。 /// One of the properties that π is conjectured to have is that it is normal, which is to say that its digits are all distributed evenly, with the implication that it is a disjunctive sequence, meaning that all possible finite sequences of digits will be present somewhere in it. If we consider π in base 16 (hexadecimal) , it is trivial to see that if this conjecture is true, then all possible finite files must exist within π. The first record of this observation dates back to 2001 (. web: netfunny.com/rhf/jokes/01/Jun/pi.html). // 人们推测π具有的性质之一是它是正规数 ，也就是说它的数字分布均匀，这意味着它是一个析取序列 ，所有可能的有限数字序列都包含在其中。如果我们考虑十六进制（16 进制）的π，很容易看出，如果这个推测成立，那么所有可能的有限文件都必须存在于π中。这一观察结果最早的记录可以追溯到 2001 年 。 /// From here, it is a small leap to see that if π contains all possible files, why are we wasting exabytes of space storing those files, when we could just look them up in π! // 由此不难看出，如果 π 包含了所有可能的文件，那么我们为什么要浪费 EB 的空间来存储这些文件，而不用直接在 π 中查找它们呢？ /// ## Every file that could possibly exist? // 所有可能存在的文件？ /// That's right! Every file you've ever created, or anyone else has created or will create! Copyright infringement? It's just a few digits of π! They were always there! // 没错！你创建过的、其他人创建过的或将来要创建的每一个文件！版权侵权？那不过是π的几个数字而已！它们一直都在那里！ /// ## But how do I look up my data in π? // 但我该如何用π查找我的数据呢？ /// As long as you know the index into π of your file and its length, its a simple task to extract the file using the Bailey–Borwein–Plouffe formula (. web: en.wikipedia.org/wiki/BBP-type_formula) Similarly, you can use the formula to initially find the index of your file // 只要知道文件在 π 中的索引和文件长度，使用 Bailey-Borwein-Plouffe 公式提取文件就非常简单。 同样，您可以使用该公式初步查找文件的索引。 /// Now, we all know that it can take a while to find a long sequence of digits in π, so for practical reasons, we should break the files up into smaller chunks that can be more readily found. // 我们都知道，要找到 π 的一长串数字可能需要一段时间，因此出于实际原因，我们应该将文件分成更小的块，以便更容易找到。 /// In this implementation, to maximise performance, we consider each individual byte of the file separately, and look it up in π. // 在这个实现中，为了最大限度地提高性能，我们将文件的每个字节单独考虑，并在 π 中查找它。 /// ## So I've looked up my bytes in π, but how do I remember where they are? // 我已经在 π 中查找了我的字节，但我该如何记住它们在哪里呢？ /// Well, you've obviously got to write them down somewhere; you could use a piece of paper, but remember all that storage space we saved by moving our data into π? Why don't we store our file locations there!?! Even better, the location of our files in π is metadata and as we all know (. web: datatechnologytoday.wordpress.com/2010/09/07/on-the-importance-of-metadata) metadata is becoming more and more important in everything we do. Doesn't it feel great to have generated so much metadata? Why waste time with old fashioned data when you can just deal with metadata, and lots of it! // 当然，你得把它们记下来；你可以用纸笔，但别忘了我们把数据移到 π 里节省了多少存储空间！为什么不把文件位置也存在那里呢？！更妙的是，文件在 π 中的位置就是元数据，而且我们都知道…… 元数据在我们所做的每一件事中都变得越来越重要。难道你不觉得吗？ 生成了这么多元数据真是太好了！为什么还要浪费时间处理老式数据呢？ 当你只需要处理元数据，而且是大量的元数据时！ /// ## Yeah, but what happens if lose my file locations? // 是啊，但如果我丢失了文件位置怎么办？ /// No problem, the locations are just metadata! Your files are still there, sitting in π - they're never going away, are they? // 没问题，这些位置信息只是元数据！你的文件仍然在那里，保存在 π 中——它们永远不会消失，对吧？ /// ## Why is this thing so slow? It took me five minutes to store a 400 line text file! // 这东西怎么这么慢？我存一个400行的文本文件竟然花了五分钟！ /// Well, this is just an initial prototype, and don't worry, there's always Moore's law! // 嗯，这只是一个初始原型，别担心，摩尔定律总会发挥作用的！ /// ## Where do we go from here? // 接下来我们该何去何从？ /// There's lots of potential for the future! // 未来充满无限可能！ /// - Variable run length search and lookup! // 可变运行长度搜索和查找！ /// - Arithmetic Coding! // 算术编码！ /// - Parallelizable lookup! // 可并行化的查找！ /// - Cloud based π lookup! // 基于云端的π值查询！ /// - πfs for Hadoop! // Hadoop 的 πfs！"
[knows.by]: https://github.com/angrykoala/awesome-esolangs.git "... /// Algorithms // 算法 /// - πfs - Pi-based storage system, store your data by searching a matching sequence in pi. // πfs - 基于π的存储系统，通过在π中搜索匹配序列来存储数据。 /// 。。。"

