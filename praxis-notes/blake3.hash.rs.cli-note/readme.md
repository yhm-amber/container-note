[impl-src/gh]: https://github.com/BLAKE3-team/BLAKE3.git "(Apache-2.0, CC0-1.0) (Languages: Assembly 58.7%, Rust 27.7%, C 11.7%, CMake 1.5%, Python 0.2%, Shell 0.1%, C++ 0.1%) the official Rust and C implementations of the BLAKE3 cryptographic hash function // BLAKE3 加密哈希函数的官方 Rust 和 C 实现 /// BLAKE3 // 布莱克3 /// BLAKE3 is a cryptographic hash function that is: // BLAKE3 是一种加密哈希函数，它具有以下特点： /// - Much faster than MD5, SHA-1, SHA-2, SHA-3, and BLAKE2. // 比 MD5、SHA-1、SHA-2、SHA-3 和 BLAKE2 快得多 。 /// - Secure, unlike MD5 and SHA-1. And secure against length extension, unlike SHA-2. // 与 MD5 和 SHA-1 不同，它更安全 。与 SHA-2 不同，它还能防止长度扩展。 /// - Highly parallelizable across any number of threads and SIMD lanes, because it's a Merkle tree on the inside. // 由于其内部是默克尔树，因此可以跨任意数量的线程和 SIMD 通道进行高度并行化 。 /// - Capable of verified streaming and incremental updates, again because it's a Merkle tree. // 能够进行经过验证的流式传输和增量更新 ，因为它是一棵默克尔树。 /// - A PRF, MAC, KDF, and XOF, as well as a regular hash. // PRF 、 MAC 、 KDF 和 XOF ，以及常规哈希。 /// - One algorithm with no variants, which is fast on x86-64 and also on smaller architectures. // 一种没有变体的算法 ，在 x86-64 以及更小的架构上速度都很快。 /// BLAKE3 is based on an optimized instance of the established hash function BLAKE2 and on the original Bao tree mode. The specifications and design rationale are available in the BLAKE3 paper. The default output size is 256 bits. The current version of Bao implements verified streaming with BLAKE3. // BLAKE3 基于已建立的哈希函数 BLAKE2 的优化版本以及原始的 Bao 树模式 。其规范和设计原理可在 BLAKE3 论文中找到。 默认输出大小为 256 位。当前版本 Bao 使用 BLAKE3 实现了经过验证的流式传输。 /// This repository is the official implementation of BLAKE3. It includes: // 本仓库是 BLAKE3 的官方实现，包含以下内容： /// - The blake3 Rust crate, which includes optimized implementations for SSE2, SSE4.1, AVX2, AVX-512, NEON, and WASM, with automatic runtime CPU feature detection on x86. The rayon feature provides multithreading. // blake3 Rust crate 包含针对 SSE2、SSE4.1、AVX2、AVX-512、NEON 和 WASM 的优化实现，并可在 x86 平台上自动检测运行时 CPU 特性。rayon rayon 提供多线程支持。 /// - The b3sum Rust crate, which provides a command line interface. It uses multithreading by default, making it an order of magnitude faster than e.g. sha256sum on typical desktop hardware. // b3sum 是一个 Rust crate，它提供了一个命令行界面。它默认使用多线程，因此在典型的桌面硬件上，它的速度比例如 sha256sum 快一个数量级。 /// - The C implementation, which like the Rust implementation includes SIMD optimizations (all except WASM), CPU feature detection on x86, and optional multithreading. See c/README.md. // C 语言实现与 Rust 实现一样，都包含了 SIMD 优化（除 WASM 外）、x86 架构上的 CPU 特性检测以及可选的多线程支持。参见 c/README.md 。 /// - The Rust reference implementation, which is discussed in Section 5.1 of the BLAKE3 paper. This implementation is much smaller and simpler than the optimized ones above. If you want to see how BLAKE3 works, or you're writing a port that doesn't need multithreading or SIMD optimizations, start here. Ports of the reference implementation to other languages are hosted in separate repositories (C, Python). // Rust 参考实现 ，在 BLAKE3 论文的 5.1 节中讨论。该实现比上述优化版本更小巧、更简洁。如果您想了解 BLAKE3 的工作原理，或者您正在编写一个不需要多线程或 SIMD 优化的移植版本，请从这里开始。参考实现的其他语言移植版本托管在单独的代码库中（ C ， Python ）。 /// - A set of test vectors that covers extended outputs, all three modes, and a variety of input lengths. // 一组测试向量 涵盖扩展输出、所有三种模式以及多种输入方式 长度。"
[spec-src/gh]: https://github.com/BLAKE3-team/BLAKE3-specs.git "(Apache-2.0, CC0-1.0) (Languages: HTML 80.2%, C 10.6%, TeX 5.1%, Assembly 1.9%, XSLT 1.0%, Python 0.7%, Other 0.5%) The BLAKE3 paper: specifications, analysis, and design rationale // BLAKE3 论文：规范、分析和设计原理 /// BLAKE3 specifications // BLAKE3 规格 /// This repository contains the specification document of the hash function BLAKE3. // 该存储库包含哈希函数 BLAKE3 的规范文档。 /// You can directly get the latest version as a PDF, or build it yourself from the LaTeX source by running make, which will generate the PDF at ./build/blake3.pdf. // 您可以直接获取最新版本的 PDF 文件 ，或者通过运行 make 从 LaTeX 源代码自行构建，这将在 ./build/blake3.pdf 生成 PDF 文件。"
[cli.cargo/crates]: https://crates.io/crates/b3sum "(: cargo install -- b3sum) (CC0-1.0 OR Apache-2.0 OR Apache-2.0 WITH LLVM-exception) (612 SLoC) (28.4 KiB) a command line implementation of the BLAKE3 hash function // BLAKE3 哈希函数的命令行实现 (src: gh:BLAKE3-team/BLAKE3.git)"
[lib.cargo/crates]: https://crates.io/crates/blake3 "(: cargo add -- blake3) (CC0-1.0 OR Apache-2.0 OR Apache-2.0 WITH LLVM-exception) (20K SLoC) (204 KiB) the BLAKE3 hash function // BLAKE3 哈希函数 (src: gh:BLAKE3-team/BLAKE3.git)"
[knowsby]: https://nc.abetech.es/index.php/s/n96mosgP9p8dzEX "(~ from: web:venomlinux.org/downloads) ..., venomlinux-wayland-sysv-x86_64-20260424.iso.b3sum, ..."
