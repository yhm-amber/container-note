[site/co]: https://blendos.co/ "blendOS /// Arch Linux made declarative, immutable and atomic. With Android app support and Fedora, Debian, CentOS Stream and Ubuntu containers available, as well as system packages/DEs/kernels from Arch Linux and the AUR. // Arch Linux 实现了声明式、不可变和原子性。它支持 Android 应用，并提供 Fedora、Debian、CentOS Stream 和 Ubuntu 容器，以及来自 Arch Linux 和 AUR 的系统软件包/桌面环境/内核。 /// - Declarative // 声明式 ///: blendOS v4 is fully declarative, allowing you to use custom packages, kernels, drivers and desktop environments on a minimal, deployable, atomic Arch Linux base system. // blendOS v4 是完全声明式的，允许您在最小的、 可部署的 、原子性的 Arch Linux 基础系统上使用自定义软件包、内核、驱动程序和桌面环境 。 /// - Atomic // 原子 ///: Unlike most other operating systems, blendOS ensures your system does not end up in a half-broken state by replacing your old root filesystem with a new one cleanly. // 与其他大多数操作系统不同，blendOS 会将旧的根文件系统干净利落地替换为新的根文件系统，从而确保系统不会处于半损坏状态。 /// - Extensible // 可扩展 ///: blendOS offers support for applications and binaries from several distributions and Android, without the fear of breaking your system through the use of podman containers. // blendOS 支持来自多个发行版和 Android 的应用程序和二进制文件，无需担心因使用 podman 容器而破坏系统。 /// - Friendly // 友好的 ///: Thanks to its immutable nature, blendOS prevents you from wrecking your system if you were to try to delete system directories, and makes troubleshooting issues intuitive for any user. // 由于 blendOS 具有不可变性 ，因此即使您尝试删除系统目录，它也不会破坏您的系统，并且使任何用户都能直观地排除故障。 /// Flatpak applications are supported out-of-the-box, and so are Arch Linux & AUR packages through the /system.yaml file. Support for applications from Android, Fedora, Ubuntu, Debian and more can be enabled through the System app on blendOS. // blendOS 默认支持 Flatpak 应用，也支持通过 /system.yaml 文件安装的 Arch Linux 和 AUR 软件包。对来自 Android 、 Fedora 、 Ubuntu 、 Debian 等平台的应用的支持可以通过 blendOS 的系统应用启用。"
[liberachat]: https://web.libera.chat/#blendos
[lemmy]: https://lemmy.world/c/blendos
[mastodon]: https://fosstodon.org/@blendOS
[matrix]: https://matrix.to/#/#blendos:matrix.org
[twitter]: https://twitter.com/@blend_os
[discord]: https://discord.gg/fvMpV8ZNxD
[telegram]: https://t.me/blendos
[revolt]: https://rvlt.gg/h3zgmesS
[reddit]: https://reddit.com/r/BlendOS
[readme.src/.site-git]: https://git.blendos.co/blendOS/blendOS.git "(GPL-3.0 OR LATER) (Markdown 100%) A seamless blend of all Linux distributions. // 无缝融合所有 Linux 发行版。 /// Welcome to blendOS!   // 欢迎使用 blendOS！ /// The only operating system you'll ever need. A seamless blend of all Linux distributions, Android apps and web apps. // 你只需要这一个操作系统。它完美融合了所有 Linux 发行版、安卓应用和网页应用。 /// This repository acts as the starting point for contributing to blendOS. // 该仓库是为 blendOS 做贡献的起点。 /// blend and akshara // blend 和 akshara /// blend and akshara are the primary components of blendOS, that handle immutability, updates, overlays and containers. Both of them are written in Python. // blend 和 akshara 是 blendOS 的主要组件，分别负责处理不可变性、更新、覆盖层和容器。它们都是用 Python 编写的。 /// The blend repository also contains 'blendOS Settings', a GUI configuration tool for blendOS that uses Electron. // blend 存储库还包含“blendOS 设置”，这是一个使用 Electron 的 blendOS GUI 配置工具。
[readme.src/gh]: https://github.com/blend-os/blendOS.git "(GPL-3.0 OR LATER) (Markdown 100%) [READ-ONLY] A seamless blend of all Linux distributions. // 无缝融合所有 Linux 发行版。 /// Welcome to blendOS!   // 欢迎使用 blendOS！ /// The only operating system you'll ever need. A seamless blend of all Linux distributions, Android apps and web apps. // 你只需要这一个操作系统。它完美融合了所有 Linux 发行版、安卓应用和网页应用。 /// This repository acts as the starting point for contributing to blendOS. // 该仓库是为 blendOS 做贡献的起点。 /// blend and akshara // blend 和 akshara /// blend and akshara are the primary components of blendOS, that handle immutability, updates, overlays and containers. Both of them are written in Python. // blend 和 akshara 是 blendOS 的主要组件，分别负责处理不可变性、更新、覆盖层和容器。它们都是用 Python 编写的。 /// The blend repository also contains 'blendOS Settings', a GUI configuration tool for blendOS that uses Electron. // blend 存储库还包含“blendOS 设置”，这是一个使用 Electron 的 blendOS GUI 配置工具。
[component:immutability-layer.src/.site-git]: https://git.blendos.co/blendOS/system-tools/akshara.git "(GPL-3.0 OR LATER) (Languages: Python 87.7%, Shell 12.3%) A modern take on managing and updating operating systems. // 一种管理和更新操作系统的现代方法。 /// akshara // 阿克沙拉 /// A simple system builder and immutability layer. // 一个简单的系统构建器和不可变层。 /// ⚠ ANY CHANGES TO /usr/bin/akshara WILL BE REVERTED AFTER EVERY UPDATE! ⚠ // ⚠ 对 /usr/bin/akshara 的任何更改都会在每次更新后被还原！ ⚠"
[component:immutability-layer.src/gh]: https://github.com/blend-os/akshara.git "(GPL-3.0 OR LATER) (Languages: Python 87.7%, Shell 12.3%) [READ-ONLY] A modern take on managing and updating operating systems. // 一种管理和更新操作系统的现代方法。 /// akshara // 阿克沙拉 /// A simple system builder and immutability layer. // 一个简单的系统构建器和不可变层。 /// ⚠ ANY CHANGES TO /usr/bin/akshara WILL BE REVERTED AFTER EVERY UPDATE! ⚠ // ⚠ 对 /usr/bin/akshara 的任何更改都会在每次更新后被还原！ ⚠"
[component:distmix-pm/.site-git]: https://git.blendos.co/blendOS/system-tools/blend.git "(GPL-3.0 OR LATER) (Languages: JavaScript 29.2%, Python 28.6%, Shell 21.0%, HTML 19.6%, CSS 1.6%) Package manager for blendOS // blendOS 的软件包管理器 /// blend // 混合 /// A tool to manage overlays, containers and multiple distributions // 用于管理覆盖网络、容器和多个发行版的工具 /// This repository also contains blend-settings, a tool for configuring blend and overlays, as well as many other utilities. // 该存储库还包含 blend-settings （一个用于配置混合和叠加的工具）以及许多其他实用程序。 /// Usage // 用法 /// It's recommended to use the blend-settings UI, instead of the blend CLI. // 建议使用 blend-settings 用户界面，而不是 blend 命令行界面。"
[component:distmix-pm/gh]: https://github.com/blend-os/blend.git "(GPL-3.0 OR LATER) (Languages: JavaScript 29.2%, Python 28.6%, Shell 21.0%, HTML 19.6%, CSS 1.6%) Package manager for blendOS // blendOS 的软件包管理器 /// blend // 混合 /// A tool to manage overlays, containers and multiple distributions // 用于管理覆盖网络、容器和多个发行版的工具 /// This repository also contains blend-settings, a tool for configuring blend and overlays, as well as many other utilities. // 该存储库还包含 blend-settings （一个用于配置混合和叠加的工具）以及许多其他实用程序。 /// Usage // 用法 /// It's recommended to use the blend-settings UI, instead of the blend CLI. // 建议使用 blend-settings 用户界面，而不是 blend 命令行界面。"
[site.src/.site-git]: https://git.blendos.co/blendOS/website.git "(GPL-3.0 OR LATER, CC-BY-SA-4.0) (Languages: HTML 78.2%, Python 21.6%, JavaScript 0.2%) blendOS website // blendOS 网站 /// Blend Docs Material // 混合文档材料 /// New BlendOS docs built with Material for MkDocs (. ghio: squidfunk/mkdocs-material). // 使用 Material for MkDocs 构建的全新 BlendOS 文档。"
[site.src/gh]: https://github.com/blend-os/website.git "(GPL-3.0 OR LATER, CC-BY-SA-4.0) (Languages: HTML 78.2%, Python 21.6%, JavaScript 0.2%) [READ-ONLY] blendOS website // blendOS 网站 /// Blend Docs Material // 混合文档材料 /// New BlendOS docs built with Material for MkDocs (. ghio: squidfunk/mkdocs-material). // 使用 Material for MkDocs 构建的全新 BlendOS 文档。"
[util:uni-pm.src/.site-git]: https://git.blendos.co/blendOS/bpkg.git "(Apache-2.0) (Languages: Python 98.0%, Shell 2.0%) A universal package manager for blendOS // BlendOS 通用软件包管理器 /// bpkg is a universal package manager for BlendOS compatible with BlendOS v3 and v4. // bpkg 是一个适用于 BlendOS 的通用软件包管理器，兼容 BlendOS v3 和 v4。 /// bpkg allows you to automatically search for a package through the repositories of your containers and automaticallly selects the best package according to your preference. // bpkg 允许您通过容器的存储库自动搜索软件包，并根据您的偏好自动选择最佳软件包。 /// NOTE: This program is alpha software. You may experience issues while using it. // 注意： 本程序为测试版软件，使用过程中可能会遇到问题。 /// (: bpkg install <package> # Install a package // 安装软件包 ## This will search for the package in your installed distros and install it from the first package manager it finds by your order of preference. Use user install <container> <package> to install to a specific container. // 这将搜索已安装发行版中的软件包，并按照您指定的优先级顺序，从找到的第一个软件包管理器进行安装。使用 user install <container> <package> 命令可以安装到指定的容器中。) (: bpkg remove <package> # Remove a package // 移除包裹 ## This will remove a package that you installed from the first container in your list the package is found in. Use user remove <container> <package> to remove from a specific container. // 这将从列表中第一个包含该软件包的容器中移除您安装的软件包。使用 user remove <container> <package> 可以从特定容器中移除该软件包。) (: bpkg update # Update packages // 更新包 ## This will update all the packages in all your containers. If update_flatpak is set to true in the config, this will also update your flatpaks. // 这将更新所有容器中的所有软件包。如果在配置中将 update_flatpak 设置为 true，则还会更新您的 Flatpak 文件。) (: bpkg search <package> # Package Search // 包裹搜索 ## You can search for a package in all of your containers. // 您可以在所有容器中搜索包裹。) (: bpkg list-containers # Container list // 容器列表 ## Lists all containers on your system (even if they aren't in the config). // 列出系统上的所有容器（即使它们不在配置中）。) (: bpkg overwrite-config # Config Regeneration // 配置重生成 ## This will pull all your containers and generate a new config file, overwriting your current one. It is recommended to run this after creating or deleting containers. // 这将拉取所有容器并生成一个新的配置文件，覆盖当前文件。建议在创建或删除容器后运行此命令。)"
[util:uni-pm.src/gh]: https://github.com/blend-os/bpkg.git "(Apache-2.0) (Languages: Python 98.0%, Shell 2.0%) [READ-ONLY] A universal package manager for blendOS // BlendOS 通用软件包管理器 /// bpkg is a universal package manager for BlendOS compatible with BlendOS v3 and v4. // bpkg 是一个适用于 BlendOS 的通用软件包管理器，兼容 BlendOS v3 和 v4。 /// bpkg allows you to automatically search for a package through the repositories of your containers and automaticallly selects the best package according to your preference. // bpkg 允许您通过容器的存储库自动搜索软件包，并根据您的偏好自动选择最佳软件包。 /// NOTE: This program is alpha software. You may experience issues while using it. // 注意： 本程序为测试版软件，使用过程中可能会遇到问题。 /// (: bpkg install <package> # Install a package // 安装软件包 ## This will search for the package in your installed distros and install it from the first package manager it finds by your order of preference. Use user install <container> <package> to install to a specific container. // 这将搜索已安装发行版中的软件包，并按照您指定的优先级顺序，从找到的第一个软件包管理器进行安装。使用 user install <container> <package> 命令可以安装到指定的容器中。) (: bpkg remove <package> # Remove a package // 移除包裹 ## This will remove a package that you installed from the first container in your list the package is found in. Use user remove <container> <package> to remove from a specific container. // 这将从列表中第一个包含该软件包的容器中移除您安装的软件包。使用 user remove <container> <package> 可以从特定容器中移除该软件包。) (: bpkg update # Update packages // 更新包 ## This will update all the packages in all your containers. If update_flatpak is set to true in the config, this will also update your flatpaks. // 这将更新所有容器中的所有软件包。如果在配置中将 update_flatpak 设置为 true，则还会更新您的 Flatpak 文件。) (: bpkg search <package> # Package Search // 包裹搜索 ## You can search for a package in all of your containers. // 您可以在所有容器中搜索包裹。) (: bpkg list-containers # Container list // 容器列表 ## Lists all containers on your system (even if they aren't in the config). // 列出系统上的所有容器（即使它们不在配置中）。) (: bpkg overwrite-config # Config Regeneration // 配置重生成 ## This will pull all your containers and generate a new config file, overwriting your current one. It is recommended to run this after creating or deleting containers. // 这将拉取所有容器并生成一个新的配置文件，覆盖当前文件。建议在创建或删除容器后运行此命令。)"
[tool:isomirrors-sync.src/.site-git]: https://git.blendos.co/blendOS/blendl.git "(Apache-2.0) (Languages: Python 100.0%) A simple Python script to sync the blendOS ISO to mirrors. // 一个简单的 Python 脚本，用于将 blendOS ISO 同步到镜像站点。"
[tool:isomirrors-sync.src/gh]: https://github.com/blend-os/blenDL.git "(Apache-2.0) (Languages: Python 100.0%) [READ-ONLY] A simple Python script to sync the blendOS ISO to mirrors. // 一个简单的 Python 脚本，用于将 blendOS ISO 同步到镜像站点。"
[tool:image-builder.src/.site-git]: https://git.blendos.co/blendOS/image-builder.git "(GPL-3.0 OR LATER) (Languages: Shell 100.0%) blendOS ISO builder // BlendOS ISO 构建器"
[tool:image-builder.src/gh]: https://github.com/blend-os/image-builder.git "(GPL-3.0 OR LATER) (Languages: Shell 100.0%) [READ-ONLY] blendOS ISO builder // BlendOS ISO 构建器"
