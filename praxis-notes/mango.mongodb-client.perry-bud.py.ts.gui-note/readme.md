[src/gh]: https://github.com/MangoQuery/app.git "(MIT ~: Skelpo GmbH) (Languages: TypeScript 49.2%, Python 40.7%, Shell 10.1%) A fast, native MongoDB GUI — built with Perry // 一个快速、原生的 MongoDB 图形用户界面——使用 Perry 构建 /// MongoDB, finally fast. // MongoDB，终于快了。 /// Mango is a native desktop MongoDB client written in TypeScript and compiled to native ARM64/x86_64 using Perry. No Electron, no JVM, no runtime overhead — just a small, fast binary that talks directly to your database. // Mango 是一个原生桌面 MongoDB 客户端，使用 TypeScript 编写，并使用 Perry 编译为原生 ARM64/x86_64 架构。它无需 Electron、JVM 或运行时开销——只是一个体积小、速度快的二进制文件，可以直接与数据库通信。 /// Features // 特征 /// - Connection management — Save, edit, and delete connection profiles. Connect via host/port or full connection string (SCRAM-SHA-256, TLS). // 连接管理 ——保存、编辑和删除连接配置文件。通过主机/端口或完整连接字符串（SCRAM-SHA-256、TLS）进行连接。 /// - Database & collection browser — Query any collection with custom filters, sort, and projection. // 数据库和集合浏览器 ——使用自定义过滤器、排序和投影查询任何集合。 /// - Document viewer — Browse query results as formatted JSON. // 文档查看器 — 以格式化的 JSON 格式浏览查询结果。 /// - Document editing — Edit documents inline and save changes back to MongoDB. // 文档编辑 ——直接在文档中编辑文档并将更改保存回 MongoDB。 /// - Document CRUD — Insert, update, delete, and duplicate documents. // 文档 CRUD — 插入、更新、删除和复制文档。 /// - Index listing — View indexes with key, uniqueness, and size info. // 索引列表 — 查看索引的键、唯一性和大小信息。 /// - Dark & light mode — Follows system preference automatically. // 深色和浅色模式 ——自动遵循系统偏好设置。 /// - Cross-platform — Targets macOS (AppKit), iOS (UIKit), Android (Views), Linux (GTK4), Windows (Win32), and the browser (WebAssembly + DOM via Perry's --target web). // 跨平台 — 目标平台包括 macOS (AppKit)、iOS (UIKit)、Android (Views)、Linux (GTK4)、Windows (Win32) 和浏览器 (WebAssembly + DOM，通过 Perry 的 --target web )。 /// Build // 建造 /// (: bun install # Install type-checking dependencies) (: perry compile src/app.ts --output mango # Compile to native binary ;: ./mango) (: perry compile src/app.ts --target web -o dist/mango.html # Compile to a single self-contained HTML+WASM file (~4 MB) ;: python3 -m http.server 8765 -d dist # Serve over HTTP (file:// won't work — fetch() hits CORS errors) ;: open http://localhost:8765/mango.html) /// How It Works // 工作原理 /// Perry compiles TypeScript to native machine code via SWC (parsing) and LLVM (codegen). Platform UI is rendered through native frameworks — AppKit on macOS, UIKit on iOS, GTK4 on Linux, etc. MongoDB access uses Perry's built-in mongodb package, which wraps the Rust MongoDB driver directly in the compiled binary. // Perry 通过 SWC（解析）和 LLVM（代码生成）将 TypeScript 编译为原生机器代码。平台 UI 通过原生框架渲染——macOS 使用 AppKit，iOS 使用 UIKit，Linux 使用 GTK4 等。MongoDB 访问使用 Perry 内置的 mongodb 包，该包将 Rust MongoDB 驱动程序直接封装在编译后的二进制文件中。 /// The result is a ~7 MB self-contained binary with sub-100 MB RAM usage and instant cold start. // 最终生成的是一个约 7 MB 的独立二进制文件，内存占用低于 100 MB，并且能够立即冷启动。 /// The web build is the same source code, compiled by Perry to WebAssembly with a JavaScript bridge that maps every Perry widget to a DOM element. SQLite-backed connection storage degrades to an in-memory transient store on web; the Hone code editor's native bindings degrade to no-ops; and you'll need to point at a Mango Server proxy if you want to talk to MongoDB from the browser. Everything else (UI, navigation, query builder, document viewer, dark/light mode, i18n) works the same as the native builds. // Web 版本与原生版本使用相同的源代码，由 Perry 编译为 WebAssembly，并通过 JavaScript 桥接将每个 Perry 小部件映射到一个 DOM 元素。基于 SQLite 的连接存储会降级为内存中的临时存储；Hone 代码编辑器的原生绑定会降级为空操作；如果要从浏览器访问 MongoDB，则需要指定一个 Mango Server 代理。其他所有功能（UI、导航、查询构建器、文档查看器、深色/浅色模式、国际化）均与原生版本相同。"
[site/com]: https://mangoquery.com/ "MongoDB， 终于快了。 // MongoDB, finally fast. /// 一款即开即用、毫不干扰的原生 MongoDB 图形界面。基于 Perry 构建，编译为单一二进制文件。 // A native MongoDB GUI that starts instantly and stays out of your way. Built with Perry, compiled to a single binary. /// ~7 MB - Binary size; <100 MB - RAM usage; <1s - Cold start; 0 - Electron /// Everything you need // 你需要的一切 /// Browse, query, and manage your MongoDB databases with a clean native interface. // 使用简洁的原生界面浏览、查询和管理您的 MongoDB 数据库。 /// - Connection Management // 连接管理 ///: Save and organize connection profiles. Supports host/port, connection strings, SCRAM-SHA-256, and TLS. // 保存和整理连接配置文件。支持主机/端口、连接字符串、SCRAM-SHA-256 和 TLS。 /// - Query Builder // 查询生成器 ///: Write filters, sort, and project with instant results. Query any collection with the full power of MongoDB's query language. // 编写筛选器、排序和投影，即可立即获得结果。利用 MongoDB 查询语言的强大功能，查询任何集合。 /// - Document Editing // 文档编辑 ///: View, edit, insert, delete, and duplicate documents inline. Changes are saved directly to MongoDB. // 支持在线查看、编辑、插入、删除和复制文档。更改将直接保存到 MongoDB。 /// - Collection Browser // 收藏浏览器 ///: Navigate databases and collections with stats at a glance. See document counts, storage size, and index info. // 通过概览统计信息浏览数据库和集合。查看文档数量、存储大小和索引信息。 /// - Index Viewer // 索引查看器 ///: List all indexes with their keys, uniqueness constraints, and sizes. Understand your data access patterns. // 列出所有索引及其键、唯一性约束和大小。了解您的数据访问模式。 /// - Dark & Light Mode // 深色和浅色模式 ///: Follows your system appearance automatically. A warm cream palette in light mode, deep charcoal in dark. // 自动跟随系统外观。浅色模式下为暖米色调，深色模式下为深炭灰色。 /// Built different // 建造方式不同 /// Mango compiles TypeScript to native machine code using Perry. No web views, no garbage collection, no runtime bloat. // Mango 使用 Perry 将 TypeScript 编译成原生机器代码。没有 Web 视图，没有垃圾回收，运行时也不会臃肿。 /// [Perry // 佩里] [TypeScript] [Cranelift // 起重机] [SWC] [Rust MongoDB Driver // Rust MongoDB 驱动程序] [Native UI] [ARM64 / x86_64] /// Try Mango today // 今天就来尝尝芒果吧！ /// Open source, free forever. Download the binary or build from source. // 开源，永久免费。可下载二进制文件或从源代码编译。 /// Runs everywhere // 到处跑 /// One codebase, native performance on every platform. // 一套代码库，在所有平台上都能实现原生性能。 /// [macOS] [Android] [iOS] [Linux] [Windows]"
[knowsby]: https://github.com/PerryTS/perry.git "Mango // 芒果 /// Native MongoDB GUI. ~7 MB binary, <100 MB RAM, sub-second cold start. // 原生 MongoDB 图形用户界面。二进制文件大小约 7 MB，内存占用小于 100 MB，冷启动时间不到一秒。"
