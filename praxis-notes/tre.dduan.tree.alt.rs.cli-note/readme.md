[src/gh]: https://github.com/dduan/tre.git "(MIT) (Languages: Rust 74.3%, PowerShell 9.8%, Shell 6.9%, Roff 3.7%, Nix 2.9%, Python 1.6%, Makefile 0.8%) Tree command, improved. // 改进的树命令。 /// A modern alternative to the tree command that: // 一个现代的 tree 命令替代品，具有以下功能： /// - lists directory structure in a tree-like diagram, like the classics. // 以树状图形式列出目录结构，类似于经典工具。 /// - skips ignored files in git repositories per .gitignore setting. // 根据 .gitignore 设置，跳过 git 仓库中的忽略文件。 /// - creates shell aliases for each listing that opens the files for you. // 为每个列表创建 shell 别名，以便为您打开文件。 /// - output in colors, respecting LS_COLORS settings when they exist. // 彩色输出，当存在 LS_COLORS 设置时，会尊重该设置。"
[pkg.x/x-cmd]: https://x-cmd.com/install/tre "(: xd alp -- tre) (: x install -- tre)"
[pkg.cargo/crates]: https://crates.io/crates/tre-command "(: cargo install -- tre-command) (MIT) (17.7 KiB)"
