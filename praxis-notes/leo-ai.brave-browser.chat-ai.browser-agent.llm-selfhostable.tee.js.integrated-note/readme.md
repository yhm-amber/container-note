[app.wui/brave-integrated]: brave://leo-ai/
[site/brave.com]: https://brave.com/leo/ "Brave Leo AI // 勇敢狮子座人工智能 /// The smart AI assistant built right into your browser. Ask questions, summarize pages, create new content, and more. Privately. // 内置于浏览器中的智能 AI 助手。提问、总结页面内容、创建新内容等等。一切尽在掌控。 /// Make every page interactive // 让每个页面都具有交互性 ///: Chat with Leo about any topic. Ask Leo to summarize webpages, generate content, translate, analyze text, and more. Without leaving the page. // 和 Leo 畅聊任何话题。让 Leo 帮你总结网页内容、生成内容、翻译、分析文本等等。所有操作都无需离开当前页面。 /// Private, anonymous, and secure // 私密、匿名且安全 ///: Leo doesn’t retain or share chats, or use them for additional model training. No account or login is required. Just open and chat. Privately. // Leo 不会保存或分享聊天记录，也不会将其用于额外的模型训练。无需注册或登录。只需打开即可私密聊天。 /// Get insights from docs // 从文档中获取见解 /// Leo can even analyze PDFs, Google Docs & Google Sheets, and more, getting you the info you need. Faster. // Leo 甚至可以分析 PDF、Google 文档和 Google 表格等文件，更快地为您提供所需信息。"
[leo.wiki/gh]: https://github.com/brave/brave-browser/wiki/Brave-Leo "Brave Leo is a private AI smart assistant that enhances your use of the Internet. // Brave Leo 是一款私人人工智能智能助手，可增强您的互联网使用体验。 /// Ask Leo to summarize a webpage you’re viewing. Or ask questions about the page, and get answers, clarifications, and other viewpoints. Brave Leo can also create content, translate between different languages, and transcribe audio and video. It can even have back-and-forth conversations. // 你可以让 Leo 帮你总结正在浏览的网页。或者向它提问，它会给出答案、解释和其他观点。勇敢的 Leo 还能创作内容、进行语言翻译，以及转录音频和视频。它甚至可以进行双向对话。"
[byom-intro.blog/brave.site]: https://brave.com/blog/byom-nightly/ "Bring Your Own Model (BYOM): using Brave Leo with your own LLMs // 自带模型（BYOM）：将 Brave Leo 与您自己的 LLM 结合使用 /// Note: As of August 22, 2024, BYOM is now in general release and available to all desktop users (with browser update version 1.69 or higher). // 注意 ：截至 2024 年 8 月 22 日，BYOM 已正式发布，所有桌面用户均可使用（浏览器版本需更新至 1.69 或更高版本）。 /// Our first step towards that promise is called “bring your own model” (or “BYOM” for short). This optional new way of using Leo, Brave’s native browser AI, allows users to link it directly with their own AI models. BYOM allows users to run Leo with local models running safely and privately on their own machines. Users will be able to chat or ask questions on webpages (and docs and PDFs) to Leo without their content ever having to leave the device. // 我们实现这一承诺的第一步是“自带模型”（简称“BYOM”）。这种使用 Leo（Brave 浏览器的原生 AI）的全新可选方式，允许用户将其与自己的 AI 模型直接连接。BYOM 使用户能够在自己的设备上安全私密地运行本地模型，从而实现 Leo 的运行。用户可以在网页（ 以及文档和 PDF ）上与 Leo 聊天或提问，而无需将内容传输到设备之外。 /// BYOM isn’t limited to just local models, either–BYOM users will also have the flexibility to connect Leo to remote models running on their own servers, or that are run by third-parties (e.g. ChatGPT). This opens new possibilities for users and organizations to leverage proprietary or custom models while still benefiting from the convenience of querying the AI directly within the browser. // BYOM 不仅限于本地模型——BYOM 用户还可以灵活地将 Leo 连接到运行在他们自己服务器上的远程模型，或由第三方（例如 ChatGPT）运行的模型。这为用户和组织利用专有或自定义模型开辟了新的可能性，同时还能享受直接在浏览器中查询 AI 的便利。 /// With BYOM, the requests are made directly from the user’s device to the specified model endpoint, bypassing Brave entirely. Brave does not act as an intermediary and has absolutely no access to—or visibility into—the traffic between the user and the model. This direct connection ensures end-to-end control for the user. // 使用 BYOM，请求直接从用户的设备发送到指定的模型端点，完全绕过 Brave 浏览器。Brave 不充当中间人，也完全无法访问或查看用户与模型之间的流量。这种直接连接确保用户拥有端到端的控制权。 /// To add your own local model to Leo, open the Brave browser and visit Settings, and then Leo. Scroll to the Bring your own model section and click Add new model. // 要将您自己的本地模型添加到 Leo，请打开 Brave 浏览器并访问 “设置” ，然后选择 “Leo”。 滚动到 “自带模型” 部分，然后单击 “添加新模型” 。 /// You’ll then be brought to a new interface where you can add the details of your model. The required fields are the following: // 接下来您将进入一个新界面，您可以在其中添加模型的详细信息。必填字段如下： /// - Label: The name of the model as it will appear in the model selection menu. // 标签 ：模型名称，将显示在模型选择菜单中。 /// - Model request name: The name of the model as it should appear in the request to the serving framework, e.g. llama-3 (note that if this name doesn’t match exactly what is expected by the serving framework, the integration will not work). // 模型请求名称 ：模型的名称，它应该出现在发送到服务框架的请求中，例如 llama-3（请注意，如果此名称与服务框架所期望的名称不完全匹配，则集成将无法工作）。 /// - Server endpoint: The url where your serving framework is “listening” for requests. If you’re not sure, check the serving framework documentation (For Ollama, this is always http://localhost:11434/v1/chat/completions). // 服务器端点 ：您的服务框架“监听”请求的 URL。如果您不确定，请查看服务框架的文档（对于 Ollama，此值始终为 http://localhost:11434/v1/chat/completions ）。 /// - API Key: third party frameworks may require authentication credentials, such as an API key or an access token. These will be added to the request header. // API 密钥 ：第三方框架可能需要身份验证凭据，例如 API 密钥或访问令牌。这些信息将添加到请求标头中。 /// Click Add model; your local model should now appear in the Leo model menu. Simply select it to use it to power your browser AI. // 点击 “添加模型” ；您的本地模型现在应该会出现在 Leo 模型菜单中。只需选择它即可使用它来驱动您的浏览器 AI。 /// Note that while we’ve used Ollama in this section, as we think it’s one of the most user-friendly frameworks to set up and run local models, the BYOM feature can be used with any local serving framework with an exposed endpoint and that conforms to the OpenAI chat protocol. // 请注意，虽然我们在本节中使用了 Ollama，因为我们认为它是设置和运行本地模型最用户友好的框架之一，但 BYOM 功能可以与任何具有公开端点且符合 OpenAI 聊天协议的本地服务框架一起使用。"
[search-chatplg.wui/.search]: https://search.brave.com/ask "Ask Brave"
[tee-news.blog/brave.site]: https://brave.com/blog/browser-ai-tee/ "Verifiable Privacy and Transparency: A new frontier for Brave AI privacy // 可验证的隐私和透明度：Brave AI 隐私的新前沿 /// Today, Brave Leo (. web: brave.com/leo) offers a new capability for cryptographically-verifiable privacy and transparency by deploying LLMs with NEAR AI (. web: docs.near.ai/cloud/private-inference) Nvidia-backed Trusted Execution Environments (aka “TEEs”, see “Know more about TEEs” section below for details). Brave believes that users must be able to verify that they are having private conversations with the model they expect. This is available in Brave Nightly (our testing and development channel) for early experimentation with DeepSeek V3.1 (we plan to extend this to more models in the future based on feedback). // 今天， Brave Leo 浏览器通过部署基于 NEAR AI 和英伟达支持的可信执行环境（简称“TEE”，详情请参见下文“了解更多关于 TEE 的信息”）的 LLM，为用户提供了一项全新的 、可加密验证的隐私和透明度功能。Brave 认为，用户必须能够验证他们正在与预期的模型进行私密对话。这项功能已在 Brave Nightly 版本 （我们的测试和开发渠道）中推出，供用户早期体验 DeepSeek V3.1 模型（我们计划未来根据用户反馈将其扩展到更多模型）。 /// By integrating Trusted Execution Environments, Brave Leo moves towards offering unmatched verifiable privacy and transparency in AI assistants, in effect transitioning from the “trust me bro” process to the privacy-by-design approach that Brave aspires to: “trust but verify (. web: brendaneich.com/2014/01/trust-but-verify)”. // 通过集成可信执行环境，Brave Leo 朝着在 AI 助手方面提供无与伦比的可验证隐私和透明度迈进，实际上是从“相信我，兄弟”的过程过渡到 Brave 所追求的隐私设计方法：“ 信任，但要验证 ”。 /// Leo is the Brave browser’s integrated, privacy-preserving AI assistant. It is powered by state-of-the-art LLMs while protecting user privacy through a privacy-preserving subscription model, no chats in the cloud, no context in the cloud, no IP address logging, and no training on users’ conversations. // Leo 是 Brave 浏览器集成的、保护隐私的 AI 助手。它采用最先进的 LLM 技术，并通过保护隐私的订阅模式来保护用户隐私，不进行云端聊天，不存储云端上下文信息，不记录 IP 地址，也不利用用户对话进行训练。 /// Brave believes that users must be able to: // Brave 认为用户必须能够： /// 1. Verifiable Privacy—Users must be able to verify that Leo’s privacy guarantees match public privacy promises. // 可验证的隐私 ——用户必须能够验证 Leo 的隐私保证与公开的隐私承诺相符。 /// 2. Verifiable Transparency in Model Selection—Users must be able to verify that Leo’s responses are, in fact, coming from a machine learning model the user expects (or pays for, in the case of Leo Premium). // 模型选择中的可验证透明度 ——用户必须能够验证 Leo 的响应是否确实来自用户期望的（或在 Leo Premium 的情况下付费的）机器学习模型。 /// The absence of these user-first features in other competing chatbot providers introduces a risk of privacy-washing. It has also been shown—both in research (e.g., “Are You Getting What You Pay For? Auditing Model Substitution in LLM APIs (. arxiv: 2504.04715)”) and in practice (e.g., backlash (. web: adversa.ai/blog/promisqroute-gpt-5-ai-router-novel-vulnerability-class) against ChatGPT)—that chatbot providers may have incentives to silently replace an expensive LLM with a cheaper-to-run, weaker LLM, and to return the results from the weaker model to the user in order to reduce GPU costs and increase profit margins. // 其他竞争聊天机器人提供商缺乏这些以用户为中心的功能，这会带来隐私洗白的风险。研究（例如，“ 你付的钱是否物有所值？LLM API 中的模型替换审计 ”）和实践（例如，针对 ChatGPT 的强烈反对 ）均表明，聊天机器人提供商可能出于降低 GPU 成本和提高利润率的目的，悄悄地用运行成本更低、性能更弱的 LLM 模型替换成本更高的 LLM 模型，并将性能较弱模型的运行结果返回给用户。 /// Brave begins this journey by removing the need to trust LLM/API providers, using Confidential LLM Computing on NEAR AI TEEs. Brave uses NEAR AI TEE-enabled Nvidia GPUs to ensure confidentiality and integrity by creating secure enclaves where data and code are processed with encryption. // Brave 浏览器通过 NEAR AI TEE 上的机密 LLM 计算 ，消除了对 LLM/API 提供商的信任需求，从而开启了这段旅程。Brave 使用支持 NEAR AI TEE 的 Nvidia GPU，通过创建安全区域来确保机密性和完整性，在这些安全区域内，数据和代码均经过加密处理。 /// These TEEs produce a cryptographic attestation report that includes measurements (hashes) of the loaded model and execution code. Such attestation reports can be cryptographically verified to gain absolute assurance that: // 这些可信执行环境 (TEE) 会生成一份加密认证报告，其中包含已加载模型和执行代码的测量值（哈希值）。此类认证报告可以进行加密验证，从而获得以下绝对保证： /// 1. A secure environment is created through a genuine Nvidia GPU TEE, which generates cryptographic proofs of its integrity and configuration. // 通过真正的 Nvidia GPU TEE 创建安全环境，该 TEE 会生成其完整性和配置的加密证明。 /// 2. Inference runs in this secure environment with full encryption that keeps user data private — no one can see any data passed by the user to the computation, or any results of the computation. // 推理在这个安全的环境中运行，采用完全加密，确保用户数据的私密性——任何人都看不到用户传递给计算的任何数据，也看不到计算的任何结果。 /// 3. The model and open source code that users expected are running unmodified by cryptographically signing every computation.  // 用户所期望的模型和开源代码未经修改地运行，每次计算都经过加密签名。 /// In Stage 1 of our development, Brave performs the cryptographic verification, and users can use “Verifiably Private with NEAR AI TEE” in Leo as follows: // 在我们的开发第一阶段，Brave 执行加密验证，用户可以在 Leo 中使用“使用 NEAR AI TEE 进行可验证的隐私保护”， 具体操作如下： /// - User selects DeepSeek V3.1 with label of Verifiably Private with NEAR AI TEE available in Leo on Brave Nightly // 用户选择带有 “可验证隐私”标签的 DeepSeek V3.1，NEAR AI TEE 可在 Brave Nightly 的 Leo 版本中使用。 /// - Brave performs NEAR AI TEE Verification, validating a cryptographic chain from NEAR open-source code (. gh: nearai/cloud-api.git) to hardware-attestation execution, ensuring responses are generated in a genuine Nvidia TEE running a specific version of the NEAR open-source server // Brave 浏览器执行 NEAR AI TEE 验证，验证从 NEAR 开源代码到硬件认证执行的加密链，确保响应是在运行特定版本 NEAR 开源代码服务器的真正的 Nvidia TEE 上生成的。 /// - Brave transmits the outcome of verification to users by showing a verified green label (depicted in the screenshot below) // Brave 浏览器会将验证结果以绿色“已验证”标签的形式（如下图所示）告知用户。 /// - User starts chatting without having to trust the API provider to see or log their data and responses // 用户无需信任 API 提供商即可开始聊天，也无需允许其查看或记录用户的数据和回复。 /// Today, we are excited to release TEE-based Confidential DeepSeek V3.1 Computing in Brave Nightly (our testing and development channel) for early experimentation and feedback. // 今天，我们很高兴地在 Brave Nightly（我们的测试和开发渠道）中发布基于 TEE 的 Confidential DeepSeek V3.1 计算，以便进行早期实验和反馈。 /// Before rolling this out more broadly, we’re focused on two things: // 在更广泛地推广这项措施之前，我们主要关注两件事： /// 1. No user-facing performance overhead—TEEs introduce some computational overhead on the GPU. However, recent advances significantly reduce this overhead—down to nearly zero in some cases—as shown in Confidential Computing on NVIDIA Hopper GPUs: A Performance Benchmark Study. We want to ensure our users don’t experience performance regressions. // 无面向用户的性能开销 ——TEE 会在 GPU 上引入一些计算开销。然而，正如 《NVIDIA Hopper GPU 上的机密计算：性能基准测试研究》 所示，最近的技术进步已显著降低了这种开销，在某些情况下甚至接近于零。我们希望确保用户不会遇到性能下降的问题。 /// 2. End-to-end verification—We’re actively researching how to extend confidential computing in Leo so that users can verify their trust in the entire pipeline, along with Brave open-sourcing all stages. In particular, we are planning to move the verification closer to users so they can reconfirm the API verification on their own in the Brave browser.   // 端到端验证 ——我们正在积极研究如何在 Leo 中扩展机密计算，以便用户能够验证他们对整个流程的信任，同时 Brave 也将开源所有阶段。特别是，我们计划将验证过程更靠近用户，以便他们可以在 Brave 浏览器中自行再次确认 API 验证。 /// A Trusted Execution Environment (TEE) is a secure area of a processor that provides an isolated computing environment, separate from traditional rich runtime environments such as an Operating System (OS). A TEE enforces strong hardware-backed guarantees of confidentiality and integrity for the code and data it hosts. These guarantees are achieved through enforcements such as dedicated memory that is accessible only to the TEE. // 可信执行环境 (TEE) 是处理器中一个安全区域，它提供了一个与传统丰富的运行时环境（例如操作系统 (OS)）隔离的计算环境。TEE 通过硬件层面的强大保障，确保其托管的代码和数据的机密性和完整性。这些保障是通过诸如专用内存等措施实现的，这些内存仅供 TEE 访问。 /// Hardware isolation ensures that even a fully compromised OS cannot access or tamper with any code or data residing inside the TEE. In addition to this, TEEs expose unique hardware primitives such as secure boot and remote attestation to ensure only trusted code is loaded into the TEE and so that external parties are able to verify the integrity of TEE. // 硬件隔离确保即使操作系统完全被攻破，也无法访问或篡改 TEE 内部的任何代码或数据。此外，TEE 还提供安全启动和远程认证等独特的硬件原语，以确保只有可信代码才能加载到 TEE 中，并使外部各方能够验证 TEE 的完整性。 /// Chip manufacturers have enabled TEEs on CPUs (traditionally) and GPUs (recently). The combination of TEE-enabled CPUs (e.g., Intel TDX) and TEE-enabled GPUs (e.g., Nvidia Hopper) enables end-to-end confidentiality, and integrity-preserving computation of LLM inference with minimal performance penalty. // 芯片制造商已在 CPU（传统上）和 GPU（最近）上启用了 TEE（终端增强实体）技术。结合使用支持 TEE 的 CPU（例如 Intel TDX）和支持 TEE 的 GPU（例如 Nvidia Hopper），可以实现端到端的机密性，并在性能损失最小的情况下，以保持完整性的方式进行 LLM 推理计算。"
[tee-news.blog/privacyguides.org]: https://privacyguides.org/news/2025/11/20/brave-announces-verifiable-and-transparent-tee-support-in-leo/ "Brave Announces Verifiable and Transparent TEE Support in Leo // Brave 宣布在 Leo 版本中提供可验证且透明的 TEE 支持 /// Today, Brave announced that their Leo AI assistant inside of the Brave browser will utilize NEAR AI Nvidia-backed Trusted Execution Environments (TEE). // 今天，Brave 宣布其 Brave 浏览器中的 Leo AI 助手将利用 NEAR AI 和 Nvidia 支持的可信执行环境 (TEE)。 /// TEEs provide an isolated, secure environment for processing user requests. The isolation works at the hardware level, and allows users to remotely cryptographically attest that their requests are actually running in a secure environment and that they're using the desired LLM. // TEE 为处理用户请求提供了一个隔离、安全的环境。这种隔离在硬件层面实现，允许用户远程加密验证其请求确实在安全环境中运行，并且正在使用所需的 LLM。 /// The Confidential Computing (. web: confidentialcomputing.io) technology is designed to protect against malicious infrastructure providers, including the owners of the servers the models are running on, and provide strong integrity guarantees, preventing tampering of your results. // 机密计算技术旨在保护用户免受恶意基础设施提供商（包括运行模型的服务器的所有者）的侵害，并提供强大的完整性保证，防止篡改结果。 /// Hardware isolation ensures that even a fully compromised OS cannot access or tamper with any code or data residing inside the TEE. In addition to this, TEEs expose unique hardware primitives such as secure boot and remote attestation to ensure only trusted code is loaded into the TEE and so that external parties are able to verify the integrity of TEE. // 硬件隔离确保即使操作系统完全被攻破，也无法访问或篡改 TEE 内部的任何代码或数据。此外，TEE 还提供安全启动和远程认证等独特的硬件原语，以确保只有可信代码才能加载到 TEE 中，并使外部各方能够验证 TEE 的完整性。 /// The new technology is available in Brave Nightly builds for early testing and feedback. // 这项新技术已在 Brave Nightly 版本中提供，供用户早期测试和反馈。 /// This announcement comes as the latest in a series of announcements from various AI providers who wish to improve the privacy and security of their AI offerings. First was Apple with their Private Cloud Compute (. web: security.apple.com/blog/private-cloud-compute), then Google with their Private AI Compute (. web: blog.google/technology/ai/google-private-ai-compute), then OpenAI announced (. web: openai.com/index/fighting-nyt-user-privacy-invasion) plans to implement “client-side encryption” for ChatGPT. // 此次公告是众多人工智能提供商为提升其人工智能产品的隐私和安全而发布的一系列公告中的最新一项。此前，苹果公司率先推出了私有云计算服务，随后谷歌推出了私有人工智能计算服务 ，OpenAI 也宣布计划为 ChatGPT 实现“客户端加密”。 /// This is another great development for private AI, I hope to see more and more AI providers adopting technologies like this to protect the privacy of their users. // 这是私有人工智能领域的又一重大进展，我希望看到越来越多的人工智能提供商采用此类技术来保护用户的隐私。"
[nearcloud-api.src/gh]: https://github.com/nearai/cloud-api.git "(PolyForm-Strict-1.0.0) (Languages: Rust 98.7%, Other 1.3%) NEAR AI Cloud API // NEAR AI 云 API /// A Rust-based cloud API for AI model inference, conversation management, and organization administration. Part of the NEAR AI platform alongside the Chat API. // 一个基于 Rust 的云端 API，用于 AI 模型推理、对话管理和组织管理。它是 NEAR AI 平台的一部分，与聊天 API 同属一个平台。"
[private-inference.docs/near.ai]: https://docs.near.ai/cloud/private-inference/ "Private Inference // 私有推理 /// When you use traditional AI services, your data passes through systems controlled by cloud providers and AI companies. Your prompts, the AI's responses, and even the processing of your requests are all visible to these third parties. This creates serious security concerns for sensitive applications. // 使用传统人工智能服务时，您的数据会经过由云服务提供商和人工智能公司控制的系统。您的提示、人工智能的响应，甚至您请求的处理过程，对这些第三方都是可见的。这会给敏感应用带来严重的安全隐患。 /// Private inference solves this problem. It ensures that AI computations happen in a completely isolated environment where no one—not the cloud provider, not the model provider, not even NEAR AI can access your data. At the same time, you can independently verify that your requests were actually processed in this secure environment through cryptographic attestation. // 私有推理解决了这个问题。 它确保人工智能计算在一个完全隔离的环境中进行，任何人都无法访问您的数据——无论是云服务提供商、模型提供商，还是 NEAR AI。同时，您可以通过加密认证独立验证您的请求是否确实在这个安全环境中处理过。 /// This guide explains how NEAR AI Cloud implements private inference using Trusted Execution Environments (TEEs), the architecture that protects your data, and the security guarantees you can rely on. // 本指南解释了 NEAR AI Cloud 如何使用可信执行环境 (TEE) 实现私有推理，该架构可保护您的数据，并提供您可以信赖的安全保证。 /// ## What is Private Inference? // 什么是私有推理？ /// Private inference is a method of running AI models where both your input data and the model's outputs remain completely hidden from everyone except the user and client even while the computation happens on remote servers you don't control. // 私有推理是一种运行 AI 模型的方法，即使计算发生在您无法控制的远程服务器上，您的输入数据和模型的输出也会对除用户和客户端以外的所有人完全隐藏。 /// Traditional cloud AI services require you to trust that providers won't access your data. Private inference eliminates this need for trust by using hardware-based security that makes it technically impossible for anyone to see your data, even with physical access to the servers. // 传统的云端人工智能服务需要您信任服务提供商不会访问您的数据。而私有推理通过使用基于硬件的安全措施，消除了这种信任需求，即使有人能够物理访问服务器，从技术上讲，也根本不可能查看您的数据。 /// NEAR AI Cloud's private inference provides three core guarantees: // NEAR AI Cloud 的私有推理提供三大核心保障： /// - Complete Privacy // 完全隐私 ///: Your prompts, model weights, and outputs are encrypted and isolated in hardware-secured environments. Infrastructure providers, model providers, and NEAR cannot access your data at any point in the process. // 您的提示信息、模型权重和输出结果均经过加密，并隔离在硬件安全的环境中。基础设施提供商、模型提供商和 NEAR 在任何阶段都无法访问您的数据。 /// - Cryptographic Verification // 密码验证 ///: Every computation generates cryptographic proof that it occurred inside a genuine, secure TEE. You can independently verify that your AI requests were processed in a protected environment without trusting any third party. // 每次计算都会生成加密证明，证明其发生在真实、安全的终端环境 (TEE) 中。您可以独立验证您的 AI 请求是否在受保护的环境中处理，而无需信任任何第三方。 /// - Production Performance // 生产性能 ///: Hardware-accelerated TEEs with NVIDIA Confidential Computing deliver high-throughput inference with minimal latency overhead, making private inference practical for real-world applications. // 采用 NVIDIA Confidential Computing 的硬件加速 TEE 可实现高吞吐量推理，同时将延迟开销降至最低，从而使私密推理在实际应用中切实可行。 /// Trusted Execution Environment (TEE) // 可信执行环境（TEE） /// NEAR AI Cloud combines Intel TDX and NVIDIA TEE technologies to create isolated, secure environments for AI computation: // NEAR AI Cloud 结合了 Intel TDX 和 NVIDIA TEE 技术，为 AI 计算创建隔离、安全的环境： /// - Intel TDX(Trust Domain Extensions) (. web: intel.com/content/www/us/en/developer/tools/trust-domain-extensions/overview.html): Creates confidential virtual machines (CVMs) that isolate your AI workloads from the host system, preventing unauthorized access to data in memory. // Intel TDX（信任域扩展） ：创建机密虚拟机 (CVM)，将您的 AI 工作负载与主机系统隔离，防止未经授权访问内存中的数据。 /// - NVIDIA TEE (. web: nvidia.com/en-us/data-center/solutions/confidential-computing): Provides GPU-level isolation for model inference, ensuring model weights and computations remain completely private during processing. // NVIDIA TEE ：为模型推理提供 GPU 级隔离，确保模型权重和计算在处理过程中完全保密。 /// - Cryptographic Attestation (. web: docs.near.ai/cloud/verification): Each TEE environment generates cryptographic proofs of its integrity and configuration, enabling independent verification of the secure execution environment. // 加密证明 ：每个 TEE 环境都会生成其完整性和配置的加密证明，从而可以独立验证安全执行环境。 /// Client-Side Encryption // 客户端加密 /// If you're using a standard OpenAI SDK or curl, your prompts are automatically protected by TLS encryption—no additional setup required. // 如果您使用的是标准的 OpenAI SDK 或 curl，则您的提示信息会自动受到 TLS 加密保护——无需额外设置。 /// [Your Machine] >-(🔒 Encrypted)--> [Internet] >-(🔒 Encrypted)--> [TEE: TLS Termination → LLM] /// Key insight: TLS termination happens inside the TEE (green box), not at an external load balancer. Your prompts remain encrypted until they reach the secure enclave. // 关键信息： TLS 终止发生在 TEE（绿色框）内部，而不是外部负载均衡器上。您的请求在到达安全区域之前始终保持加密状态。 /// Here's why this works: // 原因如下： /// 1. Standard HTTPS = TLS encryption: When you make API calls using the OpenAI SDK or curl, you're connecting via HTTPS. This means TLS encryption is applied automatically by your client before any data leaves your machine. // 标准 HTTPS = TLS 加密 ：当您使用 OpenAI SDK 或 curl 进行 API 调用时，您将通过 HTTPS 连接。这意味着在任何数据离开您的计算机之前，您的客户端会自动应用 TLS 加密。 /// 2. TLS terminates inside the TEE: Unlike traditional cloud services where TLS terminates at an external load balancer, NEAR AI Cloud terminates TLS connections inside the Trusted Execution Environment. Your encrypted data travels from your laptop directly into the secure enclave before being decrypted. // TLS 连接在可信执行环境 (TEE) 内终止 ：与传统云服务在外部负载均衡器处终止 TLS 连接不同，NEAR AI Cloud 在可信执行环境 (TEE) 内终止 TLS 连接。您的加密数据在解密之前会直接从您的笔记本电脑传输到安全区域。 /// 3. No plaintext exposure: Because TLS termination happens within the TEE, your prompts are never exposed in plaintext outside the hardware-secured environment—not to network infrastructure, not to cloud providers, not to anyone. // 不会泄露明文 ：由于 TLS 终止发生在 TEE 内部，因此您的提示信息永远不会以明文形式泄露到硬件安全环境之外——不会泄露给网络基础设施，不会泄露给云提供商，也不会泄露给任何人。 /// This means you get the same seamless developer experience as any OpenAI-compatible API, while your data remains encrypted from your machine all the way into the secure TEE. // 这意味着您可以获得与任何 OpenAI 兼容 API 相同的无缝开发者体验，同时您的数据从您的计算机到安全的 TEE 始终保持加密状态。"
[cfdcomp.site/io]: https://confidentialcomputing.io/ "Confidential Computing Consortium // 机密计算联盟"
[llm-api-audit:2504.04715.paper/arxiv]: https://arxiv.org/abs/2504.04715 "[Submitted on 7 Apr 2025 (v1), last revised 29 Sep 2025 (this version, v2)] { Computation and Language (cs.CL); Cryptography and Security (cs.CR); Machine Learning (cs.LG) } (doi: 10.48550/arXiv.2504.04715) Are You Getting What You Pay For? Auditing Model Substitution in LLM APIs // 你真的物有所值吗？LLM API 中的模型替换审计。 /// Commercial Large Language Model (LLM) APIs create a fundamental trust problem: users pay for specific models but have no guarantee that providers deliver them faithfully. Providers may covertly substitute cheaper alternatives (e.g., quantized versions, smaller models) to reduce costs while maintaining advertised pricing. We formalize this model substitution problem and systematically evaluate detection methods under realistic adversarial conditions. Our empirical analysis reveals that software-only methods are fundamentally unreliable: statistical tests on text outputs are query-intensive and fail against subtle substitutions, while methods using log probabilities are defeated by inherent inference nondeterminism in production environments. We argue that this verification gap can be more effectively closed with hardware-level security. We propose and evaluate the use of Trusted Execution Environments (TEEs) as one practical and robust solution. Our findings demonstrate that TEEs can provide provable cryptographic guarantees of model integrity with only a modest performance overhead, offering a clear and actionable path to ensure users get what they pay for. // 商业大型语言模型 (LLM) API 造成了一个根本性的信任问题：用户为特定模型付费，却无法保证提供商会忠实地交付这些模型。提供商可能会暗中用更便宜的替代方案（例如，量化版本、更小的模型）来降低成本，同时维持其宣传的价格。我们将这种模型替换问题形式化，并在真实的对抗条件下系统地评估了各种检测方法。我们的实证分析表明，纯软件方法从根本上来说是不可靠的：对文本输出进行统计测试需要大量的查询，并且无法检测到细微的替换；而使用对数概率的方法则会因生产环境中固有的推理不确定性而失效。我们认为，通过硬件级安全可以更有效地弥补这一验证差距。我们提出并评估了可信执行环境 (TEE) 的使用，将其作为一种实用且稳健的解决方案。我们的研究结果表明，TEE 能够以适度的性能开销提供可证明的模型完整性加密保证，从而为确保用户获得其付费内容提供了一条清晰可行的途径。"
[llm-api-audit:2504.04715.src/gh]: https://github.com/sunblaze-ucb/llm-api-audit.git "(MIT) (Languages: Python 94.8%, Jupyter Notebook 4.3%, Other 0.9%) LLM API Audit // LLM API 审计 /// This project provides different methods for auditing Large Language Models (LLMs) to verify service integrity. // 该项目提供了不同的方法来审核大型语言模型（LLM），以验证服务的完整性。"

