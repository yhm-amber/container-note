[old.package/cran]: https://cran.r-project.org/web/packages/httr/ "(Version:	1.4.7) (Depends:	R (≥ 3.5)) (Imports:	curl (≥ 5.0.2), jsonlite, mime, openssl (≥ 0.8), R6) (Suggests:	covr, httpuv, jpeg, knitr, png, readr, rmarkdown, testthat (≥ 0.8.0), xml2) (Published:	2023-08-15) (DOI:	10.32614/CRAN.package.httr) (Author:	Hadley Wickham [aut, cre], Posit, PBC [cph, fnd]) (Maintainer:	Hadley Wickham <hadley at posit.co>) (License:	MIT + file LICENSE) (NeedsCompilation:	no) httr: Tools for Working with URLs and HTTP // httr：用于处理 URL 和 HTTP 的工具 /// Useful tools for working with HTTP organised by HTTP verbs (GET(), POST(), etc). Configuration functions make it easy to control additional request components (authenticate(), add_headers() and so on). // 按 HTTP 动词（GET()、POST()等）组织的用于处理 HTTP 的实用工具。配置函数使控制附加请求组件（authenticate()、add_headers()等）变得容易。 (src: gh:r-lib/httr.git)"
[new.package/cran]: https://cran.r-project.org/web/packages/httr2/ "(Version:	1.2.2) (Depends:	R (≥ 4.1)) (Imports:	cli (≥ 3.0.0), curl (≥ 6.4.0), glue, lifecycle, magrittr, openssl, R6, rappdirs, rlang (≥ 1.1.0), vctrs (≥ 0.6.3), withr) (Suggests:	askpass, bench, clipr, covr, docopt, httpuv, jose, jsonlite, knitr, later (≥ 1.4.0), nanonext, otel (≥ 0.2.0), otelsdk (≥ 0.2.0), paws.common (≥ 0.8.0), promises, rmarkdown, testthat (≥ 3.1.8), tibble, webfakes (≥ 1.4.0), xml2) (Published:	2025-12-08) (DOI:	10.32614/CRAN.package.httr2) (Author:	Hadley Wickham [aut, cre], Posit Software, PBC [cph, fnd], Maximilian Girlich [ctb]) (Maintainer:	Hadley Wickham <hadley at posit.co>) (License:	MIT + file LICENSE) (NeedsCompilation:	no) httr2: Perform HTTP Requests and Process the Responses // httr2：执行 HTTP 请求并处理响应 /// Tools for creating and modifying HTTP requests, then performing them and processing the results. 'httr2' is a modern re-imagining of 'httr' that uses a pipe-based interface and solves more of the problems that API wrapping packages face. // 用于创建和修改 HTTP 请求，执行请求并处理结果的工具。'httr2' 是对 'httr' 的现代化重新设计，它使用基于管道的接口，并解决了 API 封装包面临的部分问题。 (src: gh:r-lib/httr2.git)"
[old.src/gh]: https://github.com/r-lib/httr.git "(MIT) (R 100.0%) httr: a friendly http package for R // httr：一个友好的 R 语言 HTTP 包 /// Status // 状态 /// httr is superseded: only changes necessary to keep it on CRAN will be made. We recommend using httr2 instead. // httr 已被取代：仅将进行必要的更改以保持其在 CRAN 上。我们建议使用 httr2。 /// The aim of httr is to provide a wrapper for the curl (cran: curl) package, customised to the demands of modern web APIs. // httr 的目标是为 curl 包提供一个包装器，以满足现代 Web API 的需求。 /// Key features: // 主要功能： /// - Functions for the most important http verbs: GET(), HEAD(), PATCH(), PUT(), DELETE() and POST(). // 最常用 http 动词的函数： GET() ， HEAD() ， PATCH() ， PUT() ， DELETE() 和 POST() 。 /// - Automatic connection sharing across requests to the same website (by default, curl handles are managed automatically), cookies are maintained across requests, and a up-to-date root-level SSL certificate store is used. // 自动在访问同一网站的不同请求之间共享连接（默认情况下，curl 句柄由 httr 自动管理），保持跨请求的 cookie，并使用最新的根级 SSL 证书存储。 /// - Requests return a standard reponse object that captures the http status line, headers and body, along with other useful information. // 请求返回一个标准响应对象，该对象捕获 http 状态行、头部和正文以及其他有用信息。 /// - - Response content is available with content() as a raw vector (as = 'raw'), a character vector (as = 'text'), or parsed into an R object (as = 'parsed'), currently for html, xml, json, png and jpeg. // 响应内容可以通过 content() 以原始向量（ as = 'raw' ）、字符向量（ as = 'text' ）或解析为 R 对象（ as = 'parsed' ）的形式获取，目前支持 html、xml、json、png 和 jpeg 格式。 /// - - You can convert http errors into R errors with stop_for_status(). // 你可以使用 stop_for_status() 将 http 错误转换为 R 错误。 /// - Config functions make it easier to modify the request in common ways: set_cookies(), add_headers(), authenticate(), use_proxy(), verbose(), timeout(), content_type(), accept(), progress(). // 配置函数使得以常见方式修改请求更加容易： set_cookies() ， add_headers() ， authenticate() ， use_proxy() ， verbose() ， timeout() ， content_type() ， accept() ， progress() 。 /// - Support for OAuth 1.0 and 2.0 with oauth1.0_token() and oauth2.0_token(). The demo directory has eight OAuth demos: four for 1.0 (twitter, vimeo, withings and yahoo) and four for 2.0 (facebook, github, google, linkedin). OAuth credentials are automatically cached within a project. // 支持通过 oauth1.0_token() 和 oauth2.0_token() 使用 OAuth 1.0 和 2.0。演示目录中有八个 OAuth 演示：四个用于 1.0（twitter、vimeo、withings 和 yahoo），四个用于 2.0（facebook、github、google、linkedin）。OAuth 凭证会自动缓存在一个项目中。 /// httr wouldn't be possible without the hard work of the authors of curl (cran: curl) and libcurl (web: curl.se). Thanks! httr is inspired by http libraries in other languages, such as Resty (gh: beders/Resty.git ((Java) Tiny REST client for Java and JSON)), Requests (gh: psf/requests.git ((Python) A simple, yet elegant, HTTP library)) and httparty (gh: jnunemaker/httparty.git ((Ruby) 🎉 Makes http fun again!)). // 如果没有 curl 和 libcurl 作者的辛勤工作，httr 将不可能实现。谢谢！httr 受到其他语言中的 http 库的启发，例如 Resty、Requests 和 httparty。"
[new.src/gh]: https://github.com/r-lib/httr2.git "(MIT) (R 100.0%) Make HTTP requests and process their responses. A modern reimagining of httr. // 发起 HTTP 请求并处理它们的响应。httr 的现代化重新构想。 /// httr2 (pronounced “hitter2”) is a comprehensive HTTP client that provides a modern, pipeable API for working with web APIs. It builds on top of {curl} (runiverse-web: jeroen.r-universe.dev/curl, cran: curl) to provide features like explicit request objects, built-in rate limiting & retry tooling, comprehensive OAuth support, and secure handling of secrets and credentials. // httr2（发音为“hitter2”）是一个全面的 HTTP 客户端，提供了一个现代的、可管道化的 API，用于处理 Web API。它基于包 {curl} 构建，提供显式请求对象、内置速率限制和重试工具、全面的 OAuth 支持以及安全的密钥和凭证处理。 /// Major differences to httr // 与 httr 的主要区别 /// - You can now create and modify a request without performing it. This means that there’s now a single function to perform the request and fetch the result: req_perform(). req_perform() replaces httr::GET(), httr::POST(), httr::DELETE(), and more. // 现在您可以在不执行请求的情况下创建和修改请求。这意味着现在有一个单一的函数来执行请求并获取结果： req_perform() 。 req_perform() 替换了 httr::GET() 、 httr::POST() 、 httr::DELETE() 以及更多。 /// - HTTP errors are automatically converted into R errors. Use req_error() to override the defaults (which turn all 4xx and 5xx responses into errors) or to add additional details to the error message. // HTTP 错误会自动转换为 R 错误。使用 req_error() 可以覆盖默认设置（默认将所有 4xx 和 5xx 响应转换为错误）或向错误消息添加更多详细信息。 /// - You can automatically retry if the request fails or encounters a transient HTTP error (e.g. a 429 rate limit request). req_retry() defines the maximum number of retries, which errors are transient, and how long to wait between tries. // 如果请求失败或遇到临时 HTTP 错误（例如 429 速率限制请求），您可以自动重试。 req_retry() 定义了最大重试次数、哪些错误是临时的以及重试之间的等待时间。 /// - OAuth support has been totally overhauled to directly support many more flows and to make it much easier to both customise the built-in flows and to create your own. // OAuth 支持已完全改版，以直接支持更多流程，并使自定义内置流程和创建自己的流程变得更加容易。 /// - You can manage secrets (often needed for testing) with secret_encrypt() and friends. You can obfuscate mildly confidential data with obfuscate(), preventing it from being scraped from published code. // 你可以使用 secret_encrypt() 及其相关功能来管理密钥（通常用于测试）。你可以使用 obfuscate() 来混淆轻微敏感的数据，防止其从已发布的代码中被抓取。 /// - You can automatically cache all cacheable results with req_cache(). Relatively few API responses are cacheable, but when they are it typically makes a big difference. // 你可以使用 req_cache() 自动缓存所有可缓存的响应结果。相对较少的 API 响应是可缓存的，但当他们可缓存时，通常会产生很大的影响。"
[old.site/org]: https://httr.r-lib.org/ "(@Manual{, title = {httr: Tools for Working with URLs and HTTP}, author = {Hadley Wickham}, year = {2023}, note = {https://httr.r-lib.org/, https://github.com/r-lib/httr} })"
[new.site/org]: https://httr2.r-lib.org/ "(@Manual{, title = {httr2: Perform HTTP Requests and Process the Responses}, author = {Hadley Wickham}, year = {2025}, note = {R package version 1.2.2}, url = {https://httr2.r-lib.org} })"
