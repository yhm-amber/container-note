[src/gh]: https://github.com/cloudflare/quiche.git "(BSD-2-Clause) (Languages: Rust 96.3%, C 2.5%, JavaScript 0.7%, HTML 0.2%, Shell 0.1%, Makefile 0.1%, Other 0.1%) 🥧 Savoury implementation of the QUIC transport protocol and HTTP/3 // 🥧 QUIC 传输协议和 HTTP/3 的精彩实现 /// quiche is an implementation of the QUIC transport protocol and HTTP/3 as specified by the IETF. It provides a low level API for processing QUIC packets and handling connection state. The application is responsible for providing I/O (e.g. sockets handling) as well as an event loop with support for timers. // Quiche 是 IETF 规范中 QUIC 传输协议和 HTTP/3 的实现。它提供了一个用于处理 QUIC 数据包和连接状态的底层 API。该应用程序负责提供 I/O 功能（例如套接字处理）以及支持定时器的事件循环。 /// For more information on how quiche came about and some insights into its design you can read a post (. web: blog.cloudflare.com/enjoy-a-slice-of-quic-and-rust) on Cloudflare's blog that goes into some more detail. // 想了解更多关于乳蛋饼的由来和设计方面的信息，您可以阅读 Cloudflare 博客上的一篇文章，其中有更详细的介绍。 /// Who uses quiche? // 谁会吃乳蛋饼？ /// - Cloudflare ///: quiche powers Cloudflare edge network's HTTP/3 support (. web: blog.cloudflare.com/http3-the-past-present-and-future). The web:cloudflare-quic.com website can be used for testing and experimentation. // quiche 为 Cloudflare 边缘网络的 HTTP/3 支持提供技术支持。 cloudflare-quic.com 网站可用于测试和实验。 /// - Android ///: Android's DNS resolver uses quiche to implement DNS over HTTP/3 (. web: security.googleblog.com/2022/07/dns-over-http3-in-android.html). // Android 的 DNS 解析器使用 quiche 来实现基于 HTTP/3 的 DNS 。 /// - curl ///: quiche can be integrated into curl (. gh: curl/curl.git @master/docs/HTTP3.md#quiche-version) to provide support for HTTP/3. // quiche 可以集成到 curl 中 ，以提供对 HTTP/3 的支持。"
[api.rs/tech]: https://docs.quic.tech/quiche/ "Crate quiche // 板条箱乳蛋饼"
[lib.cargo/crates]： https://crates.io/crates/quiche "(: cargo add -- quiche) (BSD-2-Clause) (303K SLoC) (3.63 MiB) 🥧 Savoury implementation of the QUIC transport protocol and HTTP/3 // 🥧 QUIC 传输协议和 HTTP/3 的精彩实现 (src: gh:cloudflare/quiche.git)"
[http3-intro.blog/cf]: https://blog.cloudflare.com/http3-the-past-present-and-future/ "HTTP/3: the past, the present, and the future // HTTP/3：过去、现在和未来 /// We are now happy to announce that QUIC and HTTP/3 support is available on the Cloudflare edge network. We’re excited to be joined in this announcement by Google Chrome and Mozilla Firefox, two of the leading browser vendors and partners in our effort to make the web faster and more reliable for all. // 我们很高兴地宣布，Cloudflare 边缘网络现已支持 QUIC 和 HTTP/3。同时， 我们也荣幸地与 Google Chrome 和 Mozilla Firefox 这两家领先的浏览器厂商和合作伙伴共同宣布这一消息，他们致力于让网络更快、更可靠，造福所有人。 /// In the words of Ryan Hamilton, Staff Software Engineer at Google, “HTTP/3 should make the web better for everyone. The Chrome and Cloudflare teams have worked together closely to bring HTTP/3 and QUIC from nascent standards to widely adopted technologies for improving the web. Strong partnership between industry leaders is what makes Internet standards innovations possible, and we look forward to our continued work together.” // 正如谷歌高级软件工程师 Ryan Hamilton 所说：“HTTP/3 应该能让所有人的网络体验更好。Chrome 和 Cloudflare 团队紧密合作，将 HTTP/3 和 QUIC 从新兴标准发展成为广泛采用的网络改进技术。行业领导者之间的强强联合是互联网标准创新得以实现的关键，我们期待继续携手合作。” /// What does this mean for you, a Cloudflare customer who uses our services and edge network to make your web presence faster and more secure? Once HTTP/3 support is enabled for your domain in the Cloudflare dashboard, your customers can interact with your websites and APIs using HTTP/3. We’ve been steadily inviting customers on our HTTP/3 waiting list to turn on the feature (so keep an eye out for an email from us), and in the coming weeks we’ll make the feature available to everyone. // 对于使用 Cloudflare 服务和边缘网络来提升网站速度和安全性的 Cloudflare 客户而言，这意味着什么？一旦您在 Cloudflare 控制面板中为您的域名启用 HTTP/3 支持，您的客户即可使用 HTTP/3 与您的网站和 API 进行交互。我们一直在逐步邀请 HTTP/3 等候名单上的客户启用此功能（请留意我们发送的电子邮件），未来几周内，我们将向所有客户开放此功能。 /// What does this announcement mean if you’re a user of the Internet interacting with sites and APIs through a browser and other clients? Starting today, you can use Chrome Canary to interact with Cloudflare and other servers over HTTP/3. For those of you looking for a command line client, curl also provides support for HTTP/3. Instructions for using Chrome and curl with HTTP/3 follow later in this post. // 如果您是经常使用浏览器或其他客户端访问网站和 API 的互联网用户，那么这项公告意味着什么？从今天起，您可以使用 Chrome Canary 通过 HTTP/3 与 Cloudflare 和其他服务器进行交互。如果您正在寻找命令行客户端， curl 也支持 HTTP/3 。本文稍后将介绍如何使用 Chrome 和 curl 进行 HTTP/3 通信。"
[quiche-intro.blog/cf]: https://blog.cloudflare.com/enjoy-a-slice-of-quic-and-rust/ "Enjoy a slice of QUIC, and Rust! // 尽情享用 QUIC 和 Rust 吧！ /// The main design principle that guided quiche’s initial development was exposing most of the QUIC complexity to applications through a minimal and intuitive API, but without making too many assumptions about the application itself, in order to allow us to reuse the same library in different contexts. // quiche 在其最早的主要开发设计原则即是通过一个最小且直观的 API 将 QUIC 的大部分复杂性暴露给应用程序，但又不做出太多关于应用程序本身的假设，以便我们可以在不同的上下文中重用同一个库。"

