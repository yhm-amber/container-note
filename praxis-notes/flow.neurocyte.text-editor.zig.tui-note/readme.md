[src/gh]: https://github.com/neurocyte/flow.git "(MIT) (Languages: Zig 99.5%, Other 0.5%) Flow Control: a programmer's text editor // 流程控制：程序员的文本编辑器 /// Flow Control: a programmer's text editor // Flow Control：一个程序员的文本编辑器 /// This is my Zig text editor. It is under active development, but very stable and is my daily driver for almost everything. // 这是我开发的 Zig 文本编辑器。它正处于积极开发中，但非常稳定，并且是我日常几乎所有工作的主要驱动工具。 /// ## Features // 功能 /// - Lightning Fast TUI with ≤6ms frame times, low latency input handling and smooth animated scrolling // 闪电般的 TUI，帧时间 ≤6ms，低延迟输入处理和流畅的动画滚动 /// - Intuitive UI with tabs, scrollbars and palettes with full mouse support for all UI elements // 直观的 UI，带有标签页、滚动条和调色板，所有 UI 元素都支持完整的鼠标操作 /// - Support for more than 70 programming languages, zero configuration needed, via tree-sitter powered syntax highlighting // 支持超过 70 种编程语言，无需配置，通过 tree-sitter 驱动的语法高亮 /// - Language Server Protocol pre configured support for most language servers // 预先配置的语言服务器协议支持大多数语言服务器 /// - Powerful multi-cursor editing and integrated clipboard history // 强大的多光标编辑和集成剪贴板历史记录 /// - Powerful configurable keybinding system that supports modal and non-modal editing styles // 强大的可配置快捷键系统，支持模态和非模态编辑风格 /// - Multiple pre-configured keybinding modes // 多种预配置的快捷键模式 /// - - Flow Control - GUI IDE style bindings (similar to vscode) // Flow Control - GUI IDE 风格的绑定（类似于 vscode） /// - - Emacs /// - - Vim /// - - Helix /// - - User created // 用户创建 /// - Hybrid rope/piece-table buffer system, edit very large files with thousands of cursors // 混合绳/片段表缓冲系统，使用数千个光标编辑非常大的文件 /// - Infinite undo (at least until you run out of ram) // 无限撤销（至少直到内存用完） /// - Full unicode support, including support for the kitty text sizing protocol // 完整的 Unicode 支持，包括对 kitty 文本尺寸协议的支持 /// - Plenty of themes included and support for vscode themes via the flow-themes project // 包含大量主题，并通过 flow-themes 项目支持 vscode 主题 /// - Runs on Linux, FreeBSD, MacOS, Windows and Android (under termux) with easy cross-compilation to all supported targets // 可在 Linux、FreeBSD、MacOS、Windows 和 Android（在 termux 下）上运行，并易于交叉编译到所有支持的目标平台 /// ## Requirements // 需要 /// - A modern terminal with 24bit color and, ideally, kitty keyboard protocol support. Kitty, Foot and Ghostty are the recommended terminals at this time. Zellij also works well. Most other terminals will work, but likely with reduced functionality. // 一个支持 24 位色彩，并且理想情况下支持 kitty 键盘协议的现代终端。目前推荐使用 kitty、Foot 和 Ghostty 终端。Zellij 也能很好地工作。大多数其他终端也能工作，但功能可能会减少。 /// - NerdFont support. Either via terminal font fallback or a patched font. // 支持 NerdFont。可以通过终端字体回退或补丁字体实现。 /// - A UTF-8 locale // UTF-8 区域配置"
[cli.x/x-cmd]: https://x-cmd.com/install/flow-control "(: x install -- flow-control)"
[site/dev]: https://flow-control.dev/ "(一个主要为像素风格字体的干净网站) Flow Control // 流程控制 /// a programmer’s text editor // 程序员的文本编辑器"
