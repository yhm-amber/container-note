[src/gh]: https://github.com/barddoo/zedis.git "(MIT) (Languages: Zig 100.0%) Redis in Zig /// A Redis-compatible in-memory data store written in Zig, designed for learning and experimentation. Zedis implements the core Redis protocol and data structures with a focus on simplicity, performance, and thread safety. // 一款用 Zig 语言编写的、兼容 Redis 的内存数据存储，专为学习和实验设计。Zedis 实现了 Redis 的核心协议和数据结构，注重简洁性、性能和线程安全。 /// Made for learning purposes. Not intended for production use. // 用于学习目的。不适用于生产环境。 /// Features // 特性 /// - Redis Protocol Compatibility: Supports the Redis Serialization Protocol (RESP)locks // Redis 协议兼容性：支持 Redis 序列化协议（RESP） /// - Multiple Data Types: String and integer value storage with automatic type conversion // 多种数据类型：支持字符串和整数值存储，并具有自动类型转换 /// - Core Commands: Essential Redis commands including GET, SET, INCR, DECR, DEL, EXISTS, and TYPE // 核心命令：包含 GET、SET、INCR、DECR、DEL、EXISTS 和 TYPE 等基本 Redis 命令 /// - High Performance: Written in Zig for optimal performance and memory safety // 高性能：使用 Zig 语言编写，以实现最佳性能和内存安全 /// - Connection Management: Handles multiple concurrent client connections // 连接管理：处理多个并发客户端连接 /// - Disk persistence (RDB): Point-in-time snapshots of your dataset. // 持久化磁盘 (RDB)：数据集的实时快照。 /// - Memory Management: No memory allocation during command execution. // 内存管理：命令执行期间不进行内存分配。 /// - Pub/Sub: Decoupled communication between services. (latest feature) 🎉 // 发布/订阅：服务之间的解耦通信。（最新功能）🎉"
