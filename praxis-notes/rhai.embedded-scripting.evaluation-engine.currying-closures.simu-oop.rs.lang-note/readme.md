[src/gh]: https://github.com/rhaiscript/rhai.git "(Apache-2.0, MIT) (Languages: Rust 92.0%, JavaScript 8.0%) Rhai - An embedded scripting language for Rust. // Rhai——一种用于 Rust 的嵌入式脚本语言。 /// Rhai - Embedded Scripting for Rust // Rhai - Rust 嵌入式脚本 /// Rhai is an embedded scripting language and evaluation engine for Rust that gives a safe and easy way to add scripting to any application. // Rhai 是一个用于 Rust 的嵌入式脚本语言和评估引擎，它提供了一种安全简便的方法，可以将脚本添加到任何应用程序中。 /// Targets and builds // 目标和构建 /// - All CPU and O/S targets supported by Rust, including: // Rust 支持的所有 CPU 和操作系统目标，包括： /// - - WebAssembly (WASM) /// - - no-std /// - Minimum Rust version 1.66.0 // 最低 Rust 版本 1.66.0 /// Standard features // 标准特性 /// - Simple language similar to JavaScript+Rust with dynamic typing. // 简单易用的语言，类似于 JavaScript+Rust，具有动态类型。 /// - Fairly efficient evaluation - 1 million iterations in 0.14 sec on a single-core 2.6 GHz Linux VM running this script. // 评估效率相当高——在运行此脚本的单核 2.6 GHz Linux VM 上，0.14 秒内完成了 100 万次迭代。 /// - Tight integration with native Rust functions and types, including getters/setters, methods and indexers. // 与 Rust 原生函数和类型紧密集成，包括 getter/setter 、 方法和索引器 。 /// - Freely pass Rust values into a script as variables/constants via an external Scope - all clonable Rust types are supported; no need to implement any special trait. Or tap directly into the variable resolution process. // 通过外部 Scope ，可以将 Rust 值作为变量 / 常量自由地传递到脚本中——支持所有可克隆的 Rust 类型；无需实现任何特殊特性。或者直接参与变量解析过程 。 /// - Built-in support for most common data types including booleans, integers, floating-point numbers (including Decimal), strings, Unicode characters, arrays (including packed byte arrays) and object maps. // 内置支持大多数常见数据类型， 包括布尔值、 整数 、 浮点数 （包括 Decimal ）、 字符串 、 Unicode 字符 、 数组 （包括压缩字节数组 ）和对象映射 。 /// - Easily call a script-defined function from Rust. // 轻松地从 Rust 调用脚本定义的函数 。 /// - Relatively little unsafe code (yes there are some for performance reasons). // unsafe 代码相对较少（是的，出于性能原因，有一些不安全代码）。 /// - Few dependencies - currently only smallvec, thin-vec, num-traits, once_cell, ahash, bitflags and smartstring. // 依赖项很少 - 目前只有 smallvec 、 thin-vec 、 num-traits 、 once_cell 、 ahash 、 bitflags 和 smartstring 。 /// - Re-entrant scripting engine can be made Send + Sync (via the sync feature). // 可重入脚本引擎可以实现 Send + Sync （通过 sync 功能）。 /// - Compile once to AST form for repeated evaluations. // 编译一次为 AST 格式，以便重复评估。 /// - Scripts are optimized (useful for template-based machine-generated scripts). // 脚本已优化 （适用于基于模板的机器生成脚本）。 /// - Easy custom API development via plugins system powered by procedural macros. // 通过由过程宏驱动的插件系统，轻松实现自定义 API 开发。 /// - Function overloading and operator overloading. // 函数重载和运算符重载 。 /// - Dynamic dispatch via function pointers with additional support for currying. // 通过函数指针进行动态分发，并额外支持柯里化 。 /// - Closures (anonymous functions) that can capture shared values. // 闭包 （匿名函数）可以捕获共享值。 /// - Some syntactic support for object-oriented programming (OOP). // 对面向对象编程（OOP） 的一些语法支持。 /// - Organize code base with dynamically-loadable modules, optionally overriding the resolution process. // 使用动态加载模块组织代码库，并可选择覆盖解析过程 。 /// - Serialization/deserialization support via serde (requires the serde feature). // 通过 serde 支持序列化/反序列化（需要 serde 功能）。 /// - Support for minimal builds by excluding unneeded language features. // 支持通过排除不需要的语言特性来构建最小版本 。 /// - A debugging interface. // 调试界面。 /// Protected against attacks // 免受攻击 /// - Don't Panic guarantee - Any panic is a bug. Rhai subscribes to the motto that a library should never panic the host system, and is coded with this in mind. // 不要恐慌保证——任何恐慌都是程序错误。Rhai 秉持着库绝不应导致宿主系统恐慌的原则，并在编写代码时始终牢记这一点。 /// - Sand-boxed - the scripting engine, if declared immutable, cannot mutate the containing environment unless explicitly permitted. // 沙盒化 ——如果脚本引擎被声明为不可变，则除非明确允许 ，否则不能改变包含它的环境。 /// - Rugged - protected against malicious attacks (such as stack-overflow, over-sized data, and runaway scripts etc.) that may come from untrusted third-party user-land scripts. // 坚固耐用 - 可抵御来自不受信任的第三方用户空间脚本的恶意攻击（例如堆栈溢出 、 数据过大 、 失控脚本等）。 /// - Track script evaluation progress and manually terminate a script run. // 跟踪脚本评估进度并手动终止脚本运行。 /// - Passes Miri. // Miri 通行。 /// For those who actually want their own language // 对于那些真正想要拥有自己语言的人来说 /// - Use as a DSL. // 用作 DSL 。 /// - Disable certain language features such as looping. // 禁用某些语言功能 ，例如循环 。 /// - Further restrict the language by surgically disabling keywords and operators. // 通过精确禁用关键字和运算符来进一步限制语言。 /// - Define custom operators. // 定义自定义运算符 。 /// - Extend the language with custom syntax. // 使用自定义语法扩展语言。 /// Rhai does not have objects per se and is not object-oriented (in the traditional sense), but it is possible to simulate object-oriented programming. // Rhai 本身没有对象 ，也不是面向对象的（传统意义上的），但可以模拟面向对象的编程。 /// o OOP or not to OOP, that is the question. // 使用面向对象编程还是不使用面向对象编程，这是一个问题。 /// Regardless of whether object-oriented programming (OOP) should be treated as a pattern or an anti-pattern (the programming world is split 50-50 on this), there are always users who would like to write Rhai in “the OOP way.” // 无论面向对象编程 (OOP) 应该被视为一种模式还是一种反模式 （编程界对此意见不一），总有一些用户希望以“OOP 的方式”编写 Rhai。 /// Rust itself is not object-oriented in the traditional sense; JavaScript also isn’t, but that didn’t prevent generations of programmers trying to shoehorn a class-based inheritance system onto it. // Rust 本身并不是传统意义上的面向对象语言；JavaScript 也不是，但这并没有阻止一代又一代的程序员尝试将基于类的继承系统硬塞到它上面。 /// So… as soon as Rhai gained in usage, way way before version 1.0, PR’s started coming in to make it possible to write Rhai in “the OOP way.” // 所以……Rhai 的使用率一上升，早在 1.0 版本之前，就出现了 PR，使得以“面向对象编程方式”编写 Rhai 成为可能。"
[cli.cargo/crates]: https://crates.io/crates/rhai "(: cargo install -- rhai) (MIT OR Apache-2.0) (37K SLoC) (328 KiB) Embedded scripting for Rust // Rust 的嵌入式脚本 (src: gh:rhaiscript/rhai.git)"
[site/rs]: https://rhai.rs/ "Embedded scripting language and engine for Rust // Rust 的嵌入式脚本语言和引擎 /// A small, fast, easy-to-use scripting language and evaluation engine that integrates tightly with Rust. Builds for most common targets including no-std and WASM. // 一款小巧、快速、易用的脚本语言和评估引擎，与 Rust 紧密集成。可构建用于包括 no-std 和 WASM 在内的大多数常见目标平台。"
[lsp.src/gh]: https://github.com/rhaiscript/lsp.git "(Apache-2.0) (Languages: Rust 96.0%, TypeScript 3.6%, JavaScript 0.4%) Language server for Rhai. // Rhai 语言服务器。"
[iex-nifbind.lib.src/gh]: https://github.com/rhaiscript/rhai_rustler.git "(Apache-2.0) (Languages: Elixir 70.3%, Rust 29.7%) Elixir NIF bindings for Rhai, an embedded scripting language and engine for Rust // Rhai 的 Elixir NIF 绑定，Rhai 是一种用于 Rust 的嵌入式脚本语言和引擎。 /// rhai_rustler exposes a subset of the Rhai API to Elixir: // rhai_rustler 向 Elixir 公开 Rhai API 的子集： /// - Engine /// - Scope /// - AST /// Eval: (: engine = Rhai.Engine.new() # Create engine instance) (: {:ok, 2} = engine |> Rhai.Engine.eval(\"1 + 1\") # Simple evaluation) (: scope = Rhai.Scope.new() |> Rhai.Scope.push(\"a\", 10) |> Rhai.Scope.push(\"b\", 3) ;: {:ok, 30} = engine |> Rhai.Engine.eval_with_scope(scope, \"a * b\") # Evaluation with scope) /// AST: (: engine = Rhai.Engine.new() # engine instance) (: scope = Rhai.Scope.new() |> Rhai.Scope.push_constant(\"a\", 10) |> Rhai.Scope.push_constant(\"b\", 3) ;: {:ok, %Rhai.AST{} = ast} = engine |> Rhai.Engine.compile_with_scope(scope, \"a * b\") ;: {:ok, 30} = engine |> Rhai.Engine.eval_ast(ast) # get & eval an ast) (: task = Task.async(fn -> Rhai.Engine.new() |> Rhai.Engine.eval_ast(ast) end) ;: {:ok, 30} = Task.await(task) # AST can be shared between engines)"
[iex-nifbind.lib.mix/hex]: https://hex.pm/packages/rhai_rustler "(: Mix.install([{:rhai_rustler}])) (License: Apache-2.0) Rhai rustler bindings (src: gh:rhaiscript/rhai_rustler.git)"
[play.wasm/.site]: https://rhai.rs/playground/ "Online Playground"
[play.src/gh]: https://github.com/rhaiscript/playground.git "(MIT) (Languages: Vue 46.9%, Rust 34.8%, JavaScript 16.8%, HTML 1.5%) A Rhai scripting playground that runs Rhai scripts using WebAssembly in a web browser. // 一个 Rhai 脚本开发平台，可以在 Web 浏览器中使用 WebAssembly 运行 Rhai 脚本。"
[book.src/gh]: https://github.com/rhaiscript/book.git "(Languages: CSS 65.3%, SCSS 33.5%, Handlebars 1.2%) The Rhai Book /// Book generated using mdBook"
[book.mdbook/.site]: https://rhai.rs/book/ "The Rhai Book"
[ml-egne.lib.src/gh]: https://github.com/rhaiscript/rhai-ml.git "(Apache-2.0, MIT) (Languages: Rust 100.0%) Machine learning for Rhai. (~ gh:cmccomb/rhai-ml.git) /// Using this crate is pretty simple! If you just want to evaluate a single line of Rhai, then you only need: // 使用这个 crate 非常简单！如果您只想评估 Rhai 的一行代码，那么您只需要： ///: (: use rhai::FLOAT ;: use rhai_ml::eval ;: ... // prepare) (: let result = eval::<FLOAT>(\"let xdata = [[1.0, 2.0], [2.0, 3.0], [3.0, 4.0]]; let ydata = [1.0, 2.0, 3.0]; let model = train(xdata, ydata, \"linear\"); let ypred = predict(xdata, model); ypred[0]\").unwrap() ;: ... // eval rhai with train/predict fn calling) /// If you need to use rhai-ml as part of a persistent Rhai scripting engine, then do this instead: // 如果您需要将 rhai-ml 用作持久化 Rhai 脚本引擎的一部分，请改为这样做： ///: (: use rhai::{Engine, packages::Package, FLOAT} ;: use rhai_ml::MLPackage ;: let mut engine = Engine::new() /* Create a new Rhai engine */ ;: engine.register_global_module(MLPackage::new().as_shared_module()) /* Add the rhai-ml package to the new engine */ ;: ...) (: let value = engine.eval::<FLOAT>(\"let xdata = [[1.0, 2.0], [2.0, 3.0], [3.0, 4.0]]; let ydata = [1.0, 2.0, 3.0]; let model = train(xdata, ydata, \"linear\"); let ypred = predict(xdata, model); ypred[0]\").unwrap() ;: ... // using `engine.eval` to run)"
[ml-egne.lib.cargo/crates.io]: https://crates.io/crates/rhai-ml "(: cargo add -- rhai-ml) (MIT OR Apache-2.0) (385 SLoC) (9.73 KiB) Machine learning in the Rhai scripting language // Rhai 脚本语言中的机器学习 (src: gh:rhaiscript/rhai-ml.git)"
[pkgtool-docgen.cli.src/gh]: https://github.com/rhaiscript/rhai-doc.git "(Apache-2.0, MIT) (Languages: Rust 73.8%, CSS 16.5%, Handlebars 9.7%) Tool to auto-generate documentation for Rhai source code. // 用于自动生成 Rhai 源代码文档的工具。 /// rhai-doc - Generates HTML Documentation from Rhai Script Files // rhai-doc - 从 Rhai 脚本文件生成 HTML 文档 /// rhai-doc is a tool for auto-generating documentation for Rhai scripts. // rhai-doc 是一个用于自动生成 Rhai 脚本文档的工具。 /// It supports writing MarkDown documentation in doc-comments of Rhai scripts and creating general-purpose documentation pages. // 它支持在 Rhai 脚本的 doc 注释中编写 Markdown 文档，并创建通用文档页面。 /// - Doc-Comments // 文档注释 ///: Rhai supports doc-comments in MarkDown format on script-defined functions. // Rhai 支持在脚本定义函数中使用 Markdown 格式的文档注释。 (位置类似 R/Java/Rust 那样) /// - Configuration File // 配置文件 ///: To generate a skeleton rhai.toml, use the new command // 要生成 rhai.toml 框架文件，请使用 new 命令 ///: (: rhai-doc new)"
[pkgtool-docgen.cli.cargo/crates]: https://crates.io/crates/rhai-doc "(: cargo install -- rhai-doc) (MIT OR Apache-2.0) (1,160 SLoC) (42.5 KiB) Documentation tool for Rhai - an embedded scripting language and engine for Rust // Rhai 的文档工具——Rhai 是一种基于 Rust 的嵌入式脚本语言和引擎。 (src: gh:rhaiscript/rhai-doc.git)"
[pkgtool-nativeimport.lib.src/gh]: https://github.com/rhaiscript/rhai-dylib.git "(Apache-2.0, MIT) (Languages: Rust 100.0%) A plugin system for the Rhai embedded scripting language. // Rhai 嵌入式脚本语言的插件系统 /// This crate exposes a simple API to load dylib Rust crates in a Rhai engine using Rhai modules. You can generate your own library project for Rhai using cargo-generate with this template. // 这个 crate 提供了一个简单的 API，可以使用 Rhai 模块在 Rhai 引擎中加载 dylib Rust crate。你可以使用 cargo-generate 和 this template 为 Rhai 生成你自己的库项目。 /// - Loader // 装载机 ///: Loader is a trait that is used to build objects that load rhai modules from dynamic libraries in memory. A libloading implementation is available, which enables you to load modules via a cdylib or dylib rust crate. // Loader 是一个 trait，用于构建从内存中的动态库加载 rhai 模块的对象。它提供了一个 libloading 实现，允许您通过 cdylib 或 dylib rust crate 加载模块。 /// - Module Resolver // 模块解析器 ///: This crate also expose a Rhai Module Resolver that loads dynamic libraries at the given path. // 该 crate 还公开了一个 Rhai 模块解析器 ，用于加载给定路径的动态库。 (即 engine.set_module_resolver(DylibModuleResolver::new()) 后可以在脚本里 import \"/usr/lib/abc\" as abc 来加载 native 语言编译好的系统动态库、然后 abc::xxx 这样用)"
[pkgtool-nativeimport.lib.cargo/crates]: https://crates.io/crates/rhai-dylib "(: cargo add -- rhai-dylib) (MIT OR Apache-2.0) (282 SLoC) (13.5 KiB) Dylib support for Rhai // Rhai 的 Dylib 支持 (src: gh:rhaiscript/rhai-dylib.git)"
[pkgtool-nativeimport.lib.intro/.book]: https://rhai.rs/book/rust/modules/resolvers/dylib.html "- Requires external crate rhai-dylib // 需要外部 crate rhai-dylib ///: DylibModuleResolver resides in the rhai-dylib crate which must be specified as a dependency // DylibModuleResolver 位于 rhai-dylib crate 中，必须将其指定为依赖项 /// - Linux or Windows only // 仅限 Linux 或 Windows ///: rhai-dylib currently supports only Linux and Windows. // rhai-dylib 目前仅支持 Linux 和 Windows 系统。 /// Parallel to how the FileModuleResolver works, DylibModuleResolver loads external native Rust modules from compiled dynamic shared libraries (e.g. .so in Linux and .dll in Windows). // 与 FileModuleResolver 工作方式类似， DylibModuleResolver 从编译的动态共享库 （例如 Linux 中的 .so 和 Windows 中的 .dll ）加载外部原生 Rust 模块 。 /// Therefore, FileModuleResolver loads Rhai script files while DylibModuleResolver loads native Rust shared libraries. It is very common to have the two work together. // 因此， FileModuleResolver 加载 Rhai 脚本文件，而 DylibModuleResolver 加载 Rust 原生共享库。两者协同工作非常常见。 /// (: let mut engine = Engine::new() ;: let mut resolvers = ModuleResolversCollection::new() /* Use a module resolvers collection */ ;: resolvers += FileModuleResolver::new() /* First search for script files in the file system */ ;: resolvers += DylibModuleResolver::new() /* Then search for shared-library plugins in the file system */ ;: ...) (: engine.set_module_resolver(resolvers) /* If there is 'path/to/abc.rhai', load it. Otherwise, check for 'path/to/abc.so' on Linux ('path/to/abc.dll' on Windows). */ ;: import \"path/to/abc\" as aa)"
[editorplg-vsc.src/gh]: https://github.com/rhaiscript/vscode-rhai.git "(MPL-2.0) (Languages: TypeScript 100.0%) (~ generated from gh:oovm/RustTemplate.git) Rhai language support for Visual Studio Code. // 添加 Visual Studio Code 对 Rhai 语言的支持。 /// Features // 功能 /// - Syntax highlighting // 语法高亮 /// Todo List // 待办 /// - Auto formatter // 自动格式化程序 /// - Language server // 语言服务器"
[editorplg-vsc.vsix/ovsx]: https://open-vsx.org/extension/rhaiscript/vscode-rhai "Syntax highlighting for Rhai scripts (src: gh:rhaiscript/vscode-rhai.git)"
[editorplg-vsc.vsix/msvs]: https://marketplace.visualstudio.com/items?itemName=rhaiscript.vscode-rhai "Syntax highlighting for Rhai scripts (src: gh:rhaiscript/vscode-rhai.git)"
[discord]: https://discord.gg/HquqbYFcZ9
[reddit]: https://reddit.com/r/Rhai
