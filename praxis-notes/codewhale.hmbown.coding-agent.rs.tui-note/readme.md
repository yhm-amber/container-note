[src/gh]: https://github.com/Hmbown/CodeWhale.git "(~> gh: Hmbown/DeepSeek-TUI.git) (MIT) (Languages: Rust 93.9%, TypeScript 2.7%, JavaScript 1.4%, Python 0.8%, Shell 0.6%, CSS 0.3%, Other 0.3%) Open-source, community-driven agent harness // 开源、社区驱动的代理框架 /// Codewhale // 代码鲸鱼 /// An open source coding agent for your terminal — bring your own model. // 适用于终端的开源编码代理——自带模型。 /// Codewhale started as a native experience for DeepSeek. It has since grown into a community-driven project: one coding harness that fits a growing international community and supports as many models and providers as possible — open models first, hosted or local, none privileged over the rest. // Codewhale 最初是 DeepSeek 的一个原生应用。如今，它已发展成为一个社区驱动的项目：一个适用于不断壮大的国际社区的编码框架，支持尽可能多的模式和服务提供商——优先考虑开放模式，无论是托管模式还是本地模式，所有模式一视同仁。 /// Give it a provider, a model, and a task. It reads your code, edits files, runs commands, and checks its own work, then stops when the job is done or it needs you. Switch models mid-task with /model. Work interactively in the TUI, or run codewhale exec in scripts and CI. It's written in Rust, licensed MIT, and runs on your machine. // 给它提供一个提供者、一个模型和一个任务。它会读取你的代码、编辑文件、运行命令并检查自身的工作，然后在任务完成或需要你干预时停止。在任务进行过程中，可以使用 /model 命令切换模型。你可以在交互式用户界面 (TUI) 中工作，或者运行 codewhale exec 可用于脚本和 CI。它使用 Rust 编写，采用 MIT 许可证，并在您的机器上运行。 /// ## What it does // 它的作用 /// - Any model, any provider. DeepSeek, Claude, GPT, Kimi, GLM, and 30+ providers, plus your own vLLM, SGLang, or Ollama with no key — all through one runtime and one toolset. Context limits and prices come from the real route, and an unknown price shows as unknown rather than $0. // 任何模型，任何提供商。DeepSeek 、Claude、GPT、Kimi、GLM 以及 30 多个提供商，再加上您自己的 vLLM、SGLang 或 Ollama（无需密钥）——所有这些都通过一个运行时和一套工具集实现。上下文限制和价格来自实际路径，未知价格将显示为未知，而不是 0 美元。 /// - Read-only until you allow more. Plan mode can't change files, and approvals gate risky commands. When an OS sandbox actually wraps a command, Codewhale says so: Seatbelt on macOS where available, opt-in bubblewrap on Linux. A repo's constitution.json compiles into write holds that even Full Access can't skip. // 默认只读，直到您授予更多权限。 计划模式无法修改文件，并且需要审批才能执行高风险命令。当操作系统沙箱实际封装命令时，Codewhale 会发出提示：macOS 系统在可用时使用 Seatbelt 封装，Linux 系统则使用可选的 bubblewrap 封装。仓库的 constitution.json 会编译成写入权限，即使是完全访问权限也无法跳过。 /// - Work you can resume. A fleet records every step to an append-only ledger, so fleet resume picks up where you left off. // 您可以继续执行工作。 车队会将每一步操作记录到仅追加的账本中，因此 fleet resume 会从您上次中断的地方继续。 /// Thanks to DeepSeek for the models and support that started the project, DataWhale 🐋 for welcoming us into the Whale Brother family, and OpenWarp and Open Design for collaborating on the terminal-agent experience. // 感谢 DeepSeek (. gh: @deepseek-ai) 提供模型和支持，使项目得以启动；感谢 DataWhale (. gh: @datawhalechina) 🐋 欢迎我们加入鲸兄弟大家庭，以及感谢 OpenWarp (. gh: zerx-lab/warp.git) 和 开放式设计 (. gh: nexu-io/open-design.git)，用于协作开发终端代理体验。"
[site/net]: https://codewhale.net/ "[数据与代码如海 // An ocean of data and code] 潜入深海，你不必亲自下潜。 // Dive into the deep, so you don't have to. /// Codewhale 把大模型的杠杆交给普通人：在你的终端里读取仓库、修改文件、运行检查、留下收据。不必已经是程序员，也能把东西做出来——运行在你自己的机器上。 // Codewhale gives ordinary people the leverage of LLMs to build things. In your terminal it reads the repo, edits files, runs checks, and leaves a receipt — without assuming you already speak code. It runs on your machine. /// 终端原生的水下壳。模型与提供商中立。本地优先。 // An underwater terminal shell. Model-neutral. Local-first. ///: 连接你已有的托管、网关或本地模型。Codewhale 在你的机器上运行；模型是可选择的组件，不是产品本身。Plan / Act / Operate 与明确的审批边界，让深潜也保持可控。 // Bring the hosted, gateway, or local model you already use. Codewhale runs on your machine and treats the model as a selectable component—not the product. Plan / Act / Operate and explicit permission postures keep the deep dive under your control. /// 从任务到经过验证的改动。 // From task to verified change. /// - 01 - 检查 // Inspect ///: 读取仓库、项目说明与任务。 // Read the repository, its instructions, and the task. /// - 02 - 执行 // Act ///: 在明确的审批边界内修改文件。 // Edit files through explicit approval boundaries. /// - 03 - 验证 // Verify ///: 运行检查并核对结果。 // Run checks and inspect the result. /// - 04 - 报告 // Report ///: 留下简洁、可追溯的工作收据 // Leave a concise, durable receipt. /// 你的模型。你的边界。 // Your model. Your boundaries. ///: 显式选择模型、工作模式与权限姿态。Codewhale 不会把未知成本显示成零，也不会把预览功能说成已发布产品。 // Choose the model, working mode, and permission posture explicitly. Unknown cost stays unknown, and preview surfaces stay labeled as such. /// - 36 个提供商路由 // 36 provider routes ///: 托管、网关与本地模型 // Hosted, gateway, and local models /// - Plan · Act · Operate // 计划 · 行动 · 运作 ///: 从只读规划到自主执行 // Read-only planning through autonomous operation /// - Ask · Auto-Review · Full Access // 提问 · 自动审核 · 完全访问权限 ///: 为任务选择权限姿态 // Choose the permission posture for the work /// - TUI · exec · web · API // 终端界面 · 自动编排 · 网页界面 · 应用接口 ///: 交互式与无头运行时界面 // Interactive and headless runtime surfaces /// Codewhale 开放运行时的文档、源码与社区入口。 // Documentation, source, and community for the open-source Codewhale runtime."
[docs/.site]: https://codewhale.net/docs "查找准确的使用说明。 // Find the guidance you need. ///: 从新手指引和安装开始，或者直接查找产品名词、模式、权限、工具、提供商、Fleet、钩子、MCP 与运行时 API。这些页面就是正式的产品文档；每页都链接仓库中的源文档，方便查阅完整细节。 // Start with the getting-started guide and installation, or go straight to vocabulary, modes, permissions, tools, providers, Fleet, hooks, MCP, and the Runtime API. These pages are the product documentation; each one cites the source documents in the repository for full detail."
[cli.cargo/crates]: https://crates.io/crates/codewhale-cli "(: cargo install -- codewhale-cli) (MIT) (12K SLoC) (123 KiB) Agentic terminal facade for open-source and open-weight coding models // 面向开源和开源编码模型的代理终端外观 (src: gh:Hmbown/CodeWhale.git)"
[cli.npm/npmjs]: https://npmjs.com/package/codewhale "(: npm i -g -- codewhale) (License: MIT)  (src: gh:Hmbown/CodeWhale.git)"
[cli.oci/ghcr]: https://github.com/Hmbown/CodeWhale/pkgs/container/codewhale "(: docker run --rm -it -e DEEPSEEK_API_KEY=... -v codewhale-home:/home/codewhale/.codewhale -- ghcr.io/hmbown/codewhale)"
[knowsby]: https://github.com/zerx-lab/zap.git "... /// - DeepSeek-TUI — first-class CLI agent partner. // DeepSeek-TUI — 一流的 CLI 代理合作伙伴。"

