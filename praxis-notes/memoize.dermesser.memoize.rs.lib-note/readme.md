[src/gh]: https://github.com/dermesser/memoize.git "(MIT) (Languages: Rust 100.0%) Macro for auto-memoizing Rust functions. // 用于自动记忆化 Rust 函数的宏。 /// A #[memoize] attribute for somewhat simple Rust functions: That is, functions with one or more Clone- and Hash-able arguments, and a Clone-able return type. That's it. // 一个用于较为简单的 Rust 函数的 #[memoize] 属性：也就是说，适用于具有一个或多个 Clone 和 Hash 的参数，以及可 Clone 的返回类型的函数。仅此而已。 /// NEWS: The crate has been updated so that you don't need to separately import lru, lazy_static, and other dependencies. Now everything should work automatically. Remember to enable the full feature to use LRU caching and other additional features. // 最新消息 ：该库已更新，因此您无需单独导入 lru 。 lazy_static 和其他依赖项已启用。现在一切应该都能自动运行了。请记住启用 full 功能以使用 LRU 缓存和其他附加功能。"
[lib.cargo/crates]: https://crates.io/crates/memoize "(: cargo add -- memoize) (MIT) (4 SLoC) (7.93 KiB) Attribute macro for auto-memoizing functions with somewhat-simple signatures // 用于对具有相对简单签名的函数进行自动记忆化的属性宏 (src: gh:dermesser/memoize.git)"

