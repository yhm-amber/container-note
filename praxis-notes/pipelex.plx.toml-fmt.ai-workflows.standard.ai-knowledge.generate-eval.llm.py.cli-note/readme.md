[core.src/gh]: https://github.com/Pipelex/pipelex.git "(MIT) (Languages: Python 96.5%, Perl 2.6%, Makefile 0.9%) Runtime & PLX language The Python library and language specification. Build, run, and compose AI workflows with structured I/O and validation. // 运行时和 PLX 语言 Python 库和语言规范。构建、运行和组合具有结构化 I/O 和验证功能的 AI 工作流。 /// Declarative language for composable Al workflows. Devtool for agents and mere humans. // 用于可组合人工智能工作流的声明式语言。面向智能体和普通用户的开发工具。 /// AI Workflows That Agents Build & Run // 智能体构建和运行的 AI 工作流程 /// Pipelex is developing the open standard for repeatable AI workflows. // Pipelex 正在开发可重复的 AI 工作流程的开放标准。 /// Write business logic, not API calls. // 编写业务逻辑，而不是 API 调用。 /// 💡 What is Pipelex? // 💡 Pipelex 是什么？ /// Pipelex is an open-source language that enables you to build and run repeatable AI workflows. Instead of cramming everything into one complex prompt, you break tasks into focused steps, each pipe handling one clear transformation. // Pipelex 是一种开源语言，可用于构建和运行可重复的 AI 工作流 。它无需将所有内容塞进一个复杂的提示框中，而是将任务分解为多个专注的步骤，每个管道处理一个清晰的转换。 /// Each pipe processes information using Concepts (typing with meaning) to ensure your pipelines make sense. The Pipelex language (.plx files) is simple and human-readable, even for non-technical users. Each step can be structured and validated, giving you the reliability of software with the intelligence of AI. // 每个管道都使用概念 （有意义的输入）处理信息，以确保管道逻辑清晰。Pipelex 语言（ .plx 文件）简单易懂，即使对于非技术用户也易于理解。每个步骤都可以进行结构化和验证，从而兼具软件的可靠性和人工智能的智能。 /// Create a complete AI workflow with a single command: // 只需一条命令即可创建完整的 AI 工作流程： /// (: pipelex build pipe 'Take a CV and Job offer in PDF, analyze if they match and generate 5 questions for the interview' --output results/cv_match.plx) /// This command generates a production-ready .plx file with domain definitions, concepts, and multiple processing steps that analyzes CV-job fit and prepares interview questions. // 该命令生成一个可用于生产的 .plx 文件，其中包含领域定义、概念和多个处理步骤，用于分析简历与工作的匹配度并准备面试问题。 /// (# Content of 'cv_match.plx' like toml format with llm-generated structured (kv/rec/list) content within it #) (# And !!! That .plx file can be using back for pipelex cli tool (another one sub-cmd) as an ai-workflow definition !!!! #) /// Run Your Pipeline // 运行您的流水线 /// (: pipelex run results/cv_match.plx) /// You can also using `--inputs inputs.json` with content (: { 'cv_pdf': { 'concept': 'PDF', 'content': { 'url': '.../John-Doe-CV.pdf' } }, 'job_offer_pdf': { 'concept': 'PDF', 'content': { 'url': '.../Job-Offer.pdf' } } } :) to Run with input file if it's necessary, i.e., the .plx file having line(s) like (: inputs = { cv_pdf = \"PDF\", job_offer_pdf = \"PDF\" } :). // 如果需要，您还可以使用 `--inputs inputs.json` 运行，其中包含 (: { 'cv_pdf': { 'concept': 'PDF', 'content': { 'url': '.../John-Doe-CV.pdf' } }, 'job_offer_pdf': { 'concept': 'PDF', 'content': { 'url': '.../Job-Offer.pdf' } } } :) ，以便使用输入文件运行，例如，.plx 文件中包含类似 (: inputs = { cv_pdf = \"PDF\", job_offer_pdf = \"PDF\" } :) 的行。 /// Via Python /// (: import asyncio,json ;: from pipelex.pipeline.execute import execute_pipeline ;: from pipelex.pipelex import Pipelex ;: async def your_pipeline(): ;:: with open('inputs.json', encoding='utf-8') as f: inputs = json.load(f) ;:: pipe_output = await execute_pipeline(pipe_code='cv_match', inputs=inputs) ;:: print(pipe_output.main_stuff_as_str) ;: Pipelex.make() ;: asyncio.run(your_pipeline()) # like pipelex run ...?)"
[core.pip/pypi]: https://pypi.org/project/pipelex/ "(: pip install -- pipelex) (License Expression: MIT) (Requires: Python <3.14, >=3.10) (Provides-Extra: [anthropic], [bedrock], [dev], [docs], [fal], [google], [google-genai], [mistralai]) The open standard for repeatable AI workflows. Write business logic, not API calls. // 可重复使用的 AI 工作流程的开放标准。编写业务逻辑，而非 API 调用。 (src: gh:Pipelex/pipelex.git)"
[langsupport-ide.vsix.src/gh]: https://github.com/Pipelex/vscode-pipelex.git "(~ forked from: gh:tamasfe/taplo.git) (MIT) (Languages: Rust 87.0%, TypeScript 8.1%, Perl 1.3%, JavaScript 1.2%, Vue 0.9%, Shell 0.8%, Other 0.7%) A Pipelex extension for VS Code & Cursor, based on taplo, the TOML toolkit written in Rust // 这是一个基于 taplo 的 Pipelex 扩展，适用于 VS Code 和 Cursor。taplo 是一个用 Rust 编写的 TOML 工具包。 /// This repo provides VS Code support for the Pipelex Language (PLX) which is based on TOML syntax. The repo is a fork of Taplo and it tracks Taplo upstream closely. // 本仓库为基于 TOML 语法的 Pipelex 语言 (PLX) 提供 VS Code 支持。本仓库是 Taplo 的一个分支，并与 Taplo 上游紧密关联。 /// About Pipelex: // 关于 Pipelex： /// Pipelex is an open-source language for building deterministic AI workflows. It enables agents and developers to transform natural language requirements into production-ready pipelines that process information reliably at scale. Unlike traditional workflow tools, Pipelex uses a declarative syntax that captures business logic directly, making pipelines readable by domain experts while remaining executable by any runtime. Write once, run anywhere, share with everyone. // Pipelex 是一种用于构建确定性 AI 工作流的开源语言。它使智能体和开发人员能够将自然语言需求转换为可用于生产环境的流水线，从而大规模可靠地处理信息。与传统的工作流工具不同，Pipelex 使用声明式语法直接捕获业务逻辑，使领域专家能够轻松阅读流水线，同时确保任何运行时环境都能执行这些流水线。一次编写，随处运行，与所有人共享。 /// What we offer in addition to Taplo // 除了 Taplo 之外，我们还提供其他服务 /// - PLX language support: Rich syntax highlighting, semantic tokens, and language features for .plx files // PLX 语言支持 ：为 .plx 文件提供丰富的语法高亮、语义标记和语言特性 /// - Concept definitions: [concept.Name] sections with specialized highlighting // 概念定义 ： [concept.Name] 部分，并带有特殊高亮显示 /// - Pipe definitions: [pipe.name] sections for workflow steps // 管道定义 ： [pipe.name] 部分，用于工作流程步骤 /// - Data injection: @variable syntax with smart highlighting // 数据注入 ：带有智能高亮显示的 @variable 语法 /// - Template variables: $variable support with Jinja2 templates // 模板变量 ：Jinja2 模板支持 $variable /// - All Taplo features retained: Complete TOML 1.0.0 support and tooling // Taplo 的所有功能均保留 ：完全支持 TOML 1.0.0 及相关工具"
[langsupport-ide.vsix/ovsx]: https://open-vsx.org/extension/Pipelex/pipelex "Pipelex Language (PLX) and TOML support // Pipelex 语言 (PLX) 和 TOML 支持"
[pipeline-apiserver.src/gh]: https://github.com/Pipelex/pipelex-api.git "(MIT) (Languages: Python 49.3%, Makefile 47.8%, Dockerfile 2.3%, Perl 0.6%) FastAPI server & Docker 🚀 Deploy Pipelex as a REST API. Self-host with Docker or integrate into your infrastructure. // FastAPI 服务器和 Docker 🚀 将 Pipelex 部署为 REST API。使用 Docker 进行自托管，或集成到您的基础架构中。 /// API of Pipelex: open-source language for AI Agents to create and run repeatable AI workflows // Pipelex API：一种用于人工智能智能体的开源语言，用于创建和运行可重复的人工智能工作流程 /// Pipelex API Server // Pipelex API 服务器 /// The official REST API server for building and executing Pipelex pipelines. Deploy your pipelines as HTTP endpoints and integrate them into any application or workflow. // 用于构建和执行 Pipelex 流水线的官方 REST API 服务器。将您的流水线部署为 HTTP 端点，并将其集成到任何应用程序或工作流中。"
[pipeline-apiserver.oci/dockerhub]: https://hub.docker.com/r/pipelex/pipelex-api "(: docker run --name pipelex-api -p 8081:8081 -e API_KEY=<your-api-key-here> -e PIPELEX_INFERENCE_API_KEY=<your-pipelex-inference-api-key> -e FAL_API_KEY=<your-fal-api-key> -- docker.io/pipelex/pipelex-api:latest) API of Pipelex: open-source language for AI Agents to create and run repeatable AI workflows // Pipelex API：一种用于人工智能智能体的开源语言，用于创建和运行可重复的人工智能工作流程"
[agent-mcpserver.src/gh]: https://github.com/Pipelex/pipelex-mcp.git "(MIT) (Languages: Makefile 53.3%, Python 41.6%, Perl 5.1%) Model Context Protocol server 🤖 Let Claude, Cursor, and other MCP-compatible agents run and build Pipelex workflows. // 模型上下文协议服务器 🤖 让 Claude、Cursor 和其他 MCP 兼容的智能体运行和构建 Pipelex 工作流。 /// MCP server to run Pipelex pipelines // MCP 服务器运行 Pipelex 管道 /// Pipelex MCP Server // Pipelex MCP 服务器 /// Enable AI agents to build and execute Pipelex pipelines on-the-fly. Just describe what you need, and watch as the agent constructs and runs the pipeline for you. // 让 AI 智能体能够即时构建和执行 Pipelex 流水线。只需描述您的需求，即可观看智能体为您构建和运行流水线。 /// The Pipelex MCP Server implements the Model Context Protocol to enable AI agents to interact with Pipelex as a native tool. This bridges the gap between conversational AI and structured, repeatable AI workflows. // Pipelex MCP 服务器实现了模型上下文协议 (MCP) ，使 AI 智能体能够像使用原生工具一样与 Pipelex 进行交互。这弥合了对话式 AI 与结构化、可重复的 AI 工作流程之间的差距。 /// With this MCP server, AI agents can: // 借助此 MCP 服务器，AI 智能体可以： /// - Build pipelines from natural language descriptions // 基于自然语言描述构建管道 /// - Execute pipelines with specific inputs // 使用特定输入执行管道 /// - Receive structured outputs ready for downstream tasks // 接收可用于后续任务的结构化输出"
[n8nworkflows-node.src/gh]: https://github.com/Pipelex/n8n-nodes-pipelex.git "(MIT) (Languages: Makefile 60.5%, TypeScript 32.4%, JavaScript 7.1%) n8n automation node ⚡ Integrate Pipelex workflows into n8n for no-code automation and orchestration. // n8n 自动化节点 ⚡ 将 Pipelex 工作流集成到 n8n 中，实现无代码自动化和编排。 /// n8n Node for running Pipelex // 用于运行 Pipelex 的 n8n 节点 /// Execute Pipelex AI pipelines in your n8n workflows // 在您的 n8n 工作流程中执行 Pipelex AI 管道"
[site/com]: https://pipelex.com/ "Built with agents. Build for agents. /// AI Workflows That Agents Build & Run // 智能体构建和运行的 AI 工作流程 /// Built by developers who were tired of rewriting the same AI patterns. // 由一群厌倦了重复编写相同人工智能模式的开发者打造。 /// Now you don't have to. // 现在你不用这么做了。"
[docs/.site]: https://docs.pipelex.com/ "What is Pipelex? // Pipelex 是什么？ /// Pipelex is an open-source language that enables agents to build and run repeatable AI workflows. Instead of cramming everything into one complex prompt, you break tasks into focused steps, each pipe handling one clear transformation. // Pipelex 是一种开源语言，它使智能体能够构建和运行可重复的 AI 工作流 。它不会将所有内容都塞进一个复杂的提示中，而是将任务分解成多个专注的步骤，每个管道处理一个明确的转换。 /// Each pipe processes information using Concepts (typing with meaning) to ensure your pipelines make sense. The Pipelex language (.plx files) is simple and human-readable, even for non-technical users. // 每个管道都使用概念 （有意义的输入）来处理信息，以确保管道逻辑清晰易懂。Pipelex 语言（ .plx 文件）简单易读，即使对于非技术用户也适用。 /// Each step can be structured and validated, so you benefit from the reliability of software, and the intelligence of AI. // 每个步骤都可以进行结构化和验证，因此您可以受益于软件的可靠性和人工智能的智能。"
[starter.src/gh]: https://github.com/Pipelex/pipelex-starter.git "(MIT) (Languages: Makefile 76.1%, Python 22.4%, Perl 1.5%) Project template Bootstrap new projects with batteries included: Makefile, tests, environment setup. // 项目模板 启动新项目，并包含所有必要组件：Makefile、测试、环境设置。 /// Simple starter template to get you up and running with a project to build and run Pipelex pipelines // 一个简单的入门模板，可帮助您快速上手构建和运行 Pipelex 管道的项目。"
[cookbook.src/gh]: https://github.com/Pipelex/pipelex-cookbook.git "(MIT) (Languages: Makefile 50.7%, Python 41.5%, Perl 7.8%) Examples & recipes Ready-to-run pipelines: classification, extraction, analysis, generation, and more. // 示例和食谱 即用型流程：分类、提取、分析、生成等。 /// Cookbook for Pipelex, the declarative language for composable Al workflows. Devtool for agents and mere humans. // Pipelex 使用指南，Pipelex 是一种用于可组合 AI 工作流的声明式语言。它是面向智能体和普通用户的开发工具。 /// Pipelex Cookbook 📚 /// Examples, recipes, and best-practice pipelines for the Pipelex AI workflow framework. // Pipelex AI 工作流框架的示例、配方和最佳实践流程。 /// Learn by doing with production-ready examples. // 通过实际生产环境案例进行实践学习。"
[twitter]: https://twitter.com/PipelexAI
[youtube]: https://youtube.com/@PipelexAI
[discord]: https://discord.gg/PmrvnkqB8r

