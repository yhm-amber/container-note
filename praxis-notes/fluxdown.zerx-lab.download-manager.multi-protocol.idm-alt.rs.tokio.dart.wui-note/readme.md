[src/gh]: https://github.com/zerx-lab/FluxDown.git "(AGPL-3.0) (Languages: Rust 43.2%, Dart 29.8%, TypeScript 20%, C++ 2.1%, CSS 1.3%, Swift 0.8%, Other 2.8%) Rust 驱动的多协议下载管理器，支持 HTTP/FTP/BitTorrent 磁力链接及 HLS/DASH 流媒体，智能多线程加速与浏览器无缝集成。精美界面，极致性能，永久免费，零广告。 /// FluxDown // 通量下降 /// Downloads, Supercharged. // 下载量激增。 /// A blazing fast, multi-protocol download manager — the free & open-source IDM alternative. // 一款速度极快、支持多种协议的下载管理器——免费开源的 IDM 替代品。 /// Highlights // 精彩片段 /// - Up to 10x faster — Rust + Tokio engine with IDM-style dynamic segmentation // 速度提升高达 10 倍 ——Rust + Tokio 引擎，采用 IDM 式动态分段 /// - Multi-protocol — HTTP/HTTPS, FTP, BitTorrent, eD2K, HLS & DASH streaming // 支持多种协议 ——HTTP/HTTPS、FTP、BitTorrent、eD2K、HLS 和 DASH 流媒体 /// - Browser integration — Chrome / Edge / Firefox extension with a 3-layer interception engine // 浏览器集成 ——Chrome/Edge/Firefox 扩展程序，带有三层拦截引擎 /// - AI-agent ready — built-in MCP (Model Context Protocol) server: let Claude, Cursor & other AI clients manage your downloads // 支持人工智能代理 ——内置 MCP（模型上下文协议）服务器：让 Claude、Cursor 和其他人工智能客户端管理您的下载 /// - Resume anywhere — full download state persisted in SQLite; survive crashes and reboots // 随时随地恢复下载 ——完整的下载状态保存在 SQLite 数据库中；即使崩溃或重启后也能继续下载。 /// - Beautiful UI — light/dark themes, 13 color schemes, responsive three-pane layout // 精美的用户界面 ——浅色/深色主题，13 种配色方案，响应式三窗格布局 /// - Clean & private — free and open source, no ads, no tracking, no account required, local-first // 干净私密 ——免费开源，无广告，不追踪，无需注册，本地优先。 /// Features // 特征 /// - Rust-Powered Engine // 锈蚀动力发动机 ///: Built on Rust and Tokio with zero-cost abstractions — memory-safe concurrency at maximum throughput // 基于 Rust 和 Tokio 构建，采用零成本抽象——在最大吞吐量下实现内存安全的并发 /// - Smart Segmentation // 智能分段 ///: Segments split dynamically at runtime; idle threads rescue slow segments, just like IDM — but smarter // 运行时动态拆分段；空闲线程会像 IDM 一样拯救慢速段，但更智能。 /// - Multi-Protocol // 多协议 ///: Dedicated engines for HTTP/HTTPS, FTP, BitTorrent (DHT/UPnP/magnet), eD2K (server + Kad DHT source finding, MD4 verification), HLS (AES-decrypt) and DASH // 专用于 HTTP/HTTPS、FTP、BitTorrent（DHT/UPnP/磁力链接）、eD2K（服务器 + Kad DHT 源查找、MD4 验证）、HLS（AES 解密）和 DASH 的引擎 /// - Speed Control // 速度控制 ///: Token-bucket global rate limiting — download in the background without killing your browsing // 令牌桶全局速率限制——在后台下载而不中断您的浏览活动 /// - Resume Anywhere // 随时随地恢复 ///: Every byte tracked in SQLite with WAL; power loss never costs you progress // 使用 WAL 跟踪 SQLite 中的每个字节；断电不会影响进度。 /// - Browser Integration // 浏览器集成 ///: Three-layer download interception, streaming media sniffing, Alt+Click bypass, right-click send // 三层下载拦截、流媒体嗅探、Alt+单击绕过、右键单击发送 /// - MCP Server // MCP 服务器 ///: Built-in Model Context Protocol endpoint (Streamable HTTP) with 12 tools — AI agents can add, monitor and control downloads // 内置模型上下文协议端点（Streamable HTTP），包含 12 种工具——AI 代理可以添加、监控和控制下载。 /// - Beautiful Interface // 美观的界面 ///: shadcn-style widgets, IDM-style segment visualization, named queues, system tray // shadcn 风格的小部件、IDM 风格的分段可视化、命名队列、系统托盘 /// - Clean & Private // 干净私密 ///: Zero ads, zero telemetry lock-in, zero accounts — your data never leaves your machine // 零广告、零遥测数据锁定、零账户——您的数据永远不会离开您的设备 /// Made by zerx-lab // 由 zerx-lab (. gh: @zerx-lab) 制作"
[site/dev]: https://fluxdown.zerx.dev/ "下载器，该换代了。 // The download manager, rebuilt. ///: 分段并行的 Rust 引擎，只是起点——HTTP、BT 磁力、ED2K、流媒体，全协议；桌面、浏览器、NAS 无头服务，全平台；插件与主题市场，开放生态。下载器，只是它的第一个身份。 // The parallel-segment Rust engine is just the start — every protocol from HTTP and BitTorrent to ED2K and HLS; every surface from desktop and browser to headless NAS; an open ecosystem of plugins and themes. A download manager is only its first job. /// 核心功能 // Features ///: 不是功能清单，是换代的理由 // Not a feature list — reasons to switch ///: 每一项都指向同一件事：把十几年没变过样的下载体验，重新做一遍。 // Every one of these points at the same thing: a download experience that hadn't changed in a decade, redone properly. /// - Rust 高性能引擎 // Rust-Powered Engine ///: 基于 Rust 和 Tokio 构建，实现最大吞吐量。零开销抽象在保证内存安全的同时，提供原生级别的并发下载性能。 // Built with Rust and Tokio for maximum throughput. Zero-cost abstractions deliver native performance while ensuring memory safety under heavy concurrent loads. /// - 多协议支持 // Multi-Protocol ///: 全面支持 HTTP、HTTPS、FTP、BitTorrent 磁力链接、ED2K 电驴链接及 HLS/DASH 流媒体。每种协议都有专属优化引擎——从 HTTP Range 请求到 DHT 节点发现，开箱即用。 // HTTP, HTTPS, FTP, BitTorrent with magnet link support, ED2K (eDonkey) links, and HLS/DASH streaming. Each protocol has a dedicated, optimized engine — from HTTP Range requests to DHT peer discovery. /// - 速度控制 // Speed Control ///: 基于令牌桶算法的全局限速器。设置带宽限制，让下载在后台运行的同时保持流畅的浏览体验。 // Token bucket-based global speed limiter. Set bandwidth limits to keep your browsing smooth while downloads run in the background. /// - 断点续传 // Resume Anywhere ///: 完整的断点续传支持。所有下载状态持久化到 SQLite — 安全关闭和重启，不丢失任何一个字节。 // Full breakpoint resume support. All download state is persisted to SQLite — safely close and restart without losing a single byte. /// - 智能分段 // Smart Segmentation ///: IDM 风格的智能分段与运行时动态拆分。空闲线程自动接管慢速分段，连接复用避免重建开销，榨取每一份带宽。 // IDM-style intelligent segmentation with runtime dynamic splitting. Idle workers auto-take over slow segments, reuse connections, and maximize throughput. /// - 浏览器集成 // Browser Integration ///: Chrome/Firefox 扩展三层拦截引擎，自动识别流媒体资源（HLS/DASH）。页面资源嗅探、智能文件类型过滤、域名规则，一键加速。 // Chrome/Firefox extension with three-layer interception engine. Auto-detects streaming media (HLS/DASH), in-page resource sniffing, smart file type filters, one-click acceleration. /// - 精美界面 // Beautiful Interface ///: 精心打磨的视觉体验——深色与浅色主题、13 套配色方案、可调节面板的响应式布局。一款真正赏心悦目的下载工具。 // Crafted with care — dark and light themes, 13 color schemes, responsive layout with resizable panels. A download manager that's actually pleasing to use. /// - 纯净安心 // Clean & Private ///: 零广告、零追踪、无需注册账号。所有数据留在本地——基于 SQLite 的本地优先架构。你的下载，只属于你。 // Zero ads, zero tracking, no account required. All data stays on your device — local-first architecture with SQLite persistence. Your downloads are your business. /// - 插件系统 // Plugin System ///: JavaScript 编写的可选插件层，在沙箱运行时中安全执行。链接解析器自动嗅探直链、通知钩子命令式重试，插件市场一键安装——内容寻址校验，全程可验证。 // An optional plugin layer written in JavaScript, running safely inside a sandboxed runtime. Link resolvers sniff out direct URLs, notification hooks trigger imperative retries, and the plugin market installs plugins in one click — content-addressed and fully verifiable. /// - 受管组件 // Managed Components ///: 按需下载并托管 ffmpeg、yt-dlp 等外部工具。插件在隔离沙箱中调用它们完成合并、转码、抽流，无需手动配置环境变量或污染系统 PATH。 // Download and manage external tools like ffmpeg and yt-dlp on demand. Plugins invoke them inside a jailed sandbox for merging, transcoding, and stream extraction — no manual PATH setup, no polluting your system. /// - 社区加速网络 // Community Speed Network ///: 下载自动选择最快线路。FluxDown 将来自社区的匿名镜像测速结果汇聚成共享路由图——下载的人越多，所有人的速度越快。不含链接、不含文件名、不含身份信息，只记录「哪个地区哪个镜像更快」。 // Downloads automatically pick the fastest route. FluxDown merges anonymous mirror speed measurements from the community into a shared routing map — the more people download, the faster everyone gets. No URLs, no file names, no identity: just which mirror is fast from which region. /// 用 Rust 重写的多协议下载管理器——动态分段、浏览器深度接管，HTTP/FTP/BT/ED2K/HLS 全覆盖。开源免费，没有广告，不限速。 // The download manager, rebuilt in Rust — dynamic segmentation, deep browser takeover, and full coverage from HTTP and FTP to BitTorrent, ED2K and HLS. Open source, no ads, no throttling."
[browser-addn.crx/chrome]: https://chromewebstore.google.com/detail/fluxdown/meleenglfggcmcajknpeeeiobnpfmahc "(102KiB)"
[browser-addn.crx/msedge]: https://microsoftedge.microsoft.com/addons/detail/fluxdown/nglkkjbogjghekbhhcnccnpfedjbdhhd ""
[browser-addn.xpi/firefox]: https://addons.mozilla.org/firefox/addon/fluxdown/ "(107.83 KB) (~ fluxdown@fluxdown.app)"
[server.oci/ghcr]: https://github.com/users/zerx-lab/packages/container/package/fluxdown-server "(: docker run -d --name fluxdown-server -p 17800:17800 -v fluxdown-data:/data -v ~/downloads:/root/Downloads --restart unless-stopped -- ghcr.io/zerx-lab/fluxdown-server:latest) 启动后访问 http://localhost:17800，首次打开会引导你自行设置访问密钥（至少 8 位，含字母和数字）。"
[knowsby]: https://github.com/rust-unofficial/awesome-rust.git "... /// - zerx-lab/FluxDown - A multi-protocol download manager with a Rust/Tokio engine, supporting HTTP/FTP, BitTorrent, eD2K, HLS and DASH, with IDM-style dynamic segmentation, browser extensions and an aria2-compatible JSON-RPC endpoint. // zerx-lab/FluxDown - 一款多协议下载管理器，采用 Rust/Tokio 引擎，支持 HTTP/FTP、BitTorrent、eD2K、HLS 和 DASH，具有 IDM 风格的动态分段、浏览器扩展和 aria2 兼容的 JSON-RPC 端点。 /// ..."

