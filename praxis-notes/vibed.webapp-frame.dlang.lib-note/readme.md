[src/gh]: https://github.com/vibe-d/vibe.d.git '(MIT) (Languages: D 97.7%, Shell 2.3%) Official vibe.d development // 官方 vibe.d 开发 /// vibe.d is a high-performance asynchronous I/O, concurrency and web application toolkit written in D. It already contains many supplemental features such as database support to be able to offer a complete development environment. For more specialized needs, there are also many compatible DUB packages available. // vibe.d 是一款用 D 语言编写的高性能异步 I/O、并发和 Web 应用工具包。它已经包含了许多辅助功能，如数据库支持，能够提供完整的开发环境。对于更专业的需求，也有许多兼容的 DUB 包可供使用。 /// (: #!/usr/bin/env dub ;: /+ dub.sdl: name "hello_vibed" dependency "vibe-d" version="~>0.9.0" +/ import vibe.vibe; void main () { listenHTTP("127.0.0.1:8080", (req, res) { res.writeBody("Hello Vibe.d: " ~ req.path); }); runApplication(); }) /// The vibe.d project is structured into multiple repositories: // vibe.d 项目被组织到多个仓库中： /// - vibe-d (this repository): The high level web and REST framework, HTTP, STMP and Redis/MongoDB functionality // vibe-d (此仓库): 高级网络和 REST 框架，HTTP，STMP 以及 Redis/MongoDB 功能 /// - vibe-http (. gh: vibe-d/vibe-http.git): HTTP client/server implementation including WebSockets, proxy, sessions etc. // vibe-http : HTTP 客户端/服务器实现，包括 WebSocket、代理、会话等。 /// - vibe-stream (. gh: vibe-d/vibe-stream.git): Advanced stream types and TLS stream support // vibe-stream : 高级流类型和 TLS 流支持 /// - vibe-inet (. gh: vibe-d/vibe-inet.git): Internet standard functionality (URL, form encoding, HTML encoding, ...) // vibe-inet : 互联网标准功能（URL、表单编码、HTML 编码、...） /// - vibe-core (. gh: vibe-d/vibe-core.git): Fiber based I/O and concurrency implementation // vibe-core : 基于 Fiber 的 I/O 和并发实现 /// - vibe-serialization (. gh: vibe-d/vibe-serialization.git): Serialization and structured data format support // vibe-serialization : 序列化和结构化数据格式支持 /// - vibe-container (. gh: vibe-d/vibe-container.git): Container implementations used throughout the project // vibe-container : 项目中使用的容器实现 /// - vibe-sdl (. gh: vibe-d/vibe-sdl.git): SDLang (. web: sdlang.org) serialization support based on sdlite (. gh: s-ludwig/sdlite.git) // vibe-sdl : 基于 sdlite 的 SDLang 序列化支持 /// - vibe-compat (. gh: vibe-d/vibe-compat.git): Legacy functionality // vibe-compat : 传统功能 /// - observable (. gh: vibe-d/observable.git): Observable, signal/slot and reactive value implementations // observable : 可观察、信号/槽和响应式值实现 /// - diet (. gh: rejectedsoftware/diet-ng.git): pug.js inspired compile-time HTML template system with vibe.web integration // diet : 受 pug.js 启发的编译时 HTML 模板系统，与 vibe.web 集成 /// - eventcore (. gh: vibe-d/eventcore.git): Low-level abstraction over the operating system’s asynchronous I/O facilities // eventcore : 操作系统异步 I/O 设施的低级抽象 /// You can find an API overview of the whole project in the API documentation (. web: vibed.org/api). // 您可以在 API 文档中找到整个项目的 API 概览。 /// Support // 支持 /// Vibe.d supports the 10 latest minor releases of DMD. For example, if the current version is v2.090.1, then v2.089.x, v2.088.x, ... v2.080.x are supported. Note that support for patch release is desireable, but only support for the last patch in a minor is guaranteed. // Vibe.d 支持 DMD 的 10 个最新小版本。例如，如果当前版本是 v2.090.1，那么 v2.089.x、v2.088.x、... v2.080.x 都受支持。请注意，补丁版本的支持是期望的，但仅保证支持小版本中的最后一个补丁。 /// Additionally, Vibe.d supports all LDC versions that implement the version of a supported frontend (e.g. by the previous rule LDC v1.20.0 implements v2.090.1 and would be supported). // 此外，Vibe.d 支持所有实现支持的前端版本的 LDC 版本（例如，根据前一条规则，LDC v1.20.0 实现了 v2.090.1 并会被支持）。'
[site/org]: https://vibed.org/ 'Asynchronous I/O that doesn’t get in your way, written in D // 异步 I/O，不干扰，使用 D 语言编写 /// Productive // 高效 /// - High-level declarative REST and web application framework // 高级声明式 REST 和 Web 应用框架 /// - Full HTTP(S) stack with client, server and proxy implementations // 完整的 HTTP(S)栈，包含客户端、服务器和代理实现 /// - Shipped with native database drivers for MongoDB and Redis // 带有 MongoDB 和 Redis 的原生数据库驱动 /// - Complete concurrency toolkit and support for low level I/O operations // 完整的并发工具包和底层 I/O 操作支持 /// - ... /// Fast // 快速 /// - Asynchronous I/O for maximum speed and minimum memory usage // 异步 I/O 以实现最大速度和最小内存使用 /// - Compile-time “Diet” templates for unparalleled dynamic page speed // 编译时 “轻量” 模板，实现无与伦比的动态页面速度 /// - Compiled to native machine code // 编译为原生机器代码 /// - Multi-threading and integrated load-balancing* // 多线程和集成负载均衡* /// - ... /// Simple // 简单 /// - Fiber based blocking programming model for concise and intuitive development // 基于 Fiber 的阻塞编程模型，实现简洁直观的开发 /// - Compact API with sensible default choices // 紧凑的 API，具有合理的默认选项 /// - Full support for exception based error handling // 全面支持基于异常的错误处理 /// - Simple access to third-party extension libraries using the DUB package system // 使用 DUB 包系统轻松访问第三方扩展库 /// - ... /// Example of a simple HTTP server // 一个简单的 HTTP 服务器示例 /// (: import vibe.vibe; void main () { listenHTTP(":8080", &handleRequest); runApplication(); } void handleRequest(HTTPServerRequest req, HTTPServerResponse res) { if (req.path == "/") res.writeBody("Hello, World!"); }) /// Example of an echo server // 一个回声服务器示例 /// (: import vibe.vibe; void main () { listenTCP(7, (conn) { conn.write(conn); }); runApplication(); })'
[docs/.site]: https://vibed.org/docs "Documentation // 文档"
[lb.src/gh:rejectedsoftware]: https://github.com/rejectedsoftware/vibedist.git "(MIT) (Languages: D 100.0%) The vibe.d load-balancer and virtual host manager // vibe.d 负载均衡器和虚拟主机管理器 /// NOTE: This project is not yet in a functional state and development is currently suspended. Development is expected to be resumed somewhere in 2017. Until then, any generic load balancing solution, such as the one integrated into Nginx can be used as a manually configured replacement. // 注意：此项目尚未处于功能状态，目前开发已暂停。预计将在 2017 年某个时候恢复开发。在此期间，任何通用的负载均衡解决方案，例如 Nginx 集成的解决方案，都可以用作手动配置的替代方案。"
[knowsby]: https://bsonspec.org/implementations.html "... /// - D ///: vibe.data.bson (. web: vibed.org/api/vibe.data.bson) - BSON serialization and value handling. // vibe.data.bson - BSON 序列化和值处理。 /// ..."


