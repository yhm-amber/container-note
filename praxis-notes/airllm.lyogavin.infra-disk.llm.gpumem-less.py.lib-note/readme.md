[src/gh]: https://github.com/lyogavin/airllm.git "(Apache-2.0) (Languages: Jupyter Notebook 95.7%, Python 4.2%, Shell 0.1%) AirLLM 70B inference with single 4GB GPU // 使用单块 4GB GPU 进行 AirLLM 70B 推理 /// AirLLM dramatically reduces inference memory usage, letting 70B large language models run on a single 4GB GPU card — without quantization, distillation, or pruning. You can even run 405B Llama 3.1 on 8GB, DeepSeek-V3 (671B) on ~12GB, and Kimi K3 (2.8T) — the largest open-source model released to date — on under 4GB, because sparse MoE models stream one expert at a time rather than a whole layer. // AirLLM 大幅降低了推理内存占用，使得 700 亿的大型语言模型可以在单张 4GB GPU 卡上运行——无需量化、蒸馏或剪枝。你甚至可以在 8GB 内存上运行 4050 亿的 Llama 3.1 ，在约 12GB 内存上运行 671 亿的 DeepSeek-V3 ，以及在不到 4GB 内存上运行迄今为止最大的开源模型 Kimi K3（2.8T） ，因为稀疏 MoE 模型每次只传输一个专家数据，而不是整个层。"
[intro/hf-blog]: https://huggingface.co/blog/lyogavin/airllm "Unbelievable! Run 70B LLM Inference on a Single 4GB GPU with This NEW Technique // 难以置信！利用这项新技术，仅需一块 4GB GPU 即可运行 700 亿次 LLM 推理！"
[lib.pip/pypi]: https://pypi.org/project/airllm/ "(: pip install -- airllm) AirLLM runs 70B large language models on a single 4GB GPU without quantization, distillation or pruning. 405B Llama 3.1 on 8GB, DeepSeek-V3 671B on ~12GB, Kimi K3 2.8T on under 4GB. // AirLLM 在单个 4GB GPU 上运行 70B 大型语言模型，无需量化、蒸馏或剪枝。405B Llama 3.1 在 8GB 上，DeepSeek-V3 671B 在 ~12GB 上，Kimi K3 2.8T 在不到 4GB 上。"
