[src/gh]: https://github.com/zipxing/rust_pixel.git "(Apache-2.0) (Languages: Rust 93.1%, Python 5.5%, JavaScript 0.4%, C++ 0.3%, Makefile 0.3%, BASIC 0.2%, Other 0.2%) Tile-first. Retro-ready. Write Once, Run Anywhere—2D Engine! // 以图块为先导。兼容复古风格。一次编写，到处运行——2D 引擎！ /// ## Core Philosophy // 核心理念 /// ### Everything is Tiles // 一切皆为瓷砖 /// TUI & sprites → buffer → cells → tiles // TUI 和精灵 → 缓冲区 → 单元格 → 图块 /// A unified rendering abstraction // 统一渲染抽象 /// High performance: Texture2DArray, single draw call // 高性能： Texture2DArray，单次绘制调用 /// ### One Codebase, Multiple Targets // 一套代码库，多个目标平台 /// Terminal · Desktop · Web // 终端 · 桌面 · 网页 /// GPU-rendered TUI — no terminal emulator // GPU 渲染的 TUI——无需终端模拟器 /// Hybrid TUI + pixel content across targets // 混合式 TUI + 跨目标像素内容 /// ### Quick Start // 快速入门 /// app! macro scaffolding // app! 宏脚手架 /// Built-in BASIC interpreter // 内置 BASIC 解释器 /// Event-driven Model–Render–Game loop // 事件驱动的模型-渲染-游戏循环 /// ## ⭐ Killer App: MDPT // ⭐ 杀手级应用：MDPT /// A Markdown-first presentation toolkit with a self-rendered TUI(no terminal emulator needed). // 一个以 Markdown 为先的演示工具包，带有自渲染的 TUI（无需终端模拟器）。 /// MDPT demonstrates RustPixel's unique capability: rendering a full-featured terminal UI in a native GPU window, completely independent of any terminal emulator. // MDPT 展示了 RustPixel 的独特功能：在原生 GPU 窗口中渲染功能齐全的终端 UI，完全独立于任何终端模拟器。 /// (: cargo pixel r mdpt g -r # (native) WGPU mode) (: cargo pixel r mdpt t -r # (native) Term mode) (: cargo pixel r mdpt w -r # (browser) Web mode)"
[demo/ghio]: https://zipxing.github.io/rust_pixel/ "🎮 Rust Pixel Online Demo // 🎮 Rust Pixel 在线演示"
[cli.cargo/crates]: https://crates.io/crates/rust_pixel "(: cargo install -- rust_pixel) (Apache-2.0) (25K SLoC) (543 KiB) 2d pixel-art game engine & rapid prototype tools support terminal, wgpu and web... // 2D 像素艺术游戏引擎和快速原型工具支持终端、GPU 和 Web …… (src: gh:zipxing/rust_pixel.git)"
