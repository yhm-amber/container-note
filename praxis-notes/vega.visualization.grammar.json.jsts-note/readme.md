
[app.web/ghio]: https://vega.github.io/editor/ "(用特定规范的 Json 绘制图表。)"
[examples:bar-chart/app.web/ghio]: https://vega.github.io/editor/#/examples/vega/bar-chart "(例子: 用特定规范的 Json 绘制的 bar 图表。)"
[site/ghio]: https://vega.github.io/vega/ "Vega – A Visualization Grammar // Vega – 一个可视化语法 /// Vega is a visualization grammar, a declarative language for creating, saving, and sharing interactive visualization designs. With Vega, you can describe the visual appearance and interactive behavior of a visualization in a JSON format, and generate web-based views using Canvas or SVG. // Vega 是一种可视化语法，一种用于创建、保存和共享交互式可视化设计的声明式语言。使用 Vega，您可以在 JSON 格式中描述可视化的外观和交互行为，并使用 Canvas 或 SVG 生成基于网页的视图。 /// Vega provides basic building blocks for a wide variety of visualization designs: data loading and transformation, scales, map projections, axes, legends, and graphical marks such as rectangles, lines, plotting symbols, etc. Interaction techniques can be specified using reactive signals that dynamically modify a visualization in response to input event streams. // Vega 为各种可视化设计提供了基本的构建模块：数据加载和转换、坐标轴、地图投影、坐标轴、图例以及矩形、线条、绘图符号等图形标记。交互技术可以使用响应式信号指定，这些信号会根据输入事件流动态地修改可视化效果。 /// A Vega specification defines an interactive visualization in a JSON format. Specifications are parsed by Vega’s JavaScript runtime to generate both static images or interactive web-based views. Vega provides a convenient representation for computational generation of visualizations, and can serve as a foundation for new APIs and visual analysis tools. // 一个 Vega 规范以 JSON 格式定义了一个交互式可视化。规范由 Vega 的 JavaScript 运行时解析，以生成静态图像或交互式基于网络的视图。Vega 为可视化计算生成提供了便捷的表示形式，并可作为新 API 和可视化分析工具的基础。"
[src/gh]: https://github.com/vega/vega.git "(BSD-3-Clause) (Languages: JavaScript 74.3%, TypeScript 24.4%, HTML 1.1%, Shell 0.2%) A visualization grammar. // 一种可视化语法。 /// Vega is a visualization grammar, a declarative format for creating, saving, and sharing interactive visualization designs. With Vega you can describe data visualizations in a JSON format, and generate interactive views using HTML5 Canvas or SVG. // Vega 是一种可视化语法，一种用于创建、保存和共享交互式可视化设计的声明式格式。使用 Vega，您可以在 JSON 格式中描述数据可视化，并使用 HTML5 Canvas 或 SVG 生成交互式视图。"
