[src/gh]: https://github.com/Hexilee/tifs.git "(Apache-2.0) (Languages: Rust 98.6%, Other 1.4%) A distributed POSIX filesystem based on TiKV, with partition tolerance and strict consistency. /// 基于 TiKV 的分布式 POSIX 文件系统，具有分区容错性和严格一致性。 /// You need a tikv cluster to run tifs. tiup (. gh: pingcap/tiup.git) is convenient to deploy one, just install it and run tiup playground. // 运行 tifs 需要一个 tikv 集群。tiup 可以很方便地部署一个 tikv 集群，只需安装它并运行 tiup playground 即可。 /// CLI: (: mkdir <mount point> ;: mount -t tifs #{ -o tls=<tls config file> # for TLS # default should be in: ~/.tifs/tls.toml }# -- tifs:<pd endpoints> <mount point>)"
[oci/dockerhub]: https://hub.docker.com/r/hexilee/tifs "(: docker pull -- docker.io/hexilee/tifs) (: docker run -d --device /dev/fuse --cap-add SYS_ADMIN -v <mount point>:/mnt:shared #{ -v <cert dir>:/root/.tifs/tls # for TLS }# -- docker.io/hexilee/tifs --mount-point /mnt --pd-endpoints <endpoints>)"

