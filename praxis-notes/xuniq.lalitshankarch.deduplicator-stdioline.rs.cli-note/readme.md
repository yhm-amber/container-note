[src/gh]: https://github.com/lalitshankarch/xuniq.git "(MIT) (Languages: Rust 100.0%) xuniq is a blazing-fast utility to remove duplicate lines from input // xuniq 是一个超快的工具，用于从输入中删除重复的行 /// xuniq: superfast line deduplicator // xuniq: 超快行去重工具 /// xuniq is a high-performance and stable line deduplicator. // xuniq 是一个高性能且稳定的行去重工具。 /// Unlike the standard uniq utility, xuniq does not require your input to be sorted. // 与标准的 uniq 工具不同， xuniq 不需要你的输入已排序。 /// (: xuniq < data.txt > /dev/null) /// Commands to reproduce // 用于重现的命令 ///: (: { : Duplicated lines && seq 1 1250000 | awk '{print \"dup_\"$1; print \"dup_\"$1}' && : Unique lines && seq 1250001 3750000 | awk '{print \"uniq_\"$1}' && :; } | shuf > data.txt; hyperfine --shell bash 'xuniq < data.txt > /dev/null' 'ripuniq < data.txt > /dev/null' 'huniq < data.txt > /dev/null' 'sort data.txt | uniq > /dev/null' 'sort -u data.txt > /dev/null' 'hist < data.txt > /dev/null' 'awk '\''!seen[$0]++'\'' data.txt > /dev/null' 'perl -ne '\''print if !$seen{$_}++'\'' data.txt > /dev/null' 'runiq < data.txt > /dev/null' --warmup 3 --export-markdown bench.md)"
[cli.cargo/crates]: https://crates.io/crates/xuniq "(: cargo install -- xuniq) (MIT) (84 SLoC) (46.5 KiB) xuniq is a blazing-fast utility to remove duplicate lines from input. // xuniq 是一个用于从输入中删除重复行的极速工具。 (src: gh:lalitshankarch/xuniq.git)"
[knowsby]: https://rustcc.cn/article?id=41dc1fc3-e6de-4dff-9166-75935cc38627 "xuniq: 超快的行去重命令行工具 /// xuniq 是一个超快的针对行内容去重的工具，并且，和 uniq 相比，它不需要先排序，可以直接对未排序的内容去重。"
