[src/gh]: https://github.com/crynta/terax-ai.git "(Apache-2.0) (Languages: TypeScript 79.7%, Rust 17.9%, CSS 1.1%, Shell 0.5%, PowerShell 0.2%, JavaScript 0.2%, Other 0.4%) Lightweight (7MB) Terminal-first AI-native dev workspace // 轻量级（7MB）终端优先的 AI 原生开发工作区 /// Terax // 特拉克斯 /// Lightweight Terminal-first AI-native dev workspace. // 轻量级、终端优先、AI 原生支持的开发工作空间。 /// Terax is a lightweight open-source terminal (ADE) built on Tauri 2 + Rust and React 19. A native PTY backend with a WebGL renderer, an agentic AI side-panel that runs against your own keys or fully local models, plus a code editor, file explorer, source control with a git graph, and a web preview pane built in. About 7-8 MB on disk. No telemetry. No account. // Terax 是一款轻量级的开源终端（ADE），基于 Tauri 2 + Rust 和 React 19 构建。它拥有原生 PTY 后端和 WebGL 渲染器，一个可基于用户自定义按键或完全本地模型运行的智能 AI 侧面板，以及内置的代码编辑器、文件资源管理器、带有 Git 图的源代码控制系统和 Web 预览窗格。磁盘占用空间仅约 7-8 MB。不收集遥测数据。无需账户。 /// ## Features // 特征 /// ### Terminal // 终端 /// - xterm.js with WebGL renderer, multi-tab with background streaming // 使用 WebGL 渲染器的 xterm.js，支持多标签页和后台流式传输 /// - GPU-accelerated block-based terminal with editor-like command input // 基于 GPU 加速的模块化终端，具有类似编辑器的命令输入方式 /// - Native PTY backend via portable-pty (zsh, bash, pwsh, fish, cmd) // 通过 portable-pty 实现原生 PTY 后端（支持 zsh、bash、pwsh、fish、cmd） /// - Split panels (horizontal and vertical) // 分体式面板（水平和垂直） /// - Inline search, link detection, true-color // 内联搜索、链接检测、真彩色 /// - Per-tab workspace environments on Windows (Local, or any installed WSL distro) // Windows 系统上的每个标签页工作区环境（本地或任何已安装的 WSL 发行版） /// ### Code editor // 代码编辑器 /// - CodeMirror 6 (supports all popular languages - TS/JS, Rust, Python, Go, C/C++, Java, HTML/CSS, JSON, Markdown, etc.) // CodeMirror 6（支持所有流行语言——TS/JS、Rust、Python、Go、C/C++、Java、HTML/CSS、JSON、Markdown 等） /// - Inline AI autocomplete with local model support // 支持本地模型的内联 AI 自动完成 /// - AI edit diffs, accept or reject hunk by hunk // AI 编辑差异，逐块接受或拒绝。 /// - Vim mode // Vim 模式 /// - Ten built-in editor themes: Atom One, Aura, Copilot, GitHub Dark / Light, Gruvbox Dark, Nord, Tokyo Night, Xcode Dark / Light // 十款内置编辑器主题：Atom One、Aura、Copilot、GitHub Dark / Light、Gruvbox Dark、Nord、Tokyo Night、Xcode Dark / Light /// ### Source control // 源码掌控 /// - Stage / unstage hunks, commit (Cmd+Enter / Ctrl+Enter), push with upstream awareness // 暂存/取消暂存代码块，提交（Cmd+Enter / Ctrl+Enter），推送并感知上游。 /// - Branch display including detached HEAD state // 分支显示，包括分离的 HEAD 状态 /// - Git history pane with a real commit graph (lane rendering for merges and branches) // 带有真实提交图的 Git 历史记录面板（合并和分支以泳道形式呈现） /// - Commit search and filter, click through to the remote commit page // 提交搜索和筛选，点击进入远程提交页面 /// ### File explorer // 文件资源管理器 /// - Catppuccin icon theme // 猫咪图标主题 /// - Fuzzy search, keyboard navigation, inline rename, context actions // 模糊搜索、键盘导航、内联重命名、上下文操作 /// - Attach files and selections directly to the AI side-panel // 将文件和选区直接附加到 AI 侧面板 /// ### Web preview // 网页预览 /// - Auto-detects local dev servers and opens them in a preview tab // 自动检测本地开发服务器并在预览选项卡中打开它们 /// - External URL preview via a native child webview // 通过原生子 WebView 预览外部 URL /// ### Themes and customization // 主题和定制 /// - Custom themes built in-app, switch between bundled presets and your own // 应用内内置自定义主题，可在预设主题和自定义主题之间切换 /// - Create your own themes, share them or import from the community // 创建您自己的主题，分享它们，或从社区导入。 /// - Background images with adjustable opacity and blur // 背景图片的透明度和模糊度可调 /// - Editor theme is independent from the app theme // 编辑器主题独立于应用程序主题 /// ### AI // 人工智能 /// - BYOK providers: OpenAI, Anthropic, Google (Gemini), Groq, xAI (Grok), Cerebras, OpenRouter, DeepSeek, Mistral, plus any OpenAI-compatible endpoint // BYOK 提供商： OpenAI、Anthropic、Google (Gemini)、Groq、xAI (Grok)、Cerebras、OpenRouter、DeepSeek、Mistral，以及任何与 OpenAI 兼容的端点 /// - Local / offline: LM Studio, MLX, Ollama // 本地/离线： LM Studio、MLX、Ollama /// - Agentic workflow: plans, sub-agents, project memory via TERAX.md, file read / write / edit / multi-edit / grep / glob, bash with approval gating, background processes // 智能体工作流： 计划、子智能体、通过 TERAX.md 进行项目记忆、文件读/写/编辑/多编辑/grep/glob 检索、带审批门控的 bash、后台进程 /// - Composer: snippets via #handle, files via @path, slash commands, voice input, attach-to-agent from explorer or selection // 编辑器： 通过 #handle 代码片段，通过 @path 导入文件，支持斜杠命令、语音输入，以及从资源管理器或选择项附加到代理。 /// - Custom agents with their own system prompt and tool subset // 具有自身系统提示和工具子集的自定义代理 /// - Plan mode for multi-step work, generates and confirms before doing // 计划模式适用于多步骤工作，会在执行前生成并确认。"
[site/app]: https://terax.app/ "A lightweight AI terminal with a built-in editor, AI agents, and live web preview. 7 MB on disk. 300 ms cold start. BYOK or fully local. // 一款轻量级 AI 终端，内置编辑器、AI 代理和实时网页预览功能。磁盘占用 7 MB，冷启动仅需 300 毫秒。支持自带密钥 (BYOK) 或完全本地部署。 /// - App size // 应用大小 ///: < 10MB // 小于 10MB /// - Cold start // 冷启动 ///: ~300ms // ~300毫秒 /// - Ever // 永远 ///: No telemetry // 无遥测数据 /// - Open source // 开源 ///: Apache-2.0"
[site.src/gh]: https://github.com/crynta/Terax-website.git "(Apache-2.0) (Languages: TypeScript 75.2%, MDX 21.5%, CSS 2.8%, JavaScript 0.5%) The website and landing page for Terax // Terax 的网站和首页"
[intro.0/youtube]: https://youtu.be/kykgXa7sm1g "[clears throat] >> This is Terex, my open-source AI terminal. I spent the last 3 weeks building it, working 12 hours a day. But the most ridiculous part about it is this. Terex is 7 MB. Warp, which if you haven't heard of it, is basically the most popular AI terminal right now, is over 400, more than 50-something times bigger. // [清嗓子] >> 这是 Terex，我的开源 AI 终端。我花了过去三周的时间开发它，每天工作 12 小时。但它最不可思议的地方在于：Terex 只有 7 MB。而 Warp，如果你还没听说过的话，它基本上是目前最流行的 AI 终端，它超过了 400 MB、比前者足足大了有 50 多倍。 /// But why did I even build this? There's Claude Code, there's Warp, regular terminals, a dozen AI terminal experiments. So what's the gap? Basically, Claude Code is a coding agent, not really a terminal. I spend most of my day in a terminal anyway, and I wanted the AI to just live there with me, not in a separate app I have to switch to. So closer in spirit to Warp, but actually lighter, cleaner, and just more terminal feeling. Most of the AI terminals I've tried felt heavy, both visually and technically. Like the AI was bolted on, not built in. I tried most of them, took what I liked, dropped what felt bloated, and built the one I actually want to use every day. And half of why I built it is just I love this stuff. Systems programming, AI engineering, and modern app development. This project is all three in one. Okay, so let's walk through it. The layout is designed to keep you in the flow. On the left, we have the file explorer with fast search, keyboard navigation, and capybara icons, for sure. // 但我为什么要建造这个？这里有克劳德代码，有 Warp，有普通终端，还有十几个 AI 终端实验。那么差距在哪里？基本上，克劳德代码是一个编码代理，而不是真正的终端。我一天的大部分时间都待在终端里，我希望人工智能能直接与我一起运行，而不是放在一个单独的应用程序里，每次都要切换到那个程序。所以，它在理念上更接近 Warp，但实际上更轻便、更简洁，也更有终端的感觉。我试用过的大多数人工智能终端都感觉很笨重，无论是从视觉还是技术层面来说。就像人工智能的部分是后加装的、而非原本内置。我试用了大多数方案，取其精华，去其糟粕，最终打造出了我真正想每天使用的那个。我开发它的一半原因就是我热爱这些东西：系统编程，人工智能工程学和现代应用程序开发。这个项目将这三者融为一体。好的，让我们一起来看看。布局设计旨在让您保持流畅的操作体验。左侧是文件资源管理器，当然，它支持快速搜索、键盘导航和水豚图标。 /// Up top, you've got your tabs, where you can switch between terminal, code editor, and web preview tabs. Which one is auto detects when the local dev server comes up, so you don't have to keep jumping back to your browser. Now, if I open a file, it doesn't just show text. // 顶部是标签页，你可以在这里切换终端、代码编辑器和网页预览标签页。本地开发服务器启动时会自动检测切换到哪个标签页，所以你不用频繁地跳转到浏览器。现在，如果我打开一个文件，它不会直接显示文本。 /// Terex has a built-in code editor with Vim mode, pre-built themes, and real-time fast AI auto-completion. And here's the part I care about most, the AI panel. I didn't want a separate AI chat eating half my screen. So I took bits I liked from Warp, bits from Notion, and ended up with this. // Terex 内置了一个代码编辑器，支持 Vim 模式、预置主题和实时快速的 AI 自动补全功能。而我最关心的部分是 AI 面板。我不想让一个单独的 AI 聊天窗口占据我半个屏幕。所以我借鉴了 Warp 和 Notion 的一些优点，最终做了这个。 /// A small input panel that lives at the bottom and mini window to see the details, which you can pop it out when you want, hide it when you don't. Underneath, it's a powerful agent with sub-agents, project memory, tasks, skills, snippets, and much more. // 底部有一个小型输入面板，还有一个用于查看详细信息的迷你窗口，可以随时弹出或隐藏。其下是一个功能强大的代理，包含子代理、项目记忆、任务、技能、代码片段等等，还有更多。 /// Voice input when you don't feel like typing. Supports all the major providers and local models through LM Studio. By the way, Gemma 4 from Google is the one I'd actually recommend trying as a local model. Here, I gave to the agent a command to initialize the tarics.md file. And now we can ask to make some changes. It reads what it needs, plans, proposes changes directly in the editor, and then you can review and approve it. // 不想打字时，可以使用语音输入。支持所有主流供应商和通过 LM Studio 提供的本地模型。顺便一提，我个人推荐尝试使用 Google 的 Gemma 4 作为本地模型。在这里，我给代理发送了一个命令以初始化 tarics.md 文件。现在我们可以请求进行一些更改。它会读取所需内容，进行规划，并在编辑器中直接提出更改建议，然后您可以审核并批准。 /// Here's the tech stack behind it. It's built with Rust on the back end and Tauri instead of Electron for the show. By using the native system web view instead of bundling a whole Chromium instance, we basically cut 90% of the usual bloat right there. And on the front end, I'm using React with its modern ecosystem. The terminal itself runs on xterm.js with the WebGL renderer. Same library VS Code uses for its terminal, by the way. But for the code editor, I went with CodeMirror 6. Unlike Monaco, the editor behind VS Code, which by itself can already be larger than 7 MB, CodeMirror is modular and more than 10 times lighter. And there's a lot of other stuff under the hood, which you can check out on the GitHub. The final result is a binary that's practically invisible to your system. You can download the app from the website or directly from GitHub repo. All links in the description. // 以下是其背后的技术栈。后端使用 Rust 构建，而前端则使用 Tauri 而非 Electron 。通过使用原生系统 Web 视图而不是打包整个 Chromium 实例，我们基本上可以省去 90% 的额外开销。前端方面，我使用了 React 及其现代化的生态系统。终端本身则运行在 xterm.js 上，并使用 WebGL 渲染器。顺便一提，它和 VS Code 的终端使用的是同一个库。不过代码编辑器我选择了 CodeMirror 6。与 VS Code 背后的编辑器 Monaco 不同（Monaco 本身就可能超过 7 MB），CodeMirror 采用模块化设计，体积小了十倍以上。而且它还有一些其他底层和细节，你可以在 GitHub 上查看。最终生成的二进制文件几乎不会影响你的系统。你可以从网站或直接从 GitHub 代码库下载该应用。所有链接都在描述中。 /// Project is fully open source with Apache 2.0 license. You can read it, contribute, and even ship your own version. Right now, it runs on macOS and Linux. Supports all the major distros and Windows version is coming soon as well. I'm going to keep working on it pretty heavily, mostly because I'm going to be using it every day. It goes on my main Mac and goes on my Arch laptop. Yeah. Issues, PRs, roasting in the issues, all welcome. // 该项目完全开源，并已获得 Apache 2.0 许可。您可以阅读、贡献代码，甚至发布您自己的版本。目前，它可在 macOS 和 Linux 上运行。支持所有主流发行版，Windows 版本也即将推出。我将继续投入大量精力进行开发，主要是因为我每天都在用它，它在我的主力 Mac 和 Arch 笔记本电脑上都有所部署。是的。欢迎提交问题、PR，甚至在问题讨论区里吐槽。 /// If you want a deeper tech video, like a full breakdown of how this is actually built, or in general you have any questions or ideas you want to see in the next videos, tell me in the comments. Feedbacks, feature requests, all of it is super helpful. If you want to support the project, star the repo and subscribe. The channel is new and it will motivate me to keep working on it and making this kind of videos. Thanks for watching this. See you in the next one. Actually, I think I'll let Terex finish the video for me. Say goodbye message to the viewers. // 如果你想看更深入的技术视频，比如它的完整构建过程解析，或者你有任何问题或想法想在问题讨论区看到，欢迎在评论区告诉我你对下一期视频的想法。反馈、功能建议等等都非常有用。如果你想支持这个项目，请给仓库点赞并订阅。频道刚起步，你的支持会激励我继续创作，制作更多这类视频。感谢观看！下次再见。其实，我想还是让特雷克斯帮我把视频做完：向观众们道别吧！"
