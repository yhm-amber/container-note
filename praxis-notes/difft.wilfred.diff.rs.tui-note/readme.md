[src/gh]: https://github.com/Wilfred/difftastic.git "(MIT) (Languages: Rust 80.0%, CSS 8.1%, HTML 6.9%, JavaScript 4.2%, Roff 0.7%, Just 0.1%) "
[pkg.x/x-cmd]: https://x-cmd.com/install/difft "(: x install -- difft) (: x env use difft)"
[pkg.cargo/crates]: https://crates.io/crates/difftastic "(: cargo install -- difftastic)"
[pkg.brew/homebrew]: https://formulae.brew.sh/formula/difftastic "(: brew install -- difftastic)"
[1min/x-cmd]: https://x-cmd.com/1min/difft "Compare the syntax diffs of files // 比较文件的语法差异 /// - Here we compare two files written in rust, highlighting the added, removed, and modified contents // 在这里，我们比较两个用 rust 编写的文件，突出显示新增、删除和修改的内容 /// - There are over 30 languages supported by difft , including java, go, c, Python . You can view the detailed instructions by using --list-languages. // difft 支持超过 30 种语言，包括 java、go、c、Python。您可以通过使用 --list-languages 查看详细说明。 /// - Note that if you give a language that difft doesn't recognize, difft will fall back to regular line-based text diff with word highlighting. // 请注意，如果您提供一个 difft 无法识别的语言，difft 将回退到常规的基于行的文本比较，并突出显示单词。 /// Integrate with git commands // 与 git 命令集成 /// - This tutorial takes the approach of setting the difft environment variable to use difft in git commands. // 本教程采用设置 difft 环境变量的方法，在 git 命令中使用 difft。 /// - If you want to use difft as the default in git commands, you can configure it with the git config command. // 如果你想在 git 命令中把 difft 设为默认，可以用 git config 命令来配置。 /// - For example, git config --global diff.external difft changes git's default diff configuration. // 例如， git config --global diff.external difft 会改变 git 的默认 diff 配置。"
[site]: https://difftastic.wilfred.me.uk/
