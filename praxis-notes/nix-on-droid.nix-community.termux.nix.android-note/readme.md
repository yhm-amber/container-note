[apk/f-droid]: https://f-droid.org/packages/com.termux.nix/ "(3.2 MiB) (MIT) Nix package manager for Android // 用于 Android 的 Nix 包管理器 /// Nix-on-Droid brings Nix package manager of NixOS fame to your mobile device. If you're not after its advanced features, you might still be interested in tapping into nixpkgs, a vast collection of precompiled software. // Nix-on-Droid 将 NixOS 著名的 Nix 包管理器带到您的移动设备上。如果您不追求其高级功能，您可能仍然会对 nixpkgs 感兴趣，这是一个庞大的预编译软件集合。 /// At first start the app will download Nix repackaged for Android, leaving you with a barebones system afterwards. If you are an experienced Nix user, you know what to do next. If you are in doubt, follow the on-screen instructions, to install more of the familiar Linux software and declaratively manage your environment. // 首次启动应用程序时，将下载针对 Android 重打包的 Nix，之后您将得到一个精简的系统。如果您是经验丰富的 Nix 用户，您知道接下来该做什么。如果您不确定，请按照屏幕上的说明操作，以安装更多熟悉的 Linux 软件并声明式地管理您的环境。 /// NOTE: this software is alpha-quality. Users that value stability and Android integration over features and package selection might better be served with Termux as of now. // 注意：此软件为 alpha 版本。目前，如果您更看重稳定性和 Android 集成而不是功能和软件包选择，Termux 可能更适合您。 /// Nix-on-Droid uses a fork of Termux application as a terminal emulator. // Nix-on-Droid 使用 Termux 应用的分支作为终端模拟器。 (src: gh:nix-community/nix-on-droid.git)"
[src/gh]: https://github.com/nix-community/nix-on-droid.git "(MIT) (Languages: Nix 67.9%, Shell 19.8%, Python 12.3%) Nix-enabled environment for your Android device. [maintainers=@t184256,@Gerschtli] // 为您的 Android 设备提供 Nix 支持的环境。 /// Nix package manager on Android, in a single-click installable package. This is not full NixOS running inside Android, but you get easy access to nixpkgs' vast collection of (precompiled!) software and the best package manager under the sun. It's prototype-grade quality as of now, but hey, it works! // Android 上的 Nix 包管理器，以单点击安装包的形式提供。这不是在 Android 内部运行的完整 NixOS，但你可以轻松访问 nixpkgs 庞大的（预编译！）软件库，以及太阳下最好的包管理器。目前它的质量还处于原型阶段，但嘿，它确实能工作！ /// It does not require root, user namespaces support or disabling SELinux, but it relies on proot and other hacks instead. It uses a fork of Termux-the-terminal-emulator app, but has no relation to Termux-the-distro. Please do not pester Termux folks about Nix-on-Droid. // 它不需要 root 权限、用户命名空间支持或禁用 SELinux，但它依赖于 proot 和其他黑客技术。它使用的是 Termux-the-terminal-emulator 应用的分支版本，但与 Termux-the-distro 无关。请不要去打扰 Termux 社区关于 Nix-on-Droid 的事情。 /// This repository contains: // 这个仓库包含： /// 1. Nix expressions that generate a bootstrap zipball, which is then used to install Nix package manager on Android along with the nix-on-droid executable. // 生成用于在 Android 上安装 Nix 包管理器（包括 nix-on-droid 可执行文件）的启动 zipball 的 Nix 表达式。 /// 2. A module system for configuring the local Nix-on-Droid installation directly on the device. // 在设备上直接配置本地 Nix-on-Droid 安装的模块系统。 /// It is only tested with aarch64 (64-bit ARM devices). It also used to compile for i686 devices, but the developers don't own any and nobody has reported whether it actually worked or not, so it's no longer built unless a user shows up. Sorry, it would not work on 32-bit ARM devices and it's not an easy feat to pull off. // 仅测试过 aarch64（64 位 ARM 设备）。它也曾用于为 i686 设备编译，但开发者没有这些设备，也没有人报告它是否真的能工作，因此不再构建，除非有用户出现。抱歉，它在 32 位 ARM 设备上无法工作，而且实现起来并不容易。"
[site/info]: https://nix-on-droid.unboiled.info/
[knowsby]: https://github.com/pythops/tenere.git "... /// ### 📱 Mobile (nix-on-droid) // 📱 移动端 (nix-on-droid) /// Tenere works on Android via nix-on-droid (demo (web: github.com/user-attachments/assets/c06e5650-0b5d-4f0a-816d-a2c1bd88774a)). // Tenere 可通过 nix-on-droid 在 Android 上运行。 /// To set up (tutorial): // 设置方法 (教程)： /// 1. Install nix-on-droid from F-Droid // 从 F-Droid 安装 nix-on-droid /// 2. Add tenere to your packages in `.config/nixpkgs/nix-on-droid.nix`: // 在 `.config/nixpkgs/nix-on-droid.nix` 中的包中添加 tenere： /// 3. Run nix-on-droid switch // 运行 nix-on-droid switch /// 4. Create your config at `.config/tenere/config.toml` // 在 `.config/tenere/config.toml` 创建您的配置"
