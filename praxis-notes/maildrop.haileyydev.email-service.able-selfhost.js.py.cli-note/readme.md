[src/gh]: https://github.com/haileyydev/maildrop.git "(GPL-3.0) (Languages: JavaScript 33.0%, Python 28.4%, CSS 27.6%, HTML 10.4%, Dockerfile 0.6%) ✉️ A simple self hostable email service // ✉️ 一个简单的可自托管邮件服务 /// Maildrop is a self hostable and easy to use email service that allows you to receive emails on any address on your domain. // Maildrop 是一个易于使用且可自托管的邮件服务，它允许您在您域名的任何地址上接收邮件。 /// It is perfect for: // 它非常适合： /// - People who want to easily use multiple email addresses. // 想要轻松使用多个邮箱地址的人。 /// - Signing up for services without without using your main email address. // 在不使用您的主要电子邮件地址的情况下注册服务。 /// - Easily creating multiple accounts on websites. // 轻松在多个网站上创建多个账户。 /// Features // 功能 /// - Random email generation // 随机电子邮件生成 /// - Use custom emails // 使用自定义邮箱 /// - Support for password protected inboxes // 支持密码保护的收件箱 /// - Clean UI // 简洁的界面 /// - Easy setup // 易于设置 /// - Automatic inbox clearing // 自动清空收件箱 /// - (Optional) Sending Emails // （可选）发送邮件 /// (: cd ./maildrop && python -m venv venv && source venv/bin/activate && pip install -r requirements.txt ;: python app.py # Maildrop will be running on port 5000 and the smtp server on port 25. // Maildrop 将在端口 5000 上运行，SMTP 服务器在端口 25 上。 # The application must be run as root for the SMTP server to work // 应用程序必须以 root 用户运行，SMTP 服务器才能正常工作) (: sudo -- docker run -d --restart unless-stopped --name maildrop -p 5000:5000 -p 25:25 -e DOMAIN='yourdomain.com' -- docker.io/haileyydev/maildrop:latest # with docker) /// Follow this guide to set up receiving emails on your domain. // 按照此指南在您的域名上设置接收邮件。 /// - Ensure port 25 is open // 确保端口 25 是开放的 /// - This is the port the smtp server uses. Some ISPs block this so you may need to use a tunnel or host maildrop in the cloud. // 这是 SMTP 服务器使用的端口。一些互联网服务提供商可能会封锁此端口，因此您可能需要使用隧道或在云端托管 maildrop。 /// - Create an A record in your dns settings // 在你的 DNS 设置中创建一个 A 记录 /// - Create this on the domain you want the maildrop running on. The ip address should be pointed to the public ip address of the server running maildrop. // 在你要运行 maildrop 的域上创建这个记录。IP 地址应该指向运行 maildrop 的服务器的公网 IP 地址。 /// - Create an MX record in your dns settings // 在你的 DNS 设置中创建一个 MX 记录 /// - Create this on the domain you want emails to be sent to. It should be pointed to the domain you created your A record on. // 在您希望邮件发送到的域名上创建此记录。它应指向您创建 A 记录的域名。 /// - Edit .env and change the domain to your domain // 编辑 .env 并将域名更改为您的域名 /// - Edit the .env file and set DOMAIN to the domain you are receiving emails on (so the domain you created the MX record on). This is used for email address generation. If you are using docker, edit the DOMAIN environment variable directly. // 编辑 .env 文件并将 DOMAIN 设置为您接收邮件的域名（即您创建 MX 记录的域名）。这用于生成电子邮件地址。如果您使用的是 Docker，请直接编辑 DOMAIN 环境变量。"
[oci/dockerhub]: https://hub.docker.com/r/haileyydev/maildrop "(: docker pull -- docker.io/haileyydev/maildrop)"

