[src/gh]: https://github.com/ThatXliner/rust-but-lisp.git "(MIT) (Languages: Rust 100.0%) Rust but LISP // Rust 但 LISP /// Hello, Hacker News. You're not wrong. This is a weekend project, not a production compiler — some Rust syntax is missing (turbofish is fixed now, lifetime bounds are on the list). The point isn't completeness; it's exploring what happens when you bolt Lisp macros onto Rust semantics. If that sounds interesting, read on. If you're looking for something to be mad about, the issue tracker is open. // 你好，Hacker News。 你说得没错。这只是个周末项目，并非生产环境的编译器——一些 Rust 语法还不完善（turbofish 的问题已经修复，生命周期限制也已列入待办事项）。重点不在于完整性，而在于探索将 Lisp 宏与 Rust 语义结合起来会发生什么。如果你对此感兴趣，请继续阅读。如果你想找点东西发泄不满， 可以去 issue tracker 看看 。 /// Rust semantics in LISP syntax. Write s-expressions, output Rust source: (s-expr → .rs → binary). // 用 LISP 语法实现 Rust 语义。编写 s 表达式，输出 Rust 源代码： (s-expr → .rs → binary) 。 /// Ownership, borrowing, lifetimes, generics, traits, pattern matching — all expressed as s-expressions. rustc still does type checking, borrow checking, and optimization. rlisp just handles the syntax. // 所有权 rustc 借用、生命周期、泛型、特性、模式匹配——所有这些都用 S 表达式表示。rustc 仍然负责类型检查、借用检查和优化。rlisp 只负责处理语法。 /// Macros // 宏 /// rlisp macros are compile-time s-expression transformers — no proc_macro crate, no token streaming, no syn/quote. A macro is just a function from s-expressions to s-expressions. // rlisp 宏是编译时 S 表达式转换器——无需 proc_macro crate，无需标记流，也无需 syn/quote。宏只是一个从 S 表达式到 S 表达式的函数。"
