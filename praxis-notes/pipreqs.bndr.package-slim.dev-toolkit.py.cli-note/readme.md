[src/gh]: https://github.com/bndr/pipreqs.git "(Apache-2.0) (Languages: Python 88.3%, Jupyter Notebook 8.6%, Makefile 3.1%) pipreqs - Generate pip requirements.txt file based on imports of any project. Looking for maintainers to move this project forward. // pipreqs - 根据任何项目的导入情况生成 pip requirements.txt 文件。现寻找维护者推进此项目的发展。 /// Why not pip freeze? // 为什么不冻结管道？ /// - pip freeze only saves the packages that are installed with pip install in your environment. // pip freeze 只会保存您环境中使用 pip install 安装的软件包。 /// - pip freeze saves all packages in the environment including those that you don't use in your current project (if you don't have virtualenv). // pip freeze 会保存环境中的所有包，包括当前项目中未使用的包（如果您没有 virtualenv ）。 /// - and sometimes you just need to create requirements.txt for a new project without installing modules. // 有时，你只需要为新项目创建 requirements.txt ，而无需安装模块。"
[cli.pip/pypi]: https://pypi.org/project/pipreqs/ "(: pip install -- pipreqs) (License: Apache Software License (Apache-2.0)) (Requires: Python >=3.8.1, <3.13) Pip requirements.txt generator based on imports in project // 基于项目中导入的 Pip requirements.txt 生成器 (src: gh:bndr/pipreqs.git)"


