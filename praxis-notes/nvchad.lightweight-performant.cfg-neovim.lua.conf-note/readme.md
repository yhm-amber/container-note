[core.src/gh]: https://github.com/NvChad/NvChad.git "(GPL-3.0) (Languages: Lua 100.0%) Blazing fast Neovim framework providing solid defaults and a beautiful UI, enhancing your neovim experience. // 提供稳定默认值和美观 UI 的极速 Neovim 框架，提升您的 neovim 体验。 /// What is it? // 这是什么？ /// - NvChad is a neovim config written in lua aiming to provide a base configuration with very beautiful UI and blazing fast startuptime (around 0.02 secs ~ 0.07 secs). We tweak UI plugins such as telescope, nvim-tree etc well to provide an aesthetic UI experience. // NvChad 是一个用 Lua 编写的 neovim 配置，旨在提供一个具有非常美观界面和极快启动时间的基线配置（大约 0.02 秒 ~ 0.07 秒）。我们对 telescope、nvim-tree 等界面插件进行了优化，以提供美观的界面体验。 /// - Lazy loading is done 93% of the time meaning that plugins will not be loaded by default, they will be loaded only when required also at specific commands, events etc. This lowers the startuptime and it was like 0.07~ secs tested on an old pentium machine 1.4ghz + 4gb ram & HDD. // 懒加载在 93% 的时间内生效，这意味着插件不会默认加载，只有在需要时才会加载，或者在特定的命令、事件等情况下加载。这降低了启动时间，在老旧的奔腾 1.4GHz + 4GB 内存和机械硬盘的机器上测试时，启动时间约为 0.07 秒。 /// - NvChad is supposed to be used with its starter config (. gh:nvchad/starter.git), so nvchad main repo ( this repo ) can be imported as a plugin via lazy's import feature and then you can easily use this repo's modules like autocmds etc. // NvChad 应该与其启动配置一起使用，因此 nvchad 主仓库（这个仓库）可以通过 lazy 的导入功能作为插件导入，然后你可以轻松地使用这个仓库的模块，如 autocmds 等。 /// Plugins list // 插件列表 /// - Many beautiful themes, theme toggler by our base46 plugin (. gh:NvChad/base46.git) // 许多美丽的主题，通过我们的 base46 插件进行主题切换 /// - Lightweight & performant ui plugin with NvChad UI (. gh:NvChad/ui.git) It provides statusline modules, tabufline ( tabs + buffer manager) , beautiful cheatsheets, NvChad updater, hide & unhide terminal buffers, theme switcher and much more! // 轻量级且性能优越的 NvChad UI 插件，提供状态行模块、tabufline（标签页+缓冲区管理器）、精美的快速参考指南、NvChad 更新器、隐藏与显示终端缓冲区、主题切换器等功能！ /// - File navigation with nvim-tree.lua (. gh:kyazdani42/nvim-tree.lua.git) // 使用 nvim-tree.lua 进行文件导航 /// - Beautiful and configurable icons with nvim-web-devicons (. gh:kyazdani42/nvim-web-devicons.git) // 使用 nvim-web-devicons 配置美观的图标 /// - Git diffs and more with gitsigns.nvim (. gh:lewis6991/gitsigns.nvim.git) // 使用 gitsigns.nvim 进行 Git 差异比较及其他功能 /// - NeoVim Lsp configuration with nvim-lspconfig (. gh:neovim/nvim-lspconfig.git) and mason.nvim (. gh:williamboman/mason.nvim.git) // NeoVim Lsp 配置使用 nvim-lspconfig 和 mason.nvim /// - Autocompletion with nvim-cmp (. gh:hrsh7th/nvim-cmp.git) // 使用 nvim-cmp 实现自动补全 /// - File searching, previewing text files and more with telescope.nvim (. gh:nvim-telescope/telescope.nvim.git). // 使用 telescope.nvim 进行文件搜索、预览文本文件等。 /// - Syntax highlighting with nvim-treesitter (. gh:nvim-treesitter/nvim-treesitter.git) // 使用 nvim-treesitter 实现语法高亮 /// - Autoclosing braces and html tags with nvim-autopairs (. gh:windwp/nvim-autopairs.git) // 使用 nvim-autopairs 自动闭合括号和 HTML 标签 /// - Indentlines with indent-blankline.nvim (. gh:lukas-reineke/indent-blankline.nvim.git) // 使用 indent-blankline.nvim 进行行缩进 /// - Useful snippets with friendly snippets (. gh:rafamadriz/friendly-snippets.git) + LuaSnip (. gh:L3MON4D3/LuaSnip.git). // 使用 friendly snippets + LuaSnip 提供有用的代码片段 /// - Popup mappings keysheet whichkey.nvim (. gh:folke/which-key.nvim.git) // 使用 whichkey.nvim 的弹出映射键位表"
[ui.src/gh]: https://github.com/NvChad/ui.git "(GPL-3.0) (Languages: Lua 100.0%) Lightweight & high performance UI plugin for nvchad // 轻量级 & 高性能的 nvchad UI 插件 /// This ui plugin is a collection of many UI modules like statusline, tabline, cheatsheet, nvdash and much more! // 这个 UI 插件是一个包含许多 UI 模块的集合，例如状态行、选项卡行、速查表、nvdash 等！"
[starter.src/gh]: https://github.com/NvChad/starter.git "(Unlicense license) (Languages: Lua 100.0%) Starter config for NvChad // NvChad 的 starter 配置 /// This repo is supposed to be used as config by NvChad users! // 这个仓库是供 NvChad 用户使用的配置文件！ /// - The main nvchad repo (NvChad/NvChad) is used as a plugin by this repo. // 这个仓库使用主 NvChad 仓库（NvChad/NvChad）作为一个插件。 /// - So you just import its modules , like require \"nvchad.options\" , require \"nvchad.mappings\" // 所以你只需要导入它的模块，比如 require \"nvchad.options\" , require \"nvchad.mappings\" /// - So you can delete the .git from this repo ( when you clone it locally ) or fork it :) // 所以你可以删除这个仓库的 .git (当你在本地克隆时) 或者 fork 它 :) /// Credits // 致谢 /// - Lazyvim starter (gh: LazyVim/starter.git) as nvchad's starter was inspired by Lazyvim's . It made a lot of things easier! // Lazyvim starter (gh: LazyVim/starter.git) 作为 nvchad 的 starter 受到了 Lazyvim 的启发。它让很多事情变得更简单！"
[site/com]: https://nvchad.com/ "Enhance your Neovim workflow // 增强你的 Neovim 工作流程 /// Blazing fast Neovim config providing solid defaults and a beautiful UI // 提供可靠默认设置和美观 UI 的极速 Neovim 配置 /// - Fast by default // 默认快速 ///: Blazing fast startup time as plugins, modules are lazy loaded wherever possible. // 插件、模块尽可能懒加载，启动速度极快。 /// - Beautiful UI // 美观的界面 ///: NvChad provides a pretty UI while still embracing the CLI. Our UI plugins are well crafted! // NvChad 提供了一个美观的界面，同时仍然拥抱命令行界面。我们的 UI 插件制作精良！ /// - Highly customizable // 高度可定制 ///: Default plugins, UI, configuration can easily be extended, disabled or overridden. // 默认插件、界面和配置可以轻松地扩展、禁用或覆盖。 /// - Update mechanism // 更新机制 ///: NvChad uses lazy.nvim for package manager, which can be used for updating nvchad too. // NvChad 使用 lazy.nvim 作为包管理器，也可以用它来更新 nvchad。 /// - Powered by Lua // 由 Lua 驱动 ///: Configuration is written in Lua which integrates well with the Neovim lua api & lua plugins. // 配置使用 Lua 书写，与 Neovim 的 Lua API 和 Lua 插件集成良好。 /// - Prettiest themes // 最漂亮的主题 ///: 68 inbuilt beautifully ported & custom themes! All the theme colors are overridable too. // 内置 68 个精美移植和定制主题！所有主题颜色都可以覆盖。 /// - Lightweight // 轻量级 ///: The config is around 900 ~ LOC. The codebase is kept as clean as possible // 配置大约有 900 ~ 行代码。代码库尽可能保持干净。 /// - Inbuilt UI Plugins // 内置 UI 插件 ///: NvChad manages its own theme plugin and UI components for statusline, bufferline etc. // NvChad 管理自己的主题插件和状态行、缓冲行等 UI 组件。 /// - Ease of Use // 易用性 ///: NvChad is used as a plugin, on top of which you make your own config! // NvChad 作为插件使用，你可以在其上配置自己的设置！"


