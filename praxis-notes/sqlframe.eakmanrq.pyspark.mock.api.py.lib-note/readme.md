[src/gh]: https://github.com/eakmanrq/sqlframe.git "(MIT) (Languages: Python 99.9%, Makefile 0.1%) Turning PySpark Into a Universal DataFrame API // 将 PySpark 转换为通用 DataFrame API /// SQLFrame implements the PySpark DataFrame API in order to enable running transformation pipelines directly on database engines - no Spark clusters or dependencies required. // SQLFrame 实现了 PySpark DataFrame API，以便能够直接在数据库引擎上运行转换管道——无需 Spark 集群或依赖项。 /// SQLFrame currently supports the following engines: // SQLFrame 目前支持以下引擎： /// - BigQuery /// - Databricks /// - DuckDB /// - Postgres /// - Snowflake /// - Spark /// There is also one engine in development. This engine lacks test coverage and robust documentation, but is available for testing: // 还有一个正在开发的引擎。该引擎缺乏测试覆盖和完善的文档，但可供测试： /// - Redshift /// SQLFrame also has a “Standalone” session that be used to generate SQL without any connection to a database engine. // SQLFrame 还有一个“独立”会话，可用于生成无需连接到任何数据库引擎的 SQL。 /// - Standalone /// SQLFrame is great for: // SQLFrame 非常适合： /// - Users who want a DataFrame API that leverages the full power of their engine to do the processing // 想要利用引擎的全部功能进行处理的用户 /// - Users who want to run PySpark code quickly locally without the overhead of starting a Spark session // 想要快速在本地运行 PySpark 代码，而无需启动 Spark 会话的用户 /// - Users who want a SQL representation of their DataFrame code for debugging or sharing with others // 想要为调试或与他人分享其 DataFrame 代码的用戶 /// - Users who want to run PySpark DataFrame code without the complexity of using Spark for processing // 想要运行 PySpark DataFrame 代码，但又不想使用 Spark 进行处理的用户"
[docs/rdt]: https://sqlframe.readthedocs.io "(: from sqlframe.standalone import StandaloneSession # from pyspark.sql import SparkSession ;: from sqlframe.standalone import functions as F # from pyspark.sql import functions as F ;: from sqlframe.standalone import StandaloneDataFrame # from pyspark.sql.dataframe import DataFrame) (: from sqlframe.spark import SparkSession # from pyspark.sql import SparkSession ;: from sqlframe.spark import functions as F # from pyspark.sql import functions as F ;: from sqlframe.spark import SparkDataFrame # from pyspark.sql.dataframe import DataFrame)"
[lib.pip/pypi]: https://pypi.org/project/sqlframe/ "(: pip install -- sqlframe) (License: MIT License (MIT)) (Requires: Python >=3.9) (Provides-Extra: [bigquery], [databricks], [dev], [docs], [duckdb], [openai], [pandas], [postgres], [redshift], [snowflake], [spark]) Turning PySpark Into a Universal DataFrame API // 将 PySpark 转换为通用 DataFrame API (src: gh:eakmanrq/sqlframe.git)"
[knows_by]: https://github.com/tobymao/sqlglot.git "What happened to sqlglot.dataframe? // sqlglot.dataframe 发生什么事了？ ///: The PySpark dataframe api was moved to a standalone library called SQLFrame in v24. It now allows you to run queries as opposed to just generate SQL. // PySpark 数据帧 API 已在 v24 版本中迁移到一个名为 SQLFrame 的独立库中。现在它允许你运行查询，而不仅仅是生成 SQL。"
