[src/gh]: https://github.com/coder/mux.git "(AGPL-3.0) (Languages: TypeScript 97.9%, Shell 0.6%, JavaScript 0.5%, Python 0.5%, CSS 0.4%, Makefile 0.1%) A desktop app for isolated, parallel agentic development // 一个用于隔离、并行智能体开发的桌面应用程序 /// Mux - Coding Agent Multiplexer // Mux - 编码代理多路复用器 /// Mux is a desktop & browser application for parallel agentic development. It enables developers to plan and execute tasks with multiple AI agents on local or remote compute. // Mux 是一款用于并行智能体开发的桌面和浏览器应用程序。它使开发者能够在本地或远程计算环境中使用多个 AI 智能体来规划和执行任务。 /// ## Features // 特性 /// - Isolated workspaces with central view on git divergence (docs) // 隔离的工作空间，中央视图显示 git 分叉情况（文档） /// - - Local: run directly in your project directory // 本地：直接在您的项目目录中运行 /// - - Worktree: git worktrees on your local machine // 工作树：您本地机器上的 git 工作树 /// - - SSH: remote execution on a server over SSH // SSH：通过 SSH 在服务器上远程执行 /// - Multi-model (sonnet-4-*, grok-*, gpt-5-*, opus-4-*) // 多模型（ sonnet-4-* ， grok-* ， gpt-5-* ， opus-4-* ） /// - - Ollama supported for local LLMs (docs) // Ollama 支持 local LLMs（文档） /// - - OpenRouter supported for long-tail of LLMs (docs) // OpenRouter 支持 LLMs 的长尾（文档） /// - VS Code Extension: Jump into Mux workspaces directly from VS Code (docs) // VS Code 扩展：直接从 VS Code 跳转到 Mux 工作区（文档） /// - Supporting UI and keybinds for efficiently managing a suite of agents // 支持 UI 和快捷键绑定，以高效管理一系列智能体 /// - Rich markdown outputs (mermaid diagrams, LaTeX, etc.) // 丰富的 Markdown 输出（mermaid 图表、LaTeX 等） /// Mux has a custom agent loop but much of the core UX is inspired by Claude Code. You'll find familiar features like Plan/Exec mode, vim inputs, /compact and new ones like opportunistic compaction and mode prompts. // Mux 拥有自定义的智能体循环，但大部分核心用户体验受到了 Claude Code 的启发。您会发现熟悉的特性，如计划/执行模式、vim 输入、 /compact ，以及新的特性，如机会性压缩和模式提示。 /// CLI /// - Run agent tasks (: npx mux run 'Fix the failing tests') /// - Start the server for remote/mobile access (: npx mux server --port 3000)"
[cli.npm/npmjs]: https://npmjs.com/package/mux "(: npm i -g -- mux) (License: AGPL-3.0-only)  (src: gh:coder/mux.git)"
[site/com]: https://mux.coder.com/ "Mux makes it easy to run parallel coding agents, each with its own isolated workspace, right from your browser or desktop. Mux is open source and LLM provider-agnostic. // Mux 可以轻松运行并行编码代理，每个代理都有自己隔离的工作空间，直接从您的浏览器或桌面开始。Mux 是开源的，并且对 LLM 提供商无依赖。"
