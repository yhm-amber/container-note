[src/gh]: https://github.com/Michael-A-Kuykendall/muxide.git "(MIT) (Languages: Rust 99.8%, Shell 0.2%) Pure Rust MP4 muxer — zero deps, no FFmpeg. Feed it encoded frames, get a standards-compliant MP4. H.264, H.265, AV1, AAC, Opus. // 纯 Rust MP4 复用器——零依赖，无需 FFmpeg。输入编码帧，即可获得符合标准的 MP4 文件。支持 H.264、H.265、AV1、AAC、Opus 等编码格式。 /// The last mile from encoder to playable MP4. // 从编码器到可播放 MP4 的最后一公里。 /// Muxide takes correctly-timestamped, already-encoded audio/video frames and produces a standards-compliant MP4 — pure Rust, minimal external dependencies, no FFmpeg. // Muxide 获取带有正确时间戳、已编码的音频/视频帧，并生成符合标准的 MP4 文件—— 纯 Rust 编写，外部依赖项极少，无需 FFmpeg。 /// Muxide solves one job cleanly: // Muxide 能干净利落地解决一个问题 ： /// Take already-encoded frames with correct timestamps → produce a standards-compliant, immediately-playable MP4 → using pure Rust. // 使用纯 Rust 语言 ，获取已编码且带有正确时间戳的帧 → 生成符合标准、可立即播放的 MP4 文件。 /// Nothing more. Nothing less. // 不多不少，仅此而已。 /// ### As a Library /// (: cargo add -- muxide) (: use muxide::api::{MuxerBuilder, VideoCodec} ;: let mut muxer = MuxerBuilder::new(file).video(VideoCodec::H264, 1920, 1080, 30.0)?.build()? ;: muxer.write_video(0.0, &h264_frame, true)? // Write your encoded frames... ;: muxer.finish()? ;: ...) /// ### As a CLI Tool /// (: cargo install -- muxide ;: muxide --help) (: muxide mux --video frames/ --output output.mp4 --width 1920 --height 1080 --fps 30) (: muxide mux --video video.h264 --audio audio.aac --output output.mp4) (: muxide validate --video frames/ --audio audio.aac) (: muxide info input.mp4) /// The CLI tool accepts raw encoded frames from stdin or files and produces MP4 output. // CLI 工具接受来自标准输入或文件的原始编码帧，并生成 MP4 输出。 /// ### Core Invariant // 核心不变量 /// make sure Your Responsibility then you have Muxide's Guarantee // 确保你的责任则 Muxide 就能保证 ///  /// - Y: ✓ Frames are already encoded // ✓ 帧已编码 | M: ✓ Valid ISO-BMFF (MP4) // ✓ 有效的 ISO-BMFF (MP4) /// - Y: ✓ Timestamps are monotonic // ✓ 时间戳是单调的 | M: ✓ Correct sample tables // ✓ 正确的样本表格 /// - Y: ✓ DTS provided for B-frames // ✓ 为 B 帧提供 DTS | M: ✓ Fast-start layout // ✓ 快速启动布局 /// - Y: ✓ Codec headers in keyframes // ✓ 关键帧中的编解码器头 | M: ✓ No post-processing needed // ✓ 无需后期处理 /// If input violates the contract, Muxide fails fast with explicit errors—no silent corruption, no guessing. // 如果输入违反了契约，Muxide 会快速失败并给出明确的错误——不会出现静默的损坏，也不会出现猜测。 /// ### Principle & Implementation /// - 🦀 Pure Rust // 🦀 纯锈 ///: No unsafe, no FFI, no C bindings // 不包含不安全绑定、FFI 绑定或 C 语言绑定。 /// - 📦 Minimal deps // 📦 最低押金 ///: Only essential Rust crates — no external binaries // 仅包含必要的 Rust crate，不包含外部二进制文件。 /// - 🧵 Thread-safe // 🧵 螺纹安全 ///: Send + Sync when writer is // 当作者在时 Send + Sync /// - ✅ Well-tested // ✅ 经过充分测试 ///: Unit, integration, property tests // 单元测试、积分测试、性质测试 /// - 📜 MIT license // 📜 MIT 许可证 ///: No GPL, no copyleft concerns // 没有 GPL 协议，也没有版权共享问题 /// - 🚨 Developer-friendly // 🚨 对开发者友好 ///: Exceptional error messages make debugging 10x faster // 异常错误信息使调试速度提升 10 倍 /// Note: no_std is not supported. Muxide requires std::io::Write. // 注意： 不支持 no_std 需要 std::io::Write 。"
[cli.cargo/crates]: https://crates.io/crates/muxide "(: cargo install -- muxide # for cli use) (: cargo add -- muxide # for lib use) (MIT) (6.8K SLoC) (1,009 KiB) Minimal-dependency pure-Rust MP4 muxer for recording applications. Includes CLI tool and library API. // 一款依赖项极少的纯 Rust 编写的 MP4 复用器，适用于录制应用程序。包含 CLI 工具和库 API。 (src: gh:Michael-A-Kuykendall/muxide.git)"

