[src/gh]: https://github.com/r-lib/here.git "(MIT) (Languages: R 100.0%) A simpler way to find your files // 一种更简单的方法来查找您的文件 /// The goal of the here package is to enable easy file referencing in [project-oriented workflows] (. https://rstats.wtf/projects.html). In contrast to using setwd(), which is fragile and dependent on the way you organize your files, here uses the top-level directory of a project to easily build paths to files. // here 包的目标是在面向项目的流程中实现轻松的文件引用。与使用 setwd() 相比，setwd() 是脆弱的，并且依赖于你组织文件的方式，here 使用项目的顶层目录来轻松构建指向文件的路径。 /// (: here::here() # get proj dir ;: here::here('something.config') # get `<proj-dir>/something.config` ;: here::here('a', 'b') # get `<proj-dir>/a/b` the path ;: # <-- Whatever which work-dir you are in always give Proj-Root Path for you !!)"
[site/r-lib.org]: https://here.r-lib.org/ "When report.Rmd is rendered, the working directory is internally set to <project root>/analysis by rmarkdown // 当 report.Rmd 被渲染时，rmarkdown 将内部将工作目录设置为 <project root>/analysis /// However, penguins.csv still lives in the data/ subdirectory. The report requires the penguins.csv file to work. // 然而， penguins.csv 仍然位于 data/ 子目录中。报告需要 penguins.csv 文件才能工作。 /// here always uses project-relative paths // here 总是使用相对于项目的路径 /// To render report.Rmd, you would have to ensure the path to penguins.csv is relative to the analysis/ directory - i.e., ../data/penguins.csv. The chunks would knit properly, but could not be run in the console since the working directory in the console isn’t analysis/. // 要渲染 report.Rmd ，你需要确保 penguins.csv 的路径相对于 analysis/ 目录——即 ../data/penguins.csv 。代码块可以正确地编译，但在控制台中无法运行，因为控制台的工作目录不是 analysis/ 。 /// The here package circumvents this issue by always referring to the project root // here 包通过始终引用项目根目录来绕过这个问题"
[package/cran]: https://cran.r-project.org/web/packages/here/ "(Version: 	1.0.2) (Imports: 	rprojroot (≥ 2.1.0)) (Suggests: 	conflicted, covr, fs, knitr, palmerpenguins, plyr, readr, rlang, rmarkdown, testthat, uuid, withr) (Published: 	2025-09-15) (DOI: 	10.32614/CRAN.package.here) (Author: 	Kirill Müller ORCID iD [aut, cre], Jennifer Bryan ORCID iD [ctb]) (Maintainer: 	Kirill Müller <kirill at cynkra.com>) (License: 	MIT + file LICENSE) (NeedsCompilation: 	no) here: A Simpler Way to Find Your Files // 这里：更简单的方式找到你的文件 /// Constructs paths to your project's files. Declare the relative path of a file within your project with 'i_am()'. Use the 'here()' function as a drop-in replacement for 'file.path()', it will always locate the files relative to your project root. // 构建项目文件路径。在项目中声明文件的相对路径，使用'i_am()'。将'here()'函数作为'file.path()'的即插即用替代品，它将始终相对于你的项目根目录定位文件。 (src: gh:r-lib/here.git)"

