[src/gh]: https://github.com/terror/swab.git "(CC0-1.0) (Languages: Rust 96.1%, Shell 2.4%, Just 1.5%) A configurable project cleaning tool // 一个可配置的项目清理工具 /// swab // 拭子 /// swab is a configurable project cleaning tool. // swab 是一款可配置的项目清洁工具。 /// Build artifacts, dependency caches, and generated files accumulate quickly across projects. Running cargo clean in one project, rm -rf node_modules in another, and hunting down .venv directories in a third gets tedious. swab automates this by detecting project types and cleaning them with a single command. // 构建产物、依赖缓存和生成的文件会在各个项目中迅速累积。在一个项目中运行 cargo clean ，在另一个项目中运行 rm -rf node_modules ，然后在第三个项目中查找 .venv 目录 ，这些操作都非常繁琐。swab 可以通过检测项目类型并使用单个命令进行清理来自动完成这些工作。 /// We currently provide 21 built-in rules (. gh: terror/swab.git @master/src/rule) that cover popular ecosystems: Rust (Cargo), Node.js, Python, Go, .NET, Swift, Elixir, Zig, and many more. The rule system is designed to be easily extended with custom rules to fit any project's specific needs. // 我们目前提供 内置 21 条规则 ，涵盖主流生态系统：Rust (Cargo)、Node.js、Python、Go、.NET、Swift、Elixir、Zig 等等。该规则系统设计易于扩展，可根据任何项目的特定需求添加自定义规则。"
[cli.cargo/crates]: https://crates.io/crates/swab "(: cargo install -- swab) (CC0-1.0) (1.8K SLoC) (32 KiB) A configurable project cleaning tool // 一个可配置的项目清理工具 (src: gh:terror/swab.git)"

