[src/gh]: https://github.com/chardet/chardet.git "(LGPL-2.1) (Languages: Python 98.8%, HTML 1.2%) Python character encoding detector // Python 字符编码检测器 /// This is a continuation of Mark Pilgrim's excellent original chardet port from C, and [Ian Cordasco]'s (. gh: @sigmavirus24) charade (. gh:sv24-archive/charade.git) Python 3-compatible fork. // 这是 Mark Pilgrim 的优秀 C 语言 chardet 移植的延续，以及 Ian Cordasco 的 charade 兼容 Python 3 的分支。"
[pkg.pip/pypi]: https://pypi.org/project/chardet/ "(: pip install -- chardet) (License: GNU Lesser General Public License v2 or later (LGPLv2+) (LGPL)) (Requires: Python >=3.7) Universal encoding detector for Python 3 // 适用于 Python 3 的通用编码检测器 (src: gh:chardet/chardet.git)"
[docs/rtd]: https://chardet.readthedocs.io/ "(: import urllib.request ;: rawdata = urllib.request.urlopen('http://yahoo.co.jp/').read() # get raw data for demo) (: import chardet ;: chardet.detect(rawdata) #> {'encoding': 'EUC-JP', 'confidence': 0.99} # got the encoding info.) (: import urllib.request ;: from chardet.universaldetector import UniversalDetector ;: usock = urllib.request.urlopen('http://yahoo.co.jp/') ;: detector = UniversalDetector() ;: for line in usock.readlines(): ;:: detector.feed(line) ;:: if detector.done: break) ;: detector.close() ;: usock.close() ;: print(detector.result) #> {'encoding': 'EUC-JP', 'confidence': 0.99} # Example: Detecting encoding incrementally // 示例：逐步检测编码)"
[knows_by]: https://textract.readthedocs.io/en/stable/python_package.html "... /// or to specify a particular output encoding (input encodings are inferred using chardet): // 或者指定特定的输出编码（输入编码使用 chardet 推断）： /// ..."

