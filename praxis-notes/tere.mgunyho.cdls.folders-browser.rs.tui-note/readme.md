[pkg.x/x-cmd]: https://x-cmd.com/install/tere "(: xd alp -- tere) (: x install -- tere)"
[src/gh]: https://github.com/mgunyho/tere.git "(EUPL-1.2) (Languages: Rust 99.5%, Shell 0.5%) Terminal file explorer // 终端文件浏览器 /// tere - a faster alternative to cd + ls // tere - cd + ls 的更快速替代方案 /// tere is a terminal file explorer. It is a faster alternative to using cd and ls to browse folders in your terminal. tere only really does one thing: it provides a TUI for efficiently navigating to a folder, and then prints the path to that folder when you exit. By configuring your shell to cd to the printed folder, you can move around in your filesystem very quickly. // tere 是一个终端文件浏览器。它是一种比使用 cd 和 ls 在终端中浏览文件夹更快的替代方案。 tere 实际上只做一件事：它提供一个 TUI 来高效地导航到一个文件夹，并在你退出时打印该文件夹的路径。通过配置你的 shell 将 cd 设置为打印的文件夹，你可以非常快速地在文件系统中移动。 /// Note that tere is not a file manager, it can only be used to browse folders, not to create, rename or delete them. // 请注意 tere 不是一个文件管理器，它只能用来浏览文件夹，不能用来创建、重命名或删除它们。 /// tere aims to be minimal and simple. It should be obvious how to use it. Navigating the file system should be efficient and require as few keystrokes as possible. A great source of inspiration for tere is the \"type-ahead search\" functionality found in many GUI file managers. // tere 旨在保持极简和简单。应该很明显如何使用它。浏览文件系统应该是高效的，并且尽可能少地使用按键。 tere 的一个伟大灵感来源是许多 GUI 文件管理器中找到的“类型前搜索”功能。 /// \"Tere\" means \"hello\" in Estonian. It also feels nice to type. // \"Tere\"在爱沙尼亚语中意为 \"你好\" 。输入它也感觉很舒服。"
[pkg.cargo/crates]: https://crates.io/crates/tere "(: cargo install -- tere) (EUPL-1.2) (287 KiB)"

