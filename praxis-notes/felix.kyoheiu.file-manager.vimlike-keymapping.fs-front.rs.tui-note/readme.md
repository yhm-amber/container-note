[src/gh]: https://github.com/kyoheiu/felix.git "(MIT) (Languages: Rust 100.0%) tui file manager with vim-like key mapping // 带有类似 Vim 的快捷键映射的 tui 文件管理器 /// felix // 菲利克斯 /// you can use felix more conveniently by installing these apps: // 您可通过安装以下应用程序更方便地使用 Felix： /// - zoxide (. gh: ajeetdsouza/zoxide.git): A smarter cd command, which enables you to jump to a directory that matches the keyword in felix. // zoxide ：更智能的 cd 命令，使您能够跳转到 felix 中与关键字匹配的目录。 /// - chafa (. web: hpjansson.org/chafa, gh: hpjansson/chafa.git): Terminal graphics for the 21st century, by which you can preview images in felix. chafa must be v1.10.0 or later. // chafa ：面向 21 世纪的终端图形界面，可用于在 Felix 中预览图像。chafa 版本必须为 v1.10.0 或更高版本。 /// - bat (. gh: sharkdp/bat.git): A cat(1) clone. Add syntax highlighting to the text preview with bat. // bat ： cat(1) 的一个克隆函数。使用 bat 为文本预览添加语法高亮显示。 /// These apps do not need any configuration to use with felix! // 这些应用无需任何配置即可与 Felix 一起使用！"
[cli.cargo/crates]: https://crates.io/crates/felix "(: cargo install -- felix) (MIT) (6.5K SLoC) (216 KiB) tui file manager with vim-like key mapping (src: gh:kyoheiu/felix.git)"
[site/dev]: https://kyoheiu.dev/felix/ "(: fx adir # working dir is 'adir' ;: fx # equals `fx .` ;: fx --init # show a shell script into `stdout` that can be sourced for shell integration) (: source <(command -- fx --init) # you can add this to such as ~/.profile # `command` command is using for suppressing shell function lookup to make sure `fx` is command not a shell-function.)"

