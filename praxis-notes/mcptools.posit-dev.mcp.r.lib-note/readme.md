[src/gh]: https://github.com/posit-dev/mcptools.git "(MIT) (Languages: R 100%) Model Context Protocol For R // R 模型上下文协议 /// mcptools implements the [Model Context Protocol](https://modelcontextprotocol.io/ 'MCP (Model Context Protocol) is an open-source standard for connecting AI applications to external systems. // MCP（模型上下文协议）是一个开源标准，用于连接 AI 应用程序到外部系统。') in R. There are two sides to mcptools: // mcptools 在 R 中实现了模型上下文协议。mcptools 有两个方面： /// R as an MCP server: // R 作为 MCP 服务器： /// - Client (~ Server): Claude Desktop + Claude Code, Copilot Chat in VSCode, Positron Assistant, ... /// - Server (~ Client, Session(Optionally)): `mcp_server()`: Tools (R functions), like: Read R Package Documentation, Run R Code, Inspect objects in the global R environment // 阅读 R 包文档、运行 R 代码、检查全局 R 环境中的对象 /// - Session(Optionally) (~ Server): `mcp_server()`: Some R Project, ..."
[package/cran]: https://cran.r-project.org/web/packages/mcptools/ "(Version: 	0.2.0) (Depends: 	R (≥ 4.1.0)) (Imports: 	cli, ellmer (≥ 0.3.0), httpuv, httr2, jsonlite, nanonext (≥ 1.6.0), processx, promises, rlang) (Suggests: 	knitr, rmarkdown, testthat (≥ 3.0.0), withr) (Published: 	2025-10-29) (DOI: 	10.32614/CRAN.package.mcptools) (Author: 	Simon Couch ORCID iD [aut, cre], Winston Chang ORCID iD [aut], Charlie Gao ORCID iD [aut], Posit Software, PBC ROR ID [cph, fnd]) (Maintainer: 	Simon Couch <simon.couch at posit.co>) (License: 	MIT + file LICENSE) (NeedsCompilation: 	no) mcptools: Model Context Protocol Servers and Clients // mcptools：模型上下文协议服务器和客户端 /// Implements the Model Context Protocol (MCP). Users can start 'R'-based servers, serving functions as tools for large language models to call before responding to the user in MCP-compatible apps like 'Claude Desktop' and 'Claude Code', with options to run those tools inside of interactive 'R' sessions. On the other end, when 'R' is the client via the 'ellmer' package, users can register tools from third-party MCP servers to integrate additional context into chats. // 实现模型上下文协议（MCP）。用户可以启动基于'R'的服务器，将函数作为工具供大型语言模型在响应用户之前调用，这些工具可以在兼容 MCP 的应用程序（如'Claude Desktop'和'Claude Code'）中运行，并可选择在交互式'R'会话中运行这些工具。另一方面，当'R'通过'ellmer'包作为客户端时，用户可以注册来自第三方 MCP 服务器的工具，以将额外的上下文集成到聊天中。 (src: gh:posit-dev/mcptools.git)"
[site/ghio]: https://posit-dev.github.io/mcptools/ "Register third-party MCP servers with ellmer chats to integrate additional context into e.g. shinychat (. gh: posit-dev/shinychat.git) and querychat (. gh: posit-dev/querychat.git) apps. // 通过 ellmer 聊天注册第三方 MCP 服务器，以将额外上下文集成到例如 shinychat 和 querychat 应用中。 /// NOTE: // 注意： ///: This package used to be called acquaint and supplied a default set of tools from btw when R was used as an MCP server. The direction of the dependency has been reversed; to use the same functionality from before, transition acquaint::mcp_server() to btw::btw_mcp_server() and acquaint::mcp_session() to btw::btw_mcp_session(). // 这个包以前叫做 acquaint，当 R 作为 MCP 服务器时，它提供了来自 btw 的默认工具集。依赖方向已经反转；要使用之前相同的功能，将 acquaint::mcp_server() 过渡到 btw::btw_mcp_server() ，将 acquaint::mcp_session() 过渡到 btw::btw_mcp_session() 。"
