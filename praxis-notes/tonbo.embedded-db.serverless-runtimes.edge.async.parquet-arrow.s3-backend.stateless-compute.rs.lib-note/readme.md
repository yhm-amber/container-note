[src/gh]: https://github.com/tonbo-io/tonbo.git "(Apache-2.0) (Languages: Rust 99.4%, Shell 0.6%) Tonbo is an embedded database for serverless and edge runtimes. // Tonbo 是一个面向无服务器和边缘运行时环境的嵌入式数据库。 /// Tonbo // 蜻蜓 /// Tonbo is an embedded database for serverless and edge runtimes. Your data is stored as Parquet on S3, coordination happens through a manifest, and compute stays fully stateless. // Tonbo 是一个面向无服务器和边缘运行时环境的嵌入式数据库。您的数据以 Parquet 格式存储在 S3 上，数据协调通过清单文件进行，计算过程完全无状态。 /// Why Tonbo? // 为什么是蜻蜓？ /// Serverless compute is stateless, but your data isn't. Tonbo bridges this gap: // 无服务器计算是无状态的，但您的数据并非如此。Tonbo 弥合了这一差距： /// - Async-first: The entire storage and query engine is fully async, built for serverless and edge environments. // 异步优先 ：整个存储和查询引擎完全异步，专为无服务器和边缘环境而构建。 /// - No server to manage: Data lives on S3, coordination via manifest, compute is stateless // 无需管理服务器 ：数据存储在 S3 上，通过清单进行协调，计算是无状态的。 /// - Arrow-native: Define rich data type, declarative schemas, query with zero-copy RecordBatch // Arrow-native ：定义丰富的数据类型、声明式模式，并使用零拷贝 RecordBatch 进行查询 /// - Runs anywhere: Tokio, WASM, edge runtimes, or as a storage engine for building your own data infrastructure. // 可在任何地方运行 ：Tokio、WASM、边缘运行时，或作为构建您自己的数据基础设施的存储引擎。 /// - Open formats: Standard Parquet files readable by any tool // 开放格式 ：任何工具均可读取的标准 Parquet 文件 /// When to use Tonbo? // 何时使用蜻蜓？ /// - Build serverless or edge applications that need a durable state layer without running a database. // 构建无需运行数据库即可拥有持久状态层的无服务器或边缘应用程序。 /// - Store append-heavy or event-like data directly in S3 and query it with low overhead. // 将追加密集型或事件型数据直接存储在 S3 中，并以低开销进行查询。 /// - Embed a lightweight MVCC + Parquet storage engine inside your own data infrastructure. // 在您自己的数据基础设施中嵌入轻量级的 MVCC + Parquet 存储引擎。 /// - Run workloads in WASM or Cloudflare Workers that require structured persistence. // 在 WASM 或 Cloudflare Workers 中运行需要结构化持久性的工作负载。 /// Run with cargo run --example <name>: // 使用 cargo run --example <name> 运行： /// - 01_basic: Define schema, insert, and query in 30 lines // 01_basic ：用 30 行定义模式、插入和查询。 /// - 02_transaction: MVCC transactions with upsert, delete, and read-your-writes // 02_transaction ：支持 upsert、delete 和读写操作的 MVCC 事务 /// - 02b_snapshot: Consistent point-in-time reads while writes continue // 02b_snapshot ：在写入持续进行的同时，保持时间点读取的一致性 /// - 03_filter: Predicates: eq, gt, in, is_null, and, or, not // 03_filter ：谓词：eq、gt、in、is_null、and、or、not /// - 04_s3: Store Parquet files on S3/R2/MinIO with zero server config // 04_s3 ：无需服务器配置即可将 Parquet 文件存储在 S3/R2/MinIO 上 /// - 05_scan_options: Projection pushdown reads only the columns you need // 05_scan_options ：投影下推仅读取您需要的列 /// - 06_composite_key: Multi-column keys for time-series and partitioned data // 06_composite_key ：用于时间序列和分区数据的多列键 /// - 07_streaming: Process millions of rows without loading into memory // 07_streaming ：无需加载到内存即可处理数百万行数据 /// - 08_nested_types: Deep struct nesting + Lists stored as Arrow StructArray // 08_nested_types ：深度结构嵌套 + 以箭头结构数组形式存储的列表 /// - 09_time_travel: Query historical snapshots via MVCC timestamps // 09_time_travel ：通过 MVCC 时间戳查询历史快照 /// Architecture // 建筑学 /// Tonbo implements a merge-tree optimized for object storage: writes go to WAL → MemTable → Parquet SSTables, with MVCC for snapshot isolation and a manifest for coordination via compare-and-swap: // Tonbo 实现了一个针对对象存储优化的合并树：写入操作进入 WAL → MemTable → Parquet SSTable，使用 MVCC 进行快照隔离，并通过清单进行比较和交换协调： /// - Stateless compute: A worker only needs to read and update the manifest; no long-lived coordinator is required. // 无状态计算 ：工作进程只需要读取和更新清单；不需要长期存在的协调器。 /// - Object storage CAS: The manifest is committed using compare-and-swap on S3, so any function can safely participate in commits. // 对象存储 CAS ：清单使用 S3 上的比较和交换进行提交，因此任何函数都可以安全地参与提交。 /// - Immutable data: Data files are write-once Parquet SSTables, which matches the strengths of S3 and other object stores. // 不可变数据 ：数据文件是只写一次的 Parquet SSTable，这与 S3 和其他对象存储的优势相匹配。"
[lib.cargo/crates]: https://crates.io/crates/tonbo "(: cargo add -- tonbo) (Apache-2.0) (12K SLoC) (141 KiB) Embedded database for serverless and edge runtimes, storing data as Parquet on S3 // 适用于无服务器和边缘运行时环境的嵌入式数据库，将数据以 Parquet 格式存储在 S3 上。 (src: gh:tonbo-io/tonbo.git)"
[site/io]: https://tonbo.io "Tonbo is an open-source, embedded database library for serverless, data-intensive applications. Runs in-process. No separate database service. // Tonbo 是一个开源的嵌入式数据库库，适用于无服务器、数据密集型应用。它在进程内运行，无需单独的数据库服务。 /// - About // 关于 ///: About Tonbo IO was conceived in 2022. I have observed that existing data processing infrastructure often requires transferring data to centralized vendors for storage and analysis, limiting true data ownership. Tonbo IO addresses this challenge by offering an edge-first embedded database that supports data storage and analysis from local devices to shared storage services, all in open-source ... // Tonbo IO 于 2022 年构思。我观察到，现有的数据处理基础设施通常需要将数据传输到集中式供应商进行存储和分析，这限制了用户对数据的真正所有权。Tonbo IO 通过提供边缘优先的嵌入式数据库来应对这一挑战，该数据库支持从本地设备到共享存储服务的数据存储和分析，并且完全开源…… /// - Blogs // 博客 ///: Tonbo is an open source embedded database library for building serverless, data intensive applications. It lets you embed data processing and analytics wherever they make sense, inside a Postgres extension, a client application, a lambda function, or even a browser page, without running or operating a separate database service. // Tonbo 是一个开源的嵌入式数据库库，用于构建无服务器、数据密集型应用程序。它允许您在任何合适的地方嵌入数据处理和分析功能，例如 Postgres 扩展、客户端应用程序、Lambda 函数，甚至是浏览器页面，而无需运行或管理单独的数据库服务。"
[book/ghio]: https://tonbo-io.github.io/tonbo/ "What is Tonbo? // 什么是蜻蜓？ /// Tonbo is an in-process KV database that can be embedded in data-intensive applications written in Rust, Python, or JavaScript (WebAssembly / Deno). It is designed for analytical processing. Tonbo can efficiently write data in real time in edge environments such as browsers and AWS Lambda, with the data stored in memory, on local disks, or in S3 using Apache Parquet format. // Tonbo 是一款进程内键值数据库，可嵌入到用 Rust、Python 或 JavaScript（WebAssembly/Deno）编写的数据密集型应用程序中。它专为分析处理而设计。Tonbo 能够高效地在浏览器和 AWS Lambda 等边缘环境中实时写入数据，并将数据存储在内存、本地磁盘或 S3（采用 Apache Parquet 格式）中。"
[discord]: https://discord.gg/j27XVFVmJM

