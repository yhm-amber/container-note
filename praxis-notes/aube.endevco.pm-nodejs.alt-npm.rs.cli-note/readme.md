[src/gh]: https://github.com/endevco/aube.git "(MIT) (Languages: Rust 79.2%, Shell 20.1%, Other 0.7%) A fast Node.js package manager // 一个快速的 Node.js 包管理器 /// Why Try It // 为什么要尝试 /// - Fast installs (. {{.site}}: benchmarks). Warm CI is about 7x faster than pnpm and 3x faster than Bun in the current benchmarks. Across the fixture set, aube runs up to ~22x faster than pnpm and up to 3x faster than Bun. // 安装速度极快 。 在当前的基准测试中，Warm CI 的速度比 pnpm 快约 7 倍，比 Bun 快约 3 倍。在所有测试用例中，aube 的运行速度比 pnpm 快约 22 倍，比 Bun 快约 3 倍。 /// - Existing lockfiles (. {{.site/pm}}: lockfiles). Reads and writes pnpm-lock.yaml, package-lock.json, npm-shrinkwrap.json, yarn.lock, and bun.lock in place. // 现有锁定文件 。 读取和写入 pnpm-lock.yaml 、 package-lock.json 、 npm-shrinkwrap.json 、 yarn.lock 和 bun.lock 。 /// - Cheap repeat commands (. {{.site/pm}}: scripts). aube run test, aube test, and aube exec vitest auto-install when dependencies are stale, then skip that work when nothing changed. // 廉价的重复命令 。aube aube run test 、 aube test 和 aube exec vitest 在依赖项过时时自动安装，然后在没有任何更改时跳过该工作。 /// - Less disk use (. {{.site/pm}}: node-modules). A global content-addressable store lets projects share package files instead of keeping a full copy of the same dependencies in every checkout. // 减少磁盘占用 。 全局内容寻址存储允许项目共享包文件，而无需在每次检出时都保留相同依赖项的完整副本。 /// - Secure defaults (. {{.site/pm}}: configuration). aube defaults to safer installs: new releases wait out a minimum age, exotic transitive dependencies are blocked, and dependency lifecycle scripts require approval. // 安全的默认设置 。aube 默认采用更安全的安装方式：新版本会等待一段最短时间，特殊的传递依赖项会被阻止，依赖项生命周期脚本需要获得批准。 /// Install // 安装 /// (: mise use -g -- aube # The recommended path is mise. // 推荐途径是 mise 。) (: mise use -- aube # Inside a project, you can also pin aube with mise. // 在项目内部，您还可以使用 mise 将 aube 固定到项目顶部。) (: npm install -g -- @endevco/aube # aube is also published on npm。 // aube 也已发布在 npm 上。) /// Commands /// (: aube install # Run aube in an existing Node.js project // 在现有的 Node.js 项目中运行 aube) (: aube install # install dependencies) (: aube add react # add a dependency) (: aube add -D vitest # add a dev dependency) (: aube remove react # remove a dependency) (: aube update # update dependencies within package.json ranges) (: aube run build # run a package.json script) (: aube test # run the test script, auto-installing first if needed) (: aube exec vitest # run a local binary) (: aube dlx cowsay hi # run a package in a throwaway environment) (: aube ci # clean, frozen install for CI)"
[cli.mise/mise-reg]: https://mise.jdx.dev/registry.html#:~:text=aube "(: mise use -g -- aube) (~ aube: github:endevco/aube) List of all tools aliased by default in mise. // mise 中默认别名的所有工具列表。 /// You can use these shorthands with mise use. This allows you to use a tool without needing to know the full name. For example, to use the aws-cli tool, you can do the following: // 你可以使用这些简写 mise use 。这样，你就可以在不知道工具全称的情况下使用它。例如，要使用 aws-cli 工具，你可以这样做： /// (: mise use aws-cli) /// instead of // 而不是 /// (: mise use aqua:aws/aws-cli) /// If a tool is not available in the registry, you can install it by its full name. github and aqua give you for example access to almost all programs available on GitHub. // 如果注册表中没有某个工具，您可以按其全名安装。例如， github 和 aqua 可以让您访问 GitHub 上几乎所有可用的程序。"
[cli.npm/npmx]: https://npmx.dev/package/@endevco/aube "(: npm i -g -- @endevco/aube) (MIT) A fast Node.js package manager that drops into existing projects. // 一个快速的 Node.js 包管理器，可轻松集成到现有项目中。 (src: gh:endevco/aube.git)"
[intro:npm-be/mise.site]: https://mise.jdx.dev/dev-tools/backends/npm.html "... With the default npm.package_manager = \"auto\" setting, mise uses aube for installing npm packages when it is installed, similar to how the pipx backend uses uv when available. If you use aube, bun, or pnpm as the package manager, that package manager must also be installed. // ... 默认情况下， npm.package_manager = \"auto\" ，如果 aube 已安装，mise 会使用 aube 来安装 npm 包，类似于 pipx 后端在可用时使用 uv 方式。如果您使用 aube 、 bun 或 pnpm 作为包管理器，则必须也安装相应的包管理器。"
