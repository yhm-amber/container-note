[src/gh]: https://github.com/cocogitto/cocogitto.git "(MIT) (Languages: Rust 96.0%, JavaScript 1.4%, CSS 0.9%, TypeScript 0.8%, SCSS 0.6%, Just 0.2%, Dockerfile 0.1%) The Conventional Commits toolbox // 常规提交工具箱 /// The Conventional Commits toolbox // 常规提交工具箱 /// - Verified commits: create conventional compliant commits with ease. // 验证提交：轻松创建符合规范的常规提交。 /// - Automatic Version bump and changelog: automatically bump versions and generate changelogs with your own custom steps and workflows. // 自动版本更新和变更日志：使用自定义步骤和工作流程自动更新版本并生成变更日志。 /// - Release profiles: your branching model requires different steps for releases, pre-release, hotfixes ? We got you covered ! // 发布配置：您的分支模型需要不同的发布步骤、预发布、热修复？我们为您提供了全面的解决方案！ /// - Depends only on libgit2: cocogitto has one standalone binary, the only system dependency is libgit2. // 仅依赖于 libgit2：cocogitto 只有一个独立的二进制文件，唯一的系统依赖是 libgit2。 /// - Conventional git log: search your commit history matching Conventional Commits items such as scope and commit type. // 遵循常规 git 日志：搜索您的提交历史，匹配常规提交（Conventional Commits）项目，如范围和提交类型。 /// - GitHub integration: enforce the conventional commits specification with our GitHub action and bot. // GitHub 集成：通过我们的 GitHub 操作和机器人强制执行常规提交（Conventional Commits）规范。 /// - Monorepo support: Automatic versioning for mono-repositories is supported out of the box. // 单仓库支持：单仓库的自动版本控制功能内置支持。"
[pkg.x/x-cmd]: https://x-cmd.com/install/cocogitto "(: xd alp -- cocogitto) (: x install -- cocogitto)"
[pkg.cargo/crates]: https://crates.io/crates/cocogitto "(: cargo install -- cocogitto) (MIT) (1.69 MiB)"
[pkg.oci/ghcr]: https://github.com/cocogitto/cocogitto/pkgs/container/cog "(: docker run -t -v \"$(pwd)\":/app/ -- ghcr.io/cocogitto/cog check)"
[site]: https://docs.cocogitto.io/
