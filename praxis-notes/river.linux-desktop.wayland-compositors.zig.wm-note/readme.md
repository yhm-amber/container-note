[src/cb]: https://codeberg.org/river/river "(source code: GPL-3.0-only, protocols: MIT, logo/docs: CC-BY-SA-4.0) (Languages: Zig 99.7%, C 0.3%) A non-monolithic Wayland compositor // 非整体式 Wayland 合成器 /// Overview // 概述 /// River is a non-monolithic Wayland compositor. Unlike other Wayland compositors, river does not combine the compositor and window manager into one program. Instead, users can choose any window manager implementing the river-window-management-v1 protocol. // River 是一个非整体式 Wayland 合成器。与其他 Wayland 合成器不同， river 并没有将合成器和窗口管理器合并到一个程序中。 相反，用户可以选择任何实现了该功能的窗口管理器。 river-window-management-v1 协议。 /// Features // 特征 /// River defers all window management policy to a separate window manager implementing the river-window-management-v1 protocol. This includes window position/size, pointer/keyboard bindings, focus management, window decorations, desktop shell graphics, and more. // River 将所有窗口管理策略都交给一个独立的窗口管理器来处理，该窗口管理器实现了 river-window-management-v1 协议。这包括窗口位置/大小、指针/键盘绑定、焦点管理、窗口装饰、桌面外壳图形等等。 /// River itself provides frame perfect rendering, good performance, support for many Wayland protocol extensions, robust Xwayland support, the ability to hot-swap window managers, and more. // River 本身提供帧完美渲染、良好的性能、对许多 Wayland 协议扩展的支持、强大的 Xwayland 支持、热插拔窗口管理器的功能等等。 /// The river-window-management-v1 protocol and other river protocol extensions are stable. We do not break window managers. // river-window-management-v1 协议及其他 river 协议扩展均稳定。我们不会破坏窗口管理器。 /// Motivation // 动机 ///: Why split the window manager to a separate process? // 为什么要将窗口管理器拆分成一个单独的进程？ /// - Significantly lower the barrier to entry for writing a Wayland window manager. // 大幅降低编写 Wayland 窗口管理器的门槛。 /// - Allow implementing Wayland window managers in high-level garbage collected languages without impacting compositor performance and latency. // 允许在高级垃圾回收语言中实现 Wayland 窗口管理器，而不会影响合成器性能和延迟。 /// - Allow hot-swapping between window managers without restarting the compositor and all Wayland programs. // 允许在不重启合成器和所有 Wayland 程序的情况下热切换窗口管理器。 /// - Promote diversity and experimentation in window manager design. // 促进窗口管理器设计的多样性和实验性。"
[src-mirror/gh]: https://github.com/riverwm/river.git "(source code: GPL-3.0-only, protocols: MIT, logo/docs: CC-BY-SA-4.0) (Languages: Zig 99.7%, C 0.3%) [mirror] A non-monolithic Wayland compositor // 非整体式 Wayland 合成器 /// Overview // 概述 /// River is a non-monolithic Wayland compositor. Unlike other Wayland compositors, river does not combine the compositor and window manager into one program. Instead, users can choose any window manager implementing the river-window-management-v1 protocol. // River 是一个非整体式 Wayland 合成器。与其他 Wayland 合成器不同， river 并没有将合成器和窗口管理器合并到一个程序中。 相反，用户可以选择任何实现了该功能的窗口管理器。 river-window-management-v1 协议。 /// Features // 特征 /// River defers all window management policy to a separate window manager implementing the river-window-management-v1 protocol. This includes window position/size, pointer/keyboard bindings, focus management, window decorations, desktop shell graphics, and more. // River 将所有窗口管理策略都交给一个独立的窗口管理器来处理，该窗口管理器实现了 river-window-management-v1 协议。这包括窗口位置/大小、指针/键盘绑定、焦点管理、窗口装饰、桌面外壳图形等等。 /// River itself provides frame perfect rendering, good performance, support for many Wayland protocol extensions, robust Xwayland support, the ability to hot-swap window managers, and more. // River 本身提供帧完美渲染、良好的性能、对许多 Wayland 协议扩展的支持、强大的 Xwayland 支持、热插拔窗口管理器的功能等等。 /// The river-window-management-v1 protocol and other river protocol extensions are stable. We do not break window managers. // river-window-management-v1 协议及其他 river 协议扩展均稳定。我们不会破坏窗口管理器。 /// Motivation // 动机 ///: Why split the window manager to a separate process? // 为什么要将窗口管理器拆分成一个单独的进程？ /// - Significantly lower the barrier to entry for writing a Wayland window manager. // 大幅降低编写 Wayland 窗口管理器的门槛。 /// - Allow implementing Wayland window managers in high-level garbage collected languages without impacting compositor performance and latency. // 允许在高级垃圾回收语言中实现 Wayland 窗口管理器，而不会影响合成器性能和延迟。 /// - Allow hot-swapping between window managers without restarting the compositor and all Wayland programs. // 允许在不重启合成器和所有 Wayland 程序的情况下热切换窗口管理器。 /// - Promote diversity and experimentation in window manager design. // 促进窗口管理器设计的多样性和实验性。"
[site/isaacfreund.com]: https://isaacfreund.com/software/river/ "Use of generative AI/LLMs is strictly forbidden for all contributions to river. // 严禁对河流的所有贡献使用生成式人工智能/LLM。"
[protocol-intro/.site-docs]: https://isaacfreund.com/docs/wayland/river-window-management-v1/ "river-window-management-v1 // 河流窗口管理-v1 /// frame-perfect window management // 完美框架窗口管理 /// This protocol allows a single “window manager” client to determine the window management policy of the compositor. State is globally double-buffered allowing for frame perfect state changes involving multiple windows. // 该协议允许单个“窗口管理器”客户端决定合成器的窗口管理策略。状态采用全局双缓冲，从而实现涉及多个窗口的帧完美状态变更。"
[wm-intro/.site-blog]: https://isaacfreund.com/blog/river-window-management/ "Separating the Wayland Compositor and Window Manager // 将 Wayland 合成器和窗口管理器分离 /// Traditional Wayland compositors have a monolithic architecture that combines the compositor and window manager into a single program. This has the downside of requiring Wayland window managers to do the significant work of implementing an entire Wayland compositor as well. // 传统的 Wayland 合成器采用单体架构，将合成器和窗口管理器集成到一个程序中。这种架构的缺点在于，它要求 Wayland 窗口管理器也承担实现整个 Wayland 合成器的繁重工作。 /// The new 0.4.0 release of river, a non-monolithic Wayland compositor, breaks from this traditional architecture and splits the window manager into a separate program. There are already many window managers compatible with river. // river 的 0.4.0 新版本是一个非整体式 Wayland 合成器，它打破了传统的架构，将窗口管理器拆分成一个独立的程序。目前已经有许多窗口管理器与 river 兼容。"
[knowsby]: https://archcraft.io/premium.html#wc_river "River Desktop /// The Ultimate River Config (A Desktop Environment Like Experience) for Archcraft. // Archcraft 的终极 River 配置（ 类似桌面环境的体验 ）。 /// This is a part of Archcraft Prime and Archcraft Wayland Special Edition ISOs. // 这是 Archcraft Prime 和 Archcraft Wayland 特别版 ISO 的一部分。"

