[src/gh]: https://github.com/pingcap/tiup.git "(Apache-2.0) (Languages: Go 93.8%, Shell 4.1%, Smarty 1.6%, Other 0.5%) A component manager for TiDB // TiDB 的组件管理器 /// tiup is a tool to download and install TiDB components. // tiup 是一个用于下载和安装 TiDB 组件的工具。 /// (: tiup playground # Run playground for Quick start) (: tiup install -- tidb tikv pd # Install components // 安装组件) (: tiup uninstall -- tidb tikv pd # Uninstall components // 卸载组件) (: tiup update --all # Update All components)"
[site/io]: https://tiup.io/ "a component manager for TiDB // TiDB 的组件管理器 /// All Components // 所有组件 /// - PCC ///: A tool used to capture plan changes among different versions of TiDB // 用于捕获不同 TiDB 版本之间计划变更的工具 /// - bench ///: Benchmark database with different workloads // 使用不同工作负载的基准数据库 /// - br ///: TiDB/TiKV cluster backup restore tool. // TiDB/TiKV 集群备份恢复工具。 /// - cdc ///: CDC is a change data capture tool for TiDB // CDC 是 TiDB 的变更数据捕获工具。 /// - chaosd ///: An easy-to-use Chaos Engineering tool used to inject failures to a physical node // 一款易于使用的混沌工程工具，用于向物理节点注入故障。 /// - client ///: Client to connect playground // 客户端连接到 Playground /// - cloud // 云 ///: CLI tool to manage TiDB Cloud // 用于管理 TiDB 云的 CLI 工具 /// - cluster // 集群 ///: Deploy a TiDB cluster for production // 为生产环境部署 TiDB 集群 /// - ctl // 控制器 ///: TiDB controller suite // TiDB 控制器套件 /// - dm ///: Data Migration Platform manager // 数据迁移平台管理器 /// - dmctl ///: dmctl component of Data Migration Platform. // 数据迁移平台的 dmctl 组件。 /// - pd-recover // pd-恢复 ///: PD Recover is a disaster recovery tool of PD, used to recover the PD cluster which cannot start or provide services normally. // PD Recover 是 PD 的灾难恢复工具，用于恢复无法正常启动或提供服务的 PD 集群。 /// - playground ///: Bootstrap a local TiDB cluster for fun // 为了好玩，引导一个本地 TiDB 集群。 /// - sync-diff-inspector // 同步差异检查器 ///: sync-diff-inspector is a tool used to verify the consistency across different MySQL-compatible data sources. // sync-diff-inspector 是一个用于验证不同 MySQL 兼容数据源之间一致性的工具。 /// - tici-meta ///: TiCI Meta service server // TiCI Meta 服务服务器 /// - tici-server ///: TiCI server // TiCI 服务器 /// - tici-worker ///: TiCI worker server // TiCI 工作服务器 /// - tici ///: TiCI server // TiCI 服务器 /// - tidb-dashboard ///: TiDB Dashboard is a Web UI for monitoring, diagnosing, and managing the TiDB cluster // TiDB Dashboard 是一个用于监控、诊断和管理 TiDB 集群的 Web 用户界面。 /// - tidb-lightning // tidb-闪电 ///: TiDB Lightning is a tool used for fast full import of large amounts of data into a TiDB cluster // TiDB Lightning 是一款用于将大量数据快速完整导入 TiDB 集群的工具。 /// - tidb ///: TiDB is an open source distributed HTAP database compatible with the MySQL protocol. // TiDB 是一个开源的分布式 HTP 数据库，与 MySQL 协议兼容。 /// - tikv-br ///: TiKV cluster backup restore tool // TiKV 集群备份恢复工具 /// - tikv-cdc ///: TiKV-CDC is a change data capture tool for TiKV // TiKV-CDC 是 TiKV 的变更数据采集工具。 /// - tiproxy ///: TiProxy is a database proxy that is based on TiDB. // TiProxy 是一个基于 TiDB 的数据库代理。"
[knowsby]: https://github.com/Hexilee/tifs.git "You need a tikv cluster to run tifs. tiup is convenient to deploy one, just install it and run tiup playground. // 运行 tifs 需要一个 tikv 集群。tiup 可以很方便地部署一个 tikv 集群，只需安装它并运行 tiup playground 即可。"

