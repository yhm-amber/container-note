[src/gh]: https://github.com/Helvesec/rmux.git "(Apache-2.0, MIT) (Languages: Rust 99.0%, Other 1.0%) Universal Rust multiplexer with a typed SDK — drive any CLI or TUI app from code. Native on Linux, macOS, and Windows. // 通用的 Rust 多路复用器，带有类型化的 SDK——可从代码驱动任何 CLI 或 TUI 应用。原生支持 Linux、macOS 和 Windows。 /// Universal Rust multiplexer for the agentic era: detachable, scriptable, and inspectable, with a tmux-compatible CLI, daemon-backed SDK, and native Ratatui integration. // 面向代理时代的通用 Rust 多路复用器：可分离、可脚本化、可检查，具有与 tmux 兼容的 CLI、守护进程支持的 SDK 和原生 Ratatui (. web: ratatui.rs) 集成。 /// Why RMUX // 为什么选择 RMUX /// RMUX exists because I believe the tmux use case has only been partially explored. My own starting point was simple: I wanted to run long-lived agents over SSH without losing their terminals, while still being able to inspect, script, and orchestrate everything around them. // RMUX 的出现是因为我认为 tmux 的应用场景还没有被充分挖掘。我的出发点很简单：我希望通过 SSH 运行长时间运行的代理程序，同时又不丢失它们的终端，并且还能对它们周围的一切进行检查、编写脚本和编排。 /// So I rebuilt that idea from scratch in Rust: a blazing-fast, tmux-compatible multiplexer with a typed SDK, persistent sessions, structured snapshots, and native local transports on Linux, macOS, and Windows, including Windows Named Pipes. // 因此，我用 Rust 从头开始​​重建了这个想法：一个速度极快、与 tmux 兼容的多路复用器，具有类型化的 SDK、持久会话、结构化快照以及在 Linux、macOS 和 Windows 上的原生本地传输，包括 Windows 命名管道。 /// RMUX is usable by agents, headless CLI workflows, and humans alike: you can give terminal apps detachable execution, reconnect later, inspect their state, drive them from code, or simply use it for normal tmux-style terminal work. // RMUX 可供代理、无头 CLI 工作流和人类使用：您可以赋予终端应用程序可分离执行、稍后重新连接、检查其状态、从代码驱动它们，或者只是将其用于普通的 tmux 风格的终端工作。 /// Use command help locally: // 在本地使用命令帮助： ///: (: rmux list-commands) (: rmux new-session --help) (: rmux split-window --help) /// Terminal compatibility notes // 终端兼容性说明 /// RMUX works with shells that query terminal capabilities, including fish. It answers terminal device-attribute probes and handles Escape-key timing so fish prompts and key sequences behave normally inside RMUX panes. // RMUX 可与查询终端功能的 shell 配合使用，包括 fish。它能够响应终端设备属性探测，并处理 Esc 键的时序，因此 fish 的提示符和按键序列在 RMUX 面板中能够正常工作。"
[cli.cargo/crates]: https://crates.io/crates/rmux "(: cargo install --locked -- rmux) (MIT OR Apache-2.0) (10K SLoC) (198 KiB) A local terminal multiplexer with a tmux-style CLI, daemon runtime, Rust SDK, and ratatui integration. // 一个具有 tmux 风格 CLI、守护进程运行时、Rust SDK 和 ratatui 集成的本地终端复用器。 (src: gh:Helvesec/rmux.git)"
[docs/.site]: https://rmux.io/docs/ "关于 RMUX /// 一个从零用 Rust 编写的本地客户端/服务器终端复用器。一个 daemon 和一个传输协议支撑三种公共入口：兼容 tmux 的 CLI、类型化 Rust SDK，以及 ratatui widget。 /// - 原生运行，无需 WSL ///: Windows 使用原生 ConPTY 和 named pipes；Linux 与 macOS 使用 Unix PTY 和 Unix-domain sockets。daemon 从不打开网络监听端口。 /// - 可从 Rust 编程控制 ///: 为 Session、Window、Pane 提供类型化 handles。可以获取结构化 snapshots、发送按键、等待输出、订阅 pane events。不需要抓取 TTY 文本。 /// - 可直接替换 tmux ///: 完整的 tmux 4.0.1 命令面 — 90 条命令、持久 sessions，现有脚本和快捷键继续可用。"
[site/io]: https://rmux.io/ "终端复用引擎，服务于 CI▌Agent▌开发者▌ 。 /// RMUX 让你的 shell 保持运行、可脚本化，并且完全可检查。你可以把它作为极快、兼容 tmux 的终端复用器，也可以作为终端原生自动化引擎，提供持久会话、快照和类型化编排。100% Rust。 /// 像使用 tmux  一样使用它，也可以像 Playwright 一样驱动它。"
