[src/gh]: https://github.com/89luca89/lilipod.git "(GPL-3.0) (Languages: Go 99.2%, Makefile 0.8%) Lilipod is a simple container manager, able to download, unpack and use OCI images from various container registries. // Lilipod 是一个简单的容器管理器，能够从各种容器镜像仓库下载、解压和使用 OCI 镜像。 /// Lilipod // 莉莉波德 /// Lilipod aims to be a very simple (as in few features) container and image manager. // Lilipod 的目标是成为一个非常简单（功能很少）的容器和镜像管理器。 /// Lilipod? // 微型脚类动物？ /// Sounds like little-pod, and I like it. // 听起来像小豆荚，我很喜欢。 /// So what does this manage? // 那么它能做到什么呢？ /// Lilipod is a very simple container manager with minimal features to: // Lilipod 是一个非常简单的容器管理器，功能极少，主要功能如下： /// - Download and manage images // 下载和管理图像 /// - Create and run containers // 创建并运行容器 /// It tries to keep a somewhat compatible CLI interface with Podman/Docker/Nerdctl // 它力求保持与 Podman/Docker/Nerdctl 相当兼容的 CLI 接口。 /// So are these containers like in Podman or Docker right? // 这些容器和 Podman 或 Docker 里的容器类似，对吧？ /// Well...superficially yes; Sure you have a separate user, mount (and optionally network, pid, ipc) namespaces, and the processes are in a pivotroot jail, but this does not manage anything else, so: // 嗯……表面上是这样；没错，你确实有单独的用户、挂载点（以及可选的网络、进程 ID、进程间通信）命名空间，而且进程位于 pivotroot jail 中，但这并不能管理其他任何东西，所以： /// - no seccomp // 无 seccomp /// - no capabilities // 无 capabilities /// - no cgroups // 无 cgroups /// If you need full blown containers, look no further than Podman or Nerdctl for your needs. // 如果您需要全功能容器， Podman (. gh: containers/podman.git) 或 Nerdctl (. gh: containerd/nerdctl.git) 就是您的最佳选择。 /// Goals // 目标 /// Lilipod wants to be: // Lilipod 想要成为： /// - nimble // 灵活 /// - single statically compiled binary // 单个静态编译二进制文件 /// - no external dependencies (as much as possible...more on this later) // 没有外部依赖（尽可能少……稍后会详细说明） /// - Follow Podman command and flags name when possible, try also to match the output // 尽可能遵循 Podman 命令和标志名称，并尽量匹配输出结果。 /// This tool does not aim to be a full replacement for Podman, Docker, Nerdctl or similar tools // 此工具并非旨在完全替代 Podman、Docker、Nerdctl 或类似工具。 /// Ok but why was it created? // 好吧，但它当初为什么会被创造出来呢？ /// Well, I felt the need to go deeper in how to download a container image from a registry... then one thing lead to another... and here we are. 👀 // 嗯，我当时觉得有必要深入研究一下如何从镜像仓库下载容器镜像……然后就顺着这个思路一步步发展下去……最后就有了这篇文章。👀 /// Also this is a nice fallback for Distrobox when no container-manager is found, or when it's not possible to install one. // 此外，当找不到容器管理器或无法安装容器管理器时，这也是 Distrobox (. gh: 89luca89/distrobox.git) 的一个不错的备用方案。 /// Being a fully self-contained binary that lives in one directory (LILIPOD_HOME) it makes it easy to install and remove without package managers. // 由于它是一个完全独立的二进制文件，位于一个目录（ LILIPOD_HOME ）中，因此无需软件包管理器即可轻松安装和卸载。 /// Limitations // 局限性 /// - by nature this tool does not use stuff like overlayfs so there is no deduplication between container's rootfs, but image layer deduplication is present // 由于该工具本身不使用 overlayfs 之类的技术，因此容器根文件系统之间不存在去重 ，但镜像层存在去重。 /// - There is no custom networking, you either share host's network or you're offline // 没有自定义网络设置，要么共享主机网络，要么离线。"
[knowsby]: https://github.com/89luca89/distrobox.git "... /// Use any Linux distribution inside your terminal. Enable both backward and forward compatibility with software and freedom to use whatever distribution you’re more comfortable with. Distrobox uses podman, docker or lilipod to create containers using the Linux distribution of your choice. The created container will be tightly integrated with the host, allowing sharing of the HOME directory of the user, external storage, external USB devices and graphical apps (X11/Wayland), and audio. // 在您的终端中使用任何 Linux 发行版。启用与软件的向后和向前兼容性，并自由使用您更舒适的发行版。Distrobox 使用 podman、docker 或 lilipod 来创建您选择的 Linux 发行版的容器。创建的容器将与主机紧密集成，允许共享用户的 HOME 目录、外部存储、外部 USB 设备和图形应用程序（X11/Wayland）以及音频。 /// ..."
