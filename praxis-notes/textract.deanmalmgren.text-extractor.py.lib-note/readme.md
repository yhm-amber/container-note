[src/gh]: https://github.com/deanmalmgren/textract.git "(MIT) (Languages: HTML 77.4%, Rich Text Format 12.4%, Python 9.2%, Shell 0.5%, Makefile 0.2%, PostScript 0.2%, Dockerfile 0.1%) extract text from any document. no muss. no fuss. // 从任何文档中提取文本。不费事。不麻烦。 /// Originally written by @deanmalmgren. Maintained by the good people at @jazzband Jazzband (. jazzband.co) // 最初由 @deanmalmgren 编写。由 @jazzband Jazzband 人员维护。"
[docs/rtd]: https://textract.readthedocs.io/ "(: import textract) (: text = textract.process('path/to/file.ext') # simple use ;: text = textract.process('path/to/a.pdf', method='pdfminer') # specify a particular method for parsing a pdf ;: text = textract.process('path/to/file.extension', encoding='ascii') # specify a particular output encoding. input encodings are inferred using gh:chardet/chardet.git ;: text = textract.process('path/to/file', extension='docx') # if file name has no extension specify like this ;: text = textract.process('path/to/norwegian.pdf', method='tesseract', language='nor') # use Tesseract OCR extract from Norwegian PDF.)"
[pkg.pip/pypi]: https://pypi.org/project/textract/ "(: pip install -- textract)  (src: gh:deanmalmgren/textract.git)"
[knows_by]: https://github.com/microsoft/markitdown.git "MarkItDown is a lightweight Python utility for converting various files to Markdown for use with LLMs and related text analysis pipelines. To this end, it is most comparable to textract (. gh: deanmalmgren/textract.git), but with a focus on preserving important document structure and content as Markdown (including: headings, lists, tables, links, etc.) While the output is often reasonably presentable and human-friendly, it is meant to be consumed by text analysis tools -- and may not be the best option for high-fidelity document conversions for human consumption. // MarkItDown 是一个轻量级的 Python 工具，用于将各种文件转换为 Markdown，以用于 LLMs 和相关的文本分析管道。为此，它与 textract 最相似，但专注于将重要文档结构和内容保留为 Markdown（包括：标题、列表、表格、链接等）。虽然输出通常足够可用且对人类友好，但它旨在被文本分析工具消费——可能不是用于人类消费的高保真文档转换的最佳选项。"

