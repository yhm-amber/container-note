[src/gh]: https://github.com/lusingander/serie.git "(MIT) (Languages: Rust 100.0%) A rich git commit graph in your terminal, like magic 📚 // 在您的终端中，丰富的 git 提交图，就像魔法 📚 /// Serie (/zéːriə/) is a TUI application that uses the terminal emulators' image display protocol to render commit graphs like git log --graph --all. // Serie ( /zéːriə/ ) 是一个 TUI 应用程序，它使用终端模拟器的图像显示协议来渲染提交图，就像 git log --graph --all 一样。 /// Why? // 为什么？ ///: While some users prefer to use Git via CLI, they often rely on a GUI or feature-rich TUI to view commit logs. Others may find git log --graph sufficient. // 虽然一些用户更喜欢通过命令行使用 Git，但他们通常依赖 GUI 或功能丰富的 TUI 来查看提交日志。其他人可能会发现 git log --graph 足够了。 ///; Personally, I found the output from git log --graph difficult to read, even with additional options. Learning complex tools just to view logs seemed cumbersome. // 我个人觉得 git log --graph 的输出难以阅读，即使有额外的选项。为了查看日志而学习复杂工具似乎很麻烦。 /// Goals // 目标 ///: - Provide a rich git log --graph experience in the terminal. // 在终端中提供丰富的 git log --graph 体验。 /// - Offer commit graph-centric browsing of Git repositories. // 提供以提交图为中心的 Git 仓库浏览功能。 /// Non-Goals // 非目标 ///: - Implement a fully-featured Git client. // 实现一个功能齐全的 Git 客户端。 /// - Create a TUI application with a complex UI. // 创建一个具有复杂界面的 TUI 应用程序。 /// - Works in any terminal environment. // 在任何终端环境中都能工作。"
[pkg.x/x-cmd]: https://x-cmd.com/install/serie "(: xd alp -- serie) (: x install -- serie)"
[pkg.brew/homebrew]: https://formulae.brew.sh/formula/serie "(: brew install -- serie) (: brew install -- lusingander/tap/serie)"
[pkg.cargo/crates.io]: https://crates.io/crates/serie "(: cargo install --locked -- serie) (MIT) (6.6K SLoC) (1.03 MiB)"
