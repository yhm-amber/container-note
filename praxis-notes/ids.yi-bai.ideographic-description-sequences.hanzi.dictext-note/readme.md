[src/gh]: https://github.com/yi-bai/ids.git "(MIT) (Languages: TEXT 100%) Yet another IDS (Ideographic Description Sequences) lists with MIT license // 又一个采用 MIT 许可证的 IDS（表意描述序列）列表 /// Contents // 内容 /// ids_lv0.txt: All differences in strokes are differentiated. // ids_lv0.txt：区分所有笔画差异。 /// ids_lv1.txt: Stroke-level differences (e.g. 丶 and 乀 in certain position) are merged. // ids_lv1.txt：笔画级别差异（例如某些位置的 ө 和 乀）已合并。 /// ids_lv2.txt: Part of variants defined in UCV list (e.g. 户戸戶), which considered as “must unifiable” are merged. // ids_lv2.txt：UCV 列表中定义的部分变体（例如户户）被认为是“必须统一的”，这些变体将被合并。 /// Format of files // 文件格式 /// Each file contains 3 columns: first column is character, second and third column each contains a semi-colon seperated list of IDS sequences. Each sequence is followed by some comma-seperated indicators within parentheses, indicating variant of the sequence. The sequences in second column and third column are considered as “primary IDS” and “alternative IDSes” respectively. // 每个文件包含三列：第一列为字符，第二列和第三列分别包含以分号分隔的 IDS 序列列表。每个序列后都跟着一些用逗号分隔的括号内的指示符，用于指示该序列的变体。第二列和第三列中的序列分别被视为“主要 IDS”和“替代 IDS”。"

