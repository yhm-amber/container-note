[mono.src/gh]: https://github.com/badlogic/pi-mono.git "(MIT) (Languages: TypeScript 96.5%, JavaScript 2.2%, Other 1.3%) AI agent toolkit: coding agent CLI, unified LLM API, TUI & web UI libraries, Slack bot, vLLM pods // AI agent toolkit: 编码代理 CLI、统一 LLM API、TUI 和 Web UI 库、Slack 机器人、vLLM 容器 /// ## Pi Monorepo // Pi 单一仓库 /// Tools for building AI agents and managing LLM deployments. // 构建 AI 代理和管理 LLM 部署的工具。 /// ## Packages // 软件包 /// - @mariozechner/pi-ai	 ///: Unified multi-provider LLM API (OpenAI, Anthropic, Google, etc.) // 统一的多提供商 LLM API（OpenAI、Anthropic、Google 等） /// - @mariozechner/pi-agent-core	 ///: Agent runtime with tool calling and state management // 具有工具调用和状态管理的代理运行时 /// - @mariozechner/pi-coding-agent	 ///: Interactive coding agent CLI // 交互式编码代理 CLI /// - @mariozechner/pi-mom	 ///: Slack bot that delegates messages to the pi coding agent // 将消息委托给 pi 编码代理的 Slack 机器人 /// - @mariozechner/pi-tui	 ///: Terminal UI library with differential rendering // 具有差异渲染的终端 UI 库 /// - @mariozechner/pi-web-ui	 ///: Web components for AI chat interfaces // 用于 AI 聊天界面的 Web 组件 /// - @mariozechner/pi-pods	 ///: CLI for managing vLLM deployments on GPU pods // 用于在 GPU pod 上管理 vLLM 部署的 CLI"
[site/dev]: https://pi.dev/ "(pi.dev domain graciously donated by // pi.dev 域名由热心人士慷慨捐赠) There are many coding agents, but this one is mine. // 有很多编程代理，但这个是我的。 /// ## About // 关于 /// Why pi? // 为何 pi？ /// Pi is a minimal terminal coding harness. Adapt pi to your workflows, not the other way around. Extend it with TypeScript extensions, skills, prompt templates, and themes. Bundle them as pi packages and share via npm or git. // Pi 是一个极简终端编程框架。适配 pi 到你的工作流程，而不是反过来。通过 TypeScript 扩展、技能、提示模板和主题来扩展它。将它们打包为 pi 包，并通过 npm 或 git 分享。 /// Pi ships with powerful defaults but skips features like sub-agents and plan mode. Ask pi to build what you want, or install a package that does it your way. // Pi 带有强大的默认设置，但会跳过子代理和计划模式等特性。请告诉 pi 按照你的需求构建，或者安装一个能按你的方式工作的软件包。 /// Four modes: interactive, print/JSON, RPC, and SDK. See clawdbot for a real-world integration. // 四种模式：交互式、打印/JSON、RPC 和 SDK。请参考 clawdbot 了解实际应用案例。 /// ## Integration // 集成 /// Four modes // 四种模式 /// - Interactive: The full TUI experience. // 交互式：完整的 TUI 体验。 /// - Print/JSON: pi -p \"query\" for scripts, --mode json for event streams. // 打印/JSON： pi -p \"query\" 用于脚本， --mode json 用于事件流。 /// - RPC: JSON protocol over stdin/stdout for non-Node integrations. See docs/rpc.md. // RPC：通过 stdin/stdout 的 JSON 协议，用于非 Node 集成。参见 docs/rpc.md。 /// - SDK: Embed pi in your apps. See clawdbot for a real-world example. // SDK：将 pi 嵌入您的应用程序。参见 clawdbot 获取实际案例。 /// ## Philosophy // 哲学 /// What we didn't build // 我们未构建的内容 /// Pi is aggressively extensible so it doesn't have to dictate your workflow. Features that other tools bake in can be built with extensions, skills, or installed from third-party pi packages. This keeps the core minimal while letting you shape pi to fit how you work. // Pi 具有高度可扩展性，因此它不必强制规定你的工作流程。其他工具内置的功能可以通过扩展、技能或从第三方 pi 软件包中安装来构建。这使核心保持简洁，同时让你能够根据你的工作方式来定制 pi。 /// - No MCP. Build CLI tools with READMEs (see Skills), or build an extension that adds MCP support. Why? // 没有 MCP。使用 README 构建 CLI 工具（参见 Skills），或构建一个添加 MCP 支持的扩展。为什么？ /// - No sub-agents. There's many ways to do this. Spawn pi instances via tmux, or build your own with extensions, or install a package that does it your way. // 没有子代理。有多种方法可以实现。可以通过 tmux 启动 pi 实例，或者使用扩展自行构建，或者安装一个符合你需求的软件包。 /// - No permission popups. Run in a container, or build your own confirmation flow with extensions inline with your environment and security requirements. // 没有权限弹窗。可以在容器中运行，或者使用扩展根据你的环境和安全要求构建自己的确认流程。 /// - No plan mode. Write plans to files, or build it with extensions, or install a package. // 没有计划模式。可以将计划写入文件，或者使用扩展构建，或者安装一个软件包。 /// - No built-in to-dos. Use a TODO.md file, or build your own with extensions. // 没有内置的待办事项。使用 TODO.md 文件，或使用扩展程序构建自己的待办事项列表。 /// - No background bash. Use tmux. Full observability, direct interaction. // 没有后台 bash。使用 tmux。完全可观察性，直接交互。 /// - Read the blog post for the full rationale. // 阅读博客文章以了解完整的理由。 /// ## Extensions // 扩展 /// Primitives, not features // 基础类型，而非功能 /// Features that other agents bake in, you can build yourself. Extensions are TypeScript modules with access to tools, commands, keyboard shortcuts, events, and the full TUI. // 其他代理内置的功能，您可以自行构建。扩展是具有访问工具、命令、键盘快捷键、事件和完整 TUI 的 TypeScript 模块。 /// Sub-agents, plan mode, permission gates, path protection, SSH execution, sandboxing, MCP integration, custom editors, status bars, overlays. Yes, Doom runs. // 子代理、计划模式、权限门、路径保护、SSH 执行、沙箱、MCP 集成、自定义编辑器、状态栏、覆盖层。是的，Doom 可以运行。 /// Don't want to build it? Ask pi to build it for you. Or install a package that does it your way. See the 50+ examples. // 不想构建？让 pi 为你构建。或者安装一个按你的方式执行的包。查看 50+ 个示例。 /// ## Queuing // 排队 /// Steer or follow up // 引导或跟进 /// Submit messages while the agent works. Enter sends a steering message (delivered after current tool, interrupts remaining tools). Alt+Enter sends a follow-up (waits until the agent finishes). // 在代理工作期间提交消息。 Enter 发送一个转向消息（在当前工具之后传递，中断剩余的工具）。 Alt+Enter 发送一个后续消息（等待代理完成）。 /// ## Context // 上下文 /// Context engineering // 上下文工程 /// Pi's minimal system prompt and extensibility let you do actual context engineering. Control what goes into the context window and how it's managed. // Pi 的最小系统提示和可扩展性使您能够进行实际的上下文工程。控制输入上下文窗口的内容以及如何管理它。 /// - AGENTS.md: Project instructions loaded at startup from ~/.pi/agent/, parent directories, and the current directory. // AGENTS.md：项目指令在启动时从 ~/.pi/agent/ 、父目录和当前目录加载。 /// - SYSTEM.md: Replace or append to the default system prompt per-project. // SYSTEM.md：按项目替换或追加默认系统提示。 /// - Compaction: Auto-summarizes older messages when approaching the context limit. Fully customizable via extensions: implement topic-based compaction, code-aware summaries, or use different summarization models. // 压缩：在接近上下文限制时自动总结旧消息。可通过扩展完全自定义：实现基于主题的压缩、代码感知摘要，或使用不同的摘要模型。 /// - Skills: Capability packages with instructions and tools, loaded on-demand. Progressive disclosure without busting the prompt cache. See skills. // 技能：具有说明和工具的能力包，按需加载。渐进式披露而不破坏提示缓存。参见技能。 /// - Prompt templates: Reusable prompts as Markdown files. Type /name to expand. See prompt templates. // 提示模板：可重用的提示，以 Markdown 文件形式存在。输入 /name 可展开。查看提示模板。 /// - Dynamic context: Extensions can inject messages before each turn, filter the message history, implement RAG, or build long-term memory. // 动态上下文：扩展可以在每轮之前注入消息，过滤消息历史，实现 RAG，或构建长期记忆。 /// ## Sessions // 会话 /// Tree-structured, shareable history // 树状结构，可共享的历史记录 /// Sessions are stored as trees. Use /tree to navigate to any previous point and continue from there. All branches live in a single file. Filter by message type, label entries as bookmarks. // 会话以树状结构存储。使用 /tree 可导航到任何先前点并从该点继续。所有分支都存在于一个文件中。按消息类型筛选，将条目标记为书签。 /// Export to HTML with /export, or upload to a GitHub gist with /share and get a shareable URL that renders it. // 使用 /export 导出为 HTML，或使用 /share 上传到 GitHub gist，并获取一个可共享的 URL 以渲染它。 /// ## Providers & Models // 供应方与模型 /// 15+ providers, hundreds of models // 15+ 供应方，数百个模型 /// Anthropic, OpenAI, Google, Azure, Bedrock, Mistral, Groq, Cerebras, xAI, Hugging Face, Kimi For Coding, MiniMax, OpenRouter, Ollama, and more. Authenticate via API keys or OAuth. // Anthropic、OpenAI、Google、Azure、Bedrock、Mistral、Groq、Cerebras、xAI、Hugging Face、Kimi For Coding、MiniMax、OpenRouter、Ollama 等。通过 API 密钥或 OAuth 进行身份验证。 /// Switch models mid-session with /model or Ctrl+L. Cycle through your favorites with Ctrl+P. // 在会话中途切换模型使用 /model 或 Ctrl+L 。使用 Ctrl+P 循环切换您最喜欢的模型。 /// Add custom providers and models via models.json or extensions. // 通过 models.json 或扩展添加自定义供应方与模型。"
[coding-agent.cli.npm/npmjs]: https://npmjs.com/package/@mariozechner/pi-coding-agent "(: npm i -- @mariozechner/pi-coding-agent) (License: MIT) (Unpacked Size: 9.23 MB) (Total Files: 599)  (src: gh: badlogic/pi-mono.git)"
[hub/.site]: https://pi.dev/packages "Packages // 包 /// Extensions, skills, themes, and more from the community. // 来自社区的扩展、技能、主题等。"
[intro.blog/lucumr.pocoo]: https://lucumr.pocoo.org/2026/1/31/pi/ "Pi: The Minimal Agent Within OpenClaw // Pi：OpenClaw 中的最小代理 /// In order to understand what’s in Pi, it’s even more important to understand what’s not in Pi, why it’s not in Pi and more importantly: why it won’t be in Pi. The most obvious omission is support for MCP. There is no MCP support in it. While you could build an extension for it, you can also do what OpenClaw does to support MCP which is to use mcporter. mcporter exposes MCP calls via a CLI interface or TypeScript bindings and maybe your agent can do something with it. Or not, I don’t know :) // 要理解 Pi 中包含的内容，理解其缺失的内容、原因以及更重要的是，为什么它将不会包含这些内容，这一点尤为重要。最明显的缺失是 MCP 的支持。它不包含 MCP 支持。虽然你可以为它构建一个扩展，但你也可以像 OpenClaw 支持 MCP 那样做，即使用 mcporter。mcporter 通过 CLI 接口或 TypeScript 绑定暴露 MCP 调用，也许你的代理可以用它做些什么。或者不，我不知道 :) /// And this is not a lazy omission. This is from the philosophy of how Pi works. Pi’s entire idea is that if you want the agent to do something that it doesn’t do yet, you don’t go and download an extension or a skill or something like this. You ask the agent to extend itself. It celebrates the idea of code writing and running code. // 这不是一个懒惰的缺失。这是 Pi 工作方式的哲学。Pi 的整个理念是，如果你希望代理执行它目前不执行的任务，你不需要去下载扩展或技能之类的。你应该要求代理自我扩展。它推崇编写和运行代码的理念。 /// That’s not to say that you cannot download extensions. It is very much supported. But instead of necessarily encouraging you to download someone else’s extension, you can also point your agent to an already existing extension, say like, build it like the thing you see over there, but make these changes to it that you like. // 这并不是说你不能下载扩展。下载扩展是被大力支持的。但与其鼓励你下载别人的扩展，你也可以让你的代理指向一个已有的扩展，比如，像你看到的那样构建它，但进行你喜欢的这些更改。 /// Agents Built for Agents Building Agents // 为代理而构建的代理 /// When you look at what Pi and by extension OpenClaw are doing, there is an example of software that is malleable like clay. And this sets certain requirements for the underlying architecture of it that are actually in many ways setting certain constraints on the system that really need to go into the core design. // 当你观察 Pi 以及 OpenClaw 所做的事情时，你会看到一个像黏土一样可塑的软件示例。而这为它的底层架构提出了一定的要求，实际上在许多方面为系统设置了一定的限制，这些限制确实需要在核心设计中加以考虑。 /// So for instance, Pi’s underlying AI SDK is written so that a session can really contain many different messages from many different model providers. It recognizes that the portability of sessions is somewhat limited between model providers and so it doesn’t lean in too much into any model-provider-specific feature set that cannot be transferred to another. // 因此，例如，Pi 的底层 AI SDK 被设计成可以真正包含来自许多不同模型提供者的多种不同消息。它认识到会话在不同模型提供者之间的可移植性是有限的，因此它不会过度依赖任何无法迁移到另一个提供者的模型提供者特定功能集。 /// The second is that in addition to the model messages it maintains custom messages in the session files which can be used by extensions to store state or by the system itself to maintain information that either not at all is sent to the AI or only parts of it. // 第二个是，除了维护模型消息外，它还在会话文件中维护自定义消息，这些消息可以被扩展用来存储状态，或者被系统本身用来维护那些要么根本不发送给 AI，要么只发送部分的信息。 /// Because this system exists and extension state can also be persisted to disk, it has built-in hot reloading so that the agent can write code, reload, test it and go in a loop until your extension actually is functional. It also ships with documentation and examples that the agent itself can use to extend itself. Even better: sessions in Pi are trees. You can branch and navigate within a session which opens up all kinds of interesting opportunities such as enabling workflows for making a side-quest to fix a broken agent tool without wasting context in the main session. After the tool is fixed, I can rewind the session back to earlier and Pi summarizes what has happened on the other branch. // 由于该系统存在且扩展状态也可以持久化到磁盘，因此它内置了热重载功能，以便代理可以编写代码、重新加载、测试，并形成一个循环，直到您的扩展实际上能够正常工作。它还附带文档和示例，代理本身可以使用这些来扩展自身。更好的是：Pi 中的会话是树形结构。您可以在会话中进行分支和导航，这为各种有趣的机会打开了大门，例如启用工作流，以便在不浪费主会话上下文的情况下修复损坏的代理工具。工具修复后，我可以将会话回退到更早的时间点，Pi 会总结其他分支上发生的事情。 /// This all matters because for instance if you consider how MCP works, on most model providers, tools for MCP, like any tool for the LLM, need to be loaded into the system context or the tool section thereof on session start. That makes it very hard to impossible to fully reload what tools can do without trashing the complete cache or confusing the AI about how prior invocations work differently. // 这一切都很重要，因为例如，如果你考虑 MCP 的工作方式，在大多数模型提供者中，用于 MCP 的工具（就像用于 LLM 的任何工具一样），需要在会话开始时加载到系统上下文或其工具部分中。这使得在不清除完整缓存或让 AI 混淆先前调用如何不同的情况下，完全重新加载工具能做什么变得非常困难甚至不可能。 /// Tools Outside The Context // 上下文之外的工具 /// An extension in Pi can register a tool to be available to the LLM to call and every once in a while I find this useful. For instance, despite my criticism of how Beads is implemented, I do think that giving an agent access to a to-do list is a very useful thing. And I do use an agent-specific issue tracker that works locally that I had my agent build itself. And because I wanted the agent to also manage to-dos, in this particular case I decided to give it a tool rather than a CLI. It felt appropriate for the scope of the problem and it is currently the only additional tool that I’m loading into my context. // 在 Pi 中，一个扩展可以注册一个工具，使其可供 LLM 调用，而且我偶尔觉得这很有用。例如，尽管我批评了 Beads 的实现方式，但我确实认为给代理访问待办事项列表是非常有用的。而且我确实使用了一个特定于代理的本地工作问题追踪器，让我代理自己构建。因为我希望代理也能管理待办事项，在这个特定情况下，我决定给它一个工具而不是一个 CLI。这感觉适合问题的范围，而且目前这是我加载到我的上下文中的唯一额外工具。 /// But for the most part all of what I’m adding to my agent are either skills or TUI extensions to make working with the agent more enjoyable for me. Beyond slash commands, Pi extensions can render custom TUI components directly in the terminal: spinners, progress bars, interactive file pickers, data tables, preview panes. The TUI is flexible enough that Mario proved you can run Doom in it. Not practical, but if you can run Doom, you can certainly build a useful dashboard or debugging interface. // 但基本上，我所添加到我的智能体中的内容要么是技能，要么是 TUI 扩展，目的是让与智能体的交互更加愉快。除了命令行指令之外，Pi 扩展可以直接在终端中渲染自定义 TUI 组件：旋转器、进度条、交互式文件选择器、数据表格、预览窗格。TUI 足够灵活，Mario 已经证明你可以在其中运行 Doom。虽然不实用，但如果你能运行 Doom，那么你当然可以构建一个有用的仪表板或调试界面。"
