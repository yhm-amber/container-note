[src/gh]: https://github.com/vercel-labs/zerolang.git "(~ gh: vercel-labs/zero.git) (Apache-2.0) (Languages: C 73.3%, JavaScript 16.9%, TypeScript 4.8%, Shell 3.0%, C++ 2.0%) The programming language for agents // 代理智能体的编程语言 /// zerolang /// Experimental programming language for agent workflows. // 用于代理工作流程的实验性编程语言。 /// Built for reliable autonomous software generation. // 专为可靠的自主软件生成而构建。 /// Optimized for: // 针对以下方面进行了优化： /// - Token efficiency // 代币效率 /// - Low memory usage // 内存占用低 /// - Fast startup // 快速启动 /// - Fast builds // 快速构建 /// - Low runtime latency // 低运行时延迟 /// - Zero dependencies // 零依赖"
[site/ai]: https://zerolang.ai/ "The programming language for agents // 智能体代理的编程语言 /// zerolang explores what a programming language can look like when agents are primary users from day one. The aim is a language that is easy to learn on the fly, deterministic to inspect and repair, standard-library first, and explicit enough that most tasks have one obvious path. // zerolang 探索了一种编程语言在从一开始就以智能体为主要用户的情况下会是什么样子。其目标是开发一种易于快速学习、易于检查和修复、优先使用标准库，并且足够明确，以至于大多数任务都有清晰的执行路径的语言。 /// (: npm exec -- skills add -g -- git@github.com:vercel-labs/zero.git) /// The current toolchain is useful for exploration, but today's syntax and APIs are not a contract. Expect breaking changes while zerolang searches for what works best for agents. Run it in a safe environment, not against production systems. // 当前的工具链对探索很有帮助，但目前的语法和 API 并非最终定稿。在 zerolang 探索最适合代理的方案期间，可能会出现一些重大变更。请在安全的环境中运行，不要在生产系统上运行。 /// - Learnable (on demand) // 可学习的 (一经请求) ///: [Small surface area // 小表面积] zerolang is aiming for a language an agent can learn while working: regular syntax, few special cases, and compiler feedback that points toward the next edit. // zerolang 的目标是开发一种智能体可以在工作中学习的语言：规则的语法、少量的特殊情况以及指向下一步编辑的编译器反馈。 /// - Library (first) // 图书馆 (第一的) ///: [Fewer dependency searches // 减少依赖关系搜索] The long-term goal is a standard library broad and consistent enough that most programs start with documented APIs, not package selection. // 长远目标是建立一个足够广泛和一致的标准库，使大多数程序都能从有文档的 API 开始，而不是从选择软件包开始。 /// - Inspectable (by tools) // 可检查的 (按工具) ///: [Deterministic repair loops // 确定性修复循环] The toolchain is intended to expose diagnostics, graphs, size reports, explanations, and repair plans as structured data agents can consume. // 该工具链旨在以结构化数据代理可以使用的形式公开诊断信息、图表、大小报告、解释和修复计划。"

