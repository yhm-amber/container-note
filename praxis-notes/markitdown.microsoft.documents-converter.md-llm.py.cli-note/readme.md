[src/gh]: https://github.com/microsoft/markitdown.git "(MIT) (Languages: Python 99.4%, Dockerfile 0.6%) Python tool for converting files and office documents to Markdown. // 用于将文件和办公文档转换为 Markdown 的 Python 工具。 /// MarkItDown now offers an MCP (Model Context Protocol) server for integration with LLM applications like Claude Desktop. See markitdown-mcp (. gh:microsoft/markitdown.git@main/packages/markitdown-mcp) for more information. // MarkItDown 现在提供了一个 MCP（模型上下文协议）服务器，用于与 Claude Desktop 等 LLM 应用程序集成。有关更多信息，请参阅 markitdown-mcp。 /// MarkItDown is a lightweight Python utility for converting various files to Markdown for use with LLMs and related text analysis pipelines. To this end, it is most comparable to textract (. gh: deanmalmgren/textract.git), but with a focus on preserving important document structure and content as Markdown (including: headings, lists, tables, links, etc.) While the output is often reasonably presentable and human-friendly, it is meant to be consumed by text analysis tools -- and may not be the best option for high-fidelity document conversions for human consumption. // MarkItDown 是一个轻量级的 Python 工具，用于将各种文件转换为 Markdown，以用于 LLMs 和相关的文本分析管道。为此，它与 textract 最相似，但专注于将重要文档结构和内容保留为 Markdown（包括：标题、列表、表格、链接等）。虽然输出通常足够可用且对人类友好，但它旨在被文本分析工具消费——可能不是用于人类消费的高保真文档转换的最佳选项。 /// MarkItDown currently supports the conversion from: // MarkItDown 目前支持从以下格式进行转换： /// - PDF /// - PowerPoint /// - Word /// - Excel /// - Images (EXIF metadata and OCR) // 图片（EXIF 元数据和 OCR） /// - Audio (EXIF metadata and speech transcription) // 音频（EXIF 元数据和语音转录） /// - HTML /// - Text-based formats (CSV, JSON, XML) // 基于文本的格式（CSV，JSON，XML） /// - ZIP files (iterates over contents) // ZIP 文件（遍历内容） /// - Youtube URLs /// - EPubs // 电子书 /// - ... and more! /// Why Markdown? // 为什么使用 Markdown？ /// Markdown is extremely close to plain text, with minimal markup or formatting, but still provides a way to represent important document structure. Mainstream LLMs, such as OpenAI's GPT-4o, natively \"speak\" Markdown, and often incorporate Markdown into their responses unprompted. This suggests that they have been trained on vast amounts of Markdown-formatted text, and understand it well. As a side benefit, Markdown conventions are also highly token-efficient. // Markdown 非常接近纯文本，具有极少的标记或格式，但仍提供了一种表示重要文档结构的方式。主流的 LLMs，如 OpenAI 的 GPT-4o，原生“会说” Markdown，并且经常在未提示的情况下将其包含在它们的响应中。这表明它们在大量格式化的 Markdown 文本上进行了训练，并且理解得很好。作为额外的优势，Markdown 的规范也非常高效地使用 token。 /// At the moment, the following optional dependencies are available: // 目前，以下可选依赖项可用： /// - [all] Installs all optional dependencies // [all] 安装所有可选依赖项 /// - [pptx] Installs dependencies for PowerPoint files // [pptx] 为 PowerPoint 文件安装依赖项 /// - [docx] Installs dependencies for Word files // [docx] 为 Word 文件安装依赖项 /// - [xlsx] Installs dependencies for Excel files // [xlsx] 为 Excel 文件安装依赖项 /// - [xls] Installs dependencies for older Excel files // [xls] 为旧版 Excel 文件安装依赖项 /// - [pdf] Installs dependencies for PDF files // [pdf] 为 PDF 文件安装依赖项 /// - [outlook] Installs dependencies for Outlook messages // [outlook] 为 Outlook 消息安装依赖项 /// - [az-doc-intel] Installs dependencies for Azure Document Intelligence // [az-doc-intel] 安装 Azure Document Intelligence 的依赖项 /// - [audio-transcription] Installs dependencies for audio transcription of wav and mp3 files // [audio-transcription] 安装用于 wav 和 mp3 文件音频转录的依赖项 /// - [youtube-transcription] Installs dependencies for fetching YouTube video transcription // [youtube-transcription] 安装用于获取 YouTube 视频转录的依赖项"
[pkg.pip/pypi]: https://pypi.org/project/markitdown/ "(: pip install -- markitdown[all]) (: git clone git@github.com:microsoft/markitdown.git && (cd markitdown && pip install -e -- packages/markitdown[all]) # install from source) (: markitdown path-to-file.pdf > document.md # Command-Line Usage) (: from markitdown import MarkItDown ;: md = MarkItDown() ;: res = md.convert('test.xlsx') ;: print(res.text_content) # Python API Usage) (License Expression: MIT) (Requires: Python >=3.10) (Provides-Extra: [all], [audio-transcription], [az-doc-intel], [docx], [outlook], [pdf], [pptx], [xls], [xlsx], [youtube-transcription]) Utility tool for converting various files to Markdown // 用于将各种文件转换为 Markdown 的工具 (src: gh:microsoft/markitdown.git)"
[knowsby]: https://ragnar.tidyverse.org/ "ragnar works with a wide variety of document types, using MarkItDown (. gh: microsoft/markitdown.git) to convert content to Markdown. // ragnar 支持多种文档类型，使用 MarkItDown 将内容转换为 Markdown。"

