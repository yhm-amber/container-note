[src/gh]: https://github.com/buresdv/Cork.git "(“Commons Clause” License Condition v1.0) (Languages: Swift 99.0%, HTML 1.0%) A fast GUI for Homebrew written in SwiftUI // 一个用 SwiftUI 编写的快速 Homebrew GUI /// Special Thanks // 特别感谢 /// I'd like to personally thank [Dmitri Bouniol] (. gh: @dimitribouniol) and [Ben Carlsson] (. blog: web:twos.dev) for coming up with a way for self-compiled builds to bypass the license check. // 我想特别感谢 Dmitri Bouniol 和 Ben Carlsson 提出了一种方法，使自编译构建可以绕过许可证检查。 /// Without them, it would be impossible to have a free self-compiled version of the app. // 没有它们，就不可能拥有一个免费的自行编译版本的应用程序。 /// Cork is not just an interface for Homebrew. It has many features that are either very hard to accomplish using Homebrew alone, or straight-up not possible. // Cork 不仅仅是一个 Homebrew 的界面。它拥有许多仅使用 Homebrew 难以实现或根本不可能实现的功能。 /// Things that are not possible without Cork // 没有 Cork 就不可能实现的事情 /// - Automatically respecting system proxy. // 自动尊重系统代理。 /// - Clearing of cached downloads. // 清除缓存的下载。 /// - Updating packages from the Menu Bar without having Cork open. // 无需打开 Cork 即可从菜单栏更新软件包。 /// - Seeing this much info about a package in one convenient location. // 在一个方便的位置查看有关软件包的这些信息。 /// - Tagging packages. This is a Cork-only feature that lets you mark any number of packages you'd like to keep track of. // 标记包。这是 Cork 独有的功能，允许你标记任何你想要跟踪的包。 /// Things that Cork makes easier // Cork 使以下事情更简单 /// - Listing of installed packages. Cork has its own way of loading packages, which is around 10 times faster than the Homebrew implementation. // 列出已安装的包。Cork 有自己的加载包的方式，比 Homebrew 的实现快约 10 倍。 /// - Knowing which packages you installed intentionally, and which packages were installed only as dependencies. While somewhat possible with the brew leaves command, it is often unreliable, often not listing packages that should be included. // 了解哪些是你有意安装的软件包，哪些只是作为依赖项安装的软件包。虽然使用 brew leaves 命令在一定程度上是可行的，但它通常不可靠，经常不列出应该包含的软件包。 /// - Updating of only selected packages. Again, while possible with Homebrew alone, Cork makes it so easy you wouldn't believe it is not this simple in Homebrew itself. // 仅更新选定的包。再次强调，虽然单独使用 Homebrew 也可能实现，但 Cork 让这一切变得如此简单，你简直不会相信 Homebrew 本身并不这么简单。 /// - Showing you exactly which packages a package is a dependency of. Super annoying in Homebrew, effortless with Cork. // 明确显示某个包是哪些包的依赖项。在 Homebrew 中非常烦人，但在 Cork 中毫不费力。 /// - Effortlessly managing Homebrew services with a simple click of a button in a beautiful sub-window. // 通过在美观的子窗口中单击按钮，轻松管理 Homebrew 服务。 /// - And many other features! Just try Cork out and try finding them all 😉 // 还有许多其他功能！快来试试 Cork，并发现它们所有 😉 /// License // 许可证 /// Cork is licensed under Commons Clause. // Cork 在 Commons Clause 下授权。 /// This means that Cork's source source is available and you can modify it, contribute to it etc., but you can't sell or distribute Cork or modified versions of it. // 这意味着 Cork 的源代码是开放的，你可以修改它、为其做贡献等，但你不能销售或分发 Cork 或其修改版本。 /// Moreover, you can’t distribute compiled versions of Cork without consulting me first. Compiling versions for your personal use is fine. // 此外，未经事先咨询我，你不能分发 Cork 的编译版本。为个人使用编译版本是可以的。"
[site/app]: https://corkmac.app/ "The Ultimate Homebrew Companion // 终极 Homebrew 伴侣 /// With Cork, you can say goodbye to the Terminal. // 有了 Cork，你可以告别终端。 /// Cork is the best Homebrew interface available // Cork 是目前最好的 Homebrew 界面 /// No alternative even comes close // 没有其他替代品能接近它"


