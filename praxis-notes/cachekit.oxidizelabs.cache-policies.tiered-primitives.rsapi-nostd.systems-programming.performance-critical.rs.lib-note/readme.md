[src/gh]: https://github.com/OxidizeLabs/cachekit.git "(Apache-2.0, MIT) (Languages: Rust 99.5%, Shell 0.5%) High-performance cache policies (FIFO/LRU/LRU-K) and tiered caching primitives for Rust systems, with optional metrics and benchmarks. // 高性能缓存策略（FIFO/LRU/LRU-K）和用于 Rust 系统的分层缓存原语，支持可选的指标和基准测试。 /// High-performance cache policies and tiered caching primitives for Rust systems with optional metrics and benchmarks. // 为 Rust 系统提供高性能缓存策略和分层缓存原语，可选的指标和基准测试。 /// Overview // 概述 /// CacheKit is a Rust library that provides: // CacheKit 是一个 Rust 库，提供： /// - High-performance cache replacement policies (e.g., FIFO, LRU, LRU-K). // 高性能缓存替换策略（例如，FIFO、LRU、LRU-K）。 /// - Tiered caching primitives to build layered caching strategies. // 用于构建分层缓存策略的分层缓存原语。 /// - Optional metrics and benchmark harnesses. // 可选的指标和基准测试框架。 /// - A modular API suitable for embedding in systems where control over caching behavior is critical. // 一个模块化的 API，适用于需要严格控制缓存行为的系统。 /// This crate is designed for systems programming, microservices, and performance-critical applications.  这个库是为系统编程、微服务和高性能关键应用设计的。 /// Features // 特性 /// - Policy implementations optimized for performance and predictability. // 针对性能和可预测性优化的策略实现。 /// - Backends that support both in-memory and composite cache strategies. // 支持内存和复合缓存策略的后端。 /// - Optional integration with metrics collectors (e.g., Prometheus/metrics crates). // 可选集成指标收集器（例如 Prometheus/metrics crates）。 /// - Benchmarks to compare policy performance under real-world workloads. // 用于在真实工作负载下比较策略性能的基准测试。 /// - Idiomatic Rust API with no_std compatibility where appropriate. // 符合规范的 Rust API，在适当情况下与 no_std 兼容。"
[site/ghio]: https://oxidizelabs.github.io/cachekit/ "Welcome to the CacheKit documentation site. // 欢迎来到 CacheKit 文档网站。"
[lib.cargo/crates]: https://crates.io/crates/cachekit "(: cargo add -- cachekit) (MIT OR Apache-2.0) (16K SLoC) (231 KiB) High-performance, policy-driven cache primitives for Rust systems (FIFO/LRU/ARC) with optional metrics. // 为 Rust 系统提供高性能、策略驱动的缓存原语（FIFO/LRU/ARC），支持可选的指标。 (src: gh:OxidizeLabs/cachekit.git)"

