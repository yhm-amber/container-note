[chat.apk/f-droid]: https://f-droid.org/packages/com.newoether.agora/ "(51 MiB) (MIT) BYOK LLM client with multi-provider access, agentic workflows, remote control // BYOK LLM 客户端，支持多供应商接入、智能体工作流和远程设备控制"
[chat.src/gh]: https://github.com/newo-ether/Agora.git "(MIT) (Languages: Kotlin 97.5%, C++ 1.5%, Other 1.0%) Android BYOK LLM client with multi-provider access, agentic workflows, and remote device control. // Android BYOK LLM 客户端，支持多提供商访问、代理工作流和远程设备控制。 /// Agora — a BYOK Android client for AI power users. Connect to 8+ built-in providers (plus unlimited custom endpoints) with your own keys, branch conversations non-linearly, run models locally via llama.cpp, and control remote machines through encrypted shell. Everything stored on-device, nothing logged elsewhere. Open source, MIT licensed. // Agora 是一款面向 AI 高级用户的自带密钥 (BYOK) 安卓客户端。它支持使用您自己的密钥连接到 8 个以上的内置提供商（以及无限量的自定义端点），支持非线性分支对话，支持通过 llama.cpp 在本地运行模型，并支持通过加密 shell 控制远程机器。所有数据都存储在设备本地，不会记录到其他任何地方。Agora 开源，采用 MIT 许可证。 /// Why Agora? // 为什么选择 Agora？ /// - No Middlemen: Direct API connections. No telemetry, no tracking, no corporate servers logging your conversations. Everything lives locally in a Room database. // 无中间商： 直接 API 连接。无遥测数据，无追踪，没有企业服务器记录您的对话。所有数据都存储在本地 Room 数据库中。 /// - Non-Linear Thought: A tree-structured message database lets you edit any past message, regenerate responses, and explore alternative branches without losing context. // 非线性思维： 树状结构的消息数据库允许您编辑任何过去的消息、重新生成回复，并在不丢失上下文的情况下探索其他分支。 /// - Agentic by Default: Multi-round tool calling with web search, code execution, remote file operations, memory management, and semantic conversation search. // 默认采用代理模式： 多轮工具调用，支持网络搜索、代码执行、远程文件操作、内存管理和语义对话搜索。 /// - Remote Control: Manage servers, edit files, and search code on remote machines via the Conch protocol — end-to-end encrypted with ECDH + AES-256-GCM. // 远程控制： 通过 Conch 协议管理服务器、编辑文件和搜索远程计算机上的代码——采用 ECDH + AES-256-GCM 进行端到端加密。 /// ## Features // 特征 /// ### Multi-Provider Access // 多提供者接入 /// - 8 built-in providers: OpenAI, Anthropic, Google Gemini, DeepSeek, Qwen (DashScope), OpenRouter, Ollama, Local (GGUF via llama.cpp) // 8 个内置提供商： OpenAI、Anthropic、Google Gemini、DeepSeek、Qwen（DashScope）、OpenRouter、Ollama、Local（GGUF，通过 llama//.cpp） /// - Unlimited custom providers with arbitrary base URLs and API keys // 支持无限数量的自定义提供商 ，可任意设置基本 URL 和 API// 密钥 /// - BYOK: Bring your own API keys — no subscriptions, no middlemen // BYOK： 自带 API// 密钥——无需订阅，无需中间商 /// - Multiple API keys per provider with named aliases for easy rotation // 每个提供商可使用多个 API 密钥，// 并为其指定别名以便于轮换。 /// - Per-provider base URL override for proxies and self-hosted endpoints // 针对代理和自托管端点的提供商基本 URL 覆盖 /// ### Agentic Tools // 代理工具 /// - Web Search — DuckDuckGo Lite (anonymous, no key), Brave, Serper, Tavily, and SearXNG integration // 网络搜索 ——DuckDuckGo Lite（匿名，无需密钥）、Brave、Serper、Tavily 和 SearXNG 集成 /// - Code Execution — Gemini code execution for running and testing code inline; Alpine Linux sandbox via PRoot with SAF file access // 代码执行 ——Gemini 代码执行，用于在线运行和测试代码；通过 PRoot 使用 Alpine Linux 沙箱和 SAF 文件访问 /// - Image Generation — BYOK text-to-image via OpenAI-compatible /v1/images/generations, rendered inline in chat // 图像生成 ——通过兼容 OpenAI 的 /v1/images/generations 实现 BYOK 文本转图像功能，并在聊天窗口中内联渲染 /// - Remote Shell & File I/O — Execute commands, read/write/edit/glob/grep files on remote servers via Conch // 远程 Shell 和文件 I/O — 通过 Conch 在远程服务器上执行命令、读取/写入/编辑/glob/grep 文件。 /// - Memory — Persistent active memory and saved memory files across conversations // 记忆 ——跨对话的持久活动记忆和已保存的记忆文件 /// - Conversation Search — RAG-powered semantic search over chat history // 对话搜索 ——基于 RAG 算法的聊天历史记录语义搜索 /// ### Thinking & Reasoning // 思考与推理 /// - Deep reasoning: OpenAI o1/o3, Anthropic extended thinking, Gemini thinking, DeepSeek-R1, Qwen QwQ // 深度推理：OpenAI o1/o3、Anthropic 扩展思维、Gemini 思维、DeepSeek-R1、Qwen QwQ /// - Configurable thinking level (low/medium/high) // 可配置的思维水平（低/中/高） /// - Streaming think-tag renderer with collapsible UI and duration tracking // 具有可折叠用户界面和持续时间跟踪功能的流式 Think-Tag 渲染器 /// ### On-Device Intelligence // 设备端智能 /// - Local LLM inference via llama.cpp — run GGUF models entirely offline // 通过 llama.cpp 进行本地 LLM 推理 ——完全离线运行 GGUF 模型 /// - Local embeddings for on-device semantic search (RAG) // 用于设备端语义搜索的本地嵌入 （RAG） /// - Ollama provider for self-hosted models on your local network // Ollama 是您本地网络上自托管模型的提供商 /// ### Remote Device Control (Conch Protocol) // 远程设备控制（海螺协议） /// - ECDH key exchange + AES-256-GCM encryption + HMAC-SHA256 signing // ECDH 密钥交换 + AES-256-GCM 加密 + HMAC-SHA256 签名 /// - Token bucket rate limiting and nonce-based anti-replay protection // 令牌桶速率限制和基于随机数的防重放保护 /// - Multi-device support — configure and switch between multiple remote servers // 支持多设备 ——配置和切换多个远程服务器 /// - MCP integration — Conch as a Claude Desktop MCP server // MCP 集成 — Conch 作为 Claude Desktop MCP 服务器 /// ### Knowledge Management // 知识管理 /// - RAG semantic search across all past conversations using cosine similarity // 使用余弦相似度对所有历史对话进行 RAG 语义搜索 /// - Configurable similarity threshold and keyword/model search methods // 可配置的相似度阈值和关键词/模型搜索方法 /// - Selectable embedding model (remote or local), independent of chat model // 可选择的嵌入模型（远程或本地），独立于聊天模型 /// - Context window management with real-time token counting and sliding window // 上下文窗口管理， 支持实时词元计数和滑动窗口 /// - Visual context rollout indicator dims messages outside the active window // 视觉上下文展开指示器会使活动窗口外的消息变暗。 /// ### Data Portability // 数据可移植性 /// - .agora Export/Import: Conversations, memories, prompts, settings, and API keys in one portable file // .agora 导出/导入： 将对话、回忆、提示、设置和 API 密钥打包到一个可移植文件中 /// - Merge, Replace, and Skip import strategies // 合并、替换和跳过导入策略 /// - Auto Backup — periodic WorkManager-based backup with configurable period, categories, and retention // 自动备份 — 基于 WorkManager 的定期备份，可配置备份周期、类别和保留期限。 /// - Third-Party Import: Claude and ChatGPT export formats (.zip / .json) // 第三方导入： Claude 和 ChatGPT 导出格式（.zip / .json） /// - API key safety warnings for both export and import workflows // 导出和导入工作流程中的 API 密钥安全警告 /// ### Customization // 定制 /// - System prompt templates with three-section editor (system prompt + user prepend + user append) // 带有三段式编辑器（系统提示符 + 用户前置 + 用户后置） 的系统提示符模板 /// - Variable substitution: {sent_time}, {sent_date}, and extensible variable system // 变量替换： {sent_time} 、 {sent_date} 和可扩展变量系统 /// - Per-conversation model and system prompt switching // 按对话模式和系统提示切换 /// - Per-message model selection from the chat bottom bar // 可从聊天底部栏选择每条消息的模式 /// - Per-conversation generation overrides (temperature, max tokens, penalties) // 每个对话的生成覆盖（温度、最大令牌数、惩罚） /// - Auto title generation with configurable model // 使用可配置模型自动生成标题 /// ### UI & UX // 用户界面与用户体验 /// - Modern Material 3 design in Jetpack Compose with dynamic color (Material You) // Jetpack Compose 中的现代 Material 3 设计，具有动态色彩（Material You） /// - Light / Dark / System theme modes with configurable color schemes // 浅色/深色/系统主题模式，可配置配色方案 /// - Non-linear branching: Edit any past message and branch into alternative conversation paths // 非线性分支： 编辑任何过去的消息，并跳转到不同的对话路径 /// - Real-time streaming with message anchoring and animated auto-scrolling // 实时流媒体播放，支持消息锚定和动画自动滚动 /// - Haptic feedback throughout the UI (long-press, selection, success/error) // 用户界面全程提供触觉反馈（长按、选择、成功/错误） /// - Immersive gesture-driven image and media viewer // 沉浸式手势驱动图像和媒体查看器 /// - Markdown rendering with syntax highlighting, LaTeX math, and code blocks // Markdown 渲染（带语法高亮）、LaTeX 数学公式和代码块 /// - Image, video, PDF, and file attachment support with thumbnails // 支持图像、视频、PDF 和文件附件，并带有缩略图 /// - iOS-style collapsing large-title in settings with shared page transition animations // 设置中类似 iOS 的折叠式大标题，带有共享页面过渡动画 /// - Blur effects with configurable performance toggle // 模糊效果，并可配置性能开关 /// - English, Chinese, and Traditional Chinese language support // 支持英语、中文和繁体中文"
[ctrl.src/gh]: https://github.com/newo-ether/conch.git "(MIT) (Languages: Go 51.9%, PowerShell 26.3%, Shell 21.0%, Makefile 0.8%) A lightweight, zero-dependency shell execution server with SSE streaming. // 一个轻量级、零依赖的 shell 执行服务器，支持 SSE 流式传输。 /// conch // 海螺 /// A lightweight, zero-dependency shell execution server with SSE streaming and end-to-end encryption. Expose a shell on any device — Linux server, Termux (Android), Windows — and control it securely over the network. Ships with a companion MCP binary for Claude Desktop integration. // 一款轻量级、零依赖的 shell 执行服务器，支持 SSE 流式传输和端到端加密。可在任何设备（Linux 服务器、Termux（Android）、Windows）上提供 shell，并通过网络安全控制。附带用于 Claude Desktop 集成的 MCP 二进制文件。 /// Features // 特征 /// - End-to-end encryption — X25519 ECDH key exchange + AES-256-GCM, authenticated by a pre-shared API key // 端到端加密 ——X25519 ECDH 密钥交换 + AES-256-GCM，通过预共享 API 密钥进行身份验证 /// - HMAC request signing — proves API key possession without transmitting it // HMAC 请求签名 ——无需传输 API 密钥即可证明其所有权 /// - Anti-replay protection — time-bucketed nonce tracking with ±5 min clock skew tolerance // 防重放保护 ——基于时间间隔的随机数跟踪，时钟偏差容差为±5 分钟 /// - SSE streaming — stdout and stderr delivered line-by-line // SSE 流式传输 ——逐行输出标准输出和标准错误输出 /// - Rate limiting — per-IP token bucket (20 req/s sustained, burst 40) // 速率限制 ——每个 IP 的令牌桶（持续 20 次请求/秒，突发 40 次请求/秒） /// - Single binary — zero dependencies, statically linked, ~8 MiB // 单个二进制文件 ——零依赖，静态链接，~8 MiB /// - Cross-platform — Linux (arm64/amd64), Windows (amd64), Termux on Android // 跨平台 ——Linux（arm64/amd64）、Windows（amd64）、Android 版 Termux /// - System service — installs as systemd (Linux), runit (Termux), or nssm (Windows) // 系统服务 ——可安装为 systemd（Linux）、runit（Termux）或 nssm（Windows） /// - MCP support — separate conch-mcp binary bridges Claude Desktop (stdio/JSON-RPC) to a remote Conch server // MCP 支持 ——独立的 conch-mcp 二进制文件桥接 Claude Desktop（stdio/JSON-RPC）与远程 Conch 服务器"
