[core.src/gh]: https://github.com/project-robius/robius.git "(MIT) (Languages: Rust 97.8%, Java 2.2%) Multi-platform Rust abstractions of platform-provided APIs and OS services // 跨平台的 Rust API 和操作系统服务的抽象"
[cargoext-packaging-cli.src/gh]: https://github.com/project-robius/robius-packaging-commands.git "(MIT) (Languages: Rust 100.0%) A multi-platform companion tool to help package your Rust app when using `cargo-packager` // 一个跨平台辅助工具，用于在使用 `cargo-packager` 打包您的 Rust 应用程序时提供帮助"
[cargoext-packaging-cli.cargo/crates]: https://crates.io/crates/robius-packaging-commands "(: cargo install --locked -- robius-packaging-commands) (: rustup update stable ;: cargo +stable install --force --locked -- cargo-packager # Install cargo-packager is necessary first.) (MIT) (398 SLoC) (9.92 KiB) A multi-platform companion tool to help package your Rust app when using `cargo-packager`. This should be invoked by cargo-packager's \"before-package\" and \"before-each-package\" hooks, which you specify in your `Cargo.toml` file under the `[package.metadata.packager]` section. // 一个跨平台辅助工具，用于在使用 `cargo-packager` 打包 Rust 应用时提供支持。它应该由 `cargo-packager` 的 \"before-package\" 和 \"before-each-package\" 钩子调用，这些钩子你在 `Cargo.toml` 文件的 `[package.metadata.packager]` 部分进行指定。 (src: gh:project-robius/robius-packaging-commands.git)"
[makepad-autodepends-lib.src/gh]: https://github.com/project-robius/robius-use-makepad.git "(MIT) (Languages: Rust 100.0%) Auto-configures all `robius-*` crates in your dependency tree to work with Makepad // 自动配置您依赖树中的所有 `robius-*` 包以与 Makepad 一起工作 /// If you're building a Makepad app that uses any robius-* crates, simply add this to your Cargo.toml to ensure that those robius-* crates are configured correctly to expect being used from within a Makepad app. // 如果你正在构建一个使用任何 robius-* crate 的 Makepad 应用，只需将此内容添加到你的 Cargo.toml 中，以确保这些 robius-* crate 被正确配置为期望在 Makepad 应用程序中使用。 /// That's all you need to do! // 这就是你需要做的全部！ /// More details // 更多详情 /// This crate exists as a convenient way to auto-configure all other robius-* crates in your dependency tree to work with Makepad. It doesn't have any code -- all it does is enable the makepad feature in the robius-android-env (. rs-crate: robius-android-env) crate, which is an \"internal\" crate that many other robius-* crates depend on. // 这个 crate 存在是为了方便地自动配置你的依赖树中所有其他 robius-* crate 以与 Makepad 一起工作。它没有任何代码——它所做的一切只是启用 makepad crate 中的 robius-android-env 功能，这是一个许多其他 robius-* crate 都依赖的 “内部” crate。 /// Simply depending on this crate will auto-configure all Robius crates to work with Makepad. // 只需依赖这个 crate，就会自动配置所有 Robius crate 以与 Makepad 一起工作。"
[makepad-autodepends-lib.cargo/crates]: https://crates.io/crates/robius-use-makepad "(: cargo add -- robius-use-makepad) (MIT) (24.8 KiB) Auto-configures all `robius-*` crates in your dependency tree to work with Makepad // 自动配置您依赖树中的所有 `robius-*` crate 以与 Makepad 一起工作 (src: gh:project-robius/robius-use-makepad.git)"
[site/rs]: https://robius.rs/ "Robius is a fully open-source, decentralized, community-driven effort to enable multi-platform application development in Rust. // Robius 是一个完全开源、去中心化、社区驱动的项目，旨在支持在 Rust 语言中进行跨平台应用开发。 /// Real apps/projects using Robius // 使用 Robius 的真实应用/项目 /// - Robrix: the Robius Matrix chat client. (. gh: project-robius/robrix.git) // Robrix：Robius 矩阵聊天客户端。 /// - - Supports Matrix chat, with more fediverse services to come! // 支持矩阵聊天，未来将支持更多 fediverse 服务！ /// - Moxin: a local explorer for open-source LLMs. (. gh: project-robius/moxin.git, gh: moly-ai/moly-ai.git) // Moxin：开源 LLMs 的本地探索器。 /// - - Allows running LLMs locally and conversing with an AI Chatbot for each model, using the WasmEdge runtime as an LLM backend runtime. // 允许在本地运行 LLMs，并与每个模型的 AI 聊天机器人进行对话，使用 WasmEdge 运行时作为 LLM 后端运行时。"
[demos/.site]: https://wasm.robius.rs/ "Makepad WASM Applications // Makepad WASM 应用程序 /// For best experience, view on narrow or mobile screen // 为获得最佳体验，请在窄屏或移动屏幕上查看"
[book.src/gh]: https://github.com/project-robius/book.git "(MIT) The Robius book: details about our vision for multi-platform app dev in Rust, plus docs, tutorials, examples, and more. // 《GitHub - project-robius/book: Robius 书籍：关于我们在 Rust 中进行多平台应用开发的愿景，还包括文档、教程、示例等。》 /// The book is written in Markdown and uses mdBook (. web: rust-lang-nursery.github.io/mdBook) to build a nicely-formatted HTML version of the book. // 这本书使用 Markdown 编写，并使用 mdBook 构建格式良好的 HTML 版本。"
[book/ghio]: https://project-robius.github.io/book/ "We believe that the Rust programming language is the right choice for the next generation of application developers, but that the language ecosystem needs a bit more love and attention to make it a first-class citizen in the world of application development, particularly on mobile platforms. // 我们相信，Rust 编程语言是下一代应用开发者的正确选择，但语言生态系统需要更多关爱和关注，才能成为应用开发领域的一流公民，特别是在移动平台。 /// We envision a future in which: // 我们憧憬一个未来： /// - Rust developers can create safe, beautiful, and robust applications that execute efficiently and performantly on a wide variety of platforms, especially mobile. // Rust 开发者能够创建安全、美观且健壮的应用，这些应用在各种平台上高效运行，尤其是在移动平台。 /// - Frontend developers using other languages are encouraged to come over to the dark side wonderful world of Rust and give it a try, with a seamless transition experience that helps overcome the steep learning curve commonly associated with Rust. // 使用其他语言的前端开发者被鼓励加入 Rust 这个奇妙的世界，并尝试使用它，同时提供无缝的过渡体验，帮助克服通常与 Rust 相关的陡峭学习曲线。 /// - The Rust ecosystem is broadened and strengthened, demonstrating to existing Rust experts in other domains that Rust is a great fit for application development, not just low-level systems and embedded programming. // Rust 生态系统得到了扩展和加强，向其他领域的现有 Rust 专家展示了 Rust 不仅适用于低级系统和嵌入式编程，还非常适合应用开发。 /// What's in a name? // 名字有什么意义？ /// The name Robius comes from the Latin word robius, meaning red in color, as in oxen, wheat, rust, etc. This makes for a nice color-based connection to Rust, our programming language of choice. // Robius 这个名字来源于拉丁语单词 robius，意为红色，如牛、小麦、锈等。这为 Rust（我们选择的编程语言）与颜色建立了一个美好的联系。 /// Robius rhymes with Mobius, and our logo/icons take inspiration from the wordplay combination of “Rust” and “Mobius strip”. // Robius 与 Mobius 押韵，我们的标志/图标从 “Rust” 和 “Mobius 带” 的词义组合中汲取灵感。 /// > Yes, technically, the original German name is Möbius, but we use an Americanized pronunciation with a long “o” sound: “Roe-bee-us” / ˈɹoʊˈbiəs. // 是的，技术上原来的德语名称是 Möbius，但我们使用美式发音，带长元音 “o” 的音： “Roe-bee-us” / ˈɹoʊˈbiəs。 /// Key Community Projects // 关键社区项目 /// The Robius ecosystem consists of several independent projects that can be composed into a complete system stack to realize fast, painless application development across multiple platforms in pure Rust. Components are loosely coupled, allowing a developer (in the future) to customize which components are used to comprise the underlying system, such as choosing // Robius 生态系统由几个独立的项目组成，这些项目可以组合成一个完整系统栈，以实现跨多个平台在纯 Rust 中的快速、无痛苦的应用开发。组件之间松散耦合，允许开发者（在未来）自定义使用哪些组件来构成底层系统，例如选择 /// - Makepad (. web: makepad.nl, gh: makepad/makepad.git) is a cross-platform UI toolkit currently under active development that offers a hybrid retained-mode and immediate-mode UI model. // Makepad 是一个跨平台 UI 工具包，目前正处于积极开发中，提供混合保留模式和即时模式的 UI 模型。 /// - - Rapid development cycle: very fast compile times due to a custom minimal dependency set, plus a custom DSL for live design that enables hot reloading of UI elements. // 快速开发周期：由于自定义的最小依赖集，编译时间非常快，再加上用于实时设计的自定义 DSL，可以启用 UI 元素的快速重新加载。 /// - - Makepad Studio: an IDE prototype built using Makepad itself with unique features like cross-process shared textures for live reload of an in-window UI app, docking tabs for file/window views, hyper-smooth code folding, and more. // Makepad Studio：一个使用 Makepad 本身构建的 IDE 原型，具有独特的功能，如跨进程共享纹理，用于窗口内 UI 应用的实时重新加载，文件/窗口视图的停靠标签，超平滑的代码折叠等。 /// - - Makepad framework: a (growing) collection of highly-performant widgets and minimal, zero/low-overhead platform abstractions. // Makepad 框架：一个（不断增长的）高性能组件集合和极简、零/低开销的平台抽象。 /// - Dioxus (. web: dioxuslabs.com, gh: dioxuslabs/dioxus.git) is a cross-platform, production-ready UI toolkit that is inspired by React, with a custom metalanguage called RSX that is used to declare UI elements/layout in an HTML-like style. // Dioxus 是一个跨平台、生产就绪的 UI 工具包，灵感来自 React，具有一个名为 RSX 的自定义元语言，用于以类似 HTML 的风格声明 UI 元素/布局。 /// - - Supports many platforms with a set of interchangeable target renderers, including desktop, webapps, static sites, text UIs, liveview, and mobile. // 支持多种平台，拥有一套可互换的目标渲染器，包括桌面、Web 应用、静态网站、文本 UI、实时视图和移动端。 /// - - Fast and memory-efficient, with perfect lighthouse scores and performance orders of magnitude better than Node or Python. // 快速且内存高效，拥有完美的 Lighthouse 评分，性能比 Node 或 Python 高出数个数量级。 /// - - Excellent built-in abstractions for state management. // 内置出色的状态管理抽象。 /// - - Easy, familiar styling using vanilla CSS or the CSS framework of your choice, e.g., Tailwind. // 使用纯 CSS 或您选择的 CSS 框架（例如 Tailwind）进行简单、熟悉的样式设计。 /// - Osiris (. gh: osiris-apis/osi.git, web: osiris.foo) is a set of Rust interfaces for developing immersive applications atop a diverse set of operating systems services and platform-specific functionality. // Osiris 是一套用于在多种操作系统服务和平台特定功能之上开发沉浸式应用的 Rust 接口。 /// - - Osiris aims to provide Rust apps with an easy canonical way to access platform features like storage, networking, multimedia (video, audio, camera), geolocation, device orientation (accelerometer, gyro), timers & alarms, notifications, clipboard, drag-n-drop, and more. // Osiris 旨在为 Rust 应用提供一个便捷的规范方式来访问平台功能，如存储、网络、多媒体（视频、音频、相机）、地理位置、设备方向（加速度计、陀螺仪）、计时器与闹钟、通知、剪贴板、拖放等。 /// - - osi: the primary Rust package that exposes direct access to OS interfaces. Higher-level Rust abstractions are coming soon, after raw interfaces for more platforms are complete. // osi : 主要的 Rust 包，提供直接访问操作系统接口。更高层次的 Rust 抽象将在更多平台的原始接口完成后推出。 /// - - Osiris offers build tooling that can: // Osiris 提供了构建工具，可以： /// - - 1. Set up new project directories with auto-generated scaffolding for platform-specific integration components that can be customized post-creation. // 设置新项目目录，使用自动生成的脚手架，用于平台特定的集成组件，这些组件可以在创建后进行定制。 /// - - 2. Generate application artifacts that adhere to the policies of each platform, i.e., packages that can be published to common app stores (Google Play, Apple App Store, Microsoft Store, etc). // 生成符合各平台政策的应用程序工件，即可以发布到通用应用商店（Google Play、Apple App Store、Microsoft Store 等）的包。 /// - ylong (. gh: openharmony/commonlibrary_rust_ylong_runtime.git) is an async runtime for Rust that includes: // ylong 是一个用于 Rust 的异步运行时，包括： /// - - Priority-based scheduling of tasks. // 基于优先级的任务调度。 /// - - Abstractions for auto-parallelizing computation, offering parallel iterators over data in a manner similar to rayon (. rs-crate: rayon), but for async tasks rather than native (OS-level) threads. // 用于自动并行化计算的抽象，提供类似于 rayon 的并行迭代器，但用于异步任务而非原生（操作系统级）线程。 /// - - Standard primitives for non-blocking async I/O, async synchronization, and async timers. // 用于非阻塞异步 I/O、异步同步和异步计时器的标准原语。 /// - - An executor and reactor to schedule tasks and respond to system events, respectively. // 一个执行器和反应器，分别用于调度任务和响应系统事件。"
[matrix]: https://matrix.to/#/#robius:matrix.org
[knows_by]: https://github.com/makepad/makepad.git "Robrix is a Matrix chat client written in Rust to demonstrate the functionality of Project Robius, a framework for multi-platform application development in Rust. Robrix is written using Makepad. // Robrix 是一个用 Rust 编写的 Matrix 聊天客户端，用于展示 Project Robius 的功能，Project Robius 是一个用于多平台应用程序开发的 Rust 框架。Robrix 是使用 Makepad 编写的。"
[robrix.src/gh]: https://github.com/project-robius/robrix.git "(MIT) (Languages: Rust 100.0%) Robrix: a multi-platform Matrix chat client written in Rust, using the Makepad UI toolkit and the Robius app dev framework // Robrix：一个用 Rust 编写的跨平台 Matrix 聊天客户端，使用 Makepad UI 工具包和 Robius 应用开发框架 /// (: cargo makepad android -- install-toolchain ;: cargo makepad android -- run -p robrix --release) (: cargo makepad apple ios -- install-toolchain ;: cargo makepad apple ios --org=rs.robius --app=robrix -- run-sim -p robrix --release ;: cargo makepad apple ios --profile=<unique-starting-hex-string> --cert=<UNIQUE_STARTING_HEX_STRING> --device=<UNIQUE-STARTING-HEX-STRING> --org=rs.robius --app=robrix -- run-device -p robrix --release #: using `cargo makepad apple list` on macos to get those hex-string.)"
[robrix.intro/gosim-china2024]: https://china2024.gosim.org/schedules/robrix--a-pure-rust-multi-platform-matrix-client-and-more "[Next Generation Internet // 下一代互联网] /// Robrix: a Pure Rust Multi-Platform Matrix Client and More // Robrix：一个纯 Rust 多平台 Matrix 客户端及更多 /// Speakers: Kevin Boos /// Date / Time: 2024-10-17 14:40 /// Robrix is a new Matrix chat client that aims to be a hub for federated services. By building atop the Robius app dev framework and the Makepad UI toolkit, Robrix works seamlessly across multiple platforms (including mobile) with no platform-specific code. // Robrix 是一款新的 Matrix 聊天客户端，旨在成为联邦服务的中心。通过基于 Robius 应用开发框架和 Makepad UI 工具包构建，Robrix 可以无缝地在多个平台（包括移动设备）上运行，无需平台特定的代码。"

