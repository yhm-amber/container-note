[site/org]: https://bsonspec.org/ "BSON, short for Bin­ary JSON (. web: json.org), is a bin­ary-en­coded seri­al­iz­a­tion of JSON-like doc­u­ments. Like JSON, BSON sup­ports the em­bed­ding of doc­u­ments and ar­rays with­in oth­er doc­u­ments and ar­rays. BSON also con­tains ex­ten­sions that al­low rep­res­ent­a­tion of data types that are not part of the JSON spec. For ex­ample, BSON has a Date type and a BinData type. // BSON，全称为二进制 JSON，是一种二进制编码的 JSON 类文档序列化格式。与 JSON 类似，BSON 支持在文档和数组中嵌入其他文档和数组。BSON 还包含扩展功能，允许表示 JSON 规范中未包含的数据类型。例如，BSON 具有日期类型和二进制数据类型。 /// BSON can be com­pared to bin­ary inter­change for­mats, like Proto­col Buf­fers (. web: code.google.com/p/protobuf). BSON is more \"schema-less\" than Proto­col Buf­fers, which can give it an ad­vant­age in flex­ib­il­ity but also a slight dis­ad­vant­age in space ef­fi­ciency (BSON has over­head for field names with­in the seri­al­ized data). // BSON 可以与二进制交换格式（如 Protocol Buffers）进行比较。与 Protocol Buffers 相比，BSON 更加 \"无模式\" ，这使其在灵活性方面具有优势，但也使其在空间效率方面略有劣势（BSON 在序列化数据中为字段名增加了开销）。 /// BSON was de­signed to have the fol­low­ing three char­ac­ter­ist­ics: // BSON 被设计为具有以下三个特性： /// - Lightweight // 轻量级 ///: Keep­ing spa­tial over­head to a min­im­um is im­port­ant for any data rep­res­ent­a­tion format, es­pe­cially when used over the net­work. // 将空间开销降至最低对任何数据表示格式都很重要，尤其是在网络中使用时。 /// - Traversable // 可遍历 ///: BSON is de­signed to be tra­versed eas­ily. This is a vi­tal prop­erty in its role as the primary data rep­res­ent­a­tion for Mon­goDB (. web: mongodb.org). // BSON 设计得易于遍历。这是它在作为 MongoDB 主要数据表示格式中的关键特性。 /// - Efficient // 高效 ///: En­cod­ing data to BSON and de­cod­ing from BSON can be per­formed very quickly in most lan­guages due to the use of C data types. // 由于使用了 C 数据类型，在大多数语言中对数据编码为 BSON 和解码 BSON 都非常快速。"
[spec/.site]: https://bsonspec.org/spec.html "BSON is a binary format in which zero or more ordered key/value pairs are stored as a single entity. We call this entity a document. // BSON 是一种二进制格式，其中零个或多个有序的键/值对作为单个实体存储。我们将这个实体称为文档。"
[impls/.site]: https://bsonspec.org/implementations.html "Implementations // 实现 /// Implementations of BSON exist for many different programming languages. Some implementations are currently embedded within official MongoDB drivers (. web: mongodb.com/docs/drivers), since MongoDB was the first large project to make use of BSON. Over time those libraries will be made more stand-alone, but they should be usable independently of MongoDB in their current state. // BSON 已有适用于多种不同编程语言的实现。一些实现目前嵌入在官方 MongoDB 驱动程序中，因为 MongoDB 是第一个使用 BSON 的大型项目。随着时间的推移，这些库将变得更加独立，但它们目前应该可以独立于 MongoDB 使用。"
[impl-assemblyscript.src/gh]: https://github.com/nearprotocol/assemblyscript-bson.git "(MIT) (Languages: TypeScript 99.1%, JavaScript 0.9%) BSON encoder / decoder for AssemblyScript // AssemblyScript 的 BSON 编码器 / 解码器 /// BSON encoder / decoder for AssemblyScript somewhat based on gh:mpaland/bsonfy.git . // AssemblyScript 的 BSON 编码器/解码器，部分基于 gh:mpaland/bsonfy.git 。 /// Special thanks to gh:MaxGraey/bignum.wasm.git for basic unit testing infra for AssemblyScript. // 特别感谢 gh:MaxGraey/bignum.wasm.git 为 AssemblyScript 提供基本单元测试基础设施。"
[impl-typescript.src/gh]: https://github.com/mpaland/bsonfy.git "(MIT) (Languages: TypeScript 99.3%, JavaScript 0.7%) Ultrafast BSON typescript serializer and parser // 超高速 BSON TypeScript 序列化和解析器"
[impl-rust.src/gh]: https://github.com/mongodb/bson-rust.git "(MIT) (Languages: Rust 98.4%, Perl 1.1%, Shell 0.5%) Encoding and decoding support for BSON in Rust // Rust 中的 BSON 编码和解码支持"
[impl-rust.lib.cargo/crates]: https://crates.io/crates/bson "(: cargo add -- bson) (MIT) (14K SLoC) (134 KiB) Encoding and decoding support for BSON in Rust // Rust 中的 BSON 编码和解码支持 (src: gh:mongodb/bson-rust.git)"
[impl-prolog.src/gh]: https://github.com/khueue/prolog-bson.git "(MIT) (Languages: Gnuplot 58.2%, Prolog 34.0%, C 6.7%, Makefile 1.1%) BSON Parser for Prolog // Prolog 的 BSON 解析器 /// Used by the Prolog MongoDB driver prolongo (. gh: khueue/prolongo.git). /// Prolog MongoDB 驱动程序 prolongo 使用该解析器。 /// Coding Guidelines // 编码规范 /// - Use empty imports (use_module(mymodule, [])) in order to not pollute the namespace. // 使用空导入（use_module(mymodule, []）），以避免污染命名空间。 /// - Always use module prefixes (mymodule:predicate(...)) in order to clarify where things are coming from. // 始终使用模块前缀（mymodule:predicate(...)），以明确来源。 /// - Always use the \"made-up\" module prefix \"core:\" when calling built-in predicates. This is completely unnecessary, and doesn't even work in all cases, but I think it is a good idea as long as it doesn't cause any problems. This decision may need to be revised when compatibility between different Prologs is investigated. // 始终在调用内置谓词时使用 \"虚构\" 的模块前缀 \"core:\" 。这完全没必要，而且在所有情况下都不起作用，但我认为只要不引起任何问题，这是个好主意。当调查不同 Prolog 之间的兼容性时，这个决定可能需要重新修订。 /// - Avoid the if-then-else construct. It just looks ugly. // 避免使用 if-then-else 结构。它看起来就是很丑。 /// - Avoid disjunctions. They are ugly, and can be replaced by properly written helpers. Think: premises are \"and\", clauses are \"or\". // 避免使用析取。它们很丑，并且可以被正确编写的辅助函数替换。思考：前提是“与”，子句是“或”。 /// - Use cuts where appropriate, and try to keep each cut on a line by itself unless its placement is obvious and consistent in each clause. PlUnit is excellent at pointing out when tests succeed but leave choice points. // 在适当的地方使用断点，并尽量使每个断点单独成行，除非它在每个子句中的位置明显且一致。PlUnit 在指出测试成功但留下选择点时非常出色。 /// - Try to avoid spaces within lists and structures, but always use spaces between arguments. // 尽量避免列表和结构中的空格，但始终在参数之间使用空格。 /// - Predicates, atoms, etc. should use \"this_naming_style\" while variables should use \"ThisNamingStyle\". // 谓词、原子等应使用 \"this_naming_style\"，而变量应使用 \"ThisNamingStyle\"。 /// - Try to stick to the PlDoc structure. // 尽量遵循 PlDoc 结构。 /// - If in doubt, consult: web: ai.uga.edu/mc/plcoding.pdf // 如有疑问，请参考： web: ai.uga.edu/mc/plcoding.pdf"
[impl-julia.src/gh]: https://github.com/JuliaIO/BSON.jl.git "(MIT-Expat) (Languages: Julia 100.0%) BSON.jl is a Julia package for working with the Binary JSON serialisation format. It can be used as a general store for Julia data structures, with the following features: // BSON.jl 是一个用于处理二进制 JSON 序列化格式的 Julia 包。它可以用作 Julia 数据结构的通用存储，具有以下功能： /// - Lightweight and ubiquitous, with a simple JSON-like data model and clients in many languages. // 轻量级且通用，具有简单的 JSON 类似数据模型和多种语言的客户端。 /// - Efficient for binary data (eg. arrays of floats). // 对二进制数据高效（例如浮点数数组）。 /// - Flexible enough to handle anything you throw at it – closures, custom types, circular data structures, etc. // 足够灵活，可以处理任何你抛给它的东西——闭包、自定义类型、循环数据结构等。 /// - Backwards compatible, so that if data layout changes old files will still load. // 向后兼容，因此如果数据布局发生变化，旧文件仍然可以加载。"
[impl-haskell.src/gh]: https://github.com/mongodb-haskell/bson.git "(Apache-2.0) (Languages: Haskell 100.0%) Haskell representation and serialization of BSON documents (version 1.0) as defined at bsonspec.org // Haskell 中的 BSON 文档表示和序列化（版本 1.0），如 bsonspec.org 中定义 /// Haskell library for the encoding and decoding BSON documents, which are JSON-like objects with a standard binary encoding, defined at bsonspec.org. This library implements version 1 of that spec. // 一个用于编码和解码 BSON 文档的 Haskell 库，BSON 文档是类似于 JSON 的对象，具有标准的二进制编码，定义在 bsonspec.org 上。该库实现了该规范的 1.0 版本。 /// A BSON Document is an untyped (dynamically type-checked) record. I.e. it is a list of name-value pairs, where a Value is a single sum type with constructors for basic types (Bool, Int, Float, String, and Time), compound types (List, and (embedded) Document), and special types (Binary, Javascript, ObjectId, RegEx, and a few others). // 一个 BSON 文档是一个无类型的（动态类型检查）记录。也就是说，它是一个名称-值对的列表，其中值是一个单一的联合类型，具有基本类型（Bool、Int、Float、String 和 Time）的构造函数、复合类型（List 和（嵌入的）Document）以及特殊类型（Binary、Javascript、ObjectId、RegEx 和其他几个）。"
[impl-haskell.lib.cabal/hackage]: https://hackage.haskell.org/package/bson "(: cabal install -- bson) bson: BSON documents are JSON-like objects with a standard binary encoding. // bson: BSON 文档是具有标准二进制编码的类似 JSON 的对象。 (Dependencies	base (>=4.9.0.0 && <5), binary (>=0.5 && <0.9), bytestring, cryptohash-md5 (>=0.11 && <0.12), data-binary-ieee754, mtl (>=2), network (<2.9), network-bsd (>=2.7 && <2.9), text (>=0.11), time [details]) (License	Apache-2.0) (Copyright	Copyright (c) 2010-2012 10gen Inc.) (Author	Tony Hannan) (Maintainer	Fedor Gogolev <knsd@knsd.net>, Greg Weber <greg@gregweber.info>) (Category	Data) (Uploaded	by VictorDenisov at 2020-03-23T04:35:32Z) (Distributions	LTSHaskell:0.4.0.1, NixOS:0.4.0.1, Stackage:0.4.0.1)"
[impl-elixir:cyanide.src/gh]: https://github.com/secomind/cyanide.git '(forked from gh:checkiz/elixir-bson.git) (Apache-2.0) (Languages: Elixir 100.0%) BSON documents in Elixir language // Elixir 语言中的 BSON 文档 /// Cyanide is a BSON library for Elixir. // Cyanide 是一个用于 Elixir 的 BSON 库。 /// (: Cyanide.encode(%{"value" => 42}) #=> {:ok, <<16, 0, 0, 0, 16, 118, 97, 108, 117, 101, 0, 42, 0, 0, 0, 0>>} ;: Cyanide.decode(<<16, 0, 0, 0, 16, 118, 97, 108, 117, 101, 0, 42, 0, 0, 0, 0>>) #=> {:ok, %{"value" => 42}})'
[impl-elixir:cyanide.lib.mix/hexpm]: https://hex.pm/packages/cyanide "(: Mix.install([ {:cyanide} ])) (License: Apache-2.0) An Elixir BSON encoding/decoding library. // 一个 Elixir BSON 编码/解码库。 (src: gh:secomind/cyanide.git)"
[impl-dlang:vibe-d.src/gh]: https://github.com/vibe-d/vibe-serialization.git "(MIT) (Languages: D 99.8%, Shell 0.2%) Serialization and structured data format support // 序列化与结构化数据格式支持 /// vibe.d serialization and data package // vibe.d 序列化和数据包 /// This package contains a generic serialization system, as well as JSON and BSON format support. Additional serialization support for SDLang (. web: sdlang.org) is available via the vibe-sdlang (. gh: vibe-d/vibe-sdl.git) package. // 此包包含一个通用的序列化系统，以及 JSON 和 BSON 格式的支持。通过 vibe-sdlang 包，还可以获得对 SDLang 的序列化支持。"
[impl-dlang:vibe-d.api/vibed.org]: https://vibed.org/api/vibe.data.bson/ "Module vibe.data.bson /// BSON serialization and value handling. // BSON 序列化和值处理。"
[impl-dart.src/gh]: https://github.com/kseo/bson_codec.git "(BSD-3-Clause) (Languages: Dart 99.1%, Shell 0.9%) BSON serialization for Dart // Dart 的 BSON 序列化 /// This package offers Dart programs a flexible serializer and deserializer for BSON documents. // 该包为 Dart 程序提供了对 BSON 文档的灵活序列化和反序列化功能。 /// Highlights // 亮点 /// - Standard Codec/Converter interface, as established by the standard 'dart:convert' package. // 符合标准 'dart:convert' 包所定义的 Codec / Converter 接口。 /// - Best effort type mapping when serializing/deserializing values. // 序列化/反序列化值时，进行最佳努力类型映射。 /// (: import 'package:bson_codec/bson_codec.dart'; main () { final doc = { '_id': 5, 'a': [2, 3, 5] }; List<int> bytes = BSON.encode(doc); final root = BSON.decode(bytes); print(doc['a'][2]); } //> 5)"
[impl-dart.lib.dart/pub]: https://pub.dev/packages/bson_codec "(: dart pub add -- bson_codec) (BSD-3-Clause) (Dependencies: bson_objectid, collection) A BSON codec for Dart. // Dart 的 BSON 编解码器。"

