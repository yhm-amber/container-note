[src/gh]: https://github.com/jazzband/pathlib2.git "(MIT) (Languages: Python 100.0%) Backport of pathlib aiming to support the full stdlib Python API. // pathlib 的向后移植，旨在支持完整的 stdlib Python API。 /// The old pathlib (. web: web.archive.org/web/20181106215056/bitbucket.org/pitrou/pathlib) module on bitbucket is no longer maintained. The goal of pathlib2 is to provide a backport of standard pathlib (. web: docs.python.org/dev/library/pathlib.html) module which tracks the standard library module, so all the newest features of the standard pathlib can be used also on older Python versions. // Bitbucket 上的旧版 pathlib 模块已停止维护。pathlib2 的目标是提供一个标准 pathlib 模块的向后移植版本，该版本与标准库模块保持一致，因此标准 pathlib 的所有最新特性也可以在旧版本的 Python 上使用。"
[lib.pip/pypi]: https://pypi.org/project/pathlib2/ "(: pip install -- pathlib2) (License: MIT License (MIT)) Object-oriented filesystem paths // 面向对象的文件系统路径 (src: gh:jazzband/pathlib2.git)"
[docs/rtd]: https://pathlib2.readthedocs.io "Welcome to pathlib2’s documentation!"

