[src/gh]: https://github.com/rstudio/renv.git "(MIT) (Languages: R 98.5%, C 1.4%, Other 0.1%) renv: Project environments for R. // renv: 用于 R 的项目环境。 /// The renv package (~ Pronounced “R” “env”) helps you create reproducible environments for your R projects. Use renv to make your R projects more isolated, portable and reproducible. // renv 包 (~ 发音为 “R” “env”) 帮助您为 R 项目创建可重复的环境。使用 renv 使您的 R 项目更加隔离、便携和可重复。 /// - Isolated: Installing a new or updated package for one project won’t break your other projects, and vice versa. That’s because renv gives each project its own private library. // 隔离：为一个项目安装新的或更新的包不会破坏您的其他项目，反之亦然。这是因为 renv 为每个项目提供自己的私有库。 /// - Portable: Easily transport your projects from one computer to another, even across different platforms. renv makes it easy to install the packages your project depends on. // 便携：轻松将您的项目从一个计算机传输到另一个计算机，甚至跨不同平台。renv 使安装您的项目所依赖的包变得容易。 /// - Reproducible: renv records the exact package versions you depend on, and ensures those exact versions are the ones that get installed wherever you go. // 可重复性：renv 记录您所依赖的确切包版本，并确保无论您在哪里，安装的都是这些确切版本。"
[package/cran]: https://cran.r-project.org/web/packages/renv/ "(Version: 	1.1.5) (Imports: 	utils) (Suggests: 	BiocManager, cli, compiler, covr, cpp11, devtools, generics, gitcreds, jsonlite, jsonvalidate, knitr, miniUI, modules, packrat, pak, R6, remotes, reticulate, rmarkdown, rstudioapi, shiny, testthat, uuid, waldo, yaml, webfakes) (Published: 	2025-07-24) (DOI: 	10.32614/CRAN.package.renv) (Author: 	Kevin Ushey ORCID iD [aut, cre], Hadley Wickham ORCID iD [aut], Posit Software, PBC [cph, fnd]) (Maintainer: 	Kevin Ushey <kevin at rstudio.com>) (License: 	MIT + file LICENSE) (NeedsCompilation: 	no) renv: Project Environments // renv: 项目环境 /// A dependency management toolkit for R. Using 'renv', you can create and manage project-local R libraries, save the state of these libraries to a 'lockfile', and later restore your library as required. Together, these tools can help make your projects more isolated, portable, and reproducible. // 一个用于 R 语言的依赖管理工具包。使用'renv'，您可以创建和管理项目本地的 R 库，将这些库的状态保存到'lockfile'中，并在需要时恢复您的库。这些工具共同可以帮助使您的项目更加隔离、便携和可重复。 (src: gh:rstudio/renv.git)"
[package/r-universe:rstudio]: https://rstudio.r-universe.dev/renv "(: install.packages('renv', repos = 'https://rstudio.r-universe.dev'))"
[site/ghio]: https://rstudio.github.io/renv/ "Use renv::init() to initialize renv in a new or existing project. This will set up a project library, containing all the packages you’re currently using. The packages (and all the metadata needed to reinstall them) are recorded into a lockfile, renv.lock, and a .Rprofile ensures that the library is used every time you open that project. // 使用 renv::init() 在新项目或现有项目中初始化 renv。这将设置一个项目库，其中包含您当前正在使用的所有软件包。这些软件包（以及重新安装它们所需的全部元数据）将被记录到锁定文件 renv.lock 中，而 .Rprofile 确保每次打开该项目时都使用该库。 /// As you continue to work on your project, you will install and upgrade packages, either using install.packages() and update.packages() or renv::install() and renv::update(). After you’ve confirmed your code works as expected, use renv::snapshot() to record the packages and their sources in the lockfile. // 随着您继续进行项目工作，您将使用 install.packages() 和 update.packages() 或 renv::install() 和 renv::update() 来安装和升级软件包。在确认代码按预期工作后，使用 renv::snapshot() 在锁文件中记录软件包及其来源。 /// Later, if you need to share your code with someone else or run your code on new machine, your collaborator (or you) can call renv::restore() to reinstall the specific package versions recorded in the lockfile. // 稍后，如果您需要与他人分享您的代码或在新的机器上运行您的代码，您的协作者（或您自己）可以调用 renv::restore() 来重新安装 lockfile 中记录的特定包版本。"

