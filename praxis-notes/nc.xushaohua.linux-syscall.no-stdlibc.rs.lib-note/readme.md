[src/gh]: https://github.com/xushaohua/nc.git "(Apache-2.0) (Languages: Rust 99.0%, Other 1.0%) Run *nix system calls directly in Rust // 在 Rust 中直接运行 *nix 系统调用 /// Access system calls directly without std or libc. // 直接访问系统调用，无需 std 或 libc 。 /// Features: // 特性： /// - No glibc or musl required // 无需 glibc 或 musl /// - Access syscalls directly, via assembly // 直接通过汇编访问系统调用 /// - No global errno variable, every function returns an errno instead // 没有全局的 errno 变量，每个函数返回一个 errno /// - Support latest kernel APIs, like io-uring and pidfd, introduced in linux 5.0+ // 支持最新的内核 API，如 io-uring 和 pidfd，这些是在 linux 5.0+中引入的 /// Demo /// Kill self // 杀死自身 ///: (: let pid = unsafe { nc::getpid() } ;: let ret = unsafe { nc::kill(pid, nc::SIGTERM) } /* here killed */ ;: ... // Never reach here.)"
[lib.cargo/crates]: https://crates.io/crates/nc "(: cargo add -- nc) (Apache-2.0) (74K SLoC) (1,023 KiB) Access system calls directly // 直接访问系统调用 (src: gh:xushaohua/nc.git)"
