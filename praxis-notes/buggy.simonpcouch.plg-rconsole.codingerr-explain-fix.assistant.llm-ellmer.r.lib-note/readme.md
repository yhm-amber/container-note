[src/gh]: https://github.com/simonpcouch/buggy.git "(MIT) (Languages: R 100.0%) Explain and Address Error Messages // 解释和解决错误消息 /// (: ) (: options(.buggy_chat = ellmer::chat_vllm(...)) ;: buggy::buggy_enable(chat = getOption('.buggy_chat')) # default will be `getOption('.buggy_chat')` # Attaches a global error handler that captures errors and provides clickable options to explain or fix them.) /// (在 Enable 中指定 chat-obj ，默认读取 option 因此建议通过 option 在 Rprofile 中设定 back end LLM 。在 Enable 后会有按钮出现在 R Console 的错误提示内。点击解释或修复会针对此错误来解释 (will run: buggy::buggy_explain()) 或修复 (will run: buggy::buggy_fix()) 。)"
[site/ghio]: https://simonpcouch.github.io/buggy/ "buggy // 有缺陷的 /// The goal of buggy is to help users understand and address error messages using LLMs. With the tool enabled, errors raised to the user are accompanied by clickable links to “explain” or “fix” the issue. Explanations are printed to the console while fixes implement changes directly; in both cases, the model is supplied context about the files you’re working in and the functions you’re working with. // buggy 的目标是帮助用户使用 LLMs 理解和解决错误消息。启用该工具后，向用户显示的错误会附带可点击的链接，用于“解释”或“修复”问题。解释内容会打印到控制台，而修复操作会直接实施变更；在这两种情况下，模型都会获得关于您正在处理的文件和函数的上下文信息。 /// Troubleshoot errors with LLMs // 使用 LLMs 排查错误 /// The buggy package provides tools for automatically explaining and fixing R errors using large language models (LLMs). When an error occurs, buggy can analyze the error message, backtrace, and context to provide a human-friendly explanation and suggest fixes. // buggy 软件包提供了使用大型语言模型（LLMs）自动解释和修复 R 错误的工具。当发生错误时，buggy 可以分析错误消息、回溯和上下文，以提供人类友好的解释并建议修复方法。 /// - buggy_enable(): Attaches a global error handler that captures errors and provides clickable options to explain or fix them. // buggy_enable() : 连接一个全局错误处理器，捕获错误并提供可点击的选项来解释或修复它们。 /// - buggy_explain(): Explains the most recent error using an LLM, offering detailed context about what went wrong and why. // buggy_explain() : 使用 LLM 解释最新的错误，提供有关错误发生原因的详细上下文。 /// - buggy_fix(): Attempts to automatically fix the most recent error by generating and applying a code fix. If buggy can find the relevant file lines, it will modify the lines directly. Regardless, it will print the proposed fix out to the console. // buggy_fix() : 尝试自动修复最新的错误，通过生成并应用代码修复。如果 buggy 能找到相关文件行，它将直接修改这些行。无论如何，它都会将建议的修复打印到控制台。"
[knows_by]: https://github.com/simonpcouch/ "Some LLM-related experiments that didn't end up going far: // 一些未走远的 LLM 相关实验： /// - simonpcouch/buggy: automatically explain and address errors // simonpcouch/buggy: 自动解释和解决错误"
[knows_by]: https://simonpcouch.com/software "- buggy // 有问题的 /// Explain and Address Error Messages // 解释和处理错误消息"
