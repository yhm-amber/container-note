[src/gh]: https://github.com/oxipng/oxipng.git "(MIT) (Languages: Rust 99.2%, Other 0.8%) Multithreaded PNG optimizer written in Rust // 用 Rust 编写的多线程 PNG 优化器 /// Oxipng // 氧化 /// Oxipng is a multithreaded lossless PNG/APNG compression optimizer. It can be used via a command-line interface or as a library in other Rust programs. It is fast and highly effective. // Oxipng 是一个多线程无损 PNG/APNG 压缩优化器。它既可以通过命令行界面使用，也可以作为库集成到其他 Rust 程序中。它速度快、效率高。"
[cli.cargo/crates]: https://crates.io/crates/oxipng "(: cargo install -- oxipng) (MIT) (4.9K SLoC) (74.8 KiB) A lossless PNG compression optimizer // 无损 PNG 压缩优化器 (src: gh:oxipng/oxipng.git)"
