[src/gh]: https://github.com/zserge/jsmn.git "(MIT) (Languages: C 97.4%, Makefile 2.6%) Jsmn is a world fastest JSON parser/tokenizer. This is the official repo replacing the old one at Bitbucket // Jsmn 是世界上速度最快的 JSON 解析器/分词器。这是官方仓库，取代了 Bitbucket 上的旧版本。 /// jsmn (pronounced like 'jasmine') is a minimalistic JSON parser in C. It can be easily integrated into resource-limited or embedded projects. // jsmn（发音类似“茉莉花”）是一个用 C 语言编写的极简 JSON 解析器。它可以轻松集成到资源受限或嵌入式项目中。 /// Philosophy // 哲学 /// Most JSON parsers offer you a bunch of functions to load JSON data, parse it and extract any value by its name. jsmn proves that checking the correctness of every JSON packet or allocating temporary objects to store parsed JSON fields often is an overkill. // 大多数 JSON 解析器都提供了一系列函数，用于加载 JSON 数据、解析数据以及按名称提取任何值。jsmn 证明，检查每个 JSON 数据包的正确性或分配临时对象来存储已解析的 JSON 字段通常是过度设计。 /// JSON format itself is extremely simple, so why should we complicate it? // JSON 格式本身非常简单，我们为什么要把它复杂化呢？ /// jsmn is designed to be robust (it should work fine even with erroneous data), fast (it should parse data on the fly), portable (no superfluous dependencies or non-standard C extensions). And of course, simplicity is a key feature - simple code style, simple algorithm, simple integration into other projects. // jsmn 的设计目标是健壮 （即使处理错误数据也能正常工作）、 快速 （能够即时解析数据）和可移植 （没有多余的依赖项或非标准的 C 扩展）。当然， 简洁性也是其关键特性——代码风格简洁、算法简单、易于集成到其他项目中。 /// Features // 特征 /// - compatible with C89 // 兼容 C89 /// - no dependencies (even libc!) // 没有依赖项（甚至 libc！） /// - highly portable (tested on x86/amd64, ARM, AVR) // 高度便携（已在 x86/amd64、ARM、AVR 上测试） /// - about 200 lines of code // 大约200行代码 /// - extremely small code footprint // 代码量极小 /// - API contains only 2 functions // API 仅包含 2 个函数 /// - no dynamic memory allocation // 无动态内存分配 /// - incremental single-pass parsing // 增量式单遍解析 /// - library code is covered with unit-tests // 库代码已编写单元测试。"
[knows.by]: https://github.com/Ferki-git-creator/jsmn_zig.git "What's This All About? // 这到底是怎么回事？ /// Ever felt like JSON parsers are over-engineered kitchen sinks that do everything except make coffee? Meet jsmn_zig - a spiritual port of the legendary JSMN to Zig, with some Zig-ish improvements. // 你是否曾觉得 JSON 解析器就像功能过剩的厨房水槽，除了煮咖啡什么都做不了？来认识一下 jsmn_zig ——它是传奇的 JSMN 的精神移植版，并针对 Zig 进行了一些改进。 /// We don't parse JSON into fancy ASTs. We give you tokens. You decide what to do with them. It's like IKEA furniture for JSON - we give you the pieces, you build the masterpiece. // 我们不会将 JSON 解析成复杂的抽象语法树 (AST)。我们只提供令牌 (token)。您决定如何使用它们。这就像 JSON 版的宜家家具——我们提供零件，您来搭建杰作。"

