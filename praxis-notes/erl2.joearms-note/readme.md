[src/gh]: https://github.com/joearms/erl2.git "(Erlang 100%) a new dialect of erlang"
[knowsby]: https://joearms.github.io/published/2013-05-31-a-week-with-elixir.html "If you don't fix this you'll spend the next 20 years explain why - just like we did in Erlang. // 如果你不修复这个问题，你将花费接下来的 20 年解释为什么——就像我们在 Erlang 中做的那样。 /// BTW the fix is really really easy. I made erl2 as an experiment to fix this. It can't be fixed in Erlang (backwards compatibility) so I did it in erl2. This needs a very small change to erl_eval and a few tweaks in the parser. // 顺便说一句，修复方法真的非常非常简单。我制作 erl2 是为了实验性地修复这个问题。在 Erlang 中无法修复（向后兼容性），所以我选择在 erl2 中修复。这需要对 erl_eval 进行一个非常小的修改，并在解析器中做一些调整。 /// Basically FORMS are not expressions, so I added a keyword def // 基本上 FORMS 不是表达式，所以我添加了一个关键字 def 。"
