[ide.src/gh]: https://github.com/HoneIDE/ide.git "(Languages: TypeScript 95.9%, Kotlin 2.4%, Other 1.7%) { @honeide/ide } AI-native code editor for all platforms — macOS, Windows, Linux, iOS, Android, Web // 全平台 AI 原生代码编辑器 — macOS、Windows、Linux、iOS、Android、Web /// Built with Perry: TypeScript compiled to native UI. Powered by @honeide/core for all headless services. // 使用 Perry 构建：TypeScript 编译为原生 UI。由 @honeide/core 驱动所有无头服务。 /// What it is // 它是什么 /// Hone is an IDE designed from the ground up for AI-assisted development: // Hone 是一款专为 AI 辅助开发而设计的 IDE： /// - Native on every platform — one TypeScript codebase, compiled to native UI via Perry // 跨平台原生支持 — 一个 TypeScript 代码库，通过 Perry 编译为原生 UI /// - Mobile-first — full IDE experience on iPad and Android tablet; focused editing on iPhone/Android phone // 移动优先 — 在 iPad 和 Android 平板电脑上提供完整的 IDE 体验；在 iPhone/Android 手机上专注编辑 /// - AI-native — inline completions, chat sidebar, autonomous agent mode, AI PR review // AI 原生 — 内联补全、聊天侧边栏、自主代理模式、AI PR 评审 /// - Fully customizable — sidebar left or right, activity bar position, keybindings, themes // 完全可定制 — 侧边栏在左或右、活动栏位置、快捷键、主题"
[site/codes]: https://hone.codes/ "Sharpen your code, natively. // 优化你的代码，本地化。 /// A native, AI-powered code editor for every platform. Written in TypeScript, compiled to native via Perry. No Electron. No compromise. // 适用于所有平台的本地化 AI 代码编辑器。使用 TypeScript 编写，通过 Perry 编译为原生应用。无需 Electron。无任何妥协。 /// [Why Hone // 为何用 Hone] Everything an editor should be. Nothing it shouldn't. // 编辑器应有的所有功能。没有它不应该有的东西。 /// Built from scratch in TypeScript, compiled to native binaries via Perry. Every component is designed for performance, modularity, and developer joy. // 使用 TypeScript 从零开始构建，通过 Perry 编译成原生二进制文件。每个组件都是为了性能、模块化和开发者乐趣而设计的。 /// - ⚡ Truly Native // 真正原生 ///: No Electron, no Chromium, no V8 runtime. Perry compiles TypeScript directly to native binaries. Under 50MB, under 100MB RAM, sub-second startup. // 无需 Electron，无需 Chromium，无需 V8 运行时。Perry 直接将 TypeScript 编译为原生二进制文件。体积小于 50MB，内存占用小于 100MB，启动时间小于秒。 /// - 🧠 AI-First Architecture  AI 首先架构 ///: AI isn't bolted on — it's woven into the editor's core. The AI sees your syntax tree, your errors, your git diff, your terminal. Deep context, not just text. // AI 并非附加，而是编织在编辑器的核心中。AI 可以看到你的语法树、你的错误、你的 git 差异、你的终端。深度上下文，而不仅仅是文本。 /// - 🔑 Bring Your Own AI // 你自带 AI ///: Claude, GPT, Gemini, Ollama, or your company's private model. Your API key, your choice. Mix different providers for different features. No vendor lock-in. // Claude、GPT、Gemini、Ollama 或您公司的私有模型。您的 API 密钥，您来选择。混合不同供应商以获取不同功能。无供应商锁定。 /// - 🤖 Agent Mode // 代理模式 ///: Describe what you want. The agent reads your code, plans changes, edits files, runs tests, and iterates — with per-hunk approval before anything is committed. // 描述您想要什么。代理会读取您的代码，规划更改，编辑文件，运行测试并迭代——在提交任何内容之前，每个代码块都需要您的批准。 /// - 🔍 Native PR Review // 本地 PR 审查 ///: Review pull requests in-editor with full syntax highlighting, LSP intelligence in diffs, and AI-powered annotations that catch bugs before humans do. // 在编辑器中审查 pull requests，支持完整的语法高亮、diff 中的 LSP 智能以及 AI 驱动的注释，能够在人类发现之前捕捉到错误。 /// - 🧩 Modular & Composable // 模块化与可组合 ///: hone-editor, hone-terminal, and hone-core are independent packages. Use the editor component in your own app. Build your own IDE on Hone's foundation. // hone-editor、hone-terminal 和 hone-core 是独立的包。在您自己的应用程序中使用编辑器组件。在 Hone 的基础上构建您自己的 IDE。 /// [Performance // 性能] The numbers Electron can't reach. // Electron 无法触及的数字。 /// When you eliminate the browser, everything gets faster. These are our targets — and we intend to hit every one. // 当你移除浏览器，一切都会更快。这些是我们的目标——我们打算实现每一个。 /// - <1s ///: Cold start // 冷启动 { vs 3-5s (VSCode) } /// - <100 ///: MB RAM idle // 兆字节级别内存空闲占用 { vs 300-500MB (VSCode) } /// - <50 ///: MB binary // 兆字节级别二进制文件大小 { vs ~300MB (VSCode) } /// - <10 ///: ms keystroke latency // 毫秒级按键延迟 { vs 30-50ms (VSCode) } /// [AI Integration // AI 集成] Your AI. Your keys. Your choice. // 你的 AI。你的密钥。你的选择。 ///: Hone never resells API access. You bring your own keys and route different providers to different features. When the next hot model drops, it works immediately. // Hone 从不转售 API 访问权限。你使用自己的密钥，并将不同的提供商路由到不同的功能。当下一个热门模型发布时，它立即生效。 /// - [Inline // 行内] Ghost Text Completion // 幽灵文本补全 ///: Context-aware suggestions powered by any provider. Sees your syntax tree, not just raw text. Renders at native speed with zero Electron overhead. // 由任何提供方支持的上下文感知建议。它能够看到你的语法树，而不仅仅是原始文本。以原生速度渲染，且零 Electron 开销。 /// - [Chat // 聊天] Context-Aware Chat // 上下文感知聊天 ///: The AI automatically sees your open files, errors, terminal output, and git state. No manual @file tagging. Switch models mid-conversation. // AI 可以自动识别您打开的文件、错误、终端输出和 git 状态。无需手动 @file 标记。可以在对话中途切换模型。 /// - [Agent // 代理] Autonomous Coding Agent // 自主编码代理 ///: Multi-file edits, terminal commands, test execution, error recovery. Full transparency with a live activity log and diff approval before any change lands. // 多文件编辑、终端命令、测试执行、错误恢复。在更改生效前，通过实时活动日志和差异审批实现完全透明。 /// - [Review // 评审] AI-Powered PR Review // AI 驱动的 PR 审查 ///: AI pre-analyzes every diff for bugs, security issues, and style problems. Review annotations appear inline. One-click fixes. Submit reviews back to GitHub/GitLab. // AI 预先分析每个差异中的错误、安全问题及风格问题。评审注释直接显示在代码行内。一键修复。将评审结果提交回 GitHub/GitLab。 /// Bring Your Own Key. Mix & Match. // 你的密钥。自由组合。 ///: Route each AI feature to a different provider. Use a fast local model for autocomplete, a powerful cloud model for agent tasks, and something in between for chat. Your infrastructure, your budget, your rules. // 将每个 AI 功能路由到不同的提供商。使用快速本地模型进行自动补全，强大的云模型处理代理任务，而聊天则使用两者之间的模型。您的基础设施，您的预算，您的规则。 /// [Platforms // 平台] One codebase. Every platform. // 一个代码库。支持所有平台。 ///: Written once in TypeScript. Compiled to native on each platform by Perry. Native look, native speed, everywhere. // 使用 TypeScript 编写一次。由 Perry 在各个平台上编译为原生代码。原生外观，原生速度，无处不在。"
[ext-api.src/gh]: https://github.com/HoneIDE/api.git "(MIT) (Languages: TypeScript 100.0%) { @honeide/api } Public extension API for Hone IDE — zero runtime, pure TypeScript types // HoneIDE/api: Hone IDE 的公共扩展 API — 无运行时，纯 TypeScript 类型 /// The public extension API for Hone IDE. A pure TypeScript type library — zero runtime code, zero dependencies. // Hone IDE 的公共扩展 API。一个纯 TypeScript 类型库——零运行时代码，零依赖。 /// Installation // 安装 /// (: npm i --save-dev -- @honeide/api) This is a development-time dependency only. Your compiled extension does not bundle this package. // 这是一个仅在开发时使用的依赖项。你的编译后的扩展不会包含这个包。"
[core.src/gh]: https://github.com/HoneIDE/core.git "(Languages: TypeScript 100.0%) { @honeide/core } IDE services layer — workspace, file watcher, layered settings, keybindings; git / search / LSP manager and AI provider system (8 adapters) in progress // IDE 服务层 — 工作区、文件监视器、分层设置、快捷键绑定；git / 搜索 / LSP 管理器和 AI 提供系统（8 个适配器）正在进行中 /// Headless services for Hone IDE — workspace, settings, git, search, AI, LSP/DAP, extensions // HoneIDE/core：为 Hone IDE 提供无头服务——工作区、设置、git、搜索、AI、LSP/DAP、扩展 /// Headless services for Hone IDE — workspace management, settings, keybindings, git, search, AI providers, LSP/DAP protocols, and more. // Hone IDE 的无头服务 — 工作空间管理、设置、快捷键绑定、git、搜索、AI 提供者、LSP/DAP 协议等。 /// No UI. No native dependencies. Pure TypeScript. Runs in any environment: IDE process, test runner, language server, CI. // 没有 UI。没有原生依赖。纯 TypeScript。可在任何环境中运行：IDE 进程、测试运行器、语言服务器、CI。 /// Design principles // 设计原则 /// - Zero UI dependencies — no DOM, no native calls, no platform assumptions // 零 UI 依赖项 — 无 DOM，无原生调用，无平台假设 /// - Fully tested — every module has unit tests; run with bun test // 完全经过测试 — 每个模块都有单元测试；使用 bun test 运行 /// - Protocol-first — LSP, DAP, and AI providers follow open standards // 以协议为先 — LSP、DAP 和 AI 提供者遵循开放标准 /// - Layered settings — user → workspace → language priority, VS Code-compatible schema // 分层设置 — 用户 → 工作区 → 语言优先级，兼容 VS Code 的架构 /// - Extension-ready — services expose the same API surface as @honeide/api // 扩展支持 — 服务暴露与 @honeide/api 相同的 API 界面"
[component-editor.src/gh]: https://github.com/HoneIDE/editor.git "(MIT) (Languages: Rust 51.0%, TypeScript 46.0%, HTML 1.6%, Other 1.4%) { @honeide/editor } Editor component — piece-table buffer, 10 languages via Lezer, LSP + DAP, ghost text, demos on macOS / iOS / Windows / Android / Web // 编辑器组件——基于 piece-table 的缓冲区，支持 10 种语言（通过 Lezer），LSP + DAP，幽灵文本，macOS / iOS / Windows / Android / Web 上的演示 /// High-performance, cross-platform code editor surface. Embeddable editor component with native rendering (Core Text, DirectWrite, Pango, Skia, DOM). // 高性能、跨平台的代码编辑器界面。可嵌入的编辑组件，支持原生渲染（Core Text、DirectWrite、Pango、Skia、DOM）。 /// High-performance, cross-platform code editor surface. Designed to be embedded by other applications — markdown editors, config editors, query consoles, or full IDEs. // 高性能、跨平台的代码编辑器界面。设计为可被其他应用程序嵌入——包括 Markdown 编辑器、配置编辑器、查询控制台或完整的 IDE。 /// All editor logic is written in TypeScript and shared across every platform. Native text rendering is handled by per-platform Rust FFI crates using each OS's native text stack. // 所有编辑器逻辑均使用 TypeScript 编写，并在所有平台上共享。原生文本渲染由每个平台的 Rust FFI crate 处理，利用每个操作系统的原生文本堆栈。 /// Features // 功能 /// - Piece table text buffer with B-tree rope indexing — O(log n) for all operations // 使用 B 树绳索索引的片段表文本缓冲区——所有操作均为 O(log n) 复杂度。 /// - Multi-cursor editing with selections, word boundaries, and cursor merging // 多光标编辑，支持选择、单词边界和光标合并 /// - Syntax highlighting for 10 languages via Lezer (TypeScript, JavaScript, HTML, CSS, JSON, Python, Rust, C++, Markdown, and more) // 通过 Lezer 提供的语法高亮，支持 10 种语言（TypeScript、JavaScript、HTML、CSS、JSON、Python、Rust、C++、Markdown 等） /// - Search and replace — literal, regex, case-sensitive, whole-word, incremental // 搜索和替换——支持字面量、正则表达式、区分大小写、整词和增量搜索 /// - Code folding — indent-based and syntax-based // 代码折叠——基于缩进和基于语法 /// - Undo/redo with time-based coalescing // 撤销/重做具有基于时间的合并 /// - Virtual scrolling — only visible lines are rendered // 虚拟滚动——仅渲染可见行 /// - Word wrap — none, word-boundary, or bounded column modes // 自动换行——无、单词边界或限定列模式 /// - Diff engine — Myers algorithm with inline character-level diffs and hunk operations // 差异引擎——Myers 算法，具有内联字符级差异和代码块操作 /// - LSP client — completion, hover, go-to-definition, references, diagnostics, formatting // LSP 客户端 — 补全、悬浮提示、跳转到定义、引用、诊断、格式化 /// - DAP client — breakpoints, stepping, stack inspection, variable evaluation // DAP 客户端 — 断点、单步执行、堆栈检查、变量评估 /// - Ghost text — inline AI completion rendering // 幽灵文本 — 行内 AI 补全渲染 /// - Minimap, find/replace widget, autocomplete overlays, diagnostic decorations // 缩略图、查找/替换组件、自动补全覆盖层、诊断装饰"
[component-terminalemu.src/gh]: https://github.com/HoneIDE/terminal.git "(MIT) (Languages: Rust 53.4%, TypeScript 46.6%) { @honeide/terminal } Cross-platform terminal emulator component for Perry applications — VT100/xterm parser, 256-color + truecolor, native rendering via Rust FFI // 跨平台终端模拟器组件，适用于 Perry 应用程序 — VT100/xterm 解析器，256 色+真彩色，通过 Rust FFI 实现原生渲染 /// Cross-platform terminal emulator component for the Hone ecosystem. Published as @honeide/terminal — any Perry application can embed a fully functional terminal with minimal integration effort. // 跨平台终端模拟器组件，适用于 Hone 生态系统。以 @honeide/terminal 形式发布——任何 Perry 应用程序都可以通过最小的集成工作嵌入一个功能完整的终端。 /// Compatibility target: xterm-256color /// 兼容目标： xterm-256color /// Features // 功能 /// - VT100/xterm parser — 14-state machine handling CSI, OSC, DCS, ESC sequences // VT100/xterm 解析器——14 状态机处理 CSI、OSC、DCS、ESC 序列 /// - 256-color + truecolor — ANSI 16, indexed 0–255, and 24-bit RGB via SGR // 256 色+真彩色——ANSI 16 色、索引 0-255 色及 24 位 RGB 通过 SGR /// - Text attributes — bold, italic, underline (single/double/curly), strikethrough, dim, inverse, invisible // 文本属性 — 粗体、斜体、下划线（单/双/花括号）、删除线、暗淡、反转、不可见 /// - Mouse tracking — X10, normal/button, SGR/1006 extended modes // 鼠标跟踪 — X10、普通/按钮、SGR/1006 扩展模式 /// - Alternate screen buffer — DECSET 1049 for vim, less, htop, etc. // 备用屏幕缓冲区 — DECSET 1049 用于 vim、less、htop 等 /// - Bracketed paste — DECSET 2004 // 带括号的粘贴 — DECSET 2004 /// - Hyperlinks — OSC 8 inline hyperlinks // 超链接 — OSC 8 内联超链接 /// - Wide characters — CJK double-width support // 宽字符 — 支持 CJK 双宽字符 /// - Scrollback — configurable ring buffer (default 10,000 lines) // 滚动回溯 — 可配置的环形缓冲区（默认 10,000 行） /// - Shell integration — OSC 133 prompt/command/output markers // Shell 集成 — OSC 133 提示/命令/输出标记 /// - Selection, copy, search — across visible buffer and scrollback // 选择、复制、搜索——跨越可见缓冲区和滚动回溯 /// - Native rendering — Core Text (macOS), Direct2D + DirectWrite (Windows), with FFI bridge for all 6 platforms // 原生渲染——Core Text (macOS)、Direct2D + DirectWrite (Windows)，所有 6 个平台均通过 FFI 桥接"
[builtin-exts.src/gh]: https://github.com/HoneIDE/extensions.git "(MIT) (Languages: TypeScript 93.9%, CSS 2.8%, Shell 1.9%, HTML 1.4%) { @honeide/extensions } Built-in extensions that ship with every Hone IDE installation — TypeScript, Python, Rust, Go, C++, HTML/CSS, JSON, Markdown, Git, Docker, TOML/YAML // 随每个 Hone IDE 安装附带的内置扩展——TypeScript、Python、Rust、Go、C++、HTML/CSS、JSON、Markdown、Git、Docker、TOML/YAML /// Built-in extensions that ship with every Hone IDE installation. This repository contains 11 extensions covering the most popular programming languages and developer tools — all implemented in TypeScript, compiled to native binaries by Perry, and loaded at runtime by Hone's extension host. // 随附于每个 Hone IDE (. web: hone.codes) 安装的内置扩展。此存储库包含 11 个扩展，涵盖了最受欢迎的编程语言和开发者工具——所有扩展均使用 TypeScript 实现，由 Perry (. hone.codes/perry) 编译为原生二进制文件，并在运行时由 Hone 的扩展主机加载。 /// Key Design Principles // 关键设计原则 /// - Single import. Every extension imports only @honeide/api — no Node.js built-ins, no third-party npm packages, no cross-extension imports. // 单一导入。每个扩展仅导入 @honeide/api — 不包含 Node.js 内建模块，不依赖第三方 npm 包，不进行跨扩展导入。 /// - AOT compilation. Perry compiles each src/index.ts directly to a native dynamic library (.dylib / .so / .dll). No JavaScript runtime is bundled with Hone. // AOT 编译。Perry 将每个 src/index.ts 直接编译为原生动态库（ .dylib / .so / .dll ）。Hone 不捆绑 JavaScript 运行时。 /// - Independence. Extensions have no runtime dependencies on each other. Disabling any extension has zero effect on the others. // 独立性。扩展之间没有运行时依赖关系。禁用任何扩展对其他扩展均无影响。 /// - Lazy activation. Extensions activate only when needed — typically when a matching file is opened (onLanguage:) or a matching workspace file is detected (workspaceContains:). // 懒加载激活。扩展仅在需要时激活——通常在打开匹配的文件时（ onLanguage: ）或检测到匹配的工作区文件时（ workspaceContains: ）。"
[builtin-themes.src/gh]: https://github.com/HoneIDE/themes.git "(MIT) (Languages: TypeScript 100.0%) { @honeide/themes } Color themes for Hone — VSCode-compatible theme collection with import tooling and schema validation // Hone 的颜色主题 — 兼容 VSCode 的主题集合，带有导入工具和架构验证 /// hone-themes // 磨练主题 /// Color themes for Hone — a VSCode-compatible theme collection. // Hone 的颜色主题——一个与 VSCode 兼容的主题集合。 /// Hone uses the VSCode color theme format, so any existing VSCode theme works out of the box. This package ships 11 curated themes and provides tooling to import and validate any others. // Hone 使用 VSCode 颜色主题格式，因此任何现有的 VSCode 主题都能直接使用。此软件包包含 11 个精选主题，并提供导入和验证其他主题的工具。"
[knowsby]: https://github.com/PerryTS/perry.git "Hone // 磨练 ///: AI-powered native code editor with built-in terminal, Git, and LSP. // 内置终端、Git 和 LSP 的 AI 驱动型原生代码编辑器。"
