[src/gh]: https://github.com/helyim/helyim.git "(Apache-2.0, MIT) (Languages: Rust 100.0%) seaweedfs implemented in pure Rust // 纯 Rust 实现的 seaweedfs /// Features // 特征 /// Additional Features // 附加功能 /// - Can choose no replication or different replication levels, rack and data center aware. // 可以选择不复制或不同的复制级别，支持机架和数据中心环境。 /// - Automatic compaction to reclaim disk space after deletion or update. // 删除或更新后自动压缩以回收磁盘空间。 /// - Automatic master servers fail over - no single point of failure (SPOF). // 自动主服务器故障转移 - 无单点故障 (SPOF)。 /// - Erasure Coding for warm storage Rack-Aware 10.4 erasure coding reduces storage cost. // 用于温存储的纠删码 Rack-Aware 10.4 纠删码可降低存储成本。 /// Usage // 用法 /// By default, the master node runs on port 9333, and the volume nodes run on port 8080. Let's start one master node, and one volume node on port 8080. Ideally, they should be started from different machines. We'll use localhost as an example. // 默认情况下，主节点运行在 9333 端口，卷节点运行在 8080 端口。我们来启动一个主节点和一个卷节点，它们都运行在 8080 端口。理想情况下，它们应该从不同的机器启动。我们以本地主机为例。 /// Helyim uses HTTP REST operations to read, write, and delete. The responses are in JSON or JSONP format. // Helyim 使用 HTTP REST 操作进行读取、写入和删除操作。响应采用 JSON 或 JSONP 格式。 /// (: helyim master # Start Master Server // 启动主服务器) (: helyim volume --port 8080 --folders ./target # Start Volume Servers // 启动卷服务器) (: helyim filer # Start Filer Server // 启动文件服务器) /// Failover Master Server // 故障转移主服务器 /// When initiating a Raft cluster, it is necessary to specify the same node sequence when starting the Leader and Follower instances. // 启动 Raft 集群时，启动 Leader 和 Follower 实例时必须指定相同的节点顺序。 /// (: helyim -- master --ip 127.0.0.1 --port 9333 --peers 127.0.0.1:9333 --peers 127.0.0.1:9335 --peers 127.0.0.1:9337 # start master1) (: helyim -- master --ip 127.0.0.1 --port 9335 --peers 127.0.0.1:9333 --peers 127.0.0.1:9335 --peers 127.0.0.1:9337 # start master1) (: helyim -- master --ip 127.0.0.1 --port 9337 --peers 127.0.0.1:9333 --peers 127.0.0.1:9335 --peers 127.0.0.1:9337 # start master1) /// You can view the cluster status by accessing http://localhost:9333/cluster/status. // 您可以通过访问 http://localhost:9333/cluster/status 查看集群状态。 /// Benchmark // 基准 /// My laptop results on Lenovo IdeaPad Pro 16 (2023) with SSD, CPU: 14 Intel Core i9 5.4GHz. // 我的笔记本电脑测试结果显示，联想 IdeaPad Pro 16 (2023) 配备 SSD，CPU：Intel Core i9 5.4GHz。 /// It seems to be slower than seaweedfs, especially in terms of reading. // 它似乎比 seaweedfs 慢，尤其是在读取方面。 /// Acknowledgments // 致谢 /// - seaweedfs (. gh: seaweedfs/seaweedfs.git) - SeaweedFS is a fast distributed storage system for blobs, objects, files, and data lake, for billions of files! Blob store has O(1) disk seek, cloud tiering. Filer supports Cloud Drive, cross-DC active-active replication, Kubernetes, POSIX FUSE mount, S3 API, S3 Gateway, Hadoop, WebDAV, encryption, Erasure Coding. // SeaweedFS ——SeaweedFS 是一个快速分布式存储系统，适用于 Blob、对象、文件和数据湖，可存储数十亿个文件！Blob 存储具有 O(1) 磁盘寻道速度和云分层功能。文件服务器支持 Cloud Drive、跨数据中心主动-主动复制、Kubernetes、POSIX FUSE 挂载、S3 API、S3 网关、Hadoop、WebDAV、加密和纠删码。 /// - zergling (. gh: july2993/zergling.git) - seaweedFS re-implemented in Rust. // zergling - seaweedFS 用 Rust 重新实现。"
[cli.cargo/crates]: https://crates.io/crates/helyim "(: cargo install -- helyim) (MIT OR Apache-2.0) (11K SLoC) (107 KiB) helyim is a object storage written by Rust. // helyim 是一个用 Rust 编写的对象存储系统。 (src: gh:helyim/helyim.git)"
[lib-proto.cargo/crates]: https://crates.io/crates/helyim-proto "(: cargo add -- helyim-proto) (MIT OR Apache-2.0) (54 SLoC) (6.31 KiB) proto for helyim // helyim 的协议 (src: gh:helyim/helyim.git)"

