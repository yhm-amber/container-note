[cli-c.src/gh]: https://github.com/proot-me/proot.git "(GPL-2.0) (Languages: C 88.4%, Shell 8.3%, Makefile 2.3%, Python 0.4%, SWIG 0.3%, Dockerfile 0.2%, Other 0.1%) chroot, mount --bind, and binfmt_misc without privilege/setup for Linux // 无需特权/设置即可在 Linux 系统中使用 chroot、mount --bind 和 binfmt_misc 命令"
[cli-rs.src/gh]: https://github.com/proot-me/proot-rs.git "(GPL-3.0) (Languages: Rust 92.4%, Shell 5.5%, C 1.5%, Dockerfile 0.6%) Rust implementation of PRoot, a ptrace-based sandbox // PRoot 的 Rust 实现，PRoot 是一个基于 ptrace 的沙箱。 /// Rust implementation of PRoot, a ptrace-based sandbox. // PRoot 的 Rust 实现，PRoot 是一个基于 ptrace 的沙箱。 /// proot-rs works by intercepting all Linux system calls that use paths (execve, mkdir, ls, ...) and translating these with the specified path bindings, in order to simulate chroot, and all this without requiring admin rights (ptrace do not require any special rights). // proot-rs 工作原理是拦截所有使用路径的 Linux 系统调用（ execve 、 mkdir 、 ls 等），并将它们转换为指定的路径绑定，以模拟 chroot ，而且这一切都不需要管理员权限（ ptrace 不需要任何特殊权限）。 /// For instance // 例如 /// - (: proot-rs -R /home/user/ -- mkdir /myfolder #: will be equivalent to: mkdir /home/user/myfolder) /// - (: proot-rs -R ./untared-docker-image -- /bin/sh #: #: download an docker oci image, untar it, then run it without needing docker.)"
[site/ghio]: https://proot-me.github.io/ "Description // 描述 /// PRoot is a user-space implementation of chroot, mount --bind, and binfmt_misc. This means that users don't need any privileges or setup to do things like using an arbitrary directory as the new root filesystem, making files accessible somewhere else in the filesystem hierarchy, or executing programs built for another CPU architecture transparently through QEMU user-mode. Also, developers can use PRoot as a generic Linux process instrumentation engine thanks to its extension mechanism, see CARE for an example. Technically PRoot relies on ptrace, an unprivileged system-call available in every Linux kernel. // PRoot 是 chroot 、 mount --bind 和 binfmt_misc 的用户空间实现。这意味着用户无需任何特权或配置即可执行诸如将任意目录用作新的根文件系统、使文件在文件系统层次结构中的其他位置可访问，或通过 QEMU 用户模式透明地执行为其他 CPU 架构编写的程序等操作。此外，由于其扩展机制，开发人员可以将 PRoot 用作通用的 Linux 进程插桩引擎，例如，请参阅 CARE 。从技术上讲，PRoot 依赖于 ptrace ，这是一个在每个 Linux 内核中都可用的非特权系统调用。 /// The new root file-system, a.k.a guest rootfs, typically contains a Linux distribution. By default PRoot confines the execution of programs to the guest rootfs only, however users can use the built-in mount/bind mechanism to access files and directories from the actual root file-system, a.k.a host rootfs, just as if they were part of the guest rootfs. // 新的根文件系统，也称为客户机根文件系统，通常包含一个 Linux 发行版。默认情况下，PRoot 限制执行 程序只能写入客户机根文件系统，但用户可以使用内置的 挂载/绑定机制，用于访问实际根文件系统（又称主机根文件系统 ）中的文件和目录，就像它们是客户机根文件系统的一部分一样。 /// When the guest Linux distribution is made for a CPU architecture incompatible with the host one, PRoot uses the CPU emulator QEMU user-mode to execute transparently guest programs. It's a convenient way to develop, to build, and to validate any guest Linux packages seamlessly on users' computer, just as if they were in a native guest environment. That way all of the cross-compilation issues are avoided. // 当客户机 Linux 发行版针对与宿主机 CPU 架构不兼容的 CPU 架构时，PRoot 会使用 CPU 模拟器 QEMU 的用户模式来透明地执行客户机程序。这是一种便捷的方式，可以在用户的​​计算机上无缝地开发、构建和验证任何客户机 Linux 软件包，就像在原生客户机环境中一样。这样就避免了所有交叉编译问题。 /// PRoot can also mix the execution of host programs and the execution of guest programs emulated by QEMU user-mode. This is useful to use host equivalents of programs that are missing from the guest rootfs and to speed up build-time by using cross-compilation tools or CPU-independent programs, like interpreters. // PRoot 还可以混合执行宿主机程序和由 QEMU 用户模式模拟的宿主机程序。这对于使用宿主机根文件系统中缺失的程序的宿主机等效程序非常有用，并且可以通过使用交叉编译工具或与 CPU 无关的程序（例如解释器）来加快构建速度。 /// It is worth noting that the guest kernel is never involved, regardless of whether QEMU user-mode is used or not. Technically, when guest programs perform access to system resources, PRoot translates their requests before sending them to the host kernel. This means that guest programs can use host resources (devices, network, ...) just as if they were \"normal\" host programs. // 值得注意的是，无论是否使用 QEMU 用户模式，客户机内核都不会参与其中。从技术上讲，当客户机程序访问系统资源时，PRoot 会先转换其请求，然后再将其发送给宿主机内核。这意味着客户机程序可以像“普通”宿主机程序一样使用宿主机资源（设备、网络等）。 /// Examples // 示例 /// In the following examples the directories /mnt/slackware-8.0 and /mnt/armslack-12.2/ contain a Linux distribution respectively made for x86 CPUs and ARM CPUs. // 在以下示例中，目录 /mnt/slackware-8.0 和 /mnt/armslack-12.2/ 分别包含为 x86 CPU 和 ARM CPU 设计的 Linux 发行版。 /// - chroot equivalent // chroot 等价物 ///: (: proot -r /mnt/slackware-8.0/ -- cat /etc/motd #> Welcome to Slackware Linux 8.0 #: To execute a command inside a given Linux distribution, just give proot the path to the guest rootfs followed by the desired command. The example below executes the program cat to print the content of a file // 要在给定的 Linux 发行版中执行命令，只需输入以下命令即可。 proot 是访客根文件系统的路径，后跟所需的命令。以下示例执行程序 cat 来打印文件内容) (: proot -r /mnt/slackware-8.0/ #: The default command is /bin/sh when none is specified. Thus the shortest way to confine an interactive shell and all its sub-programs is like that // 当未指定命令时，默认命令为 /bin/sh 。因此，限制交互式 shell 及其所有子程序的最短方法就是这样) /// - mount --bind equivalent // mount --bind 等价物 ///: (: proot -b /tmp/alternate_opt:/opt #: The bind mechanism enables one to relocate files and directories. This is typically useful to trick programs that perform access to hard-coded locations, like some installation scripts: if it install prog into /opt/bin, prog will installed in /tmp/alternate_opt/bin actually. // 绑定机制允许用户重新定位文件和目录。这通常可以用来欺骗那些访问硬编码位置的程序，例如某些安装脚本：它若是硬编码为把 prog 安到 /opt/bin ，则此处实际将被安到 /tmp/alternate_opt/bin 路径。) (: proot -b ~/alternate_hosts:/etc/hosts #: 这样则能够使得不必切 root 也能执行硬编码了要改 /etc/hosts 的脚本。) (: proot -b /bin/bash:/bin/sh #: 绑定 sh 为 bash 而非别的) /// - chroot + mount --bind equivalent // chroot + mount --bind 等价物 ///: (: proot -r /mnt/slackware-8.0/ -b /proc -- ps -o tty,command #: 若只是 proot -r /mnt/slackware-8.0/ -- ps -o tty,command 则会缺少依赖报错： Error, do this: mount -t proc none /proc) (: proot -R /mnt/slackware-8.0/ -- ps -o tty,command #: Actually there's a bunch of such specific files, that's why PRoot provides the option -R to bind automatically a pre-defined list of recommended paths // 实际上有很多这样的特定文件，因此 PRoot 提供了选项 -R 来自动绑定预定义的推荐路径列表) /// - chroot + mount --bind + su equivalent // chroot + mount --bind + su 等价物 ///: (: proot -r /mnt/slackware-8.0/ -0 -- some-commands-need-suroot #: Some programs will not work correctly if they are not run by the \"root\" user, this is typically the case with package managers. PRoot can fake the root identity and its privileges when the -0 (zero) option is specified // 某些程序如果不是由“root”用户运行，则无法正常工作，这种情况通常发生在软件包管理器上。当指定 -0 （零）选项时，PRoot 可以伪造 root 身份及其权限) (: proot -S /mnt/slackware-8.0/ -- install-pkg perl.tgz #: This option is typically required to create or install packages into the guest rootfs. Note it is not recommended to use the -R option when installing packages since they may try to update bound system files, like /etc/group. Instead, it is recommended to use the -S option. This latter enables the -0 option and binds only paths that are known to not be updated by packages // 通常需要使用此选项在客户机根文件系统中创建或安装软件包。请注意，安装软件包时不建议使用 -R 选项，因为软件包可能会尝试更新绑定的系统文件，例如 /etc/group。建议使用 -S 选项。后者会启用 -0 选项，并且仅绑定已知不会被软件包更新的路径) /// - chroot + mount --bind + binfmt_misc equivalent // chroot + mount --bind + binfmt_misc 等价物 ///: (: proot -R /mnt/armslack-12.2/ -q qemu-arm -- cat /etc/motd #> Welcome to ARMedSlack Linux 12.2 #: PRoot uses QEMU user-mode to execute programs built for a CPU architecture incompatible with the host one. From users' point-of-view, guest programs handled by QEMU user-mode are executed transparently, that is, just like host programs. To enable this feature users just have to specify which instance of QEMU user-mode they want to use with the option -q // PRoot 使用 QEMU 用户模式来执行为与主机 CPU 架构不兼容的 CPU 架构编写的程序。从用户的角度来看，由 QEMU 用户模式处理的客户程序是透明执行的，也就是说，就像主机程序一样。要启用此功能，用户只需使用选项 -q 指定要使用的 QEMU 用户模式实例即可) (: proot -R /mnt/armslack-12.2/ -q \"qemu-arm -g 1234\" -- emacs #: The parameter of the -q option is actually a whole QEMU user-mode command, for instance to enable its GDB server on port 1234 // -q 选项的参数实际上是一个完整的 QEMU 用户模式命令，例如，用于在端口 1234 上启用其 GDB 服务器) (: proot -R /mnt/armslack-12.2/ -q qemu-arm -- file /bin/echo #> [...] ELF 32-bit LSB executable, ARM [...] ;: proot -R /mnt/armslack-12.2/ -q qemu-arm -- file /host-rootfs/bin/echo #> [...] ELF 64-bit LSB executable, x86-64 [...] ;: #: PRoot allows one to mix transparently the emulated execution of guest programs and the native execution of host programs in the same file-system namespace. It's typically useful to extend the list of available programs and to speed up build-time significantly. This mixed-execution feature is enabled by default when using QEMU user-mode, and the content of the host rootfs is made accessible through /host-rootfs // PRoot 允许用户在同一文件系统命名空间中透明地混合执行客户程序的模拟执行和宿主程序的本地执行。这通常有助于扩展可用程序列表并显著加快构建速度。使用 QEMU 用户模式时，此混合执行功能默认启用，并且可以通过 /host-rootfs 访问宿主根文件系统的内容)"
