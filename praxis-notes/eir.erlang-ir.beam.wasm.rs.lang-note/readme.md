[ir.src/gh]: https://github.com/eirproject/eir.git "(Apache-2.0) (Languages: Rust 91.5%, Erlang 8.5%) Erlang ecosystem common IR // Erlang 生态系统通用 IR (中间表示) /// Eir Project // Eir 项目 /// Erlang compiler and IR implemented in Rust // 用 Rust 实现的 Erlang 编译器和 IR。 /// Details // 细节 /// Current features: // 当前功能： /// - Unified Thorin based IR representation named Eir // 基于 Thorin 的统一 IR 表示法，名为 Eir /// - Erlang frontend // Erlang 前端 /// - Core frontend // 核心前端 /// - Pattern match compilation passes // 模式匹配编译通过 /// - Basic optimization and cleanup passes // 基本优化和清理过程 /// - Naive interpreter used for testing // 用于测试的朴素解释器 /// The project is split into several crates: // 该项目分为几个模块： /// - libeir_ir - Contains the core Eir IR data structures, validation, printing // libeir_ir - 包含核心 Eir IR 数据结构、验证和打印功能 /// - libeir_syntax_erl - Frontend for Erlang, lowers to Eir. // libeir_syntax_erl - Erlang 的前端，可降级为 Eir。 /// - libeir_syntax_core - Frontend for Core Erlang, lowers to Eir. // libeir_syntax_core - Core Erlang 的前端，可降级为 Eir。 /// - libeir_passes - Compiler passes operating on Eir. // libeir_passes - 编译器在 Eir 上运行的 pass。 /// - libeir_lowerutils - Utilities for lowering Eir to SSA form. // libeir_lowerutils - 用于将 Eir 降级为 SSA 格式的实用程序。 /// - libeir_interpreter - Naive interpreter for Eir. Used to run OTP test suites. // libeir_interpreter - Eir 的简单解释器。用于运行 OTP 测试套件。 /// - libeir_intern - Symbol interning. Used by most other crates. // libeir_intern - 符号驻留。被大多数其他 crate 使用。 /// - libeir_diagnostics - Source span handling and diagnostics printing. // libeir_diagnostics - 源跨度处理和诊断打印。 /// - libeir_util - Kitchen sink of utilities used by other crates. // libeir_util - 其他箱子使用的各种实用工具的大杂烩。 /// - pattern-compiler - A generic pattern matching tree compiler. // pattern-compiler - 一个通用的模式匹配树编译器。 /// - tools - CLI tools used to work with the IR. // tools - 用于与 IR 交互的 CLI 工具。"
[wasm-beam.src/gh]: https://github.com/eirproject/whirl.git "(Languages: Rust 91.2%, JavaScript 4.6%, Shell 2.0%, Erlang 1.2%, HTML 1.0%) WebAssembly compiler and runtime for BEAM languages // BEAM 语言的 WebAssembly 编译器和运行时"
