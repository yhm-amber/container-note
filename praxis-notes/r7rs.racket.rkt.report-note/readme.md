[site/org]: https://r7rs.org/ "Revised7 Report on the Algorithmic Language Scheme // 算法语言方案修订版 7 报告 /// Scheme is a programming language designed with an emphasis on building abstractions out of simple, composable primitives. // Scheme 是一种编程语言，其设计重点在于利用简单、可组合的基本元素构建抽象概念。 /// This website covers the ongoing seventh revision of the specification since 1975, divided into two parts: a small language primarily for embedding, education, and research; and a large language to address the practical needs of mainstream software development. // 本网站涵盖了自 1975 年以来持续进行的第七次规范修订，分为两部分：一种主要用于嵌入式、教育和研究的小型语言；以及一种用于满足主流软件开发实际需求的大型语言。 /// ‘Programming languages should be designed not by piling feature on top of feature, but by removing the weaknesses and restrictions that make additional features appear necessary.’ // “编程语言的设计不应该是一味地堆砌功能，而是应该消除那些使得额外功能显得必要的弱点和限制。” /// — Revisedn Reports on the Algorithmic Language Scheme (n ≥ 3, 1986–) // — 算法语言方案修订版 n 报告 ( n ≥ 3, 1986–)"
[small.src/gh:lexi-lambda]: https://github.com/lexi-lambda/racket-r7rs.git "(Languages: Racket 78.6%, Scheme 21.4%) An implementation of R7RS in Racket // Racket 中 R7RS 的实现 /// This is an implementation of R7RS “small” in Racket. To use it, install the package and use #lang r7rs in your programs. // 这是 Racket 中 R7RS “small” 版本的实现。要使用它，请安装该软件包并在您的程序中使用 #lang r7rs 。"
[small-plg.raco/pkgs.racket]: https://pkgs.racket-lang.org/package/r7rs "(raco pkg install -- r7rs) (Dependencies: base r7rs-lib) (Authors: lexi.lambda (λ81999a1)) An implementation of R7RS as a #lang // R7RS 的 #lang 实现 (src: gh:lexi-lambda/racket-r7rs.git)"
[small-lib.raco/pkgs.racket]: https://pkgs.racket-lang.org/package/r7rs-lib "(raco pkg install -- r7rs-lib) (Dependencies: base compatibility-lib r5rs-lib r6rs-lib rackunit-lib) (Authors: lexi.lambda (λ81999a1))  (src: gh:lexi-lambda/racket-r7rs.git)"
[small.site/.site]: https://small.r7rs.org/ "Welcome to small.r7rs.org. This is the home of R7RS Working Group 1 (R7RS-WG1) (. web: scheme-reports.org/2015/working-group-1.html) and its work on the R7RS-small standard for the Scheme programming language. The R7RS-small standard (PDF) (. web: small.r7rs.org/attachment/r7rs.pdf) was ratified in 2013. The final standard as well as all working documents are now hosted here. // 欢迎访问 small.r7rs.org 。这里是 R7RS 工作组 1 (R7RS-WG1) 的官方网站，也是其制定 Scheme 编程语言 R7RS-small 标准的平台。R7RS-small 标准（ PDF ）已于 2013 年获得批准。最终标准及其所有工作文档现已托管于此。 /// (Originally, the standard and working documents were in a Trac instance, but that server was decommissioned in 2018. The pages in this domain, except for this page, are static renderings of the wiki page and tickets from a complete backup of that Trac instance as of 14 Sep 2017.) // （最初，标准文档和工作文档位于 Trac 实例中，但该服务器已于 2018 年停用。除本页面外，此域中的页面均为截至 2017 年 9 月 14 日该 Trac 实例完整备份的 wiki 页面和工单的静态渲染版本。） /// An archive of the WG1 mailing list can be found here (. web: groups.google.com/forum/#!forum/scheme-reports-wg1). // 您可以在这里找到 WG1 邮件列表的存档。 /// WG2's work (. web: scheme-reports.org/2015/working-group-2.html) on R7RS-large continues. It is hosted at r7rs.org (. web: r7rs.org), and includes most of the wiki pages used for R7RS-small, some of which have been modified since R7RS was ratified, as well as new ones, too. // WG2 对 R7RS-large 的开发工作仍在继续。它托管在 r7rs.org 上，包含了 R7RS-small 使用的大部分维基页面，其中一些页面在 R7RS 获得批准后进行了修改，此外还有一些新的页面。 /// Below is the archived WG1 home page, WikiStart (. web: small.r7rs.org/wiki/WikiStart). // 以下是已存档的 WG1 主页， WikiStart 。"
[small.wiki/.site]: https://small.r7rs.org/wiki/ "All wiki pages"
[large.src/cb]: https://codeberg.org/scheme/r7rs.git "(SchemeReport) (HTML 32.7% #e34c26, TeX 27.5% #3D6117, Racket 19.1% #3c5caa, Common Lisp 14.4% #3fb68b, Scheme 2.5% #1e4aec, other 3.5% #cccccc) Central repository for development of the R7RS Large specification of the Scheme programming language // Scheme 编程语言 R7RS Large 规范的中央存储库"

