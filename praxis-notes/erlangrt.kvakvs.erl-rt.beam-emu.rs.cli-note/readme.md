[src/gh]: https://github.com/kvakvs/ErlangRT.git "(MIT) (Languages: Rust 90.0%, Erlang 6.7%, Python 2.8%, Makefile 0.5%) Erlang Replacement Therapy. Another attempt to make Erlang runtime (BEAM emulator) in Rust. Good news: I know what to do. Bad news: I have no clue how to Rust // Erlang 替代疗法。这是用 Rust 再次尝试实现 Erlang 运行时（BEAM 模拟器）。好消息是：我知道该怎么做。坏消息是：我完全不知道该如何用 Rust 实现 Erlang。 /// ErlangRT - Runtime // ErlangRT - 运行时 /// Erlang Replacement Therapy. This is an attempt to make Erlang runtime (BEAM emulator) in Rust. This is not the first attempt so I kinda know what I'm doing. // Erlang 替代方案。这是用 Rust 实现 Erlang 运行时（BEAM 模拟器）的一次尝试。这并非我第一次尝试，所以我大概知道自己在做什么。 /// Progress to the Proof of Concept // 概念验证进展 /// - Term library 80% (remaining 80% are in progress) // 术语库已完成 80%（剩余 80% 正在进行中） /// - External Term Format (decoder 70%, encoder 0%) // 外部术语格式（解码器 70%，编码器 0%） /// - BEAM Loader - mostly done // 梁式装载机 - 基本完成 /// - VM and processes 40% // 虚拟机和进程 40% /// - VM loop and opcodes 45% (74 of 168) // VM 循环和操作码 45% (168 个中的 74 个) /// - Some basic BIFs <15% // 一些基本的 BIF<15% /// - Binaries, sub-binaries, binary heap, binary opcodes: <20% // 二进制文件、子二进制文件、二进制堆、二进制操作码：<20%"
