[src/gh]: https://github.com/facebook/prophet.git "(MIT) (Languages: Python 60.3%, R 37.9%, Stan 1.8%) Tool for producing high quality forecasts for time series data that has multiple seasonality with linear or non-linear growth. // 用于生成具有多个季节性和线性或非线性增长的时间序列数据的高质量预测工具。"
[site/ghio]: https://facebook.github.io/prophet/ "Forecasting at scale. // 规模化预测。 /// Prophet is a forecasting procedure implemented in R and Python. It is fast and provides completely automated forecasts that can be tuned by hand by data scientists and analysts. // Prophet 是一个用 R 和 Python 实现的预测程序。它运行快速，提供完全自动化的预测，可以被数据科学家和分析人员手动调整。 /// Accurate and fast. // 准确且快速。 ///: Prophet is used in many applications across Facebook for producing reliable forecasts for planning and goal setting. We’ve found it to perform better than any other approach in the majority of cases. We fit models in Stan so that you get forecasts in just a few seconds. // Prophet 被 Facebook 用于多个应用中，为规划和目标设定生成可靠的预测。我们发现它在大多数情况下表现优于任何其他方法。我们在 Stan 中拟合模型，因此你只需几秒钟即可获得预测结果。 /// Fully automatic. // 全自动。 ///: Get a reasonable forecast on messy data with no manual effort. Prophet is robust to outliers, missing data, and dramatic changes in your time series. // 无需手动操作即可在混乱数据上获得合理预测。Prophet 对离群值、缺失数据和时序数据的剧烈变化具有鲁棒性。 /// Tunable forecasts. // 可调预测。 ///: The Prophet procedure includes many possibilities for users to tweak and adjust forecasts. You can use human-interpretable parameters to improve your forecast by adding your domain knowledge. // Prophet 程序为用户提供了许多调整和优化预测的可能性。您可以通过添加领域知识，使用人类可解释的参数来改进您的预测。 /// Available in R or Python. // 支持 R 或 Python。 ///: We’ve implemented the Prophet procedure in R and Python, but they share the same underlying Stan code for fitting. Use whatever language you’re comfortable with to get forecasts. // 我们已在 R 和 Python 中实现了 Prophet 程序，但它们使用相同的底层 Stan 代码进行拟合。使用您熟悉的任何语言来获取预测。"
[pkg.pip/pypi]: https://pypi.org/project/prophet/ "(: pip install -- prophet)"
[package/cran]: https://cran.r-project.org/web/packages/prophet/ "(Version: 	1.0) (Depends: 	R (≥ 3.4.0), Rcpp (≥ 0.12.0), rlang (≥ 0.3.0.1)) (Imports: 	dplyr (≥ 0.7.7), dygraphs (≥ 1.1.1.4), extraDistr, ggplot2, grid, lubridate, methods, RcppParallel (≥ 5.0.1), rstan (≥ 2.18.1), rstantools (≥ 2.0.0), scales, StanHeaders, stats, tidyr (≥ 0.6.1), xts) (LinkingTo: 	BH (≥ 1.66.0), Rcpp (≥ 0.12.0), RcppParallel (≥ 5.0.1), RcppEigen (≥ 0.3.3.3.0), rstan (≥ 2.18.1), StanHeaders (≥ 2.18.0)) (Suggests: 	knitr, testthat, readr, rmarkdown) (Published: 	2021-03-30) (DOI: 	10.32614/CRAN.package.prophet) (Author: 	Sean Taylor [cre, aut], Ben Letham [aut]) (Maintainer: 	Sean Taylor <sjtz at pm.me>) (License: 	MIT + file LICENSE) (NeedsCompilation: 	yes) (SystemRequirements: 	GNU make, C++11) prophet: Automatic Forecasting Procedure // prophet: 自动预测程序 /// Implements a procedure for forecasting time series data based on an additive model where non-linear trends are fit with yearly, weekly, and daily seasonality, plus holiday effects. It works best with time series that have strong seasonal effects and several seasons of historical data. Prophet is robust to missing data and shifts in the trend, and typically handles outliers well. // 实现基于加性模型的时序数据预测程序，其中非线性趋势通过年、周、日季节性以及假日效应进行拟合。它最适合具有强季节性效应和多个历史季节数据的时序数据。Prophet 对缺失数据和趋势变化具有鲁棒性，通常能很好地处理异常值。"
[knows_by]: https://facebookexperimental.github.io/Robyn/ "Facebook's Prophet library to automatically decompose the trend, seasonality and holidays patterns // Facebook 的 Prophet 库自动分解趋势、季节性和假日模式"

