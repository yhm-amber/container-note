[src/gh]: https://github.com/mikecaines/stream-throttle.git "(MIT) (Languages: Rust 100.0%) Rust Stream combinator, to limit the rate at which items are produced // Rust 流组合器，用于限制项产生的速率 /// Key Features // 主要特性 /// - Throttling is implemented via poll(), and not via any sort of buffering. // 节流通过 poll() 实现，而不是通过任何形式的缓冲。 /// - The throttling behaviour can be applied to both Stream's and Future's. // 节流行为可以应用于 Stream 和 Future 。 /// - Multiple streams/futures can be throttled together as a group. // 多个流/未来可以作为一个组一起节流。 /// - Feature flags to use various timer implementations. // 使用各种计时器实现的特性标志。 /// Feature Flags // 特性标志 /// - timer-tokio: Uses the tokio::time::delay_for() timer. // timer-tokio : 使用 tokio::time::delay_for() 定时器。 /// - timer-futures-timer: Uses the futures_timer::Delay timer. // timer-futures-timer : 使用 futures_timer::Delay 定时器。 /// If you don't use the default timer (tokio), make sure to set default-features = false in your Cargo.toml, when you add stream_throttle as a dependency. // 如果你不使用默认定时器（ tokio ），在添加 stream_throttle 作为依赖时，请确保在 Cargo.toml 中设置 default-features = false 。 /// Example throttling of Stream // Stream 节流示例 /// (: let ratepool = ThrottlePool::new(ThrottleRate::new(5, Duration::new(1, 0))) // allow no more than 5 items every 1 second ;: let work = stream::repeat(()).throttle(pool).then(|_| futures::future::ready(\"do something else\")).for_each(|_| futures::future::ready(())) ;: work.await) /// Example throttling of Future // Future 节流示例 /// (: let ratepool = ThrottlePool::new(ThrottleRate::new(5, Duration::new(1, 0))) // allow no more than 5 items every 1 second ;: let work = pool.queue().then(|_| futures::future::ready(\"do something else\")) ;: work.await)"
[crates]: https://crates.io/crates/stream_throttle "(: cargo add -- stream_throttle) (MIT) (205 SLoC) (13.3 KiB) Provides a Stream combinator, to limit the rate at which items are produced. (src: gh:mikecaines/stream-throttle.git)"
