[src/gh]: https://github.com/houseabsolute/ubi.git "(Apache-2.0, MIT) (Languages: Rust 97.0%, Shell 2.7%, Other 0.3%) The Universal Binary Installer // 通用二进制安装程序 /// When I say “universal”, I mean it downloads binaries from GitHub or GitLab releases. // 我说的“通用”是指它可以从 GitHub 或 GitLab 发布页面下载二进制文件。 /// When I say “binary”, I mean it handles single-file executables like those created by most Go and Rust projects. // 我说的“二进制”是指它可以处理像大多数 Go 和 Rust 项目创建的那种单文件可执行文件。 /// When I say “installer”, I mean it plops the binary wherever you tell it to. // 我说的“安装程序”是指它会将二进制文件放置在你指定的位置。"
[lib-rs.cargo/crates]: https://crates.io/crates/ubi "(: cargo add -- ubi) (MIT OR Apache-2.0) (4.9K SLoC) (64.2 KiB) The Universal Binary Installer library // 通用二进制安装程序库 (src: gh:houseabsolute/ubi.git)"
[cli-rs.cargo/crates]: https://crates.io/crates/ubi-cli "(: cargo install -- ubi-cli) (MIT OR Apache-2.0) (299 SLoC) (32.3 KiB) The Universal Binary Installer CLI tool // 通用二进制安装程序 CLI 工具 (src: gh:houseabsolute/ubi.git)"
[knowsby]: https://mise.jdx.dev/dev-tools/backends/ubi.html "Ubi Backend /// You may install GitHub Releases and URL packages directly using ubi backend. ubi is directly compiled into the mise codebase so it does not need to be installed separately to be used. ubi is preferred over plugins for new tools since it doesn't require a plugin, supports Windows, and is really easy to use. // 您可以使用 ubi 后​​端直接安装 GitHub Releases 和 URL 包。ubi 已直接编译到 mise 代码库中，因此无需单独安装即可使用。对于新工具而言，ubi 比插件更受欢迎，因为它无需插件，支持 Windows 系统，而且非常易于使用。 /// ubi doesn't require plugins or even any configuration for each tool. What it does is try to deduce what the proper binary/tarball is from GitHub releases and downloads the right one. As long as the vendor uses a somewhat standard labeling scheme for their releases, ubi should be able to figure it out. // ubi 不需要插件，甚至不需要为每个工具进行任何配置。它的工作原理是尝试从 GitHub 发布信息中推断出正确的二进制文件/压缩包，并下载正确的版本。只要供应方使用相对标准的标签方案，ubi 就能够识别出来。 /// The code for this is inside of the mise repository at ./src/backend/ubi.rs. // 该代码位于 mise 存储库的 ./src/backend/ubi.rs 中。"

