[src/gh]: https://github.com/PerryTS/perry.git "(MIT) (Languages: Rust 85.3%, TypeScript 6.8%, JavaScript 2.8%, HTML 2.3%, Shell 1.0%, Kotlin 0.9%, Other 0.9%) A native TypeScript compiler written in Rust. Compiles TypeScript directly to executables using SWC and LLVM. // 一款用 Rust 编写的原生 TypeScript 编译器。使用 SWC 和 LLVM 直接将 TypeScript 编译成可执行文件。 /// Perry uses SWC (. swc.rs) for TypeScript parsing and LLVM (. llvm.org) for native code generation. The output is a single binary with no runtime dependencies. // Perry 使用 SWC 进行 TypeScript 解析，使用 LLVM 进行原生代码生成。输出是一个单一的二进制文件，无运行时依赖。 /// Perry compiles to native machine code via LLVM — no JIT warmup, no interpreter overhead. Key optimizations: scalar replacement of non-escaping objects (escape analysis eliminates heap allocation entirely — object fields become registers), inline bump allocator for objects that do escape, i32 loop counters for bounded array access, reassoc contract fast-math flags for f64 vectorization, integer-modulo fast path (fptosi → srem → sitofp instead of fmod), elimination of redundant js_number_coerce calls on numeric function returns, and i64 specialization for pure numeric recursive functions. // Perry 通过 LLVM 编译为原生机器代码——无需 JIT 热身，无解释器开销。关键优化：非逃逸对象的标量替换（逃逸分析完全消除堆分配——对象字段变为寄存器），逃逸对象的内联递增分配器，用于有界数组访问的 i32 循环计数器， reassoc contract 快速数学标志用于 f64 向量化，整数取模快速路径（ fptosi → srem → sitofp 而不是 fmod ），消除数值函数返回上的冗余 js_number_coerce 调用，以及纯数值递归函数的 i64 特化。 /// Perry vs compiled languages // Perry 与编译型语言 /// Perry also competes with systems languages. All implementations use f64/double to match TypeScript's number type — no SIMD intrinsics, no unsafe code. See benchmarks/polyglot/ for source and methodology. // Perry 还与系统语言竞争。所有实现都使用 f64 / double 来匹配 TypeScript 的 number 类型——没有 SIMD 内建函数，没有不安全的代码。参见 benchmarks/polyglot/ 获取源代码和方法论。 /// Perry beats or ties Rust and C++ on 7 of 8 benchmarks. Object_create dropped from 8ms to 2ms thanks to scalar replacement — non-escaping objects are decomposed into register-allocated fields, bringing Perry within striking distance of Rust/C++ (which report 0ms because their structs are stack-allocated by default). Perry's advantage on numeric benchmarks comes from domain-specific optimizations — reassoc fast-math flags, integer-modulo fast path, and i64 specialization for recursive numeric functions — that exploit properties of TypeScript's number type which strict-IEEE compilers can't assume by default. See benchmarks/polyglot/RESULTS.md for full analysis. // Perry 在 8 项基准测试中的 7 项上超越了或持平于 Rust 和 C++。由于标量替换，`Object_create` 从 8 毫秒降至 2 毫秒——非逃逸对象被分解为寄存器分配的字段，这使得 Perry 距离 Rust/C++ 仅一步之遥（它们报告 0 毫秒，因为它们的结构体默认在栈上分配）。Perry 在数值基准测试上的优势来自特定领域的优化——` reassoc ` 快速数学标志、整数取模快速路径以及递归数值函数的 `i64` 特化——这些优化利用了 TypeScript 的 ` number ` 类型的特性，而严格 IEEE 编译器无法默认假设这些特性。请参阅 ` benchmarks/polyglot/RESULTS.md ` 获取完整分析。 /// LLVM backend progress // LLVM 后端进展 /// Perry switched from Cranelift to LLVM as its sole code generation backend in v0.5.0. The initial cutover had significant performance regressions due to NaN-boxing overhead in the new backend. Subsequent optimization work recovered and surpassed the original numbers // Perry 在 v0.5.0 版本中将其唯一的代码生成后端从 Cranelift 切换到 LLVM。初始切换由于新后端中的 NaN-boxing 开销导致显著的性能回归。随后的优化工作恢复了并超过了原始数值 /// The Cranelift column is from the pre-v0.5.0 era (the old README on main). LLVM v0.5.0 was the initial cutover — it regressed badly because the new backend routed most operations through runtime helpers instead of inlining them. The current LLVM column shows the state after scalar replacement of non-escaping objects, inline bump allocators, i32 loop counters, fast-math flags, integer-mod fast paths, loop-invariant length hoisting, and redundant number-coerce elimination. LLVM now beats both Cranelift and Node on every workload. // Cranelift 列是从 v0.5.0 之前的时代（参考 main 上的旧 README）。LLVM v0.5.0 是初始切换点——由于新的后端通过运行时辅助函数路由大多数操作而不是进行内联，导致性能严重下降。当前的 LLVM 列显示了在非逃逸对象标量替换、内联递增分配器、i32 循环计数器、快速数学标志、整数模快速路径、循环不变长度提升以及冗余数字强制转换消除之后的状态。现在 LLVM 在所有工作负载上都优于 Cranelift 和 Node。 /// A note on compile times // 关于编译时间的说明 /// Cranelift is often praised for fast compilation, and it is — but the difference is smaller than you'd expect. Perry previously used Cranelift and switched to LLVM in v0.5.0. Compile times increased by only ~20-50ms (8-19%), because the bulk of Perry's compile time is SWC parsing, HIR lowering, and linking — not the codegen backend. On a typical file LLVM adds about 25ms over Cranelift while producing code that runs up to 24x faster. A worthwhile trade. // Cranelift 通常因其快速编译而受到称赞，确实如此，但差异比你预期的要小。Perry 之前使用过 Cranelift，并在 v0.5.0 版本中切换到了 LLVM。编译时间仅增加了约 20-50 毫秒（8-19%），因为 Perry 的编译时间大部分用于 SWC 解析、HIR 降级和链接——而不是代码生成后端。在典型文件上，LLVM 比 Cranelift 多增加约 25 毫秒，但生成的代码运行速度最高可达 24 倍。这是一笔值得的交易。 /// Binary Size // 二进制文件大小 /// Perry produces small, self-contained binaries with no external dependencies at run time // Perry 生成小型、自包含的二进制文件，运行时无外部依赖 /// Perry automatically detects which parts of the runtime your program uses and only links what's needed. // Perry 会自动检测您的程序使用运行时的哪些部分，并仅链接所需部分。 /// ## Installation // 安装 /// Perry ships as a prebuilt-binary npm package — the fastest way to try it, and the only install path that works on all seven supported platforms (macOS arm64/x64, Linux x64/arm64 glibc + musl, Windows x64) with one command: // Perry 作为预构建二进制 npm 包提供——这是尝试它的最快方式，也是唯一适用于所有七个支持平台（macOS arm64/x64, Linux x64/arm64 glibc + musl, Windows x64）的安装路径，只需一条命令即可完成： /// (: npm install @perryts/perry ;: npx perry compile src/main.ts -o yourapp && ./yourapp # Project-local -- recommended — pins Perry's version alongside your deps.) (: npm install -g @perryts/perry ;: perry compile src/main.ts -o yourapp && ./yourapp # Global) (: npx -y @perryts/perry compile src/main.ts -o yourapp && ./yourapp # Zero-install, one-shot) /// @perryts/perry is a thin launcher; npm automatically picks the matching prebuilt via optionalDependencies (@perryts/perry-darwin-arm64, @perryts/perry-linux-x64-musl, etc.) based on your os / cpu / libc. Requires Node.js ≥ 16 and a system C toolchain for linking (same as any Perry install — see Requirements). // @perryts/perry 是一个轻量级启动器；npm 会根据你的 os / cpu / libc 自动选择匹配的预构建版本 via optionalDependencies ( @perryts/perry-darwin-arm64 , @perryts/perry-linux-x64-musl , 等.)。需要 Node.js ≥ 16 以及用于链接的系统 C 工具链（与任何 Perry 安装相同——参见 Requirements）。"
[cli.npm/npmjs]: https://npmjs.com/package/@perryts/perry "(: npm i -- @perryts/perry) (License: MIT) Native TypeScript compiler. Compiles TypeScript source code directly to native executables via LLVM — no VM, no JIT warmup, no Node at runtime. // 原生 TypeScript 编译器。通过 LLVM 直接将 TypeScript 源代码编译为原生可执行文件——无需虚拟机，无需 JIT 热身，运行时无需 Node。 /// Installing picks the right prebuilt binary for your platform automatically — @perryts/perry declares per-platform packages as optionalDependencies and npm (≥8.12) selects the matching one based on os / cpu / libc. // 安装时会自动选择适合您平台的预构建二进制文件—— @perryts/perry 按平台声明 optionalDependencies 包，npm（≥8.12）根据 os / cpu / libc 选择匹配的版本。 (src: gh:PerryTS/perry.git)"
[cli.winget/winstall]: https://winstall.app/apps/PerryTS.Perry "(: winget install -e --id PerryTS.Perry) Native TypeScript compiler that compiles TypeScript to native executables"
[site/com]: https://perryts.com/ "One Codebase. Every Platform. Native Performance. // 一个代码库。所有平台。原生性能。 /// Perry compiles TypeScript to native GUI and CLI apps on macOS, iPadOS, iOS, Android, Linux, Windows, watchOS, tvOS, WebAssembly, and the Web. No runtime. No Electron. Just native binaries. // Perry 将 TypeScript 编译为 macOS、iPadOS、iOS、Android、Linux、Windows、watchOS、tvOS、WebAssembly 和 Web 上的原生 GUI 和 CLI 应用程序。无需运行时。无需 Electron。只需原生二进制文件。 /// (: perry compile main.ts # Compiling main.ts... # ✓ Compiled executable: main (2.3 MB) ;: ./main) /// Why Perry? // 为什么选择 Perry？ /// Everything you need to compile TypeScript to native applications // 编译 TypeScript 为原生应用程序所需的一切 /// - No Runtime Required // 无需运行时 ///: Produces standalone native executables. No Node.js, no V8, no runtime dependencies. Just a single binary that runs anywhere. // 生成独立的原生可执行文件。无需 Node.js，无需 V8，无需运行时依赖。只需一个可在任何地方运行的二进制文件。 /// - Fast Compilation // 快速编译 ///: Direct TypeScript to native code compilation using SWC for parsing and LLVM for optimized code generation. No intermediate JavaScript. // 使用 SWC 进行解析和 LLVM 进行优化代码生成的 TypeScript 直接编译为原生代码。无需中间 JavaScript。 /// - Small Binaries // 小型二进制文件 ///: Output binaries are typically 2-5MB. With optional V8 runtime for JS npm packages, 15-20MB. Ship less, deploy faster. // 输出二进制文件通常为 2-5MB。使用可选的 V8 运行时代码包，则为 15-20MB。减少打包内容，加快部署。 /// - Type-Safe // 类型安全 ///: Leverages TypeScript's type system for optimization. Types enable better code generation through monomorphization. // 利用 TypeScript 的类型系统进行优化。类型通过单例化（monomorphization）实现更好的代码生成。 /// - Comprehensive Standard Library // 全面的标凈库 ///: Built-in native implementations of fs, path, crypto, os, Buffer, child_process, and more. Use familiar Node.js APIs. // 内置 fs、path、crypto、os、Buffer、child_process 等原生实现。使用熟悉的 Node.js API。 /// - Optional V8 Runtime // 可选 V8 运行时 ///: Need to use a pure JavaScript npm package? Enable the V8 runtime flag for full npm ecosystem compatibility. // 需要使用纯 JavaScript 的 npm 包？启用 V8 运行时标志以实现完整的 npm 生态系统兼容性。 /// - 25+ Native UI Widgets // 25+ 原生 UI 组件 ///: Buttons, text fields, text areas, tables, canvas, scroll views, QR codes, secure fields, splash screens, and more — all compiling to real platform widgets via AppKit, GTK4, Win32, UIKit, and JNI. // 按钮、文本字段、文本区域、表格、画布、滚动视图、二维码、安全字段、启动画面等——所有这些都通过 AppKit、GTK4、Win32、UIKit 和 JNI 编译为真实的平台组件。 /// - Compile-Time Plugin System // 编译时插件系统 ///: Modules compose at build time — no runtime plugin overhead, no IPC boundaries. Your dependencies become direct native function calls in the final binary. // 模块在构建时组合——没有运行时插件开销，没有 IPC 边界。您的依赖项在最终二进制文件中成为直接的本地函数调用。 /// - True Multi-Threading // 真正的多线程 ///: Real OS threads with parallelMap, parallelFilter, and spawn. Compile-time safety rejects mutable captures — no SharedArrayBuffer, no workers, just threads. // 具有 parallelMap、parallelFilter 和 spawn 的真实操作系统线程。编译时安全性拒绝可变捕获——没有 SharedArrayBuffer，没有工作线程，只有线程。 /// - Compile-Time i18n // 编译时国际化 ///: Automatic string extraction, CLDR plural rules for 30+ locales, compile-time validation. Translations baked into the binary with near-zero runtime lookup. // 自动字符串提取，支持 30 多个地区的 CLDR 复数规则，编译时验证。翻译直接嵌入二进制文件，运行时查找几乎为零。 /// Native on Every Platform // 跨平台原生 /// Perry compiles your TypeScript to native UI frameworks, WebAssembly, and JavaScript — not web views, not Electron. Real native widgets on every platform, plus the web. // Perry 将您的 TypeScript 编译为原生 UI 框架、WebAssembly 和 JavaScript —— 不是 Web 视图，也不是 Electron。在所有平台上都有真实的原生组件，并且支持 Web。 /// macOS: AppKit, iOS: UIKit, iPadOS: UIKit, Android: Views, Linux: GTK4, Windows: Win32, watchOS: SwiftUI, tvOS: SwiftUI, WASM: WebAssembly, Web: JavaScript /// The only framework that checks every box // 唯一一个满足所有要求的框架 ///: TypeScript compiled to native code. Real platform widgets. No runtime overhead. // TypeScript 编译为原生代码。真实平台组件。无运行时开销。 /// 30+ Native npm Packages // 30+ 原生 npm 包 ///: Popular npm packages reimplemented in native Rust. No npm install, no node_modules, just fast native code. // 流行的 npm 包以原生 Rust 实现。无需 npm 安装，无需 node_modules，只需快速原生代码。 ///; Just import and use - Perry automatically uses the native implementation // 只需导入并使用 - Perry 会自动使用原生实现 /// From Code to App Store // 从代码到应用商店 ///: Perry doesn't just compile your app — it gets it into your users' hands. // Perry 不仅编译你的应用，还将其交付到用户手中。 /// - Build & Sign // 构建和签名 ///: Cross-platform builds from one command. Code signing for macOS, iOS, Android, and Windows handled for you. No wrestling with Xcode provisioning profiles or Android keystores. // 通过一条命令进行跨平台构建。自动处理 macOS、iOS、Android 和 Windows 的代码签名。无需与 Xcode 配置文件或 Android 密钥库进行繁琐的配置。 /// - Distribute // 分发 ///: Push to the App Store, Play Store, or ship direct downloads. Perry Publish handles packaging, notarization, and submission. // 推送到 App Store、Play Store 或直接分发下载。Perry Publish 负责打包、公证和提交。 /// - Verify // 验证 ///: Powered by Geisterhand. Automated UI testing across all 6 platforms. Know your app works everywhere before your users tell you it doesn't. // 由 Geisterhand 驱动。跨 6 个平台进行自动化 UI 测试。在用户告诉你它不起作用之前，了解你的应用在所有地方都能正常工作。 /// How It Works // 工作原理 ///: From TypeScript source to native executable in seconds // 从 TypeScript 源码到原生可执行文件仅需几秒钟 /// - 0 - CODE - (TypeScript) - { .ts files // .ts 文件 } /// - 1 - SWC - (Parser // 解析器) - { Fast parsing // 快速解析 } /// - 2 - HIR - (Transform // 转换) - { Monomorphization // 单例化 } /// - 3 - LLVM - (Codegen // 代码生成) - { Machine code // 机器码 } /// - 4 - BIN - (Executable // 可执行文件) - { 2-5 MB binary // 2-5 MB 二进制文件 } /// Compiler Internals // 编译器内部 ///: The key design decisions that make Perry fast // 使 Perry 高效运行的关键设计决策 /// - 01 - NaN-Boxing ///: Values are stored as 64-bit floats with special bit patterns for pointers, enabling union types without runtime overhead. This allows (string | number)[] to work efficiently. // 值以 64 位浮点数形式存储，并使用特殊的位模式表示指针，从而无需运行时开销即可实现联合类型。这使得 (string | number)[] 能够高效运行。 {: By encoding type information directly in the IEEE 754 NaN payload bits, Perry avoids the need for tagged unions or boxed values. A single 64-bit word can represent any JavaScript value type — numbers use their natural representation, while strings, objects, and other heap-allocated values use pointer bit patterns that fall within the NaN range. // 通过将类型信息直接编码在 IEEE 754 NaN 负载位中，Perry 避免了使用标记联合或装箱值的需要。单个 64 位字可以表示任何 JavaScript 值类型——数字使用其自然表示，而字符串、对象和其他堆分配的值则使用落在 NaN 范围内的指针位模式。 :} /// - 02 - Monomorphization // 单例化 ///: Generics are specialized at compile time, like Rust. Each type instantiation generates optimized code, eliminating runtime type checking overhead. // 泛型在编译时进行特化，类似于 Rust。每种类型实例化都会生成优化的代码，消除了运行时类型检查的开销。 {: When you write Array'<'number'>' and Array'<'string'>', Perry generates two separate, fully optimized implementations. This means array operations on numbers use direct machine arithmetic — no type guards, no dynamic dispatch, no boxing. // 当你编写 Array'<'number'>' 和 Array'<'string'>', Perry 会生成两个独立的、完全优化的实现。这意味着对数字的数组操作使用直接的机器算术——没有类型守卫，没有动态分发，没有装箱。 :} /// - 03 - Static Dispatch // 静态分发 ///: No virtual tables. Method calls are resolved at compile time, enabling direct function calls and inlining optimizations. // 没有虚拟表。方法调用在编译时解析，从而实现直接函数调用和内联优化。 {: Traditional OOP runtimes use vtables for method resolution, adding a layer of indirection on every call. Perry resolves all method calls statically during compilation, turning interface method calls into direct jumps. This also unlocks aggressive inlining — small methods are often folded directly into their call sites. // 传统的面向对象运行时使用虚表（vtables）进行方法解析，每个调用都增加了一层间接性。Perry 在编译期间静态解析所有方法调用，将接口方法调用转换为直接跳转。这也解锁了激进的内联——小方法通常直接折叠到它们的调用位置。 :} /// - 04 - Zero-Cost Abstractions // 零成本抽象 ///: TypeScript classes, interfaces, and generics compile to efficient native code with no runtime representation overhead. // TypeScript 类、接口和泛型编译成高效的本地代码，没有任何运行时表示的开销。 {: TypeScript's type system exists only at compile time — and Perry takes this literally. Interfaces produce zero runtime code. Class hierarchies are flattened. Generic constraints are resolved to concrete types. The compiled binary contains only the actual logic, with none of the abstraction machinery that interpreters typically carry. // TypeScript 的类型系统仅在编译时存在——而 Perry 正是字面意义上的实现。接口不会生成任何运行时代码。类层次结构被扁平化。泛型约束被解析为具体类型。编译后的二进制文件仅包含实际逻辑，不包含解释器通常携带的抽象机制。 :} /// TypeScript → Native. Compile TypeScript directly to standalone executables on macOS, iOS, Android, Linux, and Windows. No runtime, no Electron. // TypeScript → 本地（原生）。将 TypeScript 直接编译为 macOS、iOS、Android、Linux 和 Windows 上的独立可执行文件。无需运行时，无需 Electron。"
[docs/.site]: https://docs.perryts.com/ "Perry is a native TypeScript compiler that compiles TypeScript source code directly to native executables. No JavaScript runtime, no JIT warmup, no V8 — your TypeScript compiles to a real binary. // Perry 是一个原生 TypeScript 编译器，可以直接将 TypeScript 源代码编译为原生可执行文件。无需 JavaScript 运行时，无需 JIT 热身，无需 V8 — 你的 TypeScript 编译为一个真正的二进制文件。 /// (: perry hello.ts -o hello ;: ./hello)"
[showcase/.site]: https://perryts.com/showcase "..."
