[src/gh]: https://github.com/proot-me/proot-rs.git "(GPL-3.0) (Languages: Rust 92.4%, Shell 5.5%, C 1.5%, Dockerfile 0.6%) Rust implementation of PRoot, a ptrace-based sandbox // 基于 ptrace 的沙盒 PRoot 的 Rust 实现 (: proot-rs -R /home/user -- mkdir /abc # will be equivalent to: mkdir /home/user/abc) (: proot-rs -R -- ./img.oci /bin/sh)"
[pkg.x/x-cmd]: https://x-cmd.com/install/proot "(: xd kal -- proot) (: x install -- proot ;: x env use proot)"

