[src/gh]: https://github.com/spacedriveapp/spacebot.git "(FSL-1.1-ALv2) (Languages: Rust 80%, TypeScript 17.3%, Jinja 1.6%, Shell 0.5%, Nix 0.3%, Just 0.1%, Other 0.2%) An AI agent for teams, communities, and multi-user environments. // 适用于团队、社区和多用户环境的人工智能代理。 /// # Spacebot // 太空机器人 // The agent harness that runs teams, communities, and companies. /// 运行团队、社区和公司的代理框架。 /// Spacebot is opinionated agent infrastructure, built for teams and usable by anyone. State belongs in structured storage, not markdown files the LLM manages. Memory lives in a typed graph in SQLite. Autonomy runs on a task state machine linked to goals, not a heartbeat.json. The LLM reasons. The system holds state. // Spacebot 是一个具有明确设计理念的智能体基础设施，专为团队打造，任何人都可以使用。 状态信息存储在结构化存储中，而不是 LLM 管理的 Markdown 文件中。 内存存储在 SQLite 的类型化图中。自主性基于与目标关联的任务状态机运行，而不是基于 heartbeat.json 文件。LLM 负责推理。系统负责维护状态。 /// It gets smarter the more you use it. After complex tasks, the agent captures what it learned as reusable skills. After conversations go idle, a background process silently saves skills and memories worth keeping. Every session builds on the last — without any user action. // 它用得越多就越智能。 完成复杂任务后，智能体会将学习到的知识提炼成可复用的技能。对话结束后，后台进程会默默保存值得保留的技能和记忆。每次会话都建立在前一次的基础上——无需用户任何操作。 /// It works out of the box and scales from one person to a whole community. // 它开箱即用，可以从一个人扩展到整个社区。 /// ## The Problem // 问题 /// Most AI agent frameworks run everything in a single session. One LLM thread handles conversation, thinking, tool execution, memory retrieval, and context compaction, all in one loop. When it's doing work, it can't talk to you. When it's compacting, it goes dark. When it retrieves memories, raw results pollute the context with noise. // 大多数人工智能代理框架都在单个会话中运行所有操作。一个 LLM 线程负责处理对话、思考、工具执行、记忆检索和上下文压缩，所有这些都在一个循环中完成。当它执行任务时，无法与你对话。当它进行上下文压缩时，它会进入休眠状态。当它检索记忆时，原始结果会污染上下文，产生噪声。 /// Spacebot splits the monolith into specialized processes that only do one thing, and delegate everything else. // Spacebot 将单体应用拆分成只做一件事的专用进程，并将其他所有事情委托出去。 /// ## Spacebot + Spacedrive  太空机器人 + 太空驱动 /// Spacebot pairs with Spacedrive, an open-source cross-platform file manager built on a virtual distributed filesystem. Neither requires the other. When paired, Spacebot is the only agent harness with direct integration into a cross-device filesystem. // Spacebot 可与 Spacedrive 配合使用，Spacedrive 是一款基于虚拟分布式文件系统的开源跨平台文件管理器。两者互不依赖。配合使用后，Spacebot 是唯一一款可直接集成到跨设备文件系统中的代理框架。 /// ### What Pairing Enables Today // 配对技术如今能带来什么？ /// Multi-device access: one Spacebot instance, all your devices. Talk to your agent from your phone while a worker executes on your server. Spacedrive's P2P layer (Iroh/QUIC) routes from every device through the paired node to Spacebot. No separate SDK, no separate auth. // 多设备访问： 只需一个 Spacebot 实例，即可连接所有设备。您可以通过手机与代理对话，同时服务器端的工作进程也会运行。Spacedrive 的 P2P 层（Iroh/QUIC）会将每个设备的流量路由到配对节点，最终到达 Spacebot。无需单独的 SDK，也无需单独的身份验证。 /// Remote execution: workers can target any device in your library. A task that needs your home server's GPU, your work laptop's local repos, or your phone's camera routes through Spacedrive's permission system to the target device. From the agent's perspective, the tool call is identical. // 远程执行： 工作进程可以指定库中的任何设备。需要使用家庭服务器 GPU、工作笔记本电脑本地代码库或手机摄像头的任务，会通过 Spacedrive 的权限系统路由到目标设备。从代理的角度来看，工具调用是完全相同的。 /// File system intelligence: every directory can carry context nodes describing what it contains and what policies apply. When the agent navigates your filesystem it gets that context, not a blind listing. // 文件系统智能： 每个目录都可以携带上下文节点，描述其包含的内容以及适用的策略。当代理程序浏览文件系统时，它会获取这些上下文信息，而不是盲目地罗列目录。 /// Safe data access: Spacedrive indexes external sources (Gmail, Slack, Obsidian, GitHub, Apple Notes, contacts, calendar, browser history) as searchable data the agent can query. Every record passes through a local prompt injection classifier (Prompt Guard 2) before reaching the agent. The agent can search your emails without a malicious email hijacking it. // 安全的数据访问： Spacedrive 将外部来源（Gmail、Slack、Obsidian、GitHub、Apple Notes、联系人、日历、浏览器历史记录）索引为可搜索数据，供代理程序查询。每条记录在到达代理程序之前都会经过本地提示注入分类器（Prompt Guard 2）。代理程序可以搜索您的电子邮件，而不会被恶意邮件劫持。 /// ### Where This Is Going // 这将会走向何方？ /// A company deploys Spacebot + Spacedrive on their infrastructure. Employees install Spacedrive on their devices and join the company library. The company agent has access to employee devices through Spacedrive's permission system, with individual-level controls. The org graph in Spacebot defines hierarchy and delegation: which agents report to which, who can approve what, how tasks flow. // 一家公司在其基础设施上部署了 Spacebot + Spacedrive。员工在自己的设备上安装 Spacedrive 并加入公司库。公司代理通过 Spacedrive 的权限系统访问员工设备，并拥有个人级别的控制权限。Spacebot 中的组织结构图定义了层级关系和授权：代理的汇报对象、权限审批权限以及任务流转方式。 /// An employee talks to the company agent from their MacBook. The agent knows their projects, their device, their role, and can spawn workers on any authorized machine. They switch to their personal Spacedrive library and connect to their home Spacebot, with personal data and personal context. The app is the same. The agent is different. // 员工通过 MacBook 与公司代理进行对话。代理了解员工的项目、设备和角色，并能在任何授权机器上创建工作进程。他们切换到个人 Spacedrive 库，连接到自己的 Spacebot，其中包含个人数据和个人上下文。应用程序本身没有变化，但代理不同。 /// No other agent harness is building this. It's a category. // 目前还没有其他代理框架能够实现这一点。这开创了一个全新的类别。"
[site/sh]: https://spacebot.sh/ "Spacebot // 太空机器人 /// Thinks, executes, and responds — concurrently, not sequentially. // 思考、执行和响应——同时进行，而不是按顺序进行。 /// Built for large teams and communities. // 专为大型团队和社区而设计。"

