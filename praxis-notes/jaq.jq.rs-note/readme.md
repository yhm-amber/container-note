[src/gh]: https://github.com/01mf02/jaq.git "(MIT) (Languages: Rust 97.4%, jq 2.1%, Shell 0.5%) A jq clone focussed on correctness, speed, and simplicity // jq 克隆，专注于正确性、速度和简单性 /// jaq (pronounced /ʒaːk/, like Jacques) is a clone of the JSON data processing tool jq. jaq aims to support a large subset of jq's syntax and operations. // jaq（发音为 /ʒaːk/，如 Jacques ）是 JSON 数据处理工具 jq 的克隆。 jaq 旨在支持 jq 语法和操作的大部分子集。 /// You can try jaq online on the jaq playground. Instructions for the playground can be found [here](./jaq-play). // 您可以在 jaq Playground 上在线尝试 jaq。可以在此处找到游乐场的说明。 /// jaq focuses on three goals: // jaq 专注于三个目标： /// - Correctness: jaq aims to provide a more correct and predictable implementation of jq, while preserving compatibility with jq in most cases. // 正确性：jaq 旨在提供更加正确和可预测的 jq 实现，同时在大多数情况下保持与 jq 的兼容性。 /// - Performance: I created jaq originally because I was bothered by the long start-up time of jq 1.6, which amounts to about 50ms on my machine. This can be particularly seen when processing a large number of small files. Although the startup time has been vastly improved in jq 1.7, jaq is still faster than jq on many other benchmarks. // 性能：我最初创建 jaq 是因为我对 jq 1.6 的启动时间过长感到困扰，在我的机器上启动时间约为 50 毫秒。当处理大量小文件时，这一点尤其明显。尽管 jq 1.7 中的启动时间得到了极大改善，但在许多其他基准测试中 jaq 仍然比 jq 更快。 /// - Simplicity: jaq aims to have a simple and small implementation, in order to reduce the potential for bugs and to facilitate contributions. // 简单性：jaq 的目标是实现简单且小型的实现，以减少出现错误的可能性并促进贡献。 /// I drew inspiration from another Rust program, namely [jql](gh: yamafaktory/jql). However, unlike jql, jaq aims to closely imitate jq's syntax and semantics. This should allow users proficient in jq to easily use jaq. // 我从另一个 Rust 程序 jql 中获得了灵感。然而，与 jql 不同的是，jaq 的目标是紧密模仿 jq 的语法和语义。这应该可以让精通 jq 的用户能够轻松使用 jaq 。"

[lib.crates]: https://crates.io/crates/jaq-core "(: cargo add -- jaq-core)"
[cli.crates]: https://crates.io/crates/jaq "(: cargo install --locked -- jaq)"
[cli.bin/brew]: https://formulae.brew.sh/formula/jaq "(: brew install -- jaq)"

[play.web]: https://gedenkt.at/jaq/

