[src/gh]: https://github.com/binbandit/workty.git "(Apache-2.0, MIT) (Languages: Rust 100.0%) Git worktrees as daily-driver workspaces // Git 工作树作为日常工作区 /// I got mass-context-switching burnout. Every git stash felt like shoving clothes under the bed before guests arrive. WIP commits? \"WIP: stuff\" cluttering my history. Worktrees fixed it, but the raw commands are clunky. // 我因为频繁切换上下文而精疲力竭。每次 git stash 都像是在客人来之前把衣服塞到床底下。WIP 提交？“WIP: stuff”堆满了我的历史记录。工作树解决了这个问题，但原始命令用起来很笨拙。 /// So I built this. Now I just wnew feat/login, do the work, wcd back to main when someone pings me about a bug, fix it, and wcd right back. No stashing. No mental overhead. // 所以我建了这个。现在我只 wnew feat/login ，完成任务，有人告诉我遇到 bug 时， wcd 切回主角色，修复 bug，然后再 wcd 回来。无需仓库，也无需任何脑力投入。 /// Why not just... // 为什么不直接…… /// - Why not git stash? — Stashes get lost. I've got 47 stashes in one repo right now. No idea what's in them. // 为什么不用 git stash ？ ——暂存区很容易丢失。我现在一个仓库里就有 47 个暂存区，完全不知道里面有什么。 /// - Why not WIP commits? — They clutter history and I forget to squash them. // 为什么不提交 WIP 版本？ ——它们会使历史记录变得混乱，而且我经常忘记合并它们。 /// - Why not just raw git worktree? — The commands are verbose and I kept forgetting the syntax. This is just a nice wrapper. // 为什么不直接使用原始的 git worktree 呢？ ——命令太冗长，而且我总是记不住语法。这只是一个简洁的封装而已。 /// Safety // 安全 /// Won't delete dirty worktrees unless you --force. Prompts before destructive stuff unless you --yes. Every error tells you what to do next. // 除非使用 --force 参数，否则不会删除脏工作树。除非使用 --yes 参数，否则会在执行破坏性操作前发出提示。每个错误都会告诉您下一步该怎么做。"
[cli.cargo/crates]: https://crates.io/crates/git-workty '(: cargo install -- git-workty # Then add shell integration (so wcd, wnew, wgo actually change your directory) // 然后添加 shell 集成（这样就能真正用 wcd 、 wnew 、 wgo 更改目录）) (: eval "$(git workty init zsh)" # for zsh) (: eval "$(git workty init bash)" # for bash) (: git workty init fish | source # for fish) (: git workty install-man # To generate and install the manpage automatically: This will write git-workty.1 to ~/.local/share/man/man1. You may need to add ~/.local/share/man to your MANPATH environment variable if your system doesn\'t pick it up automatically. // 自动生成并安装手册页：这会将 git-workty.1 写入 ~/.local/share/man/man1 。如果您的系统没有自动识别它，您可能需要将 ~/.local/share/man 添加到 MANPATH 环境变量中。) (MIT OR Apache-2.0) (2.6K SLoC) (37.4 KiB) Git worktrees as daily-driver workspaces // Git 工作树作为日常工作区 (src: gh:binbandit/workty.git)'

