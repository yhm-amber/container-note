[src/gh]: https://github.com/ArchiveBox/ArchiveBox.git "(MIT) (Languages: Python 70.6%, HTML 24.4%, Shell 2.5%, Dockerfile 1.1%, JavaScript 0.9%, CSS 0.5%) 🗃 Open source self-hosted web archiving. Takes URLs/browser history/bookmarks/Pocket/Pinboard/etc., saves HTML, JS, PDFs, media, and more... // 🗃 开源自托管网络存档。支持 URLs/浏览器历史记录/书签/Pocket/Pinboard 等，保存 HTML、JS、PDF、媒体等... /// We aim to make your data immediately useful, and kept in formats that other programs can read directly. As output, we save standard HTML, PNG, PDF, TXT, JSON, WARC, SQLite, all guaranteed to be readable for decades to come. ArchiveBox also has a CLI, REST API, and webhooks so you can set up integrations with other services. // 我们的目标是使您的数据立即变得有用，并保存在其他程序可以直接读取的格式中。作为输出，我们保存标准 HTML、PNG、PDF、TXT、JSON、WARC、SQLite，所有这些都保证在未来几十年内仍然可读。ArchiveBox 还具有 CLI、REST API 和 webhook，因此您可以与其他服务设置集成。 /// 📥 You can feed ArchiveBox URLs one at a time, or schedule regular imports from your bookmarks or history, social media feeds or RSS, link-saving services like Pocket/Pinboard, our Browser Extension, and more. // 📥 您可以逐个输入 ArchiveBox 的 URL，也可以从您的书签或历史记录、社交媒体信息流或 RSS、Pocket/Pinboard 等链接保存服务、我们的浏览器扩展程序等处安排定期导入。 /// It saves snapshots of the URLs you feed it in several redundant formats. // 它将您输入的 URL 快照保存为多种冗余格式。 /// It also detects any content featured inside pages & extracts it out into a folder: // 它还会检测页面中的任何特色内容并将其提取到一个文件夹中： /// - 🌐 HTML/Any websites ➡️ original HTML+CSS+JS, singlefile HTML, screenshot PNG, PDF, WARC, title, article text, favicon, headers, ... // 🌐 HTML/任何网站 ➡️ original HTML+CSS+JS , singlefile HTML , screenshot PNG , PDF , WARC , title , article text , favicon , headers , ... /// - 🎥 Social Media/News ➡️ post content TXT, comments, title, author, images, ... // 🎥 社交媒体/新闻 ➡️ post content TXT , comments , title , author , images , ... /// - 🎬 YouTube/SoundCloud/etc. ➡️ MP3/MP4s, subtitles, metadata, thumbnail, ... // 🎬 YouTube/SoundCloud 等 ➡️ MP3/MP4 s, subtitles , metadata , thumbnail , ... /// - 💾 Github/Gitlab/etc. links ➡️ clone of GIT source code, README, images, ... // 💾 Github/Gitlab 等链接 ➡️ clone of GIT source code , README , images , ... /// - ✨ and more, see Output Formats below... // ✨ 更多，请见下方输出格式... /// You can run ArchiveBox as a Docker web app to manage these snapshots, or continue accessing the same collection using the pip-installed CLI, Python API, and SQLite3 APIs. All the ways of using it are equivalent, and provide matching features like adding tags, scheduling regular crawls, viewing logs, and more... // 你可以将 ArchiveBox 作为 Docker 网络应用程序来运行以管理这些快照，或者继续使用 pip -安装的 CLI、Python API 和 SQLite3 API 访问相同的集合。使用它的所有方式都是等效的，并提供添加标签、安排定期抓取、查看日志等相同功能... /// 🛠️ ArchiveBox uses standard tools like Chrome, wget, & yt-dlp, and stores data in ordinary files & folders. // 🛠️ ArchiveBox 使用 Chrome、 wget 和 yt-dlp 等标准工具，并将数据存储在普通文件和文件夹中。 /// (no complex proprietary formats, all data is readable without needing to run ArchiveBox) // （不使用复杂的专有格式，所有数据无需运行 ArchiveBox 即可读取） /// The goal is to sleep soundly knowing the part of the internet you care about will be automatically preserved in durable, easily accessible formats for decades after it goes down. // 目标是确保你关心的互联网部分在停止运行后，能够自动以持久且易于访问的格式保存数十年，让你安心。 /// ArchiveBox is free for everyone to self-host, but we also provide support, security review, and custom integrations to help NGOs, governments, and other organizations run ArchiveBox professionally: // ArchiveBox 可供所有人自由自托管，但我们也提供支持、安全审查和定制集成服务，以帮助非政府组织、政府及其他机构专业地运行 ArchiveBox /// - Journalists: crawling during research, preserving cited pages, fact-checking & review // 记者： crawling during research ， preserving cited pages ， fact-checking & review /// - Lawyers: collecting & preserving evidence, detecting changes, tagging & review // 律师： collecting & preserving evidence ， detecting changes ， tagging & review /// - Researchers: analyzing social media trends, getting LLM training data, crawling pipelines // 研究人员： analyzing social media trends ， getting LLM training data ， crawling pipelines /// - Individuals: saving bookmarks, preserving portfolio content, legacy / memoirs archival // 个人： saving bookmarks ， preserving portfolio content ， legacy / memoirs archival /// - Governments: snapshoting public service sites, recordkeeping compliance // 政府： snapshoting public service sites ， recordkeeping compliance /// Docker ///: (: mkdir -p -- ~/archivebox/data && cd -- ~/archivebox/data ;: docker run -v $PWD:/data -it -- archivebox/archivebox init --setup # Create a new empty directory and initialize your collection (can be anywhere) // 创建一个新的空目录并初始化你的收藏（可以在任何地方）) (: docker run -v $PWD:/data -p 8000:8000 -- archivebox/archivebox # Optional: Start the server then login to the Web UI http://127.0.0.1:8000 ⇢ Admin # completely optional, CLI can always be used without running a server) (: docker run -v $PWD:/data -it --rm -- archivebox/archivebox help) /// Pip: ///: (: pip install --upgrade-- archivebox yt-dlp playwright ;: playwright install --with-deps -- chromium ;: archivebox version) (: mkdir -p -- ~/archivebox/data && cd -- ~/archivebox/data ;: archivebox init --setup # Create a new empty directory and initialize your collection (can be anywhere) // 创建一个新的空目录并初始化你的收藏（可以在任何地方）) (: archivebox server 0.0.0.0:8000 # Optional: Start the server then login to the Web UI http://127.0.0.1:8000 ⇢ Admin # completely optional, CLI can always be used without running a server) (: archivebox help) /// Whether in Docker or not, ArchiveBox commands work the same way, and can be used to access the same data on-disk. For example, you could run the Web UI in Docker Compose, and run one-off commands with pip-installed ArchiveBox. // 无论是否在 Docker 中，ArchiveBox 命令都工作相同的方式，并且可以用来访问磁盘上的相同数据。例如，你可以在 Docker Compose 中运行 Web UI，并使用 pip -已安装的 ArchiveBox 运行一次性命令。 ///: (: archivebox add --depth=1 'https://example.com' # add a URL with pip-installed archivebox on the host) (: docker compose run -- archivebox add --depth=1 'https://example.com' # or w/ Docker Compose) (: docker run -it --rm -v $PWD:/data -- archivebox/archivebox add --depth=1 'https://example.com' # or w/ Docker, all equivalent)"
[site/io]: https://archivebox.io/ "🗃 Open source self-hosted web archiving. Takes URLs/browser history/bookmarks/Pocket/Pinboard/etc., saves HTML, JS, PDFs, media, and more... // 🗃 开源自托管网页存档。支持 URLs/浏览器历史记录/书签/Pocket/Pinboard 等，保存 HTML、JS、PDF 文件、媒体文件等... /// ArchiveBox is a powerful, self-hosted internet archiving solution to collect, save, and view websites offline. // ArchiveBox 是一个强大的、可本地部署的互联网存档解决方案，用于收集、保存和离线查看网站。 /// Without active preservation effort, everything on the internet eventually disappears or degrades. Archive.org does a great job as a centralized service, but saved URLs have to be public, and they can’t save every type of content. // 如果没有主动的保存措施，互联网上的所有内容最终都会消失或退化。Archive.org 作为一个集中式服务做得很好，但保存的 URL 必须是公开的，而且它们无法保存所有类型的内容。 /// ArchiveBox is an open source tool that lets organizations & individuals archive both public & private web content while retaining control over their data. It can be used to save copies of bookmarks, preserve evidence for legal cases, backup photos from FB/Insta/Flickr or media from YT/Soundcloud/etc., save research papers, and more… // ArchiveBox 是一个开源工具，允许组织和个人存档公共和私人网络内容，同时保留对其数据的控制。它可用于保存书签副本、保存法律案件证据、备份来自 FB/Insta/Flickr 的照片或来自 YT/Soundcloud 等的媒体、保存研究论文等……"
[demoapp.wui/.site]: https://demo.archivebox.io/ "Demo // 演示"
[pkg.pip/pypi]: https://pypi.org/project/archivebox/ "(: pip install -U --ignore-installed -- archivebox yt-dlp playwright ;: playwright install --with-deps -- chromium) (License: MIT License (MIT)) (Requires: Python <3.12, >=3.9) Self-hosted internet archiving solution. // 自托管互联网存档解决方案。 (src: gh:ArchiveBox/ArchiveBox.git)"
[pkg.oci/ghcr]: https://github.com/ArchiveBox/ArchiveBox/pkgs/container/archivebox "(: docker pull -- ghcr.io/archivebox/archivebox:main)  (src: gh:ArchiveBox/ArchiveBox.git)"
[pkg.oci/dhub]: https://hub.docker.com/r/archivebox/archivebox "(: docker pull -- docker.io/archivebox/archivebox:main) (* Sponsored OSS)  (src: gh:ArchiveBox/ArchiveBox.git)"
[browser-ext.src/gh]: https://github.com/ArchiveBox/archivebox-browser-extension.git "(MIT) (Languages: JavaScript 78.3%, HTML 21.7%) Official ArchiveBox browser extension: automatically/manually preserve your browsing history using ArchiveBox. // 官方 ArchiveBox 浏览器扩展：使用 ArchiveBox 自动/手动保存您的浏览历史记录。 /// 🗃 ArchiveBox Browser Extension // 🗃 ArchiveBox 浏览器扩展 /// This is a browser extension that lets you send individual browser tabs or all URLs matching certain patterns to your ArchiveBox instance for offline preservation. This has a couple of benefits: // 这是一个浏览器扩展，允许您将单个浏览器标签页或所有匹配特定模式的 URL 发送到您的 ArchiveBox 实例以进行离线保存。这有几个好处： /// - Own your data: save the web content that matters to you most, protect against link rot // 拥有您的数据：保存对您最重要的网络内容，防止链接失效 /// - Protect your data: save offline copies of pages in common, durable formats that will last for generations // 保护您的数据：以常见、持久的格式保存页面离线副本，使其能够保存数代 /// - Use your data: collect and tag important bookmarks, full-text search through your browsing history, automatically push captured data into other systems using ArchiveBox's APIs // 使用您的数据：收集和标记重要的书签，通过全文搜索您的浏览历史，使用 ArchiveBox 的 API 自动将捕获的数据推送到其他系统 /// ✨ Originally contributed by TJ Horner (@ gh: tjhorner), now maintained by Benmuth (@ gh: benmuth) and the ArchiveBox (@ gh: ArchiveBox) team. // ✨ 最初由 TJ Horner (@tjhorner) 贡献，现由 @benmuth 和 ArchiveBox 团队维护。"
[plg.crx/chrome]: https://chrome.google.com/webstore/detail/habonpimjphpdnmcfkaockjnffodikoj "(Size: 275KiB)"
[plg.xpi/firefox]: https://addons.mozilla.org/firefox/addon/archivebox-exporter/ "(Size: 36.45 KB)"
[plg.shortcut/icloud]: https://icloud.com/shortcuts/0d3a526e7d524447aa8c1bd63ac49014 "- Safari (not yet supported, use Save-to-ArchiveBox Shortcut instead) // Safari（尚未支持，请使用 Save-to-ArchiveBox 快捷方式）"
