[src/gh]: https://github.com/thevangelist/dembrandt.git "(MIT) (Languages: JavaScript 100.0%) Extract any website’s design system into design tokens in a few seconds: logo, colors, typography, borders, and more. One command. // 在几秒钟内提取任何网站的设计系统为设计令牌：标志、颜色、排版、边框等。一个命令。 /// What to expect from extraction? // 提取后可以期待什么？ /// - Colors (semantic, palette, CSS variables) // 颜色（语义、调色板、CSS 变量） /// - Typography (fonts, sizes, weights, sources) // 排版（字体、大小、粗细、来源） /// - Spacing (margin/padding scales) // 间距（边距/内边距尺度） /// - Borders (radius, widths, styles, colors) // 边框（半径、宽度、样式、颜色） /// - Shadows // 阴影 /// - Components (buttons, inputs, links) // 组件（按钮、输入框、链接） /// - Breakpoints // 断点 /// - Icons & frameworks // 图标 & 框架 /// Use Cases // 应用场景 /// - Brand audits & competitive analysis // 品牌审计与竞争分析 /// - Design system documentation // 设计系统文档 /// - Reverse engineering brands // 逆向工程品牌 /// - Multi-site brand consolidation // 多站点品牌整合 /// ## How It Works // 工作原理 // Uses Playwright to render the page, extracts computed styles from the DOM, analyzes color usage and confidence, groups similar typography, detects spacing patterns, and returns actionable design tokens. // 使用 Playwright 渲染页面，从 DOM 中提取计算样式，分析颜色使用和置信度，对相似的排版进行分组，检测间距模式，并返回可操作的设计令牌。 /// ### Extraction Process // 提取过程 /// - Browser Launch - Launches Chromium with stealth configuration // 浏览器启动 - 使用隐身配置启动 Chromium /// - Anti-Detection - Injects scripts to bypass bot detection // 反检测 - 注入脚本以绕过机器人检测 /// - Navigation - Navigates to target URL with retry logic // 导航 - 带重试逻辑地导航到目标 URL /// - Hydration - Waits for SPAs to fully load (8s initial + 4s stabilization) // 水合 - 等待 SPAs 完全加载（初始 8 秒 + 稳定 4 秒） /// - Content Validation - Verifies page content is substantial (>500 chars) // 内容验证 - 验证页面内容是否充实（>500 字符） /// - Parallel Extraction - Runs all extractors concurrently for speed // 并行提取 - 并行运行所有提取器以提高速度 /// - Analysis - Analyzes computed styles, DOM structure, and CSS variables // 分析 - 分析计算样式、DOM 结构和 CSS 变量 /// - Scoring - Assigns confidence scores based on context and usage // 评分 - 根据上下文和使用情况分配置信度分数 /// - Color Confidence // 颜色置信度 /// - High — Logo, brand elements, primary buttons // 高 — Logo、品牌元素、主要按钮 /// - Medium — Interactive elements, icons, navigation // Medium — 交互元素、图标、导航 /// - Low — Generic UI components (filtered from display) // Low — 通用 UI 组件（已从显示中过滤） /// - Only shows high and medium confidence colors in terminal. Full palette in JSON. // 终端中仅显示高和中等置信度的颜色。JSON 中包含完整调色板。 /// ## Limitations // 局限性 /// - Dark mode requires --dark-mode flag (not automatically detected) // 暗黑模式需要 --dark-mode 标志（不会自动检测） /// - Hover/focus states extracted from CSS (not fully interactive) // 悬停/聚焦状态从 CSS 中提取（不完全是交互式的） /// - Canvas/WebGL-rendered sites cannot be analyzed (e.g., Tesla, Apple Vision Pro demos) // 基于 Canvas/WebGL 渲染的网站无法分析（例如，Tesla、Apple Vision Pro 演示） /// - JavaScript-heavy sites require hydration time (8s initial + 4s stabilization) // JavaScript 重的网站需要水合时间（初始 8 秒 + 稳定 4 秒） /// - Some dynamically-loaded content may be missed // 部分动态加载的内容可能被遗漏 /// - Default viewport is 1920x1080 (use --mobile for responsive analysis) // 默认视口为 1920x1080（使用 --mobile 进行响应式分析） /// ## Ethics & Legality // 道德与法律 /// Dembrandt extracts publicly available design information (colors, fonts, spacing) from website DOMs for analysis purposes. This falls under fair use in most jurisdictions (USA's DMCA § 1201(f), EU Software Directive 2009/24/EC) when used for competitive analysis, documentation, or learning. // Dembrandt 从网站的 DOM 中提取公开可用的设计信息（颜色、字体、间距）用于分析目的。在大多数司法管辖区，当用于竞争分析、文档编制或学习时，这属于合理使用（美国的 DMCA § 1201(f)、欧盟软件指令 2009/24/EC）。 /// Legal: Analyzing public HTML/CSS is generally legal. Does not bypass protections or violate copyright. Check site ToS before mass extraction. // 法律：分析公共 HTML/CSS 通常合法。不会绕过保护或侵犯版权。在大量提取前请检查网站服务条款。 /// Ethical: Use for inspiration and analysis, not direct copying. Respect servers (no mass crawling), give credit to sources, be transparent about data origin. // 道德：用于灵感和分析，而非直接复制。尊重服务器（禁止大规模爬取），注明来源，公开数据来源。"
[cli.npm/npmjs]: https://npmjs.com/package/dembrandt "(: npm i -g -- dembrandt) (: npx dembrandt cantian.ai # Basic extraction for https://cantian.ai/ .) (: npx -- dembrandt --json-only -- cantian.ai # JSON output for https://cantian.ai/ .) (License: MIT) (Unpacked Size: 117 KB) (Total Files: 7) 🎨 Dembrand /// A CLI tool for extracting design tokens and brand assets from any website. Powered by Playwright with advanced bot detection avoidance. // 一个用于从任何网站提取设计代币和品牌资产的命令行工具。由 Playwright 驱动，具有高级机器人检测规避功能。 (src: gh:thevangelist/dembrandt.git)"

