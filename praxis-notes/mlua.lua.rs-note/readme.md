[src/gh]: https://github.com/mlua-rs/mlua.git "(MIT) (Rust 100%) High level Lua 5.4/5.3/5.2/5.1 (including LuaJIT) and Roblox Luau bindings to Rust with async/await support // 高级 Lua 5.4/5.3/5.2/5.1（包括 LuaJIT）和 Roblox Luau 与 Rust 的绑定，支持异步/等待"

[rlua.src/gh]: https://github.com/mlua-rs/rlua.git "(MIT) (Rust 100%) High level Lua bindings to Rust // 高级 Lua 与 Rust 的绑定  /// rlua is now deprecated in favour of mlua: see below for migration information /// rlua is now a thin transitional wrapper around mlua; it is recommended to use mlua directly for new projects and to migrate to it when convenient. mlua was a fork of rlua which has recently seen more development activity and new features. // rlua 现已弃用，取而代之的是 mlua ：请参阅下文了解迁移信息 /// rlua 现在是一个薄薄的过渡包装纸 mlua; 推荐使用 mlua 直接用于新项目并在方便时迁移到它。 mlua 曾经是一个 rlua 的叉子最近看到了更多的开发活动和新的特征。"

[crates]: https://crates.io/crates/mlua
