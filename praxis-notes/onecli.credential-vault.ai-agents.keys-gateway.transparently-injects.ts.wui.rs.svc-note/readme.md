[src/gh]: https://github.com/onecli/onecli.git "(Apache-2.0) (Languages: TypeScript 66.2%, Rust 28.3%, Dockerfile 1.8%, CSS 1.7%, JavaScript 1.1%, Shell 0.9%) Open-source credential vault, give your AI agents access to services without exposing keys. // 开源凭证保险库，让您的 AI 代理访问服务而无需暴露密钥。 /// The secret vault for AI agents. // AI 代理的秘密保险库。 /// Store once. Inject anywhere. Agents never see the keys. // 存储一次，随处注入。代理永远不会看到密钥。 /// What is OneCLI? // OneCLI 是什么？ /// OneCLI is an open-source gateway that sits between your AI agents and the services they call. Instead of baking API keys into every agent, you store credentials once in OneCLI and the gateway injects them transparently. Agents never see the secrets. // OneCLI 是一个开源网关，位于您的 AI 代理和服务之间。您不必将 API 密钥嵌入每个代理中，而是在 OneCLI 中存储一次凭证，网关会透明地注入它们。代理永远不会看到秘密。 /// Why we built it: AI agents need to call dozens of APIs, but giving each agent raw credentials is a security risk. OneCLI solves this with a single gateway that handles auth, so you get one place to manage access, rotate keys, and see what every agent is doing. // 我们构建它的原因：AI 代理需要调用数十个 API，但向每个代理提供原始凭证存在安全风险。OneCLI 通过一个处理认证的网关解决了这个问题，因此您可以在一个地方管理访问权限、轮换密钥，并查看每个代理正在做什么。 /// How it works: You store your real API credentials in OneCLI and give your agents placeholder keys (e.g. FAKE_KEY). When an agent makes an HTTP call through the gateway, the OneCLI proxy matches the request to the right credentials, swaps the FAKE_KEY for the REAL_KEY, decrypts them, and injects them into the outbound request. The agent never touches the real secrets. It just makes normal HTTP calls and the proxy handles the swap. // 工作原理：您在 OneCLI 中存储您的真实 API 凭证，并给您的代理提供占位符密钥（例如 FAKE_KEY ）。当代理通过网关发起 HTTP 调用时，OneCLI 代理将请求与正确的凭证进行匹配，将 FAKE_KEY 交换为 REAL_KEY ，解密它们，并将它们注入出站请求。代理永远不会接触真实秘密。它只需发起正常的 HTTP 调用，代理会处理交换。 /// Architecture // 架构 /// - Rust Gateway (. .@main/apps/proxy): fast HTTP gateway that intercepts outbound requests and injects credentials. Agents authenticate with access tokens via Proxy-Authorization headers. // Rust Gateway：快速 HTTP 网关，拦截出站请求并注入凭证。代理通过 Proxy-Authorization 头使用访问令牌进行身份验证。 /// - Web Dashboard (. .@main/apps/web): Next.js app for managing agents, secrets, and permissions. Provides the API the gateway uses to resolve which credentials to inject for each request. // Web Dashboard：用于管理代理、密钥和权限的 Next.js 应用程序。提供网关使用的 API，用于解析为每个请求注入哪些凭证。 /// - Secret Store: AES-256-GCM encrypted credential storage. Secrets are decrypted only at request time, matched by host and path patterns, and injected by the gateway as headers. // 密钥库：使用 AES-256-GCM 加密的凭据存储。密钥仅在请求时解密，通过主机和路径模式进行匹配，并由网关作为头部注入。 /// Quick Start // 快速入门 /// (: docker run --pull always -p 10254:10254 -p 10255:10255 -v onecli-data:/app/data -- ghcr.io/onecli/onecli # then open: http://localhost:10254, create an agent, add your secrets, and point your agent's HTTP gateway to localhost:10255 // 然后打开 http://localhost:10254 ，创建一个 Agent 、添加您的密钥，并将您 Agent 的 HTTP 网关指向 localhost:10255 。) /// Features // 功能 /// - Transparent credential injection: agents make normal HTTP calls, the gateway handles auth // 透明凭证注入：代理进行正常 HTTP 调用，网关处理认证 /// - Encrypted secret storage: AES-256-GCM encryption at rest, decrypted only at request time // 加密密钥存储：静态 AES-256-GCM 加密，仅在请求时解密 /// - Host & path matching: route secrets to the right API endpoints with pattern matching // 主机与路径匹配：通过模式匹配将密钥路由到正确的 API 端点 /// - Multi-agent support: each agent gets its own access token with scoped permissions // 多代理支持：每个代理都有自己的访问令牌，具有范围权限 /// - No external dependencies: runs with embedded PGlite (or bring your own PostgreSQL) // 无外部依赖：使用嵌入式 PGlite 运行（或自带 PostgreSQL） /// - Two auth modes: single-user (no login) for local use, or Google OAuth for teams // 两种认证模式：单用户（无需登录）用于本地使用，或 Google OAuth 用于团队 /// - Rust gateway: fast, memory-safe HTTP gateway with MITM interception for HTTPS // Rust 网关：快速、内存安全的 HTTP 网关，支持 HTTPS 的 MITM 拦截"
[site/sh]: https://onecli.sh/ "Give your agents access, // 为您的智能体提供访问权限， /// not your secrets. // 而不是您的秘密。 /// Open-source credential vault. Your agents call services and never see a key. // 开源凭证保险库。您的智能体调用服务，而无需看到密钥。 /// Why /// - [API keys scattered everywhere. // API 密钥散落各处。] -> [One encrypted vault. // 一个加密保险库。] /// - [Revoking access means hunting down keys. // 撤销访问意味着搜寻密钥。] -> [Revoke once, everywhere. // 一次性撤销，全局生效。] /// - [Agent acts without asking. // 代理无需询问即行动作。] -> [Human approves before it's done. Define policies per action. // 人类在完成前批准。针对每个动作定义策略。] /// Works with any agent framework // 支持任何代理框架 /// OpenClaw, NanoClaw, IronClaw, Dify, n8n, OpenHands, + any framework /// How it works // 如何工作 /// 1. Start OneCLI // 1. 启动 OneCLI ///: (: docker run -d -p 10254:10254 -p 10255:10255 -v onecli-data:/app/data -- ghcr.io/onecli/onecli #> { 'status': 'running', 'dashboard': 'localhost:10254', 'gateway': 'localhost:10255' }) /// 2. Add credentials // 2. 添加凭证 ///: (: open http://localhost:10254 #> { 'credentials': ['google', 'github', 'slack'] }) /// 3. Your agent just works // 3. 你的代理可以直接使用 ///: (: curl -x http://localhost:10255 https://gmail.googleapis.com/gmail/v1/users/me/messages #> { 'messages': [{ 'from': 'alice@acme.co', 'subject': 'Re: Q3 roadmap' }, { 'from': 'github@noreply.com', 'subject': 'PR #42 merged' }, { 'from': 'linear@linear.app', 'subject': 'ONC-118 assigned to you' }] }) /// What you get // 您获得 /// - Zero code changes // 无需代码更改 ///: Set HTTPS_PROXY and your agent's existing HTTP calls get credentials injected. No SDK, no wrapper. // 设置 HTTPS_PROXY，您的代理现有的 HTTP 调用将自动注入凭证。无需 SDK，无需包装。 /// - Encrypted vault // 加密保险库 ///: Credentials stored in local KMS or OneCLI Cloud. Never written to disk in plain text. // 凭证存储在本地 KMS 或 OneCLI 云中。从未以明文形式写入磁盘。 /// - Full audit trail // 完整审计追踪 ///: See every API call, which agent made it, and when. Set limits, approve or deny in real-time. // 查看每个 API 调用，哪个代理发起的，以及何时发起。设置限制，实时批准或拒绝。 /// - One Docker container // 一个 Docker 容器 ///: Proxy, vault, and dashboard all run from a single docker run command. // 代理、保险库和仪表板都从一个 docker run 命令中运行。 /// The open-source auth layer for AI agents."
[docs/.site]: https://onecli.sh/docs "OneCLI Docs // OneCLI 文档 /// Open-source secret vault for AI agents // 开源 AI 代理密钥库 /// OneCLI is an open-source secret vault for AI agents. Store credentials once, inject them anywhere. Your agents never see the keys. // OneCLI 是一个开源的 AI 代理密钥库。只需存储一次凭证，即可在任何地方注入它们。您的代理永远不会看到密钥。 /// Under the hood, OneCLI runs a transparent MITM proxy that intercepts outgoing HTTP requests, injects credentials from an encrypted vault, and routes them to the right service. No code changes needed. Manage everything from the web dashboard. // 在底层，OneCLI 运行一个透明的 MITM 代理，拦截出站 HTTP 请求，从加密的密钥库中注入凭证，并将它们路由到正确的服务。无需修改代码。通过网页仪表板管理一切。"
[cli.oci/ghcr]: https://github.com/onecli/onecli/pkgs/container/onecli "(: docker run --pull always -p 10254:10254 -p 10255:10255 -v onecli-data:/app/data -- ghcr.io/onecli/onecli) (Apache-2.0)  (src: gh:onecli/onecli.git)"
