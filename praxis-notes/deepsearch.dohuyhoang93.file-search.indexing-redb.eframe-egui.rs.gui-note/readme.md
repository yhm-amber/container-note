[src/gh]: https://github.com/dohuyhoang93/DeepSearch.git "(Apache-2.0) (Languages: Rust 93.9%, Shell 4.6%, Python 1.5%) DeepSearch // 深度搜索 /// DeepSearch is a high-performance, cross-platform file indexing and search utility developed in Rust. // DeepSearch 是一个用 Rust 开发的高性能、跨平台文件索引和搜索工具。 /// It builds a fast, searchable index of your local folders and network shared directories, allowing for near-instantaneous file name lookups. // 它可以快速构建本地文件夹和网络共享目录的可搜索索引，从而实现近乎瞬时的文件名查找。 /// ## Features // 特征 /// - Modern & Responsive GUI: A clean and intuitive graphical user interface built with eframe (egui). // 现代且响应迅速的图形用户界面 ：使用 eframe (egui) 构建的简洁直观的图形用户界面。 /// - Light & Dark Modes: Toggle between light and dark themes to suit your preference. // 浅色和深色模式 ：根据您的喜好在浅色和深色主题之间切换。 /// - Persistent Indexing: Uses a local redb database to store file indexes, allowing for fast subsequent searches without needing to re-scan entire directories. // 持久索引 ：使用本地 redb 数据库存储文件索引，从而无需重新扫描整个目录即可快速进行后续搜索。 /// - Incremental Updates: A \"Rescan\" feature efficiently rebuilds the index for a location in the background. Once complete, it atomically swaps the old index for the new one, ensuring data consistency and high performance. // 增量更新 ：“重新扫描”功能可在后台高效地重建某个位置的索引。完成后，它会以原子方式将旧索引替换为新索引，从而确保数据一致性和高性能。 /// - Optimized for Performance: Leverages Rust’s concurrency model (rayon) for both indexing and searching to maximize throughput and keep the UI responsive. // 针对性能进行了优化 ：利用 Rust 的并发模型（ rayon ）进行索引和搜索，以最大限度地提高吞吐量并保持 UI 的响应速度。 /// - Intelligent Search: File name search is insensitive to case and diacritics (e.g., thanh will match Thành). // 智能搜索 ：文件名搜索不区分大小写和变音符号（例如， thanh 将匹配 Thành ）。 /// - Live Search: Perform direct, on-the-fly searches within specified folders, including content search. // 实时搜索 ：在指定文件夹内执行直接、即时的搜索，包括内容搜索。 /// - Content Search: Search within the actual content of various file types (PDF, DOCX, XLSX, plain text). // 内容搜索 ：在各种文件类型（PDF、DOCX、XLSX、纯文本）的实际内容中进行搜索。 /// - Cross-Platform: Runs on Windows, macOS, and Linux. // 跨平台 ：可在 Windows、macOS 和 Linux 上运行。 /// ## Interface Preview // 界面预览 /// The application is organized into two main tabs: // 该应用程序分为两个主要选项卡： /// 1. Indexing Tab: For adding new folders to the index and managing existing ones (rescan, delete). // 索引选项卡 ：用于向索引添加新文件夹和管理现有文件夹（重新扫描、删除）。 /// 2. Search Tab: For performing searches across one or more indexed locations. // 搜索选项卡 ：用于在一个或多个已索引的位置执行搜索。 /// A status bar at the bottom provides feedback on current operations and shows a progress bar for long-running tasks. // 底部的状态栏提供有关当前操作的反馈，并显示长时间运行任务的进度条。"
[site/ghio]: https://dohuyhoang93.github.io/DeepSearch/ "DeepSearch // 深度搜索 /// DeepSearch is a high-performance file search utility developed in Rust. It enables fast and intuitive search operations across local folders and network shared directories (SMB), with support for parallel processing and a user-friendly CLI. // DeepSearch 是一个用 Rust 开发的高性能文件搜索工具。它支持在本地文件夹和网络共享目录 (SMB) 中进行快速直观的搜索操作，并支持并行处理和用户友好的命令行界面。 /// Features // 特征 /// - Insensitive to case and diacritics when searching file names. // 搜索文件名时不区分大小写和变音符号。 /// - Recursive directory search through all subfolders. // 递归搜索所有子文件夹。 /// - CLI fast, simple, neotech style. // CLI 快速、简单、新科技风格。 /// - Optimized performance in both local and remote environments, leveraging Rust’s concurrency model. // 利用 Rust 的并发模型，优化了本地和远程环境下的性能。 /// Search Procedure // 搜索程序 /// 1. Enter the path to the target directory. // 请输入目标目录的路径。 /// 2. Input the name of the file you want to search for. // 请输入您要查找的文件名。 /// 3. During the search process, the following commands are available: // 搜索过程中，可以使用以下命令： /// 3. - Press s to stop the search // 按 s 停止搜索 /// 3. - Press p to pause // 按 p 暂停 /// 3. - Press r to resume // 按 r 继续"

