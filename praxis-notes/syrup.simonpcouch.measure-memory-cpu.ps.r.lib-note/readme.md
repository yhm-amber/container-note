[src/gh]: https://github.com/simonpcouch/syrup.git "(MIT) (Languages: R 100.0%) Measure Memory and CPU Usage of R Code // 测量 R 代码的内存和 CPU 使用情况 /// The goal of syrup is to measure memory and CPU usage of R code by regularly taking snapshots of calls to the system command ps. The package provides an entry point (albeit coarse) to profile usage of system resources by R code run in parallel. // syrup 的目标是测量 R 代码的内存和 CPU 使用情况，通过定期对系统命令 ps 的调用进行快照。该软件包提供了一个入口点（尽管比较粗糙），用于分析并行运行的 R 代码对系统资源的使用情况。 /// The package name is an homage to syrupy (SYstem Resource Usage Profile …um, Yeah), a Python tool at jeetsukumaran/Syrupy (. gh: jeetsukumaran/Syrupy.git). // 软件包的名称是对 syrupy（SYstem Resource Usage Profile …um, Yeah）的一种致敬，这是一个位于 jeetsukumaran/Syrupy 上的 Python 工具。"
[site/ghio]: https://simonpcouch.github.io/syrup/ "The main function in the syrup package is the function by the same name. The main argument to syrup() is an expression, and the function outputs a tibble. Supplying a rather boring expression // syrup 包中的主要功能是同名的函数。 syrup() 的主要参数是一个表达式，该函数输出一个 tibble。提供一个相当无聊的表达式 /// In this tibble, id defines a specific time point at which process usage was snapshotted, and the remaining columns show output derived from ps::ps(). Notably, pid is the process ID, ppid is the process ID of the parent process, pct_cpu is the percent CPU usage, and rss is the resident set size (a measure of memory usage). // 在这个 tibble 中， id 定义了一个特定的时间点，在该时间点对进程使用情况进行了快照，其余列显示的是从 ps::ps() 派生的输出。值得注意的是， pid 是进程 ID， ppid 是父进程的进程 ID， pct_cpu 是 CPU 使用百分比， rss 是驻留集大小（内存使用量的一个度量）。 /// The function works by: // 该函数的工作原理是： /// - Setting up another R process sesh that queries memory information at a regular interval, // 启动另一个 R 进程 sesh ，该进程定期查询内存信息， /// - Evaluating the supplied expression, // 评估提供的表达式， /// - Reading the memory information back into the main process from sesh, // 将内存信息从 sesh 读回到主进程， /// - Closing sesh, and then // 关闭 sesh ，然后 /// - Returning the memory information. // 返回内存信息。"
[package/cran]: https://cran.r-project.org/web/packages/syrup/ "(Version: 	0.1.4) (Depends: 	bench) (Imports: 	callr, dplyr, ps, purrr, rlang, tibble, vctrs, withr) (Suggests: 	testthat (≥ 3.0.0)) (Published: 	2025-06-21) (DOI: 	10.32614/CRAN.package.syrup) (Author: 	Simon Couch ORCID iD [aut, cre], Posit Software, PBC [cph, fnd]) (Maintainer: 	Simon Couch <simon.couch at posit.co>) (License: 	MIT + file LICENSE) (NeedsCompilation: 	no) syrup: Measure Memory and CPU Usage for Parallel R Code // syrup: 测量并行 R 代码的内存和 CPU 使用情况 /// Measures memory and CPU usage of R code by regularly taking snapshots of calls to the system command 'ps'. The package provides an entry point (albeit coarse) to profile usage of system resources by R code run in parallel. // 通过定期对系统命令'ps'的调用进行快照来测量 R 代码的内存和 CPU 使用情况。该包提供了一个入口点（尽管比较粗糙），用于分析并行运行的 R 代码对系统资源的使用情况。 (src: gh:simonpcouch/syrup.git)"
[knows_by]: https://simonpcouch.com/software "- syrup /// Measure Memory and CPU Usage for Parallel R Code // 测量并行 R 代码的内存和 CPU 使用情况"
