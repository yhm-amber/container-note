
[src/gh]: https://github.com/jagt/clumsy.git "(MIT) (Languages: C 85.1%, Lua 6.3%, Zig 6.3%, Python 1.2%, Other 1.1%) clumsy makes your network condition on Windows significantly worse, but in a controlled and interactive manner. // 笨拙使您的 Windows 网络状况显著恶化，但以可控和交互式的方式进行。 /// Leveraging the awesome WinDivert (reqrypt.org/windivert.html), clumsy stops living network packets and capture them, lag/drop/tamper/.. the packets on demand, then send them away. Whether you want to track down weird bugs related to broken network, or evaluate your application on poor connections, clumsy will come in handy: // 利用神奇的 WinDivert，笨拙地停止网络数据包的传输并捕获它们，根据需求延迟/丢弃/篡改...数据包，然后将其发送出去。无论您是想追踪与网络故障相关的奇怪错误，还是评估在较差的连接上的应用程序，笨拙将派上用场： /// - No installation. // 无需安装 /// - No need for proxy setup or code change in your application. // 无需在您的应用程序中设置代理或更改代码。 /// - System wide network capturing means it works on any application. // 系统级网络捕获意味着它适用于任何应用程序。 /// - Works even if you're offline (ie, connecting from localhost to localhost). // 即使离线也能正常工作（例如，从本地主机连接到本地主机）。 /// - Your application keeps running, while clumsy can start and stop anytime. // 您的应用程序持续运行，而笨拙的可以随时启动和停止。 /// - Interactive control how bad the network can be, with enough visual feedback to tell you what's going on. // 交互式控制网络可能有多糟糕，同时提供足够的视觉反馈来告诉你发生了什么。"
[site/ghio]: https://jagt.github.io/clumsy/
[knowsby]: https://medium.com/@dfarkhod/how-to-simulate-unstable-network-85b2f518b546 "So, I was on this wild quest to figure out why my signalr clients just wouldn’t stop reconnecting. I stumbled upon this nifty tool that’s free, open-source, and totally chill. It’s called “Clumsy,” and you can grab it here: https://github.com/jagt/clumsy. Just download the latest version and start having a blast with your network. // 所以，我正在进行一场疯狂的探索，试图弄清楚为什么我的 SignalR 客户端总是不断重连。我偶然发现了一个免费、开源且非常酷的工具。它叫做“Clumsy”，你可以从这里获取：https://github.com/jagt/clumsy。只需下载最新版本，就可以开始享受你的网络之旅了。 /// This little champ, turned out to be my savior when it comes to dealing with network hiccups. // 这个小家伙，在处理网络故障时竟然成了我的救星。"
