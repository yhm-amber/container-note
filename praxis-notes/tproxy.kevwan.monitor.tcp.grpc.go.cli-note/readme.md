[src/gh]: https://github.com/kevwan/tproxy.git "(MIT) (Languages: Go 98.4%, Dockerfile 1.6%) A cli tool to proxy and analyze TCP connections. // 一个用于代理和分析 TCP 连接的命令行工具。 /// When I develop backend services and write go-zero (. gh:zeromicro/go-zero.git), I often need to monitor the network traffic. For example: // 当我在开发后端服务以及使用 go-zero 编写代码时，我经常需要监控网络流量。例如： /// - monitoring gRPC connections, when to connect and when to reconnect // 监控 gRPC 连接，何时连接以及何时重新连接 /// - monitoring MySQL connection pools, how many connections and figure out the lifetime policy // 监控 MySQL 连接池，有多少个连接，并确定连接的生命周期策略 /// - monitoring any TCP connections on the fly // 实时监控任意 TCP 连接 /// (: tproxy -p 8088 -r localhost:8081 -t grpc -d 100ms # - listen on localhost and port 8088 // 监听 localhost 和端口 8088 # - redirect the traffic to localhost:8081 // 将流量重定向到 localhost:8081 # - protocol type to be gRPC // 协议类型为 gRPC # - delay 100ms for each packets // 为每个数据包延迟 100 毫秒)"
[pkg.go/pkg.go.dev]: https://pkg.go.dev/github.com/kevwan/tproxy "(: go install -- github.com/kevwan/tproxy@latest)"
[pkg.x/x-cmd]: https://x-cmd.com/install/tproxy "(: xd alp -- tproxy) (: x install -- tproxy)"

