[src/gh]: https://github.com/GitoxideLabs/gitoxide.git "(Apache-2.0, MIT) (Languages: Rust 91.3%, Shell 7.3%, HTML 1.1%, Other 0.3%) An idiomatic, lean, fast & safe pure Rust implementation of Git // 一个符合惯例、精简、快速且安全的纯 Rust 实现的 Git /// gitoxide is an implementation of git written in Rust for developing future-proof applications which strive for correctness and performance while providing a pleasant and unsurprising developer experience. // gitoxide 是用 Rust 编写的 git 实现，用于开发面向未来的应用程序，力求在保证正确性和性能的同时，提供愉快且不出意外的开发者体验。 /// There are two primary ways to use gitoxide: // 使用 gitoxide 主要有两种方式： /// - As Rust library: Use the gix crate as a Cargo dependency for API access. // 作为 Rust 库 ：使用 gix crate 作为 Cargo 依赖项进行 API 访问。 /// - As command-line tool: The gix binary as development tool to help testing the API in real repositories, and the ein binary with workflow-enhancing tools. Both binaries may forever be unstable, do not rely on them in scripts. // 作为命令行工具 ： gix 二进制文件作为开发工具，用于帮助在真实代码库中测试 API； ein 二进制文件则包含工作流程增强工具。这两个二进制文件可能永远都不稳定。 不要在脚本中依赖它们 。"
[cli.cargo/crates]: https://crates.io/crates/gitoxide "(: cargo install -- gitoxide) (MIT OR Apache-2.0) (4.1K SLoC) (85.4 KiB) A command-line application for interacting with git repositories // 一个用于与 Git 仓库交互的命令行应用程序 (src: gh:GitoxideLabs/gitoxide.git)"
[lib.cargo/crates]: https://crates.io/crates/gix "(: cargo add -- gix) (MIT OR Apache-2.0) (22K SLoC) (292 KiB) Interact with git repositories just like git would // 像使用 Git 一样与 Git 仓库交互 (src: gh:GitoxideLabs/gitoxide.git)"
[knows_by]: https://github.com/josephvoss/git-remote-s3.git "Heavily leverages gitoxide and rust-s3 to do the heavy lifting talking to the respective backends. Inspired by git-remote-dropbox, git-remote-ipfs, \"How to Write a New Git Protocol\", and \"Developing a Custom Remote Git Helper\" // 大量利用 gitoxide 和 rust-s3 负责繁重的通信工作 各自的后端。灵感来自 git-remote-dropbox ， git-remote-ipfs 、 “如何编写新的 Git 协议” 和 “开发自定义远程 Git 助手”"

