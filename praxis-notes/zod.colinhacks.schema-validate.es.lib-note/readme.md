[src/gh]: https://github.com/colinhacks/zod.git "(MIT) (Languages: TypeScript 88.0%, MDX 10.6%, Other 1.4%) TypeScript-first schema validation with static type inference // 基于 TypeScript 的静态类型推断模式验证 /// What is Zod? // Zod 是什么？ /// Zod is a TypeScript-first validation library. Define a schema and parse some data with it. You'll get back a strongly typed, validated result. // Zod 是一个以 TypeScript 为主的验证库。定义一个模式并使用它解析一些数据。你会得到一个强类型、经过验证的结果。 /// Features // 特性 /// - Zero external dependencies // 零外部依赖 /// - Works in Node.js and all modern browsers // 适用于 Node.js 和所有现代浏览器 /// - Tiny: 2kb core bundle (gzipped) // 体积小： 2kb 核心包（压缩后） /// - Immutable API: methods return a new instance // 不可变 API：方法返回新的实例 /// - Concise interface // 简洁的接口 /// - Works with TypeScript and plain JS // 支持 TypeScript 和原生 JavaScript /// - Built-in JSON Schema conversion // 内置 JSON Schema 转换 /// - Extensive ecosystem // 广泛的生态系统"
[site/dev]: https://zod.dev/ "Zod provides an MCP server that can be used by agents to search Zod's docs. To add to your editor, follow [these instructions] (. https://share.inkeep.com/zod/mcp). Zod also provides an [llms.txt] (. https://zod.dev/llms.txt) file. // Zod 提供了一个 MCP 服务器，代理可以使用它来搜索 Zod 的文档。要添加到您的编辑器中，请按照以下说明操作。Zod 还提供了一个 llms.txt 文件。"
[docs/.site]: https://zod.dev/api "To validate data, you must first define a schema. Schemas represent types, from simple primitive values to complex nested objects and arrays. // 要验证数据，你必须首先定义一个模式。模式表示类型，从简单的原始值到复杂的嵌套对象和数组。"
[pkg.deno/jsrio]: https://jsr.io/@zod/zod "(: deno add -- jsr:@zod/zod) (: import * as zod from 'jsr:@zod/zod') TypeScript-first schema declaration and validation library with static type inference // 以 TypeScript 为主的模式声明和验证库，具有静态类型推断 (src: gh:colinhacks/zod.git)"
[pkg.npm/npmjs]: https://npmjs.com/package/zod "(: npm i -- zod) (License: MIT) (Unpacked Size: 4.03 MB) (Total Files: 662)  (src: gh:colinhacks/zod.git)"
[twitter]: https://twitter.com/colinhacks

