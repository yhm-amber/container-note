[src/gh]: https://github.com/Lulzx/zs3.git "(WTFPL) (Languages: Zig 69.1%, Python 30.9%) S3-compatible storage in Zig. Zero dependencies. // Zig 中兼容 S3 的存储。零依赖。 /// zs3 /// S3-compatible storage in ~3K lines of Zig. Zero dependencies. Optional distributed mode. // 约 3000 行 Zig 代码即可实现与 S3 兼容的存储。零依赖。可选分布式模式。 /// Why // 为什么 /// Most S3 usage is PUT, GET, DELETE, LIST with basic auth. You don't need 200k lines of code for that. // 大多数 S3 的使用方式是 PUT、GET、DELETE 和 LIST，并采用基本身份验证。你不需要为此编写 20 万行代码。 /// ## What it does // 它的作用 /// ### Standalone Mode: // 独立模式： /// - Full AWS SigV4 authentication (works with aws-cli, boto3, any SDK) // 完整的 AWS SigV4 身份验证（适用于 aws-cli、boto3 和任何 SDK） /// - PUT, GET, DELETE, HEAD, LIST (v2) // PUT、GET、DELETE、HEAD、LIST（v2） /// - HeadBucket for bucket existence checks // 用于桶存在性检查的 HeadBucket /// - DeleteObjects batch operation // 删除对象批量操作 /// - Multipart uploads for large files // 大文件的分段上传 /// - Range requests for streaming/seeking // 流媒体/搜索范围请求 /// - HTTP 100-continue support (boto3 compatible) // HTTP 100 继续支持（兼容 boto3） /// - ~360KB static binary // 约 360KB 静态二进制文件 /// ### Distributed Mode (IPFS-like): // 分布式模式（类似 IPFS）： /// - Content-addressed storage with BLAKE3 hashing // 基于 BLAKE3 哈希的内容寻址存储 /// - Automatic deduplication across the network // 网络自动去重 /// - Full Kademlia DHT for peer/content discovery // Kademlia DHT 完全用于对等/内容发现 /// - Peer-to-peer content transfer with quorum reads // 基于法定人数读取的点对点内容传输 /// - Inline storage for small objects (<4KB) // 用于小对象（<4KB）的内联存储 /// - Tombstone-based deletes (prevents resurrection) // 基于墓碑的删除（防止复活） /// - Block garbage collector with grace period // 阻塞垃圾回收器并设置宽限期 /// - Zero-config LAN discovery ready // 零配置 LAN 发现功能已准备就绪 /// - Same S3 API - works with existing tools // 相同的 S3 API - 可与现有工具配合使用 /// ## What it doesn't do // 它不做什么 /// - Versioning, lifecycle policies, bucket ACLs // 版本控制、生命周期策略、存储桶访问控制列表 /// - Pre-signed URLs, object tagging, encryption // 预签名 URL、对象标记、加密 /// - Anything you'd actually need a cloud provider for // 任何你真正需要云服务提供商的服务 /// If you need these, use MinIO or AWS. // 如果需要这些功能，请使用 MinIO 或 AWS。"
[lib.zig/zigistry]: https://zigistry.dev/packages/github/Lulzx/zs3/ "(: zig fetch --save https://github.com/Lulzx/zs3/archive/refs/heads/main.tar.gz)"

