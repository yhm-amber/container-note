[src/gh]: https://github.com/HeyPuter/firefox-wasm.git "(MPL-2.0) (Languages: JavaScript 97.6%, C++ 1.6%, TypeScript 0.3%, Shell 0.3%, Makefile 0.1%, C 0.1%) 🦊 Firefox in WebAssembly // 🦊 WebAssembly 中的 Firefox /// Firefox in WebAssembly // Firefox 中的 WebAssembly /// This project compiles the Gecko engine to WebAssembly so that Firefox can be loaded inside another browser. This port is made possible by emscripten and the WISP protocol for networking support. // 本项目将 Gecko 引擎编译为 WebAssembly，以便 Firefox 可以加载到其他浏览器中。此移植工作得益于 emscripten 和用于网络连接的 WISP 协议 。 /// This project contains an attempt at a JS->WASM JIT. It is currently not usable on many websites. It can be toggled with the environment variable GECKO_NOWASMJIT=1 // 本项目包含一个 JS-EWASM JIT 的尝试。目前它在许多网站上无法使用。可以通过环境变量 GECKO_NOWASMJIT=1 来切换其启用/禁用状态。"
[play.wui/puterdev-labs]: https://developer.puter.com/labs/firefox-wasm/ "Firefox in WebAssembly // Firefox 中的 WebAssembly /// The Gecko engine compiled to WebAssembly. // Gecko 引擎编译为 WebAssembly。 /// The real Firefox interface, rendering entirely inside this browser tab. // 真正的 Firefox 界面，完全在此浏览器标签页内呈现。 /// Advanced options // 高级选项 /// - GPU acceleration // GPU 加速 ///: WebGL-based rendering for web content. // 基于 WebGL 的网页内容渲染。 /// - Enable JIT // 启用即时编译 ///: Experimental JS → WASM JIT. Expect bugs. // 实验性 JS → WASM JIT。可能会有 bug。 /// Web content is proxied through a Puter-hosted Wisp server. // Web 内容通过托管在电脑上的 Wisp 服务器进行代理。 /// Networking in the browser powered by **Puter.js** // 浏览器联网功能由 **计算机.js** 提供支持"

