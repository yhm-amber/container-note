[src/gl]: https://gitlab.com/MultiOS-USB/MultiOS-USB.git "(GPL-3.0) (Languages: Shell 85.1%, Nix 14.9%) Boot operating systems directly from ISO/WIM images // 直接从 ISO/WIM 映像启动操作系统 /// MultiOS-USB // 多系统 USB /// One device with multiple ISO/WIM files. Easy to use: install once, add ISO/WIM files and boot computers from it // 一台设备，支持多个 ISO/WIM 文件。使用简便：只需安装一次，添加 ISO/WIM 文件，即可从该设备启动计算机。 /// Features: // 特征： /// - BIOS and UEFI support // BIOS 和 UEFI 支持 /// - Secure Boot support (boot, manage uefi keys) // 安全启动支持（启动、管理 UEFI 密钥） /// - Load UEFI drivers // 加载 UEFI 驱动程序 /// - Launch .efi executables and other boot loaders // 启动 .efi 可执行文件和其他引导加载程序 /// - Boot Linux from .iso images // 从 .iso 镜像启动 Linux /// - Boot WinPE from bootable .wim images // 从可启动的 .wim 映像启动 WinPE /// - Boot Windows 10/11 installer from ISO (currently, SB must be disabled during installation) // 从 ISO 文件启动 Windows 10/11 安装程序（目前，安装过程中必须禁用 SB） /// - Boot Linux installer from network (experimental) // 从网络启动 Linux 安装程序（实验性功能） /// - Boot locally installed systems: Linux, Windows // 启动本地安装的系统：Linux、Windows /// - Automatically update configuration files // 自动更新配置文件 /// - Without background services // 没有背景服务 /// - exFAT file system support // exFAT 文件系统支持 /// - Automatic detection of compatible ISO images (GRUB loopback) // 自动检测兼容的 ISO 镜像（GRUB 回环） /// - Support for systems without loopback support // 支持不具备环回功能的系统 /// - Allows customisation of ISO boot menu (for example: custom kernel options) // 允许自定义 ISO 启动菜单（例如：自定义内核选项） /// - Support for USB, SSD, nvme, mmcblk, loop, nbd and virtual disks // 支持 USB、SSD、NVMe、MMCBLK、Loop、NBD 和虚拟磁盘 /// - Support for x86, x86_64 // 支持 x86、x86_64 /// Install to usb device: (: multios-usb -l # Display available USB devices) (: sudo multios-usb /dev/sdX # Install to the selected device (replace sdX with your target device)) /// After installation: // 安装完成后： /// - You can copy your ISO files to the /ISOs directory and boot your computer from USB. // 您即可将 ISO 文件复制到 /ISOs 目录，然后从 USB 启动计算机。 ///: You can also add your own configuration files to the /MultiOS-USB/config_priv directory. They will not be deleted during the automatic MultiOS-USB update. // 您还可以将自己的配置文件添加到 /MultiOS-USB/config_priv 目录。这些文件在 MultiOS-USB 自动更新期间不会被删除。 /// - If you want to change the configuration for a given ISO - copy the one you have chosen from the config folder, paste into config_priv and update it according to your needs. // 如果要更改特定 ISO 的配置，请从配置文件夹中复制您选择的配置，粘贴到 config_priv 中，并根据您的需要进行更新。 ///: The updated configuration will be detected automatically. // 系统将自动检测更新后的配置。 /// - On the first boot with Secure Boot enabled on each new computer, a MultiOS USB certificate must be installed. // 在每台新电脑上首次启用安全启动时，必须安装 MultiOS USB 证书。 ///: Select: Enroll key from disc // 选择：从光盘注册密钥"
[src/gh]: https://github.com/Mexit/MultiOS-USB.git "(GPL-3.0) (Languages: Shell 85.1%, Nix 14.9%) Boot operating systems directly from ISO/WIM images // 直接从 ISO/WIM 映像启动操作系统 /// MultiOS-USB // 多系统 USB /// One device with multiple ISO/WIM files. Easy to use: install once, add ISO/WIM files and boot computers from it // 一台设备，支持多个 ISO/WIM 文件。使用简便：只需安装一次，添加 ISO/WIM 文件，即可从该设备启动计算机。 /// Features: // 特征： /// - BIOS and UEFI support // BIOS 和 UEFI 支持 /// - Secure Boot support (boot, manage uefi keys) // 安全启动支持（启动、管理 UEFI 密钥） /// - Load UEFI drivers // 加载 UEFI 驱动程序 /// - Launch .efi executables and other boot loaders // 启动 .efi 可执行文件和其他引导加载程序 /// - Boot Linux from .iso images // 从 .iso 镜像启动 Linux /// - Boot WinPE from bootable .wim images // 从可启动的 .wim 映像启动 WinPE /// - Boot Windows 10/11 installer from ISO (currently, SB must be disabled during installation) // 从 ISO 文件启动 Windows 10/11 安装程序（目前，安装过程中必须禁用 SB） /// - Boot Linux installer from network (experimental) // 从网络启动 Linux 安装程序（实验性功能） /// - Boot locally installed systems: Linux, Windows // 启动本地安装的系统：Linux、Windows /// - Automatically update configuration files // 自动更新配置文件 /// - Without background services // 没有背景服务 /// - exFAT file system support // exFAT 文件系统支持 /// - Automatic detection of compatible ISO images (GRUB loopback) // 自动检测兼容的 ISO 镜像（GRUB 回环） /// - Support for systems without loopback support // 支持不具备环回功能的系统 /// - Allows customisation of ISO boot menu (for example: custom kernel options) // 允许自定义 ISO 启动菜单（例如：自定义内核选项） /// - Support for USB, SSD, nvme, mmcblk, loop, nbd and virtual disks // 支持 USB、SSD、NVMe、MMCBLK、Loop、NBD 和虚拟磁盘 /// - Support for x86, x86_64 // 支持 x86、x86_64 /// Install to usb device: (: multios-usb -l # Display available USB devices) (: sudo multios-usb /dev/sdX # Install to the selected device (replace sdX with your target device)) /// After installation: // 安装完成后： /// - You can copy your ISO files to the /ISOs directory and boot your computer from USB. // 您即可将 ISO 文件复制到 /ISOs 目录，然后从 USB 启动计算机。 ///: You can also add your own configuration files to the /MultiOS-USB/config_priv directory. They will not be deleted during the automatic MultiOS-USB update. // 您还可以将自己的配置文件添加到 /MultiOS-USB/config_priv 目录。这些文件在 MultiOS-USB 自动更新期间不会被删除。 /// - If you want to change the configuration for a given ISO - copy the one you have chosen from the config folder, paste into config_priv and update it according to your needs. // 如果要更改特定 ISO 的配置，请从配置文件夹中复制您选择的配置，粘贴到 config_priv 中，并根据您的需要进行更新。 ///: The updated configuration will be detected automatically. // 系统将自动检测更新后的配置。 /// - On the first boot with Secure Boot enabled on each new computer, a MultiOS USB certificate must be installed. // 在每台新电脑上首次启用安全启动时，必须安装 MultiOS USB 证书。 ///: Select: Enroll key from disc // 选择：从光盘注册密钥"
