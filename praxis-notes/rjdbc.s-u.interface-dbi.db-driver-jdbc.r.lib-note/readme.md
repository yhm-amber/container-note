[src/gh]: https://github.com/s-u/RJDBC.git "(MIT) (Languages: R 67.6%, Java 16.8%, Shell 15.6%) R interface to databases using JDBC // 使用 JDBC 的数据库 R 接口 /// It is a back-end for the DBI interface in R and thus consult the DBI documentation for the basics. Modifications specific to RJDBC are mentioned in its R documentation. // 它是 R 中 DBI 接口的后端，因此请查阅 DBI 文档了解基础知识。RJDBC 特有的修改在它的 R 文档中有所提及。 /// Please visit the main RJDBC project page on RForge.net for details. // 请访问 RForge.net 上的 RJDBC 主项目页面以获取详细信息。 /// (: ... ;: props$drv = '<DriverClass>' |> RJDBC::JDBC('<JarPath>') ;: conn <- DBI::dbConnect |> base::do.call(props))"
[site/rforge.net]: https://rforge.net/RJDBC/ "RJDBC is a package implementing DBI in R on the basis of JDBC. This allows the use of any DBMS in R through the JDBC interface. The only requirement is working Java and a JDBC driver for the database engine to be accessed. // RJDBC 是一个基于 JDBC 在 R 中实现 DBI 的包。这允许通过 JDBC 接口在 R 中使用任何 DBMS。唯一的要求是可用的 Java 和要访问的数据库引擎的 JDBC 驱动程序。"
[package/cran]: https://cran.r-project.org/web/packages/RJDBC/ "(Version: 	0.2-10) (Depends: 	methods, DBI, rJava (≥ 0.4-15), R (≥ 2.4.0)) (Published: 	2022-03-24) (DOI: 	10.32614/CRAN.package.RJDBC) (Author: 	Simon Urbanek) (Maintainer: 	Simon Urbanek <Simon.Urbanek at r-project.org>) (License: 	MIT + file LICENSE) (NeedsCompilation: 	no) RJDBC: Provides Access to Databases Through the JDBC Interface // RJDBC: 通过 JDBC 接口提供数据库访问 /// The RJDBC package is an implementation of R's DBI interface using JDBC as a back-end. This allows R to connect to any DBMS that has a JDBC driver. // RJDBC 包是使用 JDBC 作为后端的 R 的 DBI 接口实现。这允许 R 连接到任何具有 JDBC 驱动的 DBMS。 (src: gh:s-u/RJDBC.git)"
