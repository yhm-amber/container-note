[src/gh]: https://github.com/trunk-rs/trunk.git "(Apache-2.0, MIT) (Languages: Rust 95.7%, JavaScript 2.2%, HTML 1.2%, Other 0.9%) Build, bundle & ship your Rust WASM application to the web. // 构建、打包并将您的 Rust WASM 应用程序发布到 Web。 /// Trunk // 树干 /// Build, bundle & ship your Rust WASM application to the web. // 构建、打包并将您的 Rust WASM 应用程序发布到 Web。 /// ”Pack your things, we’re going on an adventure!” ~ Ferris // “收拾好东西，我们要去冒险啦！”~ 费里斯 /// Trunk is a WASM web application bundler for Rust. Trunk uses a simple, optional-config pattern for building & bundling WASM, JS snippets & other assets (images, css, scss) via a source HTML file. // Trunk 是一个用于 Rust 的 WASM Web 应用程序打包器。Trunk 使用简单的可选配置模式，通过源 HTML 文件构建和打包 WASM、JS 代码片段和其他资源（图像、css、scss）。 /// 📦 Dev server - Trunk ships with a built-in server for rapid development workflows, as well as support for HTTP & WebSocket proxies. // 📦 开发服务器 - Trunk 版本内置服务器，可实现快速开发工作流程，并支持 HTTP 和 WebSocket 代理。 /// 🏗 Change detection - Trunk watches your application for changes and triggers builds for you, including automatic browser reloading. // 🏗 变更检测 - Trunk 会监视您的应用程序的变更，并为您触发构建，包括自动浏览器重新加载。 /// License // 执照 /// trunk is licensed under the terms of the MIT License or the Apache License 2.0, at your choosing. // trunk 版本采用 MIT 许可证或 Apache 许可证 2.0 授权，您可以自行选择。"
[cli.cargo/crates]: https://crates.io/crates/trunk "(: cargo install --locked -- trunk) (MIT OR Apache-2.0) (9.6K SLoC) (209 KiB) Build, bundle & ship your Rust WASM application to the web. // 构建、打包并将您的 Rust WASM 应用程序发布到 Web。 (src: gh:trunk-rs/trunk.git)"
[site/ghio]: https://trunk-rs.github.io/trunk/ "Overview // 概述 /// Trunk is a WASM web application bundler for Rust. Trunk uses a simple, optional-config pattern for building & bundling WASM, JS snippets & other assets (images, css, scss) via a source HTML file. /// Trunk 是一个用于 Rust 的 WASM Web 应用程序打包器。Trunk 使用简单的可选配置模式，通过源 HTML 文件构建和打包 WASM、JS 代码片段和其他资源（图像、css、scss）。 /// Motivation // 动机 /// Any wasm-bindgen-based framework will work with Trunk. If you're new to frontend development in Rust, Yew and Leptos are two popular options. // 任何基于 wasm-bindgen 框架都适用于 Trunk。如果您是 Rust 前端开发新手， Yew 和 Leptos 是两个不错的选择。 /// The easiest way to ensure that your application launches properly is to setup your app as an executable with a standard main function: // 确保应用程序正常启动的最简单方法是将应用程序设置为具有标准 main 函数的可执行文件： /// (: fn main() { /* ... your app setup code here ... */})"
