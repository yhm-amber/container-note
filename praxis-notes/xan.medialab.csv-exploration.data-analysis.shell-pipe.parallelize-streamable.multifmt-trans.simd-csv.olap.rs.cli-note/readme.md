[src.git/gh]: https://github.com/medialab/xan.git "(UNLICENSE, MIT) (Languages: Rust 99.8%, Other 0.2%) The CSV magician // CSV 魔术师 /// xan, the CSV magician // xan ，CSV 魔术师 /// xan is a command line tool that can be used to process CSV files directly from the shell. // xan 是一个命令行工具，可以直接从 shell 处理 CSV 文件。 /// It has been written in Rust to be as fast as possible, use as little memory as possible, and can very easily handle large CSV files (Gigabytes). It leverages a novel SIMD (web: en.wikipedia.org/wiki/Single_instruction,_multiple_data) CSV parser (web: docs.rs/simd-csv) and is also able to parallelize some computations (through multithreading) to make some tasks complete as fast as your computer can allow. // 它使用 Rust 编写，以尽可能快地运行，尽可能少地使用内存，并且可以非常轻松地处理大型 CSV 文件（GB 级别）。它利用了一种新颖的 SIMD CSV 解析器，并且还能够通过多线程并行化某些计算，以使某些任务尽可能快地完成，快到你的计算机允许的速度。 /// It can easily preview, filter, slice, aggregate, sort, join CSV files, and exposes a large collection of composable commands that can be chained together to perform a wide variety of typical tasks. // 它可以轻松预览、过滤、切片、聚合、排序、连接 CSV 文件，并暴露了大量可组合的命令，可以串联在一起执行各种典型的任务。 /// xan also offers its own expression language so you can perform complex tasks that cannot be done by relying on the simplest commands. This minimalistic language has been tailored for CSV data and is way faster than evaluating typical dynamically-typed languages such as Python, Lua, JavaScript etc. // xan 还提供了自己的表达式语言，因此你可以执行无法通过依赖最简单命令完成的复杂任务。这种极简主义语言是为 CSV 数据量身定制的，比评估典型的动态类型语言（如 Python、Lua、JavaScript 等）要快得多。 /// Note that this tool is originally a fork of BurntSushi's (. gh: @BurntSushi) xsv (. gh: BurntSushi/xsv.git), but has been nearly entirely rewritten at that point, to fit SciencesPo's médialab (. gh: @medialab) use-cases, rooted in web data collection and analysis geared towards social sciences (you might think CSV is outdated by now, but read our love letter (. gh: medialab/xan.git @master/docs/LOVE_LETTER.md) to the format before judging too quickly). // 请注意，这个工具最初是 BurntSushi 的 xsv 的分支，但在那时几乎完全重写了，以适应 SciencesPo 的 médialab 使用场景，根植于面向社会科学的网页数据收集和分析（你可能认为 CSV 已经过时了，但在草率判断之前，请先阅读我们写给这种格式的情书）。 /// xan therefore goes beyond typical data manipulation and expose utilities related to lexicometry, graph theory and even scraping. // 因此， xan 超越了典型的数据处理，并提供了与词频分析、图论甚至网络爬虫相关的工具。 /// Beyond CSV data, xan is able to process a large variety of CSV-adjacent data formats from many different disciplines such as web archival (.cdx) or bioinformatics (.vcf, .gtf, .sam, .bed etc.). xan is also able to convert to & from many data formats such as json, excel files, numpy arrays etc. using `xan to` and `xan from`. See this (. gh: medialab/xan.git#supported-file-formats) section for more detail. // 除了 CSV 数据， xan 能够处理来自不同学科的大量 CSV 相关数据格式，例如网络存档（ .cdx ）或生物信息学（ .vcf 、 .gtf 、 .sam 、 .bed 等）。 xan 还能使用 `xan to` (gh: medialab/xan.git @master/docs/cmd/to.md) 和 `xan from` (gh: medialab/xan.git @master/docs/cmd/from.md) 在多种数据格式之间进行转换，如 json、Excel 文件、NumPy 数组等。详情请见本节。 /// Finally, xan can be used to display CSV files in the terminal, for easy exploration, and can even be used to draw basic data visualisations: // 最后， xan 可以用于在终端中显示 CSV 文件，以便轻松探索，甚至可以用于绘制基本的数据可视化： /// - view command // 查看命令 /// - flatten command // flatten 命令 /// - categorical histogram // 分类直方图 /// - scatterplot // 散点图 /// - categorical scatterplot // 分类散点图 /// - histograms // 直方图 /// - parallel processing // 并行处理 /// - time series // 时间序列 /// - small multiples (facet grid) // 小 multiples (分面网格) /// - grouped view // 分组视图 /// - correlation matrix heatmap // 相关矩阵热图 /// - heatmap // 热图 /// ## Available commands // 可用命令 /// - help: Get help regarding the expression language // help：获取有关表达式语言的帮助 /// *Explore & visualize* // *探索和可视化* /// - count (c): Count rows in file // 计数 (c)：统计文件中的行数 /// - headers (h): Show header names // 标题 (h)：显示标题名称 /// - view (v): Preview a CSV file in a human-friendly way // 查看 (v)：以人类友好的方式预览 CSV 文件 /// - flatten: Display a flattened version of each row of a file // flatten：显示文件每行的一个扁平化版本 /// - hist: Print a histogram with rows of CSV file as bars // hist：打印 CSV 文件行作为条形的直方图 /// - plot: Draw a scatter plot or line chart // 绘图：绘制散点图或折线图 /// - heatmap: Draw a heatmap of a CSV matrix // 热力图：绘制 CSV 矩阵的热力图 /// - progress: Display a progress bar while reading CSV data // 进度条：读取 CSV 数据时显示进度条 /// *Search & filter* // *搜索与筛选* /// - search: Search for (or replace) patterns in CSV data // search：在 CSV 数据中搜索（或替换）模式 /// - grep: Coarse but fast filtering of CSV data // grep：对 CSV 数据进行粗略但快速的过滤 /// - filter: Only keep some CSV rows based on an evaluated expression // filter：根据一个评估的表达式仅保留某些 CSV 行 /// - head: First rows of CSV file // head：CSV 文件的首行 /// - tail: Last rows of CSV file // tail：CSV 文件的末行 /// - slice: Slice rows of CSV file // slice: 切割 CSV 文件的行 /// - top: Find top rows of a CSV file according to some column // top: 根据某些列查找 CSV 文件的前几行 /// - sample: Randomly sample CSV data // 样本：随机采样 CSV 数据 /// - bisect: Binary search on sorted CSV data // 二分：对排序后的 CSV 数据进行二分搜索 /// *Sort & deduplicate* // *排序和去重* /// - sort: Sort CSV data // 排序：排序 CSV 数据 /// - dedup: Deduplicate a CSV file // 去重：去重 CSV 文件 /// - shuffle: Shuffle CSV data // 洗牌：洗牌 CSV 数据 /// *Aggregate* // *聚合* /// - frequency (freq): Show frequency tables // 频率（freq）：显示频率表 /// - groupby: Aggregate data by groups of a CSV file // groupby：按 CSV 文件中的分组聚合数据 /// - stats: Compute basic statistics // stats：计算基本统计数据 /// - agg: Aggregate data from CSV file // agg：从 CSV 文件中聚合数据 /// - bins: Dispatch numeric columns into bins // bins：将数值列分配到不同的区间 /// - window: Compute window aggregations (cumsum, rolling mean, lag etc.) // 窗口：计算窗口聚合（累积和、滚动均值、滞后等） /// *Combine multiple CSV files* // *合并多个 CSV 文件* /// - cat: Concatenate by row or column // cat：按行或按列连接 /// - join: Join CSV files // join：连接 CSV 文件 /// - fuzzy-join: Join a CSV file with another containing patterns (e.g. regexes) // fuzzy-join：将一个 CSV 文件与包含模式（例如正则表达式）的另一个文件进行连接 /// - merge: Merge multiple similar already sorted CSV files // 合并：合并多个已排序的相似 CSV 文件 /// *Add, transform, drop and move columns* // *添加、转换、删除和移动列* /// - select: Select columns from a CSV file // select：从 CSV 文件中选择列 /// - drop: Drop columns from a CSV file // drop：从 CSV 文件中删除列 /// - map: Create a new column by evaluating an expression on each CSV row // map：通过在每个 CSV 行上评估表达式来创建新列 /// - transform: Transform a column by evaluating an expression on each CSV row // transform：通过在每个 CSV 行上评估表达式来转换列 /// - enum: Enumerate CSV file by preprending an index column // enum：通过在每个 CSV 文件前添加索引列来枚举 CSV 文件 /// - flatmap: Emit one row per value yielded by an expression evaluated for each CSV row // flatmap：为每个 CSV 行上评估表达式产生的每个值发射一行 /// - fill: Fill empty cells // 填充：填充空白单元格 /// - complete: Add missing rows in a column of contiguous values // 补全：在连续值的列中添加缺失的行 /// - blank: Blank down contiguous identical cell values // 空白：空白连续相同的单元格值 /// - separate: Split a single column into multiple ones // 分离：将单列拆分为多个列 /// *Format, convert & recombobulate* // *格式化、转换和重组* /// - behead: Drop header from CSV file // behead：从 CSV 文件中删除标题 /// - rename: Rename columns of a CSV file // 重命名：重命名 CSV 文件的列 /// - input: Read unusually formatted CSV data // 读取格式异常的 CSV 数据 /// - fixlengths: Makes all rows have same length // fixlengths: 使所有行具有相同的长度 /// - fmt: Format CSV output (change field delimiter) // fmt: 格式化 CSV 输出（更改字段分隔符） /// - explode: Explode rows based on some column separator // explode: 根据某些列分隔符拆分行 /// - implode: Collapse consecutive identical rows based on a diverging column // implode：根据不同的列合并连续的相同行 /// - from: Convert a variety of formats to CSV // from：将多种格式转换为 CSV /// - to: Convert a CSV file to a variety of data formats // 将 CSV 文件转换为多种数据格式 /// - scrape: Scrape HTML into CSV data // scrape: 从 HTML 中抓取数据到 CSV 格式 /// - reverse: Reverse rows of CSV data // reverse: 反转 CSV 数据的行 /// *Transpose & pivot* // *转置 & 聚合* /// - transpose (t): Transpose CSV file // transpose (t): 转置 CSV 文件 /// - pivot: Split distinct values of a column into their own columns columns // pivot：将某一列的 distinct 值拆分到各自的列中 columns /// - unpivot: Stack multiple columns into fewer // 转置：将多个列堆叠为更少的列 /// *Split a CSV file into multiple* // *将 CSV 文件拆分为多个* /// - split: Split CSV data into chunks // split：将 CSV 数据拆分为多个块 /// - partition: Partition CSV data based on a column value // partition：根据列值对 CSV 数据进行分区 /// *Parallelization* // *并行化* /// - parallel (p): Map-reduce-like parallel computation // 并行 (p): 类 map-reduce 的并行计算 /// *Generate CSV files* // *生成 CSV 文件* /// - range: Create a CSV file from a numerical range // 范围：从数值范围创建 CSV 文件 /// *Lexicometry & fuzzy matching* // *词频统计与模糊匹配* /// - tokenize: Tokenize a text column // tokenize：对文本列进行分词 /// - vocab: Build a vocabulary over tokenized documents // vocab：对分词后的文档构建词汇表 /// *Matrix & network-related commands* // *矩阵与网络相关的命令* /// - matrix: Convert CSV data to matrix data // matrix: 将 CSV 数据转换为矩阵数据 /// - network: Convert CSV data to network data // 网络：将 CSV 数据转换为网络数据 /// *Debug* // *调试* /// - eval: Evaluate/debug a single expression // 评估：评估/调试单个表达式"
[cli.cargo/crates]: https://crates.io/crates/xan "(: cargo install --locked -- xan) (: cargo install --locked --all-features -- xan # To enable all optional features) (: cargo install --locked --features parquet -- xan # To enable only specific features) (: xan completions -h # 安装补全功能的帮助手册) (: xan select foundation_year,name medias.csv | xan view # Selecting some columns ;: xan sort -s foundation_year medias.csv | xan view -s name,foundation_year # Sorting the file ;: xan frequency -s edito medias.csv | xan view # Computing frequency tables // 计算频率表 /// Using `... | xan hist` for Printing a histogram 用 `... | xan hist` 打印直方图 ;: xan stats -s indegree,edito medias.csv | xan transpose | xan view -I # Computing descriptive statistics // 计算描述性统计量 ;: xan filter 'batch > 1' medias.csv | xan count # Evaluating an expression to filter a file // 评估表达式以过滤文件) (: xan map 'fmt(\"{} ({})\", name, foundation_year) as key' medias.csv | xan select key | xan slice -l 10 # Evaluating an expression to create a new column based on other ones // 评估表达式以基于其他列创建新列 ;: xan transform name 'split(name, \".\") | first | upper' medias.csv | xan select name | xan slice -l 10 # Transform a column by evaluating an expression // 通过评估表达式转换列 ;: xan agg 'sum(indegree) as total_indegree, mean(indegree) as mean_indegree' medias.csv | xan view -I # Performing custom aggregation // 执行自定义聚合 ;: xan groupby edito 'sum(indegree) as indegree' medias.csv | xan view -I # Grouping rows and performing per-group aggregation // 分组行并执行按组聚合) (Unlicense OR MIT) (42K SLoC) (401 KiB) The CSV magician // CSV 魔术师 (src: gh:medialab/xan.git)"
[src.doi/zenodo]: https://zenodo.org/records/19485852 "(~ doi: 10.5281/zenodo.15310200) A CSV-centric data manipulation CLI tool written in Rust. // 一个用 Rust 编写的以 CSV 为中心的数据库操作命令行工具。"
[cli.x/x-cmd]: https://x-cmd.com/install/xan "(: xd alp xan) (: x install xan)"
