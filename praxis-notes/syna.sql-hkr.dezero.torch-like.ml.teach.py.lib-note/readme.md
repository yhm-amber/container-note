[src/gh]: https://github.com/sql-hkr/syna.git "(MIT) (Languages: Python 99.7%, Dockerfile 0.3%) Syna is a lightweight machine learning framework inspired by DeZero. // Syna 是一个受 DeZero 启发的轻量级机器学习框架。 /// Syna is a lightweight machine learning framework inspired by [DeZero] (. gh: oreilly-japan/deep-learning-from-scratch-3.git). Built from scratch using only NumPy, it follows a define-by-run (dynamic computation graph) approach and includes a basic reinforcement learning framework. // Syna 是一个受 DeZero 启发的轻量级机器学习框架。它完全使用 NumPy 从头构建，采用定义即运行（动态计算图）的方法，并包含一个基本的强化学习框架。 /// Unlike most frameworks that implement reinforcement learning as a separate library, Syna provides everything in a single library. // 与大多数将强化学习实现为独立库的框架不同，Syna 将所有功能都集成在单个库中。 /// Designed for beginners and researchers, Syna helps you learn the fundamentals of machine learning and the inner workings of frameworks like [PyTorch] (. gh: pytorch/pytorch.git). Performance is not the focus, and GPU support is intentionally omitted to keep the code simple and easy to understand. // Syna 专为初学者和研究人员设计，帮助您学习机器学习的基础知识以及 PyTorch 等框架的内部工作原理。性能不是重点，并且有意省略了 GPU 支持，以保持代码简单易懂。 /// (: import syna ;: import syna.functions as F ;: from syna import utils) (: x = syna.tensor(1.0) ;: y = F.tanh(x) ;: x.name = 'x' ;: y.name = 'y' ;: y.backward(create_graph=True)) (: for i in range(iters): ;:: gx = x.grad ;:: x.cleargrad() ;:: gx.backward(create_graph=True)) (: gx = x.grad ;: gx.name = 'gx' + str(iters + 1) ;: utils.viz.plot_dot_graph(gx, verbose=False, to_file='tanh.svg'))"
[lib.pip/pypi]: https://pypi.org/project/syna/ "(: pip install -- syna) (License: MIT License) (Requires: Python >=3.11) Syna is a lightweight machine learning framework inspired by DeZero. // Syna 是一个受 DeZero 启发的轻量级机器学习框架。 (src: gh:sql-hkr/syna.git)"
