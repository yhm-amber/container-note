[src/gh]: https://github.com/sibyllinesoft/valknut.git "(Languages: JavaScript 44.2%, Rust 38.3%, HTML 7.8%, Python 3.3%, Handlebars 2.6%, CSS 1.4%, Other 2.4%) Valknut – Fast, Multi-Layer Code Intelligence for Real Teams // Valknut – 为真实团队提供快速、多层代码智能 /// Valknut is a Rust-native analysis platform that combines structural heuristics, AST-driven complexity metrics, documentation audits, and optional AI guidance. The CLI ships with CI-friendly output, a documentation linter, MCP endpoints for IDE automation, and optional refactoring oracle. // Valknut 是一个 Rust 原生分析平台，它结合了结构启发式方法、基于抽象语法树 (AST) 的复杂度指标、文档审核以及可选的 AI 指导。其命令行界面 (CLI) 提供对持续集成 (CI) 友好的输出、文档检查器、用于 IDE 自动化的 MCP 端点以及可选的重构预言机。 /// At a Glance // 概览 /// - Comprehensive analysis pipeline – structure, complexity, dependency graph, coverage, LSH clone detection, refactoring scoring, and health metrics driven by AnalysisPipeline. // 综合分析流程 ——结构、复杂性、依赖关系图、覆盖率、LSH 克隆检测、重构评分和健康指标，由 AnalysisPipeline 驱动。 /// - Documentation awareness – the bundled doc-audit command finds missing/dated READMEs, TODO clusters, and style regressions using the crates/doc_audit crate. // 文档意识 ——捆绑的 doc-audit 命令使用 crates/doc_audit crate 查找缺失/过时的 README、TODO 集群和样式回归。 /// - AI & MCP integration – run valknut mcp-stdio to expose a Model Context Protocol server or enable the Gemini-powered refactoring oracle with --oracle. // AI 和 MCP 集成 – 运行 valknut mcp-stdio 以公开模型上下文协议服务器，或使用 --oracle 启用 Gemini 支持的重构预言机。 /// - High-performance internals – arena allocation, shared AST caches, SIMD-accelerated similarity, and git-aware file discovery keep large repos manageable. // 高性能的内部机制 ——竞技场分配、共享 AST 缓存、SIMD 加速相似性以及 git 感知的文件发现，使大型存储库易于管理。 /// - Battle-tested reports – export JSONL/JSON/YAML/CVS/Markdown/HTML/Sonar/CI-summary formats plus colorized console summaries. // 经过实战检验的报告 ——导出 JSONL/JSON/YAML/CVS/Markdown/HTML/Sonar/CI-summary 格式以及彩色控制台摘要。 /// Core Capabilities // 核心能力 /// - Structure Analysis – deterministic directory/file re-organization packs (src/detectors/structure) surface imbalance, whale files, and recommended splits. // 结构分析 – 确定性目录/文件重组包（ src/detectors/structure ）表面不平衡、鲸鱼文件和推荐的拆分。 /// - Complexity Intelligence – AST-backed cyclomatic/cognitive metrics and severity classification per entity (src/detectors/complexity). // 复杂性智能 – 基于 AST 的圈图/认知指标和每个实体的严重性分类（ src/detectors/complexity ）。 /// - Dependency & Impact Analysis – ProjectDependencyAnalysis builds call graphs, detects cycles, and feeds choke-point scoring plus similarity cliques. // 依赖关系和影响分析 – ProjectDependencyAnalysis 构建调用图，检测循环，并提供瓶颈评分和相似性集团。 /// - Clone Detection (opt-in) – locality-sensitive hashing with optional denoising/simd speedups for semantic clone clusters. // 克隆检测（可选） – 局部敏感哈希，可选择去噪/simd 加速语义克隆集群。 /// - Coverage Awareness – auto-discover or pin coverage files, surface gap summaries, and include them in health metrics. // 覆盖范围意识 ——自动发现或固定覆盖范围文件，显示差距摘要，并将其纳入健康指标。 /// - Refactoring Scoring – aggregated feature vectors drive health, maintainability, and technical-debt indices for gating. // 重构评分 ——聚合特征向量驱动门控的健康度、可维护性和技术债务指标。"
[cli.cargo/crates]: https://crates.io/crates/valknut-rs "(: cargo install -- valknut-rs) (MIT) (174K SLoC) (6.98 MiB) High-performance Rust implementation of valknut code analysis algorithms // 高性能 Rust 实现的 Valknut 代码分析算法 (src: gh:sibyllinesoft/valknut.git)"
[site/sibylline.dev]: https://valknut.sibylline.dev/ "Valknut provides a rich CLI interface for comprehensive code analysis. The main entry point is the analyze command, with additional commands for configuration management and integration. // Valknut 提供功能丰富的命令行界面，用于进行全面的代码分析。其主要入口点是 analyze 命令，此外还提供用于配置管理和集成的附加命令。 /// (: valknut --help) /// The primary command for analyzing codebases with comprehensive analysis including structure, complexity, and refactoring opportunities. // 用于分析代码库的主要命令，可进行包括结构、复杂性和重构机会在内的全面分析。 /// (: valknut analyze [OPTIONS] <PATHS>... # <PATHS>... - One or more directories or files to analyze // <PATHS>... - 要分析的一个或多个目录或文件) (: valknut analyze ./src # Basic analysis) (: valknut analyze --format html --out reports/ -- ./src # Generate team HTML report) (: valknut analyze --quality-gate --max-complexity 75 --min-health 60 -- ./src # Quality gate for CI/CD) (: valknut analyze --fail-on-issues -- ./src # Fail on any issues) (: valknut analyze --config custom.yml --format markdown -- ./src # Custom configuration) (: make bench-clone-verification BENCH_CLONE_PATH=../path/to/project # Benchmark clone verification (default path = .)) (: valknut analyze src crates --format html --semantic-clones --denoise --coverage-file coverage/lcov.info # Force dependency graph & docs surfaced in HTML/JSON) /// Integration Commands // 集成命令 /// (: valknut mcp-stdio [OPTIONS] # Run MCP server over stdio for Claude Code integration // 通过 stdio 运行 MCP 服务器以集成 Claude Code) (: valknut mcp-manifest [OPTIONS] # Generate MCP manifest JSON for IDE integration setup // 生成用于 IDE 集成设置的 MCP 清单 JSON)"
[demo-on-self/.site]: https://valknut.sibylline.dev/report-dev.html "Valknut Analysis Report // Valknut 分析报告 /// Comprehensive code quality analysis and refactoring guidance // 全面的代码质量分析和重构指导"

