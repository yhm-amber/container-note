[src/gh]: https://github.com/razvandimescu/numa.git "(MIT) (Languages: Rust 71.8%, HTML 17.8%, Shell 8.8%, Python 1.1%, Makefile 0.2%, CSS 0.2%, Dockerfile 0.1%) Portable DNS resolver in Rust — .numa local domains, ad blocking, developer overrides // Rust 中的便携式 DNS 解析器——.numa 本地域名、广告拦截、开发者覆盖 /// DNS you own. Everywhere you go. — numa.rs // 你自己的 DNS。无论你走到哪里。 — numa.rs /// A portable DNS resolver in a single binary. Block ads on any network, name your local services (frontend.numa), and override any hostname with auto-revert — all from your laptop, no cloud account or Raspberry Pi required. // 一个单二进制文件的便携式 DNS 解析器。在任何网络上屏蔽广告，命名你的本地服务（ frontend.numa ），并覆盖任何主机名（自动恢复）——所有功能都来自你的笔记本电脑，无需云账户或树莓派。 /// Built from scratch in Rust. Zero DNS libraries. RFC 1035 wire protocol parsed by hand. Caching, ad blocking, and local service domains out of the box. Optional recursive resolution from root nameservers with full DNSSEC chain-of-trust validation, plus a DNS-over-TLS listener for encrypted client connections (iOS Private DNS, systemd-resolved, etc.). One ~8MB binary, everything embedded. // 用 Rust 从零开始构建。零 DNS 库。手动解析 RFC 1035 线路协议。内置缓存、广告屏蔽和本地服务域名。可选的从根域名服务器进行递归解析，带有完整的 DNSSEC 信任链验证，以及用于加密客户端连接的 DNS-over-TLS 监听器（iOS 私有 DNS、systemd-resolved 等）。一个 ~8MB 的二进制文件，所有内容都嵌入其中。 /// (: cargo install -- numa) (: sudo numa # Then, open the dashboard: http://numa.numa or http://localhost:5380) (: sudo numa install # Set as system DNS) (: curl -X POST localhost:5380/services -d -d '{\"name\":\"frontend\",\"target_port\":5173}' # Name your dev services instead of remembering port numbers // 为你的开发服务命名，而不是记住端口号 /// Now https://frontend.numa works in your browser — green lock, valid cert, WebSocket passthrough for HMR. No mkcert, no nginx, no /etc/hosts // 现在 https://frontend.numa 在你的浏览器中工作——绿色锁，有效证书，WebSocket 路由热重载。无需 mkcert，无需 nginx，无需 /etc/hosts) /// Add path-based routing (app.numa/api → :5001), share services across machines via LAN discovery, or configure everything in numa.toml. // 添加基于路径的路由（ app.numa/api → :5001 ），通过局域网发现跨机器共享服务，或在 numa.toml 中配置所有内容。 /// LAN Discovery // 局域网发现 /// Run Numa on multiple machines. They find each other automatically via mDNS: // 在多台机器上运行 Numa。它们通过 mDNS 自动发现彼此： ///: From Machine B: curl http://api.numa → proxied to Machine A's port 8000. Enable with numa lan on. // 从机器 B： curl http://api.numa → 被代理到机器 A 的 8000 端口。使用 numa lan on 启用。 /// Hub mode: run one instance with bind_addr = "0.0.0.0:53" and point other devices' DNS to it — they get ad blocking + .numa resolution without installing anything. // Hub 模式：运行一个 bind_addr = \"0.0.0.0:53\" 实例，并将其他设备的 DNS 指向它——它们无需安装任何内容即可获得广告拦截+ .numa 解析。"
[cli.cargo/crates]: https://crates.io/crates/numa "(: cargo install -- numa) (MIT) (13K SLoC) (1.17 MiB) Portable DNS resolver in Rust — .numa local domains, ad blocking, developer overrides, DNS-over-HTTPS // Rust 语言编写的便携式 DNS 解析器——支持.numa 本地域名、广告拦截、开发者覆盖、DNS-over-HTTPS (src: gh:razvandimescu/numa.git)"
[site/rs]: https://numa.rs/ "Numa /// DNS you own. Everywhere you go. // 你自己的 DNS。无论你走到哪里。 /// After Numa Pompilius, who built institutions that outlasted kings. // 在努马·庞皮利乌斯之后，他建立了比国王寿命更长的制度。 /// Block ads and trackers. Override DNS for development. Name your local services with .numa domains. A single portable binary built from scratch in Rust — no Raspberry Pi, no cloud, no account. // 屏蔽广告和追踪器。为开发覆盖 DNS。用 .numa 域名命名本地服务。用 Rust 从零构建的单个便携二进制文件——无需树莓派，无需云，无需账户。 /// ## The Problem // 问题 /// DNS is a single point of control // DNS 是一个单一的控制点 /// Every time you visit a website, you ask a DNS resolver where to go. That resolver sees every domain you visit, when, and how often. Your ISP logs these queries by default. // 每次你访问一个网站，你都会询问一个 DNS 解析器该去哪里。该解析器会看到你访问的每个域名、时间以及频率。你的互联网服务提供商默认会记录这些查询。 /// Ad blockers work in one browser. Pi-hole needs a Raspberry Pi. Your local dev services live at localhost:5173 and you can never remember which port is which. // 广告拦截器只在浏览器中起作用。Pi-hole 需要一个树莓派。你本地的开发服务运行在 localhost:5173 ，而你永远记不住哪个端口对应哪个服务。 /// DNS is the foundation of everything you do on the internet, but the tools for controlling it locally are either too complex (dnsmasq + nginx + mkcert) or too limited (cloud-only, appliance-only). // DNS 是您在互联网上所做一切的基础，但用于本地控制它的工具要么过于复杂（dnsmasq + nginx + mkcert），要么过于有限（仅限云端，仅限设备）。 /// ## How It Works // 它是如何工作的 /// What it does today // 它今天的作用 /// A DNS resolver with caching, ad blocking, local service domains, and a REST API. Optional recursive resolution with DNSSEC. Everything runs in a single binary. // 一个带有缓存、广告拦截、本地服务域名和 REST API 的 DNS 解析器。可选的带有 DNSSEC 的递归解析。所有内容都在一个二进制文件中运行。 /// ### Layer 1 // 第一层 ///: Resolve & Protect // 解决 & 保护 /// - Forward mode by default — transparent proxy to your existing DNS, with caching // 默认转发模式 — 透明代理至现有 DNS，带缓存 /// - Ad & tracker blocking — 385K+ domains, zero config // 广告 & 追踪器拦截 — 385K+ 域名，无需配置 /// - Recursive resolution — opt-in, resolve from root nameservers, no upstream needed // 递归解析 — 可选，从根域名服务器解析，无需上游 /// - DNSSEC validation — chain-of-trust + NSEC/NSEC3 denial proofs (RSA, ECDSA, Ed25519) // DNSSEC 验证 — 信任链 + NSEC/NSEC3 拒绝证明（RSA、ECDSA、Ed25519） /// - TTL-aware caching (sub-ms lookups) // TTL 感知缓存（亚毫秒级查询） /// - Single binary, portable — macOS, Linux, and Windows // 单二进制文件，可移植 — macOS、Linux 和 Windows /// ### Layer 2 // 第 2 层 ///: Developer Tools // 开发者工具 /// - Local service proxy — frontend.numa instead of localhost:5173 // 本地服务代理 — frontend.numa 代替 localhost:5173 /// - Path-based routing — app.numa/api → :5001 // 基于路径的路由 — app.numa/api → :5001 /// - Ephemeral DNS overrides with auto-revert // 临时 DNS 覆盖，自动恢复 /// - LAN service discovery via mDNS // 通过 mDNS 进行局域网服务发现 /// - Conditional forwarding — plays nice with Tailscale/VPN split-DNS // 条件转发——与 Tailscale/VPN 分片 DNS 协同工作 /// - REST API — script everything, automate anything // REST API——脚本一切，自动化任何事 /// - Live dashboard with real-time stats and controls // 实时统计数据和控制的可视化仪表盘 /// ### Coming Next // 即将推出 ///: Self-Sovereign DNS // 自主权 DNS /// - pkarr integration — DNS via Mainline DHT, no registrar needed // pkarr 集成 — 通过主线 DHT 实现 DNS，无需注册商 /// - Global .numa names — self-publish, DHT-backed // 全球 .numa 名称 — 自我发布，DHT 支持 /// - .onion bridge — human-readable names for Tor hidden services // .onion 桥接 — 为 Tor 隐藏服务提供人类可读的名称 /// - Ed25519 same-key binding — zero new trust assumptions // Ed25519 相同密钥绑定 — 无需新的信任假设 /// - No blockchain required for core naming // 核心命名无需区块链"
