[src/gh]: https://github.com/emilk/egui.git "(Apache-2.0, MIT) (Languages: Rust 98.6%, Other 1.4%) egui: an easy-to-use immediate mode GUI in Rust that runs on both web and native // egui：一个易于使用的 Rust 即时模式 GUI，可在 Web 和原生平台上运行。 /// 🖌 egui: an easy-to-use GUI in pure Rust // 🖌 egui：一个易于使用的纯 Rust 图形用户界面。 /// egui development is sponsored by Rerun (. web: rerun.io), a startup building // egui 的开发由初创公司 Rerun 赞助。 /// an SDK for visualizing streams of multimodal data. // 用于可视化多模态数据流的 SDK。 /// egui (pronounced “e-gooey”) is a simple, fast, and highly portable immediate mode GUI library for Rust. egui runs on the web, natively, and in your favorite game engine. // egui（发音为“e-gooey”）是一个简单、快速且高度可移植的 Rust 即时模式 GUI 库。egui 可以在 Web 上运行，也可以在原生环境中运行，还可以在您喜欢的游戏引擎中运行。 /// egui aims to be the easiest-to-use Rust GUI library, and the simplest way to make a web app in Rust. // egui 的目标是成为最易用的 Rust GUI 库，以及用 Rust 制作 Web 应用程序的最简单方法。 /// egui can be used anywhere you can draw textured triangles, which means you can easily integrate it into your game engine of choice. // egui 可以在任何可以绘制纹理三角形的地方使用，这意味着您可以轻松地将其集成到您选择的游戏引擎中。 /// eframe (. gh: emilk/egui.git @main/crates/eframe) is the official egui framework, which supports writing apps for Web, Linux, Mac, Windows, and Android. // eframe 是官方的 egui 框架，支持为 Web、Linux、Mac、Windows 和 Android 编写应用程序。"
[lib.cargo/crates]: https://crates.io/crates/egui "(: cargo add -- egui) (MIT OR Apache-2.0) (32K SLoC) (388 KiB) An easy-to-use immediate mode GUI that runs on both web and native // 一个易于使用的即时模式图形用户界面，可在 Web 和原生平台上运行。 (src: gh:emilk/egui.git)"
[eframe.cargo/crates]: https://crates.io/crates/eframe "(: cargo add -- eframe) (MIT OR Apache-2.0) (7.0K SLoC) (129 KiB) egui framework - write GUI apps that compiles to web and/or natively // egui 框架 - 编写可编译为 Web 和/或原生应用的 GUI 应用程序 (src: gh:emilk/egui.git @main/crates/eframe)"
[site/rs]: https://egui.rs/ "egui /// egui is an immediate mode GUI library written in Rust. egui runs both on the web and natively on 🐧. On the web it is compiled to WebAssembly and rendered with WebGL. Everything you see is rendered as textured triangles. There is no DOM, HTML, JS or CSS. Just Rust. // egui 是一个用 Rust 编写的即时模式 GUI 库。egui 既可在 Web 上运行，也可在 🐧 上原生运行。在 Web 上，它会被编译成 WebAssembly 并使用 WebGL 渲染。您看到的所有内容都以纹理三角形的形式渲染。它不使用 DOM、HTML、JS 或 CSS，完全由 Rust 编写。 /// egui is designed to be easy to use, portable, and fast. // egui 的设计目标是易于使用、可移植且速度快。"
[demo/.site]: https://egui.rs/#demo "👉 Click to run the web demo 👈"
[knowsby]: https://github.com/PistonDevelopers/conrod.git "NOTICE: Original maintainer stepping down // 通知：原维护者即将卸任 /// I'll no longer be maintaining conrod for the reasons discussed [here](https://github.com/PistonDevelopers/conrod/issues/1454 'Stepping away from conrod (alternative GUI recommendations + opening for maintainers within) #1454 // 放弃 conrod（替代 GUI 建议 + 向内部维护者开放） #1454'). If you're looking for a similar yet nicer replacement GUI library, I recommend egui. If you're interested in taking over maintenance, please express your interest here. // 出于上述原因，我将不再维护连杆。如果您正在寻找类似但更优秀的 GUI 库，我推荐这款 egui 。如果您有兴趣接管维护，请在此表达您的兴趣。"

