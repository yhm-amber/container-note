[src/gh]: https://github.com/buhuipao/anyproxy.git "(MIT) (Languages: Go 86.0%, HTML 6.2%, Shell 3.8%, JavaScript 2.9%, Other 1.1%) Expose your local network and services to public safely // 安全地将本地网络和服务暴露给公众 /// AnyProxy is a modern secure tunneling solution that enables you to safely expose local services to the internet through multiple transport protocols. Built with integrated web management interfaces and intelligent monitoring systems. // AnyProxy 是一种现代安全的隧道解决方案，它通过多种传输协议让您能够安全地将本地服务暴露给互联网。集成了网页管理界面和智能监控系统。 /// (: cd ./demo ;: nano configs/client.yaml # Change: group_id ;: ls -al certs ;: docker run -d --name anyproxy-demo-client --network host -v $(pwd)/configs:/app/configs:ro -v $(pwd)/certs:/app/certs:ro -- docker.io/buhuipao/anyproxy:latest ./anyproxy-client --config configs/client.yaml # Start client with certificate mount ;: docker logs -- anyproxy-demo-client # Check if client connected successfully # Access web interface: http://localhost:8091; Login: admin / admin123) 📊 What's Included // 📊 包含内容 /// - Pre-configured client connects to demo gateway 47.107.181.88:8443 // 预配置客户端连接到演示网关 47.107.181.88:8443 /// - Web interface at http://localhost:8091 (admin / admin123) // Web 界面位于 http://localhost:8091 (admin / admin123) /// - Security: ⚠️ Must change group_id - default has security risks! // Security: ⚠️ 必须更改 group_id - 默认存在安全风险！ /// (: curl -x http://user.<your-group_id>:password@47.107.181.88:8080 -- http://httpbin.org/ip # 🧪 Test Connection // 🧪 测试连接) /// Core Concepts // 核心概念 /// AnyProxy operates on a group authentication model: // AnyProxy 采用组认证模型运行： /// - Gateway: Provides proxy services, accepts client connections // 网关：提供代理服务，接受客户端连接 /// - Client: Connects to gateway, provides access to internal services // 客户端：连接到网关，提供对内部服务的访问 /// - Group Authentication: Each client belongs to a group (group_id) and authenticates using group password (group_password) // 组认证：每个客户端属于一个组（ group_id ），并使用组密码（ group_password ）进行认证 /// Requirements // 需求 /// - Docker (recommended) or Go 1.23+ // Docker（推荐）或 Go 1.23+ /// - Public Server for Gateway deployment // 公共网关部署服务器 /// - TLS Certificates (required for transport layer security) // TLS 证书（传输层安全所需） /// ⚠️ Important Notes: // ⚠️ 重要提示： /// - Certificates Required: All Gateway and Client instances need TLS certificates for secure communication // 证书要求：所有网关和客户端实例都需要 TLS 证书进行安全通信 /// - Password Consistency: All clients with the same group_id must use identical group_password // 密码一致性：所有具有相同 group_id 的客户端必须使用相同的 group_password /// - UDP Ports: When using QUIC transport or TUIC proxy, Docker ports must be set as UDP type // UDP 端口：在使用 QUIC 传输或 TUIC 代理时，Docker 端口必须设置为 UDP 类型 /// 🔄 Multiple Transport Protocols // 🔄 多种传输协议 /// - WebSocket: Firewall-friendly, HTTP/HTTPS compatible // WebSocket：防火墙友好，兼容 HTTP/HTTPS /// - gRPC: HTTP/2 multiplexing, efficient binary protocol // gRPC：HTTP/2 多路复用，高效的二进制协议 /// - QUIC: Ultra-low latency, 0-RTT handshake, connection migration // QUIC：超低延迟，0-RTT 握手，连接迁移 /// 🚀 Triple Proxy Support // 🚀 三重代理支持 /// - HTTP Proxy: Standard HTTP CONNECT, full browser compatibility // HTTP 代理：标准 HTTP CONNECT，全浏览器兼容 /// - - Optional HTTPS proxy mode for encrypted client connections // 可选 HTTPS 代理模式，用于加密客户端连接 /// - SOCKS5 Proxy: Universal protocol support, low overhead // SOCKS5 代理：支持通用协议，低开销 /// - TUIC Proxy: UDP-based ultra-low latency proxy, 0-RTT connection // TUIC 代理：基于 UDP 的超低延迟代理，0-RTT 连接 /// 🎯 Intelligent Routing & Security // 🎯 智能路由与安全 /// - Group Routing System: Multi-environment routing based on group_id // 组路由系统：基于 group_id 的多环境路由 /// - Dynamic Authentication: Clients authenticate using group_id and group_password // 动态认证：客户端使用 group_id 和 group_password 进行身份验证 /// - Host Access Control: Precise allow/deny lists // 主机访问控制：精确的允许/拒绝列表 /// - End-to-End TLS: Mandatory encryption for all transport protocols // 端到端 TLS：所有传输协议的强制加密 /// 🖥️ Web Management Interface // 🖥️ 网页管理界面 /// - Gateway Dashboard: Real-time monitoring, client management // 网关仪表盘：实时监控，客户端管理 /// - Client Monitoring: Local connection tracking, performance analytics // 客户端监控：本地连接跟踪，性能分析 /// - Multi-Language Support: Complete English/Chinese bilingual interface // 多语言支持：完整的英/中双语界面 /// 🔐 Group-Based Authentication & Load Balancing // 🔐 基于组的认证与负载均衡 /// - Single Group ID: Use group_id directly as proxy username (e.g., prod-env:password) // 单一组 ID：直接使用 group_id 作为代理用户名（例如， prod-env:password ） /// - Round-Robin: Automatic load distribution across clients in the same group // 轮询：自动在同一个组内的客户端之间分配负载 /// - Zero-Config: No complex username formats, just group_id and password // 零配置：无需复杂的用户名格式，只需 group_id 和密码 /// - High Availability: Seamless failover when clients disconnect // 高可用性：客户端断开连接时无缝切换 /// - Persistent Credentials: Optional file or database-based credential storage for production use // 持久凭证：可选的文件或数据库凭证存储，用于生产环境"
[intro.llm/deepwiki]: https://deepwiki.com/buhuipao/anyproxy "AnyProxy is a modern secure tunneling solution that enables you to safely expose local services to the internet through multiple transport protocols. The system operates as a distributed proxy architecture where a Gateway server on a public network provides proxy services that route traffic through encrypted tunnels to Client instances in private networks. // AnyProxy 是一种现代安全的隧道解决方案，能够通过多种传输协议将本地服务安全地暴露给互联网。该系统采用分布式代理架构，在公共网络上的网关服务器提供代理服务，通过加密隧道将流量路由到私有网络中的客户端实例。 /// Core Architecture: Gateway-Client model with mandatory TLS encryption supporting HTTP CONNECT, SOCKS5, and TUIC proxy protocols over WebSocket, gRPC, or QUIC transport layers. // 核心架构：网关-客户端模型，强制使用 TLS 加密，支持通过 WebSocket、gRPC 或 QUIC 传输层上的 HTTP CONNECT、SOCKS5 和 TUIC 代理协议。"
[cli.oci/dockerhub]: https://hub.docker.com/r/buhuipao/anyproxy "(: docker pull -- docker.io/buhuipao/anyproxy) (Size: 26.9 MB)"
[cli.go/pgd]: https://pkg.go.dev/github.com/buhuipao/anyproxy/cmd/client "(: go get -- github.com/buhuipao/anyproxy/cmd/client) (License: None detected not legal advice) (Imports: 11) (Imported by: 0)  (src: gh:buhuipao/anyproxy.git)"

