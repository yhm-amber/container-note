[src/gh]: https://github.com/xiph/rav1e.git "(BSD-2-Clause) (Languages: Assembly 78.2%, Rust 21.6%, Other 0.2%) The fastest and safest AV1 encoder. // 最快的、最安全的 AV1 编码器。 /// rav1e is an AV1 video encoder. It is designed to eventually cover all use cases, though in its current form it is most suitable for cases where libaom (the reference encoder) is too slow. // rav1e 是一个 AV1 视频编码器。它旨在最终覆盖所有用例，尽管在其当前形式中，它最适用于 libaom（参考编码器）太慢的情况。 /// Features // 特性 /// - Intra, inter, and switch frames // 帧内、帧间和切换帧 /// - 64x64 superblocks // 64x64 超块 /// - 4x4 to 64x64 RDO-selected square and rectangular blocks // 4x4 至 64x64 RDO 选择的正方形和矩形块 /// - DC, H, V, Paeth, smooth, and all directional prediction modes // DC、H、V、Paeth、平滑以及所有方向预测模式 /// - DCT, (FLIP-)ADST and identity transforms (up to 64x64, 16x16 and 32x32 respectively) // DCT、(FLIP-)ADST 和恒等变换（分别高达 64x64、16x16 和 32x32） /// - 8-, 10- and 12-bit depth color // 8 位、10 位和 12 位深度色 /// - 4:2:0, 4:2:2 and 4:4:4 chroma sampling // 4:2:0、4:2:2 和 4:4:4 色度采样 /// - 11 speed settings (0-10, exhaustive to near real-time) // 11 种速度设置（0-10，从完全到接近实时） /// - Constant quantizer and target bitrate (single- and multi-pass) encoding modes // 恒定量化器和目标比特率（单次和多遍）编码模式 /// - Still picture mode // 静态图片模式"
[cli.cargo/crates]: https://crates.io/crates/rav1e "(: cargo install -- rav1e) (BSD-2-Clause) (203K SLoC) (1.4 MiB)"
[dev.oci/ghcr]: https://github.com/xiph/rav1e/pkgs/container/rav1e-devcontainer "(: docker pull -- ghcr.io/xiph/rav1e-devcontainer)"
[avif-wasm.rav1e.src/saschazar21.gh]: https://github.com/saschazar21/wasm-rav1e.git "(MIT) (Languages: Rust 90.3%, Dockerfile 9.7%) 🖼️ A WebAssembly AVIF encoder using the rav1e codec // 🖼️ 使用 rav1e 编码器的 WebAssembly AVIF 编码器 /// {https://netflixtechblog.com/avif-for-next-generation-image-coding-b1d75675fe4 --- AVIF for Next-Generation Image Coding | by Netflix Technology Blog | Netflix TechBlog // AVIF 用于下一代图像编码 | by Netflix Technology Blog | Netflix TechBlog}: We need an alternative to JPEG that a) is widely supported, b) has better compression efficiency and c) has a wider feature set. We believe AV1 Image File Format (AVIF) has the potential. Using the framework we have open sourced, AVIF compression efficiency can be seen at work and compared against a whole range of image codecs that came before it. // 我们需要一种替代 JPEG 的格式，它需要满足 a) 广泛支持，b) 更好的压缩效率，c) 更丰富的功能集。我们认为 AV1 图像文件格式（AVIF）具有这种潜力。通过我们开源的框架，可以展示 AVIF 的压缩效率，并与它之前出现的各种图像编解码器进行对比。"

