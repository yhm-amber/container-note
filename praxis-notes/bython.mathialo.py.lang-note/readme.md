[src/gh]: https://github.com/mathialo/bython.git "(MIT) (Languages: Python 88.1%, Roff 9.3%, Makefile 2.5%, Shell 0.1%) Python with braces. Because python is awesome, but whitespace is awful. // 使用大括号的 Python。因为 Python 很棒，但空白字符很糟糕。 /// Bython /// Python with braces. Because Python is awesome, but whitespace is awful. // 使用大括号。因为 Python 很棒，但空白字符很糟糕。 /// Bython is a Python preprosessor which translates curly brackets into indentation. // Bython 是一个 Python 预处理程序，它将花括号转换为缩进。 /// Key features // 主要特性 /// - “Forget” about indentaition. You should still write beautiful code, but if you mess up with tabs/spaces, or copy one piece of code to another that uses a different indentation style, it won't break. // 忘记缩进吧。你仍然可以写出漂亮的代码，但如果你在使用制表符/空格时犯错误，或者将一段代码复制到使用不同缩进样式的另一段代码中，它不会崩溃。 /// - Uses Python for interpretation, that means that all of your existing modules, like NumPy and Matplotlib still works. // 使用 Python 进行解释，这意味着你所有的现有模块，如 NumPy 和 Matplotlib 仍然可以工作。 /// Bython works by first translating Bython-files (suggested file ending: .by) into Python-files, and then using Python to run them. You therefore need a working installation of Python for Bython to work. // Bython 通过首先将 Bython 文件（建议文件扩展名：.by）翻译成 Python 文件，然后使用 Python 来运行它们来工作。因此，您需要安装 Python 才能让 Bython 正常工作。 /// (: bython -- source.by arg1 arg2 ... # to run source.by with arg1, arg2, ... as command line arguments) (: py2by -- test.py # This will create a Bython file called test.by.)"
[pkg.pip/pypi]: https://pypi.org/project/Bython/ "(: pip install -- Bython) (License: MIT) Python with braces (src: gh:mathialo/bython.git)"
