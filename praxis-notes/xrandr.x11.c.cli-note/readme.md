[src/gl]: https://gitlab.freedesktop.org/xorg/app/xrandr.git "(C 78.6%, Roff 11.9%, Perl 6.7%, M4Sugar 1.4%, Makefile 1.2%) Command line interface to X11 Resize, Rotate, and Reflect (RandR) extension // X11 调整大小、旋转和反射（RandR）扩展的命令行界面"
[list/xorg]: https://lists.x.org/mailman/listinfo/xorg
[intro/command-not-found.com]: https://command-not-found.com/xrandr "Set the size, orientation and/or reflection of the outputs for a screen. More information: <https://www.x.org/releases/current/doc/man/man1/xrandr.1.xhtml>. // 设置屏幕输出的尺寸、方向和/或反射。更多信息：<https://www.x.org/releases/current/doc/man/man1/xrandr.1.xhtml>。 (: xrandr --query # Display the current state of the system (known screens, resolutions, ...) // 显示系统当前状态（已知屏幕、分辨率等）) (: xrandr --auto # Disable disconnected outputs and enable connected ones with default settings // 禁用断开连接的输出并使用默认设置启用已连接的输出) (: xrandr --output DP1 --mode 1920x1080 --rate 60 # Change the resolution and update frequency of DisplayPort 1 to 1920x1080, 60Hz // 将 DisplayPort 1 的分辨率和刷新率更改为 1920x1080，60Hz) (: xrandr --output HDMI2 --mode 1280x1024 --right-of DP1 # Set the resolution of HDMI2 to 1280x1024 and put it on the right of DP1 // 将 HDMI2 的分辨率设置为 1280x1024，并将其放在 DP1 的右侧) (: xrandr --output VGA1 --off # Disable the VGA1 output // 禁用 VGA1 输出) (: xrandr --output LVDS1 --brightness 0.5 # Set the brightness for LVDS1 to 50% // 将 LVDS1 的亮度设置为 50%)"
[knows-by]: https://github.com/pythops/tuix.git "You need to install xrandr"

