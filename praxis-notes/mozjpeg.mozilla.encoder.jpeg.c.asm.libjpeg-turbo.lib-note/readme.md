[src/gh]: https://github.com/mozilla/mozjpeg.git "(libjpeg-turbo Licenses) (Languages: C 54.6%, Assembly 26.6%, HTML 8.9%, Java 4.4%, CMake 2.4%, Shell 0.8%, Other 2.3%) Improved JPEG encoder. // 改进的 JPEG 编码器。 /// MozJPEG improves JPEG compression efficiency achieving higher visual quality and smaller file sizes at the same time. It is compatible with the JPEG standard, and the vast majority of the world's deployed JPEG decoders. // MozJPEG 提高了 JPEG 压缩效率，在实现更高视觉质量的同时，也减小了文件大小。它与 JPEG 标准兼容，并且与世界上绝大多数已部署的 JPEG 解码器兼容。 /// MozJPEG is a patch for libjpeg-turbo (. gh: libjpeg-turbo/libjpeg-turbo.git). Please send pull requests to libjpeg-turbo if the changes aren't specific to newly-added MozJPEG-only compression code. This project aims to keep differences with libjpeg-turbo minimal, so whenever possible, improvements and bug fixes should go there first. // MozJPEG 是 libjpeg-turbo 的一个补丁。如果更改并非特定于新添加的仅 MozJPEG 使用的压缩代码，请将 pull request 发送到 libjpeg-turbo。该项目旨在使与 libjpeg-turbo 的差异最小化，因此，在可能的情况下，改进和错误修复应首先提交到那里。 /// MozJPEG is compatible with the libjpeg API and ABI. It is intended to be a drop-in replacement for libjpeg. MozJPEG is a strict superset of libjpeg-turbo's functionality. All MozJPEG's improvements can be disabled at run time, and in that case it behaves exactly like libjpeg-turbo. // MozJPEG 与 libjpeg API 和 ABI 兼容。它旨在成为 libjpeg 的即插即用替代品。MozJPEG 是 libjpeg-turbo 功能的严格超集。所有 MozJPEG 的改进都可以在运行时禁用，在这种情况下，它表现得与 libjpeg-turbo 完全一样。 /// MozJPEG is meant to be used as a library in graphics programs and image processing tools. We include a demo cjpeg command-line tool, but it's not intended for serious use. We encourage authors of graphics programs to use libjpeg's C API and link with MozJPEG library instead. // MozJPEG 旨在作为图形程序和图像处理工具的库使用。我们包含一个 cjpeg 命令行工具的演示，但它不适用于严肃用途。我们鼓励图形程序的开发者使用 libjpeg 的 C API，并链接 MozJPEG 库。 /// Features // 特性 /// - Progressive encoding with \"jpegrescan\" optimization. It can be applied to any JPEG file (with jpegtran) to losslessly reduce file size. // 使用 \"jpegrescan\" 优化的渐进式编码。它可以应用于任何 JPEG 文件（通过 jpegtran ）无损地减小文件大小。 /// - Trellis quantization. When converting other formats to JPEG it maximizes quality/filesize ratio. // 错级量化。在将其他格式转换为 JPEG 时，它可以最大化质量/文件大小比率。 /// - Comes with new quantization table presets, e.g. tuned for high-resolution displays. // 包含新的量化表预设，例如针对高分辨率显示器进行了调整。 /// - Fully compatible with all web browsers. // 完全兼容所有网络浏览器。 /// - Can be seamlessly integrated into any program that uses the industry-standard libjpeg API. There's no need to write any MozJPEG-specific integration code. // 可以无缝集成到任何使用行业标准 libjpeg API 的程序中。无需编写任何 MozJPEG 特定的集成代码。"
[turbo.site/org]: https://libjpeg-turbo.org/ "libjpeg-turbo is a JPEG image codec that uses SIMD instructions (MMX, SSE2, AVX2, Neon, AltiVec) to accelerate baseline JPEG compression and decompression on x86, x86-64, Arm, and PowerPC systems, as well as progressive JPEG compression on x86, x86-64, and Arm systems. On such systems, libjpeg-turbo is generally 2-6x as fast as libjpeg, all else being equal. On other types of systems, libjpeg-turbo can still outperform libjpeg by a significant amount, by virtue of its highly-optimized Huffman coding routines. In many cases, the performance of libjpeg-turbo rivals that of proprietary high-speed JPEG codecs. // libjpeg-turbo 是一种 JPEG 图像编解码器，它使用 SIMD 指令（MMX、SSE2、AVX2、Neon、AltiVec）来加速 x86、x86-64、Arm 和 PowerPC 系统上的基线 JPEG 压缩和解压缩，以及在 x86、x86-64 和 Arm 系统上执行渐进式 JPEG 压缩。在这些系统上，libjpeg-turbo 通常比 libjpeg 快 2-6 倍，其他条件相同。在其他类型的系统上，libjpeg-turbo 仍然可以通过其高度优化的霍夫曼编码例程显著优于 libjpeg。在许多情况下，libjpeg-turbo 的性能与专有的高速 JPEG 编解码器相当。 /// libjpeg-turbo implements both the traditional libjpeg API as well as the less powerful but more straightforward TurboJPEG API. libjpeg-turbo also features colorspace extensions that allow it to compress from/decompress to 32-bit and big-endian pixel buffers (RGBX, XBGR, etc.), as well as a full-featured Java interface. // libjpeg-turbo 实现了传统的 libjpeg API，以及功能较弱但更直接的 TurboJPEG API。libjpeg-turbo 还具有色彩空间扩展功能，允许它压缩和解压缩来自/到 32 位和大端像素缓冲区（RGBX、XBGR 等），以及一个功能齐全的 Java 接口。 /// libjpeg-turbo was originally based on libjpeg/SIMD, an MMX-accelerated derivative of libjpeg v6b developed by Miyasaka Masaru. The TigerVNC and VirtualGL projects made numerous enhancements to the codec in 2009, and in early 2010, libjpeg-turbo spun off into an independent project, with the goal of making high-speed JPEG compression/decompression technology available to a broader range of users and developers. libjpeg-turbo is an ISO/IEC and ITU-T reference implementation of the JPEG standard. // libjpeg-turbo 最初基于 libjpeg/SIMD，这是由宫崎正由开发的 libjpeg v6b 的一个 MMX 加速衍生版本。2009 年，TigerVNC 和 VirtualGL 项目对编解码器进行了大量改进，并在 2010 年初，libjpeg-turbo 从独立项目分离出来，目标是让高速 JPEG 压缩/解压缩技术能够被更广泛的用户和开发者使用。libjpeg-turbo 是 JPEG 标准的 ISO/IEC 和 ITU-T 参考实现。"
[turbo.src/gh]: https://github.com/libjpeg-turbo/libjpeg-turbo.git "(libjpeg-turbo Licenses) (Languages: C 54.4%, Assembly 27.5%, HTML 8.4%, Java 5.1%, CMake 2.6%, C++ 0.8%, Other 1.2%) Main libjpeg-turbo repository // 主 libjpeg-turbo 仓库 /// libjpeg-turbo is a JPEG image codec that uses SIMD instructions to accelerate baseline JPEG compression and decompression on x86, x86-64, Arm, PowerPC, and MIPS systems, as well as progressive JPEG compression on x86, x86-64, and Arm systems. On such systems, libjpeg-turbo is generally 2-6x as fast as libjpeg, all else being equal. On other types of systems, libjpeg-turbo can still outperform libjpeg by a significant amount, by virtue of its highly-optimized Huffman coding routines. In many cases, the performance of libjpeg-turbo rivals that of proprietary high-speed JPEG codecs. // libjpeg-turbo 是一个 JPEG 图像编解码器，它使用 SIMD 指令来加速 x86、x86-64、Arm、PowerPC 和 MIPS 系统上的基线 JPEG 压缩和解压缩，以及在 x86、x86-64 和 Arm 系统上的渐进式 JPEG 压缩。在这些系统上，libjpeg-turbo 的速度通常比 libjpeg 快 2-6 倍，其他条件相同。在其他类型的系统上，libjpeg-turbo 仍然可以通过其高度优化的霍夫曼编码例程显著优于 libjpeg。在许多情况下，libjpeg-turbo 的性能与专有的高速 JPEG 编解码器相当。 /// libjpeg-turbo implements both the traditional libjpeg API as well as the less powerful but more straightforward TurboJPEG API. libjpeg-turbo also features colorspace extensions that allow it to compress from/decompress to 32-bit and big-endian pixel buffers (RGBX, XBGR, etc.), as well as a full-featured Java interface. // libjpeg-turbo 实现了传统的 libjpeg API 以及功能较弱但更简单的 TurboJPEG API。libjpeg-turbo 还具有色彩空间扩展功能，允许它压缩和解压缩来自/到 32 位和大端像素缓冲区（RGBX、XBGR 等），以及一个功能齐全的 Java 接口。 /// libjpeg-turbo was originally based on libjpeg/SIMD, an MMX-accelerated derivative of libjpeg v6b developed by Miyasaka Masaru. The TigerVNC and VirtualGL projects made numerous enhancements to the codec in 2009, and in early 2010, libjpeg-turbo spun off into an independent project, with the goal of making high-speed JPEG compression/decompression technology available to a broader range of users and developers. libjpeg-turbo is an ISO/IEC and ITU-T reference implementation of the JPEG standard. // libjpeg-turbo 最初基于 libjpeg/SIMD，这是由宫崎正则开发的 libjpeg v6b 的 MMX 加速衍生版本。在 2009 年，TigerVNC 和 VirtualGL 项目对编解码器进行了大量改进，并于 2010 年初，libjpeg-turbo 从独立项目分离出来，目标是让高速 JPEG 压缩/解压缩技术能够被更广泛的用户和开发者使用。libjpeg-turbo 是 ISO/IEC 和 ITU-T 的 JPEG 标准参考实现。"

