[src/gh]: https://github.com/thedotmack/claude-mem.git "(AGPL-3.0) (Languages: TypeScript 89.1%, JavaScript 6.0%, HTML 4.4%, Other 0.5%) A Claude Code plugin that automatically captures everything Claude does during your coding sessions, compresses it with AI (using Claude's agent-sdk), and injects relevant context back into future sessions. // 一个 Claude Code 插件，可以自动捕获 Claude 在您的编码会话期间所做的一切，使用 AI（使用 Claude 的 agent-sdk）对其进行压缩，并将相关的上下文注入到未来的会话中。 /// Persistent memory compression system built for Claude Code. // 为 Claude Code 构建的持久内存压缩系统。 /// Claude-Mem seamlessly preserves context across sessions by automatically capturing tool usage observations, generating semantic summaries, and making them available to future sessions. This enables Claude to maintain continuity of knowledge about projects even after sessions end or reconnect. // Claude-Mem 通过自动捕获工具使用情况观察结果、生成语义摘要并将其提供给后续会话，从而无缝地保留会话之间的上下文信息。这使得 Claude 即使在会话结束或重新连接后也能保持对项目的持续了解。 /// (> /plugin marketplace add thedotmack/claude-mem) (> /plugin install claude-mem) /// Key Features: // 主要特点： /// - 🧠 Persistent Memory - Context survives across sessions // 🧠 持久记忆 - 上下文在会话之间保留 /// - 📊 Progressive Disclosure - Layered memory retrieval with token cost visibility // 📊 渐进式披露 - 分层式内存检索， Token 成本可见 /// - 🔍 Skill-Based Search - Query your project history with mem-search skill // 🔍 基于技能的搜索 - 使用 mem-search 技能查询您的项目历史记录 /// - 🖥️ Web Viewer UI - Real-time memory stream at http://localhost:37777 // 🖥️ Web 查看器 UI - 实时内存流，地址 ：http://localhost:37777 /// - 💻 Claude Desktop Skill - Search memory from Claude Desktop conversations // 💻 Claude Desktop 技能 - 从 Claude Desktop 对话中搜索记忆 /// - 🔒 Privacy Control - Use <private> tags to exclude sensitive content from storage // 🔒 隐私控制 - 使用 <private> 标签将敏感内容排除在存储之外 /// - ⚙️ Context Configuration - Fine-grained control over what context gets injected // ⚙️ 上下文配置 - 对注入的上下文进行细粒度控制 /// - 🤖 Automatic Operation - No manual intervention required // 🤖 自动运行 - 无需人工干预 /// - 🔗 Citations - Reference past observations with IDs (access via http://localhost:37777/api/observation/{id} or view all in the web viewer at http://localhost:37777) // 🔗 引用 - 使用 ID 引用过去的观测结果（可通过 http://localhost:37777/api/observation/{id} 访问，或在 http://localhost:37777 的 Web 查看器中查看所有结果）"
[site/ai]: https://claude-mem.ai/ "AI MEMORY THAT ACTUALLY WORKS // 真正有效的 AI 记忆 /// Stop explaining context. // 无需解释背景。 /// Start building faster. // 加快建设速度。 /// One AI takes notes about what another AI does. // 一个人工智能会记录另一个人工智能的行为。 /// Claude-Mem watches your AI coding assistant work and captures what matters. Every decision, every bug fix, every architectural choice — remembered automatically. // Claude-Mem 会监控您的 AI 代码助手的工作，并记录关键信息。每一个决策、每一次错误修复、每一个架构选择——都会被自动记住。 /// - Before // 先 ///: What happened in previous sessions // 之前的会话发生了什么 /// - Current // 当 ///: Present context and recent work // 当下背景和近期工作 /// - Next // 后 ///: What comes next, pending tasks // 接下来是啥、有啥待办 /// Live Observation // 现场观察 /// Your AI doesn't have to remember anymore // 你的人工智能不再需要记忆了 /// A dedicated observer AI watches every session, generating searchable observations in real-time. Complete logs with before-and-after context, organized by time—just like how humans handle the same problem. // 专用的 AI 观察员会监控每个会话，实时生成可搜索的观察结果。完整的日志包含前后对比信息，并按时间顺序组织——就像人类处理同样问题的方式一样。 /// - 🔍 Searchable by time // 按时间搜索 /// - 📸 Before & after context // 前后对比 /// - ⚡ Generated live // 实时生成 /// ## How you'd remember it — for your AI // 你会如何记住它 —— 为你的人工智能 /// Every observation is auto-categorized. Filter by decisions, bugfixes, features, or discoveries. Combine with file scope for surgical precision. // 所有观察结果都会自动分类。可按决策、错误修复、功能或发现进行筛选。结合文件范围，实现精准定位。 /// (: type:decision file:auth.ts) /// ## File & Concept Scoping // 文件和概念范围界定 /// Query by file path or semantic concept. “What decisions affected index.ts?” or “What do we know about auth?” Both work. // 可以通过文件路径或语义概念进行查询。例如，“哪些决策影响了 index.ts？”或“我们对身份验证了解多少？”两种方法都有效。 /// By file // 按文件: (: decisions for src/auth/index.ts) By concept // 按概念: (: decisions about “token refresh”) /// ## Progressive Disclosure // 渐进式披露 /// Sessions start with a lightweight index—titles, types, timestamps. The LLM fetches full observations only when it needs depth. // 会话开始时会生成一个轻量级索引，包含标题、类型和时间戳。LLM 仅在需要深度数据时才会获取完整的观测数据。 /// Token-efficient by default, never shallow when it matters. // 默认情况下采用高效的 Token 机制，关键时刻绝不采用浅层机制。 /// ## Before/After Context // 前后语境 /// Every observation includes what came before and what followed. The LLM sees causality—not snapshots. // 每一次观察都包含前因后果。LLM 关注的是因果关系，而非瞬间快照。 /// “Why did this decision lead to that bug?” becomes answerable. // “为什么这个决定会导致这个漏洞？”这个问题就能得到解答了。 /// Real-Time Agent Data // 实时智能体数据 /// An open standard for AI agent memory. // 一种用于人工智能智能体内存的开放标准。 /// RAG captures knowledge. RAD captures intelligence. // RAG 记录知识，RAD 记录情报。 /// Just as RAG (Retrieval Augmented Generation) standardized external knowledge retrieval, RAD will standardize how agents capture and retrieve their own working memory. Hook-based architecture, intelligent compression, temporal awareness — all in one open protocol. // 正如 RAG（检索增强生成）规范了外部知识检索一样，RAD 将规范智能体如何捕获和检索自身的工作记忆。基于钩子的架构、智能压缩、时间感知——所有这些都集成在一个开放协议中。"

