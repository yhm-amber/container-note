[src/gh]: https://github.com/CADmium-Co/CADmium.git "(Elastic License 2.0) (Languages: Rust 51.6%, Svelte 27.3%, TypeScript 18.3%, JavaScript 2.5%, HTML 0.2%, CSS 0.1) A CAD program that runs in the browser /// Legacy CAD programs have taken many thousands of years of collective engineering time to get where they are so this program will never be able to compete on breadth of features. But CADmium is intended to capture 80% of the most common CAD use cases while doing less than 10% of the work. For now we are targeting the home hobbyist who just wants to design a widget for their 3D printer, not a company that wants to design a car or airplane, although that will come later. // 传统的 CAD 程序花费了数千年集体工程时间才发展到今天，所以这个程序在功能广度上永远无法与之竞争。但 CADmium 旨在捕捉 80%的最常见 CAD 使用案例，而只需完成不到 10%的工作。目前，我们的目标是那些只想为他们的 3D 打印机设计小工具的家庭爱好者，而不是那些想要设计汽车或飞机的公司，尽管那将稍后到来。 /// If you're looking for: // 如果你在寻找： /// - A simple, modern, parametric CAD UI that runs in a browser // 一个简单、现代、参数化的可在浏览器中运行的 CAD UI /// - That can export solids as .step, .obj, or .cadmium (a json-based CAD format that this project is inventing) // 可以导出实体为 .step、.obj 或 .cadmium（一种基于 JSON 的 CAD 格式，该项目正在发明） /// - That can export sketches as .svg or .dxf // 可以导出草图为 .svg 或 .dxf /// - That works without an internet connection // 无需互联网连接即可工作 /// Then this project may be for you! // 那么这个项目可能适合你！ /// The boundary representation engine under the hood is truck, which is written in rust and is not dependent on any legacy b-rep engine. // 底层的边界表示引擎是 truck，它用 rust 编写，不依赖于任何遗留的 b-rep 引擎。"
[app.web/ghio]: https://cadmium-co.github.io/CADmium/ "CADmium Webapp"

[knowsby]: https://mattferraro.dev/posts/cadmium "(: CADmium export my_part.cadmium --format stl)"
