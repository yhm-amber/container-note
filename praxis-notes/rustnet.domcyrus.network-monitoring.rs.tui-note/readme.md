
[src/gh]: https://github.com/domcyrus/rustnet.git "(Apache-2.0) (Languages: Rust 96.5%, Shell 1.3%, Python 1.2%, Other 1.0%) Per-process network monitoring for your terminal with deep packet inspection. Cross-platform, sandboxed. // 针对终端的进程级网络监控，支持深度包检测。跨平台，沙盒环境。 /// Per-process network monitoring for your terminal: live TCP, UDP, and QUIC connections with deep packet inspection, sandboxed by default. // 为您的终端提供按进程的网络监控：实时 TCP、UDP 和 QUIC 连接，支持深度包检测，默认沙盒化。 /// Real-time visibility into every connection your machine makes, who owns it, and what protocol it's speaking. No tcpdump, X11 forwarding, or root piping. // 实时查看您的机器建立的每个连接、连接所有者以及所使用的协议。无需 tcpdump、X11 转发或 root 管道。 /// Features // 特征 /// - Per-process attribution: Every TCP, UDP, and QUIC connection mapped to its owning process, via eBPF on Linux, PKTAP on macOS, native APIs on Windows and FreeBSD. Wireshark and tcpdump can't do this; netstat / ss can't show live state. // 进程级归属 ：每个 TCP、UDP 和 QUIC 连接都映射到其所属进程，Linux 系统通过 eBPF，macOS 系统通过 PKTAP，Windows 和 FreeBSD 系统则通过原生 API。Wireshark 和 tcpdump 无法做到这一点； netstat / ss 无法显示实时状态。 /// - Deep packet inspection: Identify HTTP, HTTPS/TLS with SNI, DNS, SSH, FTP, QUIC, MQTT, BitTorrent, STUN, NTP, mDNS, LLMNR, DHCP, SNMP, SSDP, and NetBIOS, without external dissectors. // 深度包检测 ：无需外部解析器即可识别 HTTP、HTTPS/TLS（含 SNI）、DNS、SSH、FTP、QUIC、MQTT、BitTorrent、STUN、NTP、mDNS、LLMNR、DHCP、SNMP、SSDP 和 NetBIOS。 /// - Security sandboxing: Landlock (Linux 5.13+), Seatbelt (macOS), token privilege drop + job-object child-process block (Windows). Drops privileges immediately after libpcap initializes. See SECURITY.md. // 安全沙箱 ：Landlock（Linux 5.13+）、Seatbelt（macOS）、令牌权限丢弃 + 作业对象子进程阻塞（Windows）。在 libpcap 初始化后立即丢弃权限。参见 SECURITY.md 。 /// - TCP network analytics: Real-time retransmissions, out-of-order packets, and fast-retransmit detection, per-connection and aggregate. // TCP 网络分析 ：实时重传、乱序数据包和快速重传检测，按连接和聚合进行检测。 /// - Smart connection lifecycle: Protocol-aware timeouts with white → yellow → red staleness indicators. Toggle t to keep historic (closed) connections visible for forensics. // 智能连接生命周期 ：协议感知超时机制，并以白色→黄色→红色指示器显示连接过期状态。切换 t 开关可使历史（已关闭）连接保持可见，以便进行取证。 /// - Vim/fzf-style filtering: port:, src:, dst:, sni:, process:, state:, proto:, plus regex via /(?i)pattern/. // Vim/fzf 风格的过滤 ： port: ， src: ， dst: ， sni: process: ， state: ， proto: ，以及通过 /(?i)pattern/ 正则表达式。 /// - GeoIP enrichment: Country lookups via local MaxMind GeoLite2. No network calls. // GeoIP 信息增强 ：通过本地 MaxMind GeoLite2 进行国家/地区查找。无需网络调用。 /// - Cross-platform: Linux, macOS, Windows, FreeBSD. // 跨平台 ：Linux、macOS、Windows、FreeBSD。 /// Why RustNet? // 为什么选择 RustNet？ /// RustNet fills the gap between simple connection tools (netstat, ss) and packet analyzers (Wireshark, tcpdump): // RustNet 填补了简单连接工具（ netstat 、 ss ）和数据包分析器（ Wireshark 、 tcpdump ）之间的空白： /// - Process attribution: See which application owns each connection. Wireshark cannot provide this because it only sees packets, not sockets. // 进程归属 ：查看每个连接的所有者。Wireshark 无法提供此信息，因为它只能看到数据包，而看不到套接字。 /// - Connection-centric view: Track states, bandwidth, and protocols per connection in real-time // 以连接为中心的视图 ：实时跟踪每个连接的状态、带宽和协议 /// - SSH-friendly: TUI works over SSH so you can quickly see what's happening on a remote server without forwarding X11 or capturing traffic // 支持 SSH ：TUI 通过 SSH 工作，因此您可以快速查看远程服务器上的运行状况，而无需转发 X11 或捕获流量。 /// RustNet complements packet capture tools. Use RustNet to see what's making connections. For deep forensic analysis, use --pcap-export to capture packets with process attribution, then enrich with scripts/pcap_enrich.py and analyze in Wireshark with full PID/process context. See PCAP Export and Comparison with Similar Tools for details. // RustNet 可作为数据包捕获工具的补充。使用 RustNet 可以查看哪些进程正在建立连接 。要进行深度取证分析，请使用 --pcap-export 捕获带有进程归属信息的数据包，然后使用 scripts/pcap_enrich.py 脚本丰富数据包信息，并在 Wireshark 中使用完整的 PID/进程上下文进行分析。有关详细信息，请参阅 “PCAP 导出及与类似工具的比较” 。 /// Built on ratatui, libpcap, eBPF (libbpf-rs), DashMap, crossbeam, ring, MaxMind GeoLite2, and Landlock. See ARCHITECTURE.md for the full dependency breakdown. // 基于 ratatui、libpcap、eBPF (libbpf-rs)、DashMap、crossbeam、ring、MaxMind GeoLite2 和 Landlock 构建。完整的依赖关系详见 ARCHITECTURE.md 文件 。 /// eBPF Enhanced Process Identification (Linux Default) // eBPF 增强型进程识别（Linux 默认） /// RustNet uses kernel eBPF programs by default on Linux for enhanced performance and lower overhead process identification. However, this comes with important limitations: // RustNet 在 Linux 上默认使用内核 eBPF 程序，以提高性能并降低进程识别的开销。然而，这也带来了一些重要的局限性： /// Process Name Limitations: // 进程名称限制： /// - eBPF uses the kernel's comm field, which is limited to 16 characters // eBPF 使用内核的 comm 字段，该字段长度限制为 16 个字符。 /// - Shows the task/thread command name, not the full executable path // 显示任务/线程命令名称，而不是完整的可执行文件路径。 /// - Multi-threaded applications often show thread names instead of the main process name // 多线程应用程序通常会显示线程名称，而不是主进程名称。 /// Real-world Examples: // 实际案例： /// - Firefox: May appear as \"Socket Thread\", \"Web Content\", \"Isolated Web Co\", or \"MainThread\" // Firefox：可能显示为“Socket Thread”、“Web Content”、“Isolated Web Co”或“MainThread”。 /// - Chrome: May appear as \"ThreadPoolForeg\", \"Chrome_IOThread\", \"BrokerProcess\", or \"SandboxHelper\" // Chrome：可能显示为“ThreadPoolForeg”、“Chrome_IOThread”、“BrokerProcess”或“SandboxHelper” /// - Electron apps: Often show as \"electron\", \"node\", or internal thread names // Electron 应用：通常显示为“electron”、“node”或内部线程名称 /// - System processes: Show truncated names like \"systemd-resolve\" → \"systemd-resolve\" // 系统进程：显示截断名称，例如“systemd-resolve”→“systemd-resolve” /// Fallback Behavior: // 备用方案： /// - When eBPF fails to load or lacks sufficient permissions, RustNet automatically falls back to standard procfs-based process identification // 当 eBPF 加载失败或权限不足时，RustNet 会自动回退到基于标准 procfs 的进程识别方式。 /// - Standard mode provides full process names but with higher CPU overhead // 标准模式提供完整的进程名称，但会增加 CPU 开销。 /// - eBPF is enabled by default; no special build flags needed. To disable eBPF and use procfs-only mode, build with // eBPF 默认启用；无需特殊构建标志。要禁用 eBPF 并使用 procfs-only 模式，请使用以下命令构建 ///: (: cargo build --release --no-default-features)"
[cli.cargo/crates]: https://crates.io/crates/rustnet-monitor "(: cargo install -- rustnet-monitor) (Apache-2.0) (157K SLoC) (8.83 MiB) A cross-platform network monitoring terminal UI tool built with Rust // 一个用 Rust 编写的跨平台网络监控终端 UI 工具。 (src: gh:domcyrus/rustnet.git)"
[cli.oci/ghcr]: https://github.com/domcyrus/rustnet/pkgs/container/rustnet "(: docker pull -- ghcr.io/domcyrus/rustnet:latest)"

