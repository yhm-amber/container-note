[src/gh]: https://github.com/jlongster/tigma.git "(Languages: TypeScript 100.0%) Tigma // 蒂格玛 /// A terminal-based design tool for creating ASCII diagrams and designs. // 一款基于终端的设计工具，用于创建 ASCII 图表和设计。 /// ## Contributing // 贡献 /// I'll be honest: mot of this code was written by AI (specifically Opus 4.5). This project is mostly experimental and I do not intend it to become a real production-ready big project. For now, it's fun. // 说实话，大部分代码都是人工智能（具体来说是 Opus 4.5）编写的。这个项目目前还处于实验阶段，我并不打算把它发展成一个真正可以投入生产的大型项目。现在，它只是个娱乐项目。 /// The best way to work with this code right now is with AI assistance. The has low abstractions which is easy to read and debug, but it's more difficult to write manually because of the lack of abstractions. AI will help keep everything consistent. // 目前处理这段代码的最佳方式是借助人工智能辅助。这段代码抽象程度低，易于阅读和调试，但由于缺乏抽象，手动编写起来比较困难。人工智能将有助于保持代码的一致性。 /// I specifically used Opus 4.5 to work with this. You may find that to be the best model with this structure, but I haven't tried others. When opening PRs, please don't include a bunch of changes unrelated to the intent. // 我特意使用了 Opus 4.5 来处理这个问题。你可能会觉得它是这种结构下最好的版本，但我没有尝试过其他版本。提交 PR 时，请不要包含大量与目标无关的更改。 /// ## File Format // 文件格式 /// Designs are saved as .tigma files in JSON format. The file stores: // 设计文件以 JSON 格式保存为 .tigma 文件。该文件存储以下内容： /// - Text boxes with per-character styling (bold, color) // 具有逐字符样式（粗体、颜色）的文本框 /// - Rectangles with position, stroke color, fill color, and bold state // 矩形的位置、描边颜色、填充颜色和粗体状态 /// - Lines with position, stroke color, and bold state // 线条的位置、笔画颜色和粗体状态 /// - Freehand curves with point arrays and stroke color // 使用点阵列和描边颜色绘制自由曲线 /// - Z-index ordering for proper layering // Z 轴索引排序以实现正确的分层 /// ## Features // 特征 /// - Layered rendering: Objects stack based on z-index; newer objects appear on top by default // 分层渲染 ：对象根据 z-index 堆叠；默认情况下，较新的对象显示在最上面。 /// - Multi-selection: Select multiple objects with Shift+Click or box-select // 多选 ：按住 Shift 键并单击或使用框选框选择多个对象。 /// - Copy/Paste: Copy objects with Ctrl+D, paste at mouse position with Ctrl+V // 复制/粘贴 ：使用 Ctrl+D 复制对象，使用 Ctrl+V 粘贴到鼠标位置。 /// - Undo/Redo: Up to 100 history snapshots // 撤销/重做 ：最多 100 个历史快照 /// - Terminal responsive: Adapts to terminal resize events // 终端响应式 ：适应终端尺寸调整事件 /// - Mouse support: Full mouse interaction including hover highlighting // 鼠标支持 ：完整的鼠标交互功能，包括悬停高亮显示 /// - Live preview: See rectangles and lines as you draw them // 实时预览 ：绘制矩形和线条时即可看到它们。 /// ## Example Workflow // 示例工作流程 /// 1. Press R to select the Rectangle tool // 按 R 选择矩形工具 /// 2. Click and drag to draw a box // 点击并拖动绘制一个方框 /// 3. Press T to select the Text tool // 按 T​​ 选择文本工具 /// 4. Click inside the box and type a label // 点击框内并输入标签 /// 5. Press Escape to finish editing // 按 Escape 结束编辑 /// 6. Press M to switch to Move tool // 按 M 切换到移动工具 /// 7. Select objects and adjust positions // 选择对象并调整位置 /// 8. Use the color picker to change colors // 使用颜色选择器更改颜色 /// 9. Press Ctrl+S to save your work // 按 Ctrl+S 保存您的工作"
[cli.bunx/npmjs]: https://npmjs.com/package/tigma "(: bunx tigma) (License: MIT) (Unpacked Size: 4.02 MB) (Total Files: 14)  (src: gh:jlongster/tigma.git)"

