[src/gh]: https://github.com/paradigmxyz/reth.git "(Apache-2.0, MIT) (Languages: Rust 72.0%, HTML 27.7%, Makefile 0.1%, Python 0.1%, Mermaid 0.1%, Shell 0.0%) Modular, contributor-friendly and blazing-fast implementation of the Ethereum protocol, in Rust // 用 Rust 编写的以太坊协议的模块化、对贡献者友好且速度极快的实现。 /// What is Reth? // Reth 是什么？ /// Reth (short for Rust Ethereum, pronunciation) is a new Ethereum full node implementation that is focused on being user-friendly, highly modular, as well as being fast and efficient. Reth is an Execution Layer (EL) and is compatible with all Ethereum Consensus Layer (CL) implementations that support the Engine API. It is originally built and driven forward by Paradigm (. web: paradigm.xyz), and is licensed under the Apache and MIT licenses. // Reth（Rust Ethereum 的缩写， 发音为 Reth）是一个全新的以太坊全节点实现，它专注于用户友好、高度模块化以及快速高效。Reth 是一个执行层 (EL)，兼容所有支持引擎 API 的以太坊共识层 (CL) 实现。它最初由 Paradigm 开发和维护，并采用 Apache 和 MIT 许可证。"
[site/rs]: https://reth.rs/ "Build a Node: (: // .. snip .. ;: let handle = node_builder.with_types::<EthereumNode>().with_components(EthereumNode::components()).with_add_ons(EthereumAddOns::default()).launch().await?) /// Run a Node: (: brew install -- paradigmxyz/brew/reth # Install the binary ;: reth node --http --http.api eth,trace # Run the node with JSON-RPC enabled)"
[docs/.site]: https://reth.rs/docs/ "List of all crates"

