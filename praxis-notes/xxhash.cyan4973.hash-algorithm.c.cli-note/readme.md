[src/gh]: https://github.com/Cyan4973/xxHash.git "(BSD 2-Clause) (Languages: C 93.4%, C++ 5.3%, Other 1.3%) Extremely fast non-cryptographic hash algorithm // 速度极快的非加密哈希算法 /// xxHash is an Extremely fast Hash algorithm, processing at RAM speed limits. Code is highly portable, and produces hashes identical across all platforms (little / big endian). The library includes the following algorithms : // xxHash 是一种速度极快的哈希算法，其处理速度完全受限于内存使用速度。代码具有高度可移植性，并且在所有平台（小端/大端）上生成的哈希值都相同。该库包含以下算法： /// - XXH32 : generates 32-bit hashes, using 32-bit arithmetic // XXH32：使用 32 位算术生成 32 位哈希值 /// - XXH64 : generates 64-bit hashes, using 64-bit arithmetic // XXH64：使用 64 位算术生成 64 位哈希值 /// - XXH3 (since v0.8.0): generates 64 or 128-bit hashes, using vectorized arithmetic. The 128-bit variant is called XXH128. // XXH3（自 v0.8.0 起）：使用向量化算术生成 64 位或 128 位哈希值。128 位变体称为 XXH128。 /// All variants successfully complete the SMHasher test suite which evaluates the quality of hash functions (collision, dispersion and randomness). Additional tests, which evaluate more thoroughly speed and collision properties of 64-bit hashes, are also provided. // 所有变体均成功通过了 SMHasher 测试套件，该套件用于评估哈希函数的质量（碰撞性、分散性和随机性）。 此外，还提供了其他测试，以更全面地评估 64 位哈希的速度和碰撞特性。"
[site/com]: http://xxhash.com/ "xxHash is an extremely fast non-cryptographic hash algorithm, working at RAM speed limit. It is proposed in four flavors (XXH32, XXH64, XXH3_64bits and XXH3_128bits). The latest variant, XXH3, offers improved performance across the board, especially on small data. // xxHash 是一种速度极快的非加密哈希算法，可在内存速度极限下运行。它有四种版本（XXH32、XXH64、XXH3_64bits 和 XXH3_128bits）。最新版本 XXH3 全面提升了性能，尤其是在处理小数据集时。"
[rsimpl-lib.src/gh:@doumanash]: https://github.com/doumanash/xxhash-rust.git "(BSL-1.0) (Rust 100.0%) Rust implementation of xxhash // Rust 实现的 xxhash /// Implementation of xxHash (. gh: Cyan4973/xxHash.git) in Rust // Rust 中 xxHash (. gh: Cyan4973/xxHash.git) 的实现 /// Each algorithm is implemented via feature, allowing precise control over code size. // 每个算法都是通过特性实现的，从而可以精确控制代码大小。"
[rsimpl-lib.cargo/crates]: https://crates.io/crates/xxhash-rust "(: cargo add -- xxhash-rust) (BSL-1.0) (2.2K SLoC) (21 KiB) Implementation of xxhash (src: gh:doumanash/xxhash-rust.git)"
