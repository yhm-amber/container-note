[server-wui.src/fm]: https://framagit.org/fiat-tux/hat-softwares/lufi.git "(~ framagit: luc/lufi.git) (AGPL-3.0) (Languages: JavaScript 63.2%, Perl 29.4%, CSS 5.5%, Shell 1.1%, Other 0.8%) Project information // 项目信息 /// Let's Upload that FIle — File sharing software // 让我们上传文件——文件共享软件 /// Lufi // 路飞 /// What does Lufi mean? // Lufi 是什么意思？ /// Lufi means Let's Upload that FIle. It's a E2E encrypted file sharing software. // Lufi 的意思是“让我们上传那个文件”。它是一款端到端加密的文件共享软件。 /// Which browsers are compatible? // 哪些浏览器兼容？ /// Lufi is tested and working on the following browsers / devices : - Firefox - Chrome - Internet Explorer 11 - Microsoft Edge - Safari - iOS devices (ipad, iphone) - Android devices (Galaxy tab, Galaxy S8) // Lufi 已在以下浏览器/设备上测试并运行正常： - Firefox - Chrome - Internet Explorer 11 - Microsoft Edge - Safari - iOS 设备（iPad、iPhone） - Android 设备（Galaxy Tab、Galaxy S8） /// What does it do? // 它有什么作用？ /// It stores files and allows you to download them. // 它存储文件并允许您下载它们。 /// Is that all? No. All the files are encrypted by the browser! It means that your files never leave your computer unencrypted. The administrator of the Lufi instance you use will not be able to see what is in your file, neither will your network administrator, or your ISP. // 这就完了吗？不。所有文件都经过浏览器加密！这意味着您的文件永远不会以未加密的形式离开您的计算机。您使用的 Lufi 实例的管理员、您的网络管理员或您的 ISP 都无法查看您文件的内容。 /// The encryption key part of the URL is a anchor (Cf. Fragment Identifier (. wiki-en: Fragment_identifier)), that means this part is only processed client-side and does not reach the server. :-) // URL 中的加密密钥部分是一个锚点（参见片段标识符） (即 url 里用 '#' 符标识的部分) ，这意味着这部分内容仅在客户端处理，不会到达服务器。:-) /// Logo // 标识 /// Because Lufi is quite similar to Luffy, like in \"Monkey D. Luffy\" from One Piece manga, the logo is a straw hat, made with pain, love and Inkscape. // 因为 Lufi 与《 海贼王 》漫画中的“ 蒙奇·D·路飞 ”非常相似，所以它的标志是一顶草帽，是用痛苦、爱和 Inkscape 制作的。 /// Encryption // 加密 /// All the encryption/decryption processes take place in your browser. The encryption key is never sent over the network. // 所有加密/解密过程都在您的浏览器中进行。加密密钥绝不会通过网络传输。 /// However please note that some metadata are sent unencrypted: // 但请注意，部分元数据以未加密的方式发送： /// - the file name // 文件名 /// - its size // 它的大小 /// - its mimetype // 它的 mime 类型 /// Client // 客户 /// There is the web interface, but you can use a CLI client too! Have a look at lufi-cli or install it directly with sudo npm install -g -- lufi-cli. // 除了网页界面，您还可以使用命令行客户端！您可以查看 lufi-cli ，或者直接使用 sudo npm install -g -- lufi-cli 安装它。 /// There is another client in Python too: (~ framagit: setop/pylufic.git) . // 还有另一个 Python 客户端： framagit: setop/pylufic.git 。"
[server-wui.src-mirror/gh]: https://github.com/ldidry/lufi.git "[Read-only mirror] (AGPL-3.0) (Languages: JavaScript 63.2%, Perl 29.4%, CSS 5.5%, Shell 1.1%, Other 0.8%) Project information // 项目信息 /// Let's Upload that FIle — File sharing software // 让我们上传文件——文件共享软件 /// Lufi // 路飞 /// What does Lufi mean? // Lufi 是什么意思？ /// Lufi means Let's Upload that FIle. It's a E2E encrypted file sharing software. // Lufi 的意思是“让我们上传那个文件”。它是一款端到端加密的文件共享软件。 /// Which browsers are compatible? // 哪些浏览器兼容？ /// Lufi is tested and working on the following browsers / devices : - Firefox - Chrome - Internet Explorer 11 - Microsoft Edge - Safari - iOS devices (ipad, iphone) - Android devices (Galaxy tab, Galaxy S8) // Lufi 已在以下浏览器/设备上测试并运行正常： - Firefox - Chrome - Internet Explorer 11 - Microsoft Edge - Safari - iOS 设备（iPad、iPhone） - Android 设备（Galaxy Tab、Galaxy S8） /// What does it do? // 它有什么作用？ /// It stores files and allows you to download them. // 它存储文件并允许您下载它们。 /// Is that all? No. All the files are encrypted by the browser! It means that your files never leave your computer unencrypted. The administrator of the Lufi instance you use will not be able to see what is in your file, neither will your network administrator, or your ISP. // 这就完了吗？不。所有文件都经过浏览器加密！这意味着您的文件永远不会以未加密的形式离开您的计算机。您使用的 Lufi 实例的管理员、您的网络管理员或您的 ISP 都无法查看您文件的内容。 /// The encryption key part of the URL is a anchor (Cf. Fragment Identifier (. wiki-en: Fragment_identifier)), that means this part is only processed client-side and does not reach the server. :-) // URL 中的加密密钥部分是一个锚点（参见片段标识符） (即 url 里用 '#' 符标识的部分) ，这意味着这部分内容仅在客户端处理，不会到达服务器。:-) /// Logo // 标识 /// Because Lufi is quite similar to Luffy, like in \"Monkey D. Luffy\" from One Piece manga, the logo is a straw hat, made with pain, love and Inkscape. // 因为 Lufi 与《 海贼王 》漫画中的“ 蒙奇·D·路飞 ”非常相似，所以它的标志是一顶草帽，是用痛苦、爱和 Inkscape 制作的。 /// Encryption // 加密 /// All the encryption/decryption processes take place in your browser. The encryption key is never sent over the network. // 所有加密/解密过程都在您的浏览器中进行。加密密钥绝不会通过网络传输。 /// However please note that some metadata are sent unencrypted: // 但请注意，部分元数据以未加密的方式发送： /// - the file name // 文件名 /// - its size // 它的大小 /// - its mimetype // 它的 mime 类型 /// Client // 客户 /// There is the web interface, but you can use a CLI client too! Have a look at lufi-cli or install it directly with sudo npm install -g -- lufi-cli. // 除了网页界面，您还可以使用命令行客户端！您可以查看 lufi-cli ，或者直接使用 sudo npm install -g -- lufi-cli 安装它。 /// There is another client in Python too: (~ framagit: setop/pylufic.git) . // 还有另一个 Python 客户端： framagit: setop/pylufic.git 。"
[client-cli.src/fm]: https://framagit.org/fiat-tux/hat-softwares/lufi-cli.git "(~ framagit: luc/lufi-cli.git) (GPL-3.0) (Languages: JavaScript 100.0%) CLI client for Lufi. /// This a cli client for Lufi written in Javascript. You will need NodeJS for it to work."
[client-cli.npm/npmjs]: https://npmjs.com/package/lufi-cli "(: npm i -g -- lufi-cli) (GPL-3.0)"
[demo.wui/fiat-tux.fr]: https://lufi.fiat-tux.fr/ "(This is a demo instance, don't use it for real, just for test. Ceci est une instance de démonstration, utilisez ce site uniquement pour des tests. // 这是一个演示实例；请勿用于实际用途，仅用于测试。) Drop files here // 拖放文件至此"
[app.wui/disroot]: https://upload.disroot.org/ "[E2EE] Encrypted, temporary file hosting and sharing software. // 加密的临时文件托管和共享软件。 /// Lufi - Disroot file uploader /// Drop files here // 拖放文件至此"
