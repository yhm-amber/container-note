[src/gh]: https://github.com/jenkinpan/cargo-fresh.git "(Apache-2.0) (Languages: Rust 96.0%, Nushell 4.0%) A Rust tool for checking and updating globally installed Cargo packages with interactive mode and smart prerelease detection. // 一个用于检查和更新全局安装的 Cargo 包的 Rust 工具，具有交互模式和智能预发布检测功能。 /// A Rust tool for checking and updating globally installed Cargo packages with interactive mode and smart prerelease detection. After installation, you can use it via the cargo fresh command. The tool automatically detects your system language and displays the interface in Chinese or English accordingly. // 一个用于检查和更新全局安装的 Cargo 包的 Rust 工具，具有交互模式和智能预发布检测功能。安装后，您可以通过 cargo fresh 命令使用它。该工具会自动检测您的系统语言，并相应地以中文或英文显示界面。 /// ## Features // 功能 /// - 🔍 Automatically detect globally installed Cargo packages // 🔍 自动检测全局安装的 Cargo 包 /// - 📦 Check for the latest version of each package // 📦 检查每个包的最新版本 /// - 🎨 Colored output with clear update status display // 🎨 带有清晰更新状态显示的彩色输出 /// - ⚡ Concurrent processing for fast checking of multiple packages (3-5x faster) // ⚡ 并行处理，快速检查多个包（速度提升 3-5 倍） /// - 🛠️ Command-line argument support for flexible usage // 🛠️ 支持命令行参数，灵活使用 /// - 🔄 Default interactive update mode with one-click package updates // 🔄 默认交互式更新模式，支持一键更新包 /// - 🧠 Smart prerelease version detection and prompting // 🧠 智能预发布版本检测与提示 /// - 🌍 Automatic language detection (Chinese/English) // 🌍 自动语言检测（中文/英文） /// - 🚀 Cargo subcommand support (cargo fresh) // 🚀 支持 Cargo 子命令 ( cargo fresh ) /// - 🌐 Bilingual interface with smart language switching // 🌐 双语界面，智能语言切换 /// - 🚀 Batch operations - automatically update all packages without confirmation // 🚀 批量操作 - 自动更新所有包无需确认 /// - 🔍 Package filtering - filter packages by name patterns (supports glob patterns) // 🔍 包过滤 - 按名称模式过滤包（支持 glob 模式） /// - 🛡️ Enhanced error handling - intelligent retry mechanisms and user-friendly error messages // 🛡️ 增强错误处理 - 智能重试机制和用户友好的错误提示 /// - 📊 Performance optimization - HTTP connection pooling and request caching // 📊 性能优化 - HTTP 连接池和请求缓存 /// - ⚡ Fast installation - uses cargo binstall for faster package updates with automatic fallback // ⚡ 快速安装 - 使用 cargo binstall 以实现更快的包更新并自动回退 /// ## Language Support // 语言支持 /// The tool automatically detects your system language and displays the interface accordingly: // 该工具会自动检测您的系统语言并相应地显示界面： /// - Chinese Environment: Automatically displays Chinese interface // 中文环境：自动显示中文界面 /// - English Environment: Automatically displays English interface // 英文环境：自动显示英文界面 /// - Language Detection: Based on system environment variables (LANG, LC_ALL, LC_CTYPE) // 语言检测：基于系统环境变量（LANG, LC_ALL, LC_CTYPE） /// - You can also manually override the language by setting environment variables: // 您也可以通过设置环境变量来手动覆盖语言： ///: (: LANG=zh_CN.UTF-8 cargo fresh # Explicitly specify Chinese interface) (: LANG=en_US.UTF-8 cargo fresh # Explicitly specify English interface) /// ## Usage Examples // 使用示例 /// - Check all packages and show detailed information // 检查所有软件包并显示详细信息 ///: (: cargo fresh --verbose) /// - Show only packages with updates // 仅显示有更新的包 ///: (: cargo fresh --updates-only) /// - Combine options // 组合选项 ///: (: cargo fresh --verbose --updates-only) /// - Default interactive mode (recommended) // 默认交互模式（推荐） ///: (: cargo fresh) /// - Show only packages with updates (interactive mode) // 仅显示有更新的包（交互模式） ///: (: cargo fresh --updates-only) /// - Non-interactive mode // 非交互模式 ///: (: cargo fresh --no-interactive) /// - Include prerelease version checks (interactive mode) // 包含预发布版本检查（交互模式） ///: (: cargo fresh --include-prerelease) /// - Non-interactive mode + prerelease versions // 非交互模式 + 预发布版本 ///: (: cargo fresh --no-interactive --include-prerelease) /// - Batch mode - automatically update all packages without confirmation // 批处理模式 - 自动更新所有软件包，无需确认 ///: (: cargo fresh --batch) /// - Filter packages by name pattern (supports glob patterns) // 按名称模式筛选包（支持通配符模式） ///: (: cargo fresh --filter 'cargo*' # Only check packages starting with 'cargo') (: cargo fresh --filter '*mdbook*' # Only check packages containing 'mdbook') (: cargo fresh --filter 'nu*' # Only check packages starting with 'nu') /// - Combine new options with existing ones // 将新选项与现有选项结合 ///: (: cargo fresh --batch --filter 'cargo*' # Batch update only cargo packages) (: cargo fresh --verbose --filter '*mdbook*' # Verbose check for mdbook packages) (: cargo fresh --batch --updates-only # Batch update only packages with updates) /// - Generate shell completion scripts // 生成 shell 补全脚本 ///: (: cargo fresh completion zsh # Generate zsh completion) (: cargo fresh completion bash # Generate bash completion) (: cargo fresh completion fish # Generate fish completion) /// - Generate cargo fresh subcommand completion // 生成 cargo fresh 子命令自动完成 ///: (: cargo fresh completion zsh --cargo-fresh # Generate cargo fresh zsh completion) (: cargo fresh completion bash --cargo-fresh # Generate cargo fresh bash completion)"
[cli.cargo/crates]: https://crates.io/crates/cargo-fresh "(: cargo install -- cargo-fresh) (Apache-2.0) (1.6K SLoC) (41.5 KiB) A Rust tool for checking and updating globally installed Cargo packages with interactive mode and smart prerelease detection // 一个用于检查和更新全局安装的 Cargo 包的 Rust 工具，具有交互模式和智能预发布检测功能 (src: gh:jenkinpan/cargo-fresh.git)"

