[src/gh]: https://github.com/ggez/ggez.git "(MIT) (Languages: Rust 96.4%, WGSL 3.6%) Rust library to create a Good Game Easily // 用 Rust 创建好游戏轻松的库 /// ggez is a Rust library to create a Good Game Easily. // ggez 是一个用 Rust 编写的轻松创建游戏的库。 /// More specifically, ggez is a lightweight cross-platform game framework for making 2D and 3D games with minimum friction. It aims to implement an API based on (a Rustified version of) the LÖVE game framework. This means it contains basic and portable 2D drawing(in addition 3D which love doesn't have an API for), sound, resource loading and event handling, but finer details and performance characteristics may be different than LÖVE. // 更具体地说，ggez 是一个轻量级的跨平台游戏框架，用于制作 2D 和 3D 游戏，以最小的摩擦。它的目标是实现一个基于（LÖVE 游戏框架的 Rust 版本）的 API。这意味着它包含基本的、可移植的 2D 绘图（以及 LÖVE 没有提供 API 的 3D），声音、资源加载和事件处理，但更精细的细节和性能特征可能与 LÖVE 不同。 /// ggez is not meant to be everything to everyone, but rather a good base upon which to build. Thus it takes a fairly batteries-included approach without needing a million additions and plugins for everything imaginable, but also does not dictate higher-level functionality such as physics engine or entity component system. Instead the goal is to allow you to use whichever libraries you want to provide these functions, or build your own libraries atop ggez. // ggez 并不是为所有人提供所有功能，而是一个良好的基础，用于构建。因此它采取了相当全面的“开箱即用”方法，不需要为各种想象中的事物添加成千上万的功能和插件，但也并不强制规定高级功能，如物理引擎或实体组件系统。相反，它的目标是允许你使用你想要的任何库来提供这些功能，或者基于 ggez 构建自己的库。 /// Features // 功能 /// - Filesystem abstraction that lets you load resources from folders or zip files // 文件系统抽象，允许您从文件夹或 zip 文件中加载资源 /// - Hardware-accelerated 2D and 3D rendering built on the wgpu graphics API // 基于 wgpu 图形 API 的硬件加速 2D 和 3D 渲染 /// - Loading and playing .ogg, .wav and .flac files via the rodio crate // 通过 rodio crate 加载和播放 .ogg、.wav 和 .flac 文件 /// - TTF font rendering with glyph_brush. // 使用 glyph_brush 进行 TTF 字体渲染 /// - Interface for handling keyboard and mouse events easily through callbacks // 通过回调轻松处理键盘和鼠标事件的接口 /// - Config file for defining engine and game settings // 用于定义引擎和游戏设置的配置文件 /// - Easy timing and FPS measurement functions. // 易于使用的计时和 FPS 测量功能。 /// - Math library integration with mint. // 与 mint 集成的数学库 /// - Some more advanced graphics options: shaders, instanced draws and render targets // 一些更高级的图形选项：着色器、实例绘制和渲染目标 /// Non-Features (i.e. things to add from elsewhere if needed) // 非功能特性（即需要从其他地方添加的内容） /// - Physics // 物理 /// - Animation (check out keyframe; it works pretty well with ggez (source)) // 动画（可以查看关键帧；它与 ggez 配合得相当好（来源）） /// - GUI // 图形用户界面 /// - Assets manager // 资源管理器 /// - AI // 人工智能 /// - ECS // 实体组件系统 /// - Networking // 网络"
[lib.cargo/crates]: https://crates.io/crates/ggez "(: cargo add -- ggez) (MIT) (9.2K SLoC) (246 KiB) A lightweight game framework for making 2D games with minimum friction, inspired by Love2D. // 一个轻量级游戏框架，用于制作 2D 游戏，以最小摩擦为灵感，受 Love2D 启发。 (src: gh:ggez/ggez.git)"
[site/rs]: https://ggez.rs/ "A Rust library to create Good Games Easily // 一个用于轻松创建优秀游戏的 Rust 库 /// More specifically, ggez is a lightweight game framework for making 2D games with minimum friction. It aims to implement an API based on (a Rustified version of) the LÖVE game framework. Thus it contains portable 2D drawing, sound, resource loading and event handling. // 更具体地说，ggez 是一个轻量级游戏框架，用于制作 2D 游戏，旨在最小化开发摩擦。它旨在实现一个基于（LÖVE 游戏框架的 Rust 版本）的 API。因此它包含了可移植的 2D 绘图、声音、资源加载和事件处理功能。"
[miniquad.src/gh]: https://github.com/ggez/good-web-game.git "(MIT) (Languages: Rust 99.7%, GLSL 0.3%) An alternative ggez implementation on top of miniquad. // 基于 miniquad 的 ggez 替代实现。 /// good-web-game is a wasm32-unknown-unknown implementation of a ggez (. gh:ggez/ggez.git) subset on top of miniquad (. gh:not-fl3/miniquad.git). Originally built to run Zemeroth (. gh:ozkriff/zemeroth.git) on the web. // good-web-game 是基于 miniquad 的 ggez 子集的 wasm32-unknown-unknown 实现。最初是为了在网页上运行 Zemeroth 而构建的。 /// It currently supports most of the ggez 0.7.0 API. If you're already working with ggez you might use this library to port your game to the web (or even mobile). Since it also runs well on desktop it also offers an alternative implementation of ggez, which might always come in handy. // 它目前支持 ggez 0.7.0 API 的绝大部分。如果你已经在使用 ggez，你可以使用这个库将你的游戏移植到网页（甚至移动设备）上。由于它也能在桌面端运行良好，因此它也提供了 ggez 的另一种实现方式，这可能总是很有用。 /// If you are just looking for a well supported, more serious, minimal high-level engine on top of miniquad you might want to take a look at macroquad (. gh:not-fl3/macroquad.git). // 如果你只是想找一个在 miniquad 之上支持良好、更严肃、极简的高级引擎，你可能需要看看 macroquad。"
[miniquad-lib.cargo/crates]: https://crates.io/crates/good-web-game "(: cargo add -- good-web-game) (MIT) (5.0K SLoC) (224 KiB) An alternative implementation of the ggez game engine, based on miniquad // 基于 miniquad 的游戏引擎 ggez 的另一种实现 (src: gh:ggez/good-web-game.git)"
[knowsby]: https://github.com/ozkriff/awesome-quads.git "Game engines // 游戏引擎 /// - miniquad (. gh:not-fl3/miniquad.git) - main crate, engines below build on top of it. Supports WASM. Therefore everything build on top of it supports WASM too. // miniquad - 主 Crate，下面的引擎都构建在其之上。支持 WASM。因此，所有构建在其之上的内容也都支持 WASM。 /// - macroquad (. gh:not-fl3/macroquad.git) - simple to use library from author of miniquad. // macroquad - 由 miniquad 的作者开发的简单易用库。 /// - good-web-game (. gh:not-fl3/good-web-game.git, gh:ggez/good-web-game.git) - crate to easily port your ggez (. gh:ggez/ggez.git) to WASM. // good-web-game - 用于轻松将 ggez 移植到 WASM 的 Crate。 /// - emerald (. gh:Bombfuse/emerald.git) - game engine with physics, audio, graphics, ECS. // emerald - 带有物理、音频、图形、ECS 的游戏引擎。 /// - rust-rpg-toolkit (. gh:olefasting/rust_rpg_toolkit.git) - a framework for creating customizable and user modifiable action RPG's, using Rust and/or JSON. // rust-rpg-toolkit - 一个用于创建可定制和用户可修改的动作 RPG 的框架，使用 Rust 和/或 JSON。 /// - omegaquad (. gh:gamma-delta/omegaquad.git) - hackable, opinionated layer on top of Macroquad for writing games // omegaquad - 基于 Macroquad 的可扩展、主观的游戏开发层。"


