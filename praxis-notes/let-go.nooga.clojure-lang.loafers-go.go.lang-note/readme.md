[src/gh]: https://github.com/nooga/let-go.git "(MIT) (Languages: Go 75.1%, Clojure 20.5%, HTML 2.2%, Shell 1.9%, Fennel 0.1%, Ruby 0.1%, Other 0.1%) Almost Clojure written in Go. // 用 Go 语言编写的 Clojure 差不多是。 /// let-go // 松手 /// Greetings loafers! (λ-gophers haha, get it?) // 各位懒汉们好！ （λ-地鼠，哈哈，明白我的意思吗？） /// let-go is a Clojure dialect with a bytecode compiler and stack VM, written in Go. A single ~10MB binary, ~7ms cold start, no JVM. Roughly 95% Clojure-compatible on the jank-lang test suite. // let-go 是一个 Clojure 方言，它包含字节码编译器和栈虚拟机，使用 Go 语言编写。它只有一个约 10MB 的二进制文件，冷启动时间约 7 毫秒，无需 JVM。在 jank-lang 测试套件 (. gh: jank-lang/clojure-test-suite.git) 上，它与 Clojure 的兼容性约为 95%。 /// I started this in 2021 as an elaborate joke: an excuse to write Clojure while pretending to write Go. It turned out useful. I use it for CLIs, scripts, and web servers, and I built a daemonless container runtime on top of it. You can compile let-go programs to standalone binaries or self-contained WASM web pages. It even runs on Plan 9. // 我最初在 2021 年启动这个项目，纯粹是出于一个精心设计的玩笑：假装在写 Go，实则用 Clojure 写代码。结果它竟然派上了用场。我用它来编写命令行界面、脚本和 Web 服务器，并且还构建了一个无守护进程的容器运行时。 除此之外，你还可以将 let-go 程序编译成独立的二进制文件，或者 独立的 WASM 网页。 它甚至可以在 Plan 9 上运行 。 /// It is not a drop-in replacement for Clojure JVM. It does not load JARs and does not aim to. Most idiomatic Clojure code runs unmodified, but a real project with library dependencies will need adjustments. See Known limitations below. // 它并非 Clojure JVM 的直接替代品。它不会加载 JAR 文件，而且 并非旨在如此。大多数地道的 Clojure 代码无需修改即可运行，但实际上…… 包含库依赖项的项目需要进行调整。请参阅 以下是已知的局限性 。 /// Goals (in no particular order) // 目标（排名不分先后） /// - Quality entertainment // 优质娱乐 /// - Implement most of Clojure: persistent data structures, lazy seqs, transducers, protocols, records, multimethods, core.async, BigInts // 实现 Clojure 的大部分功能：持久化数据结构、惰性序列、转换器、协议、记录、多方法、core.async、BigInts /// - Comfy two-way Go interop (functions, structs, channels) // 舒适的双向 Go 互操作（函数、结构体、通道） /// - AOT compilation to bytecode and standalone binaries // AOT 编译为字节码和独立二进制文件 /// - Boot the runtime inside a single requestAnimationFrame (10ms left over at 60fps) // 在单个 requestAnimationFrame 内启动运行时（60fps 时剩余 10ms） /// - Compile programs to self-contained WASM web pages with terminal emulation // 将程序编译成带有终端仿真功能的独立 WASM 网页 /// - Make it legal to write Clojure at your Go dayjob // 让你的 Go 日常工作中编写 Clojure 代码合法化 /// - nREPL in the browser (let-go VM in WASM, editor over WebSocket) // 浏览器中的 nREPL（WASM 中的 let-go VM，通过 WebSocket 编辑器） /// - Stretch: let-go bytecode → Go translation // 拉伸：let-go 字节码 → Go 翻译 /// Non-goals: drop-in JVM Clojure replacement; linter/formatter for Clojure-at-large. // 非目标：可直接替换 JVM Clojure 的代码；适用于 Clojure 的 linter/formatter。 /// Behavioral differences // 行为差异 /// - concat* (used internally by quasiquote) is eager; user-facing concat is lazy // concat* （quasiquote 内部使用）是立即执行的；面向用户的 concat 是延迟执行的。 /// - <! / <!! are identical, same for >! / >!! (Go channels always block) // <! / <!! 相同， >! / >!! 也相同。（Go 频道始终阻塞） /// - go blocks are real goroutines, not IOC state machines (cheaper, and they can call blocking ops directly) // go 代码块是真正的 goroutine，而不是 IOC 状态机（成本更低，而且可以直接调用阻塞操作）。 /// - No BigDecimal: numeric tower is int64 + float64 + BigInt // 无 BigDecimal：数值塔是 int64 + float64 + BigInt /// - Regex is Go flavor (re2), not Java regex // Regex 是 Go 语言风格的（ re2 ），而不是 Java 风格的正则表达式。 /// - letfn uses atoms internally for forward references // letfn 内部使用原子进行前向引用"
[play-intro/ghio]: https://nooga.github.io/let-go/ "Almost Clojure in Go. // 用 Go 语言实现的几乎 Clojure 。 /// let-go is a Clojure-like lisp, running on a small Go-built VM instead of the JVM, so it boots fast and fits in a pocket. Your programs compile to standalone binaries. // let-go 是一个类似 Clojure 的 Lisp 程序，运行在一个小型 Go 构建的服务器上。 它使用虚拟机（VM）而不是 JVM，因此启动速度快，而且可以放进口袋。你的 程序编译成独立的二进制文件。 /// ### Usage // 用法 ///: One lg binary covers the lifecycle: open a REPL, bundle a script into a standalone executable, or build a self-contained WASM page. // 一个 lg 二进制文件涵盖了生命周期：打开 REPL，将脚本打包成一个 独立可执行文件，或构建一个独立的 WASM 页面。 /// - REPL ///: (: lg) /// - nREPL ///: (: lg -n # starts an nREPL server on port 2137 for Calva, CIDER, etc. // 在 2137 端口上启动 nREPL 服务器，用于 Calva、CIDER 等。) /// - Bundle ///: (: lg -b abcprog main.lg # produces a standalone binary. // 生成一个独立的二进制文件。) /// - WASM ///: (: lg -w ./out main.lg # a static directory with html, wasm, and runtime. // 一个包含 html、wasm 和运行时的静态目录。) /// ### Embedding // 嵌入 ///: let-go embeds in any Go program as a library. Define Go values and functions on the runtime, evaluate Clojure source against them. Channels, structs, and slices cross the boundary in both directions. // let-go 以库的形式嵌入到任何 Go 程序中。在运行时定义 Go 值和函数，并针对它们执行 Clojure 源代码。通道、结构体和切片可以双向跨越边界。 ///: (: go get github.com/nooga/let-go) (: import \"github.com/nooga/let-go/pkg/api\" ;: lg, _ := api.NewLetGo(\"user\")) (: lg.Def(\"x\", 42) ;: lg.Def(\"mul\", func(a, b int) int { return a * b })) (: v, _ := lg.Run(\"(mul x 2)\") // => 84)"
[cli.go/pdg]: https://pkg.go.dev/github.com/nooga/let-go "(: go install -- github.com/nooga/let-go@latest) (License: MIT) (Imports: 20) (Imported by: 0) "
[example:xsofy.src/gh]: https://github.com/nooga/xsofy.git "(MIT) (Languages: Clojure 100%) Roguelike that names itself each run. WIP // 每次运行都会自动命名的 Roguelike 游戏。开发中 /// Xs of Y // Y 的 Xs /// A roguelike written in a lisp, where the magic system is lisp. // 一款用 Lisp 语言编写的 Roguelike 游戏，其魔法系统也是 Lisp 语言编写的。"
[example:xsofy.wui/ghio]: https://nooga.github.io/xsofy/ "(A Char-composed TUI game in web.)"
[example:lgcr.src/gh]: https://github.com/nooga/lgcr.git "(MIT) (Languages: Clojure 74.3%, Shell 24.9%, Go 0.8%) A small Linux container runtime written in let-go // 一个用 let-go 编写的小型 Linux 容器运行时环境 /// lgcr is a small, daemonless Linux container runtime written in let-go, a Clojure dialect that compiles to bytecode for a Go VM. // lgcr 是一个用 Linux 编写的小型、无守护进程的 Linux 容器运行时。 let-go 是一个 Clojure 方言，它可以编译成 Go VM 的字节码。 /// It has a Docker-shaped CLI, but it is not a Docker clone. On Linux it bundles to one executable. On macOS, ./lgcr is a tiny native forwarder into a Lima VM named letgo, so the same command works without wrapping everything in limactl shell. // 它的命令行界面（CLI）类似于 Docker ，但它不是 Docker 的克隆。在 Linux 上，它打包成一个可执行文件。在 macOS 上， ./lgcr 是一个微型的本地转发器，它连接到名为 letgo 的 Lima 虚拟机，因此相同的命令无需在 limactl shell 中封装一切即可使用。 /// The fun part is the size/shape of it: about 3k real LOC for the runtime, content-addressable image store, networking, pure helper library, macOS forwarder, and build script. In that space it does real OCI pulls, namespaces, cgroups, overlay rootfs, PID 1 signal/reaper work, exec over a control socket, interactive ptys, bind mounts, tmpfs, capabilities, seccomp, bridge NAT, port publishing, image GC, and startup reconciliation. // 有趣的是它的规模和结构：运行时、内容寻址镜像存储、网络、纯辅助库、macOS 转发器和构建脚本的实际代码量约为 3000 行。在这有限的空间里，它实现了真正的 OCI 拉取、命名空间、cgroups、overlay 根文件系统、PID 1 信号/回收器操作、通过控制套接字执行、交互式伪终端、绑定挂载、tmpfs、功能、seccomp、桥接 NAT、端口发布、镜像垃圾回收和启动协调等功能。 /// So, yes, it is tiny and lispy. Most of the runtime state is just maps and vectors, most parsing/formatting lives in pure tested helpers, and the nasty bits stay at the syscall boundary. // 所以，是的，它很小巧，而且语法略显生硬。大部分运行时状态只是映射和向量，大部分解析/格式化操作都位于经过测试的纯辅助函数中，而那些棘手的部分则保留在系统调用边界处。 /// Current status // 当前状态 /// This is past toy-runtime territory, but it is not trying to be Docker. // 这已经超越了玩具运行时的范畴，但它并不是要成为 Docker。 /// What works: // 已可工作： /// - OCI pull: Docker Hub and v2 registries, token auth, manifest-list selection, config blobs, streaming layer downloads. // OCI 拉取 ：Docker Hub 和 v2 注册表、令牌认证、清单列表选择、配置 blob、流式层下载。 /// - Content-addressable image store: blobs, refs, manifests, snapshots, repeated-pull reuse, image GC, partial blob resume, and integrity repair. // 内容寻址映像存储 ：blobs、refs、清单、快照、重复拉取重用、映像 GC、部分 blob 恢复和完整性修复。 /// - Run lifecycle: foreground, detached, --rm, restart stopped containers, short-id lookup with ambiguity checks. // 运行生命周期 ：前台、分离、 --rm 、重启已停止的容器、带歧义检查的短 ID 查找。 /// - Real init: PID 1 inside the container namespace, signal forwarding, orphan reaping, signal-aware exit status. // 实际初始化 ：容器命名空间内的 PID 1，信号转发，孤立进程回收，信号感知退出状态。 /// - Exec: commands run through the container's existing init over a Unix control socket with fd passing; no CGO setns trickery. // 执行 ：命令通过容器现有的 init 程序，经由 Unix 控制套接字运行，并传递文件描述符；不使用 CGO setns 技巧。 /// - Interactive ptys: run -it and exec -it, raw-mode input, window-size propagation. // 交互式 ptys ： run -it 和 exec -it ，原始模式输入，窗口大小传播。 /// - Filesystem controls: overlay rootfs, bind mounts, read-only binds, tmpfs mounts, read-only rootfs with writable /tmp and /run. // 文件系统控制 ：覆盖根文件系统、绑定挂载、只读绑定、tmpfs 挂载、只读根文件系统 /tmp 和 /run 可写）。 /// - Networking: --net host, --net none, and bridge networking with NAT egress plus TCP port publishing via -p HOSTPORT:CONTPORT. // 网络 ： --net host 、 --net none 和桥接网络，通过 NAT 出口以及通过 -p HOSTPORT:CONTPORT 发布 TCP 端口。 /// - Security basics: default capability set, --cap-add, --cap-drop, no_new_privs, default seccomp filter, optional unconfined seccomp, curated /dev, and explicit AppArmor profile support. // 安全基础知识 ：默认功能集、 --cap-add 、 --cap-drop no_new_privs ，默认 seccomp 过滤器，可选的非限制性 seccomp，精选 /dev ，以及显式 AppArmor 配置文件支持。 /// - macOS workflow: ./lgcr is a native shim that forwards into a Lima VM, so the Linux runtime can be driven from macOS without typing limactl. // macOS 工作流程 ： ./lgcr 是一个原生 shim，它会将请求转发到 Lima VM，因此无需输入 limactl 即可从 macOS 驱动 Linux 运行时。 /// What is intentionally still open: // 故意保留为打开的： /// - rootless user namespaces // 无根用户命名空间 /// - image build // 镜像构建 /// - signature verification // 签名验证 /// - daemonized lifecycle event API / restart policy // 守护进程化生命周期事件 API / 重启策略 /// - broader Docker compatibility // 更广泛的 Docker 兼容性 /// - Kubernetes CRI integration, probably never // Kubernetes CRI 集成，可能永远不会 /// Quickstart // 快速入门 /// - Pull an image. ///: (: ./lgcr pull alpine:3.21) /// - Run a foreground command. ///: (: ./lgcr run alpine:3.21 echo 'hello from a lisp container runtime') /// - With cid ///: (: cid=$(./lgcr run -d alpine:3.21 sh -c 'while :; do date; sleep 1; done') # create instance) (: ./lgcr logs -f \"$cid\" # Run detached and follow logs.) (: ./lgcr exec -it \"$cid\" sh # Exec into the running container.) (: ./lgcr stop \"$cid\" # Stop it. ;: ./lgcr rm \"$cid\" # remove it.)"
