[src/gh]: https://github.com/davidspivak2/cue.git "(GPL-3.0) (Languages: TypeScript 51.7%, Python 40.8%, NSIS 2.0%, PowerShell 1.9%, Rust 1.8%, Batchfile 1.0%, Other 0.8%) Local desktop app for creating, styling, and burning subtitles into video. // 本地桌面应用程序，用于创建、样式设置和将字幕烧录到视频中。 /// Cue /// Create subtitles for any video, in any language, on your own computer. // 在任何语言中，在任何视频上，在你的电脑上创建字幕。 /// Cue uses speech recognition to build subtitles and burn them into the video. Everything runs locally: no upload, no cloud account, no subscription. // Cue 使用语音识别来创建字幕并将它们烧录到视频中。所有操作都在本地进行：无需上传，无需云账户，无需订阅。 /// Features // 功能 /// - Automatic subtitles: Add a video from Home, get an SRT file out. Uses Whisper large-v3. // 自动字幕：从主页添加视频，生成 SRT 文件。使用 Whisper large-v3。 /// - Burn-in export: New MP4 with subtitles baked in; no separate encoder step. // 烧录导出：新的 MP4 文件包含内嵌字幕；无需单独的编码步骤。 /// - Word-by-word highlighting: Karaoke in the Effects panel tracks the speaker as the video plays. // 逐字高亮：效果面板中的卡拉 OK 功能会随着视频播放跟踪发言人。 /// - Style customization: Presets plus fonts, colors, outlines, shadows, and backgrounds in the Editor Effects panel. // 样式自定义：编辑器效果面板中的预设，以及字体、颜色、轮廓、阴影和背景。 /// - Languages: Auto-detects language; supports what Whisper supports. // 语言：自动检测语言；支持 Whisper 支持的语言。 /// - RTL: Right-to-left layout for Hebrew, Arabic, and other RTL scripts. // RTL：从右到左的布局，适用于希伯来语、阿拉伯语和其他从右到左的脚本。 /// - GPU acceleration: Uses CUDA when available; otherwise CPU. // GPU 加速：在可用时使用 CUDA；否则使用 CPU。 /// - Offline: Processing stays on your machine; videos are not uploaded. // 离线：处理在您的机器上进行；视频不会被上传。 /// - Open source: GPL-3.0; see License below. // 开源：GPL-3.0；请查看下方的许可证。 /// Cue extracts the audio from your video, runs it through Whisper (via faster-whisper) to generate subtitles, and then uses FFmpeg to burn them into a new video. Word-level timing for the highlight mode comes from WhisperX alignment. // Cue 从您的视频中提取音频，通过 faster-whisper (gh: SYSTRAN/faster-whisper.git) 运行 Whisper (gh: openai/whisper.git) 以生成字幕，然后使用 FFmpeg (web: ffmpeg.org) 将其烧录到新视频中。高亮模式的逐字时间来自 WhisperX (gh: m-bain/whisperX.git) 对齐。 /// Quick start // 快速入门 /// - Home (project hub): Click Add video or drop a supported file on the empty area (MP4, MKV, MOV, M4V, WEBM). Cue opens the video in an Editor tab (you can keep several videos open and switch tabs in the title bar). // 主页（项目中心）：点击添加视频或将要支持的视频文件拖放到空白区域（MP4、MKV、MOV、M4V、WEBM）。Cue 将在编辑选项卡中打开视频（您可以同时保持多个视频打开并在标题栏中切换选项卡）。 /// - Create subtitles: In the Editor, click Create subtitles and wait for transcription. The first run may download the Whisper model, which can take a few minutes. // 创建字幕：在编辑器中，点击创建字幕并等待转录完成。第一次运行可能会下载 Whisper 模型，这可能需要几分钟时间。 /// - Review and style: Use the preview and the Effects panel for presets, typography, colors, outline, shadow, background, and Karaoke for word-by-word highlighting. // 审阅和样式：使用预览和效果面板进行预设、字体、颜色、轮廓、阴影、背景和卡拉 OK（逐字高亮）。 /// - Export: Click Export to burn subtitles into a new MP4. // 导出：点击导出将字幕烧录到新的 MP4 文件中。"
