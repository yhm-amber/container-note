[post]: https://rust-gpu.github.io/blog/2025/07/25/rust-on-every-gpu/ "Rust running on every GPU // Rust 在每款 GPU 上运行 /// The same compute logic runs on all targets, written entirely in regular Rust. No shader or kernel languages are used. // 相同的计算逻辑在所有目标上运行，完全使用常规 Rust 编写。不使用任何着色器或内核语言。"
[demo.src/gh]: https://github.com/LegNeato/rust-gpu-chimera.git "(Languages: Rust 82.6%, Shell 17.4%) Demo project showing a single Rust codebase running on CPU and directly on GPUs // 展示单个 Rust 代码库在 CPU 和直接在 GPU 上运行的演示项目 /// A cross-platform demo of a single Rust codebase running on both the CPU and GPU via CUDA, Vulkan, Metal, and DirectX. There are no shader or kernel languages used, only Rust. // 一个跨平台演示，展示单个 Rust 代码库如何在 CPU 和 GPU 上通过 CUDA、Vulkan、Metal 和 DirectX 运行。这里没有使用任何着色器或内核语言，只有 Rust。"
