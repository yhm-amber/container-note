[src/gh]: https://github.com/ouch-org/ouch.git "(MIT) (Languages: Rust 97.4%, Shell 2.5%, Nix 0.1%) Painless compression and decompression in the terminal // 终端的低痛苦压缩和解压缩工具 /// Ouch! // 哎哟! /// ouch stands for Obvious Unified Compression Helper. // ouch 代表 Obvious Unified Compression Helper （能带来直观明显体验的统一压缩助手） 。 /// It's a CLI tool for compressing and decompressing for various formats. // 这是一个用于压缩和解压缩各种格式文件的命令行工具。 /// Features // 特征 /// - Easy to use. // 便于使用。 /// - Fast. // 快速地。 /// - Great error message feedback. // 非常好的错误信息反馈。 /// - No runtime dependencies required (for Linux x86_64). // 无需运行时依赖项（适用于 Linux x86_64 ）。 /// - Accessibility mode (see more). // 辅助功能模式（ 查看更多 ）。 /// - Shell completions and man pages. // Shell 补全和 man 手册页。 /// - Detect the extensions automatically // 自动检测扩展名 /// Decompressing // 解压 /// (: ouch d a.zip b.tar.gz c.tar # Decompress multiple files) (: ouch d pics.zip --dir p001 # Decompress 'pics.zip' inside of new folder 'p001'.) /// Compressing // 压缩 // (: ouch compress one.txt two.txt abc.* archive.tar.xz # Compress two files using 'tar' and 'xz'.) /// Listing // 列出 /// (: ouch ls source-code.zip --tree # List with tree formatting) /// If you wish to exclude non-free code from your build, you can disable RAR support by building without the unrar feature. // 如果您希望从构建中排除非自由代码，可以通过构建时不使用 unrar 功能来禁用 RAR 支持。 /// If the filename has no extensions, Ouch will try to infer the format by the file signature and ask the user for confirmation. // 如果文件名没有扩展名， Ouch 将尝试通过文件签名推断文件格式，并要求用户确认。"
[repology]: https://repology.org/project/ouch "Versions for ouch"
[cli.cargo/crates]: https://crates.io/crates/ouch "(: cargo install -- ouch) (MIT) (3.5K SLoC) (76.7 KiB) ouch // 哎哟 /// A command-line utility for easily compressing and decompressing files and directories. // 一个用于轻松压缩和解压缩文件和目录的命令行实用程序。 (src: gh:ouch-org/ouch.git)"

