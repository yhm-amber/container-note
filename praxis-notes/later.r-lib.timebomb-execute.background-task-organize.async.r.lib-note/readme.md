[src/gh]: https://github.com/r-lib/later.git "(MIT) (Languages: C++ 47.0%, R 26.7%, C 26.3%) Schedule an R function or formula to run after a specified period of time // 在指定时间段后运行 R 函数或公式 /// Schedule an R function or formula to run after a specified period of time. Similar to JavaScript’s setTimeout function. Like JavaScript, R is single-threaded so there’s no guarantee that the operation will run exactly at the requested time, only that at least that much time will elapse. // 在指定时间段后执行 R 函数或公式。类似于 JavaScript 的 setTimeout 函数。与 JavaScript 类似，R 是单线程的，因此无法保证操作会精确地在请求的时间运行，只能保证至少会经过请求的时间段。 /// To avoid bugs due to reentrancy, by default, scheduled operations only run when there is no other R code present on the execution stack; i.e., when R is sitting at the top-level prompt. You can force past-due operations to run at a time of your choosing by calling later::run_now(). // 为了避免由于递归调用引起的错误，默认情况下，计划的操作仅在执行堆栈上没有其他 R 代码时运行；即当 R 位于顶级提示符时。您可以通过调用 later::run_now() 强制过期操作在您选择的时间运行。 /// The mechanism used by this package is inspired by Simon Urbanek’s background (. gh:s-u/background.git) package and similar code in Rhttpd. // 此软件包使用的机制灵感来自 Simon Urbanek 的 background 软件包以及 Rhttpd 中的类似代码。 /// (: (\ () print('Got here!')) |> later::later(5) ;: (~ print('Got here!')) |> later::later(5))"
[package/cran]: https://cran.r-project.org/web/packages/later/ "(Version: 	1.4.4) (Imports: 	Rcpp (≥ 0.12.9), rlang) (LinkingTo: 	Rcpp) (Suggests: 	knitr, nanonext, rmarkdown, testthat (≥ 3.0.0)) (Published: 	2025-08-27) (DOI: 	10.32614/CRAN.package.later) (Author: 	Winston Chang [aut], Joe Cheng [aut], Charlie Gao ORCID iD [aut, cre], Posit Software, PBC ROR ID [cph, fnd], Marcus Geelnard [ctb, cph] (TinyCThread library, https://tinycthread.github.io/), Evan Nemerson [ctb, cph] (TinyCThread library, https://tinycthread.github.io/)) (Maintainer: 	Charlie Gao <charlie.gao at posit.co>) (License: 	MIT + file LICENSE) (NeedsCompilation: 	yes) later: Utilities for Scheduling Functions to Execute Later with Event Loops // later: 用于使用事件循环调度函数稍后执行的实用工具 /// Executes arbitrary R or C functions some time after the current time, after the R execution stack has emptied. The functions are scheduled in an event loop. // 在当前时间之后、R 执行栈清空之后执行任意 R 或 C 函数。这些函数在事件循环中被调度。 (src: gh:r-lib/later.git)"
[site/r-lib.org]: https://later.r-lib.org/ "Executes a function later // 稍后执行一个函数 /// To avoid bugs due to reentrancy, by default, scheduled operations only run when there is no other R code present on the execution stack; i.e., when R is sitting at the top-level prompt. You can force past-due operations to run at a time of your choosing by calling later::run_now(). // 为了避免由于递归调用引起的错误，默认情况下，计划的操作仅在执行堆栈上没有其他 R 代码时运行；也就是说，当 R 位于顶级提示符时。您可以通过调用 later::run_now() 强制过期操作在您选择的时间运行。 /// Error handling is not particularly well-defined and may change in the future. options(error=browser) should work and errors in func should generally not crash the R process, but not much else can be said about it at this point. If you must have specific behavior occur in the face of errors, put error handling logic inside of func. // 错误处理尚未特别明确，未来可能会有所变化。使用 options(error=browser)应该可以工作， func 中的错误通常不会导致 R 进程崩溃，但目前关于这一点还能说的不多。如果你必须在错误情况下实现特定的行为，请将错误处理逻辑放在 func 中。"
[knows_by]: https://cran.r-project.org/web/packages/coro/ "... /// Suggests: /// - later (≥ 1.1.0) /// ..."

