[src/gh]: https://github.com/plokhotnyuk/jsoniter-scala.git "(MIT) (Languages: Scala 99.6%, Other 0.4%) Scala macros for compile-time generation of safe and ultra-fast JSON codecs + circe booster // 用于编译时生成安全且超快速 JSON 编解码器的 Scala 宏 + circe booster /// Acknowledgments // 致谢 /// This library had started from macros that reused jsoniter (json-iterator) for Java (. gh: json-iterator/java.git) reader and writer but then the library evolved to have its own core of mechanics for parsing and serialization. // 这个库最初是基于 Java 重用 jsoniter（json-iterator）的宏而开发的。 最初是读者和写者，但后来该库发展出了自己的解析和序列化机制的核心。 /// The idea to generate codecs by Scala macros and main details were borrowed from Kryo (. gh: evolution-gaming/kryo-macros.git) Macros (originally developed by Alexander Nemish (. gh: @nau)) and adapted for the needs of the JSON domain. // 利用 Scala 宏生成编解码器的想法以及主要细节借鉴自 Kryo 宏 （最初由 Alexander Nemish 开发）并根据 JSON 领域的需求进行了调整。 /// Other Scala macros features were peeped in AVSystem Commons (. gh: AVSystem/scala-commons.git @master/commons-macros/src/main/scala/com/avsystem/commons/macros) and magnolia (. gh: softwaremill/magnolia.git) libraries. // 在 AVSystem Commons 中还发现了其他 Scala 宏特性。还有 magnolia 库。 /// Ideas for the most efficient parsing and serialization of java.time.* values were inspired by DSL-JSON's implementation (. gh: ngs-doo/dsl-json.git) for java.time.OffsetDateTime. // 对 java.time.* 值进行最高效解析和序列化的想法源于 DSL-JSON 对 java.time.OffsetDateTime 的实现。"
[lib.mvn/mvnrepository]: https://mvnrepository.com/artifact/com.github.plokhotnyuk.jsoniter-scala/jsoniter-scala-core "(~ pkg:maven/com.github.plokhotnyuk.jsoniter-scala/jsoniter-scala-core@...)"
[knowsby]: https://github.com/json-iterator/java.git "Scala User: gh:plokhotnyuk/jsoniter-scala.git"

