[src/gh]: https://github.com/rainxchzed/Github-Store.git "(Apache-2.0) (Languages: Kotlin 97.7%, Python 2.3%) A free, open-source app store for GitHub releases — browse, discover, and install apps with one click. Powered by Kotlin and Compose Multiplatform for Android & Desktop. // 一个免费的开源应用商店，专门用于发布 GitHub 应用——一键浏览、发现和安装应用。基于 Kotlin 和 Compose，支持 Android 和桌面平台。 /// GitHub Store // GitHub 商店 /// GitHub Store is a cross‑platform “play store” for GitHub releases. It discovers repositories that ship real installable binaries and lets you install, track, and update them across platforms from one place. // GitHub Store 是一个跨平台的 GitHub 发布版本“应用商店”。它可以发现提供真正可安装二进制文件的仓库，并让你在一个地方跨平台安装、跟踪和更新这些仓库。 /// ✨ What is GitHub Store? // ✨ 什么是 GitHub Store？ /// GitHub Store is a Kotlin Multiplatform app (Android + Desktop) that turns GitHub releases into a clean, app‑store style experience: // GitHub Store 是一个 Kotlin 多平台应用程序（Android + 桌面），它将 GitHub 发布内容转化为简洁的、应用商店风格的体验： /// - Only shows repositories that actually provide installable assets (APK, EXE, DMG, AppImage, DEB, RPM, etc.). // 仅显示实际提供可安装资源（APK、EXE、DMG、AppImage、DEB、RPM 等）的存储库。 /// - Detects your platform and surfaces the correct installer. // 检测您的平台并显示正确的安装程序。 /// - Always installs from the latest published release, highlights its changelog, and can notify you when updates are available for installed apps (on Android). // 始终从最新发布的版本进行安装，突出显示其变更日志，并在已安装的应用有更新可用时通知您（在 Android 上）。 /// - Presents a polished details screen with stats, README, and developer info. // 提供精美的详细信息屏幕，包含统计数据、README 文件和开发者信息。 /// ## 🚀 Features // 🚀 功能 /// - Smart discovery // 智能发现 /// - - Home sections for “Trending”, “Recently Updated”, and “New” projects with time‑based filters. // 首页设有“热门”、“最新更新”和“最新”项目版块，并提供基于时间的筛选器。 /// - - Only repos with valid installable assets are shown. // 仅显示包含有效可安装资源的仓库。 /// - - Platform‑aware topic scoring so Android/desktop users see relevant apps first. // 基于平台的评分机制，让安卓/桌面用户优先看到相关的应用。 /// - Latest‑release installs // 最新版本安装 /// - - Fetches /releases/latest for each repo. // 获取每个仓库的 /releases/latest 。 /// - - Shows only assets from the latest release. // 仅显示最新版本中的资源。 /// - - Single “Install latest” action, plus an expandable list of all installers for that release. // 一个“安装最新版本”操作，外加一个可展开的该版本所有安装程序列表。 /// - Rich details screen // 详细信息界面 /// - - App name, version, “Install latest” button. // 应用名称、版本、“安装最新版本”按钮。 /// - - Stars, forks, open issues. // 星星、叉子、未解决的问题。 /// - - Rendered README content (“About this app”). // 渲染后的 README 内容（“关于此应用”）。 /// - - Latest release notes (body) with markdown formatting. // 最新版本说明（正文），采用 Markdown 格式。 /// - - List of installers with platform labels and file sizes. // 安装程序列表，包含平台标签和文件大小。 /// - Cross‑platform UX // 跨平台用户体验 /// - - Android: opens APK downloads with the package installer, tracks installations in a local database, and shows them in a dedicated Apps screen with update indicators. // Android：使用软件包安装程序打开 APK 下载，在本地数据库中跟踪安装情况，并在带有更新指示器的专用应用屏幕上显示它们。 /// - - Desktop (Windows/macOS/Linux): downloads installers to the user’s Downloads folder and opens them with the default handler; no hidden temp locations. // 桌面（Windows/macOS/Linux）：将安装程序下载到用户的“下载”文件夹，并使用默认处理程序打开它们；没有隐藏的临时位置。 /// - Appearance & theming // 外观和主题 /// - Material 3 design with Material 3 Expressive components on all platforms. // 所有平台均采用 Material 3 设计，并使用 Material 3 Expressive 组件。 /// - Material You dynamic color support on Android where available. // Material You 在 Android 上支持动态颜色。 /// - Optional AMOLED black mode for dark theme on OLED devices. // OLED 设备上可选的 AMOLED 黑色模式，适用于深色主题。 /// - Safety & inspection (Android) // 安全与检查（安卓） /// - Optional GitHub sign‑in via OAuth device flow for higher rate limits with minimal scopes. // 可选的 GitHub 登录方式，通过 OAuth 设备流程实现，可实现更高的速率限制和最小的范围。 /// - “Open in AppManager” action to inspect an APK’s permissions and trackers before installing. // “在 AppManager 中打开”操作可在安装前检查 APK 的权限和跟踪器。 /// ## 🔍 How does my app appear in GitHub Store? // 🔍 我的应用在 GitHub 应用商店中如何显示？ /// GitHub Store does not use any private indexing or manual curation rules. // GitHub Store 不使用任何私有索引或人工审核规则。 /// Your project can appear automatically if it follows these conditions: // 如果您的项目符合以下条件，即可自动显示： /// 1. Public repository on GitHub // GitHub 上的公共仓库 /// 1. - Visibility must be public. // 必须 public 可见。 /// 2. At least one published release // 至少有一篇已发表的文章 /// 2. - Created via GitHub Releases (not only tags). // 通过 GitHub Releases 创建（不仅限于标签）。 /// 2. - The latest release must not be a draft or prerelease. // 最新版本不能是草稿或预发布版本。 /// 3. Installable assets in the latest release // 最新版本中的可安装资源 /// 3. - The latest release must contain at least one asset file with a supported extension: // 最新版本必须包含至少一个具有受支持扩展名的资源文件： /// 3. - - Android: .apk /// 3. - - Windows: .exe, .msi /// 3. - - macOS: .dmg, .pkg /// 3. - - Linux: .deb, .rpm, .AppImage /// 3. - GitHub Store ignores GitHub’s auto‑generated source artifacts (Source code (zip) / Source code (tar.gz)). // GitHub Store 会忽略 GitHub 自动生成的源代码工件（ Source code (zip) / Source code (tar.gz) 。 /// 4. Discoverable by search / topics // 通过搜索/主题发现 /// 4. - Repositories are fetched via the public GitHub Search API. // 仓库是通过公共 GitHub 搜索 API 获取的。 /// 4. - Topic, language, and description help the ranking: // 主题、语言和描述都会影响排名： /// 4. - - Android apps: topics like android, mobile, apk. // Android 应用：诸如 android 、 mobile 、 apk 等主题。 /// 4. - - Desktop apps: topics like desktop, windows, linux, macos, compose-desktop, electron. // 桌面应用程序：主题包括 desktop 、 windows 、 linux 、 macos 、 compose-desktop 等。 electron 。 /// 4. - Having at least a few stars makes it more likely to appear under Popular/Updated/New sections. // 拥有几颗星会使其更有可能出现在“热门/更新/最新”栏目中。 /// If your repo meets these conditions, GitHub Store can find it through search and show it automatically—no manual submission required. // 如果您的仓库符合这些条件，GitHub Store 可以通过搜索找到它并自动显示——无需手动提交。 /// ## 🧭 How GitHub Store works (high‑level) // 🧭 GitHub Store 的工作原理（概述） /// 1. Search // 搜索 /// 1. - Uses GitHub’s /search/repositories endpoint with platform‑aware queries. // 使用 GitHub 的 /search/repositories 端点，并支持平台感知查询。 /// 1. - Applies simple scoring based on topics, language, and description. // 根据主题、语言和描述进行简单评分。 /// 1. - Filters out archived repos and those with too few signals. // 过滤掉已归档的仓库和信号过少的仓库。 /// 2. Release + asset check  发布 + 资产检查 /// 2. - For candidate repos, calls /repos/{owner}/{repo}/releases/latest. // 对于候选存储库，调用 /repos/{owner}/{repo}/releases/latest 。 /// 2. - Checks the assets array for platform‑specific file extensions. // 检查 assets 数组中是否存在平台特定的文件扩展名。 /// 2. - If no suitable asset is found, the repo is excluded from results. // 如果没有找到合适的资产，则将该仓库从结果中排除。 /// 3. Details screen // 详细信息 /// 3. - Repository info: name, owner, description, stars, forks, issues. // 仓库信息：名称、所有者、描述、星标数、分支数、问题数。 /// 3. - Latest release: tag, published date, body (changelog), assets. // 最新版本：标签、发布日期、正文（变更日志）、资源。 /// 3. - README: loaded from the default branch and rendered as “About this app”. // README：从默认分支加载，渲染为“关于此应用”。 /// 4. Install flow // 安装流程 /// 4. - When the user taps “Install latest”: // 当用户点击“安装最新版本”时： /// 4. - - Picks the best matching asset for the current platform. // 选择与当前平台最匹配的资产。 /// 4. - - Streams the download. // 流式传输下载。 /// 4. - - Delegates to the OS installer (APK installer on Android, default handler on desktop). // 委托给操作系统安装程序（Android 上的 APK 安装程序，桌面上的默认处理程序）。 /// 4. - - On Android, records the installation in a local database and uses package monitoring to keep the installed list in sync. // 在 Android 系统上，它会将安装过程记录在本地数据库中，并使用软件包监控来保持已安装列表的同步。 /// ## ✅ Pros / Why use GitHub Store? // ✅ 优点 / 为什么使用 GitHub Store？ /// - No more hunting through GitHub releases // 无需再在 GitHub 发布版本中苦苦寻觅 ///: See only repos that actually ship binaries for your platform. // 仅查看实际提供适用于您平台的二进制文件的仓库。 /// - Knows what you installed  // 知道你安装了什么 ///: Tracks apps installed via GitHub Store (Android) and highlights when new releases are available, so you can update them without hunting through GitHub again. // 跟踪通过 GitHub Store（Android）安装的应用，并在有新版本发布时突出显示，以便您可以更新它们而无需再次在 GitHub 上查找。 /// - Always the latest release // 始终是最新版本 ///: Installs are guaranteed to come from the latest published release; the changelog you see is exactly what you’re installing. // 保证安装的是最新发布的版本；您看到的变更日志就是您实际安装的内容。 /// - Uniform experience across platforms // 跨平台提供统一的体验 ///: Same UI and logic for Android and desktop, with platform‑native install behavior. // Android 和桌面平台采用相同的用户界面和逻辑，并具有平台原生安装行为。 /// - Open source & extensible // 开源且可扩展 ///: Written in KMP with a clear separation between networking, domain logic, and UI—easy to fork, extend, or adapt. // 采用 KMP 编写，网络、领域逻辑和 UI 之间有清晰的分离——易于 fork、扩展或改编。 /// ## 🔑 Configuration // 🔑 配置 /// GitHub Store uses a GitHub OAuth app for authentication and API rate‑limit isolation. // GitHub Store 使用 GitHub OAuth 应用进行身份验证和 API 速率限制隔离。 /// 1. Create a GitHub OAuth app at GitHub → Settings → Developer settings → OAuth Apps. // 在 GitHub → 设置 → 开发者设置 → OAuth 应用中创建 GitHub OAuth 应用。 /// 2. Set the callback URL to githubstore://callback (Not quite important). // 将回调 URL 设置为 githubstore://callback （ 不太重要 ）。 /// 3. Copy the Client ID from the OAuth app. // 从 OAuth 应用中复制客户端 ID 。 /// 4. In your project’s local.properties, add. // 在项目的 local.properties 中添加。 /// ## ⚠️ Disclaimer // ⚠️ 免责声明 /// GitHub Store only helps you discover and download release assets that are already published on GitHub by third‑party developers. // GitHub Store 仅帮助您发现和下载第三方开发者已在 GitHub 上发布的资源。 /// The contents, safety, and behavior of those downloads are entirely the responsibility of their respective authors and distributors, not this project. // 这些下载内容的安全性和行为完全由其各自的作者和分发者负责，与本项目无关。 /// By using GithubStore, you understand and agree that you install and run any downloaded software at your own risk. // 使用 GithubStore，即表示您理解并同意自行承担安装和运行任何下载软件的风险。 /// This project does not review, validate, or guarantee that any installer is safe, free of malware, or fit for any particular purpose. // 本项目不审查、验证或保证任何安装程序的安全性、无恶意软件性或适用于任何特定用途。"
[apk/f-droid]: https://f-droid.org/packages/zed.rainxch.githubstore/ "(! Anti-Features: This app promotes or depends entirely on a non-free network service) (12 MiB) (Apache-2.0) App store for GitHub releases - discover and install apps with one click // GitHub 发布版应用商店 - 一键发现并安装应用 /// Github Store is a “play store” for GitHub releases that helps you discover and install apps directly from GitHub repositories. // Github Store 是一个面向 GitHub 发行版的“应用商店”，可帮助您直接从 GitHub 存储库发现和安装应用程序。 /// The app automatically discovers repositories that ship installable binaries (APK, EXE, DMG, etc.) and lets you install the latest release with one click. // 该应用程序会自动发现提供可安装二进制文件（APK、EXE、DMG 等）的存储库，并允许您一键安装最新版本。"

