[src/gh]: https://github.com/josh-project/josh.git "(MIT) (Languages: Rust 89.7%, Go 6.2%, Shell 1.2%, Dockerfile 1.2%, CSS 0.9%, Go Template 0.3%, Other 0.5%) Git at scale platform // 大规模 Git 平台 /// Josh – “Just One Single History” – is a collection of tools and services together composing a platform for scaling out distributed development and collaboration with Git. // Josh – “Just One Single History” – 是一系列工具和服务的集合，它们共同构成了一个平台，用于扩展分布式开发和使用 Git 进行协作。 /// Our goal is to address challenges that arise when the number of people working on software within any given organization or even across multiple organizations grows. The experience we aim for is: work in a codebase of any size, with any number of contributors, without slowing down the change velocity. // 我们的目标是解决任何组织内部乃至跨组织软件开发人员数量增长时所面临的挑战。我们追求的体验是：无论代码库规模大小，无论贡献者人数多少，都能保持开发速度，而不会降低变更速度。 /// ### Repo filtering // 仓库筛选 /// One of the most common pain points software organizations encounter is the need to split their repositories into multiple per-project repos in order to address scalability, access control, and scope issues. // 软件组织遇到的最常见痛点之一是需要将代码库拆分为多个项目代码库，以解决可扩展性、访问控制和范围问题。 /// We believe that having multiple repositories is not inherently advantageous by itself, but rather is a stopgap solution driven by inefficiencies of the currently available tooling. Over the years, Git platforms like GitHub have created assumptions in the collective consciousness about what is and is not possible in Git, and to this day, this influences decisions organizations make about the way they develop software. // 我们认为，拥有多个代码仓库本身并不具有固有优势，而只是当前工具效率低下导致的权宜之计。多年来，像 GitHub 这样的 Git 平台在人们的集体意识中形成了关于 Git 功能限制的固有观念，时至今日，这些观念仍然影响着企业在软件开发方式上的决策。 /// Josh implements the core component that challenges these assumptions: fast, reversible Git history transformation enables us to not think about Git repository boundaries anymore: they disappear as soon as we can present any part of a Git repository independently, preserving history and ability to contribute. This enabled unprecedented flexibility: instead of static boundaries – which often reflect organizational boundaries and create silos – the boundaries can be decided in the moment, for any given context, thousands of times per second. // Josh 实现的核心组件挑战了这些假设：快速、可逆的 Git 历史记录转换使我们不再需要考虑 Git 仓库的边界：只要我们可以独立地呈现 Git 仓库的任何部分，同时保留历史记录和贡献能力，边界就消失了。这带来了前所未有的灵活性：不再是静态边界（通常反映组织边界并造成信息孤岛），而是可以根据任何给定的上下文，每秒数千次地即时确定边界。 /// For example, Josh enables you to represent a subfolder of your monorepo as an independent repository, limiting visibility, scope, CI rebuilds, etc. // 例如，Josh 允许您将 monorepo 的子文件夹表示为独立的存储库，从而限制可见性、范围、CI 重建等。 /// You can try this out quickly with this repo and the docs folder: // 你可以使用此仓库和 docs 文件夹快速进行尝试： /// (: git clone -- https://git.josh-project.dev/josh.git:/docs.git) /// ### Build / CI // 构建/持续集成 /// Another common use case for Josh is integration with CI and build systems. Josh offers a GraphQL API that allows you to check the state of the repository – and any partial projections within it – without actually checking out the repo. This provides a way to quickly answer the question: // Josh 的另一个常见用例是与 CI 和构建系统集成。Josh 提供了一个 GraphQL API，允许您在不实际检出代码库的情况下检查代码库的状态（以及其中的任何部分投影）。这提供了一种快速回答以下问题的方法： /// Has this code already been successfully built before? // 这段代码之前是否已经成功运行过？ /// for any subproject within the repo. // 适用于仓库中的任何子项目。"
[site/dev]: https://josh-project.dev/ "Change happens. Keep evolving. // 变化已然，当进不断。 /// Codebases see orders of magnitude more changes. Legacy tools don't scale. Josh bridges the gap between local speed and global alignment. // 代码库的变更频率呈数量级增长。传统工具无法扩展。Josh 弥合了局部速度和全局一致性之间的差距。 /// - Any Scale // 任何规模 ///: Sidestep scaling limits of Git with repository sharding. // 利用仓库分片绕过 Git 的扩展限制。 /// - Access Control // 访问控制 ///: Protect IP when working with Open Source and contractors. // 与开源软件和承包商合作时，请保护知识产权。 /// - Integration Queue // 集成队列 ///: Unblock developers and agents with asynchronous integration. // 通过异步集成，为开发者和代理商解除障碍。 /// - Scoped Builds // 范围构建 ///: Free up valueable resources by knowing what matters. Reliably. // 了解什么才是最重要的，从而释放宝贵的资源。可靠地做到。"
