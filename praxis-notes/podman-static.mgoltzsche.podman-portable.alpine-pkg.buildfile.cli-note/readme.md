[src/gh]: https://github.com/mgoltzsche/podman-static.git "(Apache-2.0) (Languages: Dockerfile 37.3%, Makefile 34.1%, Shell 28.6%) static podman binaries and container image // 静态 podman 二进制文件和容器镜像 /// This project provides alpine-based podman container images and statically linked (rootless) podman binaries for linux/amd64 and linux/arm64/v8 machines along with its dependencies (without systemd support): // 该项目提供基于 Alpine 的 podman 容器镜像和静态链接（无 root 用户）的 podman 二进制文件，适用于 linux/amd64 和 linux/arm64/v8 机器及其依赖项 （不支持 systemd） ： /// - podman (. gh: containers/podman.git) // 被征服 /// - crun (. gh: containers/crun.git) // 王冠 /// - runc (. gh: opencontainers/runc.git) // 然后 /// - conmon (. gh: containers/conmon.git) // 连接面板 /// - fuse-overlayfs (. gh: containers/fuse-overlayfs.git) and libfuse (. gh: libfuse/libfuse.git) // 结层文件系统 /// - Netavark (. gh: containers/netavark.git): container network stack and default in podman 5 or later // Netavark ：容器网络堆栈，在 podman 5 或更高版本中为默认设置。 /// - - passt/pasta (. web: passt.top/passt) /// - - aardvark-dns (. gh: containers/aardvark-dns.git) // 土豚-dns /// - catatonit (. gh: openSUSE/catatonit.git) // 容器初始化 /// CNI networking has been replaced with Netavark since Podman version 5. // 自 Podman 版本 5 起，CNI 网络已被 Netavark 取代。 /// ### OCI Dist /// Tags: /// - latest / <VERSION> ///: podman with all dependencies: runc, crun, conmon, fuse-overlayfs, netavark, pasta, aardvark-dns, catatonit. // podman 及其所有依赖项：runc、crun、conmon、fuse-overlayfs、netavark、pasta、aardvark-dns、catatonit。 /// - minimal / <VERSION>-minimal ///: podman, crun, conmon, fuse-overlayfs and netavark binaries, configured to use the host's existing namespaces (low isolation level). // podman、crun、conmon、fuse-overlayfs 和 netavark 二进制文件，配置为使用主机的现有命名空间（低隔离级别）。 /// - remote / <VERSION>-remote ///: the podman remote binary. // 远程 podman 二进制。 /// By default containers are run as user root. However the podman (uid/gid 1000) user can be used instead for which also a subuid/gid mapping is configured with the image (as described within the binary installation section below). // 默认情况下，容器以 root 用户身份运行。但是，也可以使用 podman （uid/gid 1000）用户，并且镜像中也配置了 subuid/gid 映射（如下文二进制安装部分所述）。 /// Please note that, when running non-remote podman within a docker container, the docker container needs to be --privileged. // 请注意，在 docker 容器内运行非远程 podman 时，docker 容器需要具有 --privileged 。 /// ### Bin Dist /// see releases ..."
[podman-static--podman.oci/ghcr]: https://github.com/mgoltzsche/podman-static/pkgs/container/podman-static%2Fpodman "(: docker pull -- ghcr.io/mgoltzsche/podman-static/podman:minimal)"
[podman.oci/ghcr]: https://github.com/mgoltzsche/podman-static/pkgs/container/podman "(: docker pull -- ghcr.io/mgoltzsche/podman:minimal)"
