[src/gh]: https://github.com/simonpcouch/ensure.git "(MIT) (Languages: R 100.0%) LLM-assisted unit testing for R // R 语言的 LLM 辅助单元测试 /// ensure // 确保 /// The ensure package provides an addin for drafting testthat unit testing code using LLMs. Triggering the addin will open a corresponding test file and begin writing tests into it. The ensurer is familiar with testthat 3e as well as tidy style, and incorporates context from the rest of your R package to write concise and relevant tests. /// ensure 包提供了一个插件，用于使用 LLMs 撰写 testthat 单元测试代码。触发插件将打开相应的测试文件，并开始向其中编写测试。ensurer 熟悉 testthat 3e 以及整洁风格，并结合您 R 包的其余上下文来编写简洁且相关的测试。 /// (: options(.ensure_fn = 'chat_vllm', .ensure_args = list(...)) # like `options(.xxx_chat = ellmer::chat_vllm(...))` but ensure dont use `.ensure_chat` syntax.) /// To use the ensurer, just trigger the addin (optionally selecting some code to only write tests for a certain portion of the file) and watch your testing code be written. // 要使用 ensurer，只需触发插件（可选地选择一些代码，仅针对文件的一部分编写测试），然后观察你的测试代码被自动生成。"
[site/ghio]: https://simonpcouch.github.io/ensure/ "Tips and tricks // 技巧和窍门 /// Knowing a few pieces about how ensure works will help you use it effectively: // 了解一些 ensure 的工作原理将帮助你有效地使用它： /// - The model is provided with the current contents of your test file. If you find the model is having trouble initializing objects from your package in a reasonable way, write one test in the test file yourself; many models will pattern-match based on your existing code. // 模型会获取你测试文件当前的內容。如果你发现模型在以合理的方式从你的包中初始化对象时遇到困难，你可以在测试文件中自己写一个测试；许多模型会根据你现有的代码进行模式匹配。 /// - You can test as short of selections of code as you please. Rather than testing whole source files, start off with testing single functions or even single lines of code. Generating tests, editing them to your taste, and then generating tests for new pieces code will give the model a chance to adapt to your style of testing. // 你可以随意测试尽可能短的代码片段。与其测试整个源文件，不如从测试单个函数甚至单行代码开始。生成测试、根据你的喜好编辑它们，然后为新的代码片段生成测试，将给模型一个机会来适应你的测试风格。 /// - The model is provided with the contents of your introductory vignette (based on the same rules that pkgdown uses to place a “Get Started” entry in your navbar). If knowing the basics of using your package would be helpful context for drafting unit tests, write that vignette! // 模型会获取你入门指南的內容（基于 pkgdown 将“入门”条目放置在导航栏中时所使用的相同规则）。如果了解使用你的包的基本知识对起草单元测试有帮助，请编写那个指南！"
[knows_by]: https://simonpcouch.com/software "- ensure // 确保 /// LLM-assisted unit testing for R // 使用 LLM 进行 R 语言的单元测试"

