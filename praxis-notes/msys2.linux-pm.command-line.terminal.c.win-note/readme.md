[runtime.src/gh]: https://github.com/msys2/msys2-runtime.git "(GPL-2.0, LGPL-2.1, BSD) (Languages: C 65.5%, Makefile 17.1%, C++ 9.3%, Assembly 4.2%, Linker Script 1.0%, M4 0.9%, Other 2.0%) Our friendly fork of Cygwin 💖 https://cygwin.org 💖 see the wiki for details // 我们友好地修改了 Cygwin 的一个分支 💖 https://cygwin.org 💖 详情请参阅 wiki。"
[site.src/gh]: https://github.com/msys2/msys2.github.io.git "(CC-BY-SA-4.0, CC0-1.0, BSD-3-Clause) (Languages: CSS 41.7%, Python 27.3%, XSLT 16.0%, HTML 8.4%, Shell 6.6%) The MSYS2 homepage // MSYS2 主页 /// This website is written in Markdown and gets built to a static website using mkdocs and a modified version of the mkdocs-material theme. Any new commits to the main branch will auto deploy to GitHub pages using a GitHub action. The resulting website is reachable under https://msys2.github.io and https://www.msys2.org. Changes to the main branch usually take a minute or two until they are live. // 本网站使用 Markdown 编写，并构建成静态网站。 mkdocs 和 mkdocs-material 主题的修改版本。任何新的提交都会影响到 mkdocs。 main 分支将自动部署到 GitHub Pages 这是一个 GitHub Action 。生成的网站可通过 https://msys2.github.io 和 https://www.msys2.org 访问。对 main 分支的更改通常需要一两分钟才能生效。"
[site/org]: https://msys2.org/ "MSYS2 is a collection of tools and libraries providing you with an easy-to-use environment for building, installing and running native Windows software. // MSYS2 是一套工具和库的集合，为您提供一个易于使用的环境，用于构建、安装和运行原生 Windows 软件。 /// It consists of a command line terminal called mintty, bash, version control systems like git and subversion, tools like tar and awk and even build systems like autotools, all based on a modified version of Cygwin. Despite some of these central parts being based on Cygwin, the main focus of MSYS2 is to provide a build environment for native Windows software and the Cygwin-using parts are kept at a minimum. MSYS2 provides up-to-date native builds for GCC, mingw-w64, CPython, CMake, Meson, OpenSSL, FFmpeg, Rust, Ruby, just to name a few. // 它由一个名为命令行终端的组件构成。 mintty 、bash、git 和 subversion 等版本控制系统、tar 和 awk 等工具，甚至 autotools 等构建系统，所有这些都基于修改版的 Cygwin 。尽管其中一些核心组件基于 Cygwin，但 MSYS2 的主要目标是为原生 Windows 软件提供构建环境，因此使用 Cygwin 的部分被控制在最低限度。MSYS2 为 GCC、mingw-w64、CPython、CMake、Meson、OpenSSL、FFmpeg、Rust 和 Ruby 等众多工具提供最新的原生构建版本。 /// To provide easy installation of packages and a way to keep them updated it features a package management system called Pacman, which should be familiar to Arch Linux users. It brings many powerful features such as dependency resolution and simple complete system upgrades, as well as straight-forward and reproducible package building. Our package repository contains more than 3500 pre-built packages ready to install. // 为了方便安装软件包并提供更新方式，它 它配备了一个名为“软件包管理系统”的软件包管理系统。 Pacman 对于 Arch Linux 用户来说应该很熟悉。它提供了许多强大的功能，例如依赖关系解析、简便的系统升级以及简单易用的软件包构建。我们的软件包仓库包含超过 3500 个预构建的软件包， 可直接安装。 /// For more details see 'What is MSYS2?' which also compares MSYS2 to other software distributions and development environments like Cygwin, WSL, Chocolatey, Scoop, ... and 'Who Is Using MSYS2?' to see which projects are using MSYS2 and what for. // 更多详情请参阅 “什么是 MSYS2？” 将 MSYS2 与其他软件发行版和开发环境进行比较，例如 Cygwin ， WSL ， Chocolatey 、 Scoop 等，以及 “谁在使用 MSYS2？”， 以查看哪些项目正在使用 MSYS2 以及其用途。"
[mastodon]: https://fosstodon.org/@msys2org "@msys2org@fosstodon.org"
[bluesky]: https://bsky.app/profile/msys2org.bsky.social "@msys2org.bsky.social"
[discord]: https://discord.gg/jPQdRdDcT9

