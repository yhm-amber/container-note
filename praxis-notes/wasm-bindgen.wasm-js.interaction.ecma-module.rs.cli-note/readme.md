[src/gh]: https://github.com/wasm-bindgen/wasm-bindgen.git "(Apache-2.0, MIT) (Languages: Rust 95.8%, JavaScript 2.9%, WebAssembly 1.1%, HTML 0.1%, TypeScript 0.1%, Shell 0.0%) Facilitating high-level interactions between Wasm modules and JavaScript // 促进 Wasm 模块和 JavaScript 之间的高级交互 /// Features // 特征 /// Lightweight. Only pay for what you use. wasm-bindgen only generates bindings and glue for the JavaScript imports you actually use and Rust functionality that you export. For example, importing and using the document.querySelector method doesn't cause Node.prototype.appendChild or window.alert to be included in the bindings as well. // 轻量级。 只为使用的功能付费。wasm wasm-bindgen 仅生成 用于实际使用的 JavaScript 导入的绑定和粘合，以及 Rust 您导出的功能。例如，导入和使用 document.querySelector 方法不会导致 Node.prototype.appendChild 或 window.alert 也需要包含在绑定中。 /// ECMAScript modules. Just import WebAssembly modules the same way you would import JavaScript modules. Future compatible with WebAssembly modules and ECMAScript modules integration. // ECMAScript 模块。 只需像导入 JavaScript 模块一样导入 WebAssembly 模块即可。未来将兼容 WebAssembly 模块和 ECMAScript 模块集成 。 /// Designed with the “Web IDL bindings” proposal in mind. Eventually, there won't be any JavaScript shims between Rust-generated wasm functions and native DOM methods. Because the Wasm functions are statically type checked, some of those native methods' dynamic type checks should become unnecessary, promising to unlock even-faster-than-JavaScript DOM access. // 设计时充分考虑了 “Web IDL 绑定”提案 。 最终，Rust 生成的 wasm 代码之间将不再需要任何 JavaScript 垫片。 函数和原生 DOM 方法。因为 Wasm 函数是静态的 类型检查之后，一些原生方法的动态类型检查应该会变成 没有必要，但承诺解锁比 JavaScript 更快的 DOM 访问速度。"
[book/ghio]: https://wasm-bindgen.github.io/wasm-bindgen/ "Introduction // 介绍 /// This book is about wasm-bindgen, a Rust library and CLI tool that facilitate high-level interactions between Wasm modules and JavaScript. The wasm-bindgen tool and crate are only one part of the Rust and WebAssembly ecosystem. If you're not familiar already with wasm-bindgen it's recommended to start by reading the Game of Life tutorial. If you're curious about wasm-pack, you can find that documentation here. // 本书介绍 wasm-bindgen wasm-bindgen 这是一个 Rust 库和命令行工具，它促进了 Wasm 模块和 JavaScript 之间的高级交互。 工具和 crate 只是 Rust 和 WebAssembly 生态系统的一部分。如果您还不熟悉 wasm-bindgen 建议您先阅读生命游戏教程 。如果您对 wasm-pack 感兴趣，可以在这里找到相关文档 。 /// The wasm-bindgen tool is sort of half polyfill for features like the component model proposal and half features for empowering high-level interactions between JS and wasm-compiled code (currently mostly from Rust). More specifically this project allows JS/wasm to communicate with strings, JS objects, classes, etc, as opposed to purely integers and floats. Using wasm-bindgen for example you can define a JS class in Rust or take a string from JS or return one. The functionality is growing as well! // wasm-bindgen 工具有点像是某些功能的半个 polyfill，例如： 组件模型提案和半功能，以增强高级功能 JS 与 wasm 编译代码（目前主要来自 Rust）之间的交互。 更具体地说，这个项目允许 JS/wasm 与字符串进行通信。 对象、类等，而不是纯粹的整数和浮点数。使用 例如， wasm-bindgen 允许你在 Rust 中定义一个 JS 类，或者从 JS 中获取字符串，或者返回一个字符串。它的功能也在不断增强！ /// Currently this tool is Rust-focused but the underlying foundation is language-independent, and it's hoping that over time as this tool stabilizes that it can be used for languages like C/C++! // 目前该工具主要针对 Rust，但其底层基础与语言无关，希望随着该工具的稳定发展，未来能够用于 C/C++ 等语言！"
[lib.cargo/crates]: https://crates.io/crates/wasm-bindgen "(: cargo add -- wasm-bindgen) (MIT OR Apache-2.0) (3.4K SLoC) (47.3 KiB) Easy support for interacting between JS and Rust. // 轻松支持 JS 和 Rust 之间的交互。 (src: gh:wasm-bindgen/wasm-bindgen.git)"
[cli.cargo/crates]: https://crates.io/crates/wasm-bindgen-cli "(: cargo install -- wasm-bindgen-cli) (MIT OR Apache-2.0) (2.0K SLoC) (43.3 KiB) Command line interface of the `#[wasm_bindgen]` attribute and project // 属性 `#[wasm_bindgen]` 和此项目的命令行界面。 (src: gh:wasm-bindgen/wasm-bindgen.git)"
[knowsby]: https://github.com/alexkehayias/woz.git "... /// Before we begin you must have a recent version of Rust installed as well as wasm-bindgen. // 在开始之前，您必须安装最新版本的 Rust 以及 wasm-bindgen 。 /// ..."

