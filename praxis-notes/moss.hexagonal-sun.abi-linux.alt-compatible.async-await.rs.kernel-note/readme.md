[src/gh]: https://github.com/hexagonal-sun/moss.git "(MIT) (Languages: Rust 98.8%, Other 1.2%) Rust Linux-compatible kernel // Rust Linux 兼容内核 /// moss is a Unix-like, Linux-compatible kernel written in Rust and Aarch64 assembly. // moss 是一个用 Rust 和 Aarch64 汇编语言编写的类 Unix、兼容 Linux 的内核。 /// It features a modern, asynchronous core, a modular architecture abstraction layer, and binary compatibility with Linux userspace applications (currently capable of running most BusyBox commands). // 它具有现代化的异步核心、模块化架构抽象层，并与 Linux 用户空间应用程序二进制兼容（目前能够运行大多数 BusyBox 命令）。 /// ## Features // 特性 /// ### Architecture & Memory // 架构与内存 /// - Full support for aarch64. // 完全支持 aarch64。 /// - A well-defined HAL allowing for easy porting to other architectures (e.g., x86_64, RISC-V). // 一个定义良好的 HAL，便于移植到其他架构（例如，x86_64、RISC-V）。 /// - Memory Management: // 内存管理： /// - - Full MMU enablement and page table management. // 完整的 MMU 启用和页表管理。 /// - - Copy-on-Write (CoW) pages. // 写时复制（CoW）页面。 /// - - Safe copy to/from userspace async functions. // 安全地在用户空间和内核空间之间复制异步函数。 /// - - Kernel and userspace page fault management. // 内核和用户空间的页面错误管理。 /// - - Buddy allocator for physical addresses and smalloc for boot allocations and tracking memory reservations. // 物理地址的伙伴系统分配器，以及用于引导分配和跟踪内存保留的 smalloc 。 /// ### Async Core // 异步核心 /// One of the defining features of moss is its usage of Rust's async/await model within the kernel context: // moss 的一个显著特点是其在内核环境中的使用 Rust 的 async/await 模型： /// - All non-trivial system calls are written as async functions, sleep-able functions are prefixed with .await. // 所有非平凡的系统调用都编写为 async 函数，可休眠的函数以 .await 作为前缀。 /// - The compiler enforces that spinlocks cannot be held over sleep points, eliminating a common class of kernel deadlocks. // 编译器强制执行旋转锁不能在休眠点持有，消除了常见的内核死锁类型。 /// ### Process Management // 进程管理 /// - Full task management including scheduling and task migration via IPIs. // 完整的任务管理，包括通过 IPI 进行调度和任务迁移。 /// - Currently implements 49 Linux syscalls; sufficient to execute most BusyBox commands. // 目前实现了 49 个 Linux 系统调用；足以执行大多数 BusyBox 命令。 /// - Advanced forking capabilities via clone(). // 通过 clone() 实现高级的进程分离功能。 /// - Process and thread signal delivery and raising support. // 支持进程和线程信号传递和触发。 /// ### VFS & Filesystems // VFS & 文件系统 /// - Virtual File System with full async abstractions. // 具有完整异步抽象的虚拟文件系统。 /// - Drivers: // 驱动程序： /// - - Ramdisk block device implementation. // Ramdisk 块设备实现。 /// - - FAT32 filesystem driver (ro). // FAT32 文件系统驱动（只读）。 /// - - devtmpfs driver for kernel character device access. // devtmpfs 用于内核字符设备访问的驱动。"

