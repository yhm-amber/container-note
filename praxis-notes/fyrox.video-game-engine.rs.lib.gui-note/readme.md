[book/ghio]: https://fyrox-book.github.io/ "Practical reference and user guides for Fyrox Game Engine and its editor FyroxEd. // Fyrox 游戏引擎及其编辑器 FyroxEd 的实用参考和用户指南。 /// Almost every chapter in this book can be read in any order, but we recommend reading Chapters 1, 2, 3 (they're quite small) and then going through Platformer Tutorial (2D) while learning more about specific areas that interest you from the other chapters. There is also a First-Person Shooter Tutorial (3D) and RPG Tutorial (3D). // 本书中的几乎所有章节都可以按任意顺序阅读，但我们建议先阅读第 1、2、3 章（它们相当短），然后在学习其他章节中感兴趣的具体领域时，通过平台跳跃教程（2D）进行学习。还有一个第一人称射击教程（3D）和角色扮演游戏教程（3D）。"
[book.src/gh]: https://github.com/fyrox-book/fyrox-book.github.io.git "(Languages: Rust 98.4%, Other 1.6%) A book for Fyrox Game Engine // Fyrox 游戏引擎的书籍 /// Every code snippet is tested for compilation, please run cd src/code/snippets && cargo check. // 每个代码片段都经过编译测试，可运行 cd src/code/snippets && cargo check"
[site]: https://fyrox.rs/ "Fyrox Game Engine // Fyrox 游戏引擎 /// Feature-rich Game Engine Written in Rust // 用 Rust 编写的功能丰富的游戏引擎 /// Features // 功能 /// - Editor // 编辑器 ///: Use the full-featured editor to develop games from start to finish. // 使用功能丰富的编辑器从开始到结束开发游戏。 /// - Hot Reloading // 热重载 ///: Use hot reloading of native code and assets for rapid prototyping. // 使用原生代码和资源的热重载功能进行快速原型设计。 /// - First Class 2D/3D Support // 一流 2D/3D 支持 ///: Create 2D or 3D games, or even mix 2D and 3D. // 创建 2D 或 3D 游戏，甚至混合 2D 和 3D。 /// - Flexible PBR Renderer // 灵活的 PBR 渲染器 ///: Powerful and extensible PBR renderer will cover most of your needs. // 强大且可扩展的 PBR 渲染器将满足你大部分需求。 /// - High Quality Audio System // 高品质音频系统 ///: Use powerful sound system to create rich sound environment. // 使用强大的音效系统创建丰富的声音环境。 /// - Powerful 2D/3D Physics // 强大的 2D/3D 物理引擎 ///: Rigid bodies, various colliders, joints, ray casting, etc. // 刚体、各种碰撞体、关节、射线投射等。 /// - Advanced User Interface // 高级用户界面 ///: Use powerful UI system to create interfaces limited by your imagination. // 使用强大的 UI 系统创建由你想象限制的界面。 /// - Powerful Animation System // 强大的动画系统 ///: Use built-in animation system to create complex animations. // 使用内置动画系统创建复杂动画。 /// - Multiplatform // 跨平台 ///: Make your games for PC (Windows, Linux, macOS), Web (WebAssembly), Mobile (Android and iOS). // 为 PC（Windows、Linux、macOS）、Web（WebAssembly）、移动设备（Android 和 iOS）制作游戏。 /// - Plugins & Scripting // 插件与脚本 ///: Write your game completely in Rust, while being able to run it in the editor. Extend functionality of the engine using custom plugins. // 完全使用 Rust 编写游戏，同时可以在编辑器中运行。通过自定义插件扩展引擎功能。 /// - Scene Graph // 场景图 ///: Full-featured, with various nodes (pivot, camera, mesh, light, particle system, sprite), including your own. // 功能丰富，包含各种节点（枢轴、相机、网格、光源、粒子系统、精灵），也包括您自己的。 /// - Documentation // 文档 ///: Read high-quality documentation to learn how to use the engine. // 阅读高质量的文档来学习如何使用引擎。"
[demos/site]: https://fyrox.rs/examples.html "Demo Projects // Demo 项目 /// Platformer // 平台跳跃游戏 ///: 2D Platformer // 2D 平台跳跃游戏 /// Blend shapes // 混合形状 ///: This project shows how to use blend shapes to create facial expressions for your game characters. Use the set of sliders to control each blend shape's weight. // 这个项目展示了如何使用形变动画为游戏角色创建面部表情。使用滑块组来控制每个形变动画的权重。 /// Sound // 声音 ///: This project shows sound capabilities of the engine - HRTF, reverb, audio buses. // 这个项目展示了引擎的声音功能 - HRTF、混响、音频总线。 /// Animation // 动画 ///: This project shows parts of the animation system of the engine, such as animation blending, root motion, etc. // 这个项目展示了引擎动画系统的部分内容，例如动画混合、根运动等。 /// User Interface // 用户界面 ///: This project is a showcase for the user interface system of the engine. // 这个项目是引擎用户界面系统的展示案例。 /// Fish Folly // 鱼之愚 ///: FallGuys-like 3D platformer. // 类似 FallGuys 的 3D 平台游戏。"
[demos.src/gh]: https://github.com/FyroxEngine/Fyrox-demo-projects.git "(MIT) (Languages: Rust 89.8%, CSS 4.4%, HTML 2.9%, JavaScript 2.9%) A set of demo projects for the Fyrox Game Engine. // Fyrox 游戏引擎的一套演示项目。 /// A set of demo projects for the Fyrox Game Engine. Most of the projects located in this repository in the respective folders. Navigate to a folder, and execute one of the following commands there: // 一套适用于 Fyrox 游戏引擎的演示项目。本仓库中的大多数项目都位于相应的文件夹中。导航到某个文件夹，并执行以下命令之一： /// - To run a game: cargo run --package executor --release // 要运行游戏： cargo run --package executor --release /// - To run an editor: cargo run --package editor --release // 运行编辑器： cargo run --package editor --release /// Large Games // 大型游戏 ///: There are some large game projects that developed by the community: // 社区开发了一些大型游戏项目： /// - Station Iapetus - a sci-fi, third-person 3D shooter, the largest game made with the engine by now. // Station Iapetus - 一款科幻、第三人称 3D 射击游戏，目前使用该引擎制作的最大游戏。 /// - Fish Folly - FallGuys-like platformer. // 鱼之愚行 - 类似 FallGuys 的平台跳跃游戏。 /// - RustCycles - A fast multiplayer shooter on wheels. // RustCycles - 轮式快速多人射击游戏。 /// - rusty shooter - Quake3-like shooter, currently non-maintained. // rusty shooter - 类似 Quake3 的射击游戏，目前已不再维护。"
[src/gh]: https://github.com/FyroxEngine/Fyrox.git "(MIT) (Languages: Rust 97.6%, GLSL 2.4%) 3D and 2D game engine written in Rust // 用 Rust 编写的 3D 和 2D 游戏引擎 /// Fyrox - a modern Rust game engine // Fyrox - 一个现代的 Rust 游戏引擎 /// A feature-rich, production-ready, general purpose 2D/3D game engine written in Rust with a scene editor. Formerly known as rg3d // 一个功能丰富、可用于生产、通用的 2D/3D 游戏引擎，使用 Rust 编写，并带有场景编辑器。曾被称为 rg3d /// "
[main-lib.cargo/crates]: https://crates.io/crates/fyrox "(: cargo add -- fyrox) (MIT) (10 SLoC) (29.9 KiB) A feature-rich, production-ready, general purpose 2D/3D game engine written in Rust with a scene editor. Formerly known as rg3d // 一个功能丰富、可用于生产、通用的 2D/3D 游戏引擎，使用 Rust 编写，并带有场景编辑器。曾被称为 rg3d"
[main~dylib-lib.cargo/crates]: https://crates.io/crates/fyrox-dylib "(: cargo add -- fyrox-dylib) (MIT) (1 SLoC) (27.9 KiB) Dynamic library for Fyrox Game Engine. // Fyrox 游戏引擎的动态库。 (这就是那一行代码 ->) (::: pub use fyrox_impl::*; // Just re-export everything.)"
[main~impl-lib.cargo/crates]: https://crates.io/crates/fyrox-impl "(: cargo add -- fyrox-impl) (MIT) (58K SLoC) (1.34 MiB) (这里是真正的实现，像 {crates/fyrox} 也是直接 pub 这个的东西 -- 所以它代码量也少。别的库也可能有 pub 它或者用它的情况。) (在 {crates/fyrox} 里根据 feature 是不是 dylib 决定用 impl 还是 dylib)"
[tmpl-lib.cargo/crates]: https://crates.io/crates/fyrox-template-core "(: cargo add -- fyrox-template-core) (MIT) (886 SLoC) (19.2 KiB) (这里应该是 {crates/fyrox-template} 的真正功能逻辑的实现 -- 那个 cli 就只有一个 main.rs 而已且也没几行。)"
[tmpl-cli.cargo/crates]: https://crates.io/crates/fyrox-template "(cargo install -- fyrox-template) (MIT) (68 SLoC) (6.85 KiB) Fyrox Engine Template Generator // Fyrox 引擎模板生成器 /// This tiny utility handles project and script generation for Fyrox Game Engine. // 这个小程序处理 Fyrox 游戏引擎的项目和脚本生成。"
[ed-cli.cargo/crates]: https://crates.io/crates/fyroxed "(: cargo install -- fyroxed) (MIT) (7 SLoC) (20.1 KiB) FyroxEd (standalone) // FyroxEd (独立版) /// WARNING: Standalone version of the editor is not supported, use project template generator to utilize the full power of the editor. Standalone version does not support plugins and scripts, it won't be update in next releases! // 警告：编辑器的独立版本不受支持，请使用项目模板生成器来发挥编辑器的全部功能。独立版本不支持插件和脚本，它将在未来的版本中不再更新！ /// A standalone version of FyroxEd - native editor of Fyrox engine. The standalone version allows you only to create and edit scenes, but not run your game in the editor. Please see the book to learn how to use the editor in different ways. // FyroxEd 的独立版本——Fyrox 引擎的原生编辑器。独立版本仅允许你创建和编辑场景，但不能在编辑器中运行你的游戏。请参阅书籍，了解如何以不同的方式使用编辑器。"
[ed-lib.cargo/crates]: https://crates.io/crates/fyroxed_base "(: cargo add -- fyroxed_base) (MIT) (60K SLoC) (668 KiB) Full-featured editor for Fyrox engine. This is the basic library for the editor, it cannot be run as executable. See editor-standalone for this. // Fyrox 引擎的完整功能编辑器。这是编辑器的基本库，不能作为可执行文件运行。参见 editor-standalone 了解详情。 /// The main purpose of making the editor library is to use it with static plugins, see scripting example for more info. // 制作编辑器库的主要目的是与静态插件一起使用，参见 scripting 示例获取更多信息。"
[ui-lib.cargo/crates]: https://crates.io/crates/fyrox-ui "(: cargo add -- fyrox-ui) (MIT) (37K SLoC) (379 KiB) Retained mode, general purpose, graphics API agnostic user interface library. Inspired by WPF. // 保留模式，通用，与图形 API 无关的用户界面库。受 WPF 启发。 /// NOTE: even though this crate has fyrox prefix in its name, it can be used separately without any issues. // 注意：尽管这个 crates.io 包的名称中带有 fyrox 前缀，但可以单独使用且没有任何问题。 /// Features // 特性 ///: - More than 28 widgets // 超过 28 个组件 /// - Full TTF/OTF fonts support // 完整的 TTF/OTF 字体支持 /// - Powerful layout system // 强大的布局系统 /// - Fully customizable - you can construct visual trees of any complexity: for example a tree view item can have any sub-widgets as contents. // 完全可定制 - 你可以构建任何复杂度的视觉树：例如，一个树形视图项可以包含任何子组件作为内容。 /// - GAPI-agnostic - this crate does not know anything about the rendering backend: it can be OpenGL, DirectX, Vulkan, Metal, or even built-in OS drawing API. // 与 GAPI 无关 - 这个 crates 不知道任何渲染后端：它可以是 OpenGL、DirectX、Vulkan、Metal，甚至内置的操作系统绘图 API。 /// - OS-agnostic - similar look of all widgets across all operating systems and window managers. // 与操作系统无关 - 在所有操作系统和窗口管理器中，所有小部件的外观都相似。 /// - Extendable - full support of user-defined widgets. // 可扩展 - 完全支持用户定义的小部件。 /// Widgets // 小部件 ///: - Button // 按钮 /// - Border // 边框 /// - Canvas // 画布 /// - Color picker // 颜色选择器 /// - Color field // 颜色字段 /// - Check box // 复选框 /// - Decorator // 装饰器 /// - Drop-down list // 下拉列表 /// - Grid // 网格 /// - Image // 图片 /// - List view // 列表视图 /// - Popup // 弹出窗口 /// - Progress bar // 进度条 /// - Scroll bar // 滚动条 /// - Scroll panel // 滚动面板 /// - Scroll viewer // 滚动查看器 /// - Stack panel // 堆叠面板 /// - Tab control // 选项卡控件 /// - Text // 文本 /// - Text box // 文本框 /// - Tree // 树 /// - Window // 窗口 /// - File browser // 文件浏览器 /// - File selector // 文件选择器 /// - Docking manager // 停靠管理器 /// - NumericUpDown // 数值输入框 /// - Vector2/Vector3/Vector4 editor // 二维向量/三维向量/四维向量编辑器 /// - Quaternion editor // 四元数编辑器 /// - Menu // 菜单 /// - Menu item // 菜单项 /// - Message box // 消息框 /// - Wrap panel // 包装面板 /// - Curve editor // 曲线编辑器 /// - Bit Field // 位字段 /// - User defined widget // 用户自定义组件 /// - Inspector // 检查器 /// Limitations // 限制 ///: - Since this library is OS-, GAPI-agnostic it cannot create native OS' windows, and it cannot render anything on screen. Instead, it uses an internal draw buffer which holds a list of commands, which has to be interpreted in your game/app. This is very a flexible way, but it has some limitations: multiwindow (native) configuration is hard to implement, you have to implement your own UI renderer what can be difficult if you not familiar with anything like this. // 由于这个库是操作系统和 GAPI 无关的，它不能创建原生操作系统窗口，也不能在屏幕上渲染任何内容。相反，它使用一个内部绘制缓冲区，该缓冲区包含一系列命令，这些命令需要在你的游戏/应用程序中进行解释。这是一种非常灵活的方式，但它也有一些限制：多窗口（原生）配置难以实现，你必须实现自己的 UI 渲染器，如果你不熟悉这类东西，这可能会很困难。 /// - There is still no keyboard navigation, it is planned but not with high priority. // 目前还没有键盘导航功能，这是计划中的功能，但优先级不高。 /// - No support for right-to-left text (arabic, hebrew, etc.) // 不支持从右到左的文本（阿拉伯语、希伯来语等） /// Performance // 性能 ///: - In general fyrox-ui is fast, however it can be slow if used incorrectly. Since this library uses a very complex layout system, it may work slow if there are lots of ui elements being moved (i.e. when scrolling). Hopefully it has built-in layout caching system, and it relies on layout invalidation, so it won't do layout calculations each frame - only if something significant changed (position, size, etc.). // 一般来说 fyrox-ui 运行很快，但如果使用不当可能会变慢。由于这个库使用了一个非常复杂的布局系统，当有很多 UI 元素被移动时（例如滚动时）可能会运行缓慢。希望它内置了布局缓存系统，并且依赖于布局失效机制，因此它不会每帧都进行布局计算——只有在有重要变化（如位置、大小等）时才会进行。 /// - Batching of render commands can be difficult, because this library extensively uses clipping, and each clipping geometry has to be drawn into the stencil buffer as separate draw call. Rendering still has to be optimized, it is inefficient for now. // 批量处理渲染命令可能很困难，因为该库大量使用裁剪功能，而每个裁剪几何体都必须作为单独的绘制调用绘制到模板缓冲区中。渲染仍然需要优化，目前效率不高。 /// Styling // 样式 ///: fyrox-ui uses a bit unusual way of styling - you have to replace entire sub-graphs of widget's visual trees. What does that mean? fyrox-ui uses graph to build visual trees of any complexity, each widget is a set of nodes in the graph. For example a button is a set of background and foreground widgets, background widget usually defines appearance and foreground shows a content. Content of a button can be any widget, in most common cases it is either a text or an image. So to change appearance of a button you have to define your own background widget at the building stage, by default fyrox-ui uses Decorator widget which just changes its foreground brush when it receives MouseEnter, MouseLeave, etc. message. This fact significantly complicates minor styling (like change a color), but it is super flexible approach and allows to build your own unique style. Most of widget builders provides a way to change its parts, some of them still may lack such functionality, but this should eventually be fixed. // fyrox-ui 采用了一种不太常见的方式来设置样式——你需要替换整个小型的部件视觉树。这意味着什么？fyrox-ui 使用图来构建任何复杂度的视觉树，每个部件都是图中的一组节点。例如，一个按钮是一组背景和前景部件，背景部件通常定义外观，前景部件显示内容。按钮的内容可以是任何部件，在大多数常见情况下，它要么是文本，要么是图像。因此，要改变按钮的外观，你必须在构建阶段定义自己的背景部件，默认情况下，fyrox-ui 使用 Decorator 部件，它只是在接收到 MouseEnter、MouseLeave 等消息时改变其前景画刷。这一事实显著增加了微小的样式更改（如更改颜色）的复杂性，但它是一种超级灵活的方法，并允许构建你自己的独特风格。大多数部件构建器都提供更改其部分的方法，其中一些可能仍然缺少此功能，但这种情况最终应该会被修复。"
[algo-lib.cargo/crates]: https://crates.io/crates/fyrox-core "(: cargo add -- fyrox-core) (MIT) (9.9K SLoC) (84.9 KiB) Shared library for Fyrox-engine containing linear algebra and some useful algorithms. // Fyrox 引擎的共享库，包含线性代数和一些有用的算法。 /// NOTE: even though this crate has fyrox prefix in its name, it can be used separately without any issues. // 注意：尽管这个库在名称中带有 fyrox 前缀，但可以单独使用而不会出现任何问题。"
[derive-lib.cargo/crates]: https://crates.io/crates/fyrox-core-derive "(: cargo add -- fyrox-core-derive) (MIT) (1,340 SLoC) (14.8 KiB) This crate provides with some derive macros: // 这个包提供了一些派生宏： /// - #[derive(Visit)] /// - #[derive(Inspect)] /// - #[derive(Reflect)]"
[animation-lib.cargo/crates]: https://crates.io/crates/fyrox-animation "(: cargo add -- fyrox-animation) (MIT) (4.5K SLoC) (58.8 KiB) Flexible, general-purpose, reflection-based animation system for game development. // 灵活、通用的基于反射的动画系统，用于游戏开发。 /// NOTE: even though this crate has fyrox prefix in its name, it can be used separately without any issues. // 注意：尽管这个 crates.io 包的名称中带有 fyrox 前缀，但它可以单独使用而不会出现任何问题。 /// Features // 特性 ///: - Reflection-based animation. // 基于反射的动画。 /// - Animation blending state machine. // 动画混合状态机。 /// - Sprite sheet animation. // 精灵表动画。 /// - Various blending modes - blend by weight, blend by index, blend space. // 多种混合模式 - 按权重混合、按索引混合、混合空间。 /// - Smooth state transition. // 平滑状态转换。 /// - Ability to animate any numeric property, including vectors, bools, natural and real numbers. // 能够对任何数值属性进行动画处理，包括向量、布尔值、自然数和实数。 /// - Layer masks. // 图层蒙版。 /// - Root motion. // 根运动。 /// - Fixed set of binding modes for most common cases. // 为大多数常见情况提供固定的绑定模式。 /// - Signals and events. // 信号和事件。"
[sound-lib.cargo/crates]: https://crates.io/crates/fyrox-sound "(: cargo add -- fyrox-sound) (MIT) (3.1K SLoC) (1.46 MiB) Sound library for games and interactive applications written in Rust. // 用 Rust 编写的游戏和交互式应用程序的声音库。 /// NOTE: even though this crate has fyrox prefix in its name, it can be used separately without any issues. // 注意：尽管这个 crates.io 中的包在名称中带有 fyrox 前缀，但可以单独使用而不会出现任何问题。 /// Key features // 主要特性 ///: - Generic and Spatial sound sources. // 通用和空间声音源。 /// - Built-in streaming for large sounds. // 内置流式传输用于大型声音。 /// - Raw samples playback support. // 原生样本播放支持。 /// - WAV format support (non-compressed). // WAV 格式支持（非压缩）。 /// - Vorbis/ogg support (using lewton). // Vorbis/ogg 支持（使用 lewton）。 /// - HRTF support for excellent positioning and binaural effects. // HRTF 支持，实现出色的定位和双耳效果。 /// - Reverb effect. // 混响效果。 /// Examples // 示例 ///: Examples can be found in ./examples. Make sure you run examples with --release flag, debug version is very slow and may cause tearing of output sound. // 示例可以在 ./examples 中找到。确保使用 --release 标志运行示例， debug 版本非常慢，可能会导致输出声音撕裂。 /// Supported OS // 支持的操作系统 ///: - Windows (DirectSound) /// - Linux (alsa) /// - macOS (CoreAudio) /// - WebAssembly (WebAudio) /// - Android (AAudio, API Level 26+) /// HRTF ///: This library has full HRTF support, it uses HRIR spheres generated using IRCAM HRIR database. HRIR spheres are produced using a small tool written in C++ - hrir_sphere_builder. It is very important to find HRTF that suits you because they're very individual and the overall perception is fully defined by the use of correct HRTF. // 该库支持完整的 HRTF 功能，它使用 IRCAM HRIR 数据库生成的 HRIR 球体。HRIR 球体是通过一个用 C++编写的工具——hrir_sphere_builder 生成的。找到适合你的 HRTF 非常重要，因为它们非常个性化，整体感知完全由正确使用 HRTF 来定义。 /// References // 参考文献 ///: This library wouldn't have been ever created without work of these people. Thank you all! // 没有这些人的工作，这个库永远不可能被创建。谢谢大家！ /// - Digital signal processing and filters (. https://ccrma.stanford.edu/~jos/filters/) // 数字信号处理和滤波器 /// - Physical Audio Signal Processing (. https://ccrma.stanford.edu/~jos/pasp/) // 物理音频信号处理 /// - Hannes Gamper, \"Head-related transfer function interpolation in azimuth, elevation, and distance\", The Journal of the Acoustical Society of America 134, EL547 (2013); doi: 10.1121/1.4828983 // Hannes Gamper, \"头相关传递函数在方位角、仰角和距离上的插值\", 美国声学学会杂志 134, EL547 (2013); doi: 10.1121/1.4828983 /// - Fábio P. Freeland, Luiz W. P. Biscainho, Paulo S. R. Diniz, \"Interpolation of Head-related transfer function (HRTFS): A Multi-source approarch\" // Fábio P. Freeland, Luiz W. P. Biscainho, Paulo S. R. Diniz, \"头相关传递函数 (HRTF) 的插值：一种多源方法\" /// - IRCAM Head-related impulse response database (. http://recherche.ircam.fr/equipes/salles/listen/) // IRCAM 头相关脉冲响应数据库 /// - Reverb (. https://ccrma.stanford.edu/~jos/pasp/Freeverb.html) // 混响 /// - Overlap-add convolution (. https://en.wikipedia.org/wiki/Overlap%E2%80%93add_method) - not used anymore due to significant distortions at segment boundary when impulse response changes. // 重叠相加卷积 - 由于脉冲响应变化时在分段边界处产生显著失真，因此不再使用。 /// - Overlap-save convolution (. https://dsp-nbsphinx.readthedocs.io/en/nbsphinx-experiment/nonrecursive_filters/segmented_convolution.html) - works much better when impulse response changes, there are only phase shift issues which are more or less acceptable. // 重叠保存卷积 - 当脉冲响应变化时效果更好，只有相位偏移问题，这些问题或多或少是可以接受的。 /// - OpenAL Specification (. https://openal.org/documentation/openal-1.1-specification.pdf) - distance models and general design considerations. // OpenAL 规范 - 距离模型和一般设计考虑。 /// - http://csoundjournal.com/issue9/newHRTFOpcodes.html - some ideas to remove clicks in hrtf renderer // http://csoundjournal.com/issue9/newHRTFOpcodes.html - 一些想法用于去除 HRTF 渲染器中的喀哒声 /// - https://phaidra.kug.ac.at/open/o:11024"
[graphmanage-lib.cargo/crates]: https://crates.io/crates/fyrox-graph "(: cargo add -- fyrox-graph) (MIT) (1,444 SLoC) (24.3 KiB) Graph management crate for the Fyrox engine // Fyrox 引擎的图管理库"
[grapapi-lib.cargo/crates]: https://crates.io/crates/fyrox-graphics "(: cargo add -- fyrox-graphics) (MIT) (5.4K SLoC) (66.1 KiB) Graphics API abstraction for Fyrox Game Engine // Fyrox 游戏引擎的图形 API 抽象"
[math-lib.cargo/crates]: https://crates.io/crates/fyrox-math "(: cargo add -- fyrox-math) (MIT) (4.3K SLoC) (30.9 KiB) (Math library for game development. // 用于游戏开发的数学库。)"
[pm-cli.cargo/crates]: https://crates.io/crates/fyrox-project-manager "(: cargo install -- fyrox-project-manager) (MIT) (2.2K SLoC) (174 KiB) Project manager for Fyrox engine // 为 Fyrox 引擎提供的项目管理器"
[scripts-lib.cargo/crates]: https://crates.io/crates/fyrox-scripts "(: cargo add -- fyrox-scripts) (MIT) (254 SLoC) (29.8 KiB) Standard scripts for the Fyrox engine // Fyrox 引擎的标准脚本"
