[src/gh]: https://github.com/mifi/lossless-cut.git "(GPL-2.0) (Languages: TypeScript 98.9%, Other 1.1%) The swiss army knife of lossless video/audio editing // 无损视频/音频编辑的瑞士军刀"

[app.dmg/applestore]: https://apps.apple.com/app/losslesscut/id1505323402 "(250.2 MB) ($18.99)"
[app.appx/msstore]: https://apps.microsoft.com/detail/9p30lsr4705l "(418.9 MB) (HK$154.00)"
[app.flatpak/flathub]: https://flathub.org/apps/no.mifi.losslesscut "(Download Size: 106.25 MiB, Installed Size: ~268.98 MiB) (Unverified) (: flatpak install -- flathub no.mifi.losslesscut ;: flatpak run -- no.mifi.losslesscut)"

[sponse/auth]: https://mifi.no/thanks/ "(Bitcoin: 197wMK3YkwFgqrmRQbMgFb4ADCX6q6FiMW) (Ethereum: 0xd1F16720A11ea0a1501A0DBe85F85174CB13b2b3) (Monero: 42MMTGjJhPs5ZtBg1KcYZhghfwyAvq4WsW5Nk2Z6XbYjBbmqMnZJ43QiYsAfNV2sdCMkFErqSe71tHyQcZr4dDntJsX6NPH)"
[site/auth]: https://mifi.no/losslesscut/ "The Swiss Army Knife of Lossless Video/Audio Editing // 无损视频/音频编辑的瑞士军刀 /// Shave gigabytes off video and audio files in seconds without loss of quality // 在几秒内无损削减视频和音频文件的大小 /// - Traditional video editors // 传统视频编辑器 ///: Any operation in a normal video editor // 在普通视频编辑器中的任何操作 ///; requires you to re-encode the whole file // 都需要你重新编码整个文件 ///; which can take hours and you will // 这可能需要数小时，你将 ///; gradually lose quality every time a file is processed. // 每次处理文件时，质量会逐渐下降。 /// - LosslessCut // 无损剪接 ///: LosslessCut will do the same in seconds without losing quality // LosslessCut 将在几秒钟内完成，且不会损失质量 ///; LosslessCut simply cuts the data stream and directly copies it over. // LosslessCut 只是简单地切割数据流并直接复制它。 /// Features // 特性 /// - Losslessly trim or cut out parts of video/audio // 无损剪切或裁剪视频/音频部分 /// - Lossless merge/concatenation of arbitrary files (identical codec parameters) // 无损任意文件合并/连接（相同编解码器参数） /// - Lossless stream editing: Combine arbitrary tracks from multiple files // 无损流编辑：组合来自多个文件的任意轨道 /// - Losslessly extract all tracks from a file // 无损提取文件中的所有轨道 /// - Remux into any compatible output format // 重新封装到任何兼容的输出格式 /// - Take full-resolution snapshots from videos in JPEG/PNG format // 从 JPEG/PNG 格式的视频中获取全分辨率快照 /// - Apply a per-file timecode offset in the preview // 在预览中为每个文件应用时间码偏移 /// - Change rotation/orientation metadata in videos // 修改视频中的旋转/方向元数据 /// - Powerful timeline with zoom and frame/keyframe jumping // 强大的时间轴，支持缩放和帧/关键帧跳转 /// - Auto-saves per project cut segments to file // 自动保存每个项目的剪辑片段到文件 /// - View ffmpeg last command log so you can modify and re-run modify recent commands on the command line // 查看 ffmpeg 最后的命令日志，以便您可以修改并在命令行中重新运行最近的命令 /// - Give labels to cut segments // 为剪辑片段添加标签 /// - Segment panel showing segments with details, export/import cut segments as CSV // 显示片段详情的片段面板，可将剪辑片段导出/导入为 CSV /// - Video thumbnails and audio waveform // 视频缩略图和音频波形 /// Other usage scenarios // 其他使用场景 /// - Cut out commercials from a recorded TV show // 从录制的电视节目中剪辑广告 /// - Remove audio or video track from a file // 从文件中删除音频或视频轨道 /// - Add music to a video (or replace existing audio track) // 向视频添加音乐（或替换现有音频轨道） /// - Include a subtitle into a video // 将字幕包含到视频中 /// - Extract music, video or subtitle track from a video and cut it to your needs // 从视频中提取音乐、视频或字幕轨道，并按需剪辑 /// - Change format of a H264 TV recording from TS to MP4 // 将 H264 电视录制格式从 TS 转换为 MP4 /// - Fix rotation of a video that has the wrong orientation flag set, e.g. phone recording // 修复具有错误方向标志的视频旋转，例如手机录制 /// - Quickly change a H264 MKV video to MOV or MP4 for playback on iPhone // 快速将 H264 MKV 视频转换为 MOV 或 MP4，以便在 iPhone 上播放 /// - Import a list of cut times from other tool as a EDL (edit decision list, CSV) and run these cuts with LosslessCut // 从其他工具导入切割时间列表作为 EDL（编辑决策表，CSV），并使用 LosslessCut 运行这些切割 /// - Export a list of cut times as a CSV EDL and run these in another tool // 导出切割时间列表为 CSV EDL 文件，并在其他工具中运行这些操作 /// - Loop a video / audio clip X times quickly without re-encoding // 快速循环视频/音频片段 X 次，无需重新编码 /// Notes // 注 /// * Which function is available depends on the file in question and which format/codec this file has // * 可用的功能取决于文件本身以及该文件的格式/编解码器 /// * This app is not for exact cutting. Start cut time will be \"rounded\" to the nearest previous keyframe, which may be a fraction of a second before your desired cut point, or up to several seconds, depending on the encoding. // * 该应用程序不用于精确切割。开始切割时间将被“四舍五入”到最近的先前关键帧，这可能比您期望的切割点早几分之一秒，或者早几秒，具体取决于编码方式。"

[knowsby]: https://github.com/topics/video-editor
