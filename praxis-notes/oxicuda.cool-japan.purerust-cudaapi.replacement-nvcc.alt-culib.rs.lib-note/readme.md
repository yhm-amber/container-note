[src/gh]: https://github.com/cool-japan/oxicuda.git "(Apache-2.0) (Languages: Rust 100.0%) OxiCUDA replaces the entire NVIDIA CUDA Toolkit software stack with type-safe, memory-safe Rust code. // OxiCUDA 使用类型安全、内存安全的 Rust 代码替换了整个 NVIDIA CUDA 工具包软件栈。 /// Pure Rust CUDA replacement -- cuBLAS, cuDNN, cuFFT, cuSPARSE, cuSOLVER, cuRAND and beyond in ~248K lines of safe Rust across 28 crates. // 纯 Rust CUDA 替代品 -- cuBLAS、cuDNN、cuFFT、cuSPARSE、cuSOLVER、cuRAND 以及更多，在约 248K 行安全的 Rust 代码中，分布在 28 个 crate 中。 /// OxiCUDA replaces the entire NVIDIA CUDA Toolkit software stack with type-safe, memory-safe Rust code. The only runtime dependency is the NVIDIA driver (libcuda.so / nvcuda.dll); no CUDA SDK, no nvcc, no C/C++ toolchain is needed at build time. Optimized PTX assembly is generated directly from Rust data structures, and a built-in autotuner benchmarks kernel variants per GPU architecture to achieve near-peak throughput from Turing through Blackwell. // OxiCUDA 使用类型安全、内存安全的 Rust 代码替换了整个 NVIDIA CUDA 工具包软件栈。唯一的运行时依赖是 NVIDIA 驱动程序（ libcuda.so / nvcuda.dll ）；构建时无需 CUDA SDK、无需 nvcc ，也无需 C/C++ 工具链。优化的 PTX 汇编直接从 Rust 数据结构生成，并且内置的自动调谐器针对每个 GPU 架构测试内核变体，以从 Turing 到 Blackwell 实现接近峰值吞吐量。 /// ## Feature Highlights // 特性亮点 /// ### Vol.1 -- Foundation (4 crates, 23,025 SLoC) // 第一卷 -- 基础 (4 个库，23,025 行 SLoC) /// - Dynamic driver loading via libloading -- zero build-time SDK dependency // 通过 libloading 实现动态驱动加载 -- 零构建时 SDK 依赖 /// - DeviceBuffer<T> with Rust ownership semantics -- Send + Sync, RAII // DeviceBuffer<T> 具备 Rust 所有权语义 -- Send + Sync ，RAII /// - Type-safe launch! macro with compile-time grid/block validation // 类型安全的 launch! 宏，带编译时网格/块验证 /// - CUDA Runtime API layer for high-level device management // CUDA 运行时 API 层，用于高级设备管理 /// ### Vol.2 -- PTX Codegen & Autotuner (2 crates, 43,354 SLoC) // 第 2 卷 -- PTX 代码生成 & 自动调优器 (2 个库，43,354 SLoC) /// - Rust DSL that generates PTX IR covering SM 7.5 through SM 10.0 // 生成覆盖 SM 7.5 至 SM 10.0 的 PTX IR 的 Rust DSL /// - Tensor Core support: WMMA, MMA, WGMMA instruction generation // 张量核心支持：WMMA、MMA、WGMMA 指令生成 /// - Built-in autotuner with 3-tier dispatch (cached / tuned / default) // 内置自动调谐器，具有三级调度（缓存 / 调优 / 默认） /// - Disk-based PTX cache keyed by kernel hash + GPU architecture // 基于磁盘的 PTX 缓存，以内核哈希 + GPU 架构为键 /// ### Vol.3 -- BLAS (1 crate, 21,845 SLoC) // 第 3 卷 -- BLAS（1 个包，21,845 SLoC） /// - Full BLAS Level 1/2/3 (axpy, gemv, gemm, trsm, syrk, ...) // 完整的 BLAS Level 1/2/3（axpy、gemv、gemm、trsm、syrk、...） /// - GEMM dispatch: SIMT, Tensor Core, Split-K paths // GEMM 分发：SIMT、张量核心、Split-K 路径 /// - Batched GEMM: standard, strided, grouped // 批量 GEMM：标准、带偏移、分组 /// - Precision coverage: F16, BF16, TF32, F32, F64, FP8 // 精度覆盖率：F16、BF16、TF32、F32、F64、FP8 /// - Elementwise ops (relu, gelu, sigmoid, silu) and reductions (softmax, variance) // 逐元素操作（relu、gelu、sigmoid、silu）和归约操作（softmax、方差） /// ### Vol.4 -- DNN (1 crate, 34,711 SLoC) // 第 4 卷 -- DNN（1 个库，34,711 SLoC） /// - Convolution: implicit GEMM, im2col, Winograd 3x3, direct, fused Conv+BN+Act // 卷积：隐式 GEMM、im2col、Winograd 3x3、直接、融合卷积+BN+激活 /// - FlashAttention forward/backward, PagedAttention, decode attention // FlashAttention 前向/后向、PagedAttention、解码注意力 /// - MoE: top-k routing, token permutation, fused MoE kernel // MoE：top-k 路由，token 置换，融合 MoE 内核 /// - Normalization: BatchNorm, LayerNorm, RMSNorm, GroupNorm // 归一化：BatchNorm，LayerNorm，RMSNorm，GroupNorm /// - Pooling: max, average, adaptive, global // 池化：最大值，平均值，自适应，全局 /// - Resize: nearest, bilinear, bicubic // 调整大小：最近邻，双线性，双三次 /// - Quantization: FP8, INT8, block-scaled FP4 // 量化：FP8、INT8、块缩放 FP4 /// ### Vol.5 -- Scientific Computing (4 crates, 47,946 SLoC) // 第 5 卷 -- 科学计算（4 个库，47,946 SLoC） /// - FFT: Stockham, radix-2/4/8, mixed-radix, Bluestein, C2C/R2C/C2R, 2D/3D // FFT：Stockham，基-2/4/8，混合基，Bluestein，C2C/R2C/C2R，2D/3D /// - Sparse: CSR/CSC/COO/BSR/ELL, SpMV, SpMM, SpGEMM, SDDMM, ILU(0)/IC(0) // 稀疏：CSR/CSC/COO/BSR/ELL，SpMV，SpMM，SpGEMM，SDDMM，ILU(0)/IC(0) /// - Solver: LU, QR, SVD, Cholesky, eigendecomp, CG, BiCGSTAB, GMRES // 求解器：LU，QR，SVD，Cholesky，特征分解，CG，BiCGSTAB，GMRES /// - Rand: Philox, MRG32k3a, XORWOW, Sobol, uniform/normal/Poisson /// ### Vol.6 -- Signal Processing (1 crate, 6,061 SLoC) // 第 6 卷 -- 信号处理（1 个包，6,061 SLoC） /// - Audio: MFCC, STFT, Mel filterbank, spectral features // 音频：MFCC、STFT、梅尔滤波器组、频谱特征 /// - Image: Gaussian blur, Sobel edge detection, morphological ops // 图片：高斯模糊，Sobel 边缘检测，形态学操作 /// - DCT: Types I-IV with fast algorithms // DCT：I-IV 类型，快速算法 /// - DWT: Haar, Daubechies wavelets // DWT：Haar，Daubechies 小波 /// - Filtering: IIR/FIR filters, Butterworth, Chebyshev // 滤波：IIR/FIR 滤波器，Butterworth，Chebyshev /// - Correlation: cross-correlation, autocorrelation // 相关：互相关，自相关 /// ### Vol.7 -- Computation Graph (1 crate, 4,802 SLoC) // 第 7 卷 -- 计算图（1 个包，4,802 SLoC） /// - CUDA Graph capture API (StreamCapture, GraphCapture) // CUDA 图捕获 API（StreamCapture，GraphCapture） /// - Execution plan with dependency-sorted node scheduling // 执行计划依赖排序节点调度 /// - Event-based inter-node synchronization // 基于事件的节点间同步 /// - Sequential + parallel graph executors // 顺序 + 并行图执行器 /// ### Vol.8 -- GPU Training (2 crates, 10,247 SLoC) // 第 8 卷 -- GPU 训练（2 个库，10,247 SLoC） /// - Mixed precision training (AMP): FP16/BF16 + loss scaling // 混合精度训练（AMP）：FP16/BF16 + 损失缩放 /// - Gradient accumulation and clipping; EMA (exponential moving average) // 梯度累积和裁剪；EMA（指数移动平均） /// - LR schedulers: cosine, warmup, cyclic, polynomial // 学习率调度器：余弦、预热、循环、多项式 /// - GPU-fused optimizers: Adam, AdamW, SGD, RMSProp, LAMB // GPU 融合优化器：Adam、AdamW、SGD、RMSProp、LAMB /// - Checkpointing (model save/load) // 检查点（模型保存/加载） /// - Quantization: INT8/INT4/FP8 weight quantization, block-scaled // 量化：INT8/INT4/FP8 权重量化，块缩放 /// ### Vol.9 -- Inference Engine (3 crates, 11,929 SLoC) // 第 9 卷 -- 推理引擎（3 个包，11,929 行 SLoC） /// - KV-cache with paged attention (PagedKvCache) and prefix caching // KV 缓存，带分页注意力（PagedKvCache）和前缀缓存 /// - Speculative decoding // 推测解码 /// - Distributed inference pipeline (tensor/pipeline parallelism) // 分布式推理管道（张量/管道并行） /// - LM inference: BPE tokenizer, vocabulary management, sampling strategies // 语言模型推理：BPE 分词器，词汇管理，采样策略 /// ### Vol.10 -- Reinforcement Learning (1 crate, 4,522 SLoC) // 第 10 卷 -- 强化学习 (1 个包，4,522 SLoC) /// - Replay buffers: Uniform, Prioritized (PER), N-step // 重放缓冲区：统一，优先（PER），N 步 /// - Policy distributions: Categorical, Gaussian (SAC reparameterization), Deterministic // 策略分布：分类，高斯（SAC 重新参数化），确定性 /// - Advantage estimators: GAE, TD(λ), V-trace, Retrace(λ) // 优势估计器：GAE，TD(λ)，V-trace，Retrace(λ) /// - Loss functions: PPO, DQN, Double-DQN, SAC, TD3 // 损失函数：PPO、DQN、Double-DQN、SAC、TD3 /// - Observation/reward normalization with Welford running stats // 使用 Welford 运行统计进行观察/奖励归一化 /// - Environment abstractions: Env, VecEnv (auto-reset) // 环境抽象：Env，VecEnv（自动重置） /// ### Backends (7 crates, 19,665 SLoC) // 后端 (7 个 Crate，19,665 SLoC) /// - Backend trait abstraction for multi-GPU-runtime portability // 多 GPU 运行时可移植的后端特征抽象 /// - CUB-equivalent GPU primitives (scan, reduce, sort, histogram) // CUB 等效 GPU 原语（扫描、归约、排序、直方图） /// - Metal (macOS), Vulkan Compute, WebGPU, AMD ROCm, Intel oneAPI (LevelZero) // Metal（macOS）、Vulkan 计算、WebGPU、AMD ROCm、Intel oneAPI（LevelZero） /// ## Pure Rust, Minimal Dependencies // 纯 Rust，最小依赖: /// OxiCUDA is built on a strict Pure Rust policy with minimal external dependencies. The entire codebase compiles with cargo build alone -- no C compiler, no Fortran runtime, no CUDA SDK, no nvcc, no pkg-config. // OxiCUDA 基于严格的纯 Rust 政策构建，外部依赖极小。整个代码库仅使用 cargo build 即可编译——无需 C 编译器，无需 Fortran 运行时，无需 CUDA SDK，无需 nvcc ，无需 pkg-config 。 /// The only runtime requirement is the NVIDIA GPU driver (libcuda.so on Linux, nvcuda.dll on Windows). On macOS the crate compiles but returns UnsupportedPlatform at runtime. // 唯一运行时要求是 NVIDIA GPU 驱动程序（Linux 上的 libcuda.so ，Windows 上的 nvcuda.dll ）。在 macOS 上，该 crate 可以编译，但在运行时返回 UnsupportedPlatform 。"
[lib.cargo/crates]: https://crates.io/crates/oxicuda "(: cargo add -- oxicuda) (Apache-2.0) (19K SLoC) (173 KiB) OxiCUDA - Pure Rust CUDA replacement for the COOLJAPAN ecosystem (95% performance target) // OxiCUDA - 纯 Rust CUDA COOLJAPAN 生态系统的替代品（目标性能 95%） (src: gh:cool-japan/oxicuda.git)"
