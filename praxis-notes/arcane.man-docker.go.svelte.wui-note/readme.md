[src/gh]: https://github.com/getarcaneapp/arcane.git "(BSD-3-Clause) (Languages: Svelte 46.4%, Go 38.1%, TypeScript 12.5%, Shell 2.0%, CSS 0.8%, PLpgSQL 0.1%, Other 0.1%) Modern Docker Management, Designed for Everyone. // 面向所有人的现代 Docker 管理。 /// Arcane is designed to be an easy and modern Docker management platform, built with everybody in mind. The goal of Arcane is to be built for and by the community to make sure nobody feels left out or behind with their specific features or processes. // Arcane 是一个易于使用且现代化的 Docker 管理平台，专为所有人设计。Arcane 的目标是构建一个由社区驱动和贡献的平台，以确保每个人在使用其特定功能或流程时都不会感到被遗漏或落后。"
[site/app]: https://getarcane.app/ "Modern Docker Management, Designed for Everyone. // 现代 Docker 管理，为所有人设计。 /// - Modern UI Interface // 现代 UI 界面 ///: Clean, intuitive design that makes Docker management a breeze. // 简洁直观的设计，让 Docker 管理变得轻松。 /// - Real-time Monitoring // 实时监控 ///: Live updates of container status, resource usage, and logs. // 容器状态、资源使用情况和日志的实时更新。 /// - Container Management // 容器管理 ///: Start, stop, restart, and inspect containers with ease. // 轻松启动、停止、重启和检查容器。 /// - Image Management // 图像管理 ///: Pull, and manage Docker images. // 拉取和管理 Docker 镜像。 /// - Network Configuration // 网络配置 ///: Create and configure Docker networks. // 创建和配置 Docker 网络。 /// - Volume Management // 卷管理 ///: Create and manage persistent data with Docker volumes. // 使用 Docker 卷创建和管理持久数据。 /// - Resource Visualization  资源可视化 ///: Visual graphs for CPU, memory, and network usage. // CPU、内存和网络使用情况的视觉图表。 /// Shoutout to the shadcn and shadcn-svelte teams — their design language inspired this documentation experience. Much of the UX polish pays homage to their incredible work, so be sure to explore their ecosystems! // 向 shadcn 和 shadcn-svelte 团队致敬——他们的设计语言启发了这份文档体验。许多用户体验的打磨向他们的杰出工作致敬，所以一定要探索他们的生态系统！"
[docs/.site]: https://getarcane.app/docs "Install: (docker-compose: { services: { arcane: { image: 'ghcr.io/getarcaneapp/arcane:latest', container_name: arcane, ports: ['3552:3552'], volumes: ['/var/run/docker.sock:/var/run/docker.sock', 'arcane-data:/app/data', '/host/path/to/projects:/app/data/projects'], environment: ['APP_URL=http://localhost:3552', PUID=1000, PGID=1000, ENCRYPTION_KEY=xxxxxxxxxxxxxxxxxxxxxx, JWT_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxx], restart: unless-stopped } }, volumes: { arcane-data: null } }) where ENCRYPTION_KEY must be 32 bytes (raw/base64/hex). // 其中 ENCRYPTION_KEY 必须是 32 字节（原始/base64/十六进制）。 /// You can use the Arcane CLI inside a temporary container to generate secrets in the format arcane supports, or you can use the host OSes openssl command as previously documented. // 您可以在临时容器中使用 Arcane CLI 生成 Arcane 支持的格式的密钥，或者您可以使用先前文档中提到的主机操作系统 openssl 命令。 /// (: docker run --rm -- ghcr.io/getarcaneapp/arcane:latest /app/arcane generate secret # Via Docker Container) (: arcane generate secret # Standalone Arcane Binary) /// (: docker compose up -d) Start Arcane // 开始运行奥术 /// Access Arcane // 访问奥术 /// Go to localhost:3552 in your browser and follow the setup. After your first initial login, you will be asked to change the default admin password. The default credentials are shown below. // 在浏览器中访问 localhost:3552 并按照设置步骤操作。首次登录后，系统会要求您更改默认管理员密码。默认凭证如下所示。 /// Username: arcane, Password: arcane-admin"
[arcane.oci/ghcr]: https://github.com/getarcaneapp/arcane/pkgs/container/arcane "(: pull ghcr.io/getarcaneapp/arcane) (BSD 3-Clause \"New\" or \"Revised\" License)"
[arcane-headless.oci/ghcr]: https://github.com/getarcaneapp/arcane/pkgs/container/arcane-headless "(: pull ghcr.io/getarcaneapp/arcane-headless) (BSD 3-Clause \"New\" or \"Revised\" License)"

