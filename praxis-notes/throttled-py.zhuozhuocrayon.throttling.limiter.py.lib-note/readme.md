[src/gh]: https://github.com/ZhuoZhuoCrayon/throttled-py.git "(MIT) (Languages: Python 94.7%, Lua 4.8%, Makefile 0.5%) 🔧 High-performance Python rate limiting library with multiple algorithms (Fixed Window, Sliding Window, Token Bucket, Leaky Bucket & GCRA) and storage backends (Redis, In-Memory). // 🔧 高性能 Python 速率限制库，支持多种算法（固定窗口、滑动窗口、令牌桶、漏桶 & GCRA）和多种存储后端（Redis、内存）。 /// ✨ Features // ✨ 特性 /// - Supports both synchronous and asynchronous (async / await). // 支持同步和异步（ async / await ）。 /// - Provides thread-safe storage backends: Redis, In-Memory (with support for key expiration and eviction). // 提供线程安全的存储后端：Redis、内存（支持键过期和驱逐）。 /// - Supports multiple rate limiting algorithms: Fixed Window, Sliding Window, Token Bucket, Leaky Bucket & Generic Cell Rate Algorithm (GCRA). // 支持多种速率限制算法：固定窗口、滑动窗口、令牌桶、漏桶和通用单元速率算法（GCRA）。 /// - Supports configuration of rate limiting algorithms and provides flexible quota configuration. // 支持配置限流算法，并提供灵活的配额配置。 /// - Supports immediate response and wait-retry modes, and provides function call, decorator, and context manager modes. // 支持即时响应和等待重试模式，并提供函数调用、装饰器和上下文管理器模式。 /// - Supports integration with the MCP Python SDK to provide rate limiting support for model dialog processes. // 支持与 MCP Python SDK 集成，为模型对话流程提供限流支持。 /// - Excellent performance, The execution time for a single rate limiting API call is equivalent to(see Benchmarks for details): // 性能优异，单个限流 API 调用的执行时间相当于（详情请参考基准测试）： /// - - In-Memory: ~2.5-4.5x dict[key] += 1 operations. // 内存中：~2.5-4.5 倍 dict[key] += 1 操作。 /// - - Redis: ~1.06-1.37x INCRBY key increment operations. // Redis：~1.06-1.37 倍 INCRBY key increment 操作。"
[lib.pip/pypi]: https://pypi.org/project/throttled-py/ "(: pip install -- throttled-py # [in-memory] for Use In-Memory as storage backend # [redis] for Use Redis as storage backend # [all] for Install all extras) (License: MIT License (MIT)) (Requires: Python <4.0, >=3.8) (src: gh:ZhuoZhuoCrayon/throttled-py.git)"
[docs/rtd]: https://throttled-py.readthedocs.io "throttled-py is a high-performance Python rate limiting library with multiple algorithms(Fixed Window, Sliding Window, Token Bucket, Leaky Bucket & GCRA) and storage backends (Redis, In-Memory). // throttled-py 是一个高性能的 Python 速率限制库，支持多种算法（固定窗口、滑动窗口、令牌桶、漏桶和 GCRA）以及多种存储后端（Redis、内存）。"
