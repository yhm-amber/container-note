[site/dev]: https://bloomengine.dev/ "Native games from TypeScript // TypeScript 原生游戏 ///: Write TypeScript. Ship native games. No browser, no C++. Bloom compiles your game to Metal, DirectX 12, Vulkan, and OpenGL — one codebase for every platform. // 使用 TypeScript 编写游戏，发布原生游戏。无需浏览器，无需 C++。Bloom 会将你的游戏编译成 Metal、DirectX 12、Vulkan 和 OpenGL 版本——一套代码库即可适配所有平台。 ///  /// - 0 - Runtime overhead // 0 个 - 运行时开销 /// - 5 - Target platforms // 5 个 - 目标平台 /// - 7 - Modular subsystems // 7 个 - 模块化子系统 /// - 1 - Codebase to maintain // 1 个 - 待维护的代码库 /// Why Bloom? // 为什么是布鲁姆？ ///: Bloom is the only game engine that lets you write TypeScript and ship truly native games. No Electron, no WebView, no browser runtime. Just your code, compiled to native. // Bloom 是唯一一款支持 TypeScript 编程并能真正发布原生游戏的引擎。无需 Electron、WebView 或浏览器运行时环境。只需编写代码，即可编译成原生应用。 /// - Simple API // 简单 API ///: The entire API fits on a cheatsheet. No classes, no inheritance, no magic. Just functions you call. // 整个 API 可以用一张速查表概括。没有类，没有继承，没有魔法。只有你调用的函数。 /// - True Native // 真原住民 ///: Compiles to Metal, DirectX 12, Vulkan, and OpenGL. Real GPU acceleration, real performance. // 支持编译为 Metal、DirectX 12、Vulkan 和 OpenGL。真正的 GPU 加速，真正的性能。 /// - Ship Everywhere // 到处都有船 ///: macOS, Windows, Linux, iOS, and Android from one codebase. Steam, App Store, Play Store ready. // 基于同一套代码库，支持 macOS、Windows、Linux、iOS 和 Android 系统。已适配 Steam、App Store 和 Play Store。 /// Everything you need // 你需要的一切 ///: Seven modular subsystems, each independently importable. Use only what you need. // 七个模块化子系统，每个子系统均可独立导入。仅使用所需功能。 /// - Core // 核 { bloom/core // 绽放/核心 } ///: Window creation, game loop, input handling, timing, and frame control. // 窗口创建、游戏循环、输入处理、计时和帧控制。 /// - Shapes // 形状 { bloom/shapes // 绽放/形状 } ///: 2D shape drawing — lines, rects, circles, polygons — plus collision detection. // 2D 形状绘制——线条、矩形、圆形、多边形——以及碰撞检测。 /// - Textures // 纹理 { bloom/textures // 绽放/纹理 } ///: Image loading, texture management, and efficient sprite batching. // 图像加载、纹理管理和高效的精灵批处理。 /// - Text // 文本 { bloom/text // 布鲁姆/文本 } ///: TTF and OTF font loading, text rendering, and measurement. // TTF 和 OTF 字体加载、文本渲染和测量。 /// - Audio // 声音的 { bloom/audio // 布鲁姆/音频 } ///: Sound effects, music streaming, and volume control. WAV, OGG, and MP3. // 音效、音乐串流和音量控制。支持 WAV、OGG 和 MP3 格式。 /// - Models // 模型 { bloom/models // 布鲁姆/模型 } ///: 3D model loading (glTF, OBJ), mesh generation, and animations. // 3D 模型加载（glTF、OBJ）、网格生成和动画。 /// - Math // 数学 { bloom/math // 布鲁姆/数学 } ///: Vectors, matrices, quaternions, easing functions, and random number generation. // 向量、矩阵、四元数、缓动函数和随机数生成。 /// One codebase. Every platform. // 一套代码库，覆盖所有平台。 ///: Bloom maps to the native graphics API on each platform. Your TypeScript compiles to real binaries. // Bloom 映射到各个平台的原生图形 API。你的 TypeScript 代码会被编译成实际的二进制文件。 /// - macOS: Metal /// - Windows: DirectX 12 /// - Linux: Vulkan / OpenGL /// - iOS: Metal /// - Android: Vulkan / OpenGL ES /// Familiar patterns, native power // 熟悉的模式，与生俱来的力量 /// Plain interfaces, pure functions, zero magic. If you know TypeScript, you know Bloom. // 简洁的接口、纯函数，零魔法。如果你了解 TypeScript，你就知道 Bloom 了。 /// - 2D Shapes & Collision // 2D 形状与碰撞 ///: (: import { drawRect, drawCircle, checkCollisionRecs } from 'bloom/shapes') (: drawRect(100, 100, 200, 80, Colors.GREEN) ;: drawCircle(400, 300, 50, Colors.GOLD) ;: if (checkCollisionRecs(player, enemy)) { playSound(hitSound) }) /// - 3D Camera & Models // 3D 相机和模型 ///: (: const camera: Camera3D = { position: { x: 10, y: 10, z: 10 }, target: { x: 0, y: 0, z: 0 }, up: { x: 0, y: 1, z: 0 }, fovy: 45, projection: 0 }) (: beginMode3D(camera) ;: drawModel(tree, { x: 0, y: 0, z: 0 }) ;: drawGrid(10, 1) ;: endMode3D())"
[src/gh]: https://github.com/Bloom-Engine/engine.git "(Languages: Rust 87.2%, TypeScript 7.1%, Swift 1.5%, Python 1.4%, Metal 1.3%, HTML 0.6%, Other 0.9%) Native TypeScript game engine — compiles to Metal, DirectX 12, Vulkan, and OpenGL // 原生 TypeScript 游戏引擎——可编译为 Metal、DirectX 12、Vulkan 和 OpenGL。 /// Bloom Engine // Bloom 引擎 /// Native games from TypeScript. // 使用 TypeScript 开发原生游戏。 /// Write TypeScript. Ship native games — and now the web too. Bloom compiles your game to Metal, DirectX 12, Vulkan, OpenGL, and WebGPU — one codebase for every platform. // 编写 TypeScript 代码，发布原生游戏——现在还能发布 Web 游戏。Bloom 可以将你的游戏编译成 Metal、DirectX 12、Vulkan、OpenGL 和 WebGPU 版本——一套代码即可适配所有平台。 /// Quick Start // 快速入门 /// (: import { initWindow, windowShouldClose, beginDrawing, endDrawing, clearBackground, drawText, Colors } from 'bloom') (: initWindow(800, 450, 'My Game') ;: while (!windowShouldClose()) { beginDrawing(); clearBackground(Colors.RAYWHITE); drawText('Hello, Bloom!', 190, 200, 20, Colors.DARKGRAY); endDrawing(); }) /// Web-Compatible Pattern // Web 兼容模式 /// Use runGame() for code that works on both native and web: // 使用 runGame() 函数可以编写同时适用于原生平台和 Web 平台的代码： /// (: import { initWindow, runGame, clearBackground, drawText, Colors } from 'bloom') (: initWindow(800, 450, 'My Game') ;: runGame((dt) => { clearBackground(Colors.RAYWHITE); drawText('Hello, Bloom!', 190, 200, 20, Colors.DARKGRAY); }); ) Build for web (: ./native/web/build.sh main.ts ;: cd dist/web && python3 -m http.server 8080) /// Features // 特征 /// - Simple API — Functions, not classes. The entire API fits on a cheatsheet. // 简洁的 API—— 使用函数而非类。整个 API 可以用一张速查表概括。 /// - True native — Compiles to Metal, DirectX 12, Vulkan, OpenGL, and WebGPU via wgpu. // 真正的原生应用 ——通过 wgpu 编译为 Metal、DirectX 12、Vulkan、OpenGL 和 WebGPU。 /// - Ship everywhere — macOS, Windows, Linux, iOS, tvOS, Android, and Web from one codebase. // 一套代码库即可发布到 macOS、Windows、Linux、iOS、tvOS、Android 和 Web 等平台。 /// - Unified 2D/3D — Shapes, textures, text, 3D models, and audio in one engine. // 统一的 2D/3D — 在一个引擎中集成形状、纹理、文本、3D 模型和音频。 /// - Zero magic — Explicit game loops, no hidden framework overhead. // 零魔法 ——显式游戏循环，无隐藏框架开销。"
[showcase/.site]: https://bloomengine.dev/showcase "Showcase // 展示 /// Games and projects built with Bloom Engine. // 使用 Bloom Engine 构建的游戏和项目。 /// - Bloom Garden // 布鲁姆花园 ///: A tiny garden adventure. The flagship demo game built entirely with Bloom Engine — cozy low-poly exploration with warm golden-hour lighting. // 一场迷你花园冒险。这款旗舰演示游戏完全使用 Bloom Engine 构建——温馨的低多边形探索，配以温暖的金色夕阳光影。 /// - Pong // 乒乓 ///: Classic two-player Pong. Demonstrates input handling, 2D shapes, collision detection, audio, and game state management in ~170 lines. // 经典的双人乒乓球游戏。用大约 170 行代码演示了输入处理、2D 图形、碰撞检测、音频和游戏状态管理。"
[docs/.site]: https://bloomengine.dev/docs "Documentation // 文档 /// Everything you need to build native games with TypeScript. // 使用 TypeScript 构建原生游戏所需的一切。"
[knowsby]: https://github.com/PerryTS/perry.git "Bloom Engine // 绽放引擎 ///: Native TypeScript game engine — Metal, DirectX 12, Vulkan, OpenGL. Write games in TS, ship native. // 原生 TypeScript 游戏引擎——支持 Metal、DirectX 12、Vulkan 和 OpenGL。用 TypeScript 编写游戏，以原生方式发布。"
