[src/gh]: https://github.com/Gaurav-Gosain/golars.git "(MIT) (Languages: Go 76.6%, C 10%, TypeScript 3.9%, MDX 3%, Assembly 2.5%, Python 1%, Other 3%) Pure-Go DataFrames modeled on polars, built on Apache Arrow. No cgo. // 基于 polars 的纯 Go 数据帧，基于 Apache Arrow 构建。无 cgo。 /// Eager and lazy execution with a plan-rewriting optimiser, a streaming engine, and AVX2/AVX-512 kernels on amd64 and NEON on arm64. A single go build cross-compiles to Linux, macOS, Windows. // 使用计划重写优化器、流引擎以及在 amd64 上的 AVX2/AVX-512 内核和 arm64 上的 NEON 实现急切和惰性执行。单个 go build 可以交叉编译到 Linux、macOS 和 Windows。 /// Matches or beats polars 1.39 on most polars-compare workloads, Arrow-native end to end (no conversion cost talking to polars, PyArrow, DuckDB), and ships a full terminal stack: REPL, LSP, formatter, linter, TUI data browser, SQL frontend, an MCP server for Claude Desktop / Cursor / Windsurf, and a pipe-friendly .glr scripting language. // 在大多数 polars-compare 工作负载中表现优于或持平于 polars 1.39，端到端使用 Arrow 原生（与 polars、PyArrow、DuckDB 通信无转换成本），并提供完整的终端栈：REPL、LSP、格式化工具、代码检查工具、TUI 数据浏览器、SQL 前端、用于 Claude Desktop / Cursor / Windsurf 的 MCP 服务器，以及支持管道的 .glr 脚本语言。"
[site/zip]: https://golars.gaurav.zip/ "Pure-Go DataFrames, polars-style // 纯 Go 数据帧，Polars 风格 /// Eager + lazy execution. Optimiser, streaming engine, SIMD kernels. // 贪婪 + 惰性执行。优化器，流引擎，SIMD 内核。 /// Matches or beats polars 1.39 on most polars-compare workloads. No cgo. // 在大多数 polars-compare 工作负载中表现优于或持平于 polars 1.39。无需 cgo。 ///  /// - Eager + Lazy // 急切 + 懒惰 ///: Build pipelines as logical plans. Optimiser fuses projections, pushes filters, collapses scans. Then Collect. // 构建逻辑计划作为管道。优化器融合投影、推送过滤器、压缩扫描。然后收集。 /// - Streaming engine // 流引擎 ///: Morsel-driven execution for datasets that don't fit in memory. stream.New(cfg, source, stages, sink). // 用于不适合内存中的数据集的碎片驱动执行。stream.New(cfg, source, stages, sink)。 /// - polars-grade performance // polars-grade 性能 ///: SIMD kernels on amd64 via GOEXPERIMENT=simd. AVX2 VPBLENDVB blend, SIMD compare + arithmetic. // SIMD 内核在 amd64 平台上通过 GOEXPERIMENT=simd 实现。AVX2 VPBLENDVB 混合，SIMD 比较 + 算术。 /// - .glr scripting // .glr 脚本 ///: One command per line. Drive the REPL, run from disk, embed from your own tools. // 每行一条命令。驱动 REPL，从磁盘运行，从自己的工具中嵌入。 /// - Rich CLI // 丰富的 CLI ///: golars run, fmt, lint, sql, schema, head, diff, browse, explain, completion. // golars 运行，fmt，lint，sql，schema，head，diff，浏览，解释，补全。 /// - LLM-native // LLM 原生 ///: MCP server exposes schema, head, describe, sql, diff to Claude / Cursor / Windsurf. // MCP 服务器向 Claude / Cursor / Windsurf 暴露 schema，head，describe，sql，diff。 /// MIT License, Pure Go (no cgo), Arrow-native, Polars-compatible // MIT 许可证, 纯 Go（无 cgo）, Arrow 原生, Polars 兼容"
[cli.go/pgd]: https://pkg.go.dev/github.com/Gaurav-Gosain/golars/cmd/golars "(: go install -- github.com/Gaurav-Gosain/golars/cmd/golars) (License: MIT) (Imports: 41) (Imported by: 0)  (src: gh:Gaurav-Gosain/golars.git)"
[lib.go/pgd]: https://pkg.go.dev/github.com/Gaurav-Gosain/golars "(: go get -- github.com/Gaurav-Gosain/golars) (License: MIT) (Imports: 12) (Imported by: 0)  (src: gh:Gaurav-Gosain/golars.git)"
