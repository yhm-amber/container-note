[src/gh]: https://github.com/simonpcouch/streamy.git "(MIT) (Languages: R 100.0%) Inline Contents from Generators into Documents // 从生成器到文档的行内内容 /// Given a coro generator instance, streamy inlines text into a document selection in RStudio and Positron. This is particularly helpful for streaming LLM responses into documents. // 给定一个协程生成器实例，streamy 将文本内联到 RStudio 和 Positron 中的文档选择中。这对于将 LLM 响应流式传输到文档中特别有帮助。 /// This package powers gander, chores, and ensure. // 该软件包为 gander、chores 和 ensure 提供支持。"
[site/ghio]: https://simonpcouch.github.io/streamy/ "Stream generator results into a document // 流生成器结果输出到文档 /// Given an asychronous generator that produces text, this function iteratively polls that generator and inlines its results into the currently open RStudio or Positron document. This is particularly useful for streaming results from large language models. // 给定一个异步生成器，该生成器产生文本，此函数会迭代地轮询该生成器，并将结果内联到当前打开的 RStudio 或 Positron 文档中。这对于从大型语言模型流式传输结果特别有用。 /// Details // 详情 /// This function is generally not intended for interactive usage. See the gander, chores, and ensure package, which this package powers. // 此函数通常不用于交互式使用。请参阅 gander、chores 和 ensure 包，这些包由本包提供支持。"
[package/cran]: https://cran.r-project.org/web/packages/streamy/ "(Version: 	0.2.0) (Imports: 	cli (≥ 3.6.3), coro (≥ 1.1.0), rlang, rstudioapi (≥ 0.17.1), withr) (Suggests: 	ellmer, knitr, rmarkdown, testthat (≥ 3.0.0)) (Published: 	2025-05-21) (DOI: 	10.32614/CRAN.package.streamy) (Author: 	Simon Couch [aut, cre], Posit Software, PBC [cph, fnd]) (Maintainer: 	Simon Couch <simon.couch at posit.co>) (License: 	MIT + file LICENSE) (NeedsCompilation: 	no) streamy: Inline Asynchronous Generator Results into Documents // streamy: 将行内异步生成器结果合并到文档中 /// Given a 'coro' asynchronous generator instance that produces text, write that text into a document selection in 'RStudio' and 'Positron'. This is particularly helpful for streaming large language model responses into the user's editor. // 给定一个产生文本的 'coro' 异步生成器实例，将其文本写入 'RStudio' 和 'Positron' 中的文档选择。这对于将大型语言模型响应流式传输到用户编辑器特别有帮助。 (src: gh:simonpcouch/streamy.git)"
