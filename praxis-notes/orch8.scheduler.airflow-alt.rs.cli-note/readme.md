[src/gh]: https://github.com/orch8-io/engine.git "(Business Source License 1.1) (Languages: Rust 69.1%, TypeScript 29.7%, JavaScript 0.6%, CSS 0.3%, Shell 0.2%, Dockerfile 0.1%) Orch8 Engine // Orch8 引擎 /// A self-hosted, durable workflow orchestration engine built in Rust. Define workflows as composable JSON sequences. Orch8 guarantees every step either completes, retries, or surfaces in a dead-letter queue. // Orch8 是一个用 Rust 构建的自托管、持久化的工作流编排引擎。它将工作流定义为可组合的 JSON 序列。Orch8 保证每个步骤要么完成，要么重试，要么进入死信队列。 /// Single binary. One dependency: PostgreSQL (or SQLite for dev/embedded). // 单个二进制文件。仅依赖一个 PostgreSQL（开发/嵌入式系统可使用 SQLite）。 /// Why Orch8 // 为什么 Orch8 /// Existing durable workflow engines either ship a multi-service cluster (Temporal: Cassandra + Elasticsearch + JVM workers) or assume Python everywhere (Airflow: Celery + Redis + scheduler). Both are full-time operational jobs on a small team. // 现有的持久化工作流引擎要么部署多服务集群（例如 Temporal：Cassandra + Elasticsearch + JVM 工作节点），要么假定所有地方都使用 Python（例如 Airflow：Celery + Redis + 调度器）。两者都需要小型团队投入大量时间进行运维。 /// Orch8 keeps the execution model — state-snapshot durability, retries, replay-on-restart — but trades the ecosystem for one Rust binary and Postgres. Workers in any language via REST long-poll. Higher-level building blocks (Parallel, Race, TryCatch, CancellationScope, plus LLM/HumanReview/ToolCall) shipped as first-class instead of patterns you build on activities. // Orch8 保留了执行模型——状态快照持久性、重试机制和重启后重放——但用一个 Rust 二进制文件和 Postgres 数据库取代了原有的生态系统。它支持任何语言的 Worker，并通过 REST 长轮询机制实现。更高级的构建模块（Parallel、Race、TryCatch、CancellationScope 以及 LLM/HumanReview/ToolCall）作为一等公民提供，而不是像以前那样需要基于 Activity 构建模式。 /// Features // 特征 /// - Workflow Primitives — Step, Parallel, Race, TryCatch, Loop, ForEach, Router, SubSequence, CancellationScope, AB Split // 工作流基本元素 ——步骤、并行、竞争、TryCatch、循环、ForEach、路由、子序列、取消范围、AB 拆分 /// - Scheduling — Relative delays, business-days-only with holiday awareness, timezone-per-instance, jitter, send windows, cron triggers with configurable tick interval // 调度 — 相对延迟、仅限工作日（考虑节假日）、每个实例的时区、抖动、发送窗口、可配置滴答间隔的 cron 触发器 /// - Rate Limiting — Per-resource sliding window with deferred scheduling (not rejection), resource pools with weighted rotation, daily caps, and warmup ramps // 速率限制 ——基于资源的滑动窗口，采用延迟调度（而非拒绝）、加权轮换的资源池、每日上限和预热斜坡 /// - Reliability — Crash recovery via state snapshots, configurable retry with exponential backoff, dead letter queue, idempotency keys, circuit breakers with fallback handler routing and persistent state // 可靠性 ——通过状态快照实现崩溃恢复，可配置的指数退避重试机制，死信队列，幂等键，带回退处理程序路由的熔断器以及持久状态 /// - Concurrency — Per-entity key control, priority queues (Low/Normal/High/Critical), bulk create/pause/resume/cancel, batch instance creation (up to 10k) // 并发性 ——基于实体的键控制、优先级队列（低/普通/高/紧急）、批量创建/暂停/恢复/取消、批量实例创建（最多 1 万个） /// - Multi-tenancy — Tenant-scoped queries, per-tenant rate limits, per-tenant circuit breakers, tenant isolation middleware, per-tenant noisy-neighbor protection // 多租户架构 ——租户级查询、租户级速率限制、租户级断路器、租户隔离中间件、租户级干扰防护 /// - Extensibility — External workers (any language via REST polling), gRPC sidecar plugins, WASM plugins, webhook events, workflow interceptors, emit-event with deduplication // 可扩展性 ——外部工作进程（可通过 REST 轮询使用任何语言）、gRPC sidecar 插件、WASM 插件、Webhook 事件、工作流拦截器、带去重功能的事件发送 /// - Observability — Prometheus metrics, structured JSON logging, audit log, execution tree visualization, Grafana dashboard template // 可观测性 ——Prometheus 指标、结构化 JSON 日志、审计日志、执行树可视化、Grafana 仪表板模板 /// - AI Agent Support — Unified llm_call handler covering all major providers (OpenAI, Anthropic, Gemini + 7 more), dynamic step injection (self_modify), human-in-the-loop with timeout/escalation, SSE streaming, query-instance handler for cross-workflow coordination // AI 代理支持 ——统一的 llm_call 处理程序，涵盖所有主流提供商（OpenAI、Anthropic、Gemini 及其他 7 家），动态步骤注入（self_modify），带超时/升级机制的人机交互，SSE 流式传输，以及用于跨工作流协调的查询实例处理程序 /// - Security — AES-256-GCM encryption at rest for context and credentials, OAuth2 credential refresh, API key authentication, CORS configuration // 安全性 — 静态上下文和凭据采用 AES-256-GCM 加密，支持 OAuth2 凭据刷新、API 密钥身份验证和 CORS 配置"
[playbook/.site]: https://orch8.io/playbook "Each pattern starts with the problem, explains how Orch8 solves it, walks through what happens step by step, and includes a ready-to-use sequence definition. No prior orchestration knowledge required. // 每个模式都从问题入手，解释 Orch8 如何解决问题，逐步演示其过程，并包含一个可直接使用的序列定义。无需任何编曲基础。"
[site/io]: https://orch8.io/ "Durable workflows and AI agents that survive crashes. // 持久的工作流程和能够从崩溃中恢复的 AI 代理。 /// Define multi-step sequences as JSON — email campaigns, billing retries, onboarding drips, or AI agents that call tools and wait for human review. Orch8 handles scheduling, retries, rate limits, and crash recovery. // 将多步骤序列定义为 JSON 格式——例如电子邮件营销活动、账单重试、新用户引导流程，或调用工具并等待人工审核的 AI 代理。Orch8 负责处理调度、重试、速率限制和崩溃恢复。"
