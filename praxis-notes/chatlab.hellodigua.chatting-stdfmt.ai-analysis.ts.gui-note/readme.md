[src/gh]: https://github.com/hellodigua/ChatLab.git "(AGPL-3.0) (Languages: TypeScript 58.4%, Vue 40.5%, JavaScript 0.6%, Shell 0.3%, CSS 0.2%, HTML 0.0%) Rediscover your social memories with local, AI-powered analysis. // 本地化的聊天记录分析工具，通过 AI Agent 回顾你的社交记忆。 /// ChatLab is an open-source desktop app for understanding your social conversations. It combines a flexible SQL engine with AI agents so you can explore patterns, ask better questions, and extract insights from chat data, all on your own machine. // ChatLab 是一款开源的桌面应用程序，用于理解您的社交对话。它结合了灵活的 SQL 引擎和 AI 代理，让您可以在自己的机器上探索模式、提出更好的问题，并从聊天数据中提取见解。 /// Currently supported: WhatsApp, LINE, WeChat, QQ, Discord, Instagram, and Telegram. Coming next: iMessage, Messenger, and KakaoTalk. // 目前支持：WhatsApp、LINE、WeChat、QQ、Discord、Instagram 和 Telegram。即将支持：iMessage、Messenger 和 KakaoTalk。 /// ## Core Features // 核心功能 /// - 🚀 Built for large histories: Stream parsing and multi-worker processing keep imports and analysis responsive, even at million-message scale. // 🚀 专为大型历史数据设计：流式解析和多工作进程处理确保导入和分析始终响应迅速，即使在百万级消息规模下也是如此。 /// - 🔒 Private by default: Your chat data and settings stay local. No mandatory cloud upload of raw conversations. // 🔒 默认私有：您的聊天数据和设置将保留在本地。无需强制上传原始对话到云端。 /// - 🤖 AI that can actually operate on data: Agent + Function Calling workflows can search, summarize, and analyze chat records with context. // 🤖 能够实际操作数据的 AI：Agent + 函数调用工作流可以结合上下文搜索、总结和分析聊天记录。 /// - 📊 Insight-rich visual views: See trends, time patterns, interaction frequency, rankings, and more in one place. // 📊 富含洞察的可视化视图：在一个地方查看趋势、时间模式、互动频率、排名等更多内容。 /// - 🧩 Cross-platform normalization: Different export formats are mapped into a unified model so you can analyze them consistently. // 🧩 跨平台标准化：不同的导出格式被映射到一个统一的模型中，以便您可以一致地进行分析。 /// ## System Architecture // 系统架构 /// ### Architecture Principles // 架构原则 /// - Local-first by default: Raw chat data, indexes, and settings remain on-device unless you explicitly choose otherwise. // 默认本地优先：原始聊天数据、索引和设置保留在设备上，除非您明确选择其他方式。 /// - Streaming over buffering: Stream-first parsing and incremental processing keep large imports stable and memory-efficient. // 流式处理优于缓冲：流式解析和增量处理使大型导入保持稳定且内存高效。 /// - Composable intelligence: AI features are assembled through Agent + Tool Calling, not hard-coded into one model path. // 组合式智能：AI 功能通过 Agent + 工具调用组合而成，而非硬编码到一个模型路径中。 /// - Schema-first evolution: Import, query, analysis, and visualization share a consistent data model that scales with new features. // 模式优先进化：导入、查询、分析和可视化共享一个一致的数据模型，并随着新功能的加入而扩展。 /// ### Runtime Architecture // 运行时架构 /// - Main Process (control plane): electron/main/index.ts handles lifecycle and windows. electron/main/ipc/ defines domain-scoped IPC, while electron/main/ai/ and electron/main/i18n/ provide shared AI and localization services. // 主进程（控制平面）： electron/main/index.ts 处理生命周期和窗口。 electron/main/ipc/ 定义域范围的 IPC，而 electron/main/ai/ 和 electron/main/i18n/ 提供共享的 AI 和本地化服务。 /// - Worker Layer (compute plane): electron/main/worker/ runs import, indexing, and query tasks via workerManager, keeping CPU-heavy work off the UI thread. // 工作层（计算平面）： electron/main/worker/ 通过 workerManager 运行导入、索引和查询任务，将 CPU 密集型工作从 UI 线程中分离出来。 /// - Renderer Layer (interaction plane): Vue 3 + Nuxt UI + Tailwind CSS drive management, private chat, group chat, and analysis interfaces. electron/preload/index.ts exposes tightly scoped APIs for secure process boundaries. // 渲染层（交互平面）：由 Vue 3 + Nuxt UI + Tailwind CSS 驱动管理，包括私聊、群聊和分析界面。 electron/preload/index.ts 提供紧密范围 API，确保进程边界安全。 /// ### Data Pipeline // 数据管道 /// 1. Ingestion: parser/ detects file format and dispatches to the matching parser module. // 数据摄取： parser/ 检测文件格式，并将其分派到匹配的解析模块。 /// 2. Persistence: Stream-based writes populate core local entities: sessions, members, and messages. // 持久化：基于流的写入会填充核心本地实体：会话、成员和消息。 /// 3. Indexing: Session- and time-oriented indexes are built for timeline navigation and retrieval. // 索引：为时间线导航和检索构建了会话和面向时间的索引。 /// 4. Query & Analysis: worker/query/* powers activity metrics, interaction analysis, SQL Lab, and AI-assisted exploration. // 查询与分析： worker/query/* 提供活动指标、交互分析、SQL 实验室和 AI 辅助探索功能。 /// 5. Presentation: The renderer turns query output into charts, rankings, timelines, and conversational analysis flows. // 展示：渲染器将查询输出转换为图表、排名、时间线和对话分析流程。 /// ### Extensibility & Reliability // 可扩展性与可靠性 /// - Pluggable parser architecture: Adding a new import source is mostly an extension in parser/formats/*, without reworking downstream query logic. // 可插拔的解析器架构：添加新的导入源主要是在 parser/formats/* 中的扩展，无需重新设计下游查询逻辑。 /// - Full + incremental import paths: streamImport.ts and incrementalImport.ts support both first-time onboarding and ongoing updates. // 完整的增量导入路径： streamImport.ts 和 incrementalImport.ts 支持首次设置和持续更新。 /// - Modular IPC boundaries: Domain-based IPC segmentation reduces cross-layer coupling and limits permission spread. // 模块化 IPC 边界：基于域的 IPC 分段减少了跨层耦合，并限制了权限扩散。 /// - Unified i18n evolution: Main and renderer processes share an i18n system that can evolve with product scope. // 统一 i18n 进化：主进程和渲染进程共享一个 i18n 系统，该系统可以随着产品范围的发展而进化。"
[site/fun]: https://chatlab.fun/ "ChatLab /// 重构你的社交记忆 /// 本地化的聊天记录分析工具，通过深度集成的 AI Agent，轻松分析你的聊天记录"
[stdfmt/.docs]: https://chatlab.fun//standard/chatlab-format.html "ChatLab 定义了一套标准的聊天记录数据交换格式，用于支持多平台数据的统一导入和分析。 /// 只要你将聊天记录转为该格式，那么就可以被 ChatLab 解析并使用其分析能力。"
[docs/.site]: https://chatlab.fun//usage/ "ChatLab 专注于对已导出数据的分析，我们不提供抓取数据的功能。您需要先使用官方功能或开源社区的第三方工具，将聊天记录导出后，再导入 ChatLab 进行分析。 /// 注意事项 ///: 使用第三方导出工具时，请务必仔细阅读其官方文档和安全说明。 ///; ChatLab 与以下项目无任何关联，相关链接仅作为技术信息参考提供，不代表本项目认可、担保或安全性背书。用户需自行评估并承担使用第三方工具的所有风险。"
[knowsby]: https://github.com/hicccc77/WeFlow.git "... /// - 支持格式：原始 JSON 或 ChatLab 标准格式"
