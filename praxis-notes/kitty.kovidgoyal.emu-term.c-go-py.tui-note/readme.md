[site/sw.kovidgoyal.net]: https://sw.kovidgoyal.net/kitty/ "kitty // 猫猫 /// If you live in the terminal, kitty is made for YOU! // 如果你生活在终端里，kitty 就是为你而生的！ /// The fast, feature-rich, GPU based terminal emulator. // 快速、功能丰富、基于 GPU 的终端模拟器。 /// - Uses GPU and SIMD vector CPU instructions for best in class performance // 使用 GPU 和 SIMD 向量 CPU 指令，实现顶级性能 /// - Uses threaded rendering for absolutely minimal latency // 采用线程渲染，延迟降至最低 /// - Performance tradeoffs can be tuned // 性能权衡可以调整 /// Design philosophy // 设计理念 ¶ /// kitty is designed for power keyboard users. To that end all its controls work with the keyboard (although it fully supports mouse interactions as well). Its configuration is a simple, human editable, single file for easy reproducibility (I like to store configuration in source control). // kitty 是为强大的键盘用户设计的。为此，它的所有控制功能都与键盘兼容（尽管它也完全支持鼠标交互）。它的配置是一个简单、可由人类编辑的单文件，以便于重现（我喜欢将配置存储在源代码管理中）。 /// The code in kitty is designed to be simple, modular and hackable. It is written in a mix of C (for performance sensitive parts), Python (for easy extensibility and flexibility of the UI) and Go (for the command line kittens). It does not depend on any large and complex UI toolkit, using only OpenGL for rendering everything. // kitty 中的代码设计得简单、模块化且易于扩展。它使用 C 语言（用于性能敏感的部分）、Python（用于易于扩展和 UI 的灵活性）和 Go 语言（用于命令行小猫）。它不依赖于任何大型复杂的 UI 工具包，仅使用 OpenGL 来渲染所有内容。 /// Finally, kitty is designed from the ground up to support all modern terminal features, such as Unicode, true color, bold/italic fonts, text formatting, etc. It even extends existing text formatting escape codes, to add support for features not available elsewhere, such as colored and styled (curly) underlines. One of the design goals of kitty is to be easily extensible so that new features can be added in the future with relatively little effort. // 最后，kitty 从底层设计就支持所有现代终端功能，如 Unicode、真实色彩、粗体/斜体字体、文本格式化等。它甚至扩展了现有的文本格式化转义码，以支持其他地方不可用的功能，例如彩色和样式化的（曲线）下划线。kitty 的设计目标之一是易于扩展，以便将来可以相对轻松地添加新功能。"
[src/gh]: https://github.com/kovidgoyal/kitty.git "(GPL-3.0) (Languages: Python 38.9%, C 28.2%, Go 25.7%, Objective-C 5.4%, Shell 1.2%, GLSL 0.6%) If you live in the terminal, kitty is made for you! Cross-platform, fast, feature-rich, GPU based. // 如果你生活在终端中，kitty 就是为你而生的！跨平台、快速、功能丰富、基于 GPU。"
[repology]: https://repology.org/project/kitty-terminal/

