[src/gh]: https://github.com/junkurihara/rust-rpxy.git "(MIT) (Languages: Rust 94.2%, Shell 4.8%, Dockerfile 1.0%)  A simple and ultrafast http reverse proxy serving multiple domain names and terminating TLS for http/1.1, 2 and 3, written in Rust // 一个用 Rust 编写的简单且超高速的 http 反向代理，支持多个域名，并为 http/1.1、2 和 3 终止 TLS 。"
[site]: https://rpxy.io/ "- Blazingly Fast // 迅猛快速 /// Thanks to Rust and libraries built on top of it, rpxy serves HTTP messages blazing fast. In fact, rpxy outperforms other popular reverse-proxies in terms of the speed. // 得益于 Rust 以及基于 Rust 构建的库， rpxy 能够以迅猛的速度处理 HTTP 消息。事实上， rpxy 在速度方面优于其他流行的反向代理。 /// - Multiple TLS Termination // 支持多 TLS 终端 /// You can serve multiple domain names with a single instance of rpxy. rpxy routes multiple names to appropriate backend application servers while serving TLS connections. // 您可以使用 rpxy 的单个实例来服务多个域名。 rpxy 能够在处理 TLS 连接的同时，将多个域名路由到适当的后端应用服务器。 /// - Post-Quantum Secure TLS // 后量子安全 TLS /// Future-proof your security with hybrid post-quantum cryptography. rpxy supports X25519MLKEM768 for TLS 1.3 and QUIC by default, protecting against quantum computing threats without compromising performance or requiring special configuration. // 通过混合量子后密码学保护您的安全。 rpxy 默认支持 X25519MLKEM768 用于 TLS 1.3 和 QUIC，在不影响性能或无需特殊配置的情况下，抵御量子计算威胁。 /// - ACME Out of the Box // 开箱即用的 ACME /// rpxy supports ACME protocol out of the box. You can serve your domain names with automatic certificate issuance and renewal only by opening a port for TLS. // rpxy 开箱即支持 ACME 协议。只需打开 TLS 端口，即可通过自动证书颁发和续期来服务您的域名。 /// - HTTP/3 Support // HTTP/3 支持 /// rpxy supports HTTP/3, the next generation of the HTTP protocol based on QUIC. You can serve your domain names with the latest protocol. // rpxy 支持 HTTP/3，这是基于 QUIC 协议的下一代 HTTP 协议。您可以使用最新协议来服务您的域名。 /// - Simple Configuration // 简单配置 /// rpxy is designed to be simple and easy to configure. You can serve your domain names with just a few lines of configuration in TOML format. // rpxy 设计得简单易用，只需几行 TOML 格式的配置即可服务您的域名。 /// - And Much More... // 还有更多... /// TLS sanitization, caching, load balancing, and more. rpxy is designed to be extensible and flexible for your needs. // TLS 清理、缓存、负载均衡等。 rpxy 旨在为您的需求提供可扩展和灵活的解决方案。"

[pkg.oci/dockerhub]: https://hub.docker.com/r/jqtype/rpxy "(: docker pull -- jqtype/rpxy:nightly-s2n)"
[pkg.oci/ghcr]: https://github.com/junkurihara/rust-rpxy/pkgs/container/rust-rpxy "(: docker pull -- ghcr.io/junkurihara/rust-rpxy:nightly-s2n)"

[site/gamerboy59]: https://rpxy.gamerboy59.dev/
[pkg.bin/debian/gamerboy59]: https://repo.gamerboy59.dev/debian/ "(: sudo apt-get install -y -- rpxy rpxy-webui)"
[pkg.bin/rpm/gamerboy59]: https://repo.gamerboy59.dev/debian/ "(: sudo dnf install -y -- rpxy rpxy-webui)"

[webui.src/gamerboy59.gh]: https://github.com/Gamerboy59/rpxy-webui.git "(GPL-3.0) (Languages: PHP 47.1%, Blade 46.3%, HTML 5.8%, Dockerfile 0.3%, Shell 0.3%, JavaScript 0.1%, SCSS 0.1%) A Management Webinterface for rust-rpxy // rust-rpxy 的管理网页界面"
