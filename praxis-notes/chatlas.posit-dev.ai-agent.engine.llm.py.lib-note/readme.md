[src/gh]: https://github.com/posit-dev/chatlas.git "(MIT) (Languages: Python 99.7%, Makefile 0.3%) Your friendly guide to building LLM chat apps in Python with less effort and more clarity. // 在 Python 中以更少的工作量和更高的清晰度构建 LLM 聊天应用的友好指南。 /// (: import chatlas,os # you may need `os.getenv` ;: chat = chatlas.ChatGroq(base_url = os.getenv('XXX_BASE_URL'), api_key = os.getenv('XXX_API_KEY'), model = os.getenv('XXX_MODEL')) # create chat obj by things recorded in sys-env ;: chat.chat('Hi') # user turn ;: chat.register_tool(<fn-defed>) # register tool by a Python function - fn-defed: a defined function with its good doc-comments: Descriptions & Parameters)"
[lib.pip/pypi]: https://pypi.org/project/chatlas/ "(: pip install -- chatlas) (License Expression: MIT) (Requires: Python >=3.9) A simple and consistent interface for chatting with LLMs // 一个简单且一致的与 LLMs 聊天的接口 (src: gh:posit-dev/chatlas.git)"
[site/ghio]: https://posit-dev.github.io/chatlas/ "Get started in 3 simple steps: // 在 3 个简单步骤中开始使用： /// - Choose a model provider, such as ChatOpenAI or ChatAnthropic. // 选择一个模型提供者，例如 ChatOpenAI 或 ChatAnthropic。 /// - Visit the provider’s reference page to get setup with necessary credentials. // 访问提供者的参考页面，使用必要的凭证进行设置。 /// - Create the relevant Chat client and start chatting! // 创建相关的 Chat 客户端并开始聊天！"
[knows_by]: https://github.com/tidyverse/ellmer.git "ellmer is one of a number of LLM-related packages created by Posit: /// ... /// - Looking for something similar in python? Check out chatlas!"

