[src/gh]: https://github.com/rstudio/chromote.git "(MIT) (Languages: R 99.9%, SCSS 0.1%) Chrome Remote Interface for R // R 的 Chrome 远程接口 /// Chromote is an R implementation of the Chrome DevTools Protocol. It works with Chrome, Chromium, Opera, Vivaldi, and other browsers based on Chromium. By default it uses Google Chrome (which must already be installed on the system). To use a different browser, see vignette('which-chrome'). // Chromote 是 Chrome DevTools 协议的 R 实现。它可以与 Chrome、Chromium、Opera、Vivaldi 以及其他基于 Chromium 的浏览器一起使用。默认情况下，它使用 Google Chrome（该浏览器必须在系统上已安装）。要使用不同的浏览器，请参阅 vignette('which-chrome') 。 /// Chromote is not the only R package that implements the Chrome DevTools Protocol. Here are some others: // Chromote 不是唯一实现 Chrome DevTools 协议的 R 包。以下是一些其他的： /// - crrri by Romain Lesur and Christophe Dervieux // crrri 由 Romain Lesur 和 Christophe Dervieux 提供 /// - decapitated by Bob Rudis // 被 Bob Rudis 移除 /// - chradle by Miles McBain // 由 Miles McBain 创建 /// The interface to Chromote is similar to chrome-remote-interface for node.js. // Chromote 的接口与 node.js 的 chrome-remote-interface 类似。 /// Features // 特性 /// - Install and use specific versions of Chrome from the Chrome for Testing service. // 通过 Chrome for Testing 服务安装和使用特定版本的 Chrome。 /// - Offers a synchronous API for ease of use and an asynchronous API for more sophisticated tasks. // 提供一个同步 API 以便于使用，并提供异步 API 以处理更复杂的任务。 /// - Full support for the Chrome DevTools Protocol for any version of Chrome or any Chrome-based browser. // 完全支持 Chrome DevTools 协议，适用于任何版本的 Chrome 或任何基于 Chrome 的浏览器。 /// - Includes convenience methods, like $screenshot() and $set_viewport_size(), for common tasks. // 包含用于常见任务的便捷方法，如 $screenshot() 和 $set_viewport_size() 。 /// - Automatically reconnects to previous sessions if the connection from R to Chrome is lost, for example when restarting from sleep state. // 自动在 R 到 Chrome 的连接丢失时（例如从睡眠状态重启时）重新连接到之前的会话。 /// - Powers many higher-level packages and functions, like {shinytest2} and rvest::read_html_live(). // 为许多高级包和函数提供支持，例如 {shinytest2} 和 rvest::read_html_live() 。"
[package/cran]: https://cran.r-project.org/web/packages/chromote/ "(Version: 	0.5.1) (Imports: 	cli, curl, fastmap, jsonlite, later (≥ 1.1.0), magrittr, processx, promises (≥ 1.1.1), R6, rlang (≥ 1.1.0), utils, websocket (≥ 1.2.0), withr, zip) (Suggests: 	knitr, rmarkdown, showimage, testthat (≥ 3.0.0)) (Published: 	2025-04-24) (DOI: 	10.32614/CRAN.package.chromote) (Author: 	Garrick Aden-Buie ORCID iD [aut, cre], Winston Chang [aut], Barret Schloerke ORCID iD [aut], Posit Software, PBC ROR ID [cph, fnd]) (Maintainer: 	Garrick Aden-Buie <garrick at posit.co>) (License: 	MIT + file LICENSE) (NeedsCompilation: 	no) (SystemRequirements: 	Google Chrome or other Chromium-based browser. chromium: chromium (rpm) or chromium-browser (deb)) (Language: 	en-US) chromote: Headless Chrome Web Browser Interface // chromote: 无头 Chrome 浏览器接口 /// An implementation of the 'Chrome DevTools Protocol', for controlling a headless Chrome web browser. // 'Chrome DevTools Protocol' 的实现，用于控制无头 Chrome 浏览器。 (src: gh:rstudio/chromote.git)"
[site/ghio]: https://rstudio.github.io/chromote/ "chromote /// (: b = chromote::ChromoteSession$new() ;: b$view() # This will start a headless browser and open an interactive viewer for it in a normal browser, so that you can see what the headless browser is doing. // 这将启动一个无头浏览器，并在普通浏览器中为其打开一个交互式查看器，以便您可以看到无头浏览器正在做什么。)"
[knowsby]: https://cran.r-project.org/web/packages/btw/ "... (Suggests: 	bslib (≥ 0.7.0), chromote, DBI, duckdb, gert, gh, htmltools, pandoc, shiny, shinychat (≥ 0.2.0), testthat (≥ 3.0.0), usethis) ..."

