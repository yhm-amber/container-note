[site]: https://taxy.dev/ " Taxy: Effortless Web Proxy // Taxy: 轻松的网页代理 /// A reverse proxy server with built-in WebUI. // 一个带有内置 WebUI 的反向代理服务器。 /// Key Features // 关键特性 /// - Built with Rust for optimal performance and safety, powered by tokio and hyper // 使用 Rust 构建，以实现最佳性能和安全性，由 tokio 和 hyper 支持 /// - Supports TCP, UDP, TLS, HTTP1, and HTTP2, including HTTP upgrading and WebSocket functionality // 支持 TCP、UDP、TLS、HTTP1 和 HTTP2，包括 HTTP 升级和 WebSocket 功能 /// - Partial HTTP/3 support (incoming QUIC connections only; WebTransport not supported) // 部分 HTTP/3 支持（仅限入站 QUIC 连接；不支持 WebTransport） /// - Easily deployable single binary with a built-in WebUI // 易于部署的单个二进制文件，内置 WebUI /// - Allows live configuration updates via a REST API without restarting the service // 可通过 REST API 进行实时配置更新，无需重启服务 /// - Imports TLS certificates from the GUI or can generate a self-signed certificate // 从图形界面导入 TLS 证书，或可生成自签名证书 /// - Provides Let's Encrypt support (ACME v2, HTTP challenge only) for seamless certificate provisioning // 提供 Let's Encrypt 支持（ACME v2，仅 HTTP 挑战）以实现无缝证书配置"
[src/gh]: https://github.com/picoHz/taxy.git "(MIT) (Languages: Rust 99.4%, Other 0.6%) A reverse proxy server with built-in WebUI, supporting TCP/UDP/HTTP/TLS/WebSocket, written in Rust. // 一个内置 Web UI 的反向代理服务器，支持 TCP/UDP/HTTP/TLS/WebSocket，使用 Rust 编写。"

[demo/site]: https://demo.taxy.dev/ "(: intro: https://taxy.dev/demo/)"
[pkg.oci/ghcr]: https://github.com/picoHz/taxy/pkgs/container/taxy "(: docker run -d -v taxy-config:/root/.config/taxy -p 80:80 -p 443:443 -p 127.0.0.1:46492:46492 --restart unless-stopped --name taxy -- ghcr.io/picohz/taxy:latest)"
[pkg.cargo/crates]: https://crates.io/crates/taxy "(: cargo install -- taxy) (: cargo binstall -- taxy)"
