[core.src/gh]: https://github.com/HelixDB/helix-db.git "(AGPL-3.0) (Languages: Rust 93.8%, HelixQL 5.5%, Other 0.7%) HelixDB is an open-source graph-vector database built from scratch in Rust. // HelixDB 是一个用 Rust 从头构建的开源图向量数据库。 /// HelixDB is a database that makes it easy to build all the components needed for an AI application in a single platform. // HelixDB 是一个数据库，它在一个平台上简化了构建 AI 应用所需的所有组件。 /// You no longer need a separate application DB, vector DB, graph DB, or application layers to manage the multiple storage locations to build the backend of any application that uses AI, agents or RAG. Just use Helix. // 您不再需要单独的应用程序数据库、向量数据库、图数据库或应用程序层来管理多个存储位置，以构建使用 AI、代理或 RAG 的任何应用程序的后端。只需使用 Helix 即可。 /// HelixDB primarily operates with a graph + vector data model, but it can also support KV, documents, and relational data. // HelixDB 主要采用图 + 向量数据模型，但它也可以支持 KV、文档和关系型数据。 /// Key Features // 主要特性 /// - Built-in MCP tools // 内置 MCP 工具 ///: Helix has built-in MCP support to allow your agents to discover data and walk the graph rather than generating human readable queries. // Helix 提供内置的 MCP 支持，允许您的代理发现数据并遍历图，而不是生成人类可读的查询。 /// - Built-in Embeddings // 内置语义向量嵌入 ///: No need to embed your data before sending it to Helix, just use the Embed function to vectorize text. // 无需在将数据发送给 Helix 之前嵌入数据，只需使用 Embed 函数将文本向量化。 /// - Tooling for RAG // RAG 工具 ///: HelixDB has a built-in vector search, keyword search, and graph traversals that can be used to power any type of RAG applications. // HelixDB 具有内置的向量搜索、关键词搜索和图遍历功能，可用于支持任何类型的 RAG 应用程序。 /// - Secure by Default // 默认安全 ///: HelixDB is private by default. You can only access your data through your compiled HelixQL queries. // HelixDB 默认为私有。您只能通过编译后的 HelixQL 查询访问您的数据。 /// - Ultra-Low Latency // 超低延迟 ///: Helix is built in Rust and uses LMDB as its storage engine to provide extremely low latencies. // Helix 是用 Rust 构建的，并使用 LMDB 作为其存储引擎，以提供极低的延迟。 /// - Type-Safe Queries // 类型安全的查询 ///: HelixQL is 100% type-safe, which lets you develop and deploy with the confidence that your queries will execute in production // HelixQL 是 100% 类型安全的，这让你在开发和部署时能够自信地确保你的查询在生产环境中能够执行"
[lib-py.src/gh]: https://github.com/HelixDB/helix-py.git "(MIT, Apacke-2.0) (Languages: Python 99.1%, HelixQL 0.9%) helix-db python lib + workflows // HelixDB Python 库 + 工作流 /// (: import helix ;: db = helix.Client(local=True, verbose=True, ...) ;: db.query('add_user', {'name': 'A', 'age': 666}) # To setup a simple Client to interface with a running helix instance)"
[lib-py.pip/pypi]: https://pypi.org/project/helix-py/ "(: pip install -- helix-py) (Requires: Python >=3.10)  (: pip install -- helix-py[loader] # Data loading (vectors, parquet) // 数据加载 (vectors, parquet)) (: pip install -- helix-py[chunking] # Text chunking utilities // 文本分块工具) (: pip install -- helix-py[pdf] # PDF parsing support // PDF 解析支持) (: pip install -- helix-py[mcp] # MCP server tools (graph traversal & search) // MCP 服务器工具（图遍历与搜索）) (: pip install -- helix-py[embed-gemini] # Embedders (openai, gemini, voyageai) // 向量化映射器 (openai, gemini, voyageai)) (: pip install -- helix-py[provider-gemini] # LLM providers (openai, gemini, anthropic) // LLM 供应者 (openai, gemini, anthropic)) (: pip install -- helix-py[embedders] # All embedders // 所有向量化映射器) (: pip install -- helix-py[providers] # All providers // 所有 LLM 供应者) (: pip install -- helix-py[all] # Everything // 全部) HelixDB Python SDK (src: gh:HelixDB/helix-py.git)"
[lib-rs.src/gh]: https://github.com/HelixDB/helix-rs.git "(Apacke-2.0, MIT) (Languages: Rust 96.9%, HelixQL 3.1%) Rust SDK to use HelixDB // 用于 HelixDB 的 Rust SDK /// helix-rs /// A Rust SDK for interacting with HelixDB - providing a simple, type-safe interface for database operations. // 一个用于与 HelixDB 交互的 Rust SDK - 提供一个简单、类型安全的数据库操作接口。 /// Features // 特性 /// - Type-safe query interface using Serde serialization/deserialization // 使用 Serde 序列化/反序列化的类型安全查询接口 /// - Async/await support // 支持异步/等待 /// - Configurable port settings // 可配置的端口设置 /// - Custom client implementation support via HelixDBClient trait // 通过 HelixDBClient 特性支持自定义客户端实现"
[lib-rs.cargo/crates]: https://crates.io/crates/helix-rs "(: cargo add -- helix-rs) (AGPL-3.0) (137 SLoC) (20 KiB) Rust SDK for HelixDB (src: gh:HelixDB/helix-rs.git)"
[lib-ts.src/gh]: https://github.com/HelixDB/helix-ts.git "(MIT, Apache-2.0) (Languages: TypeScript 100%) TypeScript Library for working with HelixDB // 用于与 HelixDB 一起工作的 TypeScript 库 /// (: import HelixDB from 'helix-ts' ;: const client = new HelixDB('http://localhost:6969') // Create a new HelixDB client by api-url or local port 6969 ;: await client.query('addUser', { name: 'John', age: 20 }) ;: const user = await client.query('getUser', { name: 'John' }) ;: console.log(user) // print)"
[lib-ts.npm/npmjs]: https://npmjs.com/package/helix-ts "(: npm i -- helix-ts) (License: MIT) (Unpacked Size: 10.1 kB) (Total Files: 9) TypeScript Library for working with HelixDB (src: gh:HelixDB/helix-ts.git)"
[lib-go.src/gh]: https://github.com/HelixDB/helix-go.git "(MIT, Apache-2.0) (Languages: Go 92.5%, HelixQL 7.5%) The official Go SDK for HelixDB"
[lib-go.go/pkg.go.dev]: https://pkg.go.dev/github.com/HelixDB/helix-go "(: go get -- github.com/HelixDB/helix-go) (License: Apache-2.0, MIT) (Imports: 8) (Imported by: 0)  (src: gh:HelixDB/helix-go.git)"
[site]: https://helix-db.com/ "Feed your agents more relevant context with the fastest and most scalable graph-vector database, built in Rust. // 用最快的、可扩展性最强的图-向量数据库为您的代理提供更相关的上下文，该数据库是用 Rust 构建的。 /// - Hybrid Query Traversals // 混合查询遍历 ///: Seamlessly combine vector similarity search with graph traversals in a single, powerful query. No more complex joins or multiple database calls. // 在一个强大的查询中无缝结合向量相似性搜索与图遍历。不再需要复杂的连接或多重数据库调用。 /// - Type-Safety // 类型安全 ///: Advanced static analysis provides real-time feedback, autocomplete, and error detection. Write queries with confidence. // 高级静态分析提供实时反馈、自动补全和错误检测。自信地编写查询。 (: helix check) /// - Lower Costs // 降低成本 ///: Eliminate the complexity and cost of maintaining separate vector and graph databases. One unified solution. // 消除维护独立向量数据库和图数据库的复杂性和成本。一个统一的解决方案。 /// - High Speeds // 高速 ///: Optimized for both vector similarity and graph traversal workloads with industry-leading performance metrics. // 针对向量相似性和图遍历工作负载进行了优化，具有行业领先的性能指标。"
[docs/.site]: https://docs.helix-db.com/ "A high-performance graph-vector database with its own query language designed for traversing and manipulating graph and vector data efficiently. // 一款高性能图向量数据库，拥有自己的查询语言，专为高效遍历和操作图与向量数据而设计。 /// How does HelixDB store data? // HelixDB 如何存储数据？ /// HelixDB uses a property graph model with three core components: // HelixDB 使用一个具有三个核心组件的属性图模型： /// - Nodes: Represent entities with unique identifiers and properties // 节点：表示具有唯一标识符和属性的实体 ///: (: N::NodeType { field1: String, field2: U32 }) /// - Vectors: Represent entities with unique identifiers and vector embeddings // 向量：表示具有唯一标识符和向量嵌入的实体 ///: (: V::VectorType { field1: String, field2: U32 }) /// - Edges: Represent relationships between nodes and/or vectors, with directionality and properties // 边：表示节点之间和/或向量之间的关系，具有方向性和属性 ///: (: E::EdgeType { From: String, To: String, Properties: { field1: String, field2: U32 } }) /// This model allows for rich representation of complex domains while maintaining query performance. // 该模型能够在保持查询性能的同时，对复杂领域进行丰富的表示。 /// What is a Graph Database? // 什么是图数据库？ /// Learn how graph databases store and connect data using nodes and relationships // 学习图数据库如何使用节点和关系存储和连接数据 /// A graph database is a type of database that stores data using a graph structure, where information is represented through nodes (data points) and edges (relationships between nodes). Unlike traditional databases that use tables or documents, graph databases excel at managing highly connected data and complex relationships. // 图数据库是一种使用图结构存储数据的数据库类型，其中信息通过节点（数据点）和边（节点之间的关系）来表示。与传统数据库使用表格或文档不同，图数据库擅长管理高度连接的数据和复杂关系。 /// How do graph databases work? // 图数据库是如何工作的？ /// Graph databases store data using two fundamental concepts: // 图数据库使用两个基本概念来存储数据： /// - Nodes - These are the entities or objects in your data (like people, products, or locations) // 节点 - 这些是你的数据中的实体或对象（如人、产品或地点） /// - Edges (or relationships) - These are the connections between nodes that describe how they relate to each other // 边（或关系）- 这些是节点之间的连接，描述它们如何相互关联"
[docs:hql/.site]: https://docs.helix-db.com/documentation/hql/ "What is HelixQL? // 什么是 HelixQL？ /// Learn about HelixQL, a strongly typed compiled query language for graph and vector operations in HelixDB // 了解 HelixQL，这是一种用于 HelixDB 中图和向量操作的强类型编译查询语言 /// HelixQL is a strongly typed, compiled query language for HelixDB that combines the best features of Gremlin, Cypher, and Rust to provide type-safe graph and vector queries. // HelixQL 是一种用于 HelixDB 的强类型编译查询语言，它结合了 Gremlin、Cypher 和 Rust 的最佳特性，提供类型安全的图和向量查询。 /// Why use HelixQL instead of other query languages? // 为什么使用 HelixQL 而不是其他查询语言？ /// HelixQL addresses key limitations found in existing graph query languages: // HelixQL 解决了现有图查询语言中的关键限制： /// - Type safety: Queries are validated at compile time, catching errors before runtime // 类型安全：查询在编译时进行验证，在运行前捕获错误 /// - Readability: Clean, concise syntax that remains readable even for complex queries // 可读性：简洁的语法即使在复杂的查询中也保持可读 /// - Performance: Compiled queries execute faster than dynamically parsed alternatives // 性能：编译查询比动态解析的替代方案执行更快 /// - Developer experience: IDE support with autocomplete and type checking // 开发者体验：支持自动完成和类型检查的 IDE /// Unlike Gremlin’s verbose syntax or Cypher’s runtime parsing, HelixQL provides a modern, type-safe approach to graph querying. // 与 Gremlin 冗长的语法或 Cypher 的运行时解析不同，HelixQL 提供了一种现代、类型安全的图查询方法。"

