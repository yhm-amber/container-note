[cli.src/gh]: https://github.com/gojue/ecapture.git "(Apache-2.0) (Languages: C 94.5%, Go 5.0%, Shell 0.3%, Makefile 0.2%, C++ 0.0%, Lua 0.0%) Capturing SSL/TLS plaintext without a CA certificate using eBPF. Supported on Linux/Android kernels for amd64/arm64. // 使用 eBPF 在不使用 CA 证书的情况下捕获 SSL/TLS 明文。支持 amd64/arm64 的 Linux/Android 内核。"
[gui.src/gh]: https://github.com/gojue/ecaptureQ.git "(Languages: TypeScript 46.4%, Rust 45.3%, CSS 5.7%, JavaScript 1.4%, Other 1.2%) A cross-platform GUI for ecapture built with Tauri, enabling eBPF-based, non-intrusive TLS inspection on Linux & Android, with remote clients for Windows, macOS, and Linux. // 一个使用 Tauri 构建的跨平台 GUI，用于 ecapture，支持基于 eBPF 的非侵入式 TLS 检查，适用于 Linux & Android，并提供 Windows、macOS 和 Linux 的远程客户端。 /// eCaptureQ is a cross-platform GUI for eCapture, visualizing its eBPF-powered packet capturing capabilities: capture TLS plaintext at the kernel level without needing a CA certificate or MITM. // eCaptureQ 是一个跨平台的图形界面，用于 eCapture，可视化其基于 eBPF 的数据包捕获功能：在内核级别捕获 TLS 明文，无需 CA 证书或中间人。 /// This means you can debug and analyze the encrypted communications of any program in a simpler, more efficient, and non-intrusive way. // 这意味着您可以更简单、更高效、更非侵入式地调试和分析任何程序的加密通信。"
[site/cc]: https://ecapture.cc/ "Capturing SSLKEYLOG and plaintext communications from OpenSSL and GnuTLS. Support HTTP/3 QUIC, IPv6, TLS 1.3 etc. // 捕获 OpenSSL 和 GnuTLS 的 SSLKEYLOG 和明文通信。支持 HTTP/3 QUIC、IPv6、TLS 1.3 等。"
