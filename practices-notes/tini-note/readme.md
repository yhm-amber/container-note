[src/gh]: https://github.com/krallin/tini.git "A tiny but valid `init` for containers"

[alpine]: https://pkgs.alpinelinux.org/packages?name=tini "RUN apk add --no-cache -- tini :: https://pkgs.alpinelinux.org/package/edge/community/{riscv64,aarch64,x86_64,armhf,s390x,ppc64le,x86,armv7}/tini"
[debian]: https://tracker.debian.org/pkg/tini "apt-get install -- tini"

[docker-flag]: https://docs.docker.com/engine/reference/commandline/run "docker ^1.13.x: To enable Tini, just pass the `--init` flag to `docker run`."

[usages/dockerhub]: https://hub.docker.com/u/krallin "cadvisor,elasticsearch,gentlemanjerry,webhooks-training-app,ubuntu-tini,centos-tini"
