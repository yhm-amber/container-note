🦝 `targeto` - Tar Gettor as OCI 🦝
🦾 likely by the tool set `flan-door` 🦾

: Install (once)

eval "
    $(docker run --rm -- targeto cat src.sh)
    $(docker run --rm -- targeto installer targeto tar zstd)
    : "


: Usage
docker run --rm -- targeto cat readme
