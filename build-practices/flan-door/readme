
: build by

podman build -t flan-door -f Dockerfile -- .





: show readme

podman run --rm -- flan-door helper
podman run --rm -- flan-door helper | less




: list toys

podman run --rm -- flan-door lister
podman run --rm -- flan-door lister metas dateinner
podman run --rm -- flan-door metas lister




: show toys define

podman run --rm -- flan-door helper metas dateinner
podman run --rm -- flan-door metas helper ask_user historisch







: use before installed

cat | podman run --rm -i -- flan-door proptools t XXX=xxx ...
cat | podman run --rm -i -- flan-door proptools t XXX=xxx ... | podman run --rm -i -- flan-door proptools d -D " "
cat | podman run --rm -i -- flan-door proptools d -D " " | podman run --rm -i -- flan-door proptools t XXX=xxx ...







: install some toys into an env by installer, such as proptools and metas

eval "$(podman run --rm -- ghcr.io/yhm-amber/flan-door:latest inataller ghcr.io/yhm-amber/flan-door:latest metas proptools)"



: use if installed by installer

cat | proptools t XXX=xxx ...
cat | proptools t XXX=xxx ... | proptools d -D " "
cat | proptools d -D " " | proptools t XXX=xxx ...
metas helper ask_user





: install-out all into an env

eval "$(podman run --rm -- flan-door cat src.sh)"



: use if install-outed

cat | (proptools t XXX=xxx ...)
cat | (proptools t XXX=xxx ...) | (proptools d -D " ")
cat | (proptools d -D " ") | (proptools t XXX=xxx ...)
(metas helper ask_user)







: other

还有别的需要就根据上头的提示自己举一反三吧。应该也够了。。。

